{"id":"f3d-01-upstream-pin-and-census-6uuv","title":"Pin Asupersync 0.5 browser runtime and preserve foundation probes","description":"Replace the nonportable developer-only upstream Cargo path with published Asupersync 0.5.0 after checking all used browser host/pump/task APIs. Preserve browser feature and binding versions, update development-linkage documentation, resolve lock via Cargo. Verify native real-future and burst-counter tests plus wasm32 browser compilation via RCH. Browser execution and device performance remain separate unverified evidence; do not claim fresh Safari/Chrome results.","status":"closed","priority":1,"issue_type":"task","assignee":"CopperOak","created_at":"2026-09-12T17:21:13.348245935Z","created_by":"ubuntu","updated_at":"2026-09-12T18:55:56.952543440Z","closed_at":"2026-09-12T18:55:56.952324753Z","close_reason":"Published Asupersync 0.5.0 pin, checksum lock and portable docs complete; strict RCH wasm check, native 2/2 probes and all-target Clippy PASS. No fresh browser/performance claim. UBS attempted only the four owned manifest/lock/docs files and returned environment exit2 with zero supported Rust source files; not reported as a scan pass.","source_repo":"franken_threed","source_repo_path":"/data/projects/franken_threed","compaction_level":0,"original_size":0,"comments":[{"id":1,"issue_id":"f3d-01-upstream-pin-and-census-6uuv","author":"ubuntu","text":"Published Asup0.5 source contains every used BrowserHostServices/worker pump API, burstlimit/reentry/turncounter, local joins and child regions. Binding family remains exact0.2.126/0.3.103/0.4.76; features unchanged. Cargo produced registry lock replacing four local0.4.11 family identities. cargo update -p asupersync first refused because the old source identity disappeared; generate-lockfile succeeded, unrelated smallvec patch returned to original1.16.0 via Cargo. Locked nodeps metadata and diffcheck pass. Full fmt exposes pre-existing core/runtime formatting drift; no Rust source edits. Native and wasm compilation queued behind current RCH lanes; historical browser results not relabeled.","created_at":"2026-09-12T17:23:30Z"},{"id":2,"issue_id":"f3d-01-upstream-pin-and-census-6uuv","author":"ubuntu","text":"Strict RCH wasm32 browser-feature check PASS17:59:26Z after additive pinned nightly wasm target installation on worker vmi1156319. Native real-local-future and burst-limit tests2/2PASS18:26:43Z. Logs /data/tmp/asupersync-consumer-audit-20260912-r1/f3d-wasm-check-r3.log and f3d-native-probes-r1.log (verify exact wasm log filename before final). Runtime source unchanged; only registry0.5 pin/lock and current-source docs changed. Browser execution/performance evidence remains old and is not refreshed by these checks. Native strict Clippy pending.","created_at":"2026-09-12T18:30:23Z"},{"id":3,"issue_id":"f3d-01-upstream-pin-and-census-6uuv","author":"ubuntu","text":"Correction to prior comment: wasm/browser check log is /data/tmp/asupersync-consumer-audit-20260912-r1/f3d-browser-check-r3.log, not f3d-wasm-check-r3.log. Native probe log unchanged. These are scoped RCH Cargo probes allowed by AGENTS923-924; no DSR release/browser execution claim.","created_at":"2026-09-12T18:31:00Z"},{"id":4,"issue_id":"f3d-01-upstream-pin-and-census-6uuv","author":"ubuntu","text":"All required migration probes completed through strict remote RCH: wasm32/browser check PASS (f3d-browser-check-r3.log), native real-future and burst-counter tests 2/2 PASS (f3d-native-probes-r1.log), and native runtime all-target Clippy -D warnings PASS18:37:32Z (f3d-native-clippy-r1.log), logs under /data/tmp/asupersync-consumer-audit-20260912-r1. Runtime Rust/binding versions/features unchanged. Only portable registry pin, checksum lock and current-source documentation change. Owned diff check PASS. Existing unrelated full-workspace formatting drift is not changed; historical Chrome/Safari results remain historical, no fresh browser/device/performance or release claim.","created_at":"2026-09-12T18:55:15Z"}]}
{"id":"f3d-01-upstream-pin-and-census-gl8","title":"F3D-01: Pin complete upstream/package; census every feature, registration, import, workflow and test","description":"## What this epic delivers\n\nThe immutable foundation of every later compatibility, ownership, and performance claim: a pinned Three.js r186 oracle, a pinned toolchain/dependency set, and the **single exhaustive feature manifest** (`compatibility/three-r186.json`) that records one entry per importable symbol and behavior family in the pinned release. Plan §19 lists this as F3D-01 with no dependencies; its acceptance evidence is \"full source-to-feature reconciliation; unknown entries block; actual host/dependency matrix\".\n\n## Why it exists (plan §2.2, §5.11, §5.15, §14.1, §18 Phase 0, §21)\n\n- The project promises **all existing features and functionality of r186**, not a convenient subset. That promise is meaningless until the complete surface is enumerated from the actual source tree, package exports, AST class/prototype surfaces, registration code (glTF extensions, nodes, codecs), editor/devtools/TSL tools, and official examples. A finite list in the plan document is explicitly *not* authoritative; the source is.\n- The tag-object hash (`819fadd6...`) is **not** the source commit. The compatibility anchor is the commit `148ef33ecb6d2502ff796d4554abd1549c95d519`. Confusing them corrupts every later parity claim.\n- The plan's release rule keeps import/symbol closure, validated behavior families, new-backend rendering coverage, exact-component integration, and capable-host validation as **separate** numbers. The manifest is where those numbers live. It is one generated JSON data file inside existing tooling, never a service, dashboard, or policy framework (AGENTS.md RULE 0.1).\n- Section 21 says the first session begins by \"recording the pinned Three.js commit and inspecting the complete source/package/feature/test map\". This epic is that map.\n\n## Children\n\n1. Pin r186 oracle checkout and reconcile package metadata with built contents\n2. Toolchain/workspace bootstrap: dated nightly, wasm32, pinned dependency set, budget method\n3. Symbol/export census generator for compatibility/three-r186.json\n4. Upstream test inventory and test-to-symbol mapping\n5. Feature-family reconciliation and host/dependency matrix\n6. Headline corpus H1-H8 and fidelity/control set closure; frozen benchmark corpus manifest v1\n7. Examples/editor/Inspector/devtools/TSL-tool workflow inventory and crosswalk\n8. Test evidence conventions and tiny shared logging helper (JSON-lines schema, evidence/ layout, owner/route tags)\n\n## Success criteria for the epic\n\n- Every child closed with its evidence in-tree.\n- `compatibility/three-r186.json` regenerates deterministically from the pinned checkout; the generator fails on any unmatched source/export path.\n- Counts are reported separately (symbols, families, tests, skips, unknowns). No \"100%\" is printed anywhere; the plan forbids labeling a census as implemented compatibility.\n\n## No-claim boundary\n\nA symbol census is not an implementation (AGENTS.md honesty rule 2). Nothing in this epic may be described as \"supported\", \"compatible\", or \"accelerated\".\n","acceptance_criteria":"Full source-to-feature reconciliation of Three.js r186 (commit 148ef33ecb6d2502ff796d4554abd1549c95d519) exists as one generated manifest; every unknown entry is a blocking record; the actual host/dependency matrix and the frozen benchmark corpus manifest are committed. No implementation or acceleration is claimed by this epic.","status":"open","priority":0,"issue_type":"epic","created_at":"2026-09-09T16:46:22.318780Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:26.821191Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["compat-manifest","f3d-conformance","phase-0","tier-solid","upstream-pin"]}
{"id":"f3d-01-upstream-pin-and-census-gl8.1","title":"Pin r186 oracle checkout and reconcile package metadata with built contents","description":"## Full scope\n\nPin the r186 oracle checkout and reconcile package metadata with built/package contents\n\n## Summary\n\nEstablish the immutable upstream oracle: Three.js **r186**, release date September 8, 2026, source commit `148ef33ecb6d2502ff796d4554abd1549c95d519`. Record it, obtain it reproducibly, and prove that the package manifest describes files that actually exist.\n\n## Background (plan §2.2, §5.11 item 1, §5.17, AGENTS.md \"Upstream Pin Discipline\")\n\nRelease updates are deliberate compatibility events, not automatic movements of the oracle. The annotated tag object (`819fadd6b663b74d828c6af72a543024f74d3877`) resolves to the source commit above; the two hashes must never be conflated. The plan also warns that \"a manifest entry is not proof its file exists\": package metadata (`exports`, `files`, wildcard addon/source paths) must be reconciled against the built package and the repository tree.\n\n## Technical approach\n\n- Add a small, documented procedure (script or Makefile target) that fetches the upstream repository at the exact commit into a clearly named oracle location (for example `upstream/three.js` as a git submodule pinned to the commit, or a documented shallow fetch), and verifies the checked-out HEAD equals the pinned commit. The oracle checkout is **never edited** (§14.2 uses a mapped working copy/resolver for tests).\n- Record in a single committed file (for example `upstream/PIN.md` or a section of the manifest header): release name, release date, source commit, tag-object hash, package version string, and the date of inspection.\n- Build the upstream package with its own pinned build scripts (`package.json` scripts, Rollup config) so that `build/three.module.js`, `build/three.webgpu.js`, `build/three.tsl.js`, `build/three.cjs`, and addon outputs exist locally.\n- Reconcile: enumerate every `exports` entry (root ESM/CJS, `three/webgpu`, `three/tsl`, `three/addons`, wildcard `three/addons/*`, `three/src/*`), every `files` glob, and every packaged asset; verify the target files exist; emit a JSON discrepancy list (missing file, extra file, alias duplicates). Discrepancies are recorded, not silently dropped, because duplicates/aliases must be preserved as valid import paths (§5.11).\n- Pin the Node version and use the upstream lockfile (`npm ci`) in this bead; the upstream build step is part of the reproducibility script; record the Puppeteer/browser versions used by the E2E runner as inputs for 01.4.\n\n## Success criteria\n\n- A fresh machine can reproduce the checkout and the reconciliation output byte-for-byte.\n- The reconciliation JSON lists zero *unexplained* discrepancies; explained ones (for example intentional aliases) carry a reason.\n- The pin file distinguishes tag hash vs source commit explicitly.\n\n## Test plan\n\n- Unit: `tools/upstream/verify_pin.test.mjs` asserts HEAD == pinned commit and that the tag-object hash is stored separately; reconciliation rule tests with a synthetic package.json (missing target, alias duplicate, wildcard path).\n- E2E: a fresh-clone script (`scripts/oracle-checkout.sh --verify`) on a clean directory reproduces the checkout and the reconciliation JSON byte-for-byte; run in the functional lane.\n- Logging: per-export-path lines `{step:\"resolve\", export, target, exists, reason}`; final summary with counts of resolved/explained/unexplained under `evidence/01.1/`.\n- Failure artifacts: the discrepancy list and the offending `package.json` excerpt.\n\n## Considerations\n\n- Do not vendor a modified copy of upstream. Retained components used later (F3D-04) are pinned, isolated dependencies with their licenses; they are not edits of the oracle.\n- Removed APIs from releases before r186 are not resurrected; still-shipped deprecated aliases and their warning behavior remain in scope and must appear in the reconciliation as valid paths.\n- A newer upstream release must never be substituted here; a new target gets its own pin, manifests, and results (§5.17).\n","acceptance_criteria":"A read-only pinned checkout of Three.js at commit 148ef33ecb6d2502ff796d4554abd1549c95d519 is reproducibly obtainable; package.json exports, built files, and packaged assets are reconciled with a machine-readable list of discrepancies; the tag-object hash is recorded separately from the source commit.","status":"in_progress","priority":0,"issue_type":"task","assignee":"RubyCrane","created_at":"2026-09-09T16:46:25.092875Z","created_by":"jemanuel","updated_at":"2026-09-09T18:35:53.055821Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","phase-0","upstream-pin"],"dependencies":[{"issue_id":"f3d-01-upstream-pin-and-census-gl8.1","depends_on_id":"f3d-01-upstream-pin-and-census-gl8","type":"parent-child","created_at":"2026-09-09T16:46:25.092875Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-01-upstream-pin-and-census-gl8.2","title":"Toolchain/workspace bootstrap: dated nightly, wasm32, pinned dependency set, budget method","description":"## Full scope\n\nToolchain and workspace bootstrap: dated nightly, wasm32 target, coherent pinned dependency set, budget accounting method\n\n## Summary\n\nCreate the minimal, correct build foundation: one dated nightly toolchain, the `wasm32-unknown-unknown` target, the Cargo workspace, the `#![forbid(unsafe_code)]` policy, and one coherent set of pinned dependency revisions. This is bootstrap, not a \"forest of empty crates\" (plan §21): a crate is added only when it receives its first real code.\n\n## Background (plan §13.1, §13.3, §13.4, §17.2, §23; AGENTS.md \"Toolchain\", \"Dependency Policy\", \"Planned Workspace Crates\")\n\n- The release contract uses a **dated nightly**, never a moving `nightly` tag; a new nightly is tested in a separate lane before moving the anchor.\n- The plan's source register inspected Asupersync at commit `efa5798d139ae3e877e13b2b8b5108fc5fe1a625` and other sibling manifests at then-current default branches; it explicitly requires resolving all chosen dependencies \"into one coherent set of immutable revisions rather than treating separately inspected files as a tested integration\". Version skew between Asupersync and any sibling crate must be resolved before integration: two independently instantiated Asupersync versions are not \"one runtime\".\n- Allowed dependencies are a small audited allowlist (§13.4). Forbidden in the execution core: Bevy, general `wgpu`, a new generic ECS, Tokio, mandatory Rayon, native C/C++ FFI, BLAS, Python.\n- Budgets: 205k crate subtotal, 10k charged Asupersync work, 30k reserve, 245k ceiling; ~12k JS/TS; ~15k authored WGSL. One pinned, consistently applied line-count method including tests (§17.2).\n\n## Technical approach\n\n- `rust-toolchain.toml` with an explicit `nightly-YYYY-MM-DD` and `targets = [\"wasm32-unknown-unknown\"]`; Rust 2024 edition; workspace-level lints including `unsafe_code = \"forbid\"` for first-party semantic/numerical/compiler/scene/resource crates. The browser-binding boundary is a named external boundary, documented as such.\n- Workspace `Cargo.toml` listing only crates that exist with real code at this point (expected: `f3d-core` from F3D-05 and whatever F3D-02 needs). Reserve names for the eleven planned crates in a comment; adding a twelfth crate requires explicit user approval.\n- Dependency admission records (one short markdown table or JSON, checked in): for each candidate crate, exact revision, used API, `wasm32-unknown-unknown` compile result, transitive closure, unsafe/FFI boundary, initialization requirements, incremental Wasm bytes, runtime allocation cost, demonstrated benefit. Prefer `default-features = false`.\n- Pin the JavaScript ingestion toolchain (Rollup ecosystem + Acorn-class parser) with a lockfile in a bounded `tools/` package; it is a build-time dependency, never part of the deployed Wasm.\n- Pin Naga's revision and record that its GLSL frontend targets Vulkan GLSL, not a turnkey ESSL entrance (§9.4); the ESSL normalization work is first-party (F3D-15).\n- Pin the line-count method: a single script (`tokei`/`cloc`-style with fixed configuration) reporting new Rust, new JS/TS, authored WGSL, generated code, retained upstream modules, tests/assets, third-party dependencies as separate categories (§13.5, §17.2). Report the forecast vs ceiling; the same script runs inside `f3d verify-upstream` (18a.5) and the bench report so the forecast is continuous (plan §17.1, §20 \"Plan exceeds 245k Rust lines\").\n- Record the DSR/RCH commands from AGENTS.md as the verification entry points; note that browser/GPU work never runs on RCH.\n\n## Success criteria\n\n- The workspace is created with `f3d-core` as its first member (a skeleton `lib.rs` carrying the lint configuration; 05.1 fills it); `cargo check --target wasm32-unknown-unknown` and native `cargo check` pass against that member on the pinned nightly.\n- `cargo tree` shows no forbidden crates; each direct dependency has an admission record.\n- The Asupersync revision selected here is the one used by F3D-02 and F3D-05; any needed sibling crate is on a matching Asupersync version or not used.\n- Line-count script runs and prints all categories (initially near zero) without error.\n\n## Test plan\n\n- Unit: a workspace lint test that fails if any crate outside the named binding boundary lacks `#![forbid(unsafe_code)]`; a dependency-allowlist test that fails for a direct dependency without an admission record; a line-count script self-test on a fixture tree with known counts per category.\n- E2E: `cargo check` native and `cargo check --target wasm32-unknown-unknown` on the pinned nightly via the documented DSR/RCH commands; the run log is archived.\n- Logging: toolchain/dependency resolution report `{crate, revision, features, wasm_ok, unsafe_boundary}` per dependency; line-count table by category under `evidence/01.2/`.\n- Failure artifacts: `cargo tree` output and the exact failing command with its stderr.\n\n## Considerations\n\n- Do not add `simd128` flags globally yet; F3D-06 decides SIMD use per measured crossover with a scalar reference.\n- Disk preflight (`sbh check --need 20G`) before heavy builds on this Mac (AGENTS.md).\n- Keep this bead small. The plan's own warning: writing infrastructure before a single Rust future has been polled in a browser is the canonical failure.\n","acceptance_criteria":"rust-toolchain.toml pins a dated nightly compatible with the selected Asupersync revision; the Cargo workspace exists with only the crates that receive real code in Phase 0; lockfiles and a single immutable dependency revision set (Asupersync, wasm-bindgen/js-sys/web-sys, serialization, Naga, Rollup-ecosystem + Acorn-class parser) are committed with per-dependency admission records; the line-count method is pinned.","status":"closed","priority":0,"issue_type":"task","assignee":"RusticRobin","created_at":"2026-09-09T16:46:25.172872Z","created_by":"jemanuel","updated_at":"2026-09-09T22:52:26.409851Z","closed_at":"2026-09-09T22:52:26.409469Z","close_reason":"Completed: rust-toolchain.toml pins nightly-2026-08-31 + wasm32 target; workspace has only f3d-core (real code); Cargo.lock (=1.0.229 serde, =1.0.151 serde_json) and tools/package-lock.json committed; DEPENDENCY_ADMISSION.md records every candidate with real results only (asupersync selected 4232e30, bindings resolved from its lockfile, naga none claimed); scripts/line-count.sh tested. Evidence: RCH_REQUIRE_REMOTE=1 cargo test --all-targets on hz4 at 41dc2bb exit 0 (21 pass: 10 unit, 7 handle_tests, 1 line_count, 3 workspace_lints) 22:32:40Z; cargo check --target wasm32-unknown-unknown -p f3d-core on hz4 exit 0 22:31:52Z. Commits b6b4d1e 4f4d9ca ddf1e09 + this. Verified centrally by NavyAspen.","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["asupersync-upstream","f3d-core","phase-0","toolchain"],"dependencies":[{"issue_id":"f3d-01-upstream-pin-and-census-gl8.2","depends_on_id":"f3d-01-upstream-pin-and-census-gl8","type":"parent-child","created_at":"2026-09-09T16:46:25.172872Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-01-upstream-pin-and-census-gl8.3","title":"Symbol/export census generator for compatibility/three-r186.json","description":"## Full scope\n\nSymbol/export census generator producing the single feature manifest compatibility/three-r186.json\n\n## Summary\n\nBuild the generator for the **one exhaustive feature manifest**, provisionally `compatibility/three-r186.json`: one record per importable symbol and its behavior families, expanded from the full recursive source/package tree, AST exports and class/prototype surfaces, documentation signatures, registered plugins/nodes/codecs, and official examples/tools.\n\n## Background (plan §5.11, §5.15, §14.1, §14.10; AGENTS.md \"THE NO-CUT RULE\", RULE 0.1)\n\nThe plan's scope is the union of: (1) all package export paths and shipped public entry points; (2) every export, constructor, public method/property, constant, registration/extension hook, option, serialization contract and observable source-path utility in `src/` and `examples/jsm/` recursively (a module absent from `Addons.js` is *not* excluded); (3) all official backends, alternate renderers, controls, WebAudio/media, WebXR, physics wrappers, codecs and third-party integrations shipped or exercised by pinned examples; (4) editor/player, Inspector/devtools, TSL tools, examples and manual workflows as integration consumers.\n\n`Three.WebGPU.js` exports backend classes, node builders, storage objects, render pipelines and loaders; those are part of the surface. The inspected `GLTFLoader` registers extensions beyond what documentation lists, so **registration code, not docs bullets**, is the source of truth for plugins.\n\nAGENTS.md is explicit: the manifest is one generated JSON data file inside existing tooling. Do not build a service, dashboard, or validator-for-a-validator around it.\n\n## Record schema (from §5.15)\n\nEach record carries: immutable source path + blob identity; export aliases (every valid import path that reaches it); class/method/property/options and defaults; sync/async return contract; events/errors and state effects; serialization and extension behavior; **owner route** (new Rust / general new backend / retained JS / exact backend / unresolved); supported host prerequisites; implementation and behavioral-test references; functional status; new-WebGPU-path status; explicitly intrinsic native-backend dependency (if any).\n\nBlocking statuses: `unclassified`, `unimplemented`, `untested`, `known-regression`, `stub`, `no-op-substitute`, `candidate-refusal-on-valid-source`. `retained` is ownership, not a pass. `host-blocked` requires the reference to share the missing prerequisite.\n\n## Technical approach\n\n- Implement the census as a Node script in the pinned `tools/` package using the pinned Acorn-class parser (already admitted for ingestion): parse every module, collect ESM exports (named/default/re-exports/namespace), CommonJS build exports, class declarations with prototype methods/getters/setters/static members, top-level constants, and `register*`/plugin/extension registration calls (`GLTFLoader.register`, node registrations via `addMethodChaining`/`registerNode`-style helpers, codec registrations). Resolve directory trees completely; \"truncated/paged results cannot establish the census\".\n- Cross-reference documentation signatures (`docs/`) as an additional evidence column, never as the sole source; TypeScript declarations are out of tree (`@types/three`) and are not an evidence source for the census.\n- Initial status for every record: `unclassified`. Owner route: `unresolved`. This is honest; later beads (F3D-04, F3D-18a/b) update statuses with test references.\n- Emit deterministic ordering and stable IDs (source path + export name). Print counts: total symbols, per-root counts, per-family counts (filled by 01.5), unclassified count.\n- Add a \"drift check\" mode that re-runs the census and fails if new/unmatched entries appear versus the committed manifest (§5.17, §14.10).\n\n## Success criteria\n\n- Every export reachable through any documented entry point appears exactly once as a record, with all aliases attached.\n- Running the generator twice yields identical output.\n- The generator fails on parse errors, unresolvable source locations and truncated directory listings. Records whose *status* is `unclassified` are expected at this stage (every record starts there) and never fail the census.\n- No percentage or \"coverage\" summary is printed as a single number.\n\n## Test plan\n\n- Unit: parser fixtures (re-exports, wildcard paths, class getters/statics, registration calls, CJS build exports) asserting exact records; determinism test (two runs, identical bytes); drift-mode test with an injected new export that must be reported.\n- E2E: full census over the pinned checkout in the functional lane; spot-check assertions from the plan (`three/webgpu` exports backend classes; `WebGLXRFallback` present; `Addons.js` is not the sole addon entry).\n- Logging: per-file parse events `{file, exports, classes, registrations, parse_ms}`; parse failures logged at `error` and fail the run; summary counts by root path and status (`unclassified` expected at this stage) under `evidence/01.3/`.\n- Failure artifacts: list of unmatched/unparsed files with spans.\n\n## Considerations\n\n- Keep the manifest format simple enough to diff in code review; it will be large. Consider one JSON file plus a generated compact index if needed, but not multiple services.\n- Do not attempt behavioral classification here; that is F3D-18. This bead only makes the surface complete and immutable.\n","acceptance_criteria":"A deterministic generator walks the complete pinned source/package tree (src/, examples/jsm/, Three.WebGPU.js, Addons.js, wildcard paths, editor/devtools/TSL tools) and emits one record per importable symbol with the §5.15 fields; the build fails on unmatched or truncated entries; separate counters are printed for import/symbol closure and behavior families.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:25.257795Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:11.359031Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["compat-manifest","f3d-conformance","js-host","phase-0"],"dependencies":[{"issue_id":"f3d-01-upstream-pin-and-census-gl8.3","depends_on_id":"f3d-01-upstream-pin-and-census-gl8","type":"parent-child","created_at":"2026-09-09T16:46:25.257795Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-01-upstream-pin-and-census-gl8.3","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.1","type":"blocks","created_at":"2026-09-09T16:48:33.338612Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-01-upstream-pin-and-census-gl8.4","title":"Upstream test inventory and test-to-symbol mapping","description":"## Full scope\n\nUpstream test inventory: core unit, addon unit, E2E, WebGPU screenshot, tree-shaking; counts and test-to-symbol mapping\n\n## Summary\n\nInventory the **complete** pinned test suite so later beads can run unchanged assertions against the candidate and so that the manifest knows which symbols upstream tests exercise (and which it does not).\n\n## Background (plan §2.4, §14.1, §14.3, §14.4, §14.6; AGENTS.md \"Upstream suite\")\n\nThe pinned `package.json` distinguishes core unit tests, addon unit tests, end-to-end screenshots, WebGPU screenshots, and tree-shaking checks. Unit tests import individual source files (see `test/unit/three.source.unit.js`), including renderer-internal utilities like `WebGLRenderLists`; redirecting only the package name `three` will not redirect them. The E2E runner (`test/e2e/puppeteer.js`) excludes numerous difficult examples, selects a software Vulkan ICD, uses special launch flags, and applies a per-pixel rule plus a 0.1% different-pixel limit. That lane is a regression oracle, never a performance lane.\n\nThe plan requires counting active assertions, imported tests, TODO/skips, setup/import failures, host-blocked cases and newly written feature tests **separately**, and forbids claiming totals or a completion percentage before running the census.\n\n## Technical approach\n\n- Enumerate test files per lane from the pinned scripts: `test/unit/**` (core + addons registries), `test/e2e/**`, WebGPU screenshot list, tree-shaking check inputs.\n- For each unit test module: record the module path, its import specifiers (exact source paths), the QUnit module/test names, a static assertion-count estimate, and any `todo`/`skip` markers.\n- For E2E: record the example list, the exclusion list with upstream's stated reasons, the expected-screenshot assets, launch flags, ICD selection, thresholds. Preserve these verbatim; F3D-18a reuses them.\n- Map each test module to manifest records by import path and by referenced class names; record symbols with **no** upstream test (this list is the seed for the plan's mandatory positive tests beyond upstream).\n- Emit `compatibility/upstream-tests-r186.json` (or a section within the manifest) with per-category counts.\n\n## Success criteria\n\n- Every test file in the pinned tree is accounted for in exactly one lane, or explicitly listed as unused by upstream scripts.\n- The unit-test import list demonstrates why package-name redirection alone is insufficient (a concrete list of src-level imports).\n- Output includes: number of test modules, number of tests, estimated assertions, TODO/skip counts, E2E included/excluded counts, WebGPU screenshot count. No merged percentage.\n\n## Test plan\n\n- Unit: inventory fixture with one `todo`, one `skip`, two tests and a src-level import; assert lane assignment, counts and the import list.\n- E2E: inventory over the pinned test tree; consistency check that every test-referenced source path exists in the manifest (a miss fails the run and points at 01.3).\n- Logging: per-test-module events `{module, lane, tests, est_assertions, todo, skip, imports}`; summary by lane; the list of symbols with no upstream test written to `evidence/01.4/untested-symbols.json`.\n- Failure artifacts: unresolved import paths with file/line.\n\n## Considerations\n\n- Do not run the suites here; this is inventory. Execution against the candidate is F3D-18a.\n- Upstream skips remain visible as \"unproven upstream case\" later; they never become passes.\n","acceptance_criteria":"Every active upstream test module is inventoried with its environment, import paths (including src-level imports), assertion counts, TODO/skips, E2E exclusions and the runner's launch configuration and thresholds; each test is mapped to manifest records; totals are reported by category with no aggregate percentage.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:25.342309Z","created_by":"jemanuel","updated_at":"2026-09-09T17:11:55.127067Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["compat-manifest","f3d-conformance","phase-0","testing"],"dependencies":[{"issue_id":"f3d-01-upstream-pin-and-census-gl8.4","depends_on_id":"f3d-01-upstream-pin-and-census-gl8","type":"parent-child","created_at":"2026-09-09T16:46:25.342309Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-01-upstream-pin-and-census-gl8.4","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.3","type":"blocks","created_at":"2026-09-09T16:48:33.489322Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-01-upstream-pin-and-census-gl8.5","title":"Feature-family reconciliation and host/dependency matrix","description":"## Full scope\n\nFeature-family reconciliation and host/dependency matrix (every manifest record mapped to a §5.12 family row)\n\n## Summary\n\nAttach every census record to a row of the plan's mandatory family-level work map (§5.12) and record the real host prerequisites, inherited third-party dependencies, and intrinsic native-backend contracts per family. This produces the \"actual host/dependency matrix\" named in F3D-01's acceptance evidence.\n\n## Background (plan §5.12, §5.13, §5.14, §18 Phase 0, §20 \"Scope boundaries are not feature exclusions\")\n\nThe §5.12 table is \"the mandatory family-level work map, not a finite whitelist\"; every source entry discovered under §5.11 must map to a row or add a row before release. Families include: package/module APIs; math and numeric helpers; scene graph and core data; geometry and attributes; curves/shapes/modifiers; cameras/views/output; animation and deformation; materials and render state; physical shading and color; lights/environments/shadows; textures and media sources; WebGL renderer contract; modern renderer/backend APIs; TSL/nodes/compute; legacy and dynamic shaders; postprocessing and effects; model/geometry/scientific loaders; texture/image and compression loaders; glTF and material extensions; exporters and serialization; controls and interaction; alternate renderers; audio and live media; WebXR/AR/VR; miscellaneous addons and physics integrations; Inspector/tooling/editor workflows; deployment/workers/environment behavior.\n\nEach family row also fixes its **default implementation path and proof obligations** (for example \"exact retained native GL where required\", \"new WebGPU implementation of standard execution\", \"retain full loaders/runtime dependencies\").\n\n## Technical approach\n\n- Add a `family` field to each manifest record, assigned by path rules first (for example `src/math/**` → math; `examples/jsm/loaders/**` → loaders) and then by manual review for exceptions (for example `GLBufferAttribute` belongs to geometry/attributes but carries an exact-GL requirement).\n- For each family, record: default owner route from the table; proof obligations; inherited external dependencies actually referenced by the pinned code (Draco/Basis/meshopt decoders, Ammo/Jolt/Rapier wrappers, KTX2 transcoder, three-gpu-pathtracer and any other example-referenced external package the census actually finds), each with version and license; host prerequisites (secure context, WebGPU, WebGL2, WebXR device, WebAudio activation, SharedArrayBuffer isolation, file input, network); and concrete intrinsic native-backend contracts (real GL context, sync readback, native program handles, construction-time XR fallback).\n- Emit the host/dependency matrix as a table in the manifest (or adjacent JSON) and a human-readable markdown rendering for review.\n- Any record that cannot be mapped is recorded with `family: unmapped` and counted as blocking.\n\n## Success criteria\n\n- Zero `unmapped` records at close, or each remaining one has an open bead to add its row.\n- The matrix names, for every family, the reference's real prerequisites, so that later `host-blocked` statuses can be verified against the reference (the plan forbids `host-blocked` where the reference does not share the missing prerequisite).\n- The inherited-dependency list is complete enough that F3D-10/F3D-19 can decide runtime codec inclusion by dependency closure.\n\n## Test plan\n\n- Unit: path-to-family rule tests (src/math → math; examples/jsm/loaders → loaders; GLBufferAttribute → geometry with exact-GL requirement); a rule that `unmapped` counts as blocking.\n- E2E: full reconciliation run; assertion that the plan's named examples land in the expected rows (retroreflection → physical shading; BundleGroup/QuadMesh/CanvasTarget → modern renderer APIs; WebGLXRFallback → XR).\n- Logging: per-record `{id, family, owner_default, prerequisites, inherited_deps}`; per-family counts and prerequisite summary; `unmapped` list at `error` level under `evidence/01.5/`.\n- Failure artifacts: the unmapped list with source paths.\n\n## Considerations\n\n- \"Retained\" means a production implementation wired to coherent public objects and tested end to end, not a stub or test-only alias. Recording the default route here does not close any feature.\n- Physics/engine wrappers keep the same engine version and solver semantics; this bead records those versions so later acceleration work cannot substitute an approximation.\n","acceptance_criteria":"Every manifest record maps to exactly one mandatory feature-family row (or a newly added row with rationale); inherited external dependencies, host prerequisites, and concrete native-backend requirements are recorded per family; unknown/unmapped entries are listed as blocking.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:25.428264Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:11.541185Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["compat-manifest","f3d-conformance","phase-0"],"dependencies":[{"issue_id":"f3d-01-upstream-pin-and-census-gl8.5","depends_on_id":"f3d-01-upstream-pin-and-census-gl8","type":"parent-child","created_at":"2026-09-09T16:46:25.428264Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-01-upstream-pin-and-census-gl8.5","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.3","type":"blocks","created_at":"2026-09-09T16:48:33.642033Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-01-upstream-pin-and-census-gl8.6","title":"Headline corpus H1-H8 and fidelity/control set closure; frozen benchmark corpus manifest v1","description":"## Full scope\n\nHeadline corpus H1–H8 and fidelity/control set: full source/asset dependency closure inventory and frozen benchmark corpus manifest v1\n\n## Summary\n\nPreregister and freeze the demonstration corpus **before** any candidate performance exists. Inventory the eight headline workloads and the additional fidelity/control cases down to the exact controls, shaders, assets and navigation each one reaches.\n\n## The corpus (plan §15.2, §15.3, §15.4)\n\n| ID | Example | Forces |\n|---|---|---|\n| H1 | `webgpu_performance_renderbundle` | existing `BundleGroup`s, 4,000 heterogeneous objects, `MeshToonNodeMaterial` colors, static/dynamic toggles, Inspector, **navigation-based backend selection incl. genuine WebGL** |\n| H2 | `webgl_marchingcubes` | per-step procedural geometry, resolution/blob count/isolation/wall/floor controls, env-lit Standard default plus Lambert, Phong, texture/color and **four legacy ShaderMaterial variants** |\n| H3 | `webgl_animation_multiple` | multiple animated assets with independent poses |\n| H4 | `webgl_shadowmap_performance` | repeated scene/shadow work, many objects |\n| H5 | `webgl_instancing_dynamic` | instanced geometry with dynamic instance data |\n| H6 | `webgl_postprocessing_advanced` | multiple effects, render targets, pass ordering |\n| H7 | `webgpu_skinning_instancing_individual` | compute skinning via storage buffers and custom TSL, per-instance skeleton/morph data, shadows |\n| H8 | `webgpu_compute_particles_fluid` | MLS-MPM multi-kernel compute, atomic cell data, indirect dispatch, runtime particle counts (default 32,768; storage for 131,072) |\n\nFidelity/control set (scored separately, always published): `webgpu_vxgi_sponza` (VXGI + TAA + external model index), `webgpu_postprocessing_ssgi`, `webgpu_postprocessing_traa`, `webgl_loader_gltf_transmission`, `webgpu_loader_gltf_dispersion`, `webgl_clipping_stencil`, `webgpu_clipping_stencil`, `webgpu_compute_cloth`, `webgl_materials_video` (and the WebGPU video example), a minimal deliberately GPU-bound full-screen shader (**negative control**), and a small ordinary interactive scene (**fixed-overhead/startup control**).\n\n## Why now (plan §15.1, §15.6, §16.6)\n\nSelection cannot become cherry-picking. Phase 0 may correct an invalid or unavailable candidate with a written reason and an equivalent replacement; after the manifest is frozen, a slow or difficult case stays in its declared category, and any corpus revision creates a new benchmark version that retains old results. Fewer than six headline cases, or loss of either modern-compute category (H7/H8), blocks a broad launch claim.\n\n## Technical approach\n\n- For each case, produce a closure record: HTML entry, every imported module (through `three/addons/*` and relative paths), every asset URL including ones resolved through external indexes (Sponza), GUI/control definitions with their value ranges, material selector branches, backend/URL-parameter branches (H1 reloads the page for several settings), and any `import.meta.url`/`new URL` resolution.\n- Record the unmodified default as the correctness case; define stress variants only via documented controls or one shared harness parameterization applied identically to baseline and candidate.\n- Author the two control cases that are not upstream examples (the minimal deliberately GPU-bound full-screen shader and the small ordinary interactive scene) as tiny upstream-style pages under `benchmarks/controls/`, applied identically to reference and candidate.\n- Freeze `benchmarks/headline.toml` (the headline cells; the file `f3d bench ./benchmarks/headline.toml` consumes, carrying a `version = 1` field) and `benchmarks/fidelity.toml` (the fidelity/control set): case IDs, logical workload definitions, quality settings, default and stress parameters, the observation contract (which state/events/pixels are compared), and the equal preregistered weights per cell.\n- Note per case the hypothesized bottleneck class from §15.2 as a hypothesis, explicitly not a measured diagnosis.\n\n## Success criteria\n\n- All eight headline cases and every listed control case have complete closure records; any unavailable case is replaced with a written rationale in the manifest before any candidate run.\n- Manifest v1 is committed with a version stamp and a rule that future edits require a new version.\n- H1's inventory explicitly lists both backend settings and the navigation behavior; H2's lists all material branches.\n\n## Test plan\n\n- Unit: manifest schema validation for `benchmarks/headline.toml` and `benchmarks/fidelity.toml` (case ids, version field, weights sum, stress parameters present, observation contract fields); the two authored control pages load and render under the harness.\n- E2E: closure verification per case: load each example under request logging and assert observed ⊆ recorded (any observed request not in the closure record fails the case) and report recorded − observed as \"statically reachable, not observed by this script\" (an unobserved branch may never be dropped, §3.1); GUI enumeration script asserts every recorded control exists with its range; run on Chrome and Safari.\n- Logging: per-case events `{case, modules, assets, bytes, controls, backend_modes, navigation_paths}`; unrecorded requests logged at `error` and fail the case; archived under `evidence/01.6/`.\n- Failure artifacts: request log diff and the GUI enumeration dump.\n\n## Considerations\n\n- Never increase difficulty only on the baseline or lower quality only on FrankenThreeD.\n- The negative control exists to catch false CPU-only speedup claims; it must remain in the published report even when it shows no gain.\n","acceptance_criteria":"For each of H1–H8 and every §15.4 fidelity/control case, the complete source/module/asset closure, every control, material variant, backend mode, navigation/reload behavior, external asset index, and stress parameterization is inventoried; invalid candidates are corrected with a written reason before any candidate performance is known; the benchmark corpus manifest v1 is frozen and versioned.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:25.510578Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:11.181832Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-conformance","h1","h2","h3","h4","h5","h6","h7","h8","phase-0"],"dependencies":[{"issue_id":"f3d-01-upstream-pin-and-census-gl8.6","depends_on_id":"f3d-01-upstream-pin-and-census-gl8","type":"parent-child","created_at":"2026-09-09T16:46:25.510578Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-01-upstream-pin-and-census-gl8.6","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.1","type":"blocks","created_at":"2026-09-09T16:48:33.795461Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-01-upstream-pin-and-census-gl8.7","title":"Examples/editor/Inspector/devtools/TSL-tool workflow inventory and crosswalk","description":"## Full scope\n\nExamples, editor, Inspector, devtools, TSL-tool and manual workflow inventory with crosswalk to feature families\n\n## Summary\n\nComplete the inventory of **integration consumers**: the full examples index (`examples/files.json`), the editor (`editor/js/Editor.js` and its state: scripting, history, loading/storage, XR, renderer changes, animation), the Inspector, devtools, TSL tooling, and documentation/manual examples. Produce the exhaustive feature-to-example/test crosswalk required by §5.15 and §15.5.\n\n## Background (plan §2.9, §5.11 item 4, §5.14 \"Editor/Inspector/devtools\", §14.9, §15.5)\n\nThe repository contains an editor, devtools, TSL tooling, documentation/manual examples, and the tests/examples trees. Reusing these tools is smaller than replacing them, but integration and behavioral verification are mandatory. \"Demanding does not mean comprehensive\": a fluid demo may barely exercise animation events. The crosswalk shows which families no headline demo touches so targeted tests can be written (F3D-18b, F3D-21b).\n\n## Technical approach\n\n- Parse the pinned examples index and each example HTML for imported modules and used classes; attach the example IDs to manifest records (many-to-many).\n- Walk the editor source to list commands/history entries, menus, loader integrations, script execution paths, player preview, export/publish targets, XR entry, storage (IndexedDB) usage, and renderer selection; record each as a workflow with the upstream code path.\n- Inventory Inspector hooks/panels and devtools integration points as source-visible debug APIs (they are part of the surface and must keep working; F3D telemetry is a separate namespace).\n- Inventory TSL tooling (transpiler/editor) entry points and their dependencies.\n- Emit the crosswalk: for each family, which examples/workflows exercise it; flag families with zero coverage.\n\n## Success criteria\n\n- 100% of examples index entries are accounted for (this is an inventory count, not a compatibility claim).\n- Editor workflows listed match the plan's enumeration: import/edit/undo-redo/serialize/script/player/export/publish/XR.\n- Uncovered-family list produced and referenced by the test-closure beads.\n\n## Test plan\n\n- Unit: example-index parser fixture; editor command-list extractor fixture (a small synthetic editor module with two commands).\n- E2E: inventory over `examples/files.json`, `editor/`, Inspector, devtools and TSL tooling; sample verification of five examples' module/class lists by hand-authored expectations.\n- Logging: per-example `{example, modules, classes, families}` and per-workflow `{tool, workflow, code_path}` events; parse failures at `error` (they fail the run); crosswalk summary with uncovered families under `evidence/01.7/`.\n- Failure artifacts: list of examples/workflows that failed to parse.\n\n## Considerations\n\n- Editor-authored script execution keeps its original host/security requirements; the engine's rule against introducing `eval` does not prohibit a source application's existing scripting feature (§5.14). Record this so no later bead \"sanitizes\" it away.\n","acceptance_criteria":"Every entry in the pinned examples index, every editor/player workflow (import, edit, undo/redo, scripts, animation, persistence, player, export/publish, XR, renderer changes), Inspector/devtools panels and TSL tools are inventoried and crosswalked to manifest families; uncovered families are listed for targeted tests.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:25.595664Z","created_by":"jemanuel","updated_at":"2026-09-09T17:11:55.643014Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["compat-manifest","editor","f3d-conformance","phase-0"],"dependencies":[{"issue_id":"f3d-01-upstream-pin-and-census-gl8.7","depends_on_id":"f3d-01-upstream-pin-and-census-gl8","type":"parent-child","created_at":"2026-09-09T16:46:25.595664Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-01-upstream-pin-and-census-gl8.7","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.3","type":"blocks","created_at":"2026-09-09T16:48:33.940659Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-01-upstream-pin-and-census-gl8.8","title":"Test evidence conventions and tiny shared logging helper (JSON-lines schema, evidence/ layout, owner/route tags)","description":"## Full scope\n\nTest evidence conventions and a tiny shared logging helper: one JSON-lines event schema for unit, integration and end-to-end tests in Rust and JavaScript; an `evidence/<bead>/` archive layout with per-run summaries and failure artifacts; owner/route tagging on every result; kept to a few dozen lines, never a framework.\n\n## Summary\n\nBefore the first executable evidence exists (02.2), fix how every test in the project reports what it did, so that later lanes, gates and audits can be read by humans and agents without guessing. This is a convention plus a minimal helper, not infrastructure: AGENTS.md forbids building a harness for a harness.\n\n## Background (AGENTS.md \"Output Style\", \"Testing Policy\"; plan §14.3, §14.5, §16.4, §22)\n\nEvery result must record its implementation owner (new Rust/WebGPU, general new backend, retained JS, exact component, unresolved) and, for rendering, the route that actually submitted. Measurements distinguish app time from wall time. Raw runs are preserved. Failures must show the wrong value, not just \"mismatch\". The plan's gates are decided from archived evidence, so the archive layout must be stable from the first test.\n\n## Conventions (the deliverable)\n\n- Event schema (one JSON object per line): `ts_wall` (ms, measurement clock), `ts_app` (logical/app clock when applicable), `lane` (`unit` | `integration` | `fuzz` | `e2e-functional` | `e2e-forced-new-backend` | `e2e-exact` | `bench`), `bead` (bead id), `test` (name), `step`, `level` (`info` | `warn` | `error` | `metric`), `owner` (route owner), `route` (actual submission route when rendering), `browser` (`{name, version, os}` for browser lanes), `device_generation`/`scene_generation` when relevant, `msg`, `data` (free-form object, bounded size).\n- Archive layout: `evidence/<bead-key>/<run-id>/events.jsonl` where `<bead-key>` is the short key used throughout the beads (for example `05.7`; the full bead id is recorded inside `summary.json`); `f3d verify` runs use `evidence/verify/<case>/<run-id>/` and benchmark runs `evidence/bench/<stratum>/<run-id>/`; `events.jsonl`, `summary.json` (pass/fail per test with counts and the first failing event, plus `commit` = repository HEAD and `upstream_commit` = `148ef33ecb6d2502ff796d4554abd1549c95d519`), `artifacts/` (screenshots, image diffs, traces, capability records, profiler exports). Benchmark runs additionally follow `benchmarks/protocol-v1` (03.6).\n- Rust: `f3d-core` exposes a `test-support` cargo feature (enabled only as a dev-dependency by other crates, never in production builds; no new crate) providing `pub mod test_evidence` behind `#[cfg(feature = \"test-support\")]` with a `tracing` subscriber that writes the schema to the run file; `cargo test` output stays human-readable, structured lines go to the file. JS: a ~30-line `evidence.mjs` helper used by the harness (03.1) and by every e2e script.\n- Failure rule: a failing assertion logs both observed and expected values (or pixel coordinates and channel deltas for images) and attaches artifacts; never only a boolean.\n- Bounded: production code never depends on this helper; per-frame renderer logging remains prohibited (§8.8).\n- Retention: `evidence/` is git-ignored except `evidence/**/KEEP/`; a gate decision copies every run it cites into `KEEP/` before it is written (plan §16.4: preserve raw runs); raw bench runs are always kept.\n\n## Success criteria\n\n- The helper exists in Rust and JS with one shared schema document (`docs/test-evidence.md`); 02.2's first run produces a conforming `events.jsonl` and `summary.json`; a schema validation test rejects a malformed event.\n\n## Test plan\n\n- Unit: schema round-trip tests in Rust and JS; a validator test with a deliberately malformed event (missing `owner`) that must fail.\n- E2E: 02.2 acts as the first consumer; its run directory is checked for the required files.\n- Logging: the helper is the logging; its own tests emit events to `evidence/01.8/`.\n- Failure artifacts: n/a beyond the validator's diagnostic message.\n\n## Considerations\n\n- Keep it tiny (target under 150 lines total across Rust and JS). If it grows, stop and ask (AGENTS.md checkpoint rule).\n","acceptance_criteria":"One JSON-lines event schema and evidence/<bead>/<run>/ layout are documented and implemented as a tiny Rust test subscriber plus a ~30-line JS helper; every result carries owner and route tags; the first foundation run (02.2) produces conforming events and summary; the helper stays under ~150 lines and is never a production dependency.","status":"closed","priority":0,"issue_type":"task","assignee":"RusticRobin","created_at":"2026-09-09T17:11:24.194500Z","created_by":"jemanuel","updated_at":"2026-09-10T07:25:49.795264Z","closed_at":"2026-09-10T07:25:49.794878Z","close_reason":"Completed: Rust helper crates/f3d-core/src/test_evidence.rs (test-support feature, 110 lines) + JS tools/evidence.mjs (43 lines) + docs/test-evidence.md + evidence/ gitignore with KEEP retention; validators reject missing owner. Evidence: commits 7a3ee34, 9c5c7d7, 3772bbc; strict RCH hz2 cargo test -p f3d-core --features test-support exit 0 at 07:24:56Z (23 passed, 0 failed, log rch_f3d_core_test_gl88_2.log); node --test tools/evidence.test.mjs 2/2. 153 helper lines (3 over cap, disclosed).","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","phase-0","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-01-upstream-pin-and-census-gl8.8","depends_on_id":"f3d-01-upstream-pin-and-census-gl8","type":"parent-child","created_at":"2026-09-09T17:11:24.194500Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-01-upstream-pin-and-census-gl8.8","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.2","type":"blocks","created_at":"2026-09-09T17:11:24.363715Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-02-asupersync-browser-execution-58j","title":"F3D-02: Execute Rust futures through the Asupersync browser host (Safari and Chrome)","description":"## What this epic delivers\n\nExecutable proof that Asupersync can be the **sole async foundation** for FrankenThreeD in a browser. Plan §19 lists this as F3D-02 (depends on 01) with acceptance \"actual task bodies, wakeups, cancellation/drain in Safari/Chrome\". It is the first half of kill gate 1 (Foundation).\n\n## Why it is on the critical path (plan §2.3, §11.1, §11.2, §16.7, §20; AGENTS.md \"Asupersync Is The Only Async Foundation\")\n\nThe inspected Asupersync browser documentation distinguishes the shipped JS/TS ownership and host-adapter boundary from full native-style Rust task execution: its documented ABI task-spawn allocates a task handle rather than accepting an arbitrary Rust future, and the Rust-authored browser bootstrap is characterized as a preview with a remaining host-services gap. \"Passing a `cargo check` for Wasm, or closing an ownership ledger, is not equivalent to proving that browser tasks actually executed and drained.\"\n\nThe rules that follow:\n- No Tokio, no alternative executor, no unstructured promise pool as a workaround.\n- One application Wasm instance linking the admitted Asupersync core/host integration; never a separately instantiated browser-core runtime plus a second FrankenThreeD runtime with independent memories and ownership tables.\n- Missing host functionality is implemented **upstream in Asupersync**, not forked inside FrankenThreeD. That work is charged to this project's 10,000-line foundation allocation; exceeding it requires using the reserve and reassessing viability before accidentally building a second runtime.\n- Acceptance evidence is executable behavior, not the presence of a contract document.\n\n## Children\n\n1. Gap analysis of the Asupersync browser host and version-skew resolution.\n2. Rust-authored browser program: polled futures, timers, host-turn wakeups, nonreentrant pump.\n3. Cancellation, fetch cancellation, drain before teardown, no late publication, unsupported-host behavior.\n4. Upstream Asupersync host-services contributions charged to the foundation budget.\n5. Region granularity and priority policy for FrankenThreeD.\n\n## Success criteria for the epic\n\nAll children closed; the evidence is a small checked-in browser test page plus recorded logs from installed Safari and Chrome on the physical target machines (not a bundled automation WebKit build; that is a separate environment per §12.1). The foundation gate bead consumes this evidence.\n\n## No-claim boundary\n\nThis epic establishes execution, not performance. It makes no statement about frame throughput.\n","acceptance_criteria":"A real Rust-authored browser program, linked against the admitted Asupersync core/host integration, demonstrates in installed Safari and Chrome: futures polled with observed results, timers and host-turn wakeups without native threads, a nonreentrant scheduler pump with bounded microtask bursts, cancellation delivered to running cooperative tasks, real fetch cancellation and cleanup, child tasks drained before region teardown, no late publication into a cancelled scene, and explicit behavior for unsupported hosts. Missing host pieces are implemented upstream in Asupersync and charged to the 10,000-line allocation.","status":"open","priority":0,"issue_type":"epic","created_at":"2026-09-09T16:46:22.409888Z","created_by":"jemanuel","updated_at":"2026-09-09T16:48:35.009831Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["asupersync-upstream","f3d-runtime","kill-gate","phase-0","tier-solid"],"dependencies":[{"issue_id":"f3d-02-asupersync-browser-execution-58j","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.2","type":"blocks","created_at":"2026-09-09T16:48:35.009530Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-02-asupersync-browser-execution-58j.1","title":"Asupersync browser host gap analysis and version-skew resolution","description":"## Full scope\n\nAsupersync browser host gap analysis, version-skew resolution, and upstream work plan\n\n## Summary\n\nBefore writing a line of runtime code, determine exactly what the pinned Asupersync browser host provides and what is missing for real Rust task execution, and decide how each gap is closed upstream.\n\n## Background (plan §2.3, §11.1, §11.2, §13.2, §13.3, §23 [S12]–[S15])\n\nInspected artifacts: the Asupersync README (structured concurrency, qualified adapter guarantees), `docs/wasm_browser_scheduler_semantics.md` (required host-turn behavior), `docs/WASM.md` (ownership-ledger vs task-execution scope), and `asupersync-browser-core/Cargo.toml` (existing wasm-bindgen/js-sys/web-sys and serialization boundary). Sibling numerical libraries reference **older** Asupersync versions; \"two independently instantiated versions with different ownership types are not one runtime.\"\n\n## Technical approach\n\n- Read the pinned Asupersync browser scheduler contract and WASM docs at the revision fixed in 01.2. For each foundation requirement (futures polled; timers/host-turn wakeups without threads; nonreentrant pump and bounded microtask bursts; cancellation to running tasks; fetch cancellation and cleanup; child drain before region teardown; no late publication into cancelled/replaced scene; explicit unsupported-host behavior) record: what API exists, whether it accepts an arbitrary Rust future or only allocates a handle, what the host adapter does today, and what is missing.\n- Confirm the binding family versions (wasm-bindgen/js-sys/web-sys) FrankenThreeD will share with Asupersync's boundary; document that reusing that boundary is preferred to inventing a second bridge (§13.4).\n- Decide the single Wasm instance design: FrankenThreeD's application module links Asupersync core + host integration; enumerate the exported host hooks the JS side must provide (timer, microtask, rAF/host-turn, fetch abort, worker messaging if any).\n- Produce the upstream work plan: for each gap, the target Asupersync module, the semantic contract to implement, a test to prove it, and a line estimate. Sum the estimates against 10,000 lines; flag if the sum exceeds the envelope.\n- Resolve skew: if any sibling crate (FrankenNumPy/SciPy) is wanted later, note whether its Asupersync version matches; if not, it is not used until upstream aligns.\n\n## Success criteria\n\n- Gap table complete with no \"unknown\" rows.\n- One Asupersync revision recorded in the workspace lockfile and in the dependency admission record.\n- Upstream plan reviewed by the user before 02.4 begins if estimates exceed the envelope.\n\n## Test plan\n\n- Unit: `cargo check --target wasm32-unknown-unknown` of a skeleton crate depending on the pinned Asupersync browser-core (default-features = false); a test that the pinned Asupersync revision string in Cargo.lock equals the one recorded in the admission record.\n- E2E: none (analysis bead); the gap table is reviewed against the pinned docs.\n- Logging: the gap table itself (`docs/asupersync-browser-gap.md`) with a status column and, for each `present` claim, a pointer to the executable probe in 02.2/02.3 that will verify it; the estimate sum vs the 10,000-line allocation.\n- Failure artifacts: n/a.\n\n## Considerations\n\n- An ownership ledger closing is not proof a browser callback stopped executing. Every \"present\" claim in the gap table must point at executable behavior or be marked \"unverified\".\n- Keep this analysis to what F3D-02 needs; do not audit all of Asupersync.\n","acceptance_criteria":"A written gap list maps each §11.2 foundation requirement to the current Asupersync browser host status (present, partial, missing) at the pinned revision; the Asupersync revision is fixed for the whole workspace; each missing piece has an upstream design note and an estimate charged against the 10,000-line allocation.","status":"closed","priority":0,"issue_type":"task","assignee":"ChartreuseFern","created_at":"2026-09-09T16:46:25.678731Z","created_by":"jemanuel","updated_at":"2026-09-09T23:00:31.764676Z","closed_at":"2026-09-09T23:00:31.764495Z","close_reason":"Completed: docs/asupersync-browser-host-gap.md maps each plan 11.2 foundation requirement to present/partial/missing at asupersync 4232e30 with file:line evidence (1 missing, 6 partial, 1 present); workspace revision fixed at 4232e30 (DEPENDENCY_ADMISSION.md 309d833); six upstream work items with module, contract, proof test and estimate summing to 9,000 of the 10,000-line allocation. Analysis-only bead; no runtime claims. Reviewed and committed by NavyAspen.","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["asupersync-upstream","f3d-runtime","phase-0"],"dependencies":[{"issue_id":"f3d-02-asupersync-browser-execution-58j.1","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.2","type":"blocks","created_at":"2026-09-09T16:48:34.090792Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-02-asupersync-browser-execution-58j.1","depends_on_id":"f3d-02-asupersync-browser-execution-58j","type":"parent-child","created_at":"2026-09-09T16:46:25.678731Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-02-asupersync-browser-execution-58j.2","title":"Rust-authored browser program: polled futures, timers, host-turn wakeups, nonreentrant pump","description":"## Full scope\n\nRust-authored browser program: futures actually polled, timers and host-turn wakeups without threads, nonreentrant pump with bounded microtask bursts\n\n## Summary\n\nThe first Rust code that executes in a browser: a tiny browser program that proves Asupersync-driven task execution works on the actual targets. Plan §21: \"Execute a real browser Rust future and cancellation path; address the host gap before adding renderer abstractions.\"\n\n## Background (plan §11.2, §4.9, §11.3; AGENTS.md \"Concrete anti-pattern\")\n\nThe scheduler must respect the browser event loop: task/microtask ordering and run-to-completion (§23 [S35]). A cooperative runtime must not split originally synchronous work across turns (§4.9), so the pump must be nonreentrant and bound its microtask bursts, yielding to the host on a defined policy. This bead produces executable evidence, not a document.\n\n## Technical approach\n\n- Create the first runtime crate (`f3d-runtime`, or a temporary example binary inside it) with `#![forbid(unsafe_code)]` outside the named binding boundary. Link the pinned Asupersync core and browser host integration in **one** Wasm instance.\n- Implement the host hooks in a minimal static JS shim (no `eval`): microtask scheduling, `setTimeout`/timer wakeups, host-turn (`requestAnimationFrame`/`MessageChannel`-style) wakeups, and a pump entry point.\n- Test tasks: (a) a future that awaits a timer three times and records timestamps; (b) a chain of futures communicating through Asupersync channels/joins whose observed output order is logged; (c) a task that yields per host turn 1,000 times and records that each poll happened on a distinct macrotask; (d) a reentrancy probe: a Wasm import invoked synchronously by a running task calls back into the pump entry point; the pump must reject or defer that call, never nest (a plain host callback cannot interleave on a single JS thread unless the pump itself calls out); (e) a burst probe: schedule 10,000 microtask-ready wakeups and assert the pump processes at most the configured bound before yielding.\n- Log every observation as structured lines (JSON per event: task id, event, host time) into a page buffer that the automation harness collects; also print a final summary.\n- Run on: installed Safari on the M5 MacBook and the iPhone 17 Pro Max (via the physical device), installed Chrome on the M5 MacBook. Record browser/OS build strings.\n\n## Success criteria\n\n- All five probes pass on both browsers with logs archived under `evidence/02.2/`.\n- No native thread, SharedArrayBuffer, or Atomics.wait is required (§11.6: the first product must work without shared-memory threads).\n- Wasm builds with the pinned nightly; the JS shim is static source.\n- `f3d-runtime/CONTRACT.md` is written before any other crate depends on `f3d-runtime` (AGENTS.md \"Documentation and Contracts\"): purpose and position in the dependency direction, public types and semantics, invariants, implementation ownership route, error model, determinism and numeric-fidelity class, cancellation behavior, unsafe boundary if any, feature flags, conformance tests, and no-claim boundaries.\n\n## Test plan\n\n- Unit: Rust tests for the pump's state machine (nonreentrancy flag, burst bound, wake-source ordering) using a fake host; JS unit tests for the static shim's timer/microtask/host-turn hooks.\n- E2E: `tests/e2e/foundation/execution.mjs` loads the Wasm page in installed Safari (M5 and iPhone) and Chrome (M5), waits for completion and asserts the five probes (polled futures, timers, host-turn wakeups, reentrancy rejection, burst bound); browser/OS build strings captured.\n- Logging: one event per task lifecycle step `{task, event: spawn|poll|wake|complete, source: timer|host-turn|microtask, ts_wall}`; probe summaries with counts and max burst; archived under `evidence/02.2/<browser>/`.\n- Failure artifacts: the full event stream around the first violation and a console capture.\n- iPhone fallback: if Safari automation on the physical iPhone (safaridriver with Remote Automation enabled) is unavailable, run the page manually on the device and attach the exported `events.jsonl`; a manual run on the device is valid evidence, an emulator/simulator run is not.\n\n## Considerations\n\n- 02.4 delivers the future-polling/wakeup host pieces first (the inspected gap: spawn allocates a handle and cannot poll an arbitrary future); this bead proves them. Never work around a missing piece with a hand-rolled executor in FrankenThreeD.\n- Keep the shim tiny; it will be replaced by generated host code later (F3D-05/F3D-12a), but its semantics (nonreentrancy, bounded bursts) carry forward.\n","acceptance_criteria":"A checked-in Wasm test page, built from Rust on the pinned nightly and linked against the admitted Asupersync core/host integration, runs in installed Safari and Chrome and logs observed results of real polled futures, timer-driven and host-turn-driven wakeups, and a pump that is provably nonreentrant with a bounded microtask burst per turn.","status":"in_progress","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:25.762503Z","created_by":"jemanuel","updated_at":"2026-09-12T20:19:29.943576Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["asupersync-upstream","f3d-runtime","phase-0","testing"],"dependencies":[{"issue_id":"f3d-02-asupersync-browser-execution-58j.2","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.8","type":"blocks","created_at":"2026-09-09T17:11:24.553793Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-02-asupersync-browser-execution-58j.2","depends_on_id":"f3d-02-asupersync-browser-execution-58j","type":"parent-child","created_at":"2026-09-09T16:46:25.762503Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-02-asupersync-browser-execution-58j.2","depends_on_id":"f3d-02-asupersync-browser-execution-58j.1","type":"blocks","created_at":"2026-09-09T16:48:34.243067Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-02-asupersync-browser-execution-58j.2","depends_on_id":"f3d-02-asupersync-browser-execution-58j.4","type":"blocks","created_at":"2026-09-09T17:41:09.854940Z","created_by":"jemanuel","metadata":"{}","thread_id":""}],"comments":[{"id":5,"issue_id":"f3d-02-asupersync-browser-execution-58j.2","author":"OrangePelican","text":"2026-09-12 OrangePelican verification: actual pkg103 Wasm377a02dd browser_execution probes PASS on this M4 host in installed Chrome152 and Safari26.5. Each completed timer(3), channel/join(42), host-turn(1000), reentrancy, bounded10001poll burst, cancellation, drain, stale-result, real fetch-abort, idle, and capability probes. Evidence02.2/2026-09-12T20-07-50.235Z-57477 and...241Z-57478 under /Volumes/USBNVME16TB/temp_agent_space/f3d-browser-evidence. This does not satisfy the specified M5/physical iPhone validation; bead remains open/in_progress, no full device-matrix claim.","created_at":"2026-09-12T20:19:29Z"}]}
{"id":"f3d-02-asupersync-browser-execution-58j.3","title":"Cancellation, fetch abort, drain before teardown, no late publication, unsupported-host behavior","description":"## Full scope\n\nCancellation delivered to running tasks, real fetch cancellation and cleanup, child drain before region teardown, no late publication, explicit unsupported-host behavior\n\n## Summary\n\nComplete the foundation milestone's second half: **structured cancellation and cleanup** that behave the way the plan's later lifecycle rules assume (device loss, scene replacement, stale worker results, cancelled uploads).\n\n## Background (plan §11.2, §11.4, §11.5, §14.7, §22 \"The foundation is real\")\n\n\"Cancellation is not rollback of issued effects.\" A cancelled job retains a drain obligation. No late publication into a cancelled or replaced scene. Tests must \"cancel at every supported await/publication boundary\". Explicit behavior for unsupported host contexts is required rather than a silent failure.\n\n## Technical approach\n\n- Cancellation to a running task: a cooperative loop that checks its cancellation token between bounded chunks; the test cancels from a host callback and asserts the task observes cancellation within one chunk and runs its cleanup exactly once.\n- Fetch cancellation: start a fetch of a large resource served by the local test server with a throttle; cancel the owning task; assert the browser aborted the request (server sees a disconnect; `AbortSignal` fired) and the response body stream is released. Log bytes received before abort.\n- Drain before teardown: spawn a region with N child tasks holding a counter; tear down the region; assert teardown completes only after all children have finished or been cancelled and drained, and the counter is consistent.\n- No late publication: a child produces a value after an artificial delay; the region is cancelled/replaced before the value arrives; the value must be dropped with a logged \"stale result discarded (generation mismatch)\" event, and must not appear in the replacement region's state. This is the prototype of the plan's generation-checked publication rule (§6.5, §11.5).\n- Unsupported host: run the page with a required capability disabled (for example the static shim omitting the timer hook, or a host without `AbortController`/`fetch`) and assert a defined error object and no hang.\n- Honest bound: document that a cancellation check inside a synchronous kernel cannot promise a main-thread cancellation callback runs before the browser regains control (§4.9); log the observed latency instead.\n\n## Success criteria\n\n- Five new probes pass on both browsers with archived JSON logs.\n- Cleanup counters demonstrate exactly-once cleanup; no leaked timers or fetches after teardown (verify via a post-teardown idle probe).\n\n## Test plan\n\n- Unit: Rust tests for cancellation propagation, region drain ordering and generation-checked publication using a fake host with controllable delays.\n- E2E: `tests/e2e/foundation/cancellation.mjs` on Safari and Chrome with the local throttled server: cancel-in-loop, fetch abort (server observes disconnect), drain-before-teardown, stale-result rejection, unsupported-host outcome; plus a negative run with the generation check disabled that must fail.\n- Logging: lifecycle events `{task, region, generation, event: spawn|cancel-requested|cancel-observed|cleanup|drained|discarded-stale, ts_wall}`; fetch abort logs bytes received; cancellation-latency samples as `metric` events; archived under `evidence/02.3/`.\n- Failure artifacts: leaked-resource report from the post-teardown idle probe.\n\n## Considerations\n\n- These semantics are what F3D-22a (fault injection) and F3D-11.5 rely on; do not weaken them later for convenience.\n- If upstream Asupersync lacks a piece (fetch abort integration, region drain), it is implemented in 02.4, not here.\n","acceptance_criteria":"The foundation test page additionally proves, in installed Safari and Chrome: cancellation observed inside a running cooperative task; an in-flight fetch aborted with the browser request actually cancelled and resources released; a region's child tasks fully drained before teardown returns; a delayed result that arrives after its region was cancelled/replaced is never published; and a defined, logged outcome on a host lacking a required capability.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:25.845722Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:14.800996Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["asupersync-upstream","device-loss","f3d-runtime","phase-0","testing"],"dependencies":[{"issue_id":"f3d-02-asupersync-browser-execution-58j.3","depends_on_id":"f3d-02-asupersync-browser-execution-58j","type":"parent-child","created_at":"2026-09-09T16:46:25.845722Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-02-asupersync-browser-execution-58j.3","depends_on_id":"f3d-02-asupersync-browser-execution-58j.2","type":"blocks","created_at":"2026-09-09T16:48:34.391527Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-02-asupersync-browser-execution-58j.3","depends_on_id":"f3d-02-asupersync-browser-execution-58j.4","type":"blocks","created_at":"2026-09-09T16:48:34.546192Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-02-asupersync-browser-execution-58j.4","title":"Upstream Asupersync host-services contributions (charged 10k-line allocation)","description":"## Full scope\n\nUpstream Asupersync browser host-services contributions (charged to the 10,000-line foundation allocation)\n\n## Summary\n\nDo the actual host-services work in Asupersync so that a Rust future can execute, wake, cancel and drain in the browser. This is the only place FrankenThreeD is allowed to change async foundation semantics.\n\n## Background (plan §2.3, §11.1, §11.2, §13.3, §17.1; AGENTS.md)\n\n\"Where the browser host-services seam is incomplete, implement the missing host in Asupersync rather than forking its semantics inside FrankenThreeD.\" \"The proposed 10,000-line foundation allocation is a planning envelope, not proof that the gap fits. Exceeding it requires using the reserve and reassessing project viability before building a second runtime accidentally.\" \"Reusable improvements belong upstream where reasonable, and project-required new work is charged to this project's budget.\"\n\n## Technical approach\n\n- Work from the 02.1 upstream plan. Likely items (verify against the gap table): accepting and polling arbitrary `'static` Rust futures through the browser bootstrap; timer and host-turn waker sources; a nonreentrant pump with a configurable microtask burst bound; region/scope drain integrated with browser cancellation; fetch abort integration through `AbortController`; explicit unsupported-host error surface.\n- Implement with tests inside Asupersync's own browser test lane; keep changes aligned with its structured-concurrency model (regions, scopes, cancellation as a first-class outcome).\n- Coordinate with the Asupersync repository owner (same user); if an upstream merge cannot happen immediately, pin a branch revision and record \"pending upstream\" in the admission record.\n- Count lines with the project's pinned method and record them in the budget report as \"charged Asupersync foundation work\".\n\n## Success criteria\n\n- 02.2 and 02.3 pass using only the pinned Asupersync revision plus FrankenThreeD's static JS shim.\n- No FrankenThreeD-local executor, scheduler, or task type exists.\n- Budget line: consumed lines vs 10,000; if over, a short viability note is filed and the user is informed before continuing.\n\n## Test plan\n\n- Unit: upstream Asupersync unit and browser tests for each contribution (future polling, waker sources, pump bounds, region drain, fetch abort, unsupported-host error); FrankenThreeD side runs 02.2/02.3 as integration.\n- E2E: 02.2 and 02.3 probes pass using only the pinned Asupersync revision and the static shim.\n- Logging: upstream test output archived alongside the foundation evidence under `evidence/02.4/`; the budget line (lines consumed vs 10,000) recorded in the code-count report.\n- Failure artifacts: upstream test logs.\n\n## Considerations\n\n- Do not transplant Asupersync source into this repository to hide dependency or line count (§13.3).\n- Retained upstream/external integrations (application codec/physics Wasm) keep their own host scheduling; they are disclosed components, not competing executors (§11.1).\n","acceptance_criteria":"Every gap from 02.1 needed by 02.2/02.3 is implemented in the Asupersync repository (or a pinned fork branch pending upstream merge is explicitly disclosed), with tests there; FrankenThreeD pins the resulting revision; the line count consumed is recorded against the 10,000-line allocation with a viability note if exceeded.","status":"in_progress","priority":0,"issue_type":"task","assignee":"ChartreuseFern","created_at":"2026-09-09T16:46:25.933076Z","created_by":"jemanuel","updated_at":"2026-09-09T23:01:48.503232Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["asupersync-upstream","f3d-runtime","phase-0"],"dependencies":[{"issue_id":"f3d-02-asupersync-browser-execution-58j.4","depends_on_id":"f3d-02-asupersync-browser-execution-58j","type":"parent-child","created_at":"2026-09-09T16:46:25.933076Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-02-asupersync-browser-execution-58j.4","depends_on_id":"f3d-02-asupersync-browser-execution-58j.1","type":"blocks","created_at":"2026-09-09T16:48:34.700212Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-02-asupersync-browser-execution-58j.5","title":"Region granularity and priority policy (never per-object tasks)","description":"## Full scope\n\nRegion granularity and priority policy for FrankenThreeD: session/scene/asset/shader/worker regions, never per-object tasks, measured bounds\n\n## Summary\n\nFix the shape of asynchrony in FrankenThreeD before any renderer code exists, so that later beads never create \"one task per object, triangle, uniform, or draw\".\n\n## Background (plan §11.3, §4.9; AGENTS.md \"Asupersync Is The Only Async Foundation\")\n\nRegions belong at session, scene/view, asset-preparation, shader/pipeline-preparation and worker-job granularity. The per-frame path is predominantly synchronous batched work; budget checks or chunk yields obey §4.9 (a check does not create permission to interleave an originally synchronous operation). The priority policy \"must preserve UI responsiveness and cancellation progress without starving ordinary asset work. It is an engineering policy with measured bounds, not a claim of real-time scheduling guarantees from a browser.\"\n\n## Technical approach\n\n- In `f3d-runtime`, define region types and their ownership: `SessionRegion` (owns device/session lifetime), `SceneRegion` (per scene/view; owns pending preparation for that scene generation), `AssetPrepRegion` (decode/transcode/preprocess jobs), `ShaderPrepRegion` (pipeline/variant compilation), `WorkerJobRegion` (bounded background work). Each carries a generation used by publication checks.\n- Document the invariant: frame preparation and submission run synchronously inside a host-turn callback; they never await inside the hot path; asynchronous preparation publishes only at boundaries allowed by its source adapter (§6.3).\n- Priority policy: small number of levels (UI/cancellation, scene-critical preparation, background asset work); measured bounds: maximum pump time per host turn, maximum consecutive background chunks before yielding, cancellation latency bound. These are configuration values with tests, not a scheduler research project.\n- Starvation test: run heavy asset-prep chunks while cancelling a scene region repeatedly; assert cancellation latency stays within the configured bound and asset work still makes progress.\n\n## Success criteria\n\n- Region types and generation-tagged publication helper exist and are used by 02.3's probes.\n- Policy bounds are recorded numbers with a test that fails when violated.\n\n## Test plan\n\n- Unit: Rust tests for region type invariants (child regions cannot outlive parents; generation increments on replacement) and for the policy bounds (pump time per turn, consecutive background chunks, cancellation latency) using a fake clock.\n- E2E: `tests/e2e/foundation/starvation.mjs`: heavy asset-prep chunks plus repeated scene-region cancellation; asserts cancellation latency within the configured bound and asset progress above a floor; Safari and Chrome.\n- Logging: per-turn `metric` events `{pump_ms, chunks, cancellations, latency_ms}` and a histogram summary; archived under `evidence/02.5/`.\n- Failure artifacts: the turn timeline around the bound violation.\n\n## Considerations\n\n- SharedArrayBuffer/worker pools are a later isolated-deployment option (§11.6); this bead assumes the main thread or a single supported worker.\n","acceptance_criteria":"A short design note plus code skeleton defines the region hierarchy (application session, scene/view lifetime, asset preparation, shader/pipeline preparation, long-running worker jobs), the rule that the per-frame numerical/render path is synchronous batched work outside task machinery, and a priority policy with measured bounds for UI responsiveness and cancellation progress; a test demonstrates asset work does not starve and cancellation still progresses under load.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:26.020316Z","created_by":"jemanuel","updated_at":"2026-09-09T17:11:56.518155Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["asupersync-upstream","f3d-runtime","phase-0"],"dependencies":[{"issue_id":"f3d-02-asupersync-browser-execution-58j.5","depends_on_id":"f3d-02-asupersync-browser-execution-58j","type":"parent-child","created_at":"2026-09-09T16:46:26.020316Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-02-asupersync-browser-execution-58j.5","depends_on_id":"f3d-02-asupersync-browser-execution-58j.3","type":"blocks","created_at":"2026-09-09T16:48:34.854285Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-03-reference-profiles-and-bridge-tmt","title":"F3D-03: Profile strong references and bridge alternatives","description":"## What this epic delivers\n\nThe strong, unbiased denominator and the bridge decision. Plan §19 F3D-03 depends on 01 (and the bridge measurement integrates 02). Acceptance evidence: \"Reference-only H1/H2/H7/H8 and matched completed-work data\". This epic feeds kill gate 2 (Bridge) and everything in §16.\n\n## Why (plan §1.2, §1.3, §16.1–16.6, §16.7, §20 rows 1–2)\n\nThe central hypothesis (`speedup = 1 / ((1 - p) + p/k + h)`) means the project must know how much of the reference's critical path is actually removable before writing an engine. The first risk in §20 is \"the fastest Three.js baseline already removes the available overhead\"; its earliest decisive test is the H1 baseline decomposition. The second risk is that Wasm/JS transfer erases gains; its test is the bridge probes. Both are answered here, with references that are **strong on purpose**: R0 as authored, R1 under a frozen bounded optimization recipe, R2 an equivalent current Three.js WebGPU implementation, selected by reference-only calibration and frozen before scored runs.\n\nEverything measured here must be \"completed logical frames per wall-clock second in a bounded steady-state pipeline\", never `queue.submit` time or a GPU-only kernel timer.\n\n## Children\n\n1. Shared deterministic harness (clock/random injection, real measurement clock, input scripting, checkpoints, overhead calibration).\n2. Fair uncapped offscreen lane with completion acknowledgement on both APIs.\n3. R0/R1/R2 reference configurations and frozen bounded R1 recipe; independent calibration.\n4. Reference profiles and critical-path decomposition (thesis test).\n5. Bridge alternative measurements on matched workloads.\n6. Frozen performance protocol and absolute budgets.\n7. Device matrix provisioning, capability record, reserved-device and thermal discipline.\n8. Statistical analysis pipeline (paired runs, block bootstrap, log-space aggregation).\n\n## Success criteria for the epic\n\nAll children closed; a single `benchmarks/protocol-v1.md` (with machine-readable settings) and the `evidence/03.3/` and `evidence/03.4/` directories exist; the bridge gate bead can be decided from the recorded data alone.\n\n## No-claim boundary\n\nNothing here is a FrankenThreeD result. Reference numbers are published as reference numbers.\n","acceptance_criteria":"Reference-only H1/H2/H7/H8 measurements exist on the physical strata with R0/R1/R2 configurations selected by independent calibration and frozen; the four bridge variants are measured on matched workloads with completed-work data; the full performance protocol and absolute startup/memory/tail budgets are frozen before any candidate performance is known.","status":"open","priority":0,"issue_type":"epic","created_at":"2026-09-09T16:46:22.493501Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:14.085984Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-conformance","kill-gate","phase-0","tier-solid"],"dependencies":[{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.6","type":"blocks","created_at":"2026-09-09T16:48:36.836194Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-03-reference-profiles-and-bridge-tmt.1","title":"Shared deterministic harness: injected app clock/random, real measurement clock, scripted inputs","description":"## Full scope\n\nShared deterministic harness: injected application clock and random source, separate real measurement clock, scripted inputs, frame/state checkpoints, overhead calibration\n\n## Summary\n\nBuild the one instrumentation layer both sides share, so equivalence and throughput comparisons are fair and reproducible. \"Same input source on both sides except for explicitly shared harness instrumentation\" (§1.3).\n\n## Background (plan §14.5, §16.2, §16.3, §16.4)\n\n- Use deterministic application clocks and random sources supplied by a shared harness. Overriding `performance.now()` or the animation timestamp for the application must not replace the wall clock used to compute throughput; app time and measurement time remain distinct.\n- Preserve seed consumption, timer/microtask behavior, and one update per logical frame. Capture sequences of frames and state observations, not only the first frame.\n- Test pointer/mouse/touch, wheel/pinch, keyboard, resizing, control-panel changes, loading completion/error, animation transitions, picking, material changes, scene add/remove, disposal.\n- Include acknowledgement overhead and a matched empty/light-work calibration to quantify harness overhead.\n\n## Technical approach\n\n- `tools/harness/` JS package: `installDeterministicClock(seed, stepMs)` patching only the application-visible sources (`performance.now` as seen by app code, `requestAnimationFrame` timestamps, `Date.now` where the app uses it, `Math.random` via a seeded PRNG) while keeping an unpatched `measure.now()`; the patch is applied identically to reference and candidate pages via the same script tag.\n- Input scripting: JSON event scripts replayed at logical frame indices (not wall times) through real DOM events on the real target elements, preserving pointer capture and focus semantics; GUI (lil-gui/dat.gui) interactions performed through their DOM controls.\n- Checkpoints: at configured logical frames, capture a small state observation (selected object matrices, material params, event counters) ; pixel readbacks occur only in equivalence runs and in preregistered spot-check windows excluded from the scored measurement interval, with equal frequency on both sides (§16.3); scored runs capture state hashes only.\n- Calibration: a trivial scene with the harness attached vs detached to measure the harness's own cost; recorded with every protocol run.\n- Structured logging: JSON lines per checkpoint/event with logical frame index and both clocks.\n\n## Success criteria\n\n- Two runs of the reference with the same seed produce identical event/state traces (up to documented nondeterministic diagnostic counters).\n- The harness cost is measured and reported for every device stratum.\n\n## Test plan\n\n- Unit: PRNG determinism and clock-stepping tests (JS); input-script parser tests; checkpoint serializer tests.\n- E2E: replay an input script on an upstream example twice with the same seed and assert identical event/state traces; a calibration run (harness attached vs detached) on each stratum recording overhead.\n- Logging: JSON-lines per checkpoint/event `{frame, ts_app, ts_wall, kind, state_hash, event}` (schema from 01.8); calibration results as `metric` events; archived under `evidence/03.1/<stratum>/`.\n- Failure artifacts: the first diverging trace pair with the input step that preceded it.\n\n## Considerations\n\n- Diagnostic FPS/GPU counters may differ between sides; excluded diagnostic pixels are narrowly identified before testing, never broad masks (§14.5).\n- Transient activation cannot be recreated by replaying an event after an await (§3.4); scripts must trigger activation-sensitive actions from real input events.\n- The compiler treats the harness-patched clock and random source as unbound runtime state (12a.7); a build that constant-folds them would invalidate every equivalence run.\n","acceptance_criteria":"A harness library (JS, shared verbatim by reference and candidate) provides deterministic application time and seeded randomness, a separate wall clock for throughput, scripted pointer/keyboard/wheel/resize/GUI inputs, periodic frame and state checkpoints, and a matched empty/light-work calibration run that quantifies harness overhead; it never overrides the wall clock used for scoring.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:26.105755Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:10.999520Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-conformance","js-host","phase-0","testing"],"dependencies":[{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.1","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.6","type":"blocks","created_at":"2026-09-09T16:48:35.165685Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.1","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.8","type":"blocks","created_at":"2026-09-09T17:11:24.746364Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.1","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt","type":"parent-child","created_at":"2026-09-09T16:46:26.105755Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-03-reference-profiles-and-bridge-tmt.2","title":"Fair uncapped offscreen lane with completion acks on WebGPU and WebGL2","description":"## Full scope\n\nFair uncapped offscreen lane: fixed logical time, two logical frames in flight, completion acknowledgement on WebGPU (onSubmittedWorkDone) and WebGL2 (fenceSync/clientWaitSync), validated against on-screen reference behavior\n\n## Summary\n\nBecause displayed FPS is capped (two applications capped at 120 FPS cannot show 3×), the scored measure is **completed offscreen-frame throughput** in a bounded pipeline. This bead builds that lane correctly and fairly.\n\n## Background (plan §16.2, §16.3, §23 [S49], [S50])\n\n- Freeze a small work-in-flight limit, provisionally two logical frames. Each logical frame includes all source updates, passes and effects; persistent simulation/history state advances exactly once.\n- WebGPU: record submission high-water marks and resolve bounded completion credits with `queue.onSubmittedWorkDone()` (it acknowledges a queue high-water mark, not a per-command-buffer fence).\n- WebGL2 reference: insert `fenceSync`, flush, poll `clientWaitSync` with zero timeout while yielding to the host. Neither side busy-spins; never compare per-frame `gl.finish()` on the reference with a pipelined candidate.\n- Validate the reference's offscreen harness against its original on-screen rendering first: switching a Three.js scene to a render target can change output/tone-mapping or resolve behavior; a faster but incomplete offscreen pipeline is not eligible.\n- Start the clock before the first scored update after an initial drain; stop after the last scored GPU work completes. Device loss, validation error, or rejected completion invalidates the run as a failure.\n\n## Technical approach\n\n- Harness module providing `OffscreenLane` for both APIs: creates the offscreen target(s) with the case's declared dimensions/format/samples, drives `N` logical frames with the deterministic clock, maintains the credit window, and records per-frame submit/ack timestamps.\n- For Three.js WebGL references: a thin wrapper that renders to a `WebGLRenderTarget` plus the fence-based ack, with a validation mode that compares offscreen vs canvas output at checkpoint frames (color-space-aware compare).\n- For WebGPU references (R2, and later the candidate): ack via `onSubmittedWorkDone`; frames in flight limited by credits.\n- Report: completed frames, elapsed measurement time, throughput, ack overhead, plus the on-screen pacing companion numbers (frame pacing, missed deadlines, input response, long tasks) from a separate on-screen run of the same case.\n- Logical frame rule: the lane calls the example's registered animation-loop body exactly once per logical frame (wrapping `setAnimationLoop`/rAF); multiple `renderer.render` calls inside one body are one logical frame; persistent simulation/history state therefore advances exactly once.\n\n## Success criteria\n\n- Reference offscreen output matches its on-screen output within the preregistered tolerance at all checkpoints for H1/H2/H7/H8.\n- Credit window enforcement proven by a test that artificially delays acknowledgements and observes the lane blocking at two frames.\n\n## Test plan\n\n- Unit: credit-window accounting tests (blocks at two, releases on ack, terminal path on failure) in JS; WebGL2 fence polling state machine test.\n- E2E: offscreen-vs-onscreen validation on H1/H2/H7/H8 references at checkpoints (color-space-aware compare); an artificial ack-delay test proving the lane blocks at two frames; run on Chrome and Safari.\n- Logging: per-frame `{frame, submit_ts, ack_ts, credits, api}` and a summary with throughput, ack overhead and validation result; archived under `evidence/03.2/`.\n- Failure artifacts: offscreen/onscreen image diffs; the credit timeline.\n\n## Considerations\n\n- \"An offscreen win that violates the interaction budgets does not pass the product gate\" (§16.3); the on-screen companion run is mandatory, not optional.\n","acceptance_criteria":"Both reference and candidate can render the same logical state sequence offscreen with equivalent target dimensions/formats/samples/output conversion; work in flight is capped at two logical frames using real completion acknowledgements on both APIs without busy-spinning; the reference's offscreen output is validated equivalent to its on-screen rendering; throughput is computed as credited completed frames over elapsed wall time.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:26.186878Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:15.174009Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-conformance","js-host","phase-0"],"dependencies":[{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.2","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt","type":"parent-child","created_at":"2026-09-09T16:46:26.186878Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.2","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.1","type":"blocks","created_at":"2026-09-09T16:48:35.315830Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-03-reference-profiles-and-bridge-tmt.3","title":"Reference configurations R0/R1/R2 for H1/H2/H7/H8, calibrated and frozen","description":"## Full scope\n\nReference configurations R0/R1/R2 for H1, H2, H7, H8: frozen bounded R1 recipe, equivalent WebGPU R2 where available, independent reference-only calibration and freeze\n\n## Summary\n\nConstruct the strong reference and freeze it. The denominator cannot be cherry-picked later.\n\n## Background (plan §1.3 item 4, §16.1, §15.3, §20 row 1; AGENTS.md \"References\")\n\n- **R0**: the pinned application with shared harness instrumentation only.\n- **R1**: a frozen, bounded recipe of documented precompilation, instancing/batching, render-bundle and resource-management optimizations; not an unlimited manual rewrite, replacement simulation, or hand-written FrankenThreeD-equivalent compiler. Preserve the declared object/state/control contract, not only pixels; collapsing source objects or changing custom instance IDs just to accelerate the reference is not automatically eligible.\n- **R2**: an equivalent current Three.js WebGPU implementation preserving scene/materials, logical updates, effects, resolution, precision, inputs. Where no genuinely equivalent migration exists, record the missing reference; do not invent one.\n- Select the strongest eligible configuration using independent reference-only calibration, then freeze. Do not take the fastest noisy trial among many. Publish R0 and every eligible configuration. Render bundles and upload-range coalescing are baseline capabilities, not FrankenThreeD inventions; the selected reference must not leave applicable optimizations disabled.\n- H1 already defaults to bundles; both the enabled-bundle reference and a competent instanced/material-data reference belong in the comparison if they satisfy the full observation contract.\n\n## Technical approach\n\n- Write `benchmarks/r1-recipe-v1.md`: the enumerated, bounded list of permitted transformations (for example `renderer.compileAsync` warmup, `BundleGroup`, `InstancedMesh`/`BatchedMesh` where the observation contract allows, static geometry merge only when identities are not observed, texture/material sharing, `frustumCulled` settings) with the explicit rule for what is *not* allowed.\n- For each case, author the R1 variant(s) and R2 variant(s) as separate files under `benchmarks/references/<case>/`, each with a diff against R0 and a checklist proving the observation contract is preserved (controls, events, identities).\n- Calibration protocol: write `benchmarks/calibration-v1.md` in this bead, before any run (repeat count, warmup, alternation, selection rule = highest median with a non-overlapping 95% interval over the runner-up, else the simpler configuration); protocol v1 (03.6) later inherits or supersedes it for scored runs. Run all eligible configurations on each provisioned stratum (03.7) under that calibration protocol; the analysis script (03.8) applies the selection rule; freeze the selection in `benchmarks/references/selection-v1.json`.\n- Record \"missing reference\" explicitly for any case where an equivalent R2 is not possible (for example if a legacy `ShaderMaterial` variant of H2 has no equivalent node-material port that preserves its contract).\n- The recipe may only enable optimizations the source does not already use; for H1 the bundle setting is a source control (both settings are functional cases), not an R1 transformation, and the R1 variant must not double-apply it.\n\n## Success criteria\n\n- Every headline case has a frozen selected reference with source hash and settings.\n- The equivalence checks for each R1/R2 against R0 pass (visual/state/control at checkpoints).\n- No configuration is deleted after calibration; all are published with their numbers.\n\n## Test plan\n\n- Unit: recipe checklist validator (each R1 transformation is in the allowed list; diff against R0 recorded).\n- E2E: equivalence runs of every R1/R2 variant against R0 via the harness with protocol tolerances; calibration batches per case per stratum with the repeat count from `benchmarks/calibration-v1.md`; selection rule applied by the analysis script (03.8).\n- Logging: per-variant events `{case, variant, stratum, throughput, ci_low, ci_high, equivalence: pass|fail}`; the frozen selection record with source hashes; archived under `evidence/03.3/`.\n- Failure artifacts: equivalence diffs for any ineligible variant (kept and published, never deleted).\n\n## Considerations\n\n- Improvement to reference code later creates a new frozen comparison version, never selective replacement of favorable runs.\n","acceptance_criteria":"For each of H1/H2/H7/H8: R0 (as authored plus harness), R1 (frozen bounded documented optimization recipe preserving the declared object/state/control contract), and R2 (equivalent current Three.js WebGPU implementation, or a recorded \"missing reference\" with reason) exist; the strongest eligible configuration per case is selected by reference-only calibration runs and frozen with source and settings before any candidate exists; all configurations are published.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:26.271758Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:09.273255Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-conformance","h1","h2","h7","h8","phase-0"],"dependencies":[{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.3","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt","type":"parent-child","created_at":"2026-09-09T16:46:26.271758Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.3","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.2","type":"blocks","created_at":"2026-09-09T16:48:35.469426Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.3","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.7","type":"blocks","created_at":"2026-09-09T17:41:09.074674Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.3","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.8","type":"blocks","created_at":"2026-09-09T17:41:09.272804Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-03-reference-profiles-and-bridge-tmt.4","title":"Reference profiles and critical-path decomposition (thesis test)","description":"## Full scope\n\nReference profiles and critical-path decomposition for H1/H2/H7/H8 (test of the central performance hypothesis)\n\n## Summary\n\nBefore writing an engine, measure where the strong reference actually spends its time, so the project can tell whether \"removing questions from the frame loop\" can plausibly reach 2×/3× on each case. This is the earliest decisive test for §20's first risk.\n\n## Background (plan §1.2, §2.1, §15.3, §16.7 \"Generalization gate\", §16.8)\n\n`speedup = 1 / ((1 - p) + p/k + h)`: even infinitely fast replacement cannot produce 3× unless roughly two-thirds of the relevant critical path is removable before new overhead. \"Real rendering overlaps CPU and GPU activity, so the actual experiment must measure the pipeline rather than simply add CPU and GPU timings.\" H7/H8 references must be profiled during Phase 0 \"rather than postponing the hardest viability question until the renderer is complete.\"\n\n## Technical approach\n\n- Use browser profilers (Chrome DevTools performance traces, Safari Web Inspector timelines) plus the harness's per-frame markers to attribute time: JS application update (the example's own loop), Three.js scene preparation (matrix updates, render list building, program lookup), uploads (`bufferSubData`/`writeBuffer` counts and bytes), command preparation (draw call issue), GPU execution (GPU timestamp queries where supported; otherwise inferred from the ack timeline), completion/idle.\n- Distinguish overlapped time from serial time; report both a serial attribution and the observed pipeline throughput.\n- For each case write a one-page decomposition with: measured fractions, the plan's hypothesized gain source (from §15.2) checked against the data, the estimated removable fraction `p`, the bridge overhead budget `h` this leaves for the candidate, and the implied best case. Mark everything as a structural estimate.\n- For H8 specifically: measure whether the reference is GPU-bound at default and stress particle counts; if so, record that a CPU-side specialization alone cannot reach 2× and that an equivalent compute/data-path improvement is required (§20 row \"Modern compute demos remain GPU-bound\").\n\n## Success criteria\n\n- Four decompositions with numbers on at least two strata, archived under `evidence/03.4/`.\n- Each decomposition states the implied best case and whether the case is plausibly viable; this feeds the bridge gate and later the first-product gate.\n- If a case is GPU-bound at the reference (GPU time at or above the frame interval), the decomposition states that the CPU-side removable fraction is \"not sufficient\" and names the GPU-side data-path budget the candidate must beat (feeds 20.4).\n\n## Test plan\n\n- Unit: attribution parser tests over recorded profiler exports (Chrome trace JSON, Safari timeline export) with known category totals.\n- E2E: two profile sessions per case per stratum; repeatability assertion within the protocol noise band; decomposition documents generated from the data.\n- Logging: per-case `metric` events `{case, stratum, category, ms, fraction, serial_or_overlapped}` and the implied-best-case computation with its inputs; archived under `evidence/03.4/`.\n- Failure artifacts: raw profiler exports.\n\n## Considerations\n\n- Do not multiply isolated kernel speedups (§16.8).\n- Product labels (M5, NVIDIA) do not determine exposed features; record the runtime capability record with each profile (03.7).\n","acceptance_criteria":"For each of H1/H2/H7/H8 on the phone and M5 strata (and discrete GPU where available), the selected reference's steady-state frame is decomposed into application update, scene preparation, upload, command preparation, GPU execution and completion with measured fractions; an explicit estimate of the removable fraction p and the implied best-case speedup is recorded per case with the honest caveat that it is a structural estimate, not a result.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:26.359258Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:15.714227Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-conformance","h1","h2","h7","h8","phase-0"],"dependencies":[{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.4","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt","type":"parent-child","created_at":"2026-09-09T16:46:26.359258Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.4","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.3","type":"blocks","created_at":"2026-09-09T16:48:35.622407Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.4","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.7","type":"blocks","created_at":"2026-09-09T16:48:35.774128Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-03-reference-profiles-and-bridge-tmt.5","title":"Bridge alternative measurements: direct JS vs chatty Wasm vs bulk packet vs generated JS","description":"## Full scope\n\nBridge alternative measurements on matched workloads: direct JS WebGPU vs simple Wasm-to-host calls vs bulk command packet with small JS decoder vs generated static JS submission code\n\n## Summary\n\nMeasure, do not assume, how Wasm-resident logic should talk to WebGPU. The plan requires implementing and measuring both a compact command/data packet decoded by a small JS adapter and application-specialized static JS submission code generated at build time, and keeping the better measured variant per workload class (§6.4).\n\n## Background (plan §2.8, §6.4, §16.7 \"Bridge gate\", §21, §20 row 2)\n\n\"Compiling to Wasm does not remove the browser boundary.\" The engineering goal is to remove redundant semantic work and unnecessary crossings, not to claim Wasm has no abstraction cost. Kill gate 2: \"measure direct JavaScript WebGPU, simple Wasm-to-host calls, bulk command transfer, and generated host submission for matched workloads. The selected path must demonstrate an advantage opportunity after its own overhead; no fixed numeric win is assumed before measurement.\" Section 21: \"measure the costs that could invalidate the architecture: command transfer, typed-array copying, initialization, and per-frame synchronization.\"\n\n## Technical approach\n\n- Prototype variants (throwaway-quality is acceptable; correctness of GPU output is not optional):\n  1. **Direct JS WebGPU**: hand-written JS issuing the workload.\n  2. **Chatty Wasm→host**: a Wasm module (Rust, safe, using the 02.2 runtime) calling one imported host function per draw/uniform write.\n  3. **Bulk packet**: Wasm writes a compact binary command stream into linear memory; a small static JS decoder walks it and issues WebGPU calls; measure copy vs borrowed-view variants (views must be rebuilt on memory growth; no borrow survives a host callback).\n  4. **Generated submission code**: static JS specialized to the workload's fixed structure, reading only dynamic data from Wasm memory.\n- Workloads: (a) 4,000 draws with distinct per-object uniforms (H1-shaped), (b) bulk upload of 4,000 packed `AffineRows` records per frame, (c) a frame that replays prebuilt bundles plus a small dynamic tail. Identical GPU work verified by pixel comparison.\n- Measure with 03.1/03.2: per-frame CPU time in the bridge, bytes copied, number of host calls, initialization time (Wasm instantiate + first frame), and completed-frame throughput with the two-frame credit window. Run on installed Safari and Chrome on the M5, and Safari on the phone.\n- Include the two mandatory counterexamples as correctness preconditions for any variant: red-A/blue-B queue-write snapshot and bundle-then-direct-draw state reset (§21 \"before treating the bridge as usable\"). A variant that fails them is not a candidate.\n\n## Success criteria\n\n- A table per browser per workload class: variant, throughput, bridge CPU time, copies, calls; and a written selection per class.\n- The selected variant's overhead `h` relative to direct JS is stated; if no variant leaves room for the required gains given 03.4's `p`, that is recorded as a negative bridge-gate finding.\n\n## Test plan\n\n- Unit: packet encoder/decoder round-trip tests for the bulk variant; view-rebuild-on-growth test for the borrowed-view sub-variant.\n- E2E: `tests/e2e/bridge/variants.mjs` runs the three workloads through all four variants on Safari (M5, iPhone) and Chrome (M5) with pixel-identity checks, plus the red-A/blue-B and bundle-then-direct preconditions per variant.\n- Logging: per-frame `metric` events `{variant, workload, browser, bridge_cpu_ms, bytes_copied, host_calls, throughput}` and init-time events; summary table per browser/workload; archived under `evidence/03.5/`.\n- Failure artifacts: pixel diffs for any variant that fails identity; the counterexample results.\n\n## Considerations\n\n- Generate ordinary static JavaScript; no production `eval` or `new Function` (§6.4).\n- A submitted command buffer is not a reusable render bundle; the bundle workload must record bundles legally (§6.4).\n- Don't confuse `writeBuffer` copy-alignment rules with encoder copy row-pitch rules (§6.8).\n- The two counterexamples here are prototype-grade preconditions; 05.7 is their production form. The 48-byte three-row `AffineRows` layout used by workload (b) is provisional until 05.2 freezes the schema.\n","acceptance_criteria":"Four bridge variants execute the same matched synthetic workloads (many small draws with per-object uniform data; bulk transform uploads; a bundle-replay frame) with identical GPU work; command transfer, typed-array copying, initialization and per-frame synchronization costs are measured on Safari and Chrome; the per-workload-class winner is recorded with its overhead relative to direct JS; the data is sufficient to decide the bridge gate.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:26.445986Z","created_by":"jemanuel","updated_at":"2026-09-12T20:53:24.781895Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-runtime","js-host","kill-gate","phase-0"],"dependencies":[{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.5","depends_on_id":"f3d-02-asupersync-browser-execution-58j.2","type":"blocks","created_at":"2026-09-09T16:48:36.072523Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.5","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt","type":"parent-child","created_at":"2026-09-09T16:46:26.445986Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.5","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.1","type":"blocks","created_at":"2026-09-09T16:48:35.925930Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.5","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.2","type":"blocks","created_at":"2026-09-09T17:41:09.477653Z","created_by":"jemanuel","metadata":"{}","thread_id":""}],"comments":[{"id":6,"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.5","author":"jemanuel","text":"Partial implementation verified 2026-09-12: RCH batch106 runtime 78 lib + 60 mesh tests passed; package SHA256 777094fffcfd15ce7591d85ecbaed0b77c14c257b21ed2b0a8da42c661ba2026. On this M4, Chrome and Safari each passed 26 bridge checks, including 4000 distinct AffineRows draws with all 262144 RGBA bytes identical to direct JS. Existing 12 counterexample suites also passed per browser. Evidence: 05.6/2026-09-12T20-26-47.216Z-43345 and 05.6/2026-09-12T20-26-47.217Z-43346 under /Volumes/USBNVME16TB/temp_agent_space/f3d-browser-evidence. These are debug first-draw samples, not steady-frame throughput, cold initialization, or acceleration proof. Chatty/generated variants are under review; other workloads and M5/physical-phone validation remain outstanding.","created_at":"2026-09-12T20:35:15Z"},{"id":7,"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.5","author":"jemanuel","text":"Four prototype paths now execute the same 4000-draw image on M4 Chrome/Safari: direct JS, actual Wasm-to-host callback loop, Rust bulk packet, and build-time generated static JS consuming owned Wasm bytes. RCH batch107 native 79+60 passed; Wasm package a7ca6179e9f1647b114e152d09c43b5ba73a3fef4688fe2185cdc98bca83d79d. Explicit mesh_batch browser runs 05.6/2026-09-12T20-50-42.250Z-48192 and ...48191 each passed 32 checks, including 262144-byte equality for all variants and original Error identity/abort from the actual Wasm callback. Generated JS is 279879 bytes/8004 generated lines, separately reported. Owned packing/copy is inside CPU timing. These are debug first-draw samples with device/Wasm already initialized; no steady-frame throughput, selected winner, or acceleration claim. Other workloads, per-variant counterexamples, initialization, two-frame credit window, and required M5/phone measurements remain open.","created_at":"2026-09-12T20:53:24Z"}]}
{"id":"f3d-03-reference-profiles-and-bridge-tmt.6","title":"Freeze performance protocol v1 and absolute startup/memory/tail budgets","description":"## Full scope\n\nFreeze the performance protocol and absolute budgets: startup/memory/tail numbers, repeat counts, stopping rule, aggregation weights, infrastructure-failure treatment, equivalence tolerances, deployed-byte budgets\n\n## Summary\n\nWrite down every number and rule the gates depend on **before** the candidate exists, so nothing can be tuned after seeing results. \"An unset budget leaves the gate incomplete, not automatically passed\" (§16.6).\n\n## Background (plan §14.6, §16.4, §16.5, §16.6, §17.4)\n\n- Gates: equivalence first; eligibility (all reference-valid H1–H8 cells required; candidate failures never shrink the denominator); ≥2× median paired completed-frame throughput in every required cell; ≥3× geometric mean per primary stratum; paired 95% lower bounds meet 2× per cell and exceed 2× for the stratum geomean; H7/H8 stay required; frozen tail budgets; ≤5% sustained-throughput regression on valid controls; frozen startup/memory budgets with bounded caches and no OOM; phone floors hold in the sustained segment; portable standalone passes independently; scored GPU work goes through the new renderer; functional closure independent of the corpus; Rust inside the 245k ceiling.\n- Aggregate cell ratios in log space with equal preregistered weights; bootstrap at the paired run/block level; freeze repeat count, stopping rule, weights and infra-failure treatment before scoring. \"An unfavorable valid run is not an infrastructure failure.\"\n- Tolerances for cross-backend comparisons are preregistered from baseline repeatability and the intended visual contract; upstream E2E thresholds (per-pixel rule, 0.1% limit) are preserved and never loosened.\n- Byte and peak-memory budgets are set from actual compiled artifacts for a small closed scene, each full headline artifact, and a runtime-extensible shader/loader application; no invented universal sub-megabyte target.\n\n## Technical approach\n\n- Produce `benchmarks/protocol-v1.md` plus `benchmarks/protocol-v1.json` (consumed by `f3d bench` and the analysis script) with every value above filled in from 03.3/03.4/03.7 measurements (baseline repeatability gives noise bands; reference startup/memory give budget anchors; the target experience gives tail budgets).\n- Define the sustained thermal segment: provisionally a 20-minute phone session with paired cooldown/recovery, recorded power/charging policy, and the final steady segment reported separately (§16.5).\n- Define run invalidation: device loss, async validation error, rejected completion; and the rule that an invalid hardware session is retained with reason and rerun as a complete pair.\n- Version the protocol; any change creates protocol-v2 and retains v1 results.\n- Budgets that need artifacts: startup, tail and thermal budgets are frozen here from reference and foundation measurements (§16.6); deployed-byte and peak-memory budgets per artifact class cannot exist before a compiled artifact (plan §17.4: set from actual compiled artifacts), so they are recorded as `protocol-v1.1` from 10.2's first artifacts and frozen before any scored run (11.4); `f3d bench` refuses a scored run against a protocol with unset budgets. This is an addendum, not a new protocol version for the throughput rules.\n\n## Success criteria\n\n- Every gate row in §16.6 has a concrete numeric or procedural definition in the protocol file; a reviewer can score a hypothetical run by hand.\n- The protocol is committed and referenced by the corpus manifest v1 (01.6).\n\n## Test plan\n\n- Unit: protocol JSON schema validation; a test that every §16.6 gate row has a numeric or procedural definition; analysis-script dry run on reference-vs-reference data producing intervals containing 1.\n- E2E: none (freeze bead); the protocol is reviewed and versioned.\n- Logging: the protocol file is the artifact; the dry-run report is archived under `evidence/03.6/`.\n- Failure artifacts: n/a.\n\n## Considerations\n\n- The 5% control allowance permits a small fixed-cost tradeoff, never reduced quality; a control failing fidelity is not a 5%-budget case.\n- Energy per frame is reported only with an instrument that supports it (§16.5).\n","acceptance_criteria":"benchmarks/protocol-v1 records, as explicit numbers frozen before any candidate performance: the 2x cell floor and 3x stratum geomean target with the 95% paired lower-bound rule; p95/p99 frame-time, missed-deadline and input-response budgets per stratum; cold/warm startup budgets derived from reference and foundation measurements; the 5% control regression allowance; repeat count, warmup, stopping/retest rule, equal cell weights, block-level bootstrap; infrastructure-failure treatment; preregistered visual/state tolerances derived from baseline repeatability; the sustained thermal segment definition. Deployed-byte and peak-memory budgets per artifact class are recorded as the protocol-v1.1 addendum from the first compiled artifacts (10.2) and frozen before any scored run (11.4); f3d bench refuses a scored run against a protocol with unset budgets.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:26.531525Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:10.414114Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-conformance","phase-0"],"dependencies":[{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.6","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt","type":"parent-child","created_at":"2026-09-09T16:46:26.531525Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.6","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.4","type":"blocks","created_at":"2026-09-09T16:48:36.225954Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.6","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.8","type":"blocks","created_at":"2026-09-09T16:48:36.382566Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-03-reference-profiles-and-bridge-tmt.7","title":"Device matrix provisioning, capability record, reserved-device and thermal discipline","description":"## Full scope\n\nDevice matrix provisioning, runtime capability record, reserved uncontended benchmark-device discipline, and mobile thermal protocol\n\n## Summary\n\nMake the hardware side of the benchmark honest and reproducible: real devices, exact builds, runtime-probed capabilities, and no contention.\n\n## Background (plan §12.1, §12.2, §12.5, §16.4, §16.5; AGENTS.md \"Device matrix\", \"Benchmark hardware discipline\")\n\n- Physical devices, not emulation. No fictional \"Safari on Windows NVIDIA\". A bundled automation WebKit build or an iPhone simulator is a separate test environment, not hardware evidence. WebKit documents WebGPU in Safari 26.0, but the matrix needs exact current builds and runtime probes.\n- Capture browser/OS build, adapter info, requested/enabled device features, WGSL language features, supported limits, selected canvas format/color space, execution-host capabilities. Record privacy-hidden data as unknown. Do not invent VRAM from `maxBufferSize` or infer a driver path from an adapter name.\n- GPU timing runs on a reserved, uncontended physical device; remote build workers compile but never produce timings. Pin browser builds, OS, driver where available, viewport, DPR, power mode, cache state, background-load policy.\n- Mobile: sustained 20-minute session with paired cooldown/recovery; report the final steady segment separately.\n\n## Technical approach\n\n- `benchmarks/devices-v1.json`: one entry per stratum with the physical device identity, OS/browser build pins, connection method (USB/WebDriver/remote debugging), and the pre-run checklist (close other GPU work, disable agent builds on that machine, power mode, screen brightness/DPR).\n- Capability probe page (conforming to the `CapabilityRecord` schema owned by `f3d-core`, 05.1): enumerates `navigator.gpu.requestAdapter()` info, `adapter.features`, `adapter.limits`, `navigator.gpu.wgslLanguageFeatures`, preferred canvas format, `getContext('webgl2')` parameters for the reference lane, and the harness's host capability checks; emits JSON. Unknowns are `null` with a reason.\n- Thermal script: runs a case for the sustained duration with periodic throughput samples, logs device thermal state where the OS exposes it (recorded as unknown otherwise), enforces cooldown between paired runs.\n- Reserved-device rule: a lock file/procedure so that no swarm agent starts builds on the benchmark Mac during timing; the timing session records `uptime`/load and open GPU processes as evidence.\n- Fallback-adapter guard: the capability record includes `adapter.info` and the fallback flag (`isFallbackAdapter`); any scored or calibration run on a fallback/software adapter is invalid, logged at `error`, and the reserved-device checklist requires a non-fallback adapter and the pinned browser/OS build before a run starts (§14.4).\n\n## Success criteria\n\n- A capability record exists for every available stratum/browser; missing strata are listed with a plan and are **not** silently dropped from the required set.\n- Thermal and reserved-device procedures executed once on the phone and Mac with archived logs.\n\n## Test plan\n\n- Unit: capability-record schema tests (unknowns as null with reason; no derived VRAM field).\n- E2E: capability probe page executed on every available stratum/browser; the reserved-device checklist script run once on the phone and Mac with archived output; a thermal dry run of the sustained script driving an unmodified upstream example page directly (no harness; harness-driven sustained runs are 23.3) with cooldown enforcement.\n- Logging: capability JSON per device under `evidence/03.7/devices/`; thermal dry-run time series with thermal-state proxies; reserved-device evidence (`uptime`, open GPU processes).\n- Failure artifacts: probe page console capture on any error.\n\n## Considerations\n\n- Safari results apply only where Safari runs; Chrome on iOS is checked separately for its actual engine and capabilities.\n- Linux discrete-GPU lanes are recorded separately from Windows, never merged.\n","acceptance_criteria":"The four strata (iPhone 17 Pro Max Safari with Chrome checked separately; M5 MacBook Safari and Chrome; NVIDIA discrete GPU Chrome on Windows with Linux as a separate lane; AMD discrete GPU likewise) are provisioned or their unavailability is recorded as an outstanding obligation; a capability-record schema captures browser/OS build, adapter info, requested/enabled features, WGSL language features, limits, canvas format/color space and host capabilities with unknowns recorded as unknown; the reserved-device rule and the 20-minute sustained thermal procedure are documented and scripted.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:26.617231Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:14.981486Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-conformance","f3d-core","phase-0"],"dependencies":[{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.7","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.6","type":"blocks","created_at":"2026-09-09T16:48:36.527876Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.7","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt","type":"parent-child","created_at":"2026-09-09T16:46:26.617231Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-03-reference-profiles-and-bridge-tmt.8","title":"Statistical analysis pipeline: paired runs, block bootstrap, log-space aggregation","description":"## Full scope\n\nStatistical analysis pipeline: paired alternating runs, block-level bootstrap, log-space aggregation, cell and stratum confidence intervals, raw-run retention\n\n## Summary\n\nThe scoring arithmetic of §16.4 and §16.6 as one reproducible script, so nobody hand-computes a speedup.\n\n## Background (plan §16.4, §16.6, §15.6)\n\n\"Use independent paired runs with alternating/randomized order and adequate warmup.\" \"Bootstrap or otherwise analyze at the paired run/block level rather than treating adjacent correlated frames as independent experiments.\" \"For a cell, compute throughput from completed frames divided by actual elapsed time, then the candidate/reference ratio within matched blocks. Aggregate cell ratios in log space with equal, preregistered cell weights within each stratum. Report uncertainty at both cell and stratum levels.\" \"An inconclusive confidence interval requires a new preregistered batch, not repeated peeking.\" A candidate failure is a failed cell with ratio treated as a failure, never excluded.\n\n## Technical approach\n\n- Implemented in Rust inside `f3d-conformance` (shared with `f3d bench`; Python is permitted only for one-off notebooks that never gate a verdict); the analysis entry point `f3d bench analyze` reads inputs are the raw per-run JSON produced by the harness (03.1/03.2) for reference and candidate, with run metadata (device, browser build, order, warmup).\n- Per cell: block-level paired ratios; median; bootstrap lower/upper bounds at 95%; flags: `inconclusive` when the lower bound < 2× even if the median ≥ 2×.\n- Per stratum: geometric mean over required cells in log space with equal weights; bootstrap over blocks jointly; compare lower bound to 2× and point estimate to 3×.\n- Tails: p95/p99 frame time, missed deadlines, input-response latency from the on-screen companion runs; startup cold/warm; peak memory; control regression with uncertainty.\n- Denominator rule: the required cell set comes from the corpus manifest; a missing or failed candidate run produces an explicit `FAILED_CELL` row and the stratum result is `BLOCKED`.\n- Preserve raw runs; output both human-readable markdown and JSON.\n\n## Success criteria\n\n- Reference-vs-reference dry run yields ratios near 1 with intervals containing 1.\n- Injected synthetic data with a known 2.5× effect reproduces it within the interval.\n\n## Test plan\n\n- Unit: estimator tests on synthetic block data (known 2.5x effect reproduced within the interval; reference-vs-reference near 1); a property test that no flag can drop a failed cell; batch-label separation test.\n- E2E: end-to-end run on 03.3's calibration data producing the markdown and JSON reports.\n- Logging: the reports plus a `metric` event per cell `{cell, stratum, median_ratio, ci_low, ci_high, verdict}`; archived under `evidence/03.8/`.\n- Failure artifacts: the raw-run manifest referenced by any inconclusive verdict.\n\n## Considerations\n\n- Tuning data and hidden validation inputs stay separate (§16.4); the script takes an explicit `--batch` label so tuning batches are never mixed with scored batches.\n","acceptance_criteria":"A checked-in analysis script consumes raw paired run files and produces per-cell median ratio, paired 95% lower bound, stratum geometric mean with interval, p95/p99 frame-time, missed-deadline and input-response summaries, startup and memory summaries, and control-case regressions; it refuses to aggregate cells with mismatched configurations and never drops a failed candidate cell.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:26.701669Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:13.896377Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-conformance","phase-0"],"dependencies":[{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.8","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt","type":"parent-child","created_at":"2026-09-09T16:46:26.701669Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-03-reference-profiles-and-bridge-tmt.8","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.1","type":"blocks","created_at":"2026-09-09T16:48:36.680996Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-04-module-routing-exact-boundaries-6mv","title":"F3D-04: Full module/package routing and exact-component construction boundaries","description":"## What this epic delivers\n\nThe **production compatibility routing** the whole product stands on: coherent module identity for every entry point, the isolated exact-backend component for genuine WebGL contracts, the construction-time routing boundary, and the synchronous-API/bootstrap shape. Plan §19 F3D-04 depends on 01; acceptance: \"ESM/CJS/addon/source identities; real GL/sync read; no mandatory GPU for CPU/DOM imports\".\n\n## Why (plan §3.3, §3.4, §4.7, §5.6, §6.9, §18 Phase 0, §21)\n\nVersion 2.0's central architectural change is \"one optimized backend, complete compatibility routing\": the accelerated backend is WebGPU-only, but the *product* retains the exact backend behavior needed for complete compatibility. `getContext()` returning a real WebGL context, shared-context `resetState()`, synchronous render-target readback, native program handles and construction-time XR fallbacks cannot be faked. The route is chosen **before** binding a canvas/context/device and is irreversible for that renderer lifetime. Phase 0 must \"build the ordinary production compatibility routing and smoke it against package entry points, an actual GL context/sync read, CSS/SVG, a runtime loader/exporter, and existing editor/Inspector integration\"; \"the first compatibility component may retain source wholesale; its presence is not an acceleration result.\"\n\n## Children\n\n1. Module graph resolution and JS ingestion adapter (Rollup + Acorn-class parser)\n2. Export routing and compatibility facade for every entry point; sync CJS/Node; no GPU for CPU imports\n3. Exact-backend component: pinned WebGLRenderer/WebGLBackend isolated with the genuine GL contract\n4. Renderer-construction routing hook: irreversible route selection and connected groups\n5. Bootstrap and synchronous API shape with startup-boundary and activation tests\n6. Phase 0 compatibility-route smoke suite (entry points, GL sync read, CSS/SVG, loader/exporter, editor)\n7. H1 through the compatibility boundary, unaccelerated (Section 21 dress rehearsal for F3D-11)\n\n## Success criteria for the epic\n\nAll children closed; the routing report lists static route decisions; `f3d-conformance`'s exact lane can assert real native objects; the retained-component byte count is reported separately (§13.5).\n\n## No-claim boundary\n\nExact-backend execution is never counted as the new renderer's acceleration. A compatibility-only wrapper is an early correctness baseline; it cannot pass the product gate (§18 Phase 0 exit).\n","acceptance_criteria":"All ESM/CJS/addon/source import paths resolve through one coherent versioned adapter set with preserved identities; a real GL context and synchronous read work through the pinned exact backend component selected at construction; CPU-only, DOM-only, and Node imports never require WebGPU or Wasm initialization; the Phase 0 compatibility-route smoke suite passes.","status":"open","priority":0,"issue_type":"epic","created_at":"2026-09-09T16:46:22.575442Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:27.194963Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-compiler","f3d-runtime","js-host","phase-0","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.3","type":"blocks","created_at":"2026-09-09T16:48:38.219586Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-04-module-routing-exact-boundaries-6mv.1","title":"Module graph resolution and JS ingestion adapter (Rollup + Acorn-class parser)","description":"## Full scope\n\nModule graph resolution and JS ingestion adapter (pinned Rollup-ecosystem + Acorn-class parser) producing a structured module representation for the Rust compiler\n\n## Summary\n\nThe narrow, bounded JavaScript frontend. The compiler understands Three.js semantics and a restricted numeric subset; it does not reimplement JavaScript. This bead gives Rust a faithful module graph to analyze.\n\n## Background (plan §4.1, §4.7, §3.1, §10.1; AGENTS.md \"Dependency Policy\")\n\n- \"Use the existing project's build process to normalize TypeScript, JSX, and module syntax when present. A narrow build-time JavaScript parser/helper supplies a structured module representation to the Rust compiler. Initial preference: the existing Rollup ecosystem plus an Acorn-class parser, pinned and audited during Phase 0.\"\n- \"Resolve the actual module graph, including relative imports, import maps, addon-internal imports, dynamic imports with a finite closure, and the project's existing build transforms. Preserve ES-module singleton identity and live bindings.\" \"Do not deduplicate two independently intended application instances merely because their source bytes happen to match.\"\n- Existing build tools are thin frontend adapters to the same compiler (§3.2).\n\n## Technical approach\n\n- `tools/ingest/` (JS/TS, pinned): entry discovery from HTML (`<script type=\"module\">`, import maps, `<link rel=modulepreload>`), from a module path, or from a package entry; resolution through import maps, `node_modules` for the pinned `three` package, relative paths, and `new URL(..., import.meta.url)` patterns for assets (recorded for F3D-10).\n- Per module: normalized ESM AST summary (imports with specifiers and live-binding usage, exports, top-level side effects, dynamic `import()` call sites with argument shape classification: literal / template with finite enumerable values / unresolvable), class declarations and prototype writes, and source spans for everything.\n- Output format: a versioned JSON (or compact binary) \"module graph bundle\" with content hashes per module; Rust side (F3D-12a.1) owns the typed reader. The exact schema version is part of the build cache key (§10.6).\n- Identity: each module instance gets a stable ID keyed by resolved URL (not bytes); a note records when two URLs have identical bytes so the report can mention it without merging.\n- If the project has its own build (Vite/webpack/tsc), run it first via a documented hook and ingest its output; do not invent a TS frontend.\n\n## Success criteria\n\n- Ingests H1, H2, and the Sponza example completely (all `three/addons/*`, `three/webgpu`, `three/tsl`, relative imports) with zero unresolved static imports; dynamic imports classified.\n- Round-trip fidelity: re-emitting the ingested modules must load and run without error here; the harness-trace equality check (proving nothing semantic was lost) is executed by 04.7 once the harness (03.1) exists.\n\n## Test plan\n\n- Unit: resolver fixtures (import maps, circular imports, live bindings mutated after import, finite dynamic import, variable dynamic import, same bytes at two URLs); schema-version check for the module-graph bundle.\n- E2E: ingest H1, H2 and Sponza with zero unresolved static imports; re-emit the ingested modules and assert they load and run without error in the browser (the harness-trace equality check runs in 04.7).\n- Logging: per-module `{url, resolver_step, hash, imports, dynamic_imports: [classification], spans}`; unresolved imports at `error`; archived under `evidence/04.1/`.\n- Failure artifacts: the unresolved-import report and the trace diff for round-trip failures.\n\n## Considerations\n\n- A modified addon loses optimization recognition but stays executable (§4.7); the adapter records upstream fingerprints (blob identity) for recognition, it does not reject modifications.\n- The ingestion toolchain is build-time only; it must never be a dependency of the deployed Wasm (§17.3).\n","acceptance_criteria":"Given an HTML/import-map application or module entry point, the adapter resolves the complete module graph (relative imports, import maps, addon-internal imports, dynamic imports with a finite closure, the project's existing TS/JSX build transforms) and emits a versioned structured representation consumed by Rust; ES-module singleton identity and live bindings are preserved; two independently intended application instances are never deduplicated by byte equality; unresolvable dynamic imports are reported with source spans.","notes":"2026-09-10 user-authorized critical implementation wave. Scope: Real module ingestion for H1/H2. Start implementation while dependencies await verification; existing acceptance conditions and dependency edges remain binding for closure. Owned paths: tools/ingest/** and tools/ingest tests only","status":"in_progress","priority":0,"issue_type":"task","assignee":"RubyCrane","created_at":"2026-09-09T16:46:26.788781Z","created_by":"jemanuel","updated_at":"2026-09-12T20:19:29.826572Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","js-host","phase-0","tier-solid"],"dependencies":[{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.1","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.1","type":"blocks","created_at":"2026-09-09T16:51:25.957584Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.1","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.2","type":"blocks","created_at":"2026-09-09T16:48:36.982889Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.1","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv","type":"parent-child","created_at":"2026-09-09T16:46:26.788781Z","created_by":"jemanuel","metadata":"{}","thread_id":""}],"comments":[{"id":1,"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.1","author":"OrangePelican","text":"2026-09-12 OrangePelican review: finite dynamic-import bundling preserves expression evaluation and external data/http/https imports; module graph records external URLs without fake analyzed nodes and drops closure claims through transitive/cyclic dependencies. Routing retains upstream for unanalyzed external modules. Combined RCH batch104: bundler + module_graph + build_application + route_bridge, 114 PASS, 0 failed/skipped, remote exit0 at19:54:06Z. Graph70b5be13/bridge1e3509cf/decider250f3e32 verified against worker. This is partial implementation; full bead acceptance remains open. Next bounded slice: preserve external HTML module root scripts in emitted networked output.","created_at":"2026-09-12T19:56:51Z"},{"id":4,"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.1","author":"OrangePelican","text":"2026-09-12 OrangePelican: external HTML module root scripts now survive emitted networked output with original attributes/order; local entries are bundled, all-external pages emit no fake chunks. URL canonicalization keeps the rewriter and bundler aligned. RCH batch105 all118 tests PASS,0 failed/skipped, remote20:06:30Z; all11 source hashes recorded in-job. Bundler8b332461/build_applicationec422265. No remote HTTP execution or full ingestion closure claimed.","created_at":"2026-09-12T20:19:29Z"}]}
{"id":"f3d-04-module-routing-exact-boundaries-6mv.2","title":"Export routing and compatibility facade for every entry point; sync CJS/Node; no GPU for CPU imports","description":"## Full scope\n\nExport routing and compatibility facade for every entry point (three, three/webgpu, three/tsl, three/addons, wildcard addon/source paths, CommonJS) with coherent class identity and no GPU/DOM access for CPU-only imports\n\n## Summary\n\nThe generated export routing layer that makes \"retained public modules plus generated export routing\" real for the package/module API family (§5.12 row 1) while keeping module identity coherent.\n\n## Background (plan §3.4, §4.7, §5.2, §6.9, §14.2; AGENTS.md \"Bootstrap\")\n\n- \"`three`, `three/webgpu`, `three/tsl`, and relevant `three/src/*` paths must resolve through a coherent versioned adapter set, not accidentally instantiate incompatible duplicate cores.\" \"Keep class/prototype identities coherent across retained modules and the facade.\"\n- \"Conservatively retain synchronous JS implementations for CommonJS/Node utility entry points until equivalent synchronous Wasm availability is established. Do not make `require('three')` return a Promise. Do not access `window`, `document`, or `navigator.gpu` just because a server imported `Vector3`.\"\n- \"Avoid loading a second accidental core copy, globally patching shared prototypes, or changing `instanceof`/constructor/prototype observations. Exact components use the actual pinned implementation and the same source-compatible object graph; generated optimized replicas remain private.\"\n\n## Technical approach\n\n- Generate (from the manifest, 01.3) a routing table: for each export path and symbol, the production owner module: retained upstream module (default for everything at this stage), facade module (later, for objects with packed replicas), or exact component. Emit static ESM adapter modules and a CJS adapter that re-export the same singleton objects.\n- Match upstream's identity topology exactly (plan §4.7: never deduplicate independently intended instances, never create accidental duplicates): the ESM entry points (`three`, `three/webgpu`, `three/tsl`) and addons share the pinned `build/three.core.js` instance, and the exact backend component (04.3) shares that same instance; the CommonJS adapter re-exports the pinned CJS build as its own instance exactly as upstream does; `three/src/*` imports remain separate modules as upstream (the test-only mapping for upstream unit tests is 18a.1). The identity test asserts sameness where upstream is same (`Object.getPrototypeOf(new Mesh())` chains across `three`, `three/webgpu` and addon imports) and distinctness where upstream is distinct.\n- Environment gating: CPU/DOM/Node imports must not import the runtime/host binding modules; use the ingestion graph to prove a CPU-only entry has no path to WebGPU/Wasm initialization; test in Node (CJS + ESM) that importing math/exporters works headless.\n- Recording: the routing layer writes (at build time) `resolved implementation per import` into the build report (§3.5, §14.2).\n- Version fingerprints: recognized upstream modules carry the pinned blob identity; a mismatched revision routes to its actual retained component with an \"unverified version\" status, never a name-matched shim (§4.7, §5.17).\n\n## Success criteria\n\n- All export paths from 01.1's reconciliation import successfully in browser and (where upstream supports) Node; identity tests pass; environment-gating tests pass.\n- Zero duplicated core modules in the ingested graph of H1/H2 through the facade.\n\n## Test plan\n\n- Unit: routing-table generation tests from a manifest fixture; duplicate-core detector test; environment-gating test proving a CPU-only entry has no import path to host binding modules.\n- E2E: generated import-every-path test (browser + Node CJS/ESM) over the reconciled export list; identity tests (`instanceof`, prototype chain) across `three`, `three/webgpu` and addon imports asserting sameness where upstream is same and distinctness where upstream is distinct; Node headless import of math/exporters: import succeeds, and execution parity in Node matches upstream's own Node behavior including its limitations.\n- Logging: per-import `{path, resolved_module, owner, revision}` (this is also the build report's resolved-implementation record); duplicate-core findings at `error`; archived under `evidence/04.2/`.\n- Failure artifacts: the failing import path with its resolution chain.\n\n## Considerations\n\n- Do not replace public data properties with accessors where descriptors/enumeration/external references can observe it (§4.8); the facade at this stage is pure re-export.\n- Deprecated aliases still shipped in r186 remain routed with their warning behavior.\n","acceptance_criteria":"Every export path in the reconciled package manifest resolves through a coherent versioned adapter set; instanceof/constructor/prototype observations are unchanged; require('three') stays synchronous; importing Vector3, an exporter, or a CSS renderer touches neither window/document/navigator.gpu nor Wasm instantiation; no accidental second core copy is loaded; the routing report records the resolved implementation for every import.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:26.873317Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:09.623134Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","f3d-runtime","js-host","phase-0","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.2","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.3","type":"blocks","created_at":"2026-09-09T17:44:09.622728Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.2","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv","type":"parent-child","created_at":"2026-09-09T16:46:26.873317Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.2","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.1","type":"blocks","created_at":"2026-09-09T16:48:37.284003Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-04-module-routing-exact-boundaries-6mv.3","title":"Exact-backend component: pinned WebGLRenderer/WebGLBackend isolated with the genuine GL contract","description":"## Full scope\n\nExact-backend component: pinned upstream WebGLRenderer/WebGLBackend isolated, dependency-pruned, license-attributed, exposing the genuine GL contract (getContext, extensions, sync readback, resetState, GLBufferAttribute, context loss/restore)\n\n## Summary\n\nPreserve what cannot be faked. This is the \"exact backend component\" execution route of §3.3: pinned upstream WebGLRenderer/WebGLBackend machinery required by genuinely opaque native-backend contracts, isolated and reported.\n\n## Background (plan §2.9, §3.3, §5.6, §5.12 \"WebGL renderer contract\", §6.9, §13.5, §14.9)\n\n- Preserve constructor `context`/`canvas` options, `getContext()`, `getContextAttributes()`, extensions/capabilities, native programs/resources exposed by diagnostic/state APIs, shared-context `resetState()`, GL buffer attributes, synchronous and asynchronous pixel reads, copy operations, context loss/restoration, canvas capture/preservation.\n- \"Synchronous readback is not implemented as asynchronous WebGPU mapping disguised behind the same method name.\" \"Never fabricate a native handle.\"\n- The component is a **separate, demand-loaded compatibility dependency**, not another Rust GPU abstraction. It retains its license and provenance; the code-count report lists it as retained upstream, never as Rust.\n- Backend-preserving execution also covers legitimate custom `Backend`/`Renderer` subclasses and WebXR configurations that require a GL-backed session (`WebGLXRFallback` constructs and installs a new renderer at construction time).\n\n## Technical approach\n\n- Package the pinned `WebGLRenderer` tree and the `WebGLBackend` of `three/webgpu` as an internal module group `compat/exact-gl/` that imports the shared core (04.2), pruned by dependency closure to what those paths need; keep upstream file structure and notices intact.\n- Expose construction entry points used by the routing hook (04.4): `createExactWebGLRenderer(options)` and `createExactBackendForRenderer(rendererOptions)`; both return the actual upstream objects (no wrapper proxies) so identity/prototype observations hold.\n- Connected resource groups: mark every resource (textures, render targets, geometries) that touches an exact renderer as belonging to that renderer's compatibility group; cross-group sharing with the new backend uses real copies only (§6.9), never zero-copy interchange.\n- Tests for the genuine contract (seed of F3D-18b.5): construction with a caller-supplied `WebGL2RenderingContext` (the constructor `context` option) asserting it is used verbatim; context identity across calls; `readRenderTargetPixels` synchronous visibility; `resetState()` after raw GL interleaving; extension queries; `GLBufferAttribute` with a user-created buffer; forced context loss via `WEBGL_lose_context` and restoration; `preserveDrawingBuffer` capture.\n- Report: byte size, module list, license text path, upstream commit; surfaced in the build report's retained-component section.\n\n## Success criteria\n\n- A direct construction test renders a reference scene through this component identically to upstream (screenshot lane); the H1 WebGL setting itself is verified in 04.7.\n- Contract tests pass with real native objects asserted (`instanceof WebGL2RenderingContext`, real `WebGLProgram` handles from the diagnostic API).\n\n## Test plan\n\n- Unit: component manifest test (file list, bytes, license path, upstream commit present).\n- E2E: contract tests on Chrome and Safari: `getContext()` identity, `readRenderTargetPixels` synchronous visibility, `resetState()` after raw GL interleaving, extension queries, `GLBufferAttribute` with a user buffer, forced context loss/restore, `preserveDrawingBuffer` capture; a negative test asserting a Promise-returning readback would fail; H1 WebGL setting screenshot parity with upstream.\n- Logging: per-contract events with the observed native object types and values, tagged `owner: exact-backend`; archived under `evidence/04.3/`.\n- Failure artifacts: screenshots and the GL error log.\n\n## Considerations\n\n- This component never satisfies the accelerated-rendering gate; the forced new-backend lane (F3D-18a.4) disables it and must still pass for portable families.\n- Do not fork or \"improve\" the retained code; behavior parity with the oracle is the value.\n","acceptance_criteria":"A production component wraps the pinned upstream WebGLRenderer (and the WebGL backend of the modern renderer) as an isolated, version-pinned, dependency-pruned module sharing the single core; getContext() returns the real WebGL2 context, readRenderTargetPixels is synchronous and visible on return, resetState/extension queries/native program handles/GLBufferAttribute/context loss and restore behave exactly as upstream; the component's bytes, license and provenance are reported separately.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:26.962115Z","created_by":"jemanuel","updated_at":"2026-09-09T17:35:42.976012Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-runtime","phase-0","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.3","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv","type":"parent-child","created_at":"2026-09-09T16:46:26.962115Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.3","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:48:37.435176Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-04-module-routing-exact-boundaries-6mv.4","title":"Renderer-construction routing hook: irreversible route selection and connected groups","description":"## Full scope\n\nRenderer-construction routing hook: irreversible route selection before canvas/context/device binding, connected compatibility groups, static route report\n\n## Summary\n\nThe small integration layer (inside `f3d-compiler`, `f3d-runtime`, and existing host code) that selects an execution route per renderer at a sound source boundary. It is **not** another renderer abstraction with per-draw backend dispatch.\n\n## Background (plan §3.3, §5.6, §6.9, §3.4 \"When deferral is not equivalent\"; AGENTS.md \"The exact-backend boundary is irreversible\")\n\n- \"Backend selection happens at a sound source boundary, normally renderer construction. The unit may be an entire connected renderer/resource group rather than a single draw.\"\n- \"Choose a renderer route before binding its canvas/context/device or exposing native handles. Treat it as an irreversible semantic decision for that renderer lifetime, unless the source itself supports creating/replacing the renderer at a tested boundary. Revocable numeric-island guards are not enough to justify a later switch between WebGPU and WebGL on the same exposed canvas.\"\n- \"A late method call is not permission to swap canvases, lose state, replay side effects, or synchronously block on a browser Promise.\"\n- H1: selecting WebGL must produce a genuine GL-backed renderer through the exact component; selecting WebGPU uses the new renderer.\n\n## Technical approach\n\n- Define the route enum and the decision inputs in `f3d-core`/`f3d-compiler`: source-declared backend (constructor type, `forceWebGL`, URL parameter branches like H1's), escape facts from 12a.6 (renderer/canvas/native state reaching opaque code), required native contracts (sync readback, `getContext` use, XR fallback), and host capability (WebGPU absent → exact GL where the source supports it).\n- Routing hook: the facade's renderer constructors call the router synchronously at construction; the router returns the constructed object of the chosen kind. Until the new backend exists (F3D-07), every route resolves to the exact component or the retained renderer, and that is recorded truthfully as retained/exact.\n- Connected groups: a union-find over renderer instances and resources (textures/targets/geometries) that are used by an exact renderer; any cross-use with a new-backend renderer requires an explicit copy path (later) and is flagged in the report now.\n- Irreversibility: the router records a per-renderer decision token; any attempt to re-route the same exposed canvas fails at build time when statically detectable, otherwise at construction time with the same diagnostic and span; source-supported replacement (a new renderer instance, `WebGLXRFallback`'s install) is a new decision.\n- Report: static route decisions with reasons and spans; runtime execution attribution hooks (which route actually rendered) for the ownership assertions in tests (§6.9 last paragraph).\n\n## Success criteria\n\n- H1's two backend settings resolve to exact-GL and (once available) new-WebGPU respectively, decided at construction, with the navigation-based reload producing a fresh decision.\n- A test that tries to switch a bound canvas between routes fails at build time with the correct span.\n- Report shows route per renderer and group membership.\n\n## Test plan\n\n- Unit: router decision-table tests (source-declared backend, escape facts, native contracts, host capability) covering every combination; irreversibility test (second decision on a bound canvas fails with a span); connected-group union tests.\n- E2E: H1's two backend settings resolve to exact-GL and the WebGPU route at construction; navigation reload produces a fresh decision; a fixture that attempts to re-route a bound canvas fails at build time.\n- Logging: route decision events `{site, span, route, reasons, group}` and runtime attribution events `{renderer, route, submissions}`; archived under `evidence/04.4/`.\n- Failure artifacts: the decision log and the offending source span.\n\n## Considerations\n\n- The general fallback is JavaScript/exact ownership, not an optimistic guess that an escaped object will not be modified (§5.3).\n- Multiple canvases/renderer instances have separate output and resource epochs (§8.5).\n","acceptance_criteria":"Renderer construction sites route to exactly one of: new WebGPU backend, general new backend, retained upstream renderer (the Phase 0/1 interim for the WebGPU setting, and always for CSS2D/CSS3D/SVG and other non-GPU renderers), or exact component, decided before any canvas/context/device binding or native-handle exposure, and the decision is irreversible for that renderer lifetime unless the source itself replaces the renderer at a tested boundary; resources shared with an exact renderer form a connected group that stays on that backend; static decisions and runtime executions are reported.","notes":"2026-09-10 user-authorized critical implementation wave. Scope: Irreversible renderer construction and escape routing. Start implementation while dependencies await verification; existing acceptance conditions and dependency edges remain binding for closure. Owned paths: tools/compat/** and tests/fixtures/routing/** only","status":"in_progress","priority":0,"issue_type":"task","assignee":"TopazRidge","created_at":"2026-09-09T16:46:27.049458Z","created_by":"jemanuel","updated_at":"2026-09-10T20:09:40.661316Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-compiler","f3d-runtime","new-backend","phase-0","tier-solid"],"dependencies":[{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.4","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv","type":"parent-child","created_at":"2026-09-09T16:46:27.049458Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.4","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.3","type":"blocks","created_at":"2026-09-09T16:48:37.595886Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-04-module-routing-exact-boundaries-6mv.5","title":"Bootstrap and synchronous API shape with startup-boundary and activation tests","description":"## Full scope\n\nBootstrap and synchronous API shape: source async startup boundary for device preparation, bounded deferred renders with per-use snapshots, startup-boundary and activation tests\n\n## Summary\n\nMake asynchronous preparation coexist with a synchronous public API without changing observable startup behavior.\n\n## Background (plan §3.4, §4.8, §23 [S35])\n\n- \"WebGPU device acquisition and Wasm instantiation are asynchronous preparation; many public constructors and operations are synchronous. Preserve the original module graph, live bindings, import identity, constructor/return types, side effects, callback registration, and source-required host activation.\"\n- \"CPU objects, utilities, controls, and the intended canvas can exist before accelerated device readiness, but bounded deferred renders require correct per-use snapshots, not merely a mutable scene pointer.\"\n- \"When deferral is not equivalent, use the exact compatible route from construction.\"\n- \"Media playback, pointer lock, fullscreen, XR session entry, and other activation-sensitive operations stay on their valid source host path. Replaying a captured event after awaiting initialization does not recreate transient activation.\"\n- \"Runtime failures must preserve the source API's error/cleanup contract; they must not produce a partially initialized facade that silently drops valid operations.\"\n\n## Technical approach\n\n\n- Startup analysis (operating on 04.1's module-graph bundle; re-hosted on the IR once 12a.1 exists): detect the source's own async boundary (`await renderer.init()`, `WebGPURenderer` usage patterns, `async function init()` with awaited loaders) and use it for device preparation; otherwise classify whether a pre-entry bootstrap is observation-equivalent (no synchronous canvas reads, no first-frame observation before the boundary, no activation dependence) and only then insert it; else route exact.\n- Deferred-render buffer: renders requested before readiness are recorded with per-use snapshots of the state they must observe (§6.3) and replayed at readiness in order; the buffer is bounded and the bound is reported.\n- Error contract: device acquisition failure surfaces through the source's error path (constructor throw or rejected init promise, matching upstream's shape), never a silent no-op renderer.\n- Test page suite covering the enumerated cases, with harness traces compared against the upstream reference behavior.\n- Sources authored against `WebGPURenderer` reproduce upstream's pre-init behavior exactly: `render()` before `init()` logs upstream's warning and returns the `renderAsync()` promise; `init()`, `renderAsync()`, `compileAsync()` and awaited loaders are the recognized source async boundaries. The bounded deferred-render buffer applies only to WebGL-authored sources whose synchronous first render moves to the new backend under a proven-equivalent bootstrap.\n\n## Success criteria\n\n- H1 and H2 start under the candidate with identical observable startup sequences (trace equality at the checkpoints) on both browsers.\n- Activation tests: pointer lock/fullscreen/media play invoked from a real user gesture succeed in the same conditions as upstream; a replayed post-await event does not falsely succeed.\n\n## Test plan\n\n- Unit: startup-analysis classifier tests (source async boundary detected; pre-entry bootstrap admitted only when observations allow); deferred-render buffer bound and snapshot tests.\n- E2E: one page per enumerated case (module cycles, top-level await, scripts before/after entry, DOMContentLoaded, immediate canvas access, first input, init error) plus activation cases (pointer lock, fullscreen, media play from a real gesture; replayed post-await event must not succeed); traces compared to upstream on both browsers.\n- Logging: startup timeline events `{phase: module-eval|bootstrap-start|device-ready|deferred-render|replay, ts_wall, ts_app}` and activation outcomes; archived under `evidence/04.5/`.\n- Failure artifacts: trace diff and console capture.\n\n## Considerations\n\n- Startup measurements include required compatibility code and all preparation (§3.4); the numbers feed 03.6's budgets and the cold-start report.\n","acceptance_criteria":"Public constructors and operations stay synchronous; accelerated device acquisition and Wasm instantiation use the source's existing asynchronous startup boundary when available and a compiler-owned pre-entry bootstrap only when the application's observations admit the delay; deferred renders before device readiness use correct per-use snapshots; tests cover module cycles, top-level await, scripts before/after entry, DOMContentLoaded, immediate canvas access, first user input, initialization errors, and activation-sensitive operations staying on their source host path.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:27.136737Z","created_by":"jemanuel","updated_at":"2026-09-09T17:35:41.744423Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","f3d-runtime","js-host","phase-0","testing"],"dependencies":[{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.5","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv","type":"parent-child","created_at":"2026-09-09T16:46:27.136737Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.5","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:48:37.753406Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-04-module-routing-exact-boundaries-6mv.6","title":"Phase 0 compatibility-route smoke suite (entry points, GL sync read, CSS/SVG, loader/exporter, editor)","description":"## Full scope\n\nPhase 0 compatibility-route smoke suite: package entry points, real GL context and sync read, CSS/SVG page without WebGPU, a runtime loader/exporter, editor/Inspector integration\n\n## Summary\n\nThe Phase 0 deliverable \"compatibility-route smoke evidence\" (§18 Phase 0). Small, real, and honest: it demonstrates that composing retained and exact components through the routing layer preserves functionality, and nothing more.\n\n## Background (plan §3.3 \"No WebGPU prerequisite for unrelated functionality\", §5.14, §18 Phase 0, §21)\n\n\"A CSS/SVG-only scene, numerical utility, loader/exporter, WebAudio component, or upstream-supported WebGL application must continue to function when WebGPU is absent. Select the source-compatible host route and avoid initializing unused GPU/Wasm services.\" Phase 0: \"smoke it against package entry points, an actual GL context/sync read, CSS/SVG, a runtime loader/exporter, and existing editor/Inspector integration.\"\n\n## Technical approach\n\n- Tests (browser automation, Chrome + Safari; Node for CJS):\n  1. Import-all-entry-points (reuse 04.2's generated test) in browser and Node.\n  2. Exact GL: construct via the facade with the WebGL route, render a reference scene, `readRenderTargetPixels` synchronously, compare to upstream output; assert `getContext()` identity.\n  3. CSS/SVG: run 21a.1's smoke script as part of this suite (a page using `CSS3DRenderer` and `SVGRenderer` with `navigator.gpu` removed; no Wasm instantiation, instrumented at the loader; DOM/SVG output matches upstream structurally).\n  4. Loader/exporter: load a glTF not present at build time via the routed `GLTFLoader`, mutate a transform, export via `GLTFExporter`, re-import, compare semantics (seed of F3D-19's round trip).\n  5. Editor and Inspector: run 21a.3's smoke workflow as part of this suite (upstream editor against the routed `three` build: import a model, edit a material, undo, export; Inspector on H1 through the routed build with its panels populated).\n- Every result records `owner` (retained-js / exact-backend) so nothing here is mistaken for new-backend coverage (§14.3).\n\n## Success criteria\n\n- All five pass on both browsers with owner tags; the suite runs in under a few minutes and is part of the functional lane going forward.\n\n## Test plan\n\n- Unit: none new (smoke suite).\n- E2E: the five smoke scripts (`tests/e2e/smoke/*.mjs`) on Chrome and Safari plus Node for CJS: entry-point imports; exact GL render + sync read compared to upstream; CSS/SVG page with `navigator.gpu` removed asserting no Wasm instantiation; runtime glTF load → mutate → export → re-import; editor and Inspector operations.\n- Logging: each test emits `owner` tags and a summary line stating \"0 new-backend cases\" at this stage; archived under `evidence/04.6/`.\n- Failure artifacts: screenshots, DOM dumps, exported files.\n\n## Considerations\n\n- Do not expand this into a large harness. It is a smoke suite; F3D-18 grows the real lanes.\n","acceptance_criteria":"A small automated suite proves the production routing works end to end before any acceleration exists: every package entry point imports; an exact GL renderer renders and reads back synchronously; a CSS2D/CSS3D/SVG-only page runs with WebGPU disabled and no Wasm instantiation; one loader (GLTFLoader with a runtime asset) and one exporter (GLTFExporter) work through the routed modules; the upstream editor and the Inspector load and operate against the routed build.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:27.223732Z","created_by":"jemanuel","updated_at":"2026-09-09T17:35:41.923594Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-conformance","phase-0","retained-js","testing"],"dependencies":[{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.6","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv","type":"parent-child","created_at":"2026-09-09T16:46:27.223732Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.6","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.4","type":"blocks","created_at":"2026-09-09T16:48:37.911423Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.6","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.5","type":"blocks","created_at":"2026-09-09T16:48:38.064298Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.6","depends_on_id":"f3d-21a-early-host-tool-integrations-ff1.1","type":"blocks","created_at":"2026-09-09T17:31:50.270011Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.6","depends_on_id":"f3d-21a-early-host-tool-integrations-ff1.3","type":"blocks","created_at":"2026-09-09T17:31:50.477608Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-04-module-routing-exact-boundaries-6mv.7","title":"H1 through the compatibility boundary, unaccelerated (Section 21 dress rehearsal for F3D-11)","description":"## Full scope\n\nConnect the original, unmodified H1 (webgpu_performance_renderbundle) source to the candidate compatibility boundary before anything is fast: ingest it, route it through the facade with the retained renderer and the exact GL component, preserve object identity and every dynamic/control/navigation behavior, and prove the ingestion/ownership model is viable with trace equality against upstream.\n\n## Summary\n\nPlan §21 orders this explicitly: \"Next connect the original H1 source to the candidate compatibility boundary, even before it is fast. Preserve object identity and its dynamic/control behavior. This exposes whether the intended ingestion/ownership model is viable much earlier than a hand-authored Rust demo.\" It is the unaccelerated dress rehearsal for F3D-11.1 and the input that F3D-12a's H1 analysis works on.\n\n## Background (plan §21, §4.7, §15.3, §3.4)\n\nH1 reaches `three/webgpu`, `three/tsl`, addon controls, the Inspector, and a navigation-based backend selector; both backend settings are functional cases. Running it through routing with retained ownership answers, early and cheaply, whether module identity, live bindings, the exact-GL construction path and the startup shape hold for a real application.\n\n## Technical approach\n\n- Serve the unmodified H1 page through the routing facade in a dev mode (an import map mapping `three`, `three/webgpu`, `three/tsl` and `three/addons/*` to the routed modules; no packaging and no `f3d build`, which is 10.8) with no island admitted (all regions JavaScript-owned); the renderer routes to the retained WebGPU renderer for the WebGPU setting and to the exact component for the WebGL setting.\n- Run the H1 interaction script (01.6/03.1) through both settings including the query-string navigation; compare traces and checkpoint images against unmodified upstream H1 under the same harness.\n- Record ingestion facts needed later: module graph size, dynamic import classification, escaped references of the renderer/canvas (feeds 12a.6), Inspector hook points.\n- No performance claim; this is a functional route proof. The routing hook's decision log (04.4) is this bead's report subset; the full `f3d-report.json` is 12a.9.\n\n## Success criteria\n\n- Trace and checkpoint equality with upstream for every control and both backend settings on Chrome and Safari; `instanceof`/prototype checks across facade imports hold; the exact GL setting yields a real `WebGL2RenderingContext`.\n\n## Test plan\n\n- Unit: none new; relies on 04.1/04.2/04.4 unit suites.\n- E2E: `tests/e2e/h1/routed-unaccelerated.mjs` runs the full H1 control script on both settings and both browsers in the functional lane; a second script exercises the navigation reload path and asserts a fresh route decision.\n- Logging: harness events per checkpoint with `owner: retained-js` or `owner: exact-backend` and the observed vs expected state; capability record attached; run archived under `evidence/04.7/`.\n- Failure artifacts: image diffs and trace diffs on any mismatch; the routing report snapshot.\n\n## Considerations\n\n- Do not narrow H1 to make it pass; a failing control here is a routing/ownership defect and blocks F3D-11.\n","acceptance_criteria":"Unmodified H1 runs through the routing facade in dev mode (import-map routing, no packaging) with all regions JavaScript-owned, both backend settings functional (retained WebGPU renderer and exact GL component), navigation reload producing a fresh route decision, and trace/checkpoint equality with upstream on Chrome and Safari; no speedup is claimed.","notes":"2026-09-11: RubyCrane and TopazRidge implement H1 development routing by explicit user direction (Mail6725/6726); tracker claim remains blocked by prerequisites, which remain binding for closure.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T17:11:24.913631Z","created_by":"jemanuel","updated_at":"2026-09-11T01:18:48.571877Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-compiler","f3d-runtime","h1","phase-0","phase-1","retained-js","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.7","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.1","type":"blocks","created_at":"2026-09-09T17:11:25.644689Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.7","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv","type":"parent-child","created_at":"2026-09-09T17:11:24.913631Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.7","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.4","type":"blocks","created_at":"2026-09-09T17:11:25.067957Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.7","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.5","type":"blocks","created_at":"2026-09-09T17:11:25.256402Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-04-module-routing-exact-boundaries-6mv.7","depends_on_id":"f3d-21a-early-host-tool-integrations-ff1.2","type":"blocks","created_at":"2026-09-09T17:11:25.454877Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-05-ids-layouts-epochs-transport-vqa","title":"F3D-05: IDs, layouts, epochs and safe host/resource transport","description":"## What this epic delivers\n\nThe safety substrate every later crate depends on, plus the production bridge. Plan §19 F3D-05 depends on 02 and 03; acceptance: \"Growth/reentry/stale-handle/queue snapshots and separate native-context ownership\". Together with F3D-02, it is the evidence for kill gate 1 (Foundation: \"a legal WebGPU bridge\").\n\n## Why (plan §6.5, §6.6, §6.7, §6.8, §6.9, §7.5, §11.7, §13.1; AGENTS.md \"Core Invariants\", \"Mandatory Counterexample Suite\")\n\n- Handles carry index + generation plus a separate device generation; a delayed promise must never publish into a reused slot.\n- Rust struct layout is not WGSL layout; `AffineRows` is three `vec4<f32>` rows (48 bytes), `mat4x3<f32>` is 64 bytes; storage-array stride differs from dynamic-uniform-offset alignment; padding is generated from a shared schema and tested independently of the renderer.\n- The binary boundary has a small checked ABI: protocol version, opcode/length bounds, generations, offsets, bounded commands; no `u64` through a JS Number.\n- Borrowed Wasm memory views obey a lifetime contract: no application callback, memory growth, or scheduler re-entry while a Rust borrow is live; rebuild views on growth; copy when the boundary cannot be enforced. Safe Rust in the core does not make arbitrary JS access to linear memory sound.\n- Queue writes are not per-draw snapshots; the planner tracks per-use data versions. Error scopes are shared device state.\n\n## Children\n\n1. `f3d-core` typed IDs/handles, errors, capability record, feature/route manifest types, CONTRACT.md.\n2. Wire-layout schema, generated padding, AffineRows/mat4x3 and alignment tests.\n3. Checked ABI and fuzzing.\n4. Safe host transport and audited binding boundary.\n5. Ownership modes, epochs, per-use data version primitive.\n6. Production bulk WebGPU bridge: negotiation, first correct frame, error-scope discipline.\n7. First integrated counterexample suite.\n\n## Success criteria for the epic\n\nAll children closed; `f3d-core` and the runtime bridge have `CONTRACT.md` files stating purpose, invariants, ownership route, error model, determinism class, cancellation behavior, unsafe boundary (the named binding boundary only), feature flags, conformance tests and no-claim boundaries (AGENTS.md \"Documentation and Contracts\").\n\n## No-claim boundary\n\nA rendered first frame through the bridge is an execution proof, not a speedup.\n","acceptance_criteria":"f3d-core provides generation-checked handles with a separate device generation, a generated and tested wire-layout schema (AffineRows 48 bytes vs mat4x3 64 bytes), a small checked ABI, safe borrowed-memory transport with an enforced lifetime contract, and explicit ownership modes with epochs; the production bulk WebGPU bridge renders a correct first frame and passes the first integrated counterexample suite (growth/reentry/stale-handle/queue snapshots/separate native-context ownership) on Safari and Chrome.","status":"open","priority":0,"issue_type":"epic","created_at":"2026-09-09T16:46:22.656422Z","created_by":"jemanuel","updated_at":"2026-09-09T16:48:40.419626Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-core","f3d-runtime","kill-gate","layout-abi","phase-0","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa","depends_on_id":"f3d-02-asupersync-browser-execution-58j.3","type":"blocks","created_at":"2026-09-09T16:48:40.257371Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.5","type":"blocks","created_at":"2026-09-09T16:48:40.419335Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-05-ids-layouts-epochs-transport-vqa.1","title":"f3d-core: generation-checked handles, error model, capability record, manifest types, CONTRACT.md","description":"## Full scope\n\nf3d-core crate: generation-checked typed handles with device generation, structured error model, runtime capability record, feature/route manifest types, and CONTRACT.md\n\n## Summary\n\nThe bottom of the dependency direction (`core <- everything`). Small, typed, safe.\n\n## Background (plan §6.1, §6.5, §6.8, §12.2, §17.1, §17.3; AGENTS.md \"Resource identity and lifetime\", \"Documentation and Contracts\", \"Output Style\")\n\n- Typed IDs into owned arrays; no heap-allocated trait-object graphs in the hot path.\n- \"Distinguish CPU resource existence, GPU allocation, initialized content, submitted use, and eventual retirement.\" Handle state machine must represent these.\n- \"Counter wrap must retire/invalidate an identity domain rather than produce an ABA collision.\"\n- Capability record: browser/OS build, adapter info, requested/enabled features, WGSL language features, limits, canvas format/color space, host capabilities; unknowns are unknown.\n- The feature/route manifest (from 01.3) needs a typed representation so the compiler and conformance crates share one definition; `f3d-core` owns it (§17.1 \"single feature/route manifest\").\n- Core library code does not print casually; use structured tracing/report events.\n\n## Technical approach\n\n- `Handle<D>` newtype generic over a domain marker: `u32` index + `u32` generation (or a packed `u64` split into two `u32` words at the ABI; never a JS Number carrying 64 bits). `GpuHandle<D>` adds `DeviceGeneration`. Domains: object, geometry, attribute, material, texture, render target, pipeline variant, bundle, region, task publication.\n- `Arena<D, T>` with slot generations, retirement on wrap (domain epoch bump invalidates all outstanding handles of that domain, documented), and explicit lifecycle states for GPU-backed entries.\n- Error model: `F3dError` enum with source-span-capable diagnostics for compiler use and structured codes for runtime; conversion to the report format.\n- `CapabilityRecord` struct + serde (build/diagnostic serialization only, never per-frame); `f3d-core` owns this schema and exports it as JSON schema; 03.7's probe page conforms to it.\n- `FeatureManifest` types with route/status enums including all blocking states; `f3d-core` owns the schema (exported as JSON schema) and 01.3's generator conforms to it.\n- `CONTRACT.md` with the AGENTS.md-required sections.\n\n## Success criteria\n\n- Unit tests: stale handle rejected; generation mismatch rejected; wrap retires domain; arena reuse yields a different generation; capability record round-trips against checked-in fixtures; manifest round-trips against fixtures now and against the real `compatibility/three-r186.json` once 01.3 closes.\n- `cargo test` native and `cargo check --target wasm32-unknown-unknown` pass.\n- `f3d-core/CONTRACT.md` is written before any other crate depends on `f3d-core` (AGENTS.md \"Documentation and Contracts\"): purpose and position in the dependency direction, public types and semantics, invariants, implementation ownership route, error model, determinism and numeric-fidelity class, cancellation behavior, unsafe boundary if any, feature flags, conformance tests, and no-claim boundaries.\n\n## Test plan\n\n- Unit: handle encode/decode property tests (stale rejected, generation mismatch rejected, wrap retires domain, reuse yields new generation); arena lifecycle-state transition tests; capability record and manifest round-trip against the real `compatibility/three-r186.json`.\n- E2E: none (core crate); `cargo test` native and `cargo check --target wasm32-unknown-unknown` in the functional lane.\n- Logging: test-only `tracing` events for arena transitions in failing tests (observed vs expected state); archived under `evidence/05.1/`.\n- Failure artifacts: n/a.\n\n## Considerations\n\n- Keep it within the 10,000-line budget for `f3d-core` including tests; report the count.\n- No feature flags that silently disable existing application functionality (§17.3).\n","acceptance_criteria":"f3d-core compiles for wasm32 and native with #![forbid(unsafe_code)]; handles are (index, generation) newtypes per domain with a separate DeviceGeneration for GPU resources; decoding validates ranges and rejects stale/mismatched generations; errors are structured (no stdout prints); f3d-core owns the CapabilityRecord and FeatureManifest schemas (typed Rust plus an exported JSON schema) that 03.7's probe page and 01.3's generator conform to; CONTRACT.md is written before any other crate depends on it.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:27.308250Z","created_by":"jemanuel","updated_at":"2026-09-09T17:35:42.251614Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-core","layout-abi","phase-0","tier-solid"],"dependencies":[{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.1","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.2","type":"blocks","created_at":"2026-09-09T16:48:38.375587Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.1","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa","type":"parent-child","created_at":"2026-09-09T16:46:27.308250Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-05-ids-layouts-epochs-transport-vqa.2","title":"Wire-layout schema with generated padding; AffineRows 48B vs mat4x3 64B; alignment rules","description":"## Full scope\n\nWire-layout schema with generated padding: AffineRows (three vec4 rows, 48 bytes) vs WGSL mat4x3 (64 bytes), storage stride vs dynamic-uniform-offset alignment, writeBuffer/writeTexture/copy alignment rules, round-trip tests independent of the renderer\n\n## Summary\n\nEliminate an entire class of silent corruption before any renderer exists. \"Layout is explicit, generated, and tested.\"\n\n## Background (plan §6.6, §6.8, §7.5, §23 [S34], [S36], [S45], [S46]; AGENTS.md \"Layout is explicit\")\n\n- \"'3×4 matrix' is a mathematical description, not a wire layout. Define `AffineRows` as three `vec4<f32>` rows, with translation in each row's fourth component: 48 bytes for the GPU record, with row dot-products against `(x,y,z,1)`.\"\n- \"WGSL's `mat4x3<f32>` is four padded three-component columns and occupies 64 bytes under its layout rules; writing twelve tightly packed floats into that type is wrong.\"\n- \"The schema must distinguish storage-array stride from the device's dynamic-uniform-offset alignment. Validate size/stride/round-trip tests independently of the renderer.\"\n- \"`writeBuffer` uses four-byte copy alignment and its data offsets are in typed-array elements or bytes according to input type; encoded buffer-to-texture copies have a separate row-pitch constraint. Do not apply encoder padding rules blindly to `writeTexture`.\"\n- \"Padding for vectors, matrices, array strides, uniforms, and storage buffers must be generated and tested from a shared schema.\"\n\n## Technical approach\n\n- In `f3d-core`, a tiny layout DSL (Rust data, not a new language): scalar/vector/matrix/array/struct kinds with WGSL address-space rules (uniform vs storage) computing size/alignment/offsets per the WGSL spec snapshot pinned in §23 [S36]. Generate: Rust `#[repr(C)]`-free safe writer/reader functions (byte-level, checked), and WGSL struct source text for `f3d-shader`.\n- Define the first record types: `AffineRows`, `ProjectiveMat4` (full 4×4 for non-affine inputs, §7.5), per-object material parameter block, instance record, indirect draw args.\n- Encode operation-specific alignment: `write_buffer_alignment = 4`, texture copy `bytesPerRow` multiple-of-256 rule for encoder copies only, `writeTexture` data-layout rule.\n- Tests: byte-exact expected layouts for each record; conversion from a column-major `Matrix4.elements` array to `AffineRows` and back; a deliberate `mat4x3` fixture that must be rejected by the writer; storage-array stride, `minUniformBufferOffsetAlignment` and `minStorageBufferOffsetAlignment` demonstrated as three distinct quantities with a device-limit fixture (256 for both alignments by default; dynamic storage-buffer offsets use the storage alignment).\n\n## Success criteria\n\n- Generated WGSL compiles under Naga validation (test-only) and the offsets Naga computes equal the schema's.\n- All layout tests pass; the record catalog is documented in the crate's `CONTRACT.md` with byte offsets.\n\n## Test plan\n\n- Unit: byte-exact layout tests per record (AffineRows 48 bytes with the row mapping; ProjectiveMat4; material block; instance record; indirect args); `mat4x3` fixture rejected; storage stride vs uniform offset alignment fixture (256); Naga validation of the generated WGSL with offsets asserted equal to the schema's.\n- E2E: in 05.6, an AffineRows upload with readback of a transformed vertex proves the row convention end to end on both browsers.\n- Logging: `--dump-layouts` test helper prints the layout table; mismatches log field, expected offset, actual offset; archived under `evidence/05.2/`.\n- Failure artifacts: the layout dump.\n\n## Considerations\n\n- Keep the full projective representation for non-affine inputs; do not discard the fourth row (§7.5).\n- Feature names, WGSL directives and binding APIs are versioned together (§12.2); the schema carries a version used in the build cache key.\n","acceptance_criteria":"One schema source generates both the Rust layout descriptors and the WGSL struct declarations for every GPU record type; size/stride/offset tests validate AffineRows = 48 bytes with rows [e0,e4,e8,e12], [e1,e5,e9,e13], [e2,e6,e10,e14] for a column-major Three.js matrix e; mat4x3 is proven to be 64 bytes and rejected as a target for tightly packed 12-float writes; storage-array stride, minUniformBufferOffsetAlignment and minStorageBufferOffsetAlignment are three distinct schema quantities; writeBuffer 4-byte alignment and copy row-pitch rules are encoded and tested.","notes":"2026-09-10 user-authorized critical implementation wave. Scope: Correct GPU wire layout and byte encoding. Start implementation while dependencies await verification; existing acceptance conditions and dependency edges remain binding for closure. Owned paths: crates/f3d-core/src/layout.rs and crates/f3d-core/tests/layout_tests.rs only","status":"in_progress","priority":0,"issue_type":"task","assignee":"RusticRobin","created_at":"2026-09-09T16:46:27.394086Z","created_by":"jemanuel","updated_at":"2026-09-10T20:09:30.452865Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-core","layout-abi","phase-0","tier-solid","wgsl"],"dependencies":[{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.2","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa","type":"parent-child","created_at":"2026-09-09T16:46:27.394086Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.2","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.1","type":"blocks","created_at":"2026-09-09T16:48:38.526583Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-05-ids-layouts-epochs-transport-vqa.3","title":"Checked ABI for the Wasm/JS boundary plus fuzzing of handles, layouts and plans","description":"## Full scope\n\nSmall checked ABI for the Wasm/JS boundary: protocol version, opcode and length bounds, device/scene generations, checked offsets, bounded command set, no u64 through Number, wrap retirement; fuzzing of handle decoding, layout validation and serialized plans\n\n## Summary\n\nDefine the one small binary protocol between Rust and the host adapter, and make it impossible to decode unsafely.\n\n## Background (plan §6.4, §6.8, §14.7; AGENTS.md \"Unsafe Code\")\n\n- \"Define protocol version, opcode and length bounds, device/scene generation, buffer offsets and lengths, and a bounded set of commands. Validate ranges before obtaining views or submitting work. Do not pass a Rust `u64` handle through a JavaScript Number when it may exceed exact integer range: use checked smaller IDs, two words, or a deliberate BigInt boundary.\"\n- \"Fuzz asset-pack parsing, shader preprocessing/translation, handle decoding, buffer layout validation, and serialized execution plans.\"\n- Use safe byte serialization and checked IDs rather than unchecked layout casts.\n\n## Technical approach\n\n- `f3d-core::abi`: `PacketHeader { version, device_generation, scene_generation, command_count, byte_length }`; command enum with fixed-size encodings (write-buffer range, copy, set-bind-group with dynamic offsets, draw/draw-indexed/indirect, dispatch, begin/end pass with attachment descriptors by handle, execute-bundles by handle, submit, publish-epoch). Every field bounds-checked against the packet length and the current arenas.\n- Encoder in Rust (safe, `Vec<u8>` writer with checked arithmetic) and a mirror decoder in Rust used for tests; the JS decoder (05.6) is generated from the same table so both sides agree.\n- Handles: `(index: u32, generation: u32)` pairs; generation domains and device generation checked on decode; a wrapped domain rejects all older handles.\n- Fuzzing: `cargo-fuzz` (test-only) targets for packet decode, handle decode, layout validation (05.2), and later serialized execution plans; corpus checked in.\n\n## Success criteria\n\n- Fuzz targets run for a fixed budget in a local lane with zero crashes/timeouts; malformed inputs produce `F3dError::Abi` variants with offsets.\n- ABI doc table in `CONTRACT.md` lists every opcode with encoding and validation rules.\n\n## Test plan\n\n- Unit: per-opcode encode/decode round trips; bounds tests (offset/length beyond packet, unknown opcode, version mismatch, wrapped domain); u64 split/merge tests.\n- Fuzz: `cargo-fuzz` targets for packet decode, handle decode and layout validation with a checked-in corpus; fixed-budget lane (can run on RCH; no browser).\n- Logging: decoder errors as structured `F3dError::Abi{offset, opcode, reason}`; fuzz lane summary (executions, crashes = 0) archived under `evidence/05.3/`.\n- Failure artifacts: minimized crashing inputs.\n\n## Considerations\n\n- Alignment rules belong to the actual operation (§6.8); the ABI carries `writeBuffer` alignment and encoder copy constraints as separate validated fields.\n- The command set stays small; new opcodes need a protocol version bump reflected in the build cache key.\n","acceptance_criteria":"The command packet format is versioned and bounded; every offset/length is validated before any view is created or work submitted; handles cross the boundary as two u32 words or checked smaller IDs; a fuzz target exercises packet decoding, handle decoding and layout validation without panics or out-of-bounds reads; malformed packets produce structured errors.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:27.488861Z","created_by":"jemanuel","updated_at":"2026-09-09T17:11:59.467693Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-core","f3d-runtime","layout-abi","phase-0","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.3","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa","type":"parent-child","created_at":"2026-09-09T16:46:27.488861Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.3","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.1","type":"blocks","created_at":"2026-09-09T16:48:38.678395Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-05-ids-layouts-epochs-transport-vqa.4","title":"Safe host transport: borrowed-memory lifetime contract, growth, reentry, copy accounting","description":"## Full scope\n\nSafe host transport: borrowed Wasm memory views with an enforced lifetime contract, view rebuild on growth, no callbacks/growth/reentry while a borrow is live, copy accounting, audited binding boundary aligned with Asupersync\n\n## Summary\n\nZero-copy where it is sound, copy where it is not, and always know which happened.\n\n## Background (plan §6.6, §6.8, §13.1, §13.4; AGENTS.md \"Unsafe Code\")\n\n- \"A correctness-first bridge can copy into typed arrays. An optimized bridge must account for every remaining copy. Any borrowed Wasm memory view must obey its lifetime contract, must not survive unsafe growth/reentrancy, and must not be treated as an independently owned transferable buffer.\"\n- \"Pre-reserve reasonable memory and rebuild host views when the memory buffer changes. ... Keep private borrowed views inside the trusted generated adapter; no application callback, memory growth, or scheduler re-entry is allowed while the corresponding Rust borrow is live. Copy when that boundary cannot be enforced. Release the Rust borrow before calling effectful host/user code.\"\n- \"A source view whose bytes have been synchronously copied by the upload API need not remain pinned until GPU completion; a mapped staging allocation used by an encoded copy has a different lifetime.\"\n\n## Technical approach\n\n- `f3d-runtime::host`: a `BorrowScope` type on the Rust side that hands out packet/data ranges as `(offset, length)` only; the JS adapter creates `Uint8Array(memory.buffer, offset, length)` views inside a `withBorrow(fn)` helper that asserts (debug) no re-entry, no growth (compare `memory.buffer` identity before/after), and no user callback dispatch (a re-entrancy flag checked by the callback trampolines).\n- Growth policy: pre-reserve based on the case's byte budget; after any growth, invalidate cached views and rebuild; log growth events (they are startup/tail measurements).\n- Copy accounting: counters for bytes copied by `writeBuffer` (synchronous copy semantics let the source be released immediately), bytes copied for host-side staging, and bytes passed by view; surfaced through the cost accounting hooks (F3D-07.7) and the build report.\n- Binding boundary audit record: which crates may use `wasm-bindgen`/`web-sys` (runtime host module and the generated adapter only); numerical/compiler crates forbid it by dependency direction (§17.3).\n\n## Success criteria\n\n- Tests: a borrow scope that triggers growth inside is detected; a callback inside a scope is detected; views are rebuilt after growth and reads remain correct; copy counters match expected bytes for a scripted frame.\n- The dependency graph proves no browser binding leaks into `f3d-math`/`f3d-scene`/`f3d-compiler`.\n\n## Test plan\n\n- Unit: Rust tests for borrow-scope discipline (growth inside a scope detected; callback inside a scope detected; view rebuilt after growth; copy counters exact for a scripted frame).\n- E2E: `tests/e2e/transport/growth.mjs` forces `memory.grow` inside and outside a scope on Chrome and Safari; dependency-graph test proves no browser binding leaks into f3d-math/f3d-scene/f3d-compiler.\n- Logging: growth events `{old_pages, new_pages, views_rebuilt}` and per-frame copy counters `{bytes_view, bytes_copied_write_buffer, bytes_copied_staging}`; archived under `evidence/05.4/`.\n- Failure artifacts: the borrow-scope violation stack.\n\n## Considerations\n\n- SharedArrayBuffer/worker transfer is a later isolated option (§11.6); views here are single-realm.\n- A desired speedup never justifies hand-written unsafe pointer casts in engine code (AGENTS.md).\n","acceptance_criteria":"The host adapter obtains typed-array views of Wasm memory only inside a bounded borrow scope that forbids application callbacks, memory growth and scheduler re-entry, rebuilds views after growth, and falls back to copying where the scope cannot be enforced; every remaining copy is counted and reported; the binding layer is the single named unsafe/external boundary and shares the wasm-bindgen/js-sys/web-sys family with Asupersync.","notes":"2026-09-11: GrayFox implements actual copied memory transport with QuietSnow tests by explicit user direction (Mail6722/6748); tracker claim remains blocked by prerequisites. A Rust metadata gate is not actual memory-lifetime evidence.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:27.578177Z","created_by":"jemanuel","updated_at":"2026-09-11T01:18:48.682012Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-runtime","js-host","layout-abi","phase-0","tier-solid"],"dependencies":[{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.4","depends_on_id":"f3d-02-asupersync-browser-execution-58j.2","type":"blocks","created_at":"2026-09-09T16:48:38.988744Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.4","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa","type":"parent-child","created_at":"2026-09-09T16:46:27.578177Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.4","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.3","type":"blocks","created_at":"2026-09-09T16:48:38.833817Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-05-ids-layouts-epochs-transport-vqa.5","title":"Ownership modes and epochs; per-use data version primitive","description":"## Full scope\n\nOwnership modes and epochs: JavaScript-owned, Wasm-owned, mirrored-with-epoch-contract regions; ordered versioned authority transfer; per-use data version primitive for the planner\n\n## Summary\n\nThe rule \"one authoritative owner per state region\" as code.\n\n## Background (plan §5.3, §6.2, §6.7, §5.8; AGENTS.md \"One authoritative owner per state region\", \"Queue ordering is not command-recording ordering\")\n\n- Modes: JavaScript-owned (public objects authoritative; core receives snapshots/deltas at boundaries); Wasm-owned (admitted island holds packed authoritative state; observations materialize through generated bindings); mirrored with an epoch contract (two representations, one author per epoch, explicit publication).\n- \"A mirror is not a license for two writers. Authority transfers must be ordered, versioned, and tested. The general fallback is JavaScript ownership.\"\n- \"Each dependency has a version. A derived result records the versions it consumed.\"\n- \"The planner tracks per-use data versions, not just a last value per object per frame.\"\n\n## Technical approach\n\n\n- `f3d-core::ownership`: `OwnerMode { Js, Wasm, Mirrored }`, `Epoch(u64)` per region, `RegionState` with `author` and `published_epoch`; transfer API `transfer_authority(region, from, to, at_epoch)` that fails if the current author has unpublished changes or if ordering is violated.\n- `DataVersion(u64)` per dependency slot and a `UseRecord { resource, version }` list type that plan segments store; equality of versions is the reuse condition; distinct versions within one submission force distinct buffer slices/offsets (consumed by F3D-07.4/F3D-08).\n- Tests modeling the red-A/blue-B scenario at the data level: two uses with versions 1 and 2 must map to two slices; a single-slot \"last value\" design is rejected by the type system (no API to overwrite a use's version).\n- ABI rule: `Epoch(u64)` and `DataVersion(u64)` cross the Wasm/JS boundary only as two `u32` words or stay Wasm-side; never as a JavaScript Number (05.3).\n\n## Success criteria\n\n- Unit tests for transfer ordering violations, double-writer detection in mirrored mode, and per-use version distinctness.\n- The design is referenced by `f3d-scene` (F3D-06) and the graph planner (F3D-07/08) without additions.\n\n## Test plan\n\n- Unit: transfer-ordering violation tests; double-writer detection in mirrored mode; per-use version distinctness (two uses with versions 1 and 2 map to two slices; no API to overwrite a use's version); property tests over random transfer sequences asserting the single-author invariant.\n- E2E: none (data model); consumed by 07.4/08.x tests.\n- Logging: debug dump of region authorship for the report; failing tests log the offending transfer sequence.\n- Failure artifacts: n/a.\n\n## Considerations\n\n- Authority transfer must respect §5.10: observable results (mixer updates, matrix reads) materialize at the source boundary, so a Wasm-owned region must be able to publish synchronously on demand.\n","acceptance_criteria":"Every state region carries exactly one of three ownership modes with an epoch; authority transfers are explicit, ordered and versioned with tests; a per-use data version type lets a consumer record the exact versions it observed so two uses of one resource within one submission can require two distinct data versions; the general fallback mode is JavaScript ownership.","notes":"2026-09-10 user-authorized critical implementation wave. Scope: Single-writer epochs and immutable per-use versions. Start implementation while dependencies await verification; existing acceptance conditions and dependency edges remain binding for closure. Owned paths: crates/f3d-core/src/ownership.rs and crates/f3d-core/tests/ownership_tests.rs only","status":"in_progress","priority":0,"issue_type":"task","assignee":"GrayFox","created_at":"2026-09-09T16:46:27.666440Z","created_by":"jemanuel","updated_at":"2026-09-10T20:09:34.346718Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-core","f3d-scene","phase-0","tier-solid"],"dependencies":[{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.5","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa","type":"parent-child","created_at":"2026-09-09T16:46:27.666440Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.5","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.1","type":"blocks","created_at":"2026-09-09T16:48:39.140102Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-05-ids-layouts-epochs-transport-vqa.6","title":"Production bulk WebGPU bridge: negotiation, first correct frame, error-scope discipline","description":"## Full scope\n\nProduction bulk WebGPU bridge: adapter/device negotiation before creation, first correct frame through the selected bulk variant, generated static JS adapter, serialized error-scope discipline, comparison against direct JS WebGPU\n\n## Summary\n\nHarden the winning bridge prototype into the production path. Section 21: \"submit a simple, correctly rendered frame through the bulk bridge and compare it with direct JavaScript WebGPU using the same work.\"\n\n## Background (plan §6.4, §6.8, §11.7, §12.2, §16.7 \"Foundation gate\", §21)\n\n- \"The host adapter should therefore accept coarse packets or execute statically generated host routines, not receive a Wasm-to-JavaScript call for every property.\" \"Generate ordinary static JavaScript source: no production `eval` or `new Function` requirement.\"\n- \"Negotiate the application's minimum requirements before device creation, including storage-buffer/binding counts and workgroup/texture dimensions. Select optional variants only against the features and limits actually enabled on the created device.\"\n- \"Serialize access to a device's error-scope stack. Push scopes, issue the owned API operations, and pop the scopes without an intervening await or user callback... Concurrent Asupersync tasks do not acquire private GPU error-scope stacks.\"\n- \"A synchronously returned GPU object is not by itself evidence of a valid allocation or pipeline.\"\n\n## Technical approach\n\n- `f3d-runtime::gpu_host`: device acquisition task (in an Asupersync session region) that requests an adapter, computes the required feature/limit profile from the plan, requests the device, records the capability record (05.1 type), and attaches the `uncapturederror` event listener and awaits the `device.lost` promise, both routed to structured events.\n- Generated adapter: from the ABI table (05.3), emit the static JS decoder (packet variant) and/or the specialized submission template (generated variant), depending on 03.5's per-class selection; both read views only through the borrow scope (05.4).\n- Error-scope discipline: a `withErrorScopes(kinds, fn)` helper in the adapter that pushes, runs the synchronous body, pops, then returns the promises; a debug assertion detects an `await` inside the body (by re-entrancy/turn counting).\n- First frame: a WGSL pipeline generated by the layout schema's struct text, a vertex buffer, an `AffineRows` uniform, one draw to an offscreen target and to the canvas (canvas texture acquired per render interval, §8.5), readback for comparison.\n- Measurement: reuse 03.1 to record initialization time, bytes copied, host calls, and per-frame sync; compare against the direct-JS variant from 03.5.\n\n## Success criteria\n\n- Pixel-identical frames across the bridge and direct JS on both browsers; readback of a transformed vertex proves the AffineRows row convention.\n- Capability record captured; feature/limit negotiation test: a required feature absent yields a structured, source-shaped error, not a partially initialized device.\n- Error-scope test: two concurrent preparation tasks cannot interleave scopes (assertion).\n\n## Test plan\n\n- Unit: device-profile negotiation tests (required feature absent → structured, source-shaped error); generated-adapter decoder tests against the ABI table; error-scope helper test detecting an `await` inside the body.\n- E2E: `tests/e2e/bridge/first-frame.mjs` on Safari (M5, iPhone) and Chrome (M5): first frame through the bridge pixel-identical to direct JS; AffineRows vertex readback; concurrent preparation tasks cannot interleave scopes.\n- Logging: negotiation events `{requested_features, enabled_features, limits}`; per-frame bridge cost `metric` events; capability record attached; archived under `evidence/05.6/`.\n- Failure artifacts: pixel diffs, uncaptured-error log.\n\n## Considerations\n\n- A runtime variant requiring a larger limit or unrequested feature needs an explicit new-device strategy or a legal existing-device path; it cannot silently use the adapter limit (§12.2).\n- Canvas textures are never cached in a long-lived scene cache; avoid arbitrary yields between acquisition and use (§8.5).\n","acceptance_criteria":"Using the bridge variant selected in 03.5, Rust code prepares a packet and the generated static JS adapter submits a correctly rendered simple frame (a textured, transformed mesh using AffineRows) in Safari and Chrome; device creation negotiates required features/limits and records the capability record; error scopes are pushed/popped without intervening awaits and results awaited afterwards; the same work issued by direct JS WebGPU produces identical pixels; command transfer, copies, initialization and per-frame synchronization costs are measured and recorded.","notes":"2026-09-10 user-authorized critical implementation wave. Scope: Actual first frame through Rust/Wasm to WebGPU. Start implementation while dependencies await verification; existing acceptance conditions and dependency edges remain binding for closure. Owned paths: crates/f3d-runtime/src/gpu_host.rs and tests/fixtures/gpu_bridge/** only\n2026-09-12 OrangePelican: implemented and reviewed textured AffineRows packet/export plus encoded texture uploads and texture-only bind groups. RCH batch96 native: 77 library + 59 mesh tests passed; new Wasm package SHA256 4d6fa569075fb1ed8fa3540869ef479169051409da9aaec1a849853dd1889fbc. Chrome 152 and Safari 26.5 on this M4 each passed 23 existing-fixture checks: textured readback identical to direct WebGPU across all 16,384 bytes; distinct red/green/blue/white probes; red-A/blue-B ordered uploads; no retained renderer submission. Two older compiled triangle packets remain byte-identical. Evidence: /Volumes/USBNVME16TB/temp_agent_space/f3d-browser-evidence/05.6/2026-09-12T18-02-11.298Z-31052/results.json and 2026-09-12T18-02-16.664Z-31469/results.json. This is the experimental bridge route, not general Three.js texture-material compatibility. Bead remains in progress: selected-bridge measurement/dependency acceptance and requested device matrix are not closed by this proof; no acceleration claim.","status":"in_progress","priority":0,"issue_type":"task","assignee":"ChartreuseFern","created_at":"2026-09-09T16:46:27.753890Z","created_by":"jemanuel","updated_at":"2026-09-12T21:27:32.001499Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-runtime","js-host","kill-gate","phase-0","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.5","type":"blocks","created_at":"2026-09-09T16:48:39.769753Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa","type":"parent-child","created_at":"2026-09-09T16:46:27.753890Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.2","type":"blocks","created_at":"2026-09-09T16:48:39.292956Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.3","type":"blocks","created_at":"2026-09-09T16:48:39.447977Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.4","type":"blocks","created_at":"2026-09-09T16:48:39.613706Z","created_by":"jemanuel","metadata":"{}","thread_id":""}],"comments":[{"id":2,"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","author":"OrangePelican","text":"2026-09-12 OrangePelican review: experimental mesh batch now passes Color background clear and in-place mutation with opaque foreground against pinned WebGLRenderer (existing per-channel tolerance2), Chrome152 and Safari26.5, each25 browser checks PASS. Native RCH batch103:77 library +60 runtime_mesh tests PASS. Wasm377a02dd03a1df3a5375918fdab5a8cc4870b10e91b110e127a41108b7a32439; previous vertex-color packet bytes unchanged, canvas1594B/offscreen1367B, including new export with default clear. Evidence05.6/2026-09-12T19-54-11.656Z-4538 and ...650Z-4537 under /Volumes/USBNVME16TB/temp_agent_space/f3d-browser-evidence. Debug firstdraw samples are raw overhead observations, not speedup evidence. Empty/background-only scenes remain outside this mesh slice; no broad closure claim.","created_at":"2026-09-12T19:56:51Z"},{"id":3,"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","author":"OrangePelican","text":"Correction to my 2026-09-12 timing note: source review found the legacy triangle packet creates/rendered offscreen AND canvas paths, while direct JS rendered only offscreen. Those firstdraw samples are unmatched workloads and cannot support a comparative overhead conclusion. Functional browser results and legacy packet byte comparisons remain valid. The newly-added misleading firstframe timing comparison is being removed; the next4000-transform comparison must use one offscreen pass on each path. Existing evidence is retained unchanged.","created_at":"2026-09-12T19:58:46Z"},{"id":8,"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","author":"jemanuel","text":"Reviewed and verified 2026-09-12: first-frame Rust packet now registers its actual readback buffer20 and canvas pipeline101; fresh-instance browser assertions pass before other builders can mask registration. Legacy 2952B first-frame packet is unchanged. Experimental scene adapter now honors pinned single-camera frustum bounds and frustumCulled=false; real WebGLRenderer comparisons prove cull, opt-out, and bounds mutation on M4 Chrome/Safari (explicit lane=mesh_batch, 32 checks each, 05.6/2026-09-12T20-50-42.250Z-48192 and ...48191). 126 adapter tests passed independently before the next empty-Color clear change. Rust/Wasm package a7ca6179e9f1647b114e152d09c43b5ba73a3fef4688fe2185cdc98bca83d79d. This is bounded new-backend functionality, not complete Three.js compatibility or acceleration proof.","created_at":"2026-09-12T21:01:13Z"},{"id":9,"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","author":"jemanuel","text":"2026-09-12 OrangePelican review: empty Color-background scenes now execute a zero-draw clear packet on offscreen and current canvas targets, including removal of the last mesh and subsequent background mutation. RCH batch108 passed 79 lib + 61 runtime mesh tests. JS explicit-WebGL clip convention now matches pinned WebGLRenderer even when camera.coordinateSystem is WebGPU; real near-plane pixels match and camera projection/metadata remain unchanged. Root ran 134 adapter tests and sequential Chrome/Safari mesh_batch lanes: 32 checks each, zero errors, all source/package hashes unchanged. Latest evidence under /Volumes/USBNVME16TB/temp_agent_space/f3d-browser-evidence/05.6/: 2026-09-12T21-22-42.684Z-82192/results.json and 2026-09-12T21-23-42.819Z-86079/results.json. pkg108 bg.wasm SHA256 bc788335d3e2daeb09f8db034ef97156e344cebf135f8279475a576a96f376a7. Experimental new-backend bridge only; no full compatibility, speedup, or bead-closure claim.","created_at":"2026-09-12T21:27:31Z"}]}
{"id":"f3d-05-ids-layouts-epochs-transport-vqa.7","title":"First integrated counterexample suite on the bridge (red-A/blue-B, bundle reset, layout, ABA)","description":"## Full scope\n\nFirst integrated counterexample suite on the bridge: red-A/blue-B queue-write snapshot, bundle-then-direct-draw state reset, AffineRows vs mat4x3 end to end, memory growth and reentry under borrow, stale handle publication, delayed asynchronous result (a simulated worker/decode task; the real Worker variant is added by 21b.6) after ID reuse, separate native-context ownership\n\n## Summary\n\nThe mandatory counterexamples that the plan says must be in \"the first integrated tests\" (§11.7), implemented against the real bridge before any renderer abstraction is built.\n\n## Background (plan §6.7, §8.2 \"Bundle reuse has a structural contract\", §7.5, §6.5, §6.6, §6.9, §11.7, §14.8, §23 [S33], [S37]; AGENTS.md \"Mandatory Counterexample Suite\")\n\n| Counterexample | Guards against |\n|---|---|\n| Red-A / blue-B queue-write snapshot | Treating queue writes as per-draw snapshots: two `writeBuffer` calls before one submission make both recorded draws read the second value |\n| Bundle-then-direct-draw state reset | Falsely warm binding/pipeline caches after `executeBundles` (state resets even for an empty bundle list) |\n| `AffineRows` vs `mat4x3` layout | Silent 48/64-byte layout corruption |\n| Growth/reentry under borrow | Views surviving memory growth or callbacks during a live borrow |\n| Stale handle publication | A delayed result publishing into a recycled slot |\n| Delayed asynchronous result after ID reuse (simulated worker/decode task until 21b.6 adds real workers) | ABA handle collisions |\n| Separate native-context ownership | Assuming `WebGLTexture`/`GPUTexture` interchange or shared realms |\n\n## Technical approach\n\n- Red-A/blue-B: render to target A with the shared uniform = red; mutate to blue; render to target B; submit one combined schedule built through the bridge using per-use versioned slices; assert A red, B blue. Add a \"wrong implementation\" toggle that uses one slot to prove the test detects the failure.\n- Bundle-then-direct: record a bundle binding pipeline P1/group G1; in a pass, first bind P2/G2 plus the direct draw's vertex/index buffers (so the adapter's cache believes they are current), then `executeBundles([bundle])`, then issue the direct draw needing P2/G2. WebGPU clears the pipeline, bind groups and vertex/index buffers after `executeBundles` (viewport, scissor, blend constant and stencil reference are kept), so the direct draw must re-bind; with invalidation deliberately disabled the draw fails validation or renders wrong, with the fix it renders right. Include the `executeBundles([])` variant.\n- AffineRows end-to-end: upload a known matrix as rows; vertex shader transforms `(x,y,z,1)`; read back; compare with CPU `Matrix4` result; include a `mat4x3` fixture expected to be rejected by the schema writer.\n- Growth/reentry: force `memory.grow` inside a borrow scope in debug (expect detection) and outside (expect view rebuild and correct data).\n- Stale handle / ID reuse: allocate a texture handle, retire it, allocate again in the same slot, deliver a delayed result addressed to the old generation; assert it is discarded with a logged generation mismatch (same mechanism as 02.3, now for GPU handles with device generation).\n- Native-context ownership: create an exact-GL renderer (04.3) and a new-backend device; assert resources are tracked in separate groups and that an attempted cross-use is reported, with the only supported path being a real copy (deferred to F3D-17.4).\n\n## Success criteria\n\n- Seven named tests pass; each has a mutation variant proving detection; they run in the forced-new-backend lane and in benchmark mode.\n\n## Test plan\n\n- Unit: none new (integrated suite).\n- E2E: seven named tests in `tests/e2e/counterexamples/` on Chrome and Safari, each with a mutation variant (`--wrong-impl`) that must fail; wired into the forced-new-backend lane and benchmark mode (never skipped).\n- Logging: each test logs observed pixel values/versions/generations and the expected ones, tagged `owner: new-backend` (GPU cases) or `owner: exact-backend` (native-context case); archived under `evidence/05.7/`.\n- Failure artifacts: target readbacks as images, the packet dump for the failing submission.\n\n## Considerations\n\n- These tests are part of the measured optimized path (§14.8): later beads must not add a \"benchmark mode\" that skips them.\n","acceptance_criteria":"Named regression tests exist and pass on Safari and Chrome for each counterexample; each test demonstrably fails when its guard is disabled (mutation check); the tests run as part of the foundation evidence now, are wired into the forced-new-backend lane when 18a.4 creates it, and are never disabled in benchmark mode.","notes":"2026-09-10 user-authorized critical implementation wave. Scope: Independent GPU counterexamples and implementation review. Start implementation while dependencies await verification; existing acceptance conditions and dependency edges remain binding for closure. Owned paths: tests/e2e/bridge/** only; read other implementation files without editing them","status":"in_progress","priority":0,"issue_type":"task","assignee":"QuietSnow","created_at":"2026-09-09T16:46:27.839773Z","created_by":"jemanuel","updated_at":"2026-09-10T20:12:39.867823Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-runtime","kill-gate","phase-0","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.7","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.3","type":"blocks","created_at":"2026-09-09T17:35:39.652983Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.7","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa","type":"parent-child","created_at":"2026-09-09T16:46:27.839773Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.7","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.5","type":"blocks","created_at":"2026-09-09T16:48:40.101668Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-05-ids-layouts-epochs-transport-vqa.7","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","type":"blocks","created_at":"2026-09-09T16:48:39.941068Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-06-packed-transforms-materialization-roa","title":"F3D-06: Packed transforms with complete public state/materialization","description":"## What this epic delivers\n\nThe CPU side of the execution core: fixed-size math with JavaScript numeric semantics, packed scene state with dirty propagation, a versioned dependency graph, synchronous materialization of public state, a BVH for synchronous queries, and attribute upload histories. Plan §19 F3D-06 depends on 05; acceptance: \"Matrices/attributes, synchronous queries, clone/serialize/export after updates\". Blocked by kill gate 1 because the plan forbids building the engine before real browser execution is proven.\n\n## Why (plan §5.2, §5.4, §5.8, §5.10, §6.2, §7.1–7.7)\n\nData layout matters more than elegant notation: separate hot numeric arrays from cold names/user data/handlers; use SoA or small AoSoA blocks; preserve stable public identity through handles. Transform correctness: special-case rigid/uniform-scale only after proving conditions; hierarchies produce shear; negative scale flips orientation; never normalize user-authored transforms. Public observations are semantic: `AnimationMixer.update()`, explicit matrix updates, ray queries and callbacks can synchronously observe results before `renderer.render()`.\n\n## Children\n\n1. `f3d-math`: fixed-size types, f64 public semantics, JS numeric lowering, transcendental differential tests, scalar + simd128 with measured crossover.\n2. `f3d-scene` packed transforms: hierarchy order, dirty roots, intervals, reparenting, manual flags, proven fast paths.\n3. Semantic dependency graph and versioning.\n4. Public state materialization and identity preservation.\n5. BVH and the synchronous raycast contract.\n6. Attribute CPU/upload/GPU-visible histories.\n7. Adversarial and property tests for math and scene.\n\n## Success criteria for the epic\n\nAll children closed; both crates ship `CONTRACT.md`; line counts reported against 12,000 (math) and 25,000 (scene).\n\n## No-claim boundary\n\nPacked state is a private representation. It changes no public contract, and this epic claims no speedup.\n","acceptance_criteria":"f3d-math and f3d-scene provide packed, versioned scene state whose public observations (matrices, attributes, Euler/quaternion relationship, synchronous queries, clone/serialize/export) match pinned upstream after packed updates; the attribute CPU/upload/GPU-visible histories follow r186 WebGLAttributes semantics; adversarial and property tests pass.","status":"open","priority":1,"issue_type":"epic","created_at":"2026-09-09T16:46:22.742923Z","created_by":"jemanuel","updated_at":"2026-09-09T16:48:42.675895Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-math","f3d-scene","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-06-packed-transforms-materialization-roa","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.2","type":"blocks","created_at":"2026-09-09T16:48:42.363867Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-06-packed-transforms-materialization-roa","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.5","type":"blocks","created_at":"2026-09-09T16:48:42.519680Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-06-packed-transforms-materialization-roa","depends_on_id":"f3d-gate-1-foundation-tzn","type":"blocks","created_at":"2026-09-09T16:48:42.675631Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-06-packed-transforms-materialization-roa.1","title":"f3d-math: fixed-size types, f64 public semantics, JS numeric lowering, simd128 with crossover","description":"## Full scope\n\nf3d-math crate: fixed-size vector/quaternion/matrix/affine types with f64 public semantics, explicit JavaScript numeric lowering, transcendental differential tests against the browser, scalar reference plus simd128 batches with measured crossover\n\n## Summary\n\nThe numerical foundation with the plan's numeric-fidelity rules built in.\n\n## Background (plan §5.4, §7.1, §7.3, §7.5, §23 [S47]; AGENTS.md \"Numeric fidelity\")\n\n- \"Use `f64` or retained JavaScript for observable scalar operations; convert to GPU representations at the same admitted boundaries as the reference. Preserve signed zero, exceptional values, operation ordering where observable, and relevant rounding behavior. JavaScript integer coercions, shift counts, remainder, and `Math.min`/`Math.max` behavior need explicit lowering; Rust casts and similarly named arithmetic methods are not substitutes.\"\n- \"Transcendental functions require differential testing: sharing an `f64` type alone does not prove agreement with a browser's math implementation.\"\n- Representations: quaternion for local rotation (unit fast path only when established); TRS for local transforms; packed 3×4 affine for world; 4×4 for projection; correct inverse-transpose for normals.\n- \"Use safe portable SIMD on the pinned nightly where its Wasm code generation is favorable, and keep a scalar reference. Inspect the emitted `simd128` code and benchmark it in the actual target browsers.\"\n\n## Technical approach\n\n- Types mirror Three.js public math semantics one-to-one (method names may differ; behavior must not): `Matrix4.compose/decompose`, `Euler.setFromQuaternion` for every order, `Quaternion.slerp`, `Matrix3.getNormalMatrix`, `Box3`, `Sphere`, `Ray`, `Plane`, `Triangle`, `Color` with color management hooks.\n- A `jsnum` module: `to_int32`, `to_uint32`, `js_rem`, `js_shift`, `js_min`, `js_max`, `js_round` etc., each with spec-cited tests.\n- Differential harness: a browser page computes `Math.sin/cos/tan/atan2/pow/exp/log` over a fixed input set (including edge values) and exports results; Rust tests compare bit patterns and document any differences; where the browser differs from Rust's libm, either match via the browser (retained JS for that op) or record the tolerance per op.\n- Batch kernels (transform compose, matrix multiply, normal matrix, bounds transform) with `#[cfg(target_feature = \"simd128\")]` variants and scalar defaults; a benchmark page measures crossover per browser; emitted Wasm inspected once and noted.\n\n## Success criteria\n\n- Upstream math unit tests (routed through F3D-18a when ready) pass for retained JS; for the Rust kernels, a differential test suite against `three` math over random and edge inputs passes bit-for-bit for non-transcendental ops and within documented tolerance for transcendental ones.\n- Crossover numbers recorded per browser.\n- `f3d-math/CONTRACT.md` is written before any other crate depends on `f3d-math` (AGENTS.md \"Documentation and Contracts\"): purpose and position in the dependency direction, public types and semantics, invariants, implementation ownership route, error model, determinism and numeric-fidelity class, cancellation behavior, unsafe boundary if any, feature flags, conformance tests, and no-claim boundaries.\n\n## Test plan\n\n- Unit: per-type differential tests against pinned `three` math (run in Node, exported vectors as fixtures) over random and edge inputs (signed zero, NaN, Infinity, denormals, extreme magnitudes) asserting bit-identical results for non-transcendental ops; `jsnum` spec-cited tests (`ToInt32`, shift masks, `%` sign rules, `Math.min/max` with NaN and -0); simd128-vs-scalar equality property tests.\n- E2E: browser differential page `tests/e2e/math/transcendentals.mjs` on Chrome and Safari computing `sin/cos/tan/atan2/pow/exp/log` over the fixed input set; results compared bit-for-bit against the Rust build and any difference recorded per op with the chosen policy (retained JS or documented tolerance); crossover benchmark page records scalar vs SIMD timings per batch size.\n- Logging: mismatch tables `{op, input, browser_bits, rust_bits, ulp}` at `error`; crossover `metric` events `{kernel, n, scalar_ms, simd_ms}`; archived under `evidence/06.1/<browser>/`.\n- Failure artifacts: the mismatch table and the emitted Wasm disassembly excerpt for the affected kernel.\n\n## Considerations\n\n- Dual-quaternion skinning is a separate opt-in path (§7.2); not here.\n- Do not canonicalize or repair pathological inputs in the default path (§7.4).\n","acceptance_criteria":"f3d-math provides Vec2/3/4, Quat, Euler (all orders), Mat3/Mat4, Affine3x4 (AffineRows-compatible), Color and bounds/ray/plane/triangle primitives with f64 public-facing semantics; JS Number behaviors (signed zero, NaN/Infinity propagation, ToInt32 coercions, shift counts, remainder, Math.min/max NaN and -0 rules) are implemented explicitly and tested; transcendental results are differentially tested against browser Math on the target browsers; a scalar reference exists for every simd128 batch kernel with the crossover size measured in the actual browsers.","notes":"2026-09-10 user-authorized critical implementation wave. Scope: Real f64 transform math against pinned Three.js. Start implementation while dependencies await verification; existing acceptance conditions and dependency edges remain binding for closure. Owned paths: crates/f3d-math/** only","status":"in_progress","priority":1,"issue_type":"task","assignee":"BlueBison","created_at":"2026-09-09T16:46:27.927373Z","created_by":"jemanuel","updated_at":"2026-09-10T20:09:57.563025Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-math","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-06-packed-transforms-materialization-roa.1","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.1","type":"blocks","created_at":"2026-09-09T16:48:40.577770Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-06-packed-transforms-materialization-roa.1","depends_on_id":"f3d-06-packed-transforms-materialization-roa","type":"parent-child","created_at":"2026-09-09T16:46:27.927373Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-06-packed-transforms-materialization-roa.1","depends_on_id":"f3d-gate-1-foundation-tzn","type":"blocks","created_at":"2026-09-09T17:31:52.117617Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-06-packed-transforms-materialization-roa.2","title":"f3d-scene packed transforms: hierarchy order, dirty roots, intervals, proven fast paths","description":"## Full scope\n\nf3d-scene packed transforms: SoA/AoSoA hot arrays with cold data separated, stable parent-before-child order, dirty roots and compact affected intervals, reparenting invalidation, manual-update flags honored, rigid/uniform-scale fast paths only after proven preconditions\n\n## Summary\n\nThe packed scene representation behind the public objects, built to preserve every observable upstream transform behavior.\n\n## Background (plan §5.2, §7.3, §7.4; §6.2)\n\n- \"Avoid recursively recomputing the whole tree each frame. Maintain a stable parent-before-child order, dirty roots, and compact affected intervals where the topology permits them. Reparenting invalidates the relevant topology segment. Public manual-update flags continue to control when updates are observable.\"\n- \"Special-case rigid and uniform-scale transforms only after proving the necessary conditions. A hierarchy can produce shear even when individual nodes are authored as TRS. Negative scale changes orientation; singular transforms require explicit reference-compatible behavior.\"\n- \"Do not normalize, repair, or canonicalize user-authored transforms merely to make optimization easier.\"\n- Preserve `position`, `quaternion`, `rotation`, `scale`, matrix update flags, layers, visibility, render ordering per pinned upstream; preserve the observable Euler/quaternion relationship (which is materialization, 06.4).\n\n## Technical approach\n\n- Arenas (05.1) for nodes: `local_trs`, `local_matrix`, `world_matrix` (Affine3x4 plus a full 4×4 slot when non-affine), flags (`matrix_auto_update`, `matrix_world_needs_update`, `visible`, `layers`, `frustum_culled`, `render_order`), parent/children indices; cold data (name, userData, callbacks) stays on the JS object.\n- Order maintenance: an array of node indices in DFS order with subtree extents; insert/remove/reparent update extents incrementally (or rebuild the affected subtree range).\n- Propagation: `update_world_matrices(dirty_roots)` processes intervals, respecting upstream's `updateMatrixWorld(force)` semantics exactly: a node recomputes `matrixWorld` when `matrixWorldNeedsUpdate || force` and then propagates `force = true` to its subtree; a child is visited only when `child.matrixWorldAutoUpdate === true || force === true`; `matrixAutoUpdate` controls only the local `updateMatrix()` call.\n- Fast-path admission: per subtree, track \"all descendants rigid/uniform-scale\" bits maintained on write; fall back to general affine when unknown.\n- Versioning hooks: each node's local/world version increments on write/propagate (feeds 06.3).\n\n## Success criteria\n\n- Differential tests against upstream `Object3D.updateMatrixWorld` on random hierarchies with random reparenting, negative scales, shear-producing trees, `matrixAutoUpdate=false` nodes and singular scales: bit-identical world matrices (f64).\n- Interval propagation touches only dirty subtrees (assert via counters).\n- `f3d-scene/CONTRACT.md` is written before any other crate depends on `f3d-scene` (AGENTS.md \"Documentation and Contracts\"): purpose and position in the dependency direction, public types and semantics, invariants, implementation ownership route, error model, determinism and numeric-fidelity class, cancellation behavior, unsafe boundary if any, feature flags, conformance tests, and no-claim boundaries.\n\n## Test plan\n\n- Unit: property tests over random hierarchies and operation sequences (set TRS, reparent, toggle `matrixAutoUpdate`, negative/zero/non-uniform scale, deep shear-producing trees) comparing world matrices bit-for-bit (f64) with a Node oracle running pinned `Object3D.updateMatrixWorld`; interval-propagation counter tests (nodes visited == dirty subtree size).\n- E2E: `tests/e2e/scene/hierarchy.mjs` drives H1/H3 scene graphs through scripted edits in the browser and compares `matrixWorld` observations at checkpoints with upstream.\n- Logging: per-propagation `metric` events `{dirty_roots, nodes_visited, total_nodes, intervals}`; first mismatch logged with node path, expected and actual elements; archived under `evidence/06.2/`.\n- Failure artifacts: the minimized hierarchy and op sequence (property-test shrink output).\n\n## Considerations\n\n- Public identity is preserved through handles; public objects are not the storage layout (§7.3).\n","acceptance_criteria":"f3d-scene stores local TRS, local/world matrices and flags in packed arrays keyed by handles; world-matrix propagation walks a maintained parent-before-child order updating only dirty intervals; reparenting invalidates the affected topology segment; matrixAutoUpdate/matrixWorldNeedsUpdate semantics match upstream observably; negative scale, shear from hierarchy and singular matrices produce upstream-identical results; fast paths are guarded by proven per-subtree conditions.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:28.017158Z","created_by":"jemanuel","updated_at":"2026-09-09T17:35:43.158237Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-scene","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-06-packed-transforms-materialization-roa.2","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.5","type":"blocks","created_at":"2026-09-09T16:48:40.888619Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-06-packed-transforms-materialization-roa.2","depends_on_id":"f3d-06-packed-transforms-materialization-roa","type":"parent-child","created_at":"2026-09-09T16:46:28.017158Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-06-packed-transforms-materialization-roa.2","depends_on_id":"f3d-06-packed-transforms-materialization-roa.1","type":"blocks","created_at":"2026-09-09T16:48:40.732444Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-06-packed-transforms-materialization-roa.2","depends_on_id":"f3d-gate-1-foundation-tzn","type":"blocks","created_at":"2026-09-09T17:15:27.859835Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-06-packed-transforms-materialization-roa.3","title":"Semantic dependency graph and versioning by category","description":"## Full scope\n\nSemantic dependency graph and versioning: separate dependency categories, per-dependency versions, consumed-version records, generation counters and dirty bitsets, precomputed dependency closures for common update classes\n\n## Summary\n\nMake \"removing a question from the frame loop\" concrete: the compiled program knows exactly what depends on what, with cheap versions instead of rediscovery.\n\n## Background (plan §4.10, §6.2)\n\n\"Represent dependency categories separately... A color change should not invalidate a mesh's world transform. A camera change should not cause a static geometry upload. A texture content update should not require recompiling its sampling code when type/layout are unchanged. Each dependency has a version. A derived result records the versions it consumed. Recompute only when one changes. Use compact generation counters and dirty bitsets; avoid cryptographic hashing of live state every frame. The compiler may precompute the affected dependency closure for common update classes. Dynamic insertion, removal, reparenting, and resource-layout changes rebuild only the affected structural plan.\"\n\n## Technical approach\n\n- `f3d-scene::deps`: a `DependencyKind` enum with the listed categories; per-entity per-kind `DataVersion` (05.5); a `DirtySet` per kind implemented as a bitset over arena indices; write paths bump versions and set bits.\n- Derived results (world matrix, bounds, packed uniform blocks, draw buckets) store `ConsumedVersions` and a `derive()` that checks before recomputing.\n- Precomputed closures: for update classes the compiler recognizes (many transforms from time; material color sweep; camera orbit), a static table of \"which derived results are affected\" so the frame path only clears those bits.\n- Structural changes (insert/remove/reparent/layout change) route through a `StructuralEvent` that marks plan segments for rebuild (consumed by F3D-07/08).\n\n## Success criteria\n\n- Tests assert the three non-invalidation examples from the plan; property tests assert derived results are recomputed iff a consumed version changed.\n- Frame-path cost is O(dirty), with counters proving no full scans on quiet frames.\n\n## Test plan\n\n- Unit: tests for the three non-invalidation examples (color change leaves world transform untouched; camera change triggers no geometry upload; texture content update triggers no program rebuild); property tests asserting derived results recompute iff a consumed version changed; quiet-frame test on a 10k-object synthetic scene proving O(dirty) work via counters.\n- E2E: `tests/e2e/scene/dirty-tracking.mjs` on H1 static mode: telemetry shows zero derived recomputation on quiet frames and exactly the expected recomputation on scripted edits.\n- Logging: per-frame debug dump of dirty bits per kind `{kind, dirty_count}` and derived-result recomputation counts; archived under `evidence/06.3/`.\n- Failure artifacts: the dependency graph excerpt for the wrongly recomputed or stale result.\n\n## Considerations\n\n- Versions are compact integers; never hashes of content.\n- Attribute upload versions (06.6) are a distinct history from CPU content versions.\n","acceptance_criteria":"Dependency categories (local/world transform, geometry content, geometry bounds, material program identity, material uniform values, texture content, light parameters, shadow inputs, camera state, render-target topology) have independent versions; derived results record consumed versions and recompute only when one changes; a color change does not invalidate a world transform, a camera change does not trigger a geometry upload, and a texture content update does not recompile sampling code; no per-frame cryptographic hashing of live state.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:28.108079Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:00.696456Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-core","f3d-scene","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-06-packed-transforms-materialization-roa.3","depends_on_id":"f3d-06-packed-transforms-materialization-roa","type":"parent-child","created_at":"2026-09-09T16:46:28.108079Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-06-packed-transforms-materialization-roa.3","depends_on_id":"f3d-06-packed-transforms-materialization-roa.2","type":"blocks","created_at":"2026-09-09T16:48:41.062537Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-06-packed-transforms-materialization-roa.4","title":"Public state materialization and identity preservation after packed updates","description":"## Full scope\n\nPublic state materialization and identity preservation: synchronous reads after packed updates, Euler/quaternion relationship, stable nested references, clone/toJSON/serialize/export after optimized updates, callbacks that mutate during render followed by world-matrix reads\n\n## Summary\n\nPacked state must never leak as a behavior change. This bead defines how the JS-visible objects and the packed replicas stay observably identical.\n\n## Background (plan §4.5, §5.2, §5.3, §5.10, §5.13, §6.3)\n\n- \"Reconstruction of observable objects is permitted at a declared materialization boundary, but must preserve stable identity and existing references. Replacing a referenced `Vector3` with a newly allocated lookalike is not equivalent.\"\n- \"Preserve the observable relationship between Euler rotation and quaternion changes, not just the final orientation in one frame.\" \"Tests must include callbacks that mutate scene state during rendering and subsequent reads of world matrices.\"\n- \"Finish or materialize the results at those original boundaries. Do not defer an observable animation update until a convenient frame commit.\"\n- \"Export must work after an accelerated update... Never export the initial captured scene while the user sees a newer scene.\"\n\n## Technical approach\n\n- Define the **materialization boundary** contract: for Wasm-owned regions, public objects expose current values via the generated bindings; the chosen mechanism must not replace public data properties with accessors where descriptors/enumeration/external references can observe the difference (§4.8). Where accessor conversion is unobservable for a given class (proven per class from the upstream source: for example upstream defines `Object3D.position`/`rotation`/`quaternion`/`scale` via `Object.defineProperties` as non-writable but `configurable: true`, `enumerable: true` data properties, so the descriptor shape (`writable: false`, `configurable: true`, `enumerable: true`; `modelViewMatrix`/`normalMatrix` non-enumerable) is part of the observable contract; an accessor conversion is technically possible but is observable through `Object.getOwnPropertyDescriptor` returning `get`/`set` instead of `value`, so the per-class proof must show that no reachable code observes the descriptor), the compiler records the proof; otherwise the region is JavaScript-owned with epoch publication.\n- Euler/quaternion: upstream's `Euler._onChange`/`Quaternion._onChange` callbacks keep the two in sync; the packed path must invoke the same ordering (write to one updates the other synchronously) and preserve `Euler.order`.\n- `matrix`/`matrixWorld` `.elements` arrays keep identity; packed updates write into the existing plain `Array` at the boundary (upstream `Matrix4.elements`/`Matrix3.elements` are 16-/9-element plain arrays, never typed arrays) (or the public array is the storage when the ownership mode is JS).\n- Callback tests: `onBeforeRender` mutates `position`; subsequent `updateMatrixWorld` observation in the same frame; compare traces with upstream.\n- Export hook: exporters read through the public objects; a pre-export materialization ensures Wasm-owned data is published (feeds F3D-19.4).\n\n## Success criteria\n\n- Differential traces vs upstream for the enumerated scenarios on H1/H2 objects.\n- Identity assertions (`===`) on nested references across 1,000 updates.\n\n## Test plan\n\n- Unit: identity tests (`===` on nested references across 1,000 updates); Euler/quaternion write-ordering property tests vs upstream; descriptor/enumeration tests (`Object.getOwnPropertyDescriptor`, `Object.keys`, `JSON.stringify`) equal to upstream for every recognized class; per-class proof records checked in for any accessor conversion.\n- E2E: `tests/e2e/scene/materialization.mjs` on H1/H2 objects: `onBeforeRender` mutates state and a later `matrixWorld` read observes it; `clone()`/`toJSON()`/exporter reads after packed updates equal upstream; run on both browsers.\n- Logging: materialization counts per frame as `metric` events (they are a cost in the report); mismatches logged with property path, expected and actual values; archived under `evidence/06.4/`.\n- Failure artifacts: serialized object snapshots from both sides.\n\n## Considerations\n\n- Small or frequently observed islands should remain JavaScript when crossing/packing costs dominate (§4.8); this bead's counters feed that decision.\n","acceptance_criteria":"Any public read of position/quaternion/rotation/scale/matrix/matrixWorld/attributes after a packed update returns the upstream-identical value synchronously; object.position and nested references keep their JavaScript identity across updates; clone, copy, toJSON and exporter reads observe current state; the Euler-vs-quaternion write ordering behavior matches upstream; a render callback that mutates scene state followed by a world-matrix read observes the mutation as upstream does.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:28.197128Z","created_by":"jemanuel","updated_at":"2026-09-09T17:35:39.151928Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","f3d-scene","phase-1","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-06-packed-transforms-materialization-roa.4","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.5","type":"blocks","created_at":"2026-09-09T16:48:41.377792Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-06-packed-transforms-materialization-roa.4","depends_on_id":"f3d-06-packed-transforms-materialization-roa","type":"parent-child","created_at":"2026-09-09T16:46:28.197128Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-06-packed-transforms-materialization-roa.4","depends_on_id":"f3d-06-packed-transforms-materialization-roa.2","type":"blocks","created_at":"2026-09-09T16:48:41.219154Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-06-packed-transforms-materialization-roa.5","title":"Graphics BVH and the synchronous raycast contract","description":"## Full scope\n\nGraphics BVH and the synchronous raycast contract: robust CPU builder and traversal, refit and measured rebuild heuristic, stable tie-breaking and upstream hit ordering, custom raycast overrides, face/instance identities, conservative bounds for deformed meshes\n\n## Summary\n\nSpatial queries with the plan's picking semantics intact.\n\n## Background (plan §5.10, §7.1, §7.6)\n\n- \"Retain synchronous raycasting on CPU-authoritative state. GPU picking may be an additional async API or a proven internal strategy, not a Promise substituted for a synchronous `Raycaster` result. A BVH must preserve custom `raycast` overrides, source traversal/tie ordering, and face/instance identities. Canonical triangle IDs stay separate from any private reordered acceleration structure.\"\n- \"A statistical or heuristic culling decision may choose a conservative bound, but must not discard potentially visible geometry. Deformed meshes need conservative bounds or correct refits. Objects outside the main camera frustum may still contribute to shadows, reflections, transmission, or other passes.\"\n\n## Technical approach\n\n\n- `f3d-scene::bvh`: binned SAH builder over object bounds (scene level) and optionally per-mesh triangle BVH for large geometries; refit path for transform-only changes; a measured rebuild heuristic (quality degradation threshold) recorded from benchmarks.\n- Ray query returns candidates; the actual intersection uses the object's `raycast` (retained JS when overridden, or the Rust equivalent of upstream `Mesh.raycast` when the class is unmodified and recognized), then results are sorted with upstream's comparator including its tie behavior (stable by distance; upstream sorts by `distance` with `ascSort`).\n- Identity: hits carry `faceIndex`, `instanceId`, `object` (the JS object handle), `uv`, `normal` as upstream; triangle IDs map through a canonical table if any private reordering exists.\n- Culling: frustum culling honors `frustumCulled` and `layers`; shadow/reflection passes use their own frusta (feeds F3D-08/14).\n- Prefilter scope: the BVH candidate filter applies only to unmodified recognized `Mesh` geometry with valid bounds; `Points`/`Line` (their `raycast` uses `raycaster.params.Points.threshold`/`Line.threshold`, so bounds must be expanded by the threshold or the objects tested directly), `Sprite`, `LOD`, objects with overridden or unrecognized `raycast`, and objects without geometry bounds are always tested directly; `raycaster.layers.test(object.layers)` gating and `recursive` traversal order are preserved exactly.\n\n## Success criteria\n\n- Differential tests against upstream `Raycaster.intersectObjects` over random scenes including coincident hits, negative scale, instanced meshes, skinned meshes (with `boundingBox` semantics) and subclass overrides: identical result lists.\n- Culling test: no object visible in upstream's render list is culled by the BVH.\n\n## Test plan\n\n- Unit: property tests comparing `intersectObjects` results (order, distances, `faceIndex`, `instanceId`, `uv`, `normal`) with a Node oracle over random scenes including coincident hits, negative scale, instanced meshes, skinned meshes and subclass `raycast` overrides; refit/rebuild heuristic tests; culling test asserting no object in upstream's render list is culled.\n- E2E: `tests/e2e/scene/picking.mjs` adds a synthetic picking script to H1 and runs `webgl_interactive_*`-style examples on both browsers, comparing hit lists at logical frames.\n- Logging: BVH build/refit `metric` events `{nodes, depth, build_ms, refit_ms, rebuild_reason}`; hit-list diffs logged with the ray and both lists; archived under `evidence/06.5/`.\n- Failure artifacts: the minimized scene and ray from the property-test shrink.\n\n## Considerations\n\n- GPU culling is a later optional strategy (F3D-20.6), not a different visibility contract.\n","acceptance_criteria":"A CPU BVH supports frustum culling and ray queries with reference-compatible hit ordering and tie-breaking; Raycaster results remain synchronous on CPU-authoritative state; custom raycast overrides on subclasses are honored; face/instance identities and canonical triangle IDs are preserved independent of any private reordering; deformed meshes use conservative bounds or correct refits; culling never discards potentially visible geometry.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:28.282471Z","created_by":"jemanuel","updated_at":"2026-09-09T17:35:41.046435Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-math","f3d-scene","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-06-packed-transforms-materialization-roa.5","depends_on_id":"f3d-06-packed-transforms-materialization-roa","type":"parent-child","created_at":"2026-09-09T16:46:28.282471Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-06-packed-transforms-materialization-roa.5","depends_on_id":"f3d-06-packed-transforms-materialization-roa.2","type":"blocks","created_at":"2026-09-09T16:48:41.535764Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-06-packed-transforms-materialization-roa.6","title":"Attribute histories: CPU version, upload requests (r186 semantics), GPU-visible shadow","description":"## Full scope\n\nAttribute histories: public CPU content version, source upload-request version and effective update ranges matching r186 WebGLAttributes, last GPU-visible shadow per renderer replica, gap fill from shadow or proven-equal or smaller writes, sub-word (8/16-bit element) padding rule with a retained shadow, onUpload callback ordering\n\n## Summary\n\nImplement the plan's \"CPU mutation and GPU-visible mutation are different histories\" rule as the single attribute-upload authority.\n\n## Background (plan §5.8, §23 [S41]; AGENTS.md \"CPU mutation and GPU-visible mutation are different histories\")\n\n- \"An attribute may have changed in a CPU array while the source deliberately has not requested a GPU upload. Keep the public CPU content version, the source's upload-request/version/range behavior, and the last GPU-visible content separate. Preserve the pinned renderer's effective upload ranges, mutations to those ranges, and `onUpload` callback ordering. The r186 WebGL attribute implementation explicitly tracks versions and performs its own range coalescing; do not substitute an idealized upload model.\"\n- \"Coalescing new uploads across gaps must not expose additional CPU edits that the reference would leave GPU-stale. Fill such gaps from the last GPU-visible shadow, prove the bytes equal, or keep the smaller writes. The same rule applies to padding a sub-word update to WebGPU's alignment. A scan detecting a CPU mutation is not permission to upload it early.\"\n- \"Track these histories per device/renderer replica when a resource is shared across views or renderer instances.\"\n\n## Technical approach\n\n- Read the pinned `WebGLAttributes.js` (and the WebGPU backend's attribute utilities) and transcribe its version/`updateRanges`/`clearUpdateRanges` semantics into a spec table in the bead's test file.\n- `f3d-scene::attributes`: per attribute: `cpu_version` (bumped by any write barrier or scan), `upload_request` (mirrors `needsUpdate`/`version`/`addUpdateRange` exactly), per-replica `gpu_visible: { version, shadow: Option<Vec<u8>> }`.\n- Upload planner: from `upload_request` compute effective ranges per upstream's coalescing rule; for WebGPU 4-byte alignment or when merging adjacent ranges, fill the gap bytes from the shadow (if kept) or verify equality with the current CPU bytes; if neither, emit separate writes. For sub-word ranges (8/16-bit attribute elements: normalized colors, integer attributes, interleaved 8/16-bit data) there is no split option, since `writeBuffer` cannot write fewer than 4 bytes; the shadow is therefore retained for such attributes and its bytes are reported. Shadow retention policy is per attribute (size-bounded; report bytes).\n- `onUpload` ordering: fire after the replica's upload in the same order upstream does.\n- Replica tracking: attributes used by an exact-GL renderer and a new-backend renderer have separate histories and separate GPU allocations (§5.9).\n\n## Success criteria\n\n- Differential tests against upstream: sequences of partial updates with gaps, `needsUpdate` toggles without CPU changes, CPU changes without `needsUpdate`, interleaved buffers, resizes (new array identity), draw-range changes; the GPU-visible bytes on the candidate equal upstream's at every checkpoint.\n- Sub-word padding test: a single normalized `Uint8` color component write at an odd byte offset never exposes neighboring stale CPU edits (a `Float32` element is always 4-byte aligned, so it is not a sub-word case).\n\n## Test plan\n\n- Unit: planner tests transcribed from the pinned `WebGLAttributes.js` semantics (version bump without `needsUpdate` → no upload; `addUpdateRange` sequences with gaps; `clearUpdateRanges`; resize with new array identity); property tests over random write/request sequences asserting GPU-visible bytes equal the oracle's at every checkpoint; sub-word padding test (a single normalized `Uint8` color component write at an odd byte offset never exposes neighboring stale CPU edits; `Uint16`/`Int16` variants included); per-replica independence test.\n- E2E: `tests/e2e/attributes/histories.mjs` uploads through the bridge and reads back on Chrome and Safari for interleaved buffers, instanced attributes and draw-range changes.\n- Logging: per-upload range list `{attribute, request_ranges, effective_ranges, fill_source: request|shadow|split, bytes}` in debug; mismatch logs with the first differing byte offset; archived under `evidence/06.6/`.\n- Failure artifacts: hex dumps of expected vs actual GPU-visible ranges.\n\n## Considerations\n\n- Shared CPU attributes may require independent GPU allocations when disposal/update lifetimes diverge (§5.9).\n","acceptance_criteria":"For every BufferAttribute/InterleavedBuffer/InstancedBufferAttribute the three histories are tracked separately per device/renderer replica; coalescing never uploads CPU edits the reference would leave GPU-stale; sub-word updates (8/16-bit attribute elements whose byte range is not a multiple of 4) are padded to 4-byte alignment only with bytes taken from the GPU-visible shadow or proven equal to it, and there is no split option for sub-word writes because writeBuffer cannot write fewer than 4 bytes, so the shadow is retained for such attributes; onUpload callbacks fire in upstream order; array identity, interleaving, normalized/integer interpretation, draw ranges and resize behavior are preserved.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:28.365022Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:08.542146Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-scene","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-06-packed-transforms-materialization-roa.6","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.5","type":"blocks","created_at":"2026-09-09T16:48:41.848937Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-06-packed-transforms-materialization-roa.6","depends_on_id":"f3d-06-packed-transforms-materialization-roa","type":"parent-child","created_at":"2026-09-09T16:46:28.365022Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-06-packed-transforms-materialization-roa.6","depends_on_id":"f3d-06-packed-transforms-materialization-roa.3","type":"blocks","created_at":"2026-09-09T16:48:41.692575Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-06-packed-transforms-materialization-roa.7","title":"Adversarial and property tests for math and scene state","description":"## Full scope\n\nAdversarial and property tests for math and scene state: aliasing, typed-array writes, subclass overrides, reflective access, needsUpdate, empty data, negative scale, shear, singular matrices, degenerate triangles, ray-hit ties, extreme coordinates, NaN/Infinity, integer overflow boundaries\n\n## Summary\n\nThe plan's §14.7 adversarial program for the CPU core, written now so later optimizer beads inherit it.\n\n## Background (plan §14.7, §5.2, §7.4; AGENTS.md \"Adversarial and property tests\")\n\n\"Aliasing, typed-array writes, subclass overrides, reflective access, `needsUpdate`, callbacks that reenter rendering, mutable material keys, matrix-read materialization, deoptimization after an assumption changes. Geometry/math: empty data, negative scale, shear, singular matrices, degenerate triangles, ray-hit ties, extreme coordinates, NaNs, infinities, integer overflow boundaries.\"\n\n## Technical approach\n\n- Test scaffolding in `f3d-conformance` (or the scene crate's tests) that runs the same scenario twice: once through pinned upstream (Node with `three`), once through the candidate facade, then diffs traces.\n- Scenarios: alias a `position` vector and write through the alias; write into `attribute.array` directly then toggle `needsUpdate` (and not); subclass `Mesh` overriding `updateMatrixWorld`/`raycast`; `Object.getOwnPropertyDescriptor`/`Object.keys`/`JSON.stringify` on public objects; `Reflect.set`; empty geometries; scale `-1`; parent scaled non-uniformly with rotated child (shear); zero scale (singular) then `matrixWorld` inverse use in raycast; degenerate triangles; two coincident hits; coordinates near 1e308 and 1e-308; NaN/Infinity in transforms; `Uint32`/`Int32` attribute overflow cases.\n- Property generators for operation sequences over hierarchies and attributes.\n\n## Success criteria\n\n- All scenarios match upstream byte-for-byte (f64) or per the documented transcendental tolerance.\n- The suite runs in the functional lane and in the forced-new-backend lane where rendering is involved (later).\n\n## Test plan\n\n- Unit: the adversarial suite itself (aliasing, typed-array writes, subclass overrides, reflective access, `needsUpdate`, empty data, negative scale, shear, singular matrices, degenerate triangles, ray-hit ties, extreme coordinates, NaN/Infinity, integer overflow) run against retained JS and the packed path with a Node oracle; property generators for random op sequences.\n- E2E: the same scenarios executed in the browser (`tests/e2e/scene/adversarial.mjs`) in the functional lane and, once rendering exists, in the forced-new-backend lane; never skipped in benchmark mode.\n- Logging: each scenario logs the first divergence with the operation index, property path and both values; archived under `evidence/06.7/`.\n- Failure artifacts: the shrunk op sequence.\n\n## Considerations\n\n- Deoptimization-after-assumption-change cases are added by F3D-12a/12b as guards exist; the harness must make that easy.\n","acceptance_criteria":"A named adversarial suite exercises every listed case against both retained JS and the packed path with identical observable results as upstream; property tests generate random operation sequences with reference comparison; the suite is part of the functional lane and never skipped in benchmark mode.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:28.452956Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:01.401287Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-math","f3d-scene","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-06-packed-transforms-materialization-roa.7","depends_on_id":"f3d-06-packed-transforms-materialization-roa","type":"parent-child","created_at":"2026-09-09T16:46:28.452956Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-06-packed-transforms-materialization-roa.7","depends_on_id":"f3d-06-packed-transforms-materialization-roa.4","type":"blocks","created_at":"2026-09-09T16:48:42.013112Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-06-packed-transforms-materialization-roa.7","depends_on_id":"f3d-06-packed-transforms-materialization-roa.5","type":"blocks","created_at":"2026-09-09T16:48:42.171538Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-07-initial-webgpu-render-slice-2v8","title":"F3D-07: Initial accurate new WebGPU material/render slice (H1 toon/Inspector and H2 starting materials, no retained renderer submission)","description":"## What this epic delivers\n\nThe first real frames from the **new** backend. Plan §19 F3D-07 depends on 05 and 15 (early slice); acceptance: \"Complete H1 toon/Inspector and H2 starting materials, no retained renderer submission\". F3D-15's early shader work and this epic form \"one early vertical slice rather than a circular demand for finished subsystems\".\n\n## Why (plan §2.7, §3.3, §6.3, §8.1, §8.5, §9.2, §9.3, §11.7, §16.7)\n\n\"H1 uses `MeshToonNodeMaterial` and the Inspector... H2 starts with an environment-lit Standard material and exposes Lambert, Phong, texture/color, and four legacy `ShaderMaterial` choices. A basic unlit renderer cannot preserve those demonstrations and all their application controls.\" The corrected sequence brings the node/material slice, environment handling and the finite ESSL corpus into the first product gate. \"Prevent a compatibility-only wrapper from masquerading as this project\": scored workloads execute entirely through the new backend, with a call-path check.\n\n## Children\n\n1. `f3d-gpu` device/resource layer.\n2. `f3d-graph` initial pass graph with usage-scope hazards and canvas rules.\n3. Built-in material slice for H1/H2 with color management and environment/PMREM.\n4. Source-ordered snapshot execution protocol.\n5. H1 TSL/node construction path, NodeFrame update frequencies, Inspector hooks.\n6. Forced new-backend call-path assertion and ownership assertions.\n7. Explicit cost accounting instrumentation.\n8. Camera/projection/output conventions.\n\n## Success criteria for the epic\n\nAll children closed; H1 (WebGPU setting) and H2 (default + non-legacy materials) render through the new backend in the forced-new-backend lane with equivalence checkpoints passing; `CONTRACT.md` for `f3d-gpu`, `f3d-graph` and the first `f3d-shader` surface written.\n\n## No-claim boundary\n\nCorrect frames, not fast frames. Performance is scored only in F3D-11 with the frozen protocol.\n","acceptance_criteria":"The new WebGPU backend renders H1's MeshToonNodeMaterial scene with Inspector hooks and H2's starting materials (environment-lit Standard, Lambert, Phong, texture/color) with source-ordered per-use snapshots, through f3d-gpu/f3d-graph/f3d-shader, with a call-path assertion that no retained renderer submission occurred; outputs match upstream within preregistered tolerances on Safari and Chrome.","status":"open","priority":1,"issue_type":"epic","created_at":"2026-09-09T16:46:22.829071Z","created_by":"jemanuel","updated_at":"2026-09-09T16:48:45.874452Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-graph","f3d-shader","new-backend","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","type":"blocks","created_at":"2026-09-09T16:48:45.079759Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.7","type":"blocks","created_at":"2026-09-09T16:48:45.248349Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8","depends_on_id":"f3d-15a-early-shader-spike-7k8.2","type":"blocks","created_at":"2026-09-09T16:48:45.398393Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8","depends_on_id":"f3d-15a-early-shader-spike-7k8.3","type":"blocks","created_at":"2026-09-09T16:48:45.554908Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8","depends_on_id":"f3d-gate-1-foundation-tzn","type":"blocks","created_at":"2026-09-09T16:48:45.716580Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8","depends_on_id":"f3d-gate-2-bridge-apz","type":"blocks","created_at":"2026-09-09T16:48:45.874138Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-07-initial-webgpu-render-slice-2v8.1","title":"f3d-gpu device/resource layer: generations, staging rings, budgets, async validation","description":"## Full scope\n\nf3d-gpu device/resource layer: negotiated device profile, generation-tagged resources with explicit lifecycle states, bounded staging rings and frames-in-flight backpressure, pipeline/variant caches with byte and count budgets, asynchronous validation and uncaptured-error collection, device-loss classification hooks\n\n## Summary\n\nThe resource half of the new backend, with the plan's lifetime rules built in from the first line.\n\n## Background (plan §6.5, §11.4, §11.5, §11.7, §12.2, §12.5)\n\n- \"Maintain bounded staging rings and a configurable small number of frames in flight. Exhaustion creates backpressure, not unbounded allocation. Pipeline variants, transient resources, and asset caches have explicit byte/count budgets and eviction policies.\"\n- \"Pending, not-yet-submitted commands must not reference a destroyed or recycled resource.\" \"An in-flight use can outlive that residency's public release event.\"\n- \"A failed compilation must not become a published valid cache entry. Scope memory budgets conservatively because out-of-memory handling is not a dependable capacity-discovery algorithm.\"\n- \"Treat CPU/Wasm memory, upload staging, and GPU allocations as distinct budgets even on unified-memory hardware.\"\n\n## Technical approach\n\n- Resource arenas per kind (buffer, texture, sampler, bind group layout/bind group, pipeline, bundle) using `GpuHandle` with device generation; state enum per entry; retirement queue keyed by submission epoch (consumed by F3D-22a.5).\n- Staging: N-frame ring of upload buffers sized by budget; `writeBuffer` path (synchronous copy semantics, source released immediately) vs staging-buffer + encoded copy path (mapped lifetime distinct); credit accounting.\n- Caches: pipeline variant cache keyed by shader hash + layout + render-state key, with byte/count budget and LRU eviction; transient texture pool (used by F3D-17.2) with usage/format compatibility keys.\n- Validation: pipeline creation via `createRenderPipelineAsync`/`createComputePipelineAsync` where available, results recorded per generation; error-scope helper from 05.6; `uncapturederror` and `lost` events feed structured diagnostics with generation tags.\n- Device-loss hook: on loss, mark the generation dead; all handles of that generation fail decode; recovery policy is F3D-22a.3's responsibility.\n- Eviction versus bundles: eviction is refused for pipelines/bind groups referenced by a live render bundle; any other eviction emits an invalidation event consumed by 08.4 so a bundle can never reference a destroyed object.\n\n## Success criteria\n\n- Unit tests for lifecycle transitions and budget backpressure; browser tests for async pipeline failure not being cached and for uncaptured-error routing.\n- Memory report shows the three budgets separately.\n- `f3d-gpu/CONTRACT.md` is written before any other crate depends on `f3d-gpu` (AGENTS.md \"Documentation and Contracts\"): purpose and position in the dependency direction, public types and semantics, invariants, implementation ownership route, error model, determinism and numeric-fidelity class, cancellation behavior, unsafe boundary if any, feature flags, conformance tests, and no-claim boundaries.\n\n## Test plan\n\n- Unit: lifecycle-state transition tests (CPU-exists → GPU-allocated → initialized → submitted → retired) including illegal transitions; staging-ring backpressure test (exhaustion blocks, never allocates); cache budget/eviction tests; property tests on the retirement queue vs handle generations.\n- E2E: `tests/e2e/gpu/resources.mjs` on Chrome and Safari: async pipeline failure is not cached; `uncapturederror` routes to a structured event with the right generation; device-loss hook marks the generation dead and all its handles fail decode.\n- Logging: bounded structured events for allocation, retirement, eviction and validation failure `{kind, handle, generation, bytes, reason}`; per-frame memory `metric` events for the three budgets; archived under `evidence/07.1/`.\n- Failure artifacts: the resource arena dump at the failing transition.\n\n## Considerations\n\n- No `wgpu` stack; the browser supplies platform translation (§8.1). This crate talks to the ABI/bridge, not to Metal.\n","acceptance_criteria":"f3d-gpu owns GPU resource creation/retirement behind generation-checked handles with states (CPU-exists, GPU-allocated, initialized, submitted-in-use, retired); staging rings and a configurable frames-in-flight limit produce backpressure rather than unbounded allocation; pipeline variants, transient resources and asset caches have explicit budgets and eviction; async validation results and uncaptured errors are collected per generation; a failed pipeline compilation never becomes a valid cache entry; device-loss events transition every resource of that generation.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:28.542991Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:19.927035Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","new-backend","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.1","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","type":"blocks","created_at":"2026-09-09T16:48:42.834320Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.1","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.7","type":"blocks","created_at":"2026-09-09T16:48:42.996317Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.1","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8","type":"parent-child","created_at":"2026-09-09T16:46:28.542991Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.1","depends_on_id":"f3d-gate-1-foundation-tzn","type":"blocks","created_at":"2026-09-09T17:15:28.057283Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.1","depends_on_id":"f3d-gate-2-bridge-apz","type":"blocks","created_at":"2026-09-09T17:15:28.453375Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-07-initial-webgpu-render-slice-2v8.2","title":"f3d-graph initial pass graph with usage-scope hazards and canvas rules","description":"## Full scope\n\nf3d-graph initial pass graph: resource reads/writes, explicit attachment load/store/clear/resolve, required order and history dependencies, usage-scope hazard tracking over subresource ranges (whole-buffer rule, per-draw writable alias rule), pass splitting, canvas-texture acquisition per render interval, per-canvas output epochs\n\n## Summary\n\nThe middle representation of §6.1 (\"pass graph\") at its initial scope, with hazards defined by API usage scopes rather than intuition.\n\n## Background (plan §6.1, §8.5 \"Resource hazards are defined by API usage scopes\", \"Canvas, media, and projection state\", §23 [S48], [S51])\n\n- \"Track resource versions and overlapping mip/layer/aspect ranges, not only texture names. For usage-scope validation a buffer is a whole subresource: assigning disjoint byte ranges incompatible roles does not make the shared buffer legal... A render pass has a usage scope whose restrictions can forbid an attachment/sampling combination even when a source scheduler places the corresponding draws in a seemingly safe order. A compute dispatch has its own usage-scope rules. Split passes, use a distinct versioned resource, or select another legal plan when necessary. Do not invent explicit native memory barriers that browser WebGPU does not expose.\"\n- \"Attachment load/store/clear/resolve operations are explicit dependencies, never inferred solely from whether a later pass samples the image.\"\n- \"Acquire the current canvas texture for the actual rendering interval; do not keep its texture/view in a long-lived scene cache... A zero-sized or hidden canvas follows a defined pause/resize policy without creating illegal resources. Multiple canvases/renderer instances have separate output and resource epochs.\"\n\n## Technical approach\n\n- `f3d-graph`: `Pass { kind: Render|Compute|Copy, reads: [ResourceUse], writes: [ResourceUse], attachments: [AttachmentOp], order_constraints }` with `ResourceUse { handle, version, subresource: {mips, layers, aspect} | WholeBuffer, usage }`.\n- Validator implementing the usage-scope rules: per render pass, the set of (resource, subresource) usages must be compatible (read-only combos vs single write); per dispatch (not per pass) for compute; copy commands validated separately. On conflict: automatic pass split or versioned resource duplication with a report line.\n- Scheduler: topological order respecting explicit dependencies and history (previous-frame reads); no reordering across observable boundaries (§6.3).\n- Canvas: `CanvasOutput` resource kind whose texture handle is valid only inside a frame's \"acquire → submit\" interval; the graph refuses to store it; resize/reconfigure/device change invalidates the attachment plan; hidden/zero-size policy documented.\n- Output: an \"execution plan segment\" list consumed by the bridge encoder.\n\n## Success criteria\n\n- Unit tests: an illegal same-pass attachment+sample is split; disjoint byte-range trick on one buffer is rejected; compute per-dispatch alias rule enforced; canvas texture caching attempt rejected.\n- Browser test: the plan renders H1's simple single-pass scene and a two-target scene correctly.\n- `f3d-graph/CONTRACT.md` is written before any other crate depends on `f3d-graph` (AGENTS.md \"Documentation and Contracts\"): purpose and position in the dependency direction, public types and semantics, invariants, implementation ownership route, error model, determinism and numeric-fidelity class, cancellation behavior, unsafe boundary if any, feature flags, conformance tests, and no-claim boundaries.\n\n## Test plan\n\n- Unit: validator tests (attachment+sample in one pass is split; disjoint byte-range trick on one buffer rejected; compute per-dispatch writable-alias rule; copy validated separately); canvas-texture caching attempt rejected; scheduler tests for explicit dependency ordering; fuzzed pass sets (feeds 05.3's serialized-plan fuzz target).\n- E2E: `tests/e2e/graph/basic.mjs` renders a single-pass and a two-target scene through the plan on both browsers and compares to upstream; resize/reconfigure invalidation test; two canvases with separate epochs.\n- Logging: plan dump per frame in debug `{pass, kind, reads, writes, attachments, split_reason}`; validation failures at `error` with the conflicting uses; archived under `evidence/07.2/`.\n- Failure artifacts: the serialized plan that failed validation.\n\n## Considerations\n\n- A copy cannot be inserted inside a render pass as though it were a draw (§6.7); copies are their own pass kind.\n","acceptance_criteria":"The pass graph represents render/compute passes with typed resource handles, versions and subresource ranges; validation enforces WebGPU usage-scope rules (a buffer is a whole subresource for scope purposes; distinct per-draw restrictions on writable binding aliases; attachment vs sampling conflicts inside one pass) by splitting passes or selecting another legal plan; attachment operations are explicit dependencies; canvas textures are acquired for the actual rendering interval and never cached across frames; multiple canvases/renderers have separate output epochs.","notes":"2026-09-10 user-authorized critical implementation wave. Scope: Executable pass graph with resource hazards. Start implementation while dependencies await verification; existing acceptance conditions and dependency edges remain binding for closure. Owned paths: crates/f3d-graph/** only","status":"in_progress","priority":1,"issue_type":"task","assignee":"CobaltOrchid","created_at":"2026-09-09T16:46:28.632210Z","created_by":"jemanuel","updated_at":"2026-09-10T20:09:50.646361Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-graph","new-backend","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.2","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.5","type":"blocks","created_at":"2026-09-09T16:48:43.313310Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.2","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8","type":"parent-child","created_at":"2026-09-09T16:46:28.632210Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.2","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.1","type":"blocks","created_at":"2026-09-09T16:48:43.158365Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-07-initial-webgpu-render-slice-2v8.3","title":"Built-in material slice for H1/H2 (Toon node, Standard+PMREM, Lambert, Phong, texture/color)","description":"## Full scope\n\nBuilt-in material slice for H1/H2: MeshToonNodeMaterial, environment-lit MeshStandardMaterial with PMREM, MeshLambert, MeshPhong, texture/color variants and required depth/normal paths, with pinned lighting equations, color management, tone mapping, premultiplication and output conversion\n\n## Summary\n\nExactly the material surface the first product gate needs, implemented accurately, not generically.\n\n## Background (plan §2.7, §9.1, §9.2, §9.6, §15.3)\n\n- \"Pin the exact upstream material and lighting behavior. Implement Basic, normal/depth/distance, Lambert/Phong/Toon/Matcap, Standard/Physical... in the order the test/dependency census requires.\" H1 needs Toon (node material); H2 needs environment-lit Standard by default plus Lambert/Phong/texture/color branches (the four legacy `ShaderMaterial` variants come through 15a.2).\n- \"Match color-space handling, texture interpretation, exposure/tone mapping, premultiplication, environment filtering, normal/tangent conventions, and output conversion. Use controlled reference scenes and material sweeps, including high-contrast and near-degenerate cases.\"\n- \"It is not enough to render a visually plausible generic PBR model.\"\n\n## Technical approach\n\n- `f3d-shader` material semantics: transcribe the pinned upstream shader chunks/node functions for the listed materials (Lambert/Phong BRDFs, Standard's GGX/Schlick/multiscatter as pinned, Toon's gradient step, PMREM sampling and `envMapIntensity`, `LinearSRGB`/`sRGB` conversions, tone-mapping operators in use by H2's default, `outputColorSpace`) into typed shader IR emitted as WGSL through the F3D-15a pipeline (node path for Toon; ESSL/chunk path for the classic materials).\n- PMREM generation: retain the upstream `PMREMGenerator` construction logic where it is JS-only cold code, executing its passes through the new backend (this is \"retained construction/baking logic\" allowed by §5.12 Lights row), with the call-path check confirming submission goes through the new backend.\n- Resource binding: material parameter blocks from the layout schema (05.2); textures/samplers per material map slots used by these materials (`map`, `envMap`, `normalMap` if H2 uses them; verify from the source inventory in 01.6).\n- Differential tests: reference scenes (sphere/plane sweeps over roughness/metalness/emissive/opacity, environment intensity, tone mapping on/off) rendered by upstream WebGL and WebGPU renderers and by the candidate; compare with color-space-aware metrics from 03.6; diagnostic targets for normals/depth/albedo.\n- H2's non-legacy branches (verify the exact list from 01.6's closure record) also require: cube reflection and refraction environment mapping (`CubeReflectionMapping`/`CubeRefractionMapping`, the `liquid` branch), `flatShading`, `vertexColors` (the color branches), a repeating `map` texture (the textured branch), and the renderer-side lazy cube-to-PMREM conversion for `MeshStandardMaterial` env maps (the example passes a raw `CubeTexture`, so the `WebGLCubeUVMaps` equivalent runs inside the renderer, not application code).\n\n## Success criteria\n\n- H1's toon scene and H2's default/Lambert/Phong/texture/color branches match upstream at all checkpoints in the forced-new-backend lane.\n- Sweep tests pass within tolerance; high-contrast/near-degenerate cases included.\n\n## Test plan\n\n- Unit: shader-IR snapshot tests per material (Toon node, Standard with PMREM, Lambert, Phong, texture/color, depth/normal); parameter-block layout tests; color-space conversion unit tests against upstream's `ColorManagement` values.\n- E2E: differential sweeps `tests/e2e/materials/h1h2-slice.mjs` on Chrome and Safari: sphere/plane sweeps over roughness/metalness/emissive/opacity/envMapIntensity/tone mapping compared to upstream WebGL and WebGPU renderers with the 03.6 tolerances; diagnostic targets (normal/depth/albedo) compared; H1 toon scene and H2 branches at checkpoints in the forced-new-backend lane.\n- Logging: per-sweep-step `metric` events `{material, params, max_err, mean_err, worst_region}`; diagnostic mismatch events name the channel; archived under `evidence/07.3/`.\n- Failure artifacts: reference/candidate/diff images per failing step and the diagnostic target images.\n\n## Considerations\n\n- Do not \"fuse\" or approximate here (§9.6); this is the conservative reference generation path that later optimizations are compared against.\n- Full material closure is F3D-14; do not expand scope beyond the H1/H2 inventory.\n","acceptance_criteria":"The listed materials render through the new backend with pinned upstream lighting equations, color-space handling, texture interpretation, exposure/tone mapping, environment filtering (PMREM), normal/tangent conventions and output conversion; controlled reference scenes and parameter sweeps match upstream within preregistered tolerances; diagnostic targets (normal/depth/channel) match.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:28.720866Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:19.209489Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-shader","new-backend","phase-1","tier-solid","wgsl"],"dependencies":[{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.3","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8","type":"parent-child","created_at":"2026-09-09T16:46:28.720866Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.3","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.2","type":"blocks","created_at":"2026-09-09T16:48:43.468470Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.3","depends_on_id":"f3d-15a-early-shader-spike-7k8.2","type":"blocks","created_at":"2026-09-09T16:48:43.785826Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.3","depends_on_id":"f3d-15a-early-shader-spike-7k8.3","type":"blocks","created_at":"2026-09-09T16:48:43.631118Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-07-initial-webgpu-render-slice-2v8.4","title":"Source-ordered snapshot execution protocol with per-use buffer slices","description":"## Full scope\n\nSource-ordered snapshot execution protocol: per-admitted-operation guard/execute/materialize, render-boundary consumption at proper versions, per-draw and per-pass snapshots of callback mutations using immutable per-use buffer slices, reentrant logical render contexts, submission at observation boundaries, no coalescing of observable operations\n\n## Summary\n\nThe execution protocol that makes queue-ordering correctness structural rather than accidental. With no admitted islands this protocol is the General WebGPU route of §3.3 (conservative snapshots at every boundary, runtime variants, unoptimized scheduling); it must work for any routed application, not only islands, and it is what the forced lane runs for non-corpus examples.\n\n## Background (plan §6.3, §6.7, §5.5, §8.2 \"Incremental transforms and uploads\")\n\n- \"There is no new universal application tick that owns the timing of every animation, callback, and render. The compiled program follows the source call/effect order. A browser presentation frame may contain zero, one, or many logical render/compute operations.\"\n- \"A material change between two draws requires two draw-visible values even when both commands share one submission. Reentrant rendering uses nested logical render contexts and restores the outer camera, target, viewport/scissor, callback, and resource state at the same boundaries as the reference. It cannot overwrite an outer operation's scratch packet.\"\n- \"No optimization coalesces away an observable render, update, clear, read, callback, or history transition.\"\n- Strategies for per-use values: immutable per-use buffer slices with distinct bindings/offsets, source-indexed parameter tables, legal encoded copies between passes, or separate submissions; optional immediates as an admitted strategy, not a portability assumption.\n\n## Technical approach\n\n- `f3d-runtime::frame`: `LogicalRender` context stack; `render(scene, camera, target)` entry performs upstream's ordered steps (matrix world updates unless auto-update disabled, `onBeforeRender` callbacks in upstream order, render list assembly consuming versions from 06.3, uploads from 06.6 at their request versions, node updates via F3D-07.5, draw emission, `onAfterRender`).\n- Per-use snapshots: parameter blocks are written into a per-frame ring region with an offset per (draw, version); a bind group with dynamic offset (respecting the device's uniform offset alignment from 05.2) selects the slice; a changed value between draws allocates a new slice, never overwrites.\n- Submission boundaries: a submission is issued at each point the source could observe GPU results or at the end of the host turn, whichever the source's contract requires; readbacks and `onSubmittedWorkDone` credits attach to those epochs.\n- Reentrancy: nested `render()` from a callback pushes a new context with its own scratch region; outer state restored on pop; assertion that the outer packet is untouched.\n\n## Success criteria\n\n- Red-A/blue-B passes through this protocol (not just the bridge test); nested-render test with distinct cameras/targets; a callback that mutates a material between two draws produces two values.\n- Trace equality with upstream for H1/H2 callback ordering.\n\n## Test plan\n\n- Unit: slice allocation tests (a changed value between draws allocates a new slice; alignment honored); context-stack tests (push/pop restores outer state; outer packet untouched); protocol ordering tests with a scripted fake renderer.\n- E2E: `tests/e2e/protocol/*.mjs` in the forced-new-backend lane on both browsers: red-A/blue-B through the protocol; nested render with distinct cameras/targets; callback mutating a material between two draws yields two values; H1/H2 callback-order trace equality with upstream.\n- Logging: per-frame `metric` events `{slices_allocated, submissions, materializations, contexts_max_depth}`; ordering violations logged with the two conflicting steps; archived under `evidence/07.4/`.\n- Failure artifacts: readbacks of the affected targets and the packet dump.\n\n## Considerations\n\n- Loading callbacks observable before the next animation tick cannot be delayed to a frame boundary (§6.3).\n- Optional immediates are a later capability-checked strategy (F3D-20.3).\n","acceptance_criteria":"The frame path implements the §6.3 protocol exactly: validate guards, execute source-ordered updates exactly once, materialize state visible at return; at render boundaries consume source-visible state and upload requests at their versions, perform reference-required matrix/node/callback updates in order, snapshot each draw/pass's required data version including callback mutations, refresh only invalidated plan segments, emit legal ordered queue writes/copies and command work, submit at the required boundary; nested render calls restore outer camera/target/viewport/scissor/callback/resource state; a material change between two draws in one submission yields two draw-visible values.","notes":"2026-09-11: CobaltOrchid implements source-ordered nested render protocol by explicit user direction (Mail6727); tracker claim remains blocked by prerequisites. Native/browser verification and original acceptance remain required.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:28.809103Z","created_by":"jemanuel","updated_at":"2026-09-11T01:18:48.788816Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-graph","f3d-runtime","new-backend","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.4","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.5","type":"blocks","created_at":"2026-09-09T16:48:44.264515Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.4","depends_on_id":"f3d-06-packed-transforms-materialization-roa.3","type":"blocks","created_at":"2026-09-09T16:48:44.104733Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.4","depends_on_id":"f3d-06-packed-transforms-materialization-roa.6","type":"blocks","created_at":"2026-09-09T17:41:18.592161Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.4","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8","type":"parent-child","created_at":"2026-09-09T16:46:28.809103Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.4","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.2","type":"blocks","created_at":"2026-09-09T16:48:43.944706Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-07-initial-webgpu-render-slice-2v8.5","title":"H1 TSL/node construction path, NodeFrame update frequencies, Inspector hooks","description":"## Full scope\n\nH1 TSL/node construction path: JavaScript-constructed node graphs translated to typed shader IR, dynamic uniforms retained as data, structural invalidation, NodeFrame update/updateBefore/updateAfter at FRAME/RENDER/OBJECT frequencies with reference identity and the return-false marker rule, reentrant context stack, Inspector hooks\n\n## Summary\n\nThe node/TSL entrance at H1's scope, with NodeFrame semantics preserved exactly.\n\n## Background (plan §9.3, §2.7, §5.14 \"Editor/Inspector/devtools\", §23 [S42])\n\n- \"Let ordinary JavaScript construct TSL graphs, then translate recognized nodes into typed shader IR. Retain dynamic uniforms as data; their changes do not by themselves require rebuilding the shader graph. Structural changes invalidate the affected program and pass dependencies. Do not imply that current Three.js necessarily rebuilds every graph each frame.\"\n- \"Preserve `updateBefore`, `update`, and `updateAfter` at the source's FRAME, RENDER, or OBJECT frequency, with the correct renderer/camera/material/object/scene/compute context and update-reference identity. The pinned `NodeFrame` also treats a callback returning `false` specially when advancing update markers. A physical batch must not collapse multiple logically distinct updates or advance a marker before a failed update would have done so upstream. Reentrant rendering uses an explicit context stack.\"\n- Inspector: \"Rust source maps and generated shader inspection augment rather than replace existing APIs.\"\n\n## Technical approach\n\n- Retain upstream's TSL construction modules (cold JS) so the application's node objects are real upstream nodes; add a translator that walks the graph (via the node's public `getNodeType`/`generate`-adjacent structure or a recognized node table) into `f3d-shader` IR; unrecognized node → conservative path: use upstream's WGSL node builder output as the shader source (retained shader-construction helper, allowed and charged, §9.3) submitted through the new backend.\n- Uniform data path: `UniformNode` values bound to parameter block slots; version bumps only.\n- NodeFrame emulation: implement the pinned `NodeFrame` update-marker semantics (`frameId`, `renderId`, per-object markers, `updateBeforeType`/`updateType`/`updateAfterType`, the `false` return rule) in the runtime; call the node's JS callbacks with the same context object shape upstream provides.\n- Inspector: keep upstream's Inspector module and its renderer hooks working against the routed renderer object; where the Inspector reads renderer stats, provide the public contract's values (logical counts) and expose F3D physical telemetry only under the separate F3D namespace (§5.7).\n- Retained-builder host: upstream's `WGSLNodeBuilder` is constructed with `(object, renderer)` and queries the renderer/backend (capabilities, limits, node library, lighting/environment nodes); the fallback drives it through a headless retained `Renderer` instance with submission disabled (or a minimal backend shim exposing only the queries the builder makes), compiles and submits the resulting WGSL through the new backend, and attributes the use as retained shader construction in the report.\n\n## Success criteria\n\n- H1 renders via node translation with per-object toon colors; uniform-change no-rebuild test; structural-change invalidation test; NodeFrame marker tests transcribed from the pinned source (including `false`).\n- Inspector opens on H1 and shows the same public fields as upstream.\n\n## Test plan\n\n- Unit: NodeFrame marker state-machine tests transcribed from the pinned source (FRAME/RENDER/OBJECT frequencies, `updateBefore`/`update`/`updateAfter`, the `false` return rule, reference identity); translator snapshot tests for H1's toon graph; uniform-change no-rebuild and structural-change invalidation tests.\n- E2E: `tests/e2e/nodes/h1-toon.mjs` in the forced-new-backend lane: H1 renders via translation with per-object colors; scripted uniform sweeps cause zero recompiles; a structural edit invalidates only affected programs; the Inspector opens on H1 and its panels match upstream's public fields.\n- Logging: graph translation report `{node, recognized|retained-builder}` in the build report; per-frame node-update `metric` events `{frequency, count}`; archived under `evidence/07.5/`.\n- Failure artifacts: IR dump of the failing graph and the Inspector panel snapshot.\n\n## Considerations\n\n- Full node coverage is F3D-16; do not chase it here.\n- Never substitute a constant or silently keep an old graph (§9.3).\n","acceptance_criteria":"H1's MeshToonNodeMaterial graphs constructed by ordinary JS are translated to shader IR and WGSL; changing a uniform node's value does not rebuild the graph, a structural change invalidates only affected programs/passes; node update callbacks run at the pinned NodeFrame frequencies with the correct renderer/camera/material/object/scene context and identity, a callback returning false does not advance the marker, and physical batching never collapses logically distinct updates; the upstream Inspector attaches to the routed renderer and its panels populate with truthful data.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:28.897263Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:18.851159Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-runtime","f3d-shader","new-backend","phase-1","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.5","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8","type":"parent-child","created_at":"2026-09-09T16:46:28.897263Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.5","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.3","type":"blocks","created_at":"2026-09-09T16:48:44.434602Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.5","depends_on_id":"f3d-21a-early-host-tool-integrations-ff1.3","type":"blocks","created_at":"2026-09-09T17:31:50.066925Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-07-initial-webgpu-render-slice-2v8.6","title":"Forced new-backend call-path assertion and per-test ownership assertions","description":"## Full scope\n\nForced new-backend call-path assertion and per-test ownership assertions: prove scored and forced-lane rendering never goes through retained renderer submission\n\n## Summary\n\nThe mechanism that keeps a compatibility wrapper from masquerading as this project.\n\n## Background (plan §3.3, §5.16, §6.9, §14.1, §14.2, §16.6 \"New-backend ownership\"; AGENTS.md \"Three production-route lanes\")\n\n- \"The H1–H8 scored workloads execute entirely through that new GPU backend, with a call-path check that rules out retained renderer submission.\"\n- \"Every accelerated test asserts that its designated rendering work uses the new backend; every exact-component test asserts that it receives actual native objects and preserves the source's lifecycle.\"\n- \"Lane 2 exists specifically to prevent a compatibility-only wrapper from masquerading as this project.\"\n\n## Technical approach\n\n- Attribution: the bridge/adapter tags each `queue.submit` with the route (`new-backend`, `exact-backend`, `retained-renderer`) and the logical render context; retained upstream renderers are wrapped at their submission entry (WebGL `gl.drawElements`/WebGPU backend `submit`) only for attribution counting in test builds.\n- Lane switch: a switch set at build time (`f3d build --lane forced-new-backend`) or injected by the harness as a global before module evaluation (an environment variable cannot reach browser code) makes the router refuse retained renderer submission for all portable families (exact-GL contract cases are excluded from that lane by declaration, not by silent skipping).\n- Assertion helpers for tests: `expectRoute('new-backend')`, `expectNativeGL(ctx)`.\n- Reporting: per-test owner tags flow into the manifest status updater (F3D-18a.5).\n\n## Success criteria\n\n- H1/H2 in the forced lane render with zero retained submissions; deliberately routing a draw through a retained renderer fails the assertion with a stack.\n\n## Test plan\n\n- Unit: attribution counter tests with a fake adapter (each route increments its own counter; the lane switch refuses retained submission with a stack).\n- E2E: H1/H2 in the forced-new-backend lane show zero retained submissions; a deliberate retained draw fails `expectRoute('new-backend')` with the offending call path; an exact-lane test asserts `expectNativeGL`.\n- Logging: per-frame route counts `{new_backend, exact_backend, retained}` (bounded) and per-test owner tags; archived under `evidence/07.6/`.\n- Failure artifacts: the attribution stack for the offending submission.\n\n## Considerations\n\n- Retained cold helpers (construction/baking logic) are allowed and charged; only *submission* is what this asserts.\n","acceptance_criteria":"A runtime attribution mechanism records which route submitted every command buffer; a test-lane switch disables retained renderer submission entirely; every accelerated test asserts its designated rendering went through the new backend and every exact-component test asserts it received actual native objects; an accidental retained submission fails the test with the offending call path.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:28.987526Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:20.297773Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","f3d-runtime","new-backend","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.6","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8","type":"parent-child","created_at":"2026-09-09T16:46:28.987526Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.6","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.1","type":"blocks","created_at":"2026-09-09T16:48:44.594609Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-07-initial-webgpu-render-slice-2v8.7","title":"Explicit cost accounting instrumentation in a separate F3D telemetry namespace","description":"## Full scope\n\nExplicit cost accounting instrumentation (debug/performance build): CPU preparation, bridge encoding, upload bytes/calls, binding changes, bundle rebuilds, draw/dispatch counts, pipeline creation, staging memory, GPU timing where supported; removable/bounded in production; separate F3D telemetry namespace\n\n## Summary\n\nKnow where every microsecond and byte goes, without perturbing the thing being measured.\n\n## Background (plan §8.8, §5.7, §16.8; AGENTS.md \"Output Style\")\n\n- \"The debug/performance build records CPU preparation, bridge encoding, upload bytes/calls, binding changes, bundle rebuilds, draw/dispatch counts, pipeline creation, staging memory, and GPU timing where supported. Instrumentation must be removable or bounded in production.\"\n- \"Never present the original renderer's logical draw count as the new backend's actual GPU work. F3D telemetry lives in its own diagnostic namespace.\"\n- Ablations (§16.8) and attribution need these counters.\n\n## Technical approach\n\n- `f3d-runtime::telemetry`: fixed-size per-frame counter struct (no allocation), timestamps from the measurement clock, optional GPU timestamp queries when the `timestamp-query` feature is enabled on the device, recorded via `timestampWrites` on pass descriptors (recorded as unavailable otherwise; Chrome quantizes timestamps to 100 µs unless a developer flag is set, so the resolution/quantization is captured in the capability record and reported with every GPU timing); ring of the last N frames.\n- Exposure: `window.__f3d.telemetry` (or the report API) in debug/perf builds; `f3d bench` consumes it; the build report includes structural estimates separately from measured timings (§3.5).\n- Compile-time feature flag to remove the layer; a bounded sampling mode for production diagnostics.\n\n## Success criteria\n\n- Counters validated against known synthetic frames (exact draw/upload counts).\n- Overhead of the instrumentation measured and reported (must be small relative to the protocol's noise band).\n\n## Test plan\n\n- Unit: counter validation on synthetic frames with exact expected draw/upload/binding counts; an allocation-counter test proving no per-draw heap allocation; a compile-out test for the production feature flag.\n- E2E: `tests/e2e/telemetry/counters.mjs` compares counters with the adapter's actual WebGPU call counts on both browsers; instrumentation overhead measured against the protocol noise band.\n- Logging: the counters are the logging; overhead reported as a `metric` event; archived under `evidence/07.7/`.\n- Failure artifacts: the counter diff table.\n\n## Considerations\n\n- GPU timestamps: account for timer precision; unavailable telemetry stays unavailable (§16.4).\n","acceptance_criteria":"A bounded instrumentation layer records the listed counters per logical frame and exposes them through an F3D-specific diagnostic namespace and the build/bench reports; production builds compile it out or bound it; the renderer never allocates a JSON event or runs a statistical controller per draw; upstream renderer.info contracts are untouched.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:29.083229Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:23.333122Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-gpu","f3d-runtime","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.7","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8","type":"parent-child","created_at":"2026-09-09T16:46:29.083229Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.7","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.1","type":"blocks","created_at":"2026-09-09T16:48:44.762907Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-07-initial-webgpu-render-slice-2v8.8","title":"Camera/projection/output conventions on the new backend","description":"## Full scope\n\nCamera, projection and output conventions on the new backend: reversed/logarithmic depth, cube-face orientation, viewport/scissor origins, alpha mode, color space, render-target output conversion; never overwrite the public WebGL projection matrix; same camera to a target and to the canvas\n\n## Summary\n\nCoordinate-system correctness at the renderer level (the shader-level treatment is 15a.4).\n\n## Background (plan §8.5 \"Canvas, media, and projection state\", §9.4 last paragraph)\n\n\"Keep public camera/matrix observations consistent with the source backend. A private clip-depth/projection conversion must not overwrite a public WebGL projection matrix with WebGPU conventions. Match reversed/logarithmic depth, cube-face orientation, viewport/scissor origins, alpha mode, color space, and render-target output conversion. Add tests for rendering the same camera to both a target and the canvas; a generic final tone-mapping pass is not automatically equivalent.\"\n\n## Technical approach\n\n- Projection handling: the public `camera.projectionMatrix` stays as upstream computes it; the backend derives its clip-space adjustment (WebGPU 0..1 depth vs WebGL -1..1) privately in the shader IR (uniform or constant fold), never writing back.\n- Depth modes: reversed depth and logarithmic depth per the renderer flags in use by the corpus; cube camera face orientation per upstream's `CubeCamera` conventions; viewport/scissor with the correct origin conventions; `alphaMode`, `outputColorSpace`, premultiplied output.\n- Tests transcribed from upstream expectations: render a checker scene to canvas and to a target; compare both to upstream's WebGPU renderer output and to its WebGL output; a scene with `logarithmicDepthBuffer`; a cube camera environment capture compared face by face.\n- Source-convention rule: the private clip-depth conversion applies only to WebGL-authored sources; `WebGPURenderer`-authored sources already carry WebGPU conventions in their public projection matrix (upstream sets `camera.coordinateSystem`), so no conversion is applied; exactly one conversion per source kind is asserted (shared rule with 15a.4).\n\n## Success criteria\n\n- All comparisons pass within tolerance on both browsers; public projection matrix equality asserted before and after render.\n\n## Test plan\n\n- Unit: projection-adjustment tests (public `projectionMatrix` unchanged before/after render; private clip-space adjustment applied once); depth-mode tests (reversed, logarithmic); viewport/scissor origin tests; cube-face orientation tests.\n- E2E: `tests/e2e/output/conventions.mjs` on both browsers: checker scene rendered to canvas and to a target compared against upstream WebGPU and WebGL output; logarithmic-depth scene; cube camera capture compared face by face; alpha mode/color space/premultiplied output cases.\n- Logging: per-case `metric` events `{case, target: canvas|rt, max_err, face}`; archived under `evidence/07.8/`.\n- Failure artifacts: per-face/per-target diffs.\n\n## Considerations\n\n- Two full-screen passes are not equivalent to their algebraic composition at higher precision (§5.5); output conversion stays a distinct step.\n","acceptance_criteria":"Public camera/matrix observations remain identical to the source backend while the new backend applies any private clip-depth/projection conversion internally; reversed and logarithmic depth, cube-face orientation, viewport/scissor origins, alpha mode, color space and render-target output conversion match upstream; a test renders the same camera to a render target and to the canvas and compares both against upstream.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:29.173014Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:13.192655Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-shader","new-backend","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.8","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8","type":"parent-child","created_at":"2026-09-09T16:46:29.173014Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-07-initial-webgpu-render-slice-2v8.8","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.3","type":"blocks","created_at":"2026-09-09T16:48:44.918979Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-08-legal-grouping-versions-bundles-ago","title":"F3D-08: Legal grouping, per-use uniform versions and render bundles","description":"## What this epic delivers\n\nThe first genuine sources of gain, each behind a legality statement. Plan §19 F3D-08 depends on 06, 07 and the early slice of 12; acceptance: \"Guards/callbacks/IDs, red-A/blue-B and bundle/direct tests\".\n\n## Why (plan §8.2, §8.3, §5.5, §6.7, §14.8, §16.1)\n\n\"Render bundles, compute shaders, instancing, and WebGPU support are baseline techniques, not FrankenThreeD's novelty.\" The gain must come from combining them with program specialization and better ownership/layout, and each optimization needs \"a small legality statement, a conservative executable reference, counterexample tests, and an ablation measurement\".\n\n## Children\n\n1. Draw buckets, persistent geometry/index buffers, packed transform/material tables, incremental upload range merging.\n2. Automatic instance formation legality.\n3. Persistent programs and bindings with asynchronous precompilation and bounded caches.\n4. Render bundles with a structural key, executeBundles state invalidation and rebuild-cost measurement.\n5. Rendering-order legality.\n6. Portable indirect draw/dispatch strategy.\n7. Extended red-A/blue-B regressions.\n\n## Success criteria for the epic\n\nAll children closed; ablation toggles exist for buckets/instancing/bundles so F3D-20.5 can attribute gains; the legality statements live in the crate docs.\n\n## No-claim boundary\n\nSpeedups from this epic are measured only under the frozen protocol in F3D-11.\n","acceptance_criteria":"Draw organization (buckets, persistent geometry, packed tables, incremental uploads), automatic instance formation, persistent programs/bindings and render bundles are implemented under explicit legality rules that preserve logical IDs, callback order and rendering order; the red-A/blue-B and bundle-then-direct regressions plus their extensions pass; bundle rebuild cost is measured.","status":"open","priority":1,"issue_type":"epic","created_at":"2026-09-09T16:46:22.912746Z","created_by":"jemanuel","updated_at":"2026-09-09T16:48:48.636568Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-graph","new-backend","phase-1","tier-frontier","tier-solid"],"dependencies":[{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago","depends_on_id":"f3d-06-packed-transforms-materialization-roa.3","type":"blocks","created_at":"2026-09-09T16:48:47.995573Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago","depends_on_id":"f3d-06-packed-transforms-materialization-roa.6","type":"blocks","created_at":"2026-09-09T16:48:48.156179Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.4","type":"blocks","created_at":"2026-09-09T16:48:48.324683Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.2","type":"blocks","created_at":"2026-09-09T16:48:48.482305Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago","depends_on_id":"f3d-gate-2-bridge-apz","type":"blocks","created_at":"2026-09-09T16:48:48.636277Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-08-legal-grouping-versions-bundles-ago.1","title":"Draw buckets, persistent geometry, packed transform/material tables, upload range merging","description":"## Full scope\n\nDraw organization: bounded buckets keyed by pipeline, geometry layout, binding strategy and ordering constraints; persistent geometry/index buffers; packed per-object transform and legal material parameter tables; incremental derived-field updates and upload range merging by measured cost\n\n## Summary\n\nTurn \"which objects share a program and which values changed\" into packed tables and a stable bucket plan.\n\n## Background (plan §8.2 \"Incremental transforms and uploads\", §8.3, §4.10, §4.4 last paragraph)\n\n- \"Use a bounded set of buckets keyed by pipeline, geometry layout, binding strategy, and ordering constraints. Keep geometry/index buffers persistent. Represent per-object transforms and legal material parameters in packed tables.\"\n- \"Update only the derived fields and upload ranges whose input versions changed. Merge nearby ranges when that costs less than issuing many tiny uploads.\"\n- \"The compiler must not claim work proportional only to the number of changed fields unless its mutation coverage actually supports that claim.\"\n\n## Technical approach\n\n- Bucket key = (pipeline variant, vertex layout, binding strategy, ordering class from 08.5); bucket membership recomputed only on structural events (06.3); per-bucket contiguous slices of the transform/material tables so uploads are range-based.\n- Tables: `AffineRows` array and material parameter blocks (05.2 layout) in storage buffers or dynamic-offset uniform slices depending on the device profile; per-use versions (07.4) for values that change within a frame.\n- Upload merging: a cost model with measured constants per device (from 03.5/05.6 bridge data) deciding merge vs split; must respect 06.6's gap rule when merging across bytes the source has not requested.\n- Persistent geometry: residency generations per geometry replica; re-upload only on attribute request versions.\n\n## Success criteria\n\n- On H1 with static toggles: uploads per frame ≈ 0 bytes for transforms; with dynamic: bytes proportional to moved objects; bucket count stable across frames (counters from 07.7).\n- Rendering order equals upstream's render list order within each ordering class (verified by trace).\n\n## Test plan\n\n- Unit: bucket-key derivation tests; range-merge cost-model tests with fixed constants (merge only when cheaper; 06.6 gap rule respected); residency-generation tests for persistent geometry.\n- E2E: `tests/e2e/grouping/buckets.mjs` on H1 in the forced-new-backend lane on both browsers: static toggle → ~0 transform upload bytes per frame; dynamic → bytes proportional to moved objects; bucket count stable; render order equals upstream's render list order within each ordering class (trace).\n- Logging: per-frame bucket summary `{buckets, draws, upload_ranges, upload_bytes}` and the upload range list in debug; archived under `evidence/08.1/`.\n- Failure artifacts: bucket assignment dump and the order-trace diff.\n\n## Considerations\n\n- Vertex pulling is not assumed to beat ordinary buffers (§8.3); it is an optional measured strategy later.\n","acceptance_criteria":"The render list is organized into a bounded set of buckets; geometry/index buffers persist across frames with residency generations; per-object AffineRows and material parameters live in packed tables updated only for changed versions; nearby dirty ranges are merged only when measured cheaper than separate uploads; H1's 4,000 objects produce a small stable bucket set with upload bytes proportional to changed fields, without changing rendering order semantics.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:29.260856Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:17.980427Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-graph","new-backend","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.1","depends_on_id":"f3d-06-packed-transforms-materialization-roa.3","type":"blocks","created_at":"2026-09-09T16:48:46.030348Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.1","depends_on_id":"f3d-06-packed-transforms-materialization-roa.6","type":"blocks","created_at":"2026-09-09T16:48:46.190851Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.1","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.4","type":"blocks","created_at":"2026-09-09T16:48:46.351506Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.1","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.7","type":"blocks","created_at":"2026-09-09T17:41:17.980163Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.1","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago","type":"parent-child","created_at":"2026-09-09T16:46:29.260856Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.1","depends_on_id":"f3d-gate-2-bridge-apz","type":"blocks","created_at":"2026-09-09T17:15:28.658493Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-08-legal-grouping-versions-bundles-ago.2","title":"Automatic instance formation legality (IDs, winding, callback multiplicity)","description":"## Full scope\n\nAutomatic instance formation legality: full draw-observation contract, preserved logical object/face/vertex/instance IDs including custom shader uses of indices, no accidental instance ID for originally noninstanced draws, negative-determinant winding buckets, source-ordered per-object callbacks under batching (callback multiplicity counterexample), unknown shader side effects prevent batching\n\n## Summary\n\nInstancing as a legal transformation, not a pipeline-key coincidence.\n\n## Background (plan §8.2 \"Automatic instance formation\", §5.5, §14.8; AGENTS.md counterexample \"Callback multiplicity under batching\")\n\n\"Merge draw preparation only when the full draw-observation contract permits it, not merely when pipeline keys match. Preserve logical object, face, vertex, and instance IDs, including custom shader uses of instance/vertex indices. An originally noninstanced draw must not accidentally acquire a different logical instance ID after batching. Negative-determinant transforms may require a separate winding bucket. Per-object callbacks remain source-ordered effects; merging physical draws does not erase or reorder them. Unknown shader side effects or callback-dependent state prevent the affected batching.\"\n\n## Technical approach\n\n- Legality inputs from 12a.2's effect analysis: does any shader (built-in, node, or ESSL) read GLSL `gl_InstanceID`/`gl_VertexID`, TSL `instanceIndex`/`vertexIndex`, or WGSL `@builtin(instance_index)`/`@builtin(vertex_index)`? Does the object have callbacks with observable effects? Is the material shared with mutable per-object state (`onBeforeRender` sets uniforms)? Is picking (raycast) dependent on `instanceId`?\n- Merge plan: candidate group → instance stream with a logical-ID remap table so any observed index is translated back to the source's logical value (for shaders that read the index, either provide the source value through a per-instance attribute or refuse to merge).\n- Winding: objects with negative determinant get their own bucket with flipped front face.\n- Callback execution: batching changes physical draws only; the runtime still invokes each object's callbacks in upstream order with the same arguments; a counterexample test counts and orders callbacks with and without batching.\n- Refusal reporting: each refused merge records a reason and source span in the build report (§3.5).\n- Per-object visibility, `layers`, `frustumCulled` and per-object frustum culling remain per logical object inside a merged stream: hidden or culled objects are removed by compaction (08.6) or the group falls back to separate draws; a hidden object never renders and never shifts other instances' logical IDs.\n\n## Success criteria\n\n- H1 (originally non-instanced objects with distinct materials) merges legally where the contract permits and the report explains each refusal; picking IDs on an instanced test scene match upstream.\n- Callback multiplicity/order test passes; negative-scale objects render with correct facing.\n\n## Test plan\n\n- Unit: property tests over synthetic scenes with random callback/index usage asserting merges happen iff the observation contract permits; ID remap tests; negative-determinant bucket tests; callback multiplicity/order counterexample with a mutation variant.\n- E2E: `tests/e2e/grouping/instancing.mjs` in the forced lane on both browsers: H1 merges legally with every refusal explained in the report; picking IDs on an instanced test scene match upstream; a shader reading `instanceIndex` keeps source-visible values; negative-scale facing correct.\n- Logging: merge decisions `{group, merged: bool, reason, span}`; callback-order traces; archived under `evidence/08.2/`.\n- Failure artifacts: the report excerpt and the callback trace diff.\n\n## Considerations\n\n- Collapsing source objects to accelerate is only legal for the candidate when identities are unobservable; the same rule constrained the R1 reference (§15.3), so the comparison stays fair.\n","acceptance_criteria":"Draws merge into instance streams or grouped plans only when the compiler proves the observation contract permits it; instance/vertex/face IDs observed by shaders or picking are preserved; negative-determinant objects go to a separate winding bucket; onBeforeRender/onAfterRender and material callbacks fire per logical object in upstream order regardless of physical merging; a counterexample test proves callbacks are neither erased nor reordered; unknown shader side effects or callback-dependent state disable batching for the affected objects.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:29.349881Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:19.744493Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","f3d-graph","new-backend","phase-1","tier-frontier"],"dependencies":[{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.2","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago","type":"parent-child","created_at":"2026-09-09T16:46:29.349881Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.2","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago.1","type":"blocks","created_at":"2026-09-09T16:48:46.514202Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.2","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.2","type":"blocks","created_at":"2026-09-09T16:48:46.673709Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-08-legal-grouping-versions-bundles-ago.3","title":"Persistent material programs and bindings with async precompile and bounded cache","description":"## Full scope\n\nPersistent material programs and bindings: shader-program identity separate from mutable uniform values, specialization on geometry layout/material features/render state/lighting configuration, asynchronous precompilation of common variants, bounded variant cache, bind group reuse until structural dependencies change\n\n## Summary\n\nAnswer \"which objects share a program\" once, at build time where possible.\n\n## Background (plan §8.2 \"Persistent material programs\", \"Persistent bindings and bundles\", §9.3, §9.5, §16.9)\n\n\"Specialize on geometry layout, material features, render state, and lighting configuration. Separate shader-program identity from mutable uniform values. Precompile common variants asynchronously and bound the variant cache.\" \"Reuse bindings and legal render bundles until their structural dependencies change.\" Also: \"A finite proven variant set may be emitted ahead of time. Otherwise include the necessary runtime compiler/construction path automatically when reachable.\"\n\n## Technical approach\n\n- Program key type in `f3d-shader`; the compiler's analysis (12a) enumerates the variant set reachable in the application (for H1/H2: the finite material/control branches); variants get emitted WGSL ahead of time and are compiled asynchronously during startup within the region policy (02.5).\n- Runtime variant path: a variant not in the precompiled set compiles on demand (it is a measured interaction-tail cost, §9.5); cache budgeted and evicting (07.1).\n- Bind group cache keyed by (layout, resource identities, offsets); invalidated by resource generation changes.\n- Report: variant table with source spans of the branches that produced them.\n\n## Success criteria\n\n- H2's material selector switches produce zero recompiles after warmup (all branches precompiled); a synthetic unseen variant compiles at runtime and is logged as such.\n- Uniform sweeps cause zero pipeline creations (07.7 counters).\n\n## Test plan\n\n- Unit: program-key derivation tests (uniform value changes do not change the key; layout/feature/state/lighting changes do); cache budget/eviction tests; variant-table generation test from a fixture application.\n- E2E: `tests/e2e/programs/variants.mjs` on both browsers: H2 material-selector sweep after warmup causes zero recompiles; uniform sweeps cause zero pipeline creations (07.7 counters); a synthetic unseen variant compiles at runtime and is logged as such; startup precompile timeline recorded.\n- Logging: variant compile events `{variant_key, source_spans, precompiled: bool, compile_ms}` (feed the cold-start report); per-frame pipeline-creation counters; archived under `evidence/08.3/`.\n- Failure artifacts: the variant table and the counter timeline.\n\n## Considerations\n\n- Never keep rendering an old shader or placeholder while a variant compiles and report success (§9.5): an unseen variant reached at a synchronous `render()` is compiled with synchronous `createRenderPipeline` (the browser stalls exactly as upstream's synchronous program compilation would); `createRenderPipelineAsync` is used only for precompilation and at the source's own async boundaries; no placeholder frame is ever drawn.\n","acceptance_criteria":"Program identity is a key over geometry layout, material feature set, render state and lighting configuration; uniform value changes never trigger recompilation; common variants precompile asynchronously (createRenderPipelineAsync) within a bounded cache; bind groups persist across frames until a bound resource identity/offset changes; the build report lists precompiled variants and any runtime-compiled ones.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:29.439365Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:20.103047Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-shader","new-backend","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.3","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.3","type":"blocks","created_at":"2026-09-09T16:48:46.835664Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.3","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago","type":"parent-child","created_at":"2026-09-09T16:46:29.439365Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-08-legal-grouping-versions-bundles-ago.4","title":"Render bundles with a structural key, executeBundles invalidation, rebuild-cost measurement","description":"## Full scope\n\nRender bundles with a structural contract: bundle key (device generation, pipeline/layout, attachment formats and sample count, depth/stencil compatibility, bound resource identities, offsets, recorded draw parameters), rebuild rules for changed bindings/dynamic offsets/ranges/counts, frame-ring variants, measured rebuild cost and avoidance where bundles lose, executeBundles state invalidation, bundle-then-direct regression\n\n## Summary\n\nBundles done by the book. Upstream H1 already uses bundles; the candidate must use them at least as well and never incorrectly.\n\n## Background (plan §8.2 \"Bundle reuse has a structural contract\", §6.4, §23 [S37]; AGENTS.md \"Bundles reset render-pass state\")\n\n- \"A bundle key includes the device generation, pipeline/layout, attachment formats and sample count, depth/stencil compatibility, bound resource identities, offsets, and recorded draw parameters. Changing data in an already bound buffer can be legal; changing a recorded binding, dynamic offset, geometry range, or draw count is not an arbitrary in-place patch to the bundle. Frame-ring variants must have matching recorded bindings or be rebuilt.\"\n- \"After `executeBundles`, invalidate the host adapter's cached render-pass bindings and pipeline state before issuing subsequent direct draws. The API clears that state, even for an empty bundle sequence.\"\n- \"Command buffers themselves are freshly encoded for submission; a submitted command buffer is not a reusable render bundle.\"\n\n## Technical approach\n\n- `f3d-gpu::bundles`: bundle records with the structural key; encoder emits `GPURenderBundleEncoder` recordings via the ABI; validity checked each frame against resource generations and recorded parameters.\n- Frame ring: dynamic offsets passed to `setBindGroup` are recorded into the bundle, so per-frame ring slices cannot be swapped under one bundle. Keep one bundle variant per ring slot (each recording that slot's bindings/offsets, all sharing the structural key except the slot) and rebuild a slot's variant only when its structural key changes; alternatively keep a single bundle whose recorded bindings are slot-independent by addressing ring data through a per-slot indirection buffer updated with `writeBuffer` (legal: data changes in an already bound buffer do not invalidate the bundle). Measure both; document the chosen approach and its cost.\n- State invalidation: the adapter's pass-state cache is reset after any `executeBundles`, including empty lists.\n- Measurement: per-device bundle record cost vs saved per-frame encode cost; a heuristic threshold with ablation toggle.\n\n## Success criteria\n\n- Bundle-then-direct regression (from 05.7) passes through this code path with distinct bindings; the empty-list variant passes.\n- H1 bundle on/off toggle matches upstream's observable behavior; counters show bundles reused across frames when static.\n\n## Test plan\n\n- Unit: bundle-key derivation and invalidation tests (data change in a bound buffer keeps the bundle; binding/offset/range/count change rebuilds); frame-ring variant tests; adapter state-reset test after `executeBundles` including the empty list.\n- E2E: `tests/e2e/bundles/*.mjs` in the forced lane on both browsers: bundle-then-direct regression with distinct bindings through this code path; H1 bundle toggle observable behavior equals upstream; counters show reuse across static frames; per-device rebuild-cost measurement recorded.\n- Logging: bundle events `{bundle, event: build|reuse|invalidate, reason, record_ms}` and per-frame reuse counters; archived under `evidence/08.4/`.\n- Failure artifacts: readbacks after the bundle/direct transition and the bundle key diff.\n\n## Considerations\n\n- Bundles must not retain an expired external video texture (§8.5); the key includes the external texture's per-frame identity, which forces exclusion of such draws from bundles.\n","acceptance_criteria":"Static draw portions become render bundles keyed by the full structural key; changing data in an already bound buffer reuses the bundle, changing a recorded binding/dynamic offset/geometry range/draw count rebuilds it; frame-ring variants have matching recorded bindings or are rebuilt; after executeBundles the adapter invalidates cached pass state before direct draws (including the empty-list case); bundle rebuild cost is measured per device and bundles are disabled for buckets where they lose; H1's bundle toggle behaves as upstream.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:29.537171Z","created_by":"jemanuel","updated_at":"2026-09-09T17:31:47.805979Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-graph","new-backend","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.4","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago","type":"parent-child","created_at":"2026-09-09T16:46:29.537171Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.4","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago.1","type":"blocks","created_at":"2026-09-09T16:48:46.997157Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.4","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago.3","type":"blocks","created_at":"2026-09-09T16:48:47.167409Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-08-legal-grouping-versions-bundles-ago.5","title":"Rendering-order legality and conservative opaque batching","description":"## Full scope\n\nRendering-order legality: transparency sorting and custom blending, renderOrder, equal-depth surfaces, stencil, clipping, custom depth behavior, side-effecting shaders and callbacks; conservative opaque-batching rules; adversarial order tests\n\n## Summary\n\n\"Rendering order is semantic.\" The bucket planner must never reorder what upstream's observable order fixes.\n\n## Background (plan §5.5, §8.5 \"Preserve sorted transparency\")\n\n- \"Transparency, stencil operations, depth behavior, clipping, feedback, and callbacks can make order observable. Grouping and pass scheduling must respect those dependencies. Opaque batching is not automatically legal either. Equal-depth surfaces, `renderOrder`, custom depth behavior, side-effecting shaders, and callbacks can expose changes. Use conservative legality rules and test adversarial examples.\"\n- \"Weighted blended order-independent transparency is not an equivalent replacement for arbitrary sorted alpha blending and is not the default.\"\n\n## Technical approach\n\n- Transcribe upstream's render list sorting (`painterSortStable`/`reversePainterSortStable`, `renderOrder`, `groupOrder`, program/material/z keys) into the ordering classes; buckets exist within an ordering class and never across a boundary that upstream's comparator makes observable.\n- Legality rule for opaque reordering within a class: identical depth function/write/test state, no stencil ops, no blending, no `depthTest=false`, no equal-depth candidates with different colors (detected conservatively via bounds overlap + depth-equality risk), no side-effecting shaders (storage writes/atomics), no order-dependent callbacks; otherwise preserve exact order.\n- Transparent objects: always upstream order; custom blending states preserved.\n- Adversarial suite: coplanar quads with different colors and equal depth; `renderOrder` mutated per frame; stencil masks; clipping planes with `clipIntersection`; a shader writing to a storage buffer; `onBeforeRender` toggling visibility.\n- Public ordering controls are the primary contract: `renderer.sortObjects = false` preserves insertion order exactly; user comparators installed with `setOpaqueSort`/`setTransparentSort` execute the user's comparator with no ordering-class inference across it; the opaque/transmissive/transparent list split of the pinned `RenderList` (WebGPU renderer) is preserved.\n\n## Success criteria\n\n- Pixel and callback-order equivalence with upstream across the adversarial suite on both browsers.\n\n## Test plan\n\n- Unit: ordering-class derivation tests transcribed from upstream's sort comparators (`painterSortStable`, `reversePainterSortStable`, `renderOrder`, `groupOrder`); legality-rule tests for opaque reordering (equal-depth risk, stencil, blending, side effects, callbacks disable it).\n- E2E: `tests/e2e/order/adversarial.mjs` in the forced lane on both browsers: coplanar quads with equal depth, per-frame `renderOrder` mutation, stencil masks, clipping with `clipIntersection`, storage-writing shader, `onBeforeRender` visibility toggles; the `webgl_clipping_stencil` and `webgpu_clipping_stencil` fidelity cases via `f3d verify` (ordering, clipping and stencil correctness); pixel and callback-order equality with upstream.\n- Logging: ordering-class assignment per object in debug and the reorder decisions with reasons; archived under `evidence/08.5/`.\n- Failure artifacts: pixel diffs and the draw-order trace.\n\n## Considerations\n\n- \"A fused shader must preserve required intermediate quantization and sampling boundaries\" (§5.5); no fusion here.\n","acceptance_criteria":"Grouping and pass scheduling respect every dependency that can make order observable; opaque batching is applied only where the conservative legality rule proves equivalence; sorted transparency and custom blending are preserved (no weighted-blended OIT substitution); adversarial tests with equal depths, renderOrder changes, stencil/clip interplay and side-effecting shaders match upstream pixel output and callback order.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:29.630333Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:19.564905Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-graph","new-backend","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.5","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago","type":"parent-child","created_at":"2026-09-09T16:46:29.630333Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.5","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago.1","type":"blocks","created_at":"2026-09-09T16:48:47.333535Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-08-legal-grouping-versions-bundles-ago.6","title":"Portable indirect draw/dispatch strategy with capability fallbacks","description":"## Full scope\n\nPortable indirect draw/dispatch strategy: portable indirect commands with fixed slots or compacted instance streams, zero-instance slot cost accounted, capability-checked indirect-first-instance with bucket-base fallback, no indirect-count/multi-draw or bindless assumptions, bounded material/texture tables with compatible formats\n\n## Summary\n\nGPU-side selection without assuming native-style capabilities.\n\n## Background (plan §8.3, §2.6, §12.3)\n\n\"For GPU-selected work, use portable indirect draw/dispatch commands and fixed slots or compacted instance streams as appropriate. Do not require a universally available indirect-count or multi-draw API. A zero-instance indirect slot is a conservative option, but its submission/validation cost still counts. Optional indirect-first-instance behavior must be capability-checked; a bucket-base index or equivalent legal representation supplies the fallback. Texture arrays or material tables require compatible formats/layouts and bounded bindings; do not assume arbitrary bindless texture access.\" \"The architecture should not depend on universal support for native-style multi-draw-indirect, arbitrary bindless resources, mesh shaders, ray-tracing hardware, or shared CPU/GPU memory.\"\n\n## Technical approach\n\n- Indirect args buffer records from the layout schema; a per-bucket indirect slot; compaction kernel (compute) for GPU culling (used by F3D-20.6) writing instance counts; fallback CPU-side counts.\n- Capability record gating: `indirect-first-instance` feature → use `firstInstance`; else bucket-base uniform + instance index arithmetic in shaders.\n- Material tables: storage-buffer material blocks indexed per instance; texture arrays only for compatible format/size groups; otherwise per-bucket bindings.\n- Cost accounting: number of indirect slots submitted (including zero-instance) in the telemetry.\n\n## Success criteria\n\n- Both capability paths render identically on a test scene; counters show zero-instance slot counts; no use of unavailable APIs on any stratum's capability record.\n\n## Test plan\n\n- Unit: indirect-args record layout tests; capability-gating tests (feature present → `firstInstance`; absent → bucket-base fallback); compaction kernel unit test via a CPU reference.\n- E2E: `tests/e2e/indirect/paths.mjs` renders a test scene identically under both capability paths (device profile toggled) on both browsers; counters show zero-instance slot counts; no unavailable API used on any stratum's capability record.\n- Logging: chosen path per bucket `{bucket, path, slots, zero_instance_slots}`; archived under `evidence/08.6/`.\n- Failure artifacts: pixel diffs between paths.\n\n## Considerations\n\n- Priority 2: needed for H7/H8 routes (F3D-16) and GPU culling, not for the first product gate.\n","acceptance_criteria":"GPU-selected work uses portable indirect draw/dispatch with either fixed slots or compacted streams; optional indirect-first-instance behavior is capability-checked with a bucket-base fallback; zero-instance slots are costed in the plan; texture arrays/material tables use compatible formats and bounded bindings; no code path requires indirect-count, multi-draw, or arbitrary bindless access.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:29.721616Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:23.149756Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-graph","new-backend","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.6","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago","type":"parent-child","created_at":"2026-09-09T16:46:29.721616Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.6","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago.1","type":"blocks","created_at":"2026-09-09T16:48:47.504268Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-08-legal-grouping-versions-bundles-ago.7","title":"Extended red-A/blue-B regressions (compute, multi-camera, nested callbacks, two frames in flight)","description":"## Full scope\n\nExtended red-A/blue-B regressions: compute uniforms, multiple cameras, nested render callbacks, two logical frames in flight\n\n## Summary\n\nThe plan's explicit extensions of the core counterexample, now that buckets, slices and bundles exist.\n\n## Background (plan §6.7, §14.8; AGENTS.md \"Queue ordering is not command-recording ordering\")\n\n\"Extend it to compute uniforms, multiple cameras, nested callbacks, and two logical frames in flight. The planner tracks per-use data versions, not just a last value per object per frame.\"\n\n## Technical approach\n\n- Compute: two dispatches sharing a uniform block with different values in one submission; readback both outputs.\n- Multiple cameras: render the same scene with camera A then camera B (changing a shared material color between them) to two targets in one submission.\n- Nested callbacks: `onBeforeRender` of an object triggers `renderer.render(otherScene, otherCamera, target)`; verify the outer draw's parameters are unaffected and both outputs correct.\n- Two frames in flight: with the credit window at two, mutate a shared uniform each logical frame; assert frame N's output reflects N's value while N+1 is already encoded.\n- Each with a \"wrong implementation\" toggle in test builds to prove detection.\n\n## Success criteria\n\n- Four named tests pass on both browsers; mutation checks fail as expected.\n\n## Test plan\n\n- Unit: none new (integrated regressions).\n- E2E: four named tests in `tests/e2e/counterexamples/queue-*.mjs` (compute uniforms; multiple cameras in one submission; nested render from a callback; two logical frames in flight with per-frame uniform mutation) on both browsers, each with a `--wrong-impl` mutation variant that must fail; enabled in the forced lane and benchmark mode.\n- Logging: observed vs expected per-target pixel values and per-use versions; archived under `evidence/08.7/`.\n- Failure artifacts: target readbacks and the packet dump.\n\n## Considerations\n\n- These stay enabled in benchmark mode (they are part of the measured optimized path).\n","acceptance_criteria":"The queue-write snapshot regression is extended to compute uniforms, multiple cameras rendering in one submission, nested render calls from callbacks, and two logical frames in flight; each variant passes and has a mutation check proving detection; all run in the forced lane and in benchmark mode.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:29.812789Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:04.000909Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","new-backend","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.7","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.4","type":"blocks","created_at":"2026-09-09T16:48:47.836020Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.7","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago","type":"parent-child","created_at":"2026-09-09T16:46:29.812789Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-08-legal-grouping-versions-bundles-ago.7","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago.4","type":"blocks","created_at":"2026-09-09T16:48:47.665626Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-09-marching-cubes-kernel-fb3","title":"F3D-09: Complete marching-cubes computation/attribute behavior (H2)","description":"## What this epic delivers\n\nThe H2 half of the first product gate's compute content. Plan §19 F3D-09 depends on 04 and 05; acceptance: \"H2 geometry/normals/colors and every parameter/material variant\".\n\n## Why (plan §4.6, §7.7, §15.3, §2.7)\n\n\"Known procedural geometry addon → safe Rust kernel behind the same API contract.\" \"Start procedural geometry with known addon implementations such as marching cubes. CPU Wasm execution offers an early route to improvements without changing CPU-visible geometry.\" H2 \"calls its procedural update on each animation step and exposes resolution, blob count, isolation, and wall/floor controls... Its default material and every material-selector branch, including the four legacy ShaderMaterial variants, belong in the first product slice; the UI cannot be restricted to an easy shader after the fact.\"\n\n## Children\n\n1. Marching cubes safe-Rust kernel behind the same API.\n2. Rebuilt-geometry attribute behavior and allocation/upload reduction with differential tests.\n3. H2 complete material/control closure wiring.\n\n## Success criteria for the epic\n\nAll children closed; H2's geometry output is bit-identical (within the f32 storage the addon already uses) to the upstream addon for scripted control sequences; the kernel is derived from the addon's source semantics, not selected by example filename.\n\n## No-claim boundary\n\n\"Faster\" is not claimed until F3D-11.4 measures it under the frozen protocol.\n","acceptance_criteria":"The MarchingCubes addon's per-step computation runs as a safe Rust kernel behind the identical public API with identical CPU-visible geometry, normals, colors, topology, output order and bounds for every resolution/blob/isolation/wall/floor setting; rebuilt-geometry attribute behavior follows upstream upload semantics with reduced allocation/upload work; every H2 material selector branch, including the four legacy ShaderMaterial variants, remains reachable and correct.","status":"open","priority":1,"issue_type":"epic","created_at":"2026-09-09T16:46:22.993216Z","created_by":"jemanuel","updated_at":"2026-09-09T16:48:50.106474Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-math","f3d-scene","h2","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-09-marching-cubes-kernel-fb3","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:48:49.947676Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-09-marching-cubes-kernel-fb3","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.4","type":"blocks","created_at":"2026-09-09T16:48:50.106147Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-09-marching-cubes-kernel-fb3.1","title":"MarchingCubes addon safe-Rust kernel behind the identical public API","description":"## Full scope\n\nMarchingCubes addon safe-Rust kernel behind the identical public API: field updates, polygonization, normals and colors, resolution/isolation/blob/wall/floor controls; identical CPU-visible geometry, output order, topology and bounds; synchronous observations preserved\n\n## Summary\n\nThe first \"known addon → Rust kernel\" lowering, done as a semantic port of the pinned source.\n\n## Background (plan §4.6, §4.7, §7.7, §15.3, §23 [S29], [S40])\n\n\"The first kernel must be derived from source semantics, not selected by an example filename, benchmark flag, or a memorized input trace.\" \"GPU generation is valuable only where output order, precision, topology, bounds, and synchronous observations can still satisfy the contract\" (so this is a CPU/Wasm kernel).\n\n## Technical approach\n\n- Read the pinned `MarchingCubes.js` fully: field array layout (`field`, `normal_cache`, `palette`), `addBall`'s falloff math, `addPlaneX/Y/Z`, the `polygonize` triangle table and vertex interpolation (`VIntX/Y/Z`), the normal cache computation, color handling, `count`/`maxPolyCount` limits, `enableUvs`, `enableColors`, `isolation`, `resolution` changes (reallocation), draw range setting, and attribute `needsUpdate` behavior.\n- Implement the same algorithms in safe Rust operating on packed `f32` fields/outputs in Wasm memory. The public `Float32Array`s (`positionArray`, `normalArray`, `uvArray`, `colorArray`, the attribute arrays) remain JavaScript-owned (05.5): the kernel computes into Wasm memory and copies out at the `update()` boundary (05.4 copy accounting), preserving array identity. A zero-copy view of Wasm memory is never handed to application code: such a view detaches (length 0) on `memory.grow`, which would break `geometry.attributes.position.array` identity and every CPU observation; a view is permitted only for a region proven never to grow (pre-reserved, growth forbidden) and used internally.\n- Recognition: the compiler (12a) recognizes the unmodified pinned addon by blob fingerprint and swaps the update path; a modified copy keeps retained JS (§4.7).\n- Exact float behavior: the addon computes in JS doubles then stores into `Float32Array`; replicate the same expression order in f64 and store as f32 to keep bit identity; transcendental uses (if any) follow 06.1's differential policy.\n- Identical public API means every member of the pinned class: `init(resolution)`, `reset`, `addBall(ballx, bally, ballz, strength, subtract, colors)`, `addPlaneX/Y/Z(strength, subtract)`, `setCell`, `getCell`, `blur`, `update`, the `enableUvs`/`enableColors`/`isolation`/`resolution`/`field`/`palette`/`count`/`maxPolyCount` properties, and attributes created with the same `DynamicDrawUsage` flags; signatures and semantics verified against the pinned source, not from memory.\n\n## Success criteria\n\n- Differential test: random control sequences (resolution ∈ {28, 40, 56, 80}, blobs, isolation, walls) produce byte-identical position/normal/color arrays and identical draw ranges vs upstream.\n- Public API identity: `instanceof MarchingCubes`, properties enumerable as upstream, synchronous `update()` observation of results.\n\n## Test plan\n\n- Unit: differential tests vs a Node oracle running the pinned `MarchingCubes.js` over random control sequences (resolution in {28, 40, 56, 80}, blob counts, isolation, walls/floor, `enableUvs`/`enableColors`) asserting byte-identical position/normal/color/uv arrays and identical draw ranges; property tests over field operations (`addBall`, `addPlaneX/Y/Z`, `reset`); identity tests (`instanceof`, enumerable properties, synchronous `update()` results).\n- E2E: `tests/e2e/h2/kernel.mjs` runs the addon through the routed build in the browser with the same sequences and compares to upstream; a modified-addon fixture stays on retained JS.\n- Logging: per-update `metric` events `{resolution, polygons, kernel_ms}` (timings are not claims); first array mismatch logged with index and both values; archived under `evidence/09.1/`.\n- Failure artifacts: the shrunk control sequence and array diffs.\n\n## Considerations\n\n- SIMD variants only after the scalar port is bit-identical (06.1 policy).\n","acceptance_criteria":"A Rust kernel (in f3d-math/f3d-scene) reproduces the pinned examples/jsm/objects/MarchingCubes.js algorithms (addBall, addPlaneX/Y/Z, reset, update/polygonize, normal and color generation, enableUvs/enableColors, the draw-range and attribute update behavior) with identical output for any control sequence; the public MarchingCubes class keeps its identity, properties and synchronous behavior; unrecognized subclass modifications fall back to the retained JS implementation.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:29.902398Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:19.026883Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-math","f3d-scene","h2","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-09-marching-cubes-kernel-fb3.1","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:48:48.961813Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-09-marching-cubes-kernel-fb3.1","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.4","type":"blocks","created_at":"2026-09-09T17:41:18.391019Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-09-marching-cubes-kernel-fb3.1","depends_on_id":"f3d-06-packed-transforms-materialization-roa.1","type":"blocks","created_at":"2026-09-09T16:48:48.800899Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-09-marching-cubes-kernel-fb3.1","depends_on_id":"f3d-09-marching-cubes-kernel-fb3","type":"parent-child","created_at":"2026-09-09T16:46:29.902398Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-09-marching-cubes-kernel-fb3.2","title":"Rebuilt-geometry attribute behavior and allocation/upload reduction for H2","description":"## Full scope\n\nRebuilt-geometry attribute behavior for H2: draw ranges, versioned upload ranges, buffer residency reuse across per-step rebuilds, reduced allocation and upload work; differential tests against the upstream addon at the GPU-visible level\n\n## Summary\n\nMake H2's per-frame rebuild cheap without changing what the GPU sees.\n\n## Background (plan §5.8, §15.2 H2 \"reduced allocation/upload work\", §6.6)\n\nThe candidate gain for H2 is \"bulk safe-Rust geometry kernel and reduced allocation/upload work\". The attribute-history rules (06.6) apply: the addon sets draw ranges and `needsUpdate` on its attributes each update; the candidate must follow the source's request behavior.\n\n## Technical approach\n\n- Wire the kernel's output buffers to the attribute history system: kernel writes bump CPU version; the addon's `needsUpdate`/draw-range calls set the request version and ranges; the uploader computes effective ranges per upstream coalescing (the WebGL path uploads the full buffer when `updateRanges` is empty and `needsUpdate` is set; replicate exactly what the pinned attributes code does for this addon's usage).\n- Residency: the position/normal/color buffers keep their GPU allocation across updates (no per-frame allocation); resolution changes reallocate per upstream (new array identity).\n- Counters: allocations per frame, upload bytes per frame from 07.7.\n\n## Success criteria\n\n- GPU readback of the vertex buffer after each scripted step equals upstream's uploaded bytes (obtained from a reference run with an instrumented `bufferSubData`).\n- Zero GPU allocations per steady-state frame; upload bytes per frame equal upstream's effective range bytes (or fewer only where upstream also would not have uploaded).\n\n## Test plan\n\n- Unit: planner tests for the addon's request pattern (draw range set + `needsUpdate` per update) reproducing the pinned attributes code's effective ranges; residency reuse tests (no reallocation across updates; resolution change reallocates with new identity).\n- E2E: `tests/e2e/h2/uploads.mjs` in the forced lane on both browsers: GPU readback after each scripted step equals the bytes uploaded by an instrumented upstream run; steady-state frames show zero GPU allocations and upload bytes equal to upstream's effective ranges.\n- Logging: per-frame `{effective_ranges, bytes, allocations}`; the instrumented reference's `bufferSubData` log stored as a fixture; archived under `evidence/09.2/`.\n- Failure artifacts: byte-range diffs.\n\n## Considerations\n\n- Do not \"optimize\" by uploading only changed triangles unless the source's request contract says those bytes are the requested ones (§5.8).\n","acceptance_criteria":"Per-step geometry rebuilds reuse GPU residency and upload only the source-requested ranges per upstream semantics; the GPU-visible bytes and draw ranges equal upstream's at every checkpoint; allocation and upload counters show the reduction; no early upload of CPU edits that upstream would leave GPU-stale.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:29.993009Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:04.352360Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-scene","h2","new-backend","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-09-marching-cubes-kernel-fb3.2","depends_on_id":"f3d-06-packed-transforms-materialization-roa.6","type":"blocks","created_at":"2026-09-09T16:48:49.283837Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-09-marching-cubes-kernel-fb3.2","depends_on_id":"f3d-09-marching-cubes-kernel-fb3","type":"parent-child","created_at":"2026-09-09T16:46:29.993009Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-09-marching-cubes-kernel-fb3.2","depends_on_id":"f3d-09-marching-cubes-kernel-fb3.1","type":"blocks","created_at":"2026-09-09T16:48:49.122426Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-09-marching-cubes-kernel-fb3.3","title":"H2 complete material and control closure (incl. four legacy ShaderMaterial variants)","description":"## Full scope\n\nH2 complete material and control closure: default environment-lit Standard, Lambert, Phong, texture/color branches, the four legacy ShaderMaterial variants, and every GUI control reachable and correct through the routed build\n\n## Summary\n\nThe \"no UI restriction after the fact\" guarantee for H2.\n\n## Background (plan §2.7, §15.3, §9.5, §23 [S40])\n\n\"H2 starts with an environment-lit Standard material and exposes Lambert, Phong, texture/color, and four legacy `ShaderMaterial` choices.\" \"The UI cannot be restricted to an easy shader after the fact.\" \"Never keep rendering an old shader, a placeholder, a simpler material or a constant node while reporting success.\"\n\n## Technical approach\n\n- From 01.6's H2 closure record, generate the control sweep script (harness input format, 03.1): each material option, each slider extreme and mid value, the material color sliders (hue/saturation/lightness via the example's `updateColor`), toggles, and any other control 01.6 records.\n- Run through the routed build in the forced new-backend lane; compare checkpoints (visual with 03.6 tolerances; state traces) against upstream.\n- Verify the variant table (08.3) contains all material branches as precompiled and that the four legacy shaders come from the ESSL pipeline (15a.2) with no retained renderer submission (07.6).\n\n## Success criteria\n\n- All sweep steps pass on both browsers; report shows every branch's owner route as new-backend.\n\n## Test plan\n\n- Unit: sweep-script generation test from 01.6's H2 closure record (every material option and control extreme/mid value present).\n- E2E: `tests/e2e/h2/sweep.mjs` in the forced-new-backend lane on Chrome and Safari through every material selector value (Standard with env, Lambert, Phong, texture/color, the four legacy ShaderMaterial variants) and every control; checkpoints compared to upstream with 03.6 tolerances; route assertion per step.\n- Logging: per-step events `{control_state, owner, route, max_err}`; the precompiled variant table attached; archived under `evidence/09.3/`.\n- Failure artifacts: reference/candidate/diff images per failing step.\n\n## Considerations\n\n- Any legacy shader variant that cannot be translated portably is a **new-backend coverage gap** that blocks the first product gate; it is not reclassified as legacy-only (§9.5).\n","acceptance_criteria":"A scripted sweep through every H2 material selector value and every control (speed, numBlobs, resolution, isolation, floor, wallx, wallz, dummy toggles as present in the pinned source) renders through the new backend with equivalence checkpoints passing; no branch is removed, stubbed or routed to a placeholder shader; the build report lists the finite variant set as precompiled.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:30.082082Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:19.390942Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","h2","new-backend","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-09-marching-cubes-kernel-fb3.3","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.3","type":"blocks","created_at":"2026-09-09T16:48:49.611066Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-09-marching-cubes-kernel-fb3.3","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.6","type":"blocks","created_at":"2026-09-09T17:41:17.773732Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-09-marching-cubes-kernel-fb3.3","depends_on_id":"f3d-09-marching-cubes-kernel-fb3","type":"parent-child","created_at":"2026-09-09T16:46:30.082082Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-09-marching-cubes-kernel-fb3.3","depends_on_id":"f3d-09-marching-cubes-kernel-fb3.1","type":"blocks","created_at":"2026-09-09T16:48:49.446438Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-09-marching-cubes-kernel-fb3.3","depends_on_id":"f3d-15a-early-shader-spike-7k8.2","type":"blocks","created_at":"2026-09-09T16:48:49.786748Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-10-standalone-networked-packaging-jlu","title":"F3D-10: Full standalone plus networked/module packaging","description":"## What this epic delivers\n\nThe product's output form. Plan §19 F3D-10 depends on 04 and 07; acceptance: \"Reachable modes/codecs embedded; native H1 switch/reload; live mode not falsified as offline\".\n\n## Why (plan §3.1, §10.1–10.6, §16.9, §22 \"The product is real\")\n\n\"Self-contained HTML as the primary deployment form for applications with a closable code/resource graph. Also provide normal networked/module output for live-data, external-service, runtime-import, and library use cases. A deployment limitation is reported as such, not relabeled as an unsupported Three.js feature.\" \"A proof of unreachability may omit a component; an unobserved branch in a captured trace may not.\"\n\n## Children\n\n1. Asset manifest and virtual resource resolver\n2. Standalone HTML packer: safe embedding, memory accounting, CSP publication\n3. Networked/module deployment mode and the isolated accelerated profile\n4. Strict closure diagnostics and sandboxed capture (index locking, limits, traversal rejection)\n5. Packaging verification harness (no-subresource, navigation, H1 GL after reload, interaction sweep)\n6. Build cache on complete semantic inputs, deterministic rebuilds, incremental build cost\n7. Asset preprocessing legality and runtime codec inclusion policy\n8. f3d CLI orchestration: build/serve/explain with deterministic text and JSON output\n9. Build-tool frontend adapters: Rollup/Vite plugin and Node API as thin wrappers over the f3d compiler\n\n## Success criteria for the epic\n\nAll children closed; H1 and H2 standalone artifacts pass the packaging verification harness on both browsers; byte and peak-memory numbers are reported per artifact and feed 03.6's budgets.\n\n## No-claim boundary\n\n\"A single file is a packaging mode, not a universal deployment environment.\" HTTPS/localhost serving is required for WebGPU; `file://` is not promised.\n","acceptance_criteria":"The compiler emits either one self-contained HTML (all reachable code, compatibility components, Wasm, WGSL, CSS/fonts, codecs, assets and notices embedded; no undeclared subresource fetch after document load) or a networked/module build preserving live behavior; H1's native backend switch and reload work in the standalone artifact; live-data applications are never falsified as offline; packaging verification, build caching and the first f3d CLI surface exist.","status":"open","priority":1,"issue_type":"epic","created_at":"2026-09-09T16:46:23.075463Z","created_by":"jemanuel","updated_at":"2026-09-09T17:15:29.893443Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-assets","f3d-cli","packaging","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-10-standalone-networked-packaging-jlu","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:48:52.078664Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.6","type":"blocks","created_at":"2026-09-09T16:48:52.245978Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-10-standalone-networked-packaging-jlu.1","title":"Asset manifest and virtual resource resolver","description":"## Full scope\n\nAsset manifest and virtual resource resolver: logical URI, content hash, media type, source/license metadata and dependencies; preserved relative base paths, setPath/setResourcePath, URL modifiers, query/fragment behavior and new URL(..., import.meta.url); one explicit closure policy for code, CSS, workers, shader includes and model resources\n\n## Summary\n\nThe resolver that lets embedding change physical URLs while preserving logical loader/resource identity.\n\n## Background (plan §10.1, §5.7 last paragraph, §10.5)\n\n\"Resolve assets through a manifest containing logical URI, content hash, media type, source/license metadata, and dependencies. A small virtual-resource resolver preserves relative base paths, loader `setPath`/`setResourcePath`, URL modifiers, query/fragment behavior, and `new URL(..., import.meta.url)` as required by the source application. Code, CSS, worker, shader-include, and model-resource resolution use the same explicit closure policy. Rewriting every request to an unrelated blob URL without a logical identity map is insufficient.\"\n\n## Technical approach\n\n- Schema (Rust types in `f3d-assets`, serde for build-time JSON): `AssetRecord { logical_uri, hash, media_type, bytes_len, license, provenance, deps: [logical_uri], reachable_via: [source spans] }`.\n- Discovery: from the ingested module graph (04.1) and the corpus closure records (01.6): static string URLs, template URLs with finite closure, `import.meta.url` resolutions, loader `setPath` bases; dynamic capture (10.4) supplies observed requests but cannot prove unreachability.\n- Runtime resolver (static JS in the generated host code) with two mechanisms and no global monkey-patching of `fetch`/`XMLHttpRequest` (which would change observable behavior): (a) `LoadingManager.setURLModifier` for loader traffic (it only affects loaders that call `manager.resolveURL`); (b) build-time rewriting of application URL expressions classified literal/finite by 10.4 (spans recorded), including `new URL(..., import.meta.url)`, `Worker` script URLs, media element `src`, and CSS `url()`; mapping logical URIs to embedded bytes (blob or data URLs created on demand and released) or to networked paths; preserves `?query#fragment` semantics that loaders inspect.\n- Identity map available to the report and to `f3d explain`.\n\n## Success criteria\n\n- H1/H2/Sponza closures resolve every request; a resolver test replays each recorded request and gets the right bytes with the right media type.\n- `setPath`/`setResourcePath`/URL-modifier tests from upstream loader tests pass through the resolver.\n- `f3d-assets/CONTRACT.md` is written before any other crate depends on `f3d-assets` (AGENTS.md \"Documentation and Contracts\"): purpose and position in the dependency direction, public types and semantics, invariants, implementation ownership route, error model, determinism and numeric-fidelity class, cancellation behavior, unsafe boundary if any, feature flags, conformance tests, and no-claim boundaries.\n\n## Test plan\n\n- Unit: schema tests; resolution-rule tests (relative bases, `setPath`/`setResourcePath`, URL modifiers, `?query#fragment`, `import.meta.url`, worker/CSS/shader-include requests); identity-map tests (logical → physical, never an orphan blob URL).\n- E2E: `tests/e2e/assets/resolver.mjs` replays every recorded request from the H1/H2/Sponza closures through the resolver in embedded and networked modes on both browsers and asserts bytes and media types; upstream loader tests exercising `setPath`/`setResourcePath`/`setURLModifier` pass through the resolver.\n- Logging: per-request `{logical, physical, source, media_type, bytes}`; unresolved requests at `error`; archived under `evidence/10.1/`.\n- Failure artifacts: the request diff.\n\n## Considerations\n\n- Some HTTP/cache observations are incompatible with a fully embedded build; report those as admission boundaries, not fabricated network behavior (§10.1).\n","acceptance_criteria":"f3d-assets defines the asset manifest schema and a resolver that maps every request an application can make (loader paths, LoadingManager URL modifiers, import.meta.url-relative assets, worker scripts, CSS, shader includes) to a logical identity and embedded/networked physical location; requests are never rewritten to unrelated blob URLs without a logical identity map; the manifest carries hashes, media types, licenses and dependency edges.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:30.172656Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:20.654869Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-assets","f3d-compiler","packaging","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-10-standalone-networked-packaging-jlu.1","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.1","type":"blocks","created_at":"2026-09-09T16:48:50.265673Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu.1","depends_on_id":"f3d-10-standalone-networked-packaging-jlu","type":"parent-child","created_at":"2026-09-09T16:46:30.172656Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu.1","depends_on_id":"f3d-gate-1-foundation-tzn","type":"blocks","created_at":"2026-09-09T17:31:52.333735Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-10-standalone-networked-packaging-jlu.2","title":"Standalone HTML packer: safe embedding, memory accounting, CSP publication","description":"## Full scope\n\nStandalone HTML packer: embed all reachable code, compatibility components, Wasm, WGSL, CSS/fonts, codecs, assets and notices; context-aware HTML/script encoding validated by reparsing; bounded-chunk base64 with memory accounting; no undeclared subresource fetch; download bytes and peak startup memory reported; CSP allowances published\n\n## Summary\n\nThe single-file deliverable, packaged safely and measured honestly.\n\n## Background (plan §10.3, §10.4, §3.1)\n\n- \"The precise guarantee is no undeclared code/asset/subresource fetch after each document load.\"\n- \"Base64 increases encoded size, and decoding can create transient copies. The packer should use bounded chunks and release intermediate references promptly, but must not assume that the browser immediately frees embedded document text... Report both download bytes and peak startup memory. Do not pretend an embedded binary automatically gets the same streaming-compilation path as a separately served Wasm response.\"\n- \"Serialize embedded data with a context-aware HTML/script encoder: a source string containing `</script>` or attacker-controlled markup must not terminate a generated element. Escaping data for JSON alone does not establish safe HTML embedding. Validate generated scripts by reparsing them.\"\n- \"Publish the required CSP allowances, including Wasm execution and any blob-worker/resource requirements.\"\n\n## Technical approach\n\n- `f3d-cli`/`f3d-assets` packer: assembles the output HTML from a template with static generated host JS, embedded module code (retained upstream modules, exact components, adapters) as inline module scripts or import-map data URLs preserving module identity, Wasm as base64 (chunked), WGSL as strings, assets as base64 with media types, CSS/fonts inline, third-party notices/licenses section.\n- Encoder: escape `</script`, `<!--`, and U+2028/2029 inside script bodies; HTML-escape attribute contexts; a reparse validation step (using the pinned parser toolchain) that asserts the produced document parses into the expected element/script structure.\n- Memory accounting: startup instrumentation measuring string sizes, decoded byte totals, Wasm memory, staging and GPU allocations (07.1 budgets); reported per artifact.\n- Wasm instantiation from bytes with a note that streaming compilation is unavailable for embedded bytes; measure compile time as part of cold startup.\n- CSP: emit the minimal directive set (`script-src` with hashes/nonces as needed, `wasm-unsafe-eval` or the applicable directive, `worker-src blob:` if blob workers are used) in the report and as an optional `<meta>`.\n- Embedded modules: modules embedded as import-map `data:` URLs (or inline module scripts) may import only bare specifiers resolved by the import map (relative imports are rewritten at build time because `data:` URLs have no base); `import.meta.url` inside embedded modules is redirected through the resolver (10.1); the CSP set therefore includes `script-src data:` (or hashes for inline modules) and the artifact is validated by loading it under that exact CSP in the packaging harness (10.5).\n\n## Success criteria\n\n- H1/H2 standalone artifacts load under a network deny-list with zero blocked requests; the injection fixture (`</script>` in a shader string) produces a valid document.\n- Byte and memory reports produced; they seed 03.6's budgets.\n\n## Test plan\n\n- Unit: encoder tests (`</script`, `<!--`, U+2028/2029 inside script bodies; attribute contexts); reparse validation test; chunked base64 decoder tests; artifact composition accounting tests.\n- E2E: `tests/e2e/packaging/standalone.mjs` loads H1/H2 artifacts under a deny-all network interceptor on Chrome and Safari (zero blocked requests needed); injection fixture (a shader string containing `</script>`) yields a valid document; startup memory accounting captured; encoder fuzz test in the tooling lane.\n- Logging: artifact composition table `{category, bytes}` per build; startup `metric` events `{phase, bytes_decoded, wasm_pages, staging_bytes, gpu_bytes, ms}`; CSP directive set; archived under `evidence/10.2/`.\n- Failure artifacts: the intercepted-request log and the reparse diagnostics.\n\n## Considerations\n\n- Base64 vs alternative encodings: measure; keep the simplest correct one.\n- Use HTTPS/localhost for verification; `file://` is not a promise (§10.3).\n","acceptance_criteria":"f3d build --standalone emits one HTML whose only network request after document load is none (verified), initializes Wasm from embedded bytes, encodes embedded data with a context-aware encoder that survives a source string containing </script> or attacker-controlled markup, decodes in bounded chunks releasing intermediates, and reports download bytes plus peak startup memory (HTML/source strings, decoded bytes, Wasm memory, staging, GPU) and the required CSP directives.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:30.267150Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:20.836088Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-assets","f3d-cli","packaging","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-10-standalone-networked-packaging-jlu.2","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","type":"blocks","created_at":"2026-09-09T16:48:50.588910Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu.2","depends_on_id":"f3d-10-standalone-networked-packaging-jlu","type":"parent-child","created_at":"2026-09-09T16:46:30.267150Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu.2","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.1","type":"blocks","created_at":"2026-09-09T16:48:50.426890Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-10-standalone-networked-packaging-jlu.3","title":"Networked/module deployment mode and the isolated accelerated profile","description":"## Full scope\n\nNetworked/module deployment mode and the isolated accelerated profile: preserved live fetch/HTTP behavior, external services, supported dynamic module resolution, media/permissions and library/Node consumption; isolated profile with verified response headers as an additive, separately measured mode\n\n## Summary\n\nThe \"full networked/module deployment\" that makes live applications and library consumers first-class, not a workaround.\n\n## Background (plan §3.1, §10.4, §10.5, §16.6 \"Deployment\")\n\n\"Full networked/module deployment: preserve live fetch/HTTP behavior, external services, supported dynamic module resolution, media/permissions, and library/Node consumption. Required when those source behaviors are inherently non-embeddable; this is a first-release product mode, not an unsupported-feature workaround. A deployment selector cannot claim an offline snapshot preserves a live service.\" \"Isolated accelerated deployment: the same functionality with verified response headers and optional worker/shared-memory features. This is additive and separately measured; it cannot rescue the portable accelerated score.\"\n\n## Technical approach\n\n- Output layout: `dist/index.html`, module files preserving the original module graph (rewritten only for the routed entry points), Wasm as a separate `.wasm` (streaming compilation possible), assets as files, plus a manifest.\n- Live behavior: the resolver (10.1) in networked mode passes through to real URLs with original headers/credentials; dynamic imports with unresolvable closure remain dynamic; media/permission flows untouched.\n- Library mode: an npm-consumable package output for applications that import the routed `three` facade in their own bundler; Node consumption remains synchronous (04.2).\n- Isolated profile: emits a header manifest (`Cross-Origin-Opener-Policy`, `Cross-Origin-Embedder-Policy`) and a verification step in `f3d serve`/verify that checks `crossOriginIsolated`; optional features gated on it; scored separately.\n- Selector logic: the strict standalone build fails (10.4) on unclosable live behavior and the report recommends networked mode; the report never says \"offline equivalent\" for such apps.\n\n## Success criteria\n\n- A networked build of Sponza (external model index) preserves live fetch behavior; the H1 networked build behaves as upstream; a library build passes the Node import tests.\n- Isolated-profile verification detects missing headers.\n\n## Test plan\n\n- Unit: output-layout tests (module graph preserved; only routed entry points rewritten); header-manifest generation tests; selector-logic tests (live-service app cannot be labeled offline).\n- E2E: `tests/e2e/packaging/networked.mjs`: Sponza networked build preserves live fetch through the external index; H1 networked build matches upstream traces; library build passes the Node import tests; isolated-profile verification detects missing COOP/COEP headers with a local server toggling them.\n- Logging: deployment report `{mode, reasons, headers_verified, crossOriginIsolated}`; per-request pass-through log; archived under `evidence/10.3/`.\n- Failure artifacts: header dump and request log.\n\n## Considerations\n\n- Shared-memory worker execution needs isolation supplied by the host, not a meta tag (§10.3).\n","acceptance_criteria":"f3d build --deployment networked emits a multi-file module build that preserves live network behavior, dynamic imports, media/permission flows and library consumption; the isolated accelerated profile adds verified cross-origin-isolation headers and optional worker/shared-memory features without changing functionality; the deployment selector refuses to label a live-service application as an offline equivalent.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:30.354452Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:05.070817Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-assets","f3d-cli","packaging","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-10-standalone-networked-packaging-jlu.3","depends_on_id":"f3d-10-standalone-networked-packaging-jlu","type":"parent-child","created_at":"2026-09-09T16:46:30.354452Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu.3","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.1","type":"blocks","created_at":"2026-09-09T16:48:50.749332Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-10-standalone-networked-packaging-jlu.4","title":"Strict closure diagnostics and sandboxed capture (index locking, limits, traversal rejection)","description":"## Full scope\n\nStrict closure diagnostics and safe capture: unresolved URL expressions and dynamic imports reported with spans, external model index locking (Sponza), asset-pack limits, path-traversal rejection, sandboxed dynamic capture without credentials or private-network access, captured code treated as untrusted\n\n## Summary\n\nClose the graph honestly and safely, or fail loudly.\n\n## Background (plan §10.5, §10.6, §3.5)\n\n\"A strict standalone build fails when it cannot close the resource graph. It reports the unresolved URL expression or dynamic import rather than quietly retaining network dependencies. The Sponza example resolves a model through an external model index. The packer must lock and include the selected index/model dependencies, not leave a mutable branch URL in the output.\" \"Bound recursive assets, compressed expansion, geometry dimensions, shader size, and allocations. Reject path traversal or resource references outside the declared project/allowlist. Dynamic capture is an isolated build step without developer credentials, default private-network access, or arbitrary install-script privileges. Captured application code is untrusted, not part of the compiler's authority.\"\n\n## Technical approach\n\n- Closure analysis in `f3d-compiler`: classify each URL expression (literal / finite / unresolvable) with spans; unresolvable → strict failure with a suggested fix and a note that networked mode is available (§3.5 \"changes needed to unlock\").\n- External indexes: when a loader fetches an index then a model (Sponza), the capture step records the resolved chain; the packer pins the index content and the selected entries; a mutable branch URL left in output is a build failure.\n- Limits: configurable maxima (asset count, total bytes, decompressed size, geometry vertex/index counts, shader source size) with clear errors.\n- Sandboxed capture: run the application in a headless browser process with a network allowlist limited to the declared project origin(s) plus the explicitly declared external asset origins from the closure record (01.6; Sponza's model index and model live on external origins), no cookies/credentials, private-network requests blocked; the capture output is data only.\n- Path checks: canonicalize and verify every embedded file is inside the declared roots.\n- Capture reachability must not depend on GPU availability: capture runs with the retained renderer or the exact component (headless Chrome WebGPU needs a software adapter and Safari has no headless WebGPU); a GPU-dependent branch that cannot execute in capture is recorded as reachable-unverified, never as unreachable.\n\n## Success criteria\n\n- Fixture with a variable dynamic import fails strict mode with the right span; Sponza closes with the index locked; traversal fixture rejected; a private-network fetch in capture is blocked and reported.\n\n## Test plan\n\n- Unit: URL-expression classification tests (literal/finite/unresolvable with spans); limit tests (asset count/bytes/decompressed size/vertex counts/shader size); traversal rejection tests; index-locking test with a synthetic model index.\n- E2E: `tests/e2e/packaging/strict.mjs`: a variable dynamic import fails strict mode with the right span and a networked-mode hint; Sponza closes with the index locked; a private-network fetch during sandboxed capture is blocked and reported; capture runs without cookies/credentials (asserted by the local server).\n- Logging: closure report listing every reference `{expr, span, class, resolution}`; capture network log with dispositions; archived under `evidence/10.4/`.\n- Failure artifacts: the closure report and the blocked-request log.\n\n## Considerations\n\n- \"An unobserved branch in a captured trace may not [be omitted]\" (§3.1): capture adds evidence of reachability, never proof of unreachability.\n","acceptance_criteria":"A strict standalone build fails with precise source spans when it cannot close the resource graph; the Sponza-style external index is locked and its selected dependencies embedded; recursive assets, compressed expansion, geometry dimensions, shader size and allocations are bounded; references outside the declared project/allowlist are rejected; dynamic capture runs isolated without developer credentials, default private-network access or install-script privileges.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:30.443259Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:21.217291Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-assets","f3d-compiler","packaging","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-10-standalone-networked-packaging-jlu.4","depends_on_id":"f3d-10-standalone-networked-packaging-jlu","type":"parent-child","created_at":"2026-09-09T16:46:30.443259Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu.4","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.1","type":"blocks","created_at":"2026-09-09T16:48:50.913417Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-10-standalone-networked-packaging-jlu.5","title":"Packaging verification harness (no-subresource, navigation, H1 GL after reload, interaction sweep)","description":"## Full scope\n\nPackaging verification harness: deny network after each document load, clear caches, execute the complete interaction script through every reachable mode, allow only the frozen HTML document on intentional navigation with content identity checks, H1 count/bundle navigation and genuine WebGL backend after navigation, loader success/error, file input, resize, material switches, disposal, reopen, packed hash and license verification\n\n## Summary\n\nProof that the single file is really self-contained and really complete.\n\n## Background (plan §10.6, §15.3 \"H1's complete backend and navigation contract\", §16.6 \"Standalone validity\")\n\n\"After delivering each document, deny network subresource/code/asset requests, clear incidental caches, and execute the complete application interaction script, including every reachable mode. When a control intentionally navigates, allow only the frozen HTML document request and verify its content identity; then reapply the no-subresource rule... Test H1's count/bundle navigation as well as continuous animation. Its genuine-WebGL backend request must create the source-correct exact backend after navigation and remains a required functional test. Only the separately scored accelerated run fixes the WebGPU setting.\"\n\n## Technical approach\n\n- Harness mode in the tooling package: launch the artifact under HTTPS/localhost; install request interception denying everything except the document itself; on navigation (query-string changes for H1) allow the document request only if its bytes hash-match the artifact; replay the interaction script from 01.6/03.1 for every reachable mode.\n- Assertions: zero denied-and-needed requests (a blocked request that the app needed shows up as an error/trace divergence); exact-GL route asserted after H1's WebGL navigation (`expectNativeGL`); loader success and error paths (an intentionally missing asset must produce the source's error behavior, not a hang); file input via a scripted file; resize; material switches; `dispose()` then reopen.\n- Verification of packed hashes vs manifest and presence of every license notice for embedded third-party/upstream code.\n\n## Success criteria\n\n- H1 and H2 standalone artifacts pass on Chrome and Safari; the report lists modes exercised.\n\n## Test plan\n\n- Unit: interceptor rule tests (document-only allowance on navigation with hash check; everything else denied).\n- E2E: the harness itself: H1 and H2 standalone artifacts on Chrome and Safari under deny-all interception with cleared caches; full interaction script through every reachable mode; H1 navigation reload allowed only for the frozen document with content identity; `expectNativeGL` after the WebGL branch; loader success and error paths; file input; resize; material switches; dispose and reopen; packed hashes and license notices verified.\n- Logging: every intercepted request with disposition `{url, allowed, reason, hash_match}`; per-mode results with owner/route; archived under `evidence/10.5/<artifact>/`.\n- Failure artifacts: the denied-but-needed request list, screenshots and trace diffs.\n- E2E: load every artifact under the exact CSP its report publishes (enforced by the local server's headers); any CSP violation fails the artifact.\n\n## Considerations\n\n- WebXR and permission-dependent behavior need capable, explicitly identified environments; they are not silently credited here (§10.6).\n","acceptance_criteria":"For each standalone artifact, an automated run denies network subresource/code/asset requests, clears incidental caches, replays the full interaction script including every reachable mode, permits only the frozen HTML document request on intentional navigation and verifies its content identity, and checks H1's WebGL backend selection creates the exact GL component after navigation; hashes and licenses are verified; results are separate from any performance number.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:30.531469Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:21.036834Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-conformance","packaging","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-10-standalone-networked-packaging-jlu.5","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.4","type":"blocks","created_at":"2026-09-09T16:48:51.254028Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu.5","depends_on_id":"f3d-10-standalone-networked-packaging-jlu","type":"parent-child","created_at":"2026-09-09T16:46:30.531469Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu.5","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.2","type":"blocks","created_at":"2026-09-09T16:48:51.095825Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-10-standalone-networked-packaging-jlu.6","title":"Build cache on complete semantic inputs, deterministic rebuilds, incremental build cost","description":"## Full scope\n\nBuild cache keyed on complete semantic inputs, deterministic rebuild verification, content-addressed build units, and incremental build cost measurement (a color-only edit must not rebuild every generated kernel)\n\n## Summary\n\nDeveloper experience and cache correctness are product properties.\n\n## Background (plan §10.6 last paragraph, §16.9)\n\n\"Cache build artifacts using the complete semantic inputs... A stale cache hit must not resurrect a different material program or incompatible Wasm layout. Verify deterministic rebuilds where the build inputs admit determinism.\" \"Rebuilding every generated kernel for a color-only edit is a design regression even if the deployed frame is fast.\" \"Use bounded per-application specialization and content-addressed build units inside the existing compiler, not a new build platform.\"\n\n## Technical approach\n\n- Cache key derivation in `f3d-cli`: hash of the module graph bundle (04.1), asset manifest hashes, compiler crate revision, pinned upstream commit, toolchain, target features, ABI/layout schema versions (05.2/05.3), shader options, admission profile.\n- Content-addressed units: per-island kernel, per-variant WGSL, per-asset preprocessed blob.\n- Determinism check: build twice, compare artifact hashes; document any nondeterministic inputs (timestamps) and remove them.\n- Measurements: scripted edits (change a material color; change an update function) with timings of the incremental rebuild; reported in the DX section of the report.\n\n## Success criteria\n\n- Deterministic rebuild for H1/H2; stale-key negative test (bump the layout version, cache must miss); color-only edit rebuilds only the affected units.\n\n## Test plan\n\n- Unit: cache-key derivation tests (each semantic input changes the key; a layout-version bump misses); content-addressed unit tests.\n- E2E: deterministic-rebuild check on H1/H2 (build twice, identical hashes); scripted-edit timing script (material color change rebuilds only material-variant units; update-function change rebuilds only its island; asset swap; control addition) with cache hit/miss logs.\n- Logging: cache events `{unit, key, hit|miss, reason}` and per-stage timings `{stage, ms}`; archived under `evidence/10.6/`.\n- Failure artifacts: the two artifact hash manifests on nondeterminism.\n\n## Considerations\n\n- Keep it inside the CLI; no daemon or build service.\n","acceptance_criteria":"Build artifacts are cached by source/module and asset hashes, compiler and library revisions, target/toolchain/features, ABI/layout version, shader options and admission profile; a stale cache hit can never resurrect a different material program or incompatible Wasm layout; deterministic rebuilds are verified where inputs admit determinism; clean and incremental conversion times, Rust/Wasm and shader compile times, cache hit correctness, generated bytes and the latency of a common material/update edit are measured and reported.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:46:30.625532Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:05.591085Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-cli","f3d-compiler","packaging","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-10-standalone-networked-packaging-jlu.6","depends_on_id":"f3d-10-standalone-networked-packaging-jlu","type":"parent-child","created_at":"2026-09-09T16:46:30.625532Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu.6","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.2","type":"blocks","created_at":"2026-09-09T16:48:51.421352Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-10-standalone-networked-packaging-jlu.7","title":"Asset preprocessing legality and runtime codec inclusion policy","description":"## Full scope\n\nAsset preprocessing legality and runtime codec inclusion policy: predecode/reorganize static data only when public observations stay correct, no synchronous-ification of asynchronous loader callbacks, byte deduplication without sharing mutable objects, automatic inclusion of runtime decoders/transcoders when a caller can supply unseen inputs\n\n## Summary\n\nOptimize assets without changing loader contracts, and never ship a \"please preconvert your model\" error.\n\n## Background (plan §10.1, §10.2, §5.13)\n\n- \"Static model/texture data can be predecoded or reorganized when public observations remain correct. Preserve required source metadata and response shapes: a caller may request text/bytes, inspect a loader result, or construct a second instance independently. Deduplicating asset bytes must not accidentally share mutable scene objects, materials, animation state, or callback identities.\"\n- \"Preprocessing must not make an originally asynchronous loader callback unexpectedly synchronous.\"\n- \"Runtime decoding and transcoding are mandatory whenever a caller can provide previously unseen compressed models, textures, archives, or local files. Include the needed existing codec or a verified replacement automatically. A build-time-only decoder cannot satisfy that contract.\"\n- \"Do not substitute lossier textures or geometry... Use capability-selected embedded variants, an included runtime transcoder, or a justified equivalent representation; one compressed format is not assumed to cover every target.\"\n\n## Technical approach\n\n- Preprocessing passes in `f3d-assets` (build-time only): glTF binary consolidation, texture format variants per capability profile (with the original retained when the profile is unknown), no vertex/index reordering for loader-visible geometry (the attribute arrays are public, so reordering is always observable); only representation changes invisible to loader results (container consolidation, texture container variants) are candidates.\n- Async preservation: the resolver returns data through the same promise/callback microtask structure the original fetch would use (at least one turn of asynchrony), preserving `onProgress`/`onLoad`/`onError` order.\n- Dedupe: identical bytes share storage; loader outputs are constructed per call (upstream loaders already construct fresh objects from parsed data; verify per loader).\n- Codec inclusion rule in the closure analysis: if any reachable path accepts external input (file input, URL parameter, drag/drop, network fetch of unknown assets), include the runtime decoder(s) of every loader reachable from that path; report their bytes separately (§13.5).\n\n## Success criteria\n\n- Tests: predecoded glTF yields loader results equal (deep semantic compare) to runtime parsing; async ordering test; dedupe identity test; codec inclusion test on an app with file input.\n\n## Test plan\n\n- Unit: preprocessing legality tests (predecoded glTF yields loader results deep-equal to runtime parsing; async ordering preserved with at least one turn of asynchrony; `onProgress`/`onLoad`/`onError` order); dedupe identity tests (two loads produce independent objects); codec-inclusion rule tests on a fixture app with file input.\n- E2E: `tests/e2e/assets/preprocessing.mjs` on both browsers: preprocessed vs runtime-parsed scene semantic equality; a standalone artifact with file input loads an unseen KTX2/Draco asset because the codec was included.\n- Logging: preprocessing decisions `{asset, pass, proof_ref}` and codec inclusion list with bytes; archived under `evidence/10.7/`.\n- Failure artifacts: the semantic diff of loader results.\n\n## Considerations\n\n- Mipmaps, block edges, orientation, premultiplication, color conversion and samplers must survive any texture variant (§10.2).\n","acceptance_criteria":"Static models/textures may be predecoded or reorganized only with a recorded proof that response shapes, loader results, metadata and independent-instance construction remain correct; originally asynchronous callbacks stay asynchronous with preserved causal order; deduplicated bytes never share mutable scene objects, materials, animation state or callback identities; the closure analysis includes the runtime codec (Draco/Basis/KTX2/meshopt and any other reachable decoder) whenever the application can receive previously unseen compressed inputs.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:46:30.718583Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:21.402542Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-assets","f3d-compiler","loaders","packaging","phase-1","phase-2","tier-solid"],"dependencies":[{"issue_id":"f3d-10-standalone-networked-packaging-jlu.7","depends_on_id":"f3d-10-standalone-networked-packaging-jlu","type":"parent-child","created_at":"2026-09-09T16:46:30.718583Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu.7","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.1","type":"blocks","created_at":"2026-09-09T16:48:51.592387Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-10-standalone-networked-packaging-jlu.8","title":"f3d CLI orchestration: build/serve/explain with deterministic text and JSON output","description":"## Full scope\n\nf3d CLI orchestration (f3d-cli): build --compat full (default) with --standalone/--deployment networked, serve, explain; deterministic human text plus stable JSON output; --require-accelerated-webgpu diagnostic strict mode; inspect/verify/verify-upstream/bench subcommands accrue from their owning beads (inspect 12a.9, verify 18a.7, verify-upstream 18a.5, bench 11.4)\n\n## Summary\n\nThe thin orchestration layer (7,000-line budget) over the compiler, packer, and harness. Section 21 warns a polished CLI is not a substitute for a working artifact; this bead stays minimal and grows only as commands get real backends.\n\n## Background (plan §3.2, §3.5, §17.1; AGENTS.md \"Output Style\")\n\n```\nf3d inspect ./src/main.ts --format json\nf3d build ./index.html --compat full --standalone --out ./dist/demo.html\nf3d build ./src/main.ts --compat full --deployment networked --out ./dist\nf3d verify ./index.html --candidate ./dist/demo.html\nf3d verify-upstream --ref r186 --features all --tests all\nf3d bench ./benchmarks/headline.toml --device local\nf3d explain ./dist/demo.f3d-report.json\nf3d serve ./dist --port 8080\n```\n\"`--compat full` is the default, not an optional premium or later phase. A developer may additionally request `--require-accelerated-webgpu`... That diagnostic strict mode fails if its acceleration requirement cannot be met; it does not redefine the full product's functional scope.\" \"CLI output must be deterministic and documented, with stable JSON alongside human-readable text.\"\n\n## Technical approach\n\n- `f3d-cli` crate with a small argument parser (audited dependency or hand-rolled), subcommand dispatch to `f3d-compiler`/`f3d-assets`/`f3d-conformance` APIs; `serve` is a minimal static HTTPS/localhost server with optional isolation headers (10.3).\n- `explain` renders the build report JSON (12a.9) as text.\n- JSON envelopes with a schema version; exit codes documented.\n- Native asset/build tooling never becomes a dependency of the deployed Wasm (§17.3).\n\n## Application profiles (plan §3.2)\n\nAn application profile (the `--profile` input consumed by `build`, `verify` and `bench`) selects inputs, settings, permissions and performance workloads for a run. It **cannot** delete valid upstream API contracts or reachable controls from the full-compatibility claim: a profile that omits a control still ships that control's code and its compatibility components; only the strict `--require-accelerated-webgpu` diagnostic may fail a build, and only for an unmet acceleration requirement. The CLI rejects any profile field that would narrow functional scope and says so in the report.\n\n## Success criteria\n\n- `f3d build` produces H1/H2 artifacts; `f3d serve` serves them for the verification harness; `f3d explain` prints the report; JSON output validated against a schema in tests.\n- `f3d-cli/CONTRACT.md` is written before any other crate depends on `f3d-cli` (AGENTS.md \"Documentation and Contracts\"): purpose and position in the dependency direction, public types and semantics, invariants, implementation ownership route, error model, determinism and numeric-fidelity class, cancellation behavior, unsafe boundary if any, feature flags, conformance tests, and no-claim boundaries.\n\n## Test plan\n\n- Unit: argument parsing and exit-code tests per subcommand; JSON envelope schema validation with snapshot tests of text output; profile validation tests (a profile field that would narrow functional scope is rejected with a message).\n- E2E: `f3d build` produces H1/H2 artifacts; `f3d serve` serves them for the packaging harness; `f3d explain` prints the report; the `--require-accelerated-webgpu` strict mode fails only for an unmet acceleration requirement (fixture).\n- Logging: structured stderr diagnostics (bounded) with stable codes; command timing `metric` events; archived under `evidence/10.8/`.\n- Failure artifacts: the failing command line with stdout/stderr.\n\n## Considerations\n\n- Do not add commands without a backend; a stub subcommand is exactly the process-porn AGENTS.md forbids.\n","acceptance_criteria":"The f3d binary exposes build/serve/explain with the proposed flags; --compat full is the default; --require-accelerated-webgpu fails only when an acceleration requirement is unmet and never narrows the functional artifact; every command emits deterministic text and stable JSON; errors are structured and actionable; inspect, verify, verify-upstream and bench are wired as they land in F3D-12a.9, F3D-18a.7, F3D-18a.5 and F3D-11.4 respectively.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:30.810149Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:16.244288Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-cli","packaging","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-10-standalone-networked-packaging-jlu.8","depends_on_id":"f3d-10-standalone-networked-packaging-jlu","type":"parent-child","created_at":"2026-09-09T16:46:30.810149Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu.8","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.2","type":"blocks","created_at":"2026-09-09T16:48:51.755254Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu.8","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.3","type":"blocks","created_at":"2026-09-09T16:48:51.916703Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-10-standalone-networked-packaging-jlu.9","title":"Build-tool frontend adapters: Rollup/Vite plugin and Node API as thin wrappers over the f3d compiler","description":"## Full scope\n\nBuild-tool frontend adapters: a Rollup/Vite-family plugin and a small Node API that invoke the same `f3d` compiler (`build`, `verify`, `explain`) from an application's existing build, so ordinary bundler, module and Node-side utility integration are real delivery paths rather than a separate toolchain; the adapters are thin, pinned, audited, and never a dependency of the deployed Wasm.\n\n## Summary\n\nPlan §3.2: \"Existing build tools are thin frontend adapters to the same compiler. Ordinary bundler, module, Node-side utility, and upstream editor/player integration are required delivery paths, not excuses to invent another language toolchain.\" F3D-10.3 provides the networked/library outputs; this bead provides the integration surface developers actually use to produce them from their own projects.\n\n## Background (plan §3.2, §4.1, §10.4, §17.3)\n\nThe JS ingestion toolchain is already the pinned Rollup ecosystem plus an Acorn-class parser (04.1). A plugin that runs inside that ecosystem gives projects with existing Vite/Rollup builds a one-line integration: their build normalizes TypeScript/JSX, `f3d` ingests the normalized graph, and the plugin emits the full-compatibility artifact (standalone when closable, networked otherwise) plus the report. Native asset/build tooling must never become a dependency of the deployed Wasm (§17.3).\n\n## Technical approach\n\n- `tools/adapters/rollup-plugin-f3d` (works under Vite's Rollup pipeline) exposing `f3d({ mode: 'standalone' | 'networked', profile, requireAcceleratedWebGPU })`; it hands the bundler's resolved module graph to `f3d build` (10.8) and writes the artifact and `f3d-report.json` into the bundler's output directory; errors surface as bundler diagnostics with the same source spans as the CLI.\n- Node API `@f3d/build` with `build()`, `verify()`, `explain()` wrappers over the CLI's JSON envelopes (no second implementation).\n- Documentation examples for a plain HTML project, a Vite project and a library consumer; each is an integration test fixture.\n\n## Success criteria\n\n- H1 and H2 built through the plugin produce byte-identical artifacts to `f3d build` on the same inputs; a TypeScript Vite fixture builds end to end; diagnostics carry spans; the deployed Wasm has no dependency on the adapter package.\n\n## Test plan\n\n- Unit: plugin option validation; envelope wrapper tests against recorded CLI JSON; a dependency-graph test that the adapter is absent from the artifact.\n- E2E: `tests/e2e/adapters/*.mjs` building the three fixtures through the plugin and running the packaging harness on the outputs on both browsers.\n- Logging: plugin events `{fixture, mode, ms, artifact_hash}` and bundler diagnostics captured; archived under `evidence/10.9/`.\n- Failure artifacts: bundler logs and artifact hash diffs.\n\n## Considerations\n\n- A modified `three` in the project's `node_modules` is routed as an unrecognized revision with truthful status (04.2); the plugin never silently swaps it.\n","acceptance_criteria":"A pinned Rollup/Vite plugin and a Node API invoke the same f3d build/verify/explain with identical outputs to the CLI, surface diagnostics with source spans in the bundler, build a TypeScript Vite fixture end to end, and are never a dependency of the deployed Wasm.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T17:15:28.825295Z","created_by":"jemanuel","updated_at":"2026-09-09T17:15:29.397638Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-cli","js-host","packaging","phase-2","tier-solid"],"dependencies":[{"issue_id":"f3d-10-standalone-networked-packaging-jlu.9","depends_on_id":"f3d-10-standalone-networked-packaging-jlu","type":"parent-child","created_at":"2026-09-09T17:15:28.825295Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu.9","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.3","type":"blocks","created_at":"2026-09-09T17:15:29.180220Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu.9","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.8","type":"blocks","created_at":"2026-09-09T17:15:28.984799Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-10-standalone-networked-packaging-jlu.9","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:29.397330Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-11-first-product-gate-ui0","title":"F3D-11: First complete-application and new-backend product gate (H1 and H2, >=2x on phone and M5)","description":"## What this epic delivers\n\nKill gate 3. Plan §19 F3D-11 depends on 08, 09, 10, 12 (early), 15 (early), 22 (early); acceptance: \"Full H1/H2 behavior and >=2× named new-backend phone/M5 results with actual full artifact\".\n\n## Why (plan §16.7 \"First product gate\", §18 Phase 1, §20 row 1)\n\n\"Automatically transform the complete H1 and H2 applications, preserve all controls/backend modes and material variants, and separately score H1's WebGPU dynamic workload plus H2 through the new renderer. Both H1 backend modes must work; retained exact execution cannot count as the new-backend speedup. Include actual guards, materialization, bootstrap, and bridge costs. Require the >=2× floor against the independently selected strong references on the phone and M5 strata before broad compatibility implementation proceeds. If H1 is not viable against its competent reference, do not replace it with an easier toy without explicitly reopening the project decision.\"\n\nPhase 1 deliverables: \"full H1/H2 output artifacts; every control/mode works; all guards, compatibility bytes and setup costs measured; independently validated new-backend phone/M5 speedups; no file-name/trace-specialized cheat.\" Exit: \"first product performance gate and both applications' full functional tests pass. Exact H1 GL mode is not counted as accelerated; it is still implemented and tested.\"\n\n## Children\n\n1. H1 automatic end-to-end transformation.\n2. H2 automatic end-to-end transformation.\n3. H1/H2 equivalence validation.\n4. Scored paired runs on the phone and M5 strata.\n5. Kill gate 3 decision record.\n\n## Success criteria for the epic\n\nAll children closed with the decision recorded. If the gate fails, this epic closes with a \"failed gate\" reason and the project decision is reopened by the user; Phase 2+ epics must not proceed as if it passed.\n\n## No-claim boundary\n\nPassing this gate is a result for two applications on two strata. It is not a universal claim and not the launch contract (§16.6, §22).\n","acceptance_criteria":"The complete H1 and H2 applications are transformed automatically with every control, backend mode, navigation path and material variant intact; H1's WebGPU dynamic workload and H2 are scored through the new renderer with actual guards, materialization, bootstrap, bridge and compatibility bytes in the ordinary full artifact; paired runs against the frozen strong references on the iPhone 17 Pro Max and M5 strata meet the >=2x floor with 95% lower bounds; both applications' full functional tests pass; the kill-gate decision is recorded.","status":"open","priority":0,"issue_type":"epic","created_at":"2026-09-09T16:46:23.163665Z","created_by":"jemanuel","updated_at":"2026-09-09T16:48:57.381488Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-conformance","h1","h2","kill-gate","new-backend","phase-1","tier-frontier"],"dependencies":[{"issue_id":"f3d-11-first-product-gate-ui0","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago","type":"blocks","created_at":"2026-09-09T16:48:56.110054Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0","depends_on_id":"f3d-09-marching-cubes-kernel-fb3","type":"blocks","created_at":"2026-09-09T16:48:56.290184Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0","depends_on_id":"f3d-10-standalone-networked-packaging-jlu","type":"blocks","created_at":"2026-09-09T16:48:56.462311Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0","depends_on_id":"f3d-12a-early-islands-route-analysis-63y","type":"blocks","created_at":"2026-09-09T16:48:56.646652Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0","depends_on_id":"f3d-15a-early-shader-spike-7k8","type":"blocks","created_at":"2026-09-09T16:48:56.826651Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu","type":"blocks","created_at":"2026-09-09T16:48:57.029613Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0","depends_on_id":"f3d-gate-1-foundation-tzn","type":"blocks","created_at":"2026-09-09T16:48:57.207987Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0","depends_on_id":"f3d-gate-2-bridge-apz","type":"blocks","created_at":"2026-09-09T16:48:57.381204Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-11-first-product-gate-ui0.1","title":"H1 automatic end-to-end transformation (all controls, both backends, navigation, Inspector)","description":"## Full scope\n\nH1 automatic end-to-end transformation: original webgpu_performance_renderbundle source through the compiler to a full artifact with every control (static/dynamic, object count, bundle toggle, materials, camera controls, resize), both backend settings (WebGPU via new backend, WebGL via exact component), navigation/reload behavior, Inspector, and no file-name/trace specialization\n\n## Summary\n\nThe first real product: H1, whole, automatic.\n\n## Background (plan §2.7, §15.3, §4.7, §18 Phase 1, §21)\n\n- \"H1 already defaults to 4,000 objects, uses multiple geometry types and distinct toon-material colors, and supports render bundles and dynamic behavior. It is a particularly good test of whether the compiler can combine equivalent material programs and packed state without changing source-level identity.\"\n- \"The first compiler slice must already cover the guards, alias checks, input packing, output materialization, and conservative path required by the H1 update loop... the first kernel must be derived from source semantics, not selected by an example filename, benchmark flag, or a memorized input trace.\"\n- H1's complete backend and navigation contract (§15.3): both source backend settings, static/dynamic updates, object counts, bundle settings, materials, camera controls, resizing; page reloads for several settings; WebGL choice produces a genuine GL-backed renderer.\n- \"Next connect the original H1 source to the candidate compatibility boundary, even before it is fast. Preserve object identity and its dynamic/control behavior.\" (§21)\n\n## Technical approach\n\n- Run the full pipeline: ingest (04.1) → routing (04.2/04.4) → semantic analysis and island extraction of H1's animation loop (12a.2–12a.5: per-object rotation/position updates driven by time → bulk kernel + packed `AffineRows` upload) → material program specialization (08.3) + legal grouping (08.2) + bundles (08.4) → pass graph/execution plan (07.2/07.4) → packaging (10.2) with the exact GL component embedded for the WebGL branch.\n- Verify each control via the interaction script; verify navigation reload creates a fresh route decision; verify the Inspector.\n- Anti-cheat: build a copy of H1 with renamed identifiers, reordered declarations and a different file name; the compiler must produce equivalent islands/plans (report diff) without any example-specific table.\n\n## Success criteria\n\n- All controls and both backends pass the packaging verification harness (10.5) on Chrome and Safari; the build report shows the islands, guards, groupings and the exact-GL route for the WebGL branch; the renamed-copy test passes.\n\n## Test plan\n\n- Unit: none new (end-to-end product bead); relies on the compiler and packaging unit suites.\n- E2E: `tests/e2e/h1/full-artifact.mjs` on Chrome and Safari: packaging verification harness (10.5) over every control and both backend settings; navigation reload; Inspector operation; the anti-cheat renamed/reordered copy of H1 produces an equivalent report (islands, guards, groupings, routes) via `f3d inspect` diff; forced-new-backend lane equivalence at checkpoints.\n- Logging: build report archived as evidence; harness events per control with owner/route; report-diff summary for the renamed copy; archived under `evidence/11.1/`.\n- Failure artifacts: screenshots, trace diffs, both build reports.\n\n## Considerations\n\n- Do not narrow the artifact to the WebGPU branch to save bytes; the performance setting chooses what is measured, not what is shipped (§15.3).\n","acceptance_criteria":"f3d build on the unmodified H1 source produces a standalone artifact in which all controls work, the WebGPU setting renders through the new backend with guarded update islands for the dynamic loop, the WebGL setting constructs a genuine GL renderer through the exact component after navigation, the Inspector operates, and the compiler's decisions are derived from source semantics (verified by transforming a renamed and lightly reordered copy of H1 with identical results).","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:30.899167Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:17.540029Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-compiler","h1","new-backend","phase-1","tier-frontier"],"dependencies":[{"issue_id":"f3d-11-first-product-gate-ui0.1","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.4","type":"blocks","created_at":"2026-09-09T16:48:53.242900Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.1","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.7","type":"blocks","created_at":"2026-09-09T17:11:26.034615Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.1","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.5","type":"blocks","created_at":"2026-09-09T16:48:52.746690Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.1","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.6","type":"blocks","created_at":"2026-09-09T17:41:17.539452Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.1","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.8","type":"blocks","created_at":"2026-09-09T17:41:17.123048Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.1","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago.2","type":"blocks","created_at":"2026-09-09T16:48:52.582554Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.1","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago.4","type":"blocks","created_at":"2026-09-09T16:48:52.415587Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.1","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.5","type":"blocks","created_at":"2026-09-09T16:48:52.915276Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.1","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.8","type":"blocks","created_at":"2026-09-09T16:48:53.409877Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.1","depends_on_id":"f3d-11-first-product-gate-ui0","type":"parent-child","created_at":"2026-09-09T16:46:30.899167Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.1","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.5","type":"blocks","created_at":"2026-09-09T16:48:53.081962Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.1","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.6","type":"blocks","created_at":"2026-09-09T17:41:16.716989Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-11-first-product-gate-ui0.2","title":"H2 automatic end-to-end transformation (kernel, all materials, export/read after updates)","description":"## Full scope\n\nH2 automatic end-to-end transformation: original webgl_marchingcubes source through the compiler to a full artifact with the marching-cubes kernel, every material branch including the four legacy ShaderMaterial variants, environment lighting, all controls, and current-state export/read observations after accelerated updates\n\n## Summary\n\nH2, whole, automatic, including the legacy shader branches that a \"static shaders only\" product would skip.\n\n## Background (plan §15.3, §2.7, §5.13 \"Export must work after an accelerated update\", §18 Phase 1)\n\nPhase 1: \"Integrate marching cubes, legal batching/bundles, resource histories, standalone packaging and current-state export/read observations.\"\n\n## Technical approach\n\n- Pipeline as in 11.1; addon recognition (09.1) by pinned blob identity; ESSL variants via 15a.2; env-lit Standard via 07.3.\n- Observation tests after accelerated updates: read `geometry.attributes.position.array` values post-update; `mesh.matrixWorld` after transform animation; `GLTFExporter`/`OBJExporter` export of the current marching-cubes mesh compared to an upstream export at the same logical frame (semantic compare).\n- Anti-cheat renamed-copy test as for H1.\n\n## Success criteria\n\n- Sweep (09.3) passes in the artifact; export/read tests pass; packaging harness passes on both browsers.\n\n## Test plan\n\n- Unit: none new.\n- E2E: `tests/e2e/h2/full-artifact.mjs` on both browsers: packaging harness over the material/control sweep (09.3) in the artifact; post-update observation tests (`geometry.attributes.position.array` values, `matrixWorld`, `GLTFExporter`/`OBJExporter` output compared semantically to an upstream export at the same logical frame); renamed-copy anti-cheat diff.\n- Logging: harness events per step; export comparison summary `{format, nodes, attributes, max_delta}`; archived under `evidence/11.2/`.\n- Failure artifacts: exported files from both sides and the semantic diff.\n\n## Considerations\n\n- If a legacy shader variant fails translation, this bead is blocked (new-backend coverage gap), not narrowed.\n","acceptance_criteria":"f3d build on the unmodified H2 source produces a standalone artifact where the addon kernel is recognized by source fingerprint and lowered, all material/control branches render through the new backend, and reading geometry/transform state or exporting the scene after an accelerated update returns current values.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:30.997309Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:17.334150Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","h2","new-backend","phase-1","tier-frontier"],"dependencies":[{"issue_id":"f3d-11-first-product-gate-ui0.2","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.8","type":"blocks","created_at":"2026-09-09T17:41:17.333890Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.2","depends_on_id":"f3d-09-marching-cubes-kernel-fb3.2","type":"blocks","created_at":"2026-09-09T16:48:53.746461Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.2","depends_on_id":"f3d-09-marching-cubes-kernel-fb3.3","type":"blocks","created_at":"2026-09-09T16:48:53.584457Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.2","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.5","type":"blocks","created_at":"2026-09-09T16:48:53.911935Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.2","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.8","type":"blocks","created_at":"2026-09-09T16:48:54.241908Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.2","depends_on_id":"f3d-11-first-product-gate-ui0","type":"parent-child","created_at":"2026-09-09T16:46:30.997309Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.2","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.5","type":"blocks","created_at":"2026-09-09T16:48:54.075011Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.2","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.6","type":"blocks","created_at":"2026-09-09T17:41:16.913105Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-11-first-product-gate-ui0.3","title":"H1/H2 equivalence validation: visual, state, control and lifecycle profiles","description":"## Full scope\n\nH1/H2 equivalence validation: visual, state, control and lifecycle profiles at logical checkpoints with preregistered tolerances; event/state trace comparison; upstream unit-test lane green for the modules H1/H2 reach\n\n## Summary\n\n\"Every scored application also passes image/state/interaction checks before timing results count; a green test suite alone does not establish full functionality\" (§1.3).\n\n## Background (plan §14.5, §14.6, §16.6 \"Equivalence\")\n\nCompare frames and state observations at corresponding logical times; specify required causal order rather than identical wall-clock arrival; diagnostic counters may differ; excluded diagnostic pixels are narrowly identified before testing.\n\n## Technical approach\n\n- Run the harness-scripted sequences (03.1) on reference and candidate with identical seeds; compare at checkpoints using 03.6's tolerances (color-space-aware metric, edge/silhouette check, region checks, depth/normal diagnostic targets where the candidate exposes them).\n- Trace diff for events (callbacks, loader completion, control changes) with causal-order rules.\n- Lifecycle: resize mid-run, dispose and recreate, navigation reload (H1) with state re-established.\n- Upstream unit lane (18a.2) filtered to the modules reached by H1/H2 (from the closure records) must be green with owners recorded.\n\n## Success criteria\n\n- Equivalence profiles pass on Chrome and Safari on the M5 and Safari on the phone; failures block 11.4.\n\n## Test plan\n\n- Unit: none new (uses `f3d verify`'s comparison library tests from 18a.7).\n- E2E: `f3d verify` on H1 (both settings) and H2 on Chrome-M5, Safari-M5 and Safari-iPhone with protocol v1 tolerances: checkpoint images, event/state traces with causal-order rules, lifecycle steps (resize mid-run, dispose/recreate, H1 navigation reload); the mapped upstream unit lane filtered to modules reached by H1/H2 must be green with owners recorded.\n- Logging: per-checkpoint metrics and trace-diff events; per-test owner tags from the unit lane; archived under `evidence/11.3/<stratum>/`.\n- Failure artifacts: reference/candidate/diff images and trace diffs (goldens are never regenerated from the candidate).\n\n## Considerations\n\n- Never regenerate goldens from the candidate; a mismatch is a defect until proven otherwise (AGENTS.md honesty rule 5).\n","acceptance_criteria":"Both applications pass their full equivalence profile against the frozen reference: checkpoint images within the preregistered color-space-aware tolerances, identical event/state traces at corresponding logical times, all controls and lifecycle transitions (resize, disposal, reload) matching; the mapped upstream unit suites relevant to the reached modules pass with recorded owners; no speedup number is considered before this passes.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:31.086714Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:09.223829Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","h1","h2","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-11-first-product-gate-ui0.3","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.1","type":"blocks","created_at":"2026-09-09T16:48:54.753019Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.3","depends_on_id":"f3d-11-first-product-gate-ui0","type":"parent-child","created_at":"2026-09-09T16:46:31.086714Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.3","depends_on_id":"f3d-11-first-product-gate-ui0.1","type":"blocks","created_at":"2026-09-09T16:48:54.414514Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.3","depends_on_id":"f3d-11-first-product-gate-ui0.2","type":"blocks","created_at":"2026-09-09T16:48:54.592547Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.3","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.7","type":"blocks","created_at":"2026-09-09T17:44:09.223467Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.3","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.8","type":"blocks","created_at":"2026-09-09T17:44:09.021787Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.3","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.2","type":"blocks","created_at":"2026-09-09T16:48:54.926437Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.3","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.7","type":"blocks","created_at":"2026-09-09T17:11:27.140225Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-11-first-product-gate-ui0.4","title":"Scored paired runs on the phone and M5 strata (>=2x floor with 95% lower bounds)","description":"## Full scope\n\nScored paired runs on the iPhone 17 Pro Max and M5 strata: H1 WebGPU dynamic workload and H2 through the new renderer, full artifact including guards/materialization/bootstrap/bridge/compatibility bytes, frozen protocol, paired 95% lower bounds against the >=2x floor, cold/warm startup and memory, published with all reference configurations\n\n## Summary\n\nThe first FrankenThreeD performance numbers, produced exactly the way the plan says numbers must be produced.\n\n## Background (plan §16.2–16.6, §16.7; AGENTS.md \"Performance Program\", \"Benchmark hardware discipline\")\n\nMeasure completed logical frames per wall-clock second in the bounded pipeline; two frames in flight; completion acknowledgements on both sides; paired alternating runs with warmup; block bootstrap; log-space aggregation; report median, p95, p99, paired speedup, peak memory, upload totals; cold and warm startup measured separately; GPU timing runs on a reserved device with no other agent builds.\n\n## Technical approach\n\n- `f3d bench benchmarks/headline.toml --device <stratum>` (the bench subcommand lands here, wired into 10.8) runs the offscreen lane (03.2) for candidate and frozen reference (03.3) with the protocol's repeat count and order, collecting raw runs; the analysis script (03.8) computes the results.\n- Ownership: 07.6's attribution counters must show zero retained submissions in scored frames.\n- Startup: cold (cleared caches, fresh document) and warm; components: document acquisition, decoding, Wasm compile, shader/pipeline compile, asset prep, first correct frame, first interactive frame.\n- Memory: peak from 10.2's accounting plus browser-reported where available.\n- On-screen companion runs for pacing/input latency.\n\n## Success criteria\n\n- A results document per stratum with cell ratios, lower bounds, verdict per gate row (equivalence, 2× floor, confidence, tails, startup/memory, ownership), and the raw-run archive.\n\n## Test plan\n\n- Unit: bench-subcommand argument and protocol-loading tests; a dry run with reference-vs-reference data asserting ratios near 1.\n- E2E: `f3d bench benchmarks/headline.toml --device <stratum>` for H1 (WebGPU dynamic) and H2 on the reserved phone and M5 (Safari and Chrome) with protocol v1 repeat counts and alternating order; ownership assertion (zero retained submissions) on every scored frame; cold/warm startup and on-screen companion runs; analysis via 03.8.\n- Logging: raw per-frame JSON per run `{frame, submit_ts, ack_ts, credits}`; startup phase timings; capability records; the analysis report with per-cell verdicts; archived under `evidence/11.4/<stratum>/`.\n- Failure artifacts: any invalidated run retained with its reason (device loss, validation error, rejected completion).\n\n## Considerations\n\n- An unfavorable valid run is not an infrastructure failure. An inconclusive interval means a new preregistered batch, not peeking.\n- Never write \"faster\" anywhere until this bead's report exists (AGENTS.md honesty rule 1).\n","acceptance_criteria":"With protocol v1 and its byte/memory budget addendum (03.6, frozen from 10.2's first artifacts) in place, paired alternating runs on reserved uncontended devices produce per-cell median ratios and 95% lower bounds for H1 (WebGPU dynamic setting) and H2 on Safari-phone, Safari-M5 and Chrome-M5; the new-backend ownership assertion holds for all scored frames; cold and warm startup, peak memory and tail metrics are reported against the frozen budgets; results are published with R0 and every eligible reference configuration.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:31.179087Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:18.191138Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","h1","h2","kill-gate","new-backend","phase-1","tier-frontier"],"dependencies":[{"issue_id":"f3d-11-first-product-gate-ui0.4","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.6","type":"blocks","created_at":"2026-09-09T16:48:55.264518Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.4","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.7","type":"blocks","created_at":"2026-09-09T16:48:55.426375Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.4","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.8","type":"blocks","created_at":"2026-09-09T16:48:55.598605Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.4","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.7","type":"blocks","created_at":"2026-09-09T17:41:18.190812Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.4","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.2","type":"blocks","created_at":"2026-09-09T17:41:10.570056Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.4","depends_on_id":"f3d-11-first-product-gate-ui0","type":"parent-child","created_at":"2026-09-09T16:46:31.179087Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.4","depends_on_id":"f3d-11-first-product-gate-ui0.3","type":"blocks","created_at":"2026-09-09T16:48:55.100918Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.4","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu.5","type":"blocks","created_at":"2026-09-09T16:48:55.761830Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-11-first-product-gate-ui0.5","title":"Kill gate 3 decision record (first product gate)","description":"## Full scope\n\nKill gate 3 decision record: pass/fail against the first-product gate; on failure reopen the project decision rather than substituting an easier demo\n\n## Summary\n\nThe point of Phase 1 is to be able to stop. This bead makes the decision explicit and durable.\n\n## Background (plan §16.7, §20, §1.3 item 6)\n\n\"If H1 is not viable against its competent reference, do not replace it with an easier toy without explicitly reopening the project decision.\" \"Do not launch the project as a performance replacement if these requirements fail. A foundation gate can stop development long before full compatibility is implemented.\" Responses from §20 on failure: stop or substantially revise the thesis; do not weaken the reference; change granularity/ownership; abandon per-method Wasm wrapping; keep affected state JavaScript-owned and report the cost.\n\n## Technical approach\n\n- `docs/gates/gate-3-first-product.md` with the table of results, the verdict, the date, the protocol version, and links to raw evidence.\n- If PASS: note that broad compatibility implementation (Phase 2+) may proceed and that the result is not a universal claim.\n- If FAIL: freeze the tree state, disclose exactly which condition failed, and hand the decision to the user (AGENTS.md \"Mandatory checkpoint and stop rule\").\n\n## Success criteria\n\n- The document exists and is referenced by the README's status section (the README must stop saying \"no speedup measured\" only when a real number exists, and must state the number with its interval and device).\n\n## Test plan\n\n- Unit: none (decision record).\n- E2E: none; the decision cites 11.3 and 11.4 evidence directories by run id.\n- Logging: the decision document lists, per stratum, the evidence run ids, the verdict per gate row and the protocol version; README status updated to state the measured number with interval and device or the FAIL.\n- Failure artifacts: n/a.\n\n## Considerations\n\n- \"A changed product decision or benchmark corpus requires a newly labeled plan/results version and cannot retroactively turn the old failed gate green\" (§16.6).\n","acceptance_criteria":"A short decision document records, per stratum, the equivalence status, the H1/H2 cell results with lower bounds, ownership verification, startup/memory/tail status, and an explicit PASS or FAIL against §16.7's first-product gate; on FAIL it records that the project decision is reopened and lists the plan's permitted responses (change granularity/ownership, keep affected state JS-owned, stop) without weakening the reference or replacing H1.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:31.270Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:06.813544Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","kill-gate","phase-1"],"dependencies":[{"issue_id":"f3d-11-first-product-gate-ui0.5","depends_on_id":"f3d-11-first-product-gate-ui0","type":"parent-child","created_at":"2026-09-09T16:46:31.270Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-11-first-product-gate-ui0.5","depends_on_id":"f3d-11-first-product-gate-ui0.4","type":"blocks","created_at":"2026-09-09T16:48:55.939005Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12a-early-islands-route-analysis-63y","title":"F3D-12 (early slice): Sound closed-island extraction for the H1 update loop, guards/materialization/codegen, and irreversible route analysis","description":"## What this epic delivers\n\nThe compiler's first real capability, scoped to what the first product gate needs. Plan §19 F3D-12 depends on 04 and 05; acceptance: \"Guards/materialization before 11; escaped native access selected exactly before binding\". The general island optimizer is the later closure epic (F3D-12b).\n\n## Why (plan §4.3–4.9, §4.7 \"The first compiler slice\", §6.1, §3.5)\n\n\"A translation from `object.position.x = value` into an equally fine-grained Wasm call merely relocates overhead.\" The unit of specialization is a closed update island; a trace can suggest an island but cannot justify erasing a possible behavior; write barriers are source-level, not Proxies; guard failure must not run an update twice; specialization is partial evaluation with an explicit environment; synchronous work cannot acquire invisible yield points. \"Those costs cannot be implemented after the first performance result.\"\n\n## Children\n\n1. Semantic IR and bounded worklist optimizer skeleton.\n2. Semantic and effect analysis for the H1 update loop and the island admission contract.\n3. Source-level write barriers.\n4. Guards, safe deoptimization, materialization, dual emission and cost model.\n5. Kernel code generation to application Wasm and static JS host glue.\n6. Irreversible route/escape analysis.\n7. Partial-evaluation environment rules.\n8. Synchronous run-to-completion enforcement.\n9. Build report and `f3d explain`/`f3d inspect`.\n\n## Success criteria for the epic\n\nAll children closed; H1's update loop is lowered with guards and a conservative path; the report explains every barrier with a source span; `f3d-compiler` ships `CONTRACT.md`.\n\n## No-claim boundary\n\nIsland extraction here covers recognized patterns for H1 (and H2's addon recognition). General numeric-loop lifting is explicitly later (§4.6).\n","acceptance_criteria":"f3d-compiler admits a closed update island only when it can answer who mutates each value (including aliases), who reads intermediate state and when, which operations are pure, which numeric semantics are required and what invalidates the specialization; it instruments provably covered writes at build time without Proxies, emits both the optimized kernel and its conservative production path with guards that never cause double execution, materializes public state at declared boundaries, enforces the synchronous run-to-completion rule, selects exact routes for escaped native access before any binding, and produces the build report.","status":"open","priority":0,"issue_type":"epic","created_at":"2026-09-09T16:46:23.246744Z","created_by":"jemanuel","updated_at":"2026-09-09T16:49:00.772764Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","phase-1","tier-frontier"],"dependencies":[{"issue_id":"f3d-12a-early-islands-route-analysis-63y","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:49:00.254013Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.4","type":"blocks","created_at":"2026-09-09T16:49:00.431662Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.4","type":"blocks","created_at":"2026-09-09T16:49:00.601251Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.5","type":"blocks","created_at":"2026-09-09T16:49:00.772372Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12a-early-islands-route-analysis-63y.1","title":"Semantic IR and bounded worklist optimizer skeleton in f3d-compiler","description":"## Full scope\n\nSemantic IR in f3d-compiler: object identity, ownership, numeric types, update dependencies, effects, observations and source locations as typed IDs into owned arrays; bounded worklist optimizer skeleton; typed reader for the ingested module graph\n\n## Summary\n\nThe compiler's data model, kept deliberately small.\n\n## Background (plan §6.1, §4.1, §1.4)\n\n\"Three representations are enough... Semantic IR records object identity, ownership, numeric types, update dependencies, effects, observations, and source locations... The representations should use typed IDs into owned arrays. Avoid heap-allocated trait-object graphs in the hot path. A simple worklist optimizer with a bounded set of transformations is preferable to a general theorem prover or unrestricted equality-saturation engine.\" First-class objects: \"application modules, semantic scene state, update kernels, render/compute passes, resources, and compiled schedules.\"\n\n## Technical approach\n\n- `f3d-compiler::ir`: arenas for `Module`, `Function`, `Stmt/Expr` (a restricted expression language for admitted numeric code plus opaque nodes for everything else), `ObjectShape` (recognized Three.js classes with known property layouts from the manifest), `Value` (numeric type lattice: JS number, int32-coerced, f32-stored; unknown), `Effect` (write to property/array/callback/host), `Observation` (read at a boundary), `Span`.\n- Reader for the 04.1 bundle with schema version checks.\n- Worklist: ordered pass list (recognition → alias analysis → effect analysis → island admission → guard synthesis → codegen prep) with per-pass change flags and a fixpoint cap; deterministic ordering for reproducible builds.\n\n## Success criteria\n\n- Round-trip test: bundle → IR → summary dump is deterministic; unit tests for arena/span handling; the optimizer reaches fixpoint on H1/H2 within the cap.\n- `f3d-compiler/CONTRACT.md` is written before any other crate depends on `f3d-compiler` (AGENTS.md \"Documentation and Contracts\"): purpose and position in the dependency direction, public types and semantics, invariants, implementation ownership route, error model, determinism and numeric-fidelity class, cancellation behavior, unsafe boundary if any, feature flags, conformance tests, and no-claim boundaries.\n\n## Test plan\n\n- Unit: arena/span handling tests; bundle-reader round trip with schema-version mismatch rejected; worklist fixpoint tests (deterministic pass order; iteration cap reached on a pathological fixture reports rather than loops); fuzz target for the bundle reader (05.3 lane).\n- E2E: the compiler's IR summary dump (a test helper; later surfaced as `f3d inspect` in 12a.9) over H1/H2 is deterministic (snapshot tests).\n- Logging: pass-by-pass change counts `{pass, iteration, changed}` in verbose mode; archived under `evidence/12a.1/`.\n- Failure artifacts: the IR dump at the failing pass.\n\n## Considerations\n\n- Keep the expression language restricted; unknown constructs are opaque and force JS ownership, never approximated.\n","acceptance_criteria":"f3d-compiler defines the semantic IR with typed handles for modules, functions, objects, properties, values, effects and observations, each carrying source spans; the module-graph bundle from the ingestion adapter is read into it; a worklist optimizer runs a bounded, ordered set of passes to a fixpoint with an iteration cap; there are no heap-allocated trait-object graphs in the hot path.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:31.367378Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:10.050374Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","phase-0","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-12a-early-islands-route-analysis-63y.1","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.3","type":"blocks","created_at":"2026-09-09T17:44:09.424152Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.1","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.1","type":"blocks","created_at":"2026-09-09T16:48:57.751789Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.1","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.1","type":"blocks","created_at":"2026-09-09T16:48:57.563554Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.1","depends_on_id":"f3d-12a-early-islands-route-analysis-63y","type":"parent-child","created_at":"2026-09-09T16:46:31.367378Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12a-early-islands-route-analysis-63y.2","title":"Semantic/effect analysis for the H1 update loop and the island admission contract","description":"## Full scope\n\nSemantic and effect analysis for the H1 update loop and the island admission contract: who mutates each value including aliases, who reads intermediate state and when, pure vs effectful operations, required numeric semantics, invalidation conditions; domain contract of ordinary guarded data properties, admitted number/array types, known call targets and covered observations; prototype changes, replaced methods, reflection, unknown callbacks, eval and escaped aliases force JavaScript ownership\n\n## Summary\n\nThe five questions of §4.3, answered by analysis for the first real workload.\n\n## Background (plan §4.3, §4.8, §4.4 last paragraphs, §5.3)\n\nAdmission questions: (1) Who can mutate each value, including aliases? (2) Who can read intermediate state, and when? (3) Which operations are pure numeric work, and which have effects? (4) Which numeric semantics are required? (5) What invalidates the specialization?\n\"Pure islands need a concrete domain contract: ordinary guarded data properties, admitted number/array types, known call targets, and covered observations. A guard must not invoke a getter or Proxy trap an extra time. Prototype changes, replaced methods, reflection, unknown callbacks, `eval`, and escaped aliases may force JavaScript ownership.\"\n\"Unknown or escaped mutations select a conservative ownership mode. In particular, arbitrary writes through a retained typed-array alias cannot generally be discovered by instrumenting only the original variable.\"\n\n## Technical approach\n\n- Recognition of Three.js shapes from the manifest (Object3D/Mesh/Material/BufferAttribute property layouts) and of the pinned upstream method semantics for calls the island uses (`Object3D.rotation.x +=`, `position.set`, `Matrix4.compose` reached via `updateMatrixWorld`).\n- Alias analysis over the module graph: track references to island objects (array elements, closures, event handlers, Inspector registration); an escape to unknown code marks the object's mutable regions as JS-owned unless covered by write barriers (12a.3).\n- Effect classification per statement: pure numeric; property write covered; property write uncovered (escape); callback invocation (ordered barrier); host call.\n- Numeric semantics: mark operations needing JS lowering (06.1's `jsnum`), f32 storage boundaries (attribute arrays), and transcendental uses.\n- Invalidation set: shape changes (prototype/method replacement detected by guards), material identity changes, object array membership changes, callback identity changes.\n- Output: `IslandCandidate` records + refusal diagnostics with spans.\n\n## Success criteria\n\n- H1's loop admits with the expected coverage; adversarial variants refuse with the correct reasons; the analysis of H2 recognizes the addon call site as a known-kernel island (09.1) and its material selector as JS-owned control flow.\n\n## Test plan\n\n- Unit: analysis fixtures for H1's loop shape (admits with expected coverage) and adversarial variants (captured alias to `position`, replaced `updateMatrixWorld`, Proxy-wrapped object, `eval`, reflective access) that must refuse with the correct reason; H2 fixture recognizes the addon call site as a known-kernel island and the material selector as JS-owned control flow.\n- E2E: the compiler's analysis dump (a test helper; later surfaced as `f3d inspect` in 12a.9) on the ingested H1 (from 04.7) emits the island candidate report; snapshot-tested.\n- Logging: per-object ownership decision table `{object, region, mode, reason, span}` in verbose builds; refusals at `warn` with spans; archived under `evidence/12a.2/`.\n- Failure artifacts: the analysis report for the failing fixture.\n\n## Considerations\n\n- \"Observing a property stay constant for 1,000 frames is not proof that it is constant\" (§4.3): no trace-based admission.\n","acceptance_criteria":"For H1's animation loop (per-object rotation/position updates from time and per-object parameters over the object array, plus the render call), the analysis produces an island candidate with the five admission answers recorded, a list of covered writes/reads, the observation boundaries (render, callbacks, Inspector reads), and refusals with spans for any construct outside the domain contract; the same analysis on adversarial variants (an alias to an object's position captured elsewhere, a replaced updateMatrixWorld, a Proxy) refuses admission.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:31.463659Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:10.229616Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","h1","phase-1","tier-frontier"],"dependencies":[{"issue_id":"f3d-12a-early-islands-route-analysis-63y.2","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.7","type":"blocks","created_at":"2026-09-09T17:11:25.839847Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.2","depends_on_id":"f3d-12a-early-islands-route-analysis-63y","type":"parent-child","created_at":"2026-09-09T16:46:31.463659Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.2","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.1","type":"blocks","created_at":"2026-09-09T16:48:57.950109Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.2","depends_on_id":"f3d-gate-1-foundation-tzn","type":"blocks","created_at":"2026-09-09T17:31:52.554188Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12a-early-islands-route-analysis-63y.3","title":"Source-level write barriers (no Proxies) with conservative ownership for escapes","description":"## Full scope\n\nSource-level write barriers: instrument provably covered writes at build time, coalesce field changes into frame or observation-boundary batches, handle aliases, compound assignment, evaluation order, setters, method overrides and exceptions; no universal JavaScript Proxies; uncovered/escaped mutations select conservative ownership with disclosed cost\n\n## Summary\n\nChange detection that does not change behavior.\n\n## Background (plan §4.4)\n\n\"Instrument provably covered writes at build time. Coalesce the resulting field changes into a frame or observation-boundary batch. Account for aliases, compound assignments, evaluation order, setters, method overrides, and exceptions. Do not wrap the entire application in JavaScript Proxies... Respect Three.js's explicit upload-version contract where it applies; where additional synchronization is needed, pay for a scan/copy or retain the state in JavaScript and disclose the cost. The compiler must not claim work proportional only to the number of changed fields unless its mutation coverage actually supports that claim.\"\n\n## Technical approach\n\n- Rewrite pass (in the JS emission side of the compiler, using the pinned AST toolchain for printing): for each covered write site, emit the original write plus a barrier call recording (object handle, field id) into a typed dirty batch (bitset in Wasm memory or a JS typed array flushed at the boundary); the barrier is emitted immediately after the completed write in the same statement sequence (a barrier before the write would record a dirty bit for a write that never happened); temporaries are used only to preserve JavaScript evaluation order when the target expression itself has side effects (for example `getObj().x = f()`), never to move the barrier ahead of the write.\n- Setters/`_onChange`: Three.js `Euler`/`Quaternion` use change callbacks; barriers integrate with those semantics (06.4) rather than duplicating them.\n- Exceptions: a throw after several writes leaves the batch consistent with what upstream would have observed (no rollback, no missing dirty bits).\n- Uncovered regions: the region is JS-owned (snapshot at boundary via a bounded scan/copy) with the scanned byte count in the report.\n- Measurement: barrier cost per write and batch flush cost, reported via 07.7.\n\n## Success criteria\n\n- H1 with barriers produces identical traces to unmodified H1; dirty batches exactly match the set of written fields under property tests including compound assignments and exceptions; escaped-alias fixture routes to scan/copy with the cost reported.\n\n## Test plan\n\n- Unit: AST-level rewrite tests per construct (simple write, compound assignment, setter, overridden method, throwing RHS, alias write); property tests on generated write sequences asserting the dirty batch equals the set of written fields; escaped-alias fixture routes to scan/copy with the byte count in the report.\n- E2E: `tests/e2e/compiler/barriers.mjs`: H1 with barriers produces identical traces to unmodified H1 on both browsers; barrier overhead measured via 07.7 counters.\n- Logging: barrier counts and batch sizes per frame (debug) and the scan/copy byte counts per region; archived under `evidence/12a.3/`.\n- Failure artifacts: the rewritten source excerpt and the batch diff.\n\n## Considerations\n\n- Never introduce accessors where descriptors/enumeration are observable (§4.8).\n","acceptance_criteria":"The compiler rewrites covered write sites in the application's own source (static output, no Proxies) to record field changes into a batch consumed at the next observation boundary; aliases, compound assignments, evaluation order, setters, overrides and exceptions are handled with tests; writes the analysis cannot cover route the region to JavaScript ownership or a scan/copy path whose byte cost appears in the report; the barrier overhead is measured.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:31.551530Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:10.604610Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","js-host","phase-1","tier-frontier"],"dependencies":[{"issue_id":"f3d-12a-early-islands-route-analysis-63y.3","depends_on_id":"f3d-12a-early-islands-route-analysis-63y","type":"parent-child","created_at":"2026-09-09T16:46:31.551530Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.3","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.2","type":"blocks","created_at":"2026-09-09T16:48:58.140625Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12a-early-islands-route-analysis-63y.4","title":"Guards, safe deoptimization, materialization, dual emission and cost model","description":"## Full scope\n\nGuards, safe deoptimization and materialization: guards validate shape/material/callback assumptions before pure kernels run; effectful blocks stay ordered barriers; guard failure never double-executes an update; declared materialization boundaries preserve identity; both the optimized kernel and its conservative production path are emitted; the cost model includes guard scans and facade traffic and keeps small or frequently observed islands in JavaScript\n\n## Summary\n\nSpecialization that is safe to be wrong about.\n\n## Background (plan §4.5, §4.8, §14.8; AGENTS.md counterexample \"Guard failure without double-execution\")\n\n\"A specialized island may depend on a material configuration, object shape, or callback identity. A guard validates these assumptions before the affected work executes. On guard failure, invalidate the relevant plan and use the compatibility execution path until a new specialization is ready. Guard failure must not cause the application to run an update twice... pure kernels are guarded before execution, and effectful blocks remain ordered barriers unless a stronger transformation is justified.\" \"Emit both the optimized kernel and its production conservative path. Define guard failure, materialization, and re-entry before emitting optimized code. The cost model includes guard scans and facade traffic, not just the inner Rust loop. Small or frequently observed islands should remain JavaScript when crossing/packing costs dominate.\" \"Guards/materialization are part of the measured optimized path, not disabled in benchmark mode.\"\n\n## Technical approach\n\n- Guard synthesis from 12a.2's invalidation set: cheap checks (prototype identity, method identity, material identity/keys, array length/identity, callback identity) executed before the kernel; guard must not invoke getters/Proxy traps.\n- Deopt protocol: on failure, mark the plan segment invalid, execute the conservative path (the original JS statements, still with barriers) for this operation, schedule re-specialization off the hot path (region policy 02.5).\n- Materialization: per island, the boundary set (render, callbacks, explicit reads discovered by analysis, exporter entry) where packed state is published to public objects (06.4 mechanism).\n- Cost model: constants measured by micro-runs in 03.5/05.6 (crossing cost, bytes/µs packing); an island is admitted only if estimated savings exceed guard + facade cost with margin; decision recorded in the report.\n- Tests: guard failure after a side-effecting statement in the same source operation → no double effect; deopt after replacing a method at runtime; an intentionally tiny island stays JS.\n\n## Success criteria\n\n- Counterexample and deopt tests pass; H1's island admitted with its cost line; a synthetic small-island fixture refused by cost.\n\n## Test plan\n\n- Unit: guard synthesis tests from invalidation sets; guard-failure-without-double-execution counterexample (side effect before the failing guard runs exactly once); deopt-after-method-replacement test; cost-model tests with measured constants (a tiny island is refused; H1's island admitted with its cost line); a test that guards never invoke getters/Proxy traps.\n- E2E: `tests/e2e/compiler/guards.mjs` on both browsers: runtime method replacement at a random frame falls back correctly with trace equality; mutation check disabling the guard demonstrates the failure it prevents.\n- Logging: guard outcomes per frame (bounded) `{island, guards_checked, failures, deopts}`; re-specialization events off the hot path; archived under `evidence/12a.4/`.\n- Failure artifacts: the trace around the guard failure and the cost-model inputs.\n\n## Considerations\n\n- Re-specialization is off the hot path; it never blocks a synchronous operation (§4.9).\n","acceptance_criteria":"Every admitted island has a guard set, a materialization plan and a conservative path; a guard failure mid-frame falls back to the conservative path for that operation without re-running already-performed side effects (counterexample test); the cost model's inputs (guard scan cost, packing/unpacking bytes, crossing counts) are measured constants per device and a small island is demonstrably kept in JS; deoptimization after an assumption change is tested.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:31.644973Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:12.701788Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","phase-1","tier-frontier"],"dependencies":[{"issue_id":"f3d-12a-early-islands-route-analysis-63y.4","depends_on_id":"f3d-02-asupersync-browser-execution-58j.5","type":"blocks","created_at":"2026-09-09T17:41:12.701317Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.4","depends_on_id":"f3d-06-packed-transforms-materialization-roa.4","type":"blocks","created_at":"2026-09-09T16:48:58.489251Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.4","depends_on_id":"f3d-12a-early-islands-route-analysis-63y","type":"parent-child","created_at":"2026-09-09T16:46:31.644973Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.4","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.2","type":"blocks","created_at":"2026-09-09T16:48:58.315638Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12a-early-islands-route-analysis-63y.5","title":"Kernel code generation to application Wasm plus static JS host glue","description":"## Full scope\n\nKernel code generation: admitted islands to generated Rust kernels compiled into the application-specific Wasm with the pinned nightly; static JavaScript host glue (no eval/new Function); input packing and output materialization; H1 update kernel producing packed AffineRows uploads\n\n## Summary\n\nFrom island to executable, end to end.\n\n## Background (plan §4.1, §4.6 row 1, §6.4, §17.2)\n\n\"Generated Rust kernels are compiled into an application-specific Wasm module with the pinned nightly toolchain.\" \"Many object transforms updated from time and numeric parameters → Bulk CPU Wasm kernel and packed transform upload.\" \"Generate ordinary static JavaScript source: no production `eval` or `new Function` requirement.\" \"Application-specific generated kernels are also reported per artifact; generators must not be used to disguise a giant checked-in engine.\"\n\n## Technical approach\n\n- Rust codegen templates for the restricted expression language: loops over packed SoA arrays (06.2), `jsnum` calls where required, f32 conversions at the same boundaries as the source; kernel signature takes handles/ranges, returns nothing; results land in packed state that 06.4 materializes and 08.1 uploads.\n- Build integration: the generated kernel source is emitted into the application's build directory as a generated Cargo package (build output, never a checked-in workspace crate; the eleven-crate rule is untouched) that depends on `f3d-core`/`f3d-math`/`f3d-scene`/`f3d-runtime` and is compiled by the CLI with the pinned toolchain into the single application Wasm (Asupersync + f3d runtime + kernels); build cache keys (10.6).\n- JS glue emission: static module with the barrier batch flush, kernel invocation at the source's synchronous call site (the animation loop statement is replaced by `kernel_update_h1(t, ...)` plus the conservative fallback under the guard), and materialization calls.\n- Generated-code report: line counts and bytes per artifact.\n\n## Success criteria\n\n- H1 builds with its kernel; trace equality vs unmodified H1; uploads are packed `AffineRows` ranges; the generated Rust is readable and free of unsafe.\n\n## Test plan\n\n- Unit: golden tests for generated Rust and JS on fixtures (no `unsafe`, no `eval`/`new Function`); build-integration test compiling the generated crate with the pinned toolchain into the single application Wasm; input packing/output materialization round-trip tests.\n- E2E: H1 builds with its kernel; `tests/e2e/compiler/kernel-h1.mjs` asserts trace equality vs unmodified H1 and that uploads are packed AffineRows ranges (counters) on both browsers.\n- Logging: codegen summary in the build report `{islands, generated_rust_lines, generated_js_lines, wasm_bytes}`; archived under `evidence/12a.5/`.\n- Failure artifacts: the generated sources and the compiler diagnostics.\n\n## Considerations\n\n- The kernel runs to completion synchronously (12a.8); no chunk yields.\n","acceptance_criteria":"The compiler emits Rust source for admitted islands (H1's per-object update loop over packed arrays using f3d-math with JS numeric lowering), compiles it, as a per-application generated package in the build cache (never a workspace crate), with the workspace crates the runtime needs (f3d-core, f3d-math, f3d-scene, f3d-graph, f3d-gpu, f3d-runtime) into one application Wasm linked with the Asupersync runtime, and emits static JS glue that packs inputs at the boundary, invokes the kernel synchronously, and materializes outputs; the generated code is reported per artifact and never disguises a checked-in engine.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:31.739786Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:08.816384Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","f3d-runtime","js-host","phase-1","tier-frontier"],"dependencies":[{"issue_id":"f3d-12a-early-islands-route-analysis-63y.5","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.4","type":"blocks","created_at":"2026-09-09T16:48:58.844013Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.5","depends_on_id":"f3d-06-packed-transforms-materialization-roa.2","type":"blocks","created_at":"2026-09-09T16:48:59.020845Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.5","depends_on_id":"f3d-12a-early-islands-route-analysis-63y","type":"parent-child","created_at":"2026-09-09T16:46:31.739786Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.5","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.4","type":"blocks","created_at":"2026-09-09T16:48:58.667296Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.5","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.7","type":"blocks","created_at":"2026-09-09T17:44:08.601958Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.5","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.8","type":"blocks","created_at":"2026-09-09T17:44:08.816061Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.5","depends_on_id":"f3d-gate-1-foundation-tzn","type":"blocks","created_at":"2026-09-09T17:15:28.255134Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12a-early-islands-route-analysis-63y.6","title":"Irreversible route and escape analysis feeding the construction hook","description":"## Full scope\n\nIrreversible route and escape analysis: detect renderer/canvas/native state escaping to opaque code that may call GL-specific or native-backend methods, required native contracts (sync readback, getContext, XR fallback), and select the exact component before any binding; report decisions with source spans\n\n## Summary\n\nThe compile-time half of \"the backend choice is irreversible\".\n\n## Background (plan §5.6, §6.9, §20 \"Late backend choice breaks canvas identity\")\n\n\"The source/runtime and dependency analysis determines the safe selection before an irreversible canvas/device/native-handle binding. Unknown future native use through escaped references requires conservative exact selection.\" \"If a renderer or its canvas/native state can escape to opaque code that may call GL-specific or native-backend methods, preserve the corresponding exact component from construction. If the source closes those observations, its non-native work can use the new backend without carrying legacy decisions in the hot loop.\"\n\n## Technical approach\n\n- Escape analysis on the IR: track the renderer object, its `domElement`/canvas, `getContext()` results, `renderer.state`/`properties` accessors, `xr` manager; any flow into unknown callee, global, event handler, or retained addon whose fingerprint indicates native use → escape.\n- Native contract detection: calls to `readRenderTargetPixels` (sync), `getContext`, `resetState`, `forceContextLoss`, `GLBufferAttribute` construction, `WebGLXRFallback`, and explicit source-observable backend selection (`forceWebGL: true` on `WebGPURenderer`, and user-facing backend-mode controls such as H1's navigation-based selection, §15.3). A plain `new WebGLRenderer()` with closed observations is not by itself a native contract: plan §5.12 (WebGL renderer contract) allows source-equivalent non-native rendering to specialize into WebGPU after sound analysis, and H2–H6 are scored that way.\n- Output: `RouteDecision { site, route, reasons, spans }` consumed by 04.4; report section.\n\n## Success criteria\n\n- Fixtures pass (exact for escape/native contracts, new-backend for closed cases); H1's two branches classified correctly; H2 (WebGLRenderer as authored with no native observations) classified as new-backend-eligible for the accelerated route with the exact component retained for its functional GL lane only if the source contract requires it (record the reasoning).\n\n## Test plan\n\n- Unit: escape-analysis fixtures (renderer passed to an unknown plugin → exact; `readRenderTargetPixels` sync use → exact; `getContext` → exact; `WebGLXRFallback` → exact; closed observations → new-backend-eligible); decision snapshot for the corpus (H1 both branches; H2 reasoning recorded).\n- E2E: 04.4's routing hook consumes the decisions for H1/H2 in the routed builds; the report shows spans and reasons.\n- Logging: decision table `{site, span, route, reasons}`; archived under `evidence/12a.6/`.\n- Failure artifacts: the flow trace that produced a wrong classification.\n\n## Considerations\n\n- \"Do not assume arbitrary subclasses can be reproduced by a name-based shim\" (§5.6): unknown `Renderer`/`Backend` subclasses are exact by default.\n","acceptance_criteria":"The analysis classifies every renderer construction site as new-backend-eligible or exact-required using conservative escape analysis and native-contract detection; the routing hook consumes the classification before binding; H1's WebGL branch and a fixture passing the renderer to an unknown plugin both select exact; a fixture with closed observations selects the new backend; each decision carries spans and reasons in the report.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:31.831909Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:05.375243Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-compiler","phase-0","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-12a-early-islands-route-analysis-63y.6","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.4","type":"blocks","created_at":"2026-09-09T16:48:59.370590Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.6","depends_on_id":"f3d-12a-early-islands-route-analysis-63y","type":"parent-child","created_at":"2026-09-09T16:46:31.831909Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.6","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.1","type":"blocks","created_at":"2026-09-09T16:48:59.197512Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12a-early-islands-route-analysis-63y.7","title":"Partial-evaluation environment rules (no baking of unbound runtime state)","description":"## Full scope\n\nPartial-evaluation environment rules: offline evaluation only for inputs proven independent of unbound runtime state; DOM, viewport, adapter, query string, locale, clock, random values and network responses are not constants; preserve random-source consumption and observable construction/ID ordering; runtime-created objects and unobserved branches remain executable\n\n## Summary\n\nSpecialization is partial evaluation with an explicit environment, not snapshotting.\n\n## Background (plan §4.8, §3.1)\n\n\"Offline evaluation is allowed only for inputs proven independent of unbound runtime state. A captured DOM, viewport, adapter, query string, locale, clock, random value, or successful network response is not automatically an application constant. Preserve random-source consumption and observable construction/ID ordering when baking work would otherwise advance them differently. Runtime-created objects and unobserved branches remain executable; a capture is not a complete application specification.\"\n\n## Technical approach\n\n- Environment model in the IR: a set of unbound sources with taint propagation; constant folding stops at tainted values.\n- ID/random preservation: any baked construction must reproduce the same `id` sequences for every counter-assigned class (`Object3D`, `Material`, `BufferGeometry`, `Texture`, `Source`, `Skeleton`, and any other the census finds) and the same `Math.random` consumption count as the runtime path (either do not bake constructions, or emit an explicit \"advance counters\" prologue proven equivalent).\n- Branch executability: dead-code elimination uses static reachability only; capture data is advisory (for variant precompilation priority), never for removal.\n\n## Success criteria\n\n- Fixtures pass; H1's `count`/`dynamic` URL-parameter branches remain executable in the artifact; ID sequences match upstream in trace tests.\n\n## Test plan\n\n- Unit: taint-lattice tests (each unbound source stops folding); ID/random preservation tests (baked construction reproduces the same `id` sequences for every counter-assigned class and the same `Math.random` consumption count as runtime); a captured-trace fixture proving no branch is eliminated from capture data.\n- E2E: H1's URL-parameter branches remain executable in the artifact (packaging harness); ID-sequence equality vs upstream in the browser.\n- Logging: list of folded constants with proof sources and the counter-advance prologue if emitted; archived under `evidence/12a.7/`.\n- Failure artifacts: the folding decision with its taint path.\n\n## Considerations\n\n- Interacts with 10.4 (capture is sandboxed and untrusted).\n","acceptance_criteria":"The compiler's constant-folding/baking passes consult an explicit environment model that marks the listed sources as unbound; a fixture that reads window.innerWidth or Math.random at module scope is not baked; object ID ordering (Object3D.id counters) and random consumption are preserved relative to upstream; a captured trace can never mark a branch as dead.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:31.918428Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:10.782498Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-12a-early-islands-route-analysis-63y.7","depends_on_id":"f3d-12a-early-islands-route-analysis-63y","type":"parent-child","created_at":"2026-09-09T16:46:31.918428Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.7","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.2","type":"blocks","created_at":"2026-09-09T16:48:59.542044Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12a-early-islands-route-analysis-63y.8","title":"Synchronous run-to-completion enforcement and the sync-yield counterexample","description":"## Full scope\n\nSynchronous run-to-completion enforcement: admitted synchronous kernels never acquire host yield points; chunking only for source-asynchronous work, isolated worker jobs, or proven interleaving equivalence; exceptions and partial effects preserved; synchronous-yield interleaving counterexample; honest cancellation-latency bound reporting\n\n## Summary\n\nA cooperative runtime does not get to change the browser's run-to-completion semantics.\n\n## Background (plan §4.9, §11.3, §6.3; AGENTS.md \"Synchronous work cannot acquire invisible yield points\"; counterexample \"Synchronous-yield interleaving\")\n\n\"Doing so can expose half-updated state, alter Promise ordering, or deliver user input inside a computation that previously ran to completion... Preserve source exceptions and partial effects: an effectful loop that throws after several mutations cannot be replaced by an all-or-nothing bulk commit unless those intermediate effects are unobservable. Partial numeric work may use scratch output and commit only where that is actually equivalent.\"\n\n## Technical approach\n\n- Type-level enforcement: kernels are plain synchronous functions; the runtime's frame path has no async entry; a lint in the compiler rejects any emitted `await`/timer inside a synchronous island; chunked execution is available only through region tasks for source-async work (02.5).\n- Exceptions: the kernel's numeric work writes scratch output and commits per element in source order when effects are observable, or bulk-commits only when the analysis proves intermediate effects unobservable; on a thrown exception (from a guarded callback or a coerced non-numeric operand), state matches upstream's partial state.\n- Counterexample test: a source loop with a microtask scheduled mid-way (`queueMicrotask`) and a synchronous read after the loop; a deliberately chunked implementation shows the divergence; the candidate matches upstream.\n- Cancellation bound: for kernels exceeding a threshold, the report states that cancellation cannot be observed before the kernel returns.\n\n## Success criteria\n\n- Counterexample and exception-partial-effect tests pass; lint rejects a synthetic yielding kernel.\n\n## Test plan\n\n- Unit: compiler lint rejecting a synthetic yielding kernel; exception-partial-effect tests (throw after N mutations leaves exactly upstream's partial state); scratch-commit equivalence tests.\n- E2E: `tests/e2e/compiler/sync-yield.mjs` on both browsers: a source loop with a microtask scheduled mid-way and a synchronous read after it; the deliberately chunked implementation diverges, the candidate matches upstream; kernel-duration histogram collected.\n- Logging: kernel duration `metric` events feeding the cancellation-bound statement in the report; the interleaving trace on failure; archived under `evidence/12a.8/`.\n- Failure artifacts: the trace diff showing the exposed intermediate state.\n\n## Considerations\n\n- \"Unknown/non-numeric operands stay on the JavaScript path rather than introducing a Rust panic or an unintended coercion\" (§5.4).\n","acceptance_criteria":"The compiler and runtime cannot insert an await or host yield inside an originally synchronous source operation; an effectful loop that throws after several mutations keeps its partial effects on the candidate exactly as upstream; a counterexample test proves that splitting the update across turns would expose half-updated state or alter Promise ordering and that the candidate does not; the report states the cancellation-latency bound for long synchronous kernels.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:32.010308Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:08.207341Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","f3d-runtime","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-12a-early-islands-route-analysis-63y.8","depends_on_id":"f3d-12a-early-islands-route-analysis-63y","type":"parent-child","created_at":"2026-09-09T16:46:32.010308Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.8","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.4","type":"blocks","created_at":"2026-09-09T16:48:59.723325Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12a-early-islands-route-analysis-63y.9","title":"Build report (text and stable JSON), f3d explain and f3d inspect","description":"## Full scope\n\nBuild report (human text and stable JSON) and f3d explain/inspect: Three.js version and asset/module hashes; specialized/general/retained/exact ownership; functional gaps vs host limitations vs optimization-only barriers with no status conversion; structural cost estimates distinct from measured timings; precise source spans and reasons for barriers; unlock suggestions; device profile and optional capability variants\n\n## Summary\n\nThe report that makes the compiler useful to humans and coding agents alike.\n\n## Background (plan §3.5, §5.7, §13.5, §16.9)\n\n\"Every build should explain, in human-readable text and stable JSON: the selected Three.js version and asset/module hashes; which work was specialized, executed by the general backend, retained in JS, or kept on an exact backend; distinct functional gaps, host/source limitations, and optimization-only barriers, with no conversion of one status into another; estimated structural costs, clearly distinguished from measured timings; precise source spans and reasons for specialization barriers; changes needed to unlock a fast path without weakening behavior; the selected device profile and optional capability variants. For example, a report might explain that a typed array escapes to an unknown plugin, so safe change detection requires a conservative copy; it should identify that escape and the copied byte count. It must not claim a measured improvement before a benchmark runs.\"\n\n## Technical approach\n\n- Report schema (versioned JSON, serde in `f3d-compiler`): header (pin, hashes), ownership table (per module/object region/renderer), islands (admitted with guards and cost estimates; refused with spans/reasons/unlock hints), routes (12a.6), variants (08.3), assets (10.1), retained components and bytes (04.3/13.5), estimates section, measurements section (empty until `f3d bench` attaches results), capability profile.\n- Text renderer with stable ordering; `f3d inspect` runs analysis without codegen.\n- Source maps for generated kernels/JS so spans point to original application lines (§16.9).\n\n## Success criteria\n\n- H1/H2 reports snapshot-tested; the escape example above is reproduced by a fixture; schema validated.\n\n## Test plan\n\n- Unit: report schema validation; snapshot tests for H1/H2 reports (text and JSON); the plan's escape example reproduced by a fixture (typed array escapes to an unknown plugin → conservative copy with byte count and span); source-map resolution tests for generated kernel/JS spans.\n- E2E: `f3d explain` and `f3d inspect` CLI tests over the corpus artifacts; the measurements section is empty until a bench result is attached (asserted).\n- Logging: none beyond the report; schema-version events in verbose mode.\n- Failure artifacts: the report diff on snapshot changes.\n\n## Considerations\n\n- \"Distinguish structural cost estimates from measured timings everywhere they appear\" (AGENTS.md honesty rule 8).\n","acceptance_criteria":"Every build emits demo.f3d-report.json plus text with the enumerated sections; statuses are never converted between categories; estimates are labeled as estimates; every barrier has a span and a reason; f3d explain renders it and f3d inspect produces the analysis-only form for a source without building; no measured-improvement claim appears unless a bench result is attached.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:32.101223Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:08.384560Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-cli","f3d-compiler","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-12a-early-islands-route-analysis-63y.9","depends_on_id":"f3d-12a-early-islands-route-analysis-63y","type":"parent-child","created_at":"2026-09-09T16:46:32.101223Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.9","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.4","type":"blocks","created_at":"2026-09-09T16:48:59.896183Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12a-early-islands-route-analysis-63y.9","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.6","type":"blocks","created_at":"2026-09-09T16:49:00.079814Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12b-generalize-islands-ownership-a3s","title":"F3D-12 (closure): Generalize island extraction, aliases/effects/guards, ownership/materialization, dirty propagation and dynamic topology without losing public functionality","description":"## What this epic delivers\n\nPhase 2's compiler generalization. Plan §18 Phase 2: \"Generalize aliases/effects/guards, ownership/materialization, dirty propagation, dynamic topology, animation and procedural kernels... Exit: held-out applications and dynamic input sequences work without F3D-specific valid-input refusals. Tests prove both successful specialization and conservative exact/general execution.\"\n\n## Why (plan §4.6 last sentence, §14.8, §18 Phase 2, §22)\n\n\"General numeric-loop lifting follows these recognized paths; it must not become a prerequisite for the first useful result.\" Now that the first result exists, generalization must not lose any public functionality: \"Preserve the complete scalar/public API via retained modules where that is best; shared state must remain correct for controls, raycasts, cloning, serialization and exporters immediately after optimized updates.\"\n\n## Children\n\n1. General alias/effect analysis and the §4.6 pattern lowerings.\n2. Ownership/materialization generalization for shared state.\n3. Dynamic topology and structural plan rebuilds.\n4. Held-out corpus and unseen input sequences with deoptimization tests.\n5. Per-pass legality statements, conservative references, counterexamples and ablations.\n\n## Success criteria for the epic\n\nAll children closed; the generalization gate (G4) can be evaluated with these results.\n\n## No-claim boundary\n\nNo \"general JavaScript compilation\" (optional research tier). Unrecognized code stays JavaScript-owned and executable.\n","acceptance_criteria":"The compiler admits islands for the §4.6 pattern table beyond H1/H2 (repeated geometry/material, stable material/lighting, known AnimationMixer paths, procedural addons, TSL graphs, retained DOM controls) under generalized alias/effect analysis; shared state stays correct for controls, raycasts, cloning, serialization and exporters immediately after optimized updates; dynamic insertion/removal/reparenting/layout changes rebuild only affected plans; held-out applications and unseen input sequences work with no F3D-specific valid-input refusals; each optimizer pass has a legality statement, conservative reference, counterexamples and an ablation.","status":"open","priority":1,"issue_type":"epic","created_at":"2026-09-09T16:46:23.329618Z","created_by":"jemanuel","updated_at":"2026-09-09T16:49:02.770614Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","f3d-scene","phase-2","tier-frontier"],"dependencies":[{"issue_id":"f3d-12b-generalize-islands-ownership-a3s","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T16:49:02.770269Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12b-generalize-islands-ownership-a3s","depends_on_id":"f3d-12a-early-islands-route-analysis-63y","type":"blocks","created_at":"2026-09-09T16:49:02.582405Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12b-generalize-islands-ownership-a3s.1","title":"General alias/effect analysis and the section 4.6 pattern lowerings","description":"## Full scope\n\nGeneral alias/effect analysis and the §4.6 pattern lowerings: repeated geometry/material with differing transforms or colors to instance streams; stable material/lighting combinations to prebuilt variants and bindings; known AnimationMixer/track paths to batched interpolation preserving public events; known procedural addons to safe kernels; TSL graphs with known node semantics to WGSL and persistent plans; DOM controls and unusual effectful callbacks retained with boundary synchronization\n\n## Summary\n\nExtend the compiler from \"H1's loop\" to the plan's first-compilation-target table.\n\n## Background (plan §4.6, §4.3, §4.4, §12.3)\n\n| Pattern | Initial lowering |\n|---|---|\n| Many object transforms updated from time and numeric parameters | Bulk CPU Wasm kernel and packed transform upload |\n| Repeated geometry/material with differing transforms or colors | Instance stream or equivalent grouped draw plan |\n| Stable material/lighting combinations | Prebuilt shader variants and bindings |\n| Known AnimationMixer/track paths | Batched interpolation and pose propagation, preserving public events |\n| Known procedural geometry addon | Safe Rust kernel behind the same API contract |\n| TSL graph with known node semantics | WGSL generation and persistent resource/pass plan |\n| DOM controls and unusual effectful callbacks | Retain JavaScript and synchronize at explicit boundaries |\n\n## Technical approach\n\n- Generalize alias analysis to interprocedural flows within the application module graph (bounded context sensitivity), with escape classes: module-local, addon-internal (fingerprinted), unknown.\n- Pattern recognizers keyed on manifest shapes and pinned addon fingerprints; admission contracts per pattern (for example AnimationMixer: mixer/actions/clips not subclassed, no custom `PropertyBinding` overrides, event listeners preserved as ordered effects).\n- Lowerings reuse F3D-08 (instances/variants), F3D-13 (animation kernels), F3D-09-style kernels, F3D-07.5/F3D-16 (TSL). Division of labour with 13.1: this bead delivers the AnimationMixer recognizer, its admission contract and the call-site rewrite hook; the kernel and event semantics are delivered by 13.1 (which depends on this bead).\n- Held-out discipline: recognizers are semantic (shape + fingerprint), never example-path based.\n- Before any recognizer work, write `benchmarks/heldout-v1.toml` (the preregistered held-out application list with the selection date); 12b.4 verifies that date precedes this bead's first change.\n\n## Success criteria\n\n- H3 (animation multiple) and H5 (instancing dynamic) admit their expected islands; H4/H6 admit the stable-material and grouping patterns; synthetic negative fixtures refuse; reports explain.\n\n## Test plan\n\n- Unit: recognizer tests per §4.6 pattern on synthetic applications (repeated geometry/material; stable material/lighting; AnimationMixer paths with and without subclassing; procedural addon by fingerprint; TSL graph; DOM controls) asserting admission or refusal with reasons; interprocedural alias fixtures (module-local, addon-internal, unknown escape).\n- E2E: trace-equivalence runs on H3–H6 and held-out apps via `f3d verify` in the forced-new-backend lane on both browsers; the build reports list admitted islands per pattern.\n- Logging: per-pattern admission counts and refusal reasons with spans; archived under `evidence/12b.1/`.\n- Failure artifacts: the analysis report for the wrongly admitted/refused fixture.\n\n## Considerations\n\n- Effectful callbacks remain ordered barriers unless a stronger transformation is justified (§4.5).\n","acceptance_criteria":"Each pattern in the §4.6 table has a recognizer, an admission contract and a lowering with tests on synthetic applications and on H3–H6; unrecognized code remains JS-owned and executable; every refusal is reported with a span and an unlock hint.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:32.195852Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:05.956865Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","phase-2","tier-frontier"],"dependencies":[{"issue_id":"f3d-12b-generalize-islands-ownership-a3s.1","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T16:49:01.148522Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12b-generalize-islands-ownership-a3s.1","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.3","type":"blocks","created_at":"2026-09-09T16:49:00.964727Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12b-generalize-islands-ownership-a3s.1","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s","type":"parent-child","created_at":"2026-09-09T16:46:32.195852Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12b-generalize-islands-ownership-a3s.2","title":"Ownership/materialization generalization for shared state after optimized updates","description":"## Full scope\n\nOwnership and materialization generalization: shared state stays correct for controls, raycasts, cloning, serialization and exporters immediately after optimized updates; mirrored-epoch regions with explicit publication; escaped typed-array aliases handled by scan/copy with disclosed cost\n\n## Summary\n\nExtend 06.4's guarantees from H1/H2's specific reads to every public read path the generalized islands can affect.\n\n## Background (plan §5.3, §5.10, §18 Phase 2)\n\n\"Shared state must remain correct for controls, raycasts, cloning, serialization and exporters immediately after optimized updates.\"\n\n## Technical approach\n\n- Enumerate read paths per admitted pattern from the manifest (which public methods read which fields); materialization boundaries extended accordingly; where a read is discovered only at runtime (reflection), the guard on the region falls back to JS ownership.\n- Mirrored-epoch regions for animation state (mixer time/weights) and instance data, with `publish()` at the source boundaries.\n- Tests: for each corpus app, inject a read of each kind at random logical frames and compare to upstream.\n\n## Success criteria\n\n- All injected reads match; no region has two writers (05.5 invariant check enabled in tests).\n\n## Test plan\n\n- Unit: read-path enumeration tests from the manifest (which public methods read which fields); mirrored-epoch publication tests; property tests over read-injection schedules with the single-writer invariant check enabled.\n- E2E: `tests/e2e/compiler/read-paths.mjs` on H3–H6 and held-out apps: OrbitControls reads, `Raycaster` over accelerated meshes, `clone()`, `toJSON()`, `GLTFExporter` injected at random logical frames and compared to upstream on both browsers.\n- Logging: materialization and publication events per region `{region, mode, epoch, trigger}`; mismatch events with property path and values; archived under `evidence/12b.2/`.\n- Failure artifacts: serialized object graphs from both sides.\n\n## Considerations\n\n- Do not defer an observable animation update to a convenient frame commit (§5.10).\n","acceptance_criteria":"Any public read path (OrbitControls reading camera state, Raycaster over accelerated meshes, object.clone(), toJSON(), GLTFExporter) observes current values immediately after an optimized update, across all admitted patterns; mirrored regions publish at explicit epochs; escaped-alias regions are JS-owned with the scan bytes in the report; tests cover each read path on H3–H6 and held-out apps.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:32.285287Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:08.732642Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","f3d-scene","phase-2","tier-frontier"],"dependencies":[{"issue_id":"f3d-12b-generalize-islands-ownership-a3s.2","depends_on_id":"f3d-06-packed-transforms-materialization-roa.4","type":"blocks","created_at":"2026-09-09T16:49:01.501472Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12b-generalize-islands-ownership-a3s.2","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s","type":"parent-child","created_at":"2026-09-09T16:46:32.285287Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12b-generalize-islands-ownership-a3s.2","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s.1","type":"blocks","created_at":"2026-09-09T16:49:01.333891Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12b-generalize-islands-ownership-a3s.3","title":"Dynamic topology and structural plan rebuilds","description":"## Full scope\n\nDynamic topology and structural plan rebuilds: insertion, removal, reparenting and resource-layout changes rebuild only the affected structural plan segments (buckets, bundles, dependency closures); dirty propagation generalized to arbitrary scene edits at runtime\n\n## Summary\n\nThe compiled plan must survive the application changing its own structure.\n\n## Background (plan §6.2 last paragraph, §18 Phase 2)\n\n\"Dynamic insertion, removal, reparenting, and resource-layout changes rebuild only the affected structural plan.\"\n\n## Technical approach\n\n- Structural event stream (06.3) carries insert/remove/reparent, geometry/material replacement, attribute-layout change, light/camera/render-target addition; each plan segment (bucket 08.1, bundle 08.4, dependency closure 06.3, variant bindings 08.3, pass-graph resources 07.2) registers which structural facts it depends on.\n- Invalidation: an event marks only the segments whose registered facts changed; rebuild runs at the next boundary in source order; counters record segments rebuilt vs total.\n- Reentrancy: edits made inside render callbacks are applied per 07.4's context rules (visible to the current logical render exactly as upstream).\n- Latency: rebuild durations feed the interaction-tail metrics (plan §16.9).\n\nPrior notes retained:\n- Structural event stream (06.3) drives segment invalidation: buckets (08.1), bundles (08.4), dependency closures (06.3), material variant bindings (08.3), pass graph resources (07.2); each with a locality test.\n- Rebuild cost measured and reported in the interaction-tail metrics (§16.9).\n\n## Success criteria\n\n- Locality counters; trace equality on edit scripts; rebuild latencies recorded.\n\n## Test plan\n\n- Unit: property tests over random edit sequences (add/remove/reparent, geometry/material replacement, attribute layout change, light/render-target addition) asserting locality counters (only affected segments rebuilt) and correctness against a Node oracle.\n- E2E: `tests/e2e/compiler/topology.mjs` on H1/H3/H5 and held-out apps with scripted edits on both browsers; trace equality; rebuild latencies recorded for the interaction-tail report.\n- Logging: structural events and rebuilt segments `{event, segments_rebuilt, ms}`; archived under `evidence/12b.3/`.\n- Failure artifacts: the plan diff before/after the edit.\n\n## Considerations\n\n- Edits inside callbacks during render obey reentrancy rules (07.4).\n","acceptance_criteria":"Runtime scene edits (add/remove/reparent objects, replace geometries/materials, change attribute layouts, add lights, add render targets) invalidate and rebuild only affected plan segments with counters proving locality; correctness is preserved (trace equality) on scripted edit sequences over H1/H3/H5 and held-out apps.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:32.387218Z","created_by":"jemanuel","updated_at":"2026-09-09T17:31:48.572860Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","f3d-graph","f3d-scene","phase-2","tier-solid"],"dependencies":[{"issue_id":"f3d-12b-generalize-islands-ownership-a3s.3","depends_on_id":"f3d-06-packed-transforms-materialization-roa.3","type":"blocks","created_at":"2026-09-09T16:49:01.861133Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12b-generalize-islands-ownership-a3s.3","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s","type":"parent-child","created_at":"2026-09-09T16:46:32.387218Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12b-generalize-islands-ownership-a3s.3","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s.1","type":"blocks","created_at":"2026-09-09T16:49:01.685205Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12b-generalize-islands-ownership-a3s.4","title":"Held-out application corpus and unseen input sequences with deoptimization tests","description":"## Full scope\n\nHeld-out application corpus and unseen input sequences: a set of applications not used for tuning runs through the same compiler with no example-path selection or captured-output replay; deoptimization after assumption changes and dynamic input sequences are tested; no F3D-specific valid-input refusals\n\n## Summary\n\nProof that the compiler generalizes rather than memorizes.\n\n## Background (plan §14.8, §16.7 \"Generalization gate\", §18 Phase 2 exit, §20 \"Full-suite success hides untested missing features\")\n\n\"A held-out source corpus and unseen input/control sequences must exercise the same compiler; selecting kernels by example path or replaying captured outputs is disallowed.\" Phase 2 exit: \"held-out applications and dynamic input sequences work without F3D-specific valid-input refusals.\"\n\n## Technical approach\n\n- Preregistration check: `benchmarks/heldout-v1.toml` (written by 12b.1 before any recognizer work; verify that its selection date precedes 12b.1's first change; the list is immutable thereafter) listing upstream examples outside H1–H8 and outside any tuning set, chosen across families (an animation example, an instancing example, a postprocessing example, a loader example, a controls-heavy example, a CSS/SVG example), with the selection date recorded before generalization work touches them.\n- Unseen inputs: input scripts generated from a different seed family than the tuning scripts (03.1).\n- Deoptimization scenarios injected by the harness at random logical frames: replace a method on a recognized class, alias an attribute array and write through the alias, change a material's cache key; the candidate must fall back per 12a.4 with trace equality.\n- Every application is built by the same compiler with no example-specific tables (12b.1 recognizers are semantic); `f3d verify` on both browsers; any valid-input refusal in the report is a blocking defect.\n\nPrior notes retained:\n- Preregister the held-out list in `benchmarks/heldout-v1.toml` before generalization work uses them (record the date); include varied families (an animation example, an instancing example, a postprocessing example, a loader example, a controls-heavy example, a CSS/SVG example).\n- Unseen input scripts generated from a different seed family than the tuning scripts.\n- Deopt scenarios injected via the harness at random frames.\n\n## Success criteria\n\n- All held-out apps pass equivalence; deopt scenarios match upstream; zero refusals.\n\n## Test plan\n\n- Unit: none new; the held-out list file is schema-validated and its preregistration date checked.\n- E2E: build and `f3d verify` every preregistered held-out application with unseen input scripts (different seed family) on both browsers; deopt scenarios injected at random frames (method replacement, buffer aliasing, material key change); zero valid-input refusals asserted from the reports.\n- Logging: per-app summary `{app, islands_admitted, refusals, verify_result, deopts_triggered}`; archived under `evidence/12b.4/`.\n- Failure artifacts: verify artifacts and the refusal list.\n\n## Considerations\n\n- Held-out performance is measured in G4/F3D-23, not here; this bead is about functionality and generalization.\n","acceptance_criteria":"At least a preregistered set of held-out upstream examples (chosen from the pinned index outside H1–H8 and outside the tuning set) build and pass equivalence with unseen input scripts; deopt tests mutate assumptions at runtime (replace a method, alias a buffer, change a material key) and the candidate falls back correctly; any valid-input refusal is a blocking defect.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:32.479414Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:09.864848Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","phase-2","testing","tier-frontier"],"dependencies":[{"issue_id":"f3d-12b-generalize-islands-ownership-a3s.4","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s","type":"parent-child","created_at":"2026-09-09T16:46:32.479414Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12b-generalize-islands-ownership-a3s.4","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s.2","type":"blocks","created_at":"2026-09-09T16:49:02.042364Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12b-generalize-islands-ownership-a3s.4","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s.3","type":"blocks","created_at":"2026-09-09T16:49:02.223513Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-12b-generalize-islands-ownership-a3s.5","title":"Per-optimizer-pass legality statements, conservative references, counterexamples, ablation toggles","description":"## Full scope\n\nPer-optimizer-pass evidence: a legality statement, a conservative executable reference, counterexample tests and an ablation toggle for every pass (update compilation, dirty tracking, program merging, instancing, bundles, upload coalescing, pass/resource optimization)\n\n## Summary\n\nThe plan's evidence discipline for optimizations, applied uniformly.\n\n## Background (plan §14.8, §16.8)\n\n\"Each optimizer pass needs a small legality statement, a conservative executable reference, counterexample tests, and an ablation measurement.\" \"Use a few strong invariants rather than a large bureaucracy: one owner per mutable epoch, no stale publication, preserved observation order, legal resource usage, and no optimization result accepted before equivalence passes.\"\n\n## Technical approach\n\n- Registry: a static table in `f3d-compiler` (`passes.rs`) listing every optimizer pass with: name, legality-statement doc anchor, the conservative-path switch (compile-time flag or runtime toggle), counterexample test names, ablation entry name; a test asserts each entry resolves (doc anchor exists, switch is live, tests exist).\n- Legality statements: one paragraph per pass in the crate docs stating the invariant it preserves (one owner per mutable epoch; no stale publication; preserved observation order; legal resource usage) and its preconditions.\n- Counterexamples: each pass has at least one test that fails when its legality rule is bypassed (mutation run in CI-free local lane).\n- Ablation: `f3d bench --ablate` (20.5) iterates the registry.\n\nPrior notes retained:\n- A registry of passes with metadata (name, legality doc link, conservative-path switch, counterexample test names); the ablation harness (F3D-20.5) iterates it.\n- Keep this as a small table plus conventions, not a framework.\n\n## Success criteria\n\n- Registry complete for all existing passes; CI-free local check that every pass has all four items.\n\n## Test plan\n\n- Unit: a registry-consistency test asserting every pass has a legality doc link, a conservative-path switch, at least one counterexample test name that resolves, and an ablation entry; each counterexample test fails when its pass's legality rule is bypassed (mutation runs).\n- E2E: the ablation harness (20.5) iterates the registry once on a small scene to prove every switch is live.\n- Logging: registry dump and mutation-run results `{pass, counterexample, detected: bool}`; archived under `evidence/12b.5/`.\n- Failure artifacts: the undetected mutation's diff.\n\n## Considerations\n\n- \"The reference is available for development comparisons; production does not double-render every frame to prove itself continuously\" (§14.8).\n","acceptance_criteria":"Each optimizer pass is documented with its legality statement in the crate docs, has a switch that routes to its conservative reference, has at least one counterexample test that fails when the legality rule is violated, and is measurable in isolation by the ablation harness.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:32.581508Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:22.411088Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","f3d-conformance","phase-2","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-12b-generalize-islands-ownership-a3s.5","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s","type":"parent-child","created_at":"2026-09-09T16:46:32.581508Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-12b-generalize-islands-ownership-a3s.5","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s.1","type":"blocks","created_at":"2026-09-09T16:49:02.408358Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-13-animation-math-geometry-closure-3cr","title":"F3D-13: Complete animation/math/geometry scalar behavior and bulk kernels","description":"## What this epic delivers\n\nClosure of the CPU-side feature families and the animation gain path. Plan §19 F3D-13 depends on 06, 11 and 12 (early); acceptance: \"All contracts via real owners; event/IK/retarget/query/export combinations and held-out gains\".\n\n## Why (plan §5.12 rows Math, Scene graph, Geometry, Curves, Animation; §7.7; §15.2 H3/H5)\n\n\"Complete retained contract with batched Rust kernels; synchronous state and callback/event order; not just sampled poses.\" \"Retained reference-compatible scalar surface; safe Rust bulk kernels when proven; edge cases, coercions, mutability, return identity.\"\n\n## Children\n\n1. Animation contract and batched kernels.\n2. Skeleton/skinning/morph CPU path, retargeting/IK/character utilities, dual-quaternion as opt-in.\n3. Math family closure.\n4. Geometry and attributes family closure.\n5. Curves/shapes/modifiers/CSG closure.\n6. Scene graph and core data family closure.\n7. Procedural geometry kernels beyond marching cubes and the GPU-generation legality rule.\n8. H3/H5 end-to-end routes and held-out measurement.\n\n## Success criteria for the epic\n\nAll children closed; the manifest shows every record in these families with an owner and positive behavioral test reference; no `unclassified`/`unimplemented` in these families.\n\n## No-claim boundary\n\nRetained modules count as compositional compatibility, not Rust feature coverage (§14.3).\n","acceptance_criteria":"Every animation, math, scene-graph, geometry/attribute and curve/shape/modifier contract in the manifest has a real production owner (retained reference-compatible scalar surface or proven Rust kernel) with edge cases, coercions, mutability and return identity tested; batched animation kernels preserve synchronous state and callback/event order; H3 and H5 have measured end-to-end routes on held-out inputs.","status":"open","priority":1,"issue_type":"epic","created_at":"2026-09-09T16:46:23.410933Z","created_by":"jemanuel","updated_at":"2026-09-09T16:49:06.424152Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["animation","f3d-math","f3d-scene","h3","h5","phase-2","tier-frontier","tier-solid"],"dependencies":[{"issue_id":"f3d-13-animation-math-geometry-closure-3cr","depends_on_id":"f3d-06-packed-transforms-materialization-roa","type":"blocks","created_at":"2026-09-09T16:49:06.063599Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T16:49:06.250246Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.4","type":"blocks","created_at":"2026-09-09T16:49:06.423839Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-13-animation-math-geometry-closure-3cr.1","title":"Animation contract and batched kernels (events, mixer state, synchronous observation)","description":"## Full scope\n\nAnimation contract and batched kernels: every clip/action/mixer/group/track/interpolant, property binding and mixing, blending/additive/crossfade/loop/events; batched interpolation and pose propagation preserving interpolation type, track ordering, event times, loop events, morph weights and observable mixer state; AnimationMixer.update results visible synchronously; pose caching only when time/weights/skeleton agree; invariant data shared without forcing shared poses\n\n## Summary\n\nAnimation as ordered effects with a fast interior.\n\n## Background (plan §5.12 \"Animation and deformation\", §7.7, §5.10, §4.6 row 4)\n\n\"For recognized animation paths, lower interpolation, pose propagation, and buffer packing together. Preserve interpolation type, track ordering, additive/blended behavior, event times, loop events, morph weights, and observable mixer state. Animation events remain ordered effects, not GPU-only state. Cache pose work only when source time/weights and skeleton state agree. Share invariant data across instances without forcing all instances to share a pose.\" \"`AnimationMixer.update()`... can synchronously observe results before `renderer.render()` is called. Finish or materialize the results at those original boundaries.\"\n\n## Technical approach\n\n- Retain upstream animation classes as the public contract; the compiler recognizes the mixer update call and the bound property set (12b.1); the kernel evaluates tracks (linear/step/cubic interpolants with upstream's exact math incl. `CubicInterpolant` and quaternion slerp flavors), applies blending/additive/weights with upstream's `PropertyMixer` accumulation order, writes results to packed targets (transforms, morph weights) and materializes to public objects at `update()` return.\n- Events: dispatched at their upstream position within the action iteration, never deferred to after the batch. Upstream `AnimationMixer.update` dispatches `loop`/`finished` synchronously inside the per-action loop, and a listener may mutate actions not yet processed in that same `update()` (`action.stop()`, `crossFadeTo`), which the remaining loop then observes. The batched kernel therefore evaluates ahead only across actions whose event listeners are provably absent (no `loop`/`finished` listeners on the mixer at update entry); if any listener exists, the kernel evaluates action-by-action, yields to the listener at the exact action index and re-reads mixer/action state afterwards, or the update falls back to the retained per-action path. A counterexample test registers a `loop` listener that stops a later action and asserts identical results.\n- Unbindable/unusual bindings (custom property paths, non-numeric targets) stay on the retained path per binding; tracks with overridden `createInterpolant`, custom interpolant classes, `InterpolateSmooth` or `InterpolantFactoryMethod*` subclasses stay on the retained path per track.\n- Pose cache keyed on (clip, time, weights, skeleton generation).\n\n## Success criteria\n\n- Upstream animation unit tests pass (routed); differential traces on H3 and animation examples show identical poses, events and mixer state at every update; kernel path attribution shows batched evaluation for recognized cases.\n\n## Test plan\n\n- Unit: property tests over track/interpolant types (linear/step/cubic, quaternion slerp flavors), blending/additive/weights with upstream's `PropertyMixer` accumulation order, loop modes and event times against a Node oracle; pose-cache key tests; unusual-binding fixtures staying on the retained path.\n- E2E: `tests/e2e/animation/mixer.mjs` on H3 and animation examples on both browsers: identical poses, events (`loop`, `finished`) and mixer state at every `AnimationMixer.update` boundary; mapped upstream animation unit tests green; kernel attribution shows batched evaluation for recognized cases.\n- Logging: per-update `metric` events `{tracks_evaluated, events_emitted, batched: bool}`; mismatch events with track name, time and both values; archived under `evidence/13.1/`.\n- Failure artifacts: pose dumps and event traces from both sides.\n\n## Considerations\n\n- Retargeting/IK are 13.2; export after animation is 19.4.\n","acceptance_criteria":"All animation-family manifest records have owners: retained upstream classes for the public contract, with recognized mixer/track paths lowered to batched Rust kernels whose outputs and event sequences equal upstream at every AnimationMixer.update boundary; property bindings to arbitrary targets remain correct; events ('loop', 'finished') fire in upstream order and time; H3's mixers are lowered.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:32.680173Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:05.731132Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["animation","f3d-math","f3d-scene","h3","phase-2","tier-frontier"],"dependencies":[{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.1","depends_on_id":"f3d-06-packed-transforms-materialization-roa.4","type":"blocks","created_at":"2026-09-09T16:49:02.947569Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.1","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T16:49:03.312048Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.1","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.5","type":"blocks","created_at":"2026-09-09T16:49:03.119103Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.1","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s.1","type":"blocks","created_at":"2026-09-09T17:44:05.730784Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.1","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr","type":"parent-child","created_at":"2026-09-09T16:46:32.680173Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-13-animation-math-geometry-closure-3cr.2","title":"Skeleton/skinning/morph CPU path, retargeting/IK utilities, dual-quaternion as opt-in","description":"## Full scope\n\nSkeleton, skinning and morph CPU path plus retargeting/IK/character utilities: matrix-based skinning by default, bone matrices/bind matrices/skeleton textures materialized as upstream, morph target influences and relative morphs, SkeletonUtils retargeting, CCDIKSolver and related helpers retained; dual-quaternion skinning as a separate opt-in path never substituted by default\n\n## Summary\n\nDeformation state on the CPU side with upstream's exact math and public observations.\n\n## Background (plan §7.1 \"Skeletal deformation\", §7.2, §5.12 Animation row, §14.9 combination \"skinning+morphs+shadows+export\")\n\n\"Skeletal deformation: the reference application's matrix-based skinning by default.\" \"Dual-quaternion skinning similarly belongs in a separate opt-in path. It does not in general produce the same deformation as linear matrix blending, so substituting it by default would violate the core purpose of this project.\"\n\n## Technical approach\n\n- Bone hierarchy inside the packed transforms (06.2); `Skeleton.update()` semantics (bone inverses, `boneMatrices` array identity, `boneTexture` layout/size rules as pinned) reproduced with public array identity preserved.\n- Morph targets: `morphTargetInfluences` arrays, relative morphs, morph texture packing as pinned.\n- Retain `SkeletonUtils`, `CCDIKSolver`, `MMD` helpers as retained JS with tests.\n- Opt-in DQ path with its own tests and a label in the report.\n\n## Success criteria\n\n- Upstream tests pass; differential pose tests; combination test skinning+morphs+shadows+export (with F3D-14/19) scheduled in 18b.2.\n\n## Test plan\n\n- Unit: bone-matrix computation tests bit-for-bit vs upstream for scripted poses; `boneMatrices`/`boneTexture` identity and layout tests; morph influence and relative-morph tests; DQ path tests behind the opt-in flag only.\n- E2E: `tests/e2e/animation/skinning.mjs` on skinned examples and H7's CPU-side skeleton state on both browsers; retargeting (`SkeletonUtils`) and IK (`CCDIKSolver`) examples through the routed build; mapped upstream tests green.\n- Logging: per-update `metric` events `{bones, morphs, skeleton_generation}`; mismatch events with bone name and matrices; archived under `evidence/13.2/`.\n- Failure artifacts: bone matrix dumps.\n\n## Considerations\n\n- Skeleton generation participates in pose-cache keys (13.1) and in H7's per-instance data (16.5).\n","acceptance_criteria":"Skeleton/skin/morph manifest records have owners and positive tests; bone matrix computation matches upstream bit-for-bit for scripted poses; morph weights/relative morphs match; retargeting/IK utilities work through the routed build; a dual-quaternion path exists only behind an explicit opt-in and is excluded from equivalence scoring by default.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:32.777611Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:09.602127Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["animation","f3d-math","f3d-scene","phase-2","tier-solid"],"dependencies":[{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.2","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr","type":"parent-child","created_at":"2026-09-09T16:46:32.777611Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.2","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr.1","type":"blocks","created_at":"2026-09-09T16:49:03.504592Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-13-animation-math-geometry-closure-3cr.3","title":"Math family closure (every math and addon numeric utility owned and tested)","description":"## Full scope\n\nMath family closure: every vector, matrix, quaternion, Euler, color/color-management, bounds, ray, plane, triangle, interpolant, coordinate and MathUtils operation; addon OBB/capsule/noise/hull/octree/sampling/LUT utilities; edge cases, coercions, mutability and return identity\n\n## Summary\n\nClose the scalar math surface completely, mostly by retaining and testing.\n\n## Background (plan §5.12 \"Math and numeric helpers\", §14.9)\n\n\"Retained reference-compatible scalar surface; safe Rust bulk kernels when proven; edge cases, coercions, mutability, return identity.\" \"Every existing callable/option/behavior family... an executable positive case... Constructor existence, identical function names, or merely retaining source bytes is insufficient.\"\n\n## Technical approach\n\n- Retained upstream math modules remain the public owners; where 06.1 kernels exist they are used only inside islands.\n- Generate the family test list from the manifest; for each record write/derive a positive test (many come from upstream unit tests; fill gaps for untested methods, coercion inputs like strings/undefined, NaN handling, chained `return this`).\n- Addon utilities (`OBB`, `Capsule`, `ImprovedNoise`/`SimplexNoise`, `ConvexHull`, `Octree`, `MeshSurfaceSampler`, `LUT*` loaders' math parts) retained and tested.\n\n## Success criteria\n\n- Manifest status for the family: all records `retained`+tested or `new-rust`+tested; upstream math tests green.\n\n## Test plan\n\n- Unit: the generated family suite (one positive test per manifest record with invalid-input, mutation, serialization and `return this` identity cases); mapped upstream math unit tests; kernel-vs-retained differential tests where 06.1 kernels are used.\n- E2E: browser run of the family suite on both browsers (some math behavior differs by engine for transcendentals; 06.1's policy applies).\n- Logging: per-record status updates `{record, owner, test, result}` feeding the manifest; archived under `evidence/13.3/`.\n- Failure artifacts: the failing record's inputs and both outputs.\n\n## Considerations\n\n- No coverage percentage headline; per-family counts only.\n","acceptance_criteria":"Every math-family manifest record has an owner (retained reference-compatible scalar surface by default; Rust kernel where proven and used) and a positive behavioral test including invalid-input, mutation, serialization and identity cases; return identity (methods returning this) and mutability semantics match; upstream math unit tests pass through the routed build.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:46:32.869232Z","created_by":"jemanuel","updated_at":"2026-09-09T17:15:24.262369Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-math","phase-2","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.3","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:49:03.855670Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.3","depends_on_id":"f3d-06-packed-transforms-materialization-roa.1","type":"blocks","created_at":"2026-09-09T16:49:03.670714Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.3","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:24.262013Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.3","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr","type":"parent-child","created_at":"2026-09-09T16:46:32.869232Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-13-animation-math-geometry-closure-3cr.4","title":"Geometry and attributes family closure (incl. GLBufferAttribute on exact GL)","description":"## Full scope\n\nGeometry and attributes family closure: every core/addon geometry class, indexed/non-indexed/interleaved/instanced/normalized/integer attributes, groups and draw ranges, bounds/normals/tangents computation, morph attributes, buffer update/copy/resize behavior; GLBufferAttribute using a real GL buffer on the exact route\n\n## Summary\n\nClose the geometry surface with the native data path and honest exact-route handling.\n\n## Background (plan §5.12 \"Geometry and attributes\", §5.8, §14.9 \"GLBufferAttribute behavior\")\n\n\"Native data path plus retained constructors/utilities; actual GLBufferAttribute use on exact GL route, not a dummy buffer.\"\n\n## Technical approach\n\n- All `*Geometry` constructors retained (they are CPU generators) with their outputs flowing into the attribute system (06.6); every attribute kind mapped to layout rules (normalized/integer interpretation, interleaved strides) in the vertex layout keys (08.1).\n- `computeVertexNormals`/`computeTangents`/bounds retained or kernel-backed with differential tests.\n- `GLBufferAttribute`: only meaningful with a real GL context; the exact route must consume the user's buffer; the new backend reports it as an intrinsic native contract for that geometry (connected group, 04.4).\n\n## Success criteria\n\n- Family suite green; GLBufferAttribute test with a real GL buffer on the exact lane.\n\n## Test plan\n\n- Unit: generated family suite per geometry/attribute record (constructors, attribute kinds, groups/draw ranges, bounds/normals/tangents, morph attributes, update/copy/resize identity rules) with a Node oracle; vertex-layout key tests for every attribute kind.\n- E2E: browser family suite in the functional and forced lanes on both browsers; `GLBufferAttribute` test with a real user-created `WebGLBuffer` in the exact lane.\n- Logging: per-record status updates and layout-key dumps; archived under `evidence/13.4/`.\n- Failure artifacts: attribute array diffs.\n\n## Considerations\n\n- Resize semantics (new array identity) and `copy`/`clone` identity rules are part of the positive tests.\n","acceptance_criteria":"Every geometry/attribute manifest record has an owner and positive tests; the native data path (packed replicas + attribute histories) covers all attribute kinds; GLBufferAttribute works with a user-provided WebGLBuffer on the exact GL route and is never given a dummy buffer; upstream geometry tests pass.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:46:32.965830Z","created_by":"jemanuel","updated_at":"2026-09-09T17:15:24.484679Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-gpu","f3d-scene","phase-2","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.4","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.3","type":"blocks","created_at":"2026-09-09T16:49:04.203173Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.4","depends_on_id":"f3d-06-packed-transforms-materialization-roa.6","type":"blocks","created_at":"2026-09-09T16:49:04.032260Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.4","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:24.484361Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.4","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr","type":"parent-child","created_at":"2026-09-09T16:46:32.965830Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-13-animation-math-geometry-closure-3cr.5","title":"Curves, shapes and modifiers closure (NURBS, extrusion/text, modifiers, CSG)","description":"## Full scope\n\nCurves, shapes and modifiers closure: all paths/curves, NURBS, shape holes/triangulation, extrusion/text/fonts, geometry generators, subdivision/simplification/tessellation/curve modifiers and official CSG integrations; complete algorithms retained first, measured kernels later; topology, attributes and synchronous results preserved\n\n## Summary\n\nRetain the complete algorithm inventory; accelerate only what measurement justifies.\n\n## Background (plan §5.12 \"Curves, shapes and modifiers\")\n\n\"Retain complete algorithms first; accelerate measured kernels; preserve topology, attributes and synchronous results.\"\n\n## Technical approach\n\n- Retained modules through the facade for every record in the family (from the census; note `SubdivisionModifier` is not in r186, `EdgeSplitModifier` is, and every other modifier the census lists is included): paths/curves (`Curve`, `CatmullRomCurve3`, Bezier variants, `NURBSCurve`/`NURBSSurface`/`NURBSVolume`, `ParametricGeometry`), shapes (`Shape`, `ShapePath`, `ShapeUtils` triangulation, holes), extrusion/text (`ExtrudeGeometry`, `TextGeometry` with `FontLoader`), generators (lathe/tube/parametric), modifiers (`SimplifyModifier`, `TessellateModifier`, `CurveModifier`, subdivision), and the official CSG example integrations (retained with their external dependency).\n- Generated positive tests per record with geometry snapshots vs a Node oracle: topology, attribute order, bounds, groups; degenerate inputs (self-intersecting shapes, holes touching edges, zero-length curves, empty fonts) as upstream handles them.\n- Kernel candidates (triangulation, extrusion) recorded with the bit-identical-topology requirement; none is enabled without the differential test.\n\nPrior notes retained:\n- Retained modules with generated positive tests (including degenerate shapes, holes, self-intersections as upstream handles them, font glyph edge cases).\n- Optional kernel candidates recorded (triangulation, extrusion) with a rule: bit-identical topology required.\n\n## Success criteria\n\n- Family suite green; manifest updated.\n\n## Test plan\n\n- Unit: generated family suite (paths/curves/NURBS/shape holes/triangulation/extrusion/text with fonts/generators/modifiers/CSG integrations) with geometry snapshots vs a Node oracle, including degenerate shapes, holes and self-intersections as upstream handles them; kernel candidates require bit-identical topology tests.\n- E2E: browser family suite on both browsers; relevant upstream examples through the routed build.\n- Logging: per-record status updates; topology/attribute-order diffs on mismatch; archived under `evidence/13.5/`.\n- Failure artifacts: geometry dumps.\n\n## Considerations\n\n- Third-party CSG/engine integrations keep their versions and semantics (§5.14).\n","acceptance_criteria":"Every curve/shape/modifier manifest record (including NURBS utilities, ShapeUtils triangulation, ExtrudeGeometry/TextGeometry with FontLoader fonts, EdgeSplitModifier, SimplifyModifier, TessellateModifier, CurveModifier, official CSG example integrations) has a retained owner with positive tests; any later kernel acceleration keeps identical topology/attribute order and synchronous results.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:46:33.058039Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:08.092077Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-scene","phase-2","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.5","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:24.695069Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.5","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr","type":"parent-child","created_at":"2026-09-09T16:46:33.058039Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.5","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr.4","type":"blocks","created_at":"2026-09-09T16:49:04.399541Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-13-animation-math-geometry-closure-3cr.6","title":"Scene graph and core data family closure (JSON, disposal/reuse, subclass and reentry)","description":"## Full scope\n\nScene graph and core data family closure: Object3D/Scene/Group and every renderable object class with its per-instance data contract (Mesh, SkinnedMesh, InstancedMesh, BatchedMesh, LOD, Sprite, Points, Line/LineSegments/LineLoop, Bone), hierarchy/layers/visibility, EventDispatcher events, cloning, JSON serialization/deserialization (ObjectLoader), userData, manual matrix flags, traversal helpers, disposal/reuse, CPU and GPU resource observations, subclass and reentry behavior\n\n## Summary\n\nClose the object model with versioned packed replicas behind the JS semantic objects.\n\n## Background (plan §5.12 \"Scene graph and core data\", §5.9, §5.2)\n\n\"JS semantic objects with versioned packed replicas; include subclass and reentry behavior.\" \"Disposal releases a residency, not necessarily the public object... A legal reused material, texture, or geometry receives fresh backend residency without changing its existing JavaScript identity.\"\n\n## Technical approach\n\n- Positive tests generated per record; residency tests: dispose then reuse a geometry/material/texture and assert fresh GPU residency with the same JS object; renderer shutdown vs user disposal vs cache eviction vs device loss distinguished (with F3D-07.1 states).\n- Traversal helpers (`traverse`, `traverseVisible`, `getObjectByName`) retained and covered.\n- Renderable object classes and their data contracts: `InstancedMesh` (`instanceMatrix`/`instanceColor`, `setMatrixAt`/`getMatrixAt`, morph instance data, `count`), `BatchedMesh` (`addGeometry`/`addInstance`, `setMatrixAt`/`setColorAt`, per-instance visibility, sorting, `optimize`), `LOD` (`update`, levels), `Sprite`, `Points`, `Line`/`LineSegments`/`LineLoop`, `Bone`, `SkinnedMesh`; positive tests for their mutation/`needsUpdate`/count/visibility behavior and the identity of their attribute arrays.\n\n## Success criteria\n\n- Family suite green; residency/reuse tests pass in the forced lane.\n\n## Test plan\n\n- Unit: generated family suite (Object3D/Scene/Group, layers/visibility, events, cloning, JSON via ObjectLoader round trips, userData, manual matrix flags, traversal, disposal/reuse, subclass and reentry); residency tests (dispose then reuse a geometry/material/texture keeps JS identity and gets fresh residency; shutdown vs user disposal vs cache eviction vs device loss distinguished).\n- E2E: browser family suite in the functional and forced lanes on both browsers; mapped upstream core tests green.\n- Logging: per-record status updates; residency events `{object, event, generation}`; archived under `evidence/13.6/`.\n- Failure artifacts: JSON round-trip diffs.\n\n## Considerations\n\n- Automatic deduplication must not merge observable ownership or events (§5.9).\n","acceptance_criteria":"Every scene-graph/core manifest record has an owner and positive tests including subclass overrides and reentrant use; JSON round trips through ObjectLoader match upstream; disposal releases residencies without changing public identity and legal reuse works; upstream core tests pass.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:46:33.150993Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:08.268870Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-scene","phase-2","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.6","depends_on_id":"f3d-06-packed-transforms-materialization-roa.4","type":"blocks","created_at":"2026-09-09T16:49:04.599702Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.6","depends_on_id":"f3d-06-packed-transforms-materialization-roa.7","type":"blocks","created_at":"2026-09-09T16:49:04.775674Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.6","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:24.907503Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.6","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr","type":"parent-child","created_at":"2026-09-09T16:46:33.150993Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-13-animation-math-geometry-closure-3cr.7","title":"Procedural geometry kernels beyond marching cubes and the GPU-generation legality rule","description":"## Full scope\n\nProcedural geometry kernels beyond marching cubes and the GPU-generation legality rule: recognized addon generators lowered to safe Rust with identical outputs; GPU generation permitted only where output order, precision, topology, bounds and synchronous observations still satisfy the contract; physics-engine state never altered by rendering optimization\n\n## Summary\n\nExtend the H2 approach to other recognized generators, with the GPU rule written down.\n\n## Background (plan §7.7)\n\n\"Start procedural geometry with known addon implementations such as marching cubes... GPU generation is valuable only where output order, precision, topology, bounds, and synchronous observations can still satisfy the contract. Rendering optimization must not change a physics engine's authoritative time step, solver iterations, simulation precision, or state.\"\n\n## Technical approach\n\n- Inventory the procedural generators reached by the corpus and held-out applications (from 01.6/12b.4 closure records): noise-based terrain/height fields, water/sea surface updates, CPU particle emitters, lathe/parametric updates in animation loops, and any addon `update()` that rebuilds geometry per frame.\n- For each recognized (pinned-fingerprint) generator: port to a safe Rust kernel operating on packed attribute arrays (09.1 pattern), reproduce the JS expression order and f32 storage boundaries for bit identity, keep synchronous observations; unrecognized or modified generators stay retained.\n- GPU-generation legality rule (documented in `f3d-compiler` docs and enforced by a test): a generator may move to GPU only if its output is never read on the CPU (no attribute reads, no bounds/raycast dependency), output order/topology/precision are preserved, and bounds are computed conservatively on the CPU or by an equivalent kernel.\n- Physics invariance: kernels never write wrapper/engine state; a test runs a physics example with and without the kernels and compares engine state hashes.\n\nPrior notes retained:\n- Inventory generators reached by the corpus/held-out apps (for example terrain noise, water surfaces, particle emitters on CPU); lower the recognized ones; keep the rest retained.\n- GPU legality rule document + tests (a generator whose output is read on the CPU stays CPU).\n\n## Success criteria\n\n- Lowered generators bit-identical; rule enforced.\n\n## Test plan\n\n- Unit: per-generator differential tests vs the retained addon (bit-identical outputs); GPU-legality rule tests (a generator whose output is read on the CPU is refused GPU generation); a physics-state invariance test (kernel runs leave wrapper state untouched).\n- E2E: examples using the lowered generators through the routed build on both browsers with trace equality.\n- Logging: per-generator `{generator, lowered|retained, reason}` and output diffs on failure; archived under `evidence/13.7/`.\n- Failure artifacts: array diffs.\n\n## Considerations\n\n- Keep within budget; do not chase every addon generator if unused by any corpus/held-out app (record the gap as retained, tested).\n","acceptance_criteria":"At least the procedural generators used by the corpus and held-out apps beyond MarchingCubes have kernel lowerings with bit-identical outputs, or are explicitly retained with reasons; a written legality rule governs GPU generation and is enforced by tests; no kernel modifies a physics wrapper's authoritative state.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:46:33.251967Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:24.166731Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","f3d-scene","phase-2","tier-frontier"],"dependencies":[{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.7","depends_on_id":"f3d-09-marching-cubes-kernel-fb3.1","type":"blocks","created_at":"2026-09-09T16:49:04.953504Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.7","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s.1","type":"blocks","created_at":"2026-09-09T16:49:05.143691Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.7","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr","type":"parent-child","created_at":"2026-09-09T16:46:33.251967Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-13-animation-math-geometry-closure-3cr.8","title":"H3 and H5 end-to-end routes and measured throughput","description":"## Full scope\n\nH3 (webgl_animation_multiple) and H5 (webgl_instancing_dynamic) end-to-end routes: automatic transformation, equivalence, and measured completed-frame throughput under the frozen protocol on available strata, published as held-out-style results\n\n## Summary\n\nTwo more headline cells, measured the same way as H1/H2.\n\n## Background (plan §15.2 H3/H5, §16.8)\n\nH3: \"Batched animation, interpolation, transforms, shared invariant data.\" H5: \"Update-kernel compilation and upload efficiency rather than naive instancing claims.\"\n\n## Technical approach\n\n- Build H3 (`webgl_animation_multiple`) and H5 (`webgl_instancing_dynamic`) with `f3d build`; confirm the reports show the intended islands (H3: recognized mixer/track paths lowered to batched kernels with events preserved; H5: the per-instance update loop lowered to a kernel with packed instance-matrix uploads following 06.6's request semantics) and explain every refusal.\n- Extend the frozen references (03.3) with R0/R1/R2 for H3 and H5 under the bounded recipe; calibrate and freeze before candidate runs.\n- `f3d verify` on both browsers over every control; then `f3d bench` paired cells on the reserved M5 and phone under protocol v1; ablations: animation batching (H3), update kernel and upload coalescing (H5).\n- Publish per-stratum results documents with intervals and ownership assertions; results feed G4 and 23.2 unchanged.\n\nPrior notes retained:\n- Same pipeline as 11.1/11.2; reference configurations from 03.3 extended to H3/H5 (R1 recipe applied; frozen before candidate runs).\n- `f3d bench` cells; ablations for animation batching and update kernels.\n\n## Success criteria\n\n- Results documents per stratum with verdicts vs the 2× floor; feeds G4 and F3D-23.2.\n\n## Test plan\n\n- Unit: none new.\n- E2E: build H3 and H5; `f3d verify` equivalence on both browsers; `f3d bench` paired runs on the reserved M5 and phone under protocol v1 with frozen references; ablation toggles for animation batching (H3) and update kernels (H5).\n- Logging: raw per-frame bench JSON, analysis reports with intervals, ablation deltas; archived under `evidence/13.8/<stratum>/`.\n- Failure artifacts: invalidated runs retained with reasons; verify artifacts.\n\n## Considerations\n\n- A cell below 2× is reported as such; it is not removed.\n","acceptance_criteria":"H3 and H5 build automatically, pass equivalence, and produce paired throughput results with intervals on at least the M5 and phone strata through the new backend; the report attributes gains to animation batching (H3) and update-kernel/upload efficiency (H5) via ablation toggles; no claim beyond the measured cells.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:46:33.347949Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:21.892172Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","h3","h5","new-backend","phase-2","tier-frontier"],"dependencies":[{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.8","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago.2","type":"blocks","created_at":"2026-09-09T16:49:05.889818Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.8","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s.4","type":"blocks","created_at":"2026-09-09T16:49:05.704116Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.8","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr","type":"parent-child","created_at":"2026-09-09T16:46:33.347949Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.8","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr.1","type":"blocks","created_at":"2026-09-09T16:49:05.329710Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-13-animation-math-geometry-closure-3cr.8","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr.2","type":"blocks","created_at":"2026-09-09T16:49:05.516471Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-14-material-light-texture-surface-5lj","title":"F3D-14: Entire standard material/light/texture/renderer format surface through the new backend","description":"## What this epic delivers\n\nPhase 3's rendering closure for the standard surface. Plan §19 F3D-14 depends on 07 and 11; acceptance: \"Every inventoried family and options, not only example-selected features\".\n\n## Why (plan §5.12 rows Materials, Physical shading, Lights, Textures, Cameras, Modern renderer APIs; §5.16; §8.4–8.6; §9.2; §18 Phase 3)\n\n\"Finish every standard material/map/state/light/shadow/texture/camera/render-target path... Run all applicable rendering cases with retained renderer submission disabled, beyond H1–H8. Close color/precision/history/diagnostic/resource-life combinations and port no feature by substituting a simpler visual effect.\" \"A portable material or pass is not marked 'native-backend-bound' merely because implementing it is difficult.\"\n\n## Children\n\n1. All built-in materials with every parameter/map/state combination.\n2. Physical shading closure.\n3. Lights, environments and shadows including the clustered path.\n4. Textures and media sources including external video lifecycle.\n5. Cameras, views and output.\n6. Render-target contract.\n7. Modern renderer/backend public APIs.\n8. Mobile bandwidth and tile behavior.\n9. Diagnostic shader targets and material differential infrastructure.\n\n## Success criteria for the epic\n\nAll children closed; the forced new-backend lane passes for every portable family; the manifest's new-WebGPU-path status is set for every record in these families; `f3d-shader` line count reported against 30,000 and `f3d-gpu` against 27,000.\n\n## No-claim boundary\n\nNumerical/backend semantics that genuinely require original GL are separately evidenced per record; never a blanket category.\n","acceptance_criteria":"Every inventoried material, physical-shading extension, light/environment/shadow technique, texture/media source, camera/view/output mode, render-target contract and modern renderer/backend API has a working path through the new WebGPU backend (or a separately evidenced exact/native exception) with property sweeps and combination tests, not only example-selected features.","status":"open","priority":2,"issue_type":"epic","created_at":"2026-09-09T16:46:23.496557Z","created_by":"jemanuel","updated_at":"2026-09-09T16:49:09.516421Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-graph","f3d-shader","new-backend","phase-3","tier-solid"],"dependencies":[{"issue_id":"f3d-14-material-light-texture-surface-5lj","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8","type":"blocks","created_at":"2026-09-09T16:49:09.329336Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T16:49:09.516121Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-14-material-light-texture-surface-5lj.1","title":"All built-in materials with every parameter, map, state and transition through the new backend","description":"## Full scope\n\nAll built-in materials through the new backend: Basic, Normal, Depth, Distance, Lambert, Phong, Toon, Matcap, Standard, Physical, Line/LineDashed, Points, Sprite, Shadow; every parameter and map (map, alphaMap, aoMap, bumpMap, displacementMap, emissiveMap, envMap, lightMap, metalnessMap, normalMap, roughnessMap, specularMap, gradientMap, matcap, etc.) and valid combination; alpha test/hash/coverage, blending modes, depth/stencil, sides, clipping planes, shadow/depth overrides, dynamic option changes and state transitions\n\n## Summary\n\nFrom the H1/H2 slice to the whole built-in material surface.\n\n## Background (plan §5.12 \"Materials and render state\", §9.2, §14.9)\n\n\"Full standard material paths through the new backend; dynamic options and state transitions; genuine backend extensions retain exact execution.\" Combinations: \"clipping+stencil+transparency; instancing+negative scale+custom shader indices+picking; transmission+color management+render targets\".\n\n## Technical approach\n\n- Extend `f3d-shader`'s material semantics with every pinned chunk/node for the listed materials; generate variants from the material feature set (08.3 keys); render-state descriptors (blending factors/equations incl. custom blending, depth func/write, stencil ops, cull mode, polygon offset, alpha to coverage, clipping planes as uniforms/discards per upstream).\n- Sweep generator in `f3d-conformance`: per material, all boolean flags × representative map sets × pairwise generation with a fixed seed; reference images from upstream WebGL and WebGPU renderers; diagnostic targets (14.9).\n- Runtime transitions: mutate flags/maps mid-run; compare.\n\n## Success criteria\n\n- Sweep and transition tests green in the forced lane on both browsers; manifest statuses set.\n\n## Test plan\n\n- Unit: shader-IR snapshot tests per material and feature flag; render-state descriptor mapping tests (blending factors/equations incl. custom, depth func/write, stencil ops, cull mode, polygon offset, alpha-to-coverage, clipping planes); variant-key tests.\n- E2E: generated pairwise sweeps `tests/e2e/materials/sweep-<material>.mjs` in the forced-new-backend lane on Chrome and Safari against upstream WebGL and WebGPU references with 03.6 tolerances and 14.9 diagnostic targets; runtime transition script (needsUpdate, blending/side/clipping toggles mid-run); the known high-risk combinations that involve materials.\n- Logging: per-step `metric` events `{material, flags, maps, max_err, mean_err, worst_region, diag_channel}`; recompile/rebind events on transitions; archived under `evidence/14.1/`.\n- Failure artifacts: reference/candidate/diff images and diagnostic-channel images per failing step.\n\n## Considerations\n\n- Alpha hash/coverage and `alphaToCoverage` depend on sample counts; keep quality settings equal to the reference.\n","acceptance_criteria":"Every built-in material class and node-material counterpart renders through the new backend with all pinned parameters/maps; a generated pairwise sweep over maps/state flags plus the known high-risk combinations matches upstream within tolerance; changing options at runtime (needsUpdate transitions, blending/side/clipping toggles) recompiles/rebinds correctly; the manifest records the new-WebGPU-path status per material property.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:24.234992Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:10.862088Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-shader","new-backend","phase-3","tier-solid","wgsl"],"dependencies":[{"issue_id":"f3d-14-material-light-texture-surface-5lj.1","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.3","type":"blocks","created_at":"2026-09-09T16:49:06.602993Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.1","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T16:49:06.788844Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.1","depends_on_id":"f3d-14-material-light-texture-surface-5lj","type":"parent-child","created_at":"2026-09-09T16:48:24.234992Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.1","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51.1","type":"blocks","created_at":"2026-09-09T16:49:06.967190Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-14-material-light-texture-surface-5lj.2","title":"Physical shading closure (clearcoat, sheen, anisotropy, iridescence, transmission, dispersion, retroreflection)","description":"## Full scope\n\nPhysical shading closure: pinned Standard/Physical equations and every extension (clearcoat and clearcoat normal/roughness, sheen, anisotropy, iridescence, transmission/thickness/attenuation/volume, IOR/specular color and intensity, dispersion, and every r186-introduced property including retroreflective controls); all tone-mapping operators, color spaces and premultiplication; reference scenes and sweeps including high-contrast and near-degenerate cases\n\n## Summary\n\nThe exact pinned physical model, including the properties no demo exercises.\n\n## Background (plan §5.12 \"Physical shading and color\", §9.2, §15.4)\n\n\"Versioned shader semantics and property sweeps, with texture/animation/light combinations; no generic-PBR substitution.\" \"The physical-material matrix must cover the actual pinned features, including clearcoat, transmission, attenuation, iridescence, sheen, anisotropy, dispersion, and every newly introduced r186 property, including retroreflective controls, whether or not the current demo corpus exercises it.\"\n\n## Technical approach\n\n- Transcribe pinned `MeshPhysicalMaterial`/node-material physical chunks (BRDF terms, multiscatter, iridescence Fresnel, sheen, clearcoat, transmission with the transmission render target and mip sampling, dispersion sampling, retroreflection) into shader IR; tone mapping operators (`Linear`, `Reinhard`, `Cineon`, `ACESFilmic`, `AgX`, `Neutral`, custom) and color-space conversions.\n- Transmission requires the transmission pass (with F3D-17 integration for render targets).\n- Sweeps with controlled environments and high-contrast/degenerate inputs (zero roughness, IOR extremes, thickness 0).\n\n## Success criteria\n\n- Sweeps green; `webgl_loader_gltf_transmission` and `webgpu_loader_gltf_dispersion` fidelity cases pass.\n\n## Test plan\n\n- Unit: shader-IR snapshot tests for every physical extension term and tone-mapping operator; color-space conversion tests vs upstream `ColorManagement`.\n- E2E: physical sweeps `tests/e2e/materials/physical.mjs` (clearcoat, sheen, anisotropy with tangent conventions, iridescence thin-film, transmission with render-target color management, dispersion, retroreflection, IOR/specular/volume extremes, every tone mapping and output color space) in the forced lane on both browsers; the glTF transmission and dispersion fidelity cases via `f3d verify`.\n- Logging: per-step metrics as in 14.1 plus the GPU error-envelope check per case; archived under `evidence/14.2/`.\n- Failure artifacts: diff and diagnostic images.\n\n## Considerations\n\n- \"Define the allowed GPU error/behavior envelope before optimization and require the source and candidate to stay inside it\" (§5.4).\n","acceptance_criteria":"Physical-shading manifest records all have new-WebGPU-path status with sweeps matching upstream within tolerance, including transmission with render-target color management, dispersion, iridescence thin-film math, anisotropy with tangent conventions, and retroreflection; no generic-PBR substitution; the glTF transmission and dispersion fidelity cases render correctly.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:24.339629Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:07.317370Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-shader","new-backend","phase-3","tier-solid","wgsl"],"dependencies":[{"issue_id":"f3d-14-material-light-texture-surface-5lj.2","depends_on_id":"f3d-14-material-light-texture-surface-5lj","type":"parent-child","created_at":"2026-09-09T16:48:24.339629Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.2","depends_on_id":"f3d-14-material-light-texture-surface-5lj.1","type":"blocks","created_at":"2026-09-09T16:49:07.153835Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.2","depends_on_id":"f3d-14-material-light-texture-surface-5lj.6","type":"blocks","created_at":"2026-09-09T17:44:07.316988Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-14-material-light-texture-surface-5lj.3","title":"Lights, environments and shadows closure incl. clustered path with no dropped lights","description":"## Full scope\n\nLights, environments and shadows: every core/addon light (directional/point/spot/hemisphere/rect-area/ambient/light probes/IES/projector/sun-style addons), environment maps and PMREM, cube maps, all shadow techniques (basic/PCF/PCFSoft/VSM, point-light cube shadows, spot/directional cascades via CSM), contact and progressive shadow techniques, helpers/generators; clustered/Forward+ path with overflow spill and infinite-range separate path; shadow caching only when all inputs unchanged; source callbacks still executed\n\n## Summary\n\nLighting and shadows at full inventory, with caching that never skips a required effect.\n\n## Background (plan §5.12 \"Lights, environments and shadows\", §8.4, §8.5 first paragraph)\n\n\"Cache shadow work only when all shadow inputs are unchanged, including geometry deformation, visibility, light/camera state, alpha/displacement effects, and render-target contents. Execute any source-required callback or node update even when the GPU result can be reused; skip the entire operation only if its effects are also proven absent.\" Clustered: \"It must include every light that can contribute under the source's model. Finite influence bounds require a valid bound; infinite-range lights need a separate path. Overflow in a tile/cluster light list cannot drop lights. Use an overflow spill or exact slower path.\"\n\n## Technical approach\n\n- Light uniform structures per pinned layouts; shadow map passes in the pass graph with dependency versions (06.3 categories: light params, shadow inputs); cache validity computed from consumed versions.\n- Retain construction/baking logic (PMREM, LightProbeGenerator, progressive light map addons) executing through the new backend.\n- Clustered path as an execution strategy selected by the specialization portfolio (F3D-20.2) with the overflow rule and the forward path as the conservative reference.\n- Public shadow-update flags are the primary contract: with `renderer.shadowMap.autoUpdate = false` shadows re-render only when `shadowMap.needsUpdate` is set, and `LightShadow.autoUpdate`/`needsUpdate` behave exactly as upstream, even if inputs changed; the version-based cache may only skip work that the public flags would have performed and proven-unchanged inputs allow; tests cover both flag states.\n\n## Success criteria\n\n- Family suite green; H4 and the shadow examples pass equivalence; a many-light test with forced cluster overflow shows no dropped light.\n\n## Test plan\n\n- Unit: light uniform layout tests; shadow-cache validity tests from consumed versions (deformation, visibility, light/camera, alpha/displacement, target contents each invalidate); clustered-path tests with a CPU reference (overflow spill keeps every light; infinite-range lights take the separate path).\n- E2E: sweeps over every light/shadow/environment type in the forced lane on both browsers vs upstream; H4 equivalence; a many-light scene with forced cluster overflow shows no dropped light; a callback-inside-shadow-pass test proves source-required callbacks still run when the GPU result is reused.\n- Logging: shadow cache events `{light, reused: bool, invalidation_reason}`; cluster stats `{tiles, max_per_tile, spills}`; per-step error metrics; archived under `evidence/14.3/`.\n- Failure artifacts: diff images and the light-list dump for the overflow case.\n\n## Considerations\n\n- Clustered lighting exists in upstream examples; the R1/R2 references include it where applicable (§8.4).\n","acceptance_criteria":"All light/shadow/environment manifest records have new-WebGPU-path status with tests; shadow passes cache only when geometry deformation, visibility, light/camera state, alpha/displacement effects and target contents are unchanged, while source-required callbacks/node updates still run; a clustered path exists for many-light workloads with a proven no-dropped-light rule and the simple forward path retained; H4's shadow workload renders correctly.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:24.433539Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:07.555001Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-graph","f3d-shader","new-backend","phase-3","tier-solid"],"dependencies":[{"issue_id":"f3d-14-material-light-texture-surface-5lj.3","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.2","type":"blocks","created_at":"2026-09-09T16:49:07.515162Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.3","depends_on_id":"f3d-14-material-light-texture-surface-5lj","type":"parent-child","created_at":"2026-09-09T16:48:24.433539Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.3","depends_on_id":"f3d-14-material-light-texture-surface-5lj.1","type":"blocks","created_at":"2026-09-09T16:49:07.337848Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-14-material-light-texture-surface-5lj.4","title":"Textures and media sources closure incl. external video lifecycle","description":"## Full scope\n\nTextures and media sources: all texture/source/sampler classes (2D, cube, 3D, arrays, data/depth/storage/render-target textures, external/video/canvas/HTML sources, compressed formats and mips, partial updates, filters/anisotropy/wrap, color space, flipY/premultiply/unpack alignment); device-lawful formats without quality reduction; external video lifetime (refresh at media/render boundaries, VideoFrame ownership, close exactly once, seeking/pause/replacement/origin tests)\n\n## Summary\n\nEvery texture kind, plus the special rules for media.\n\n## Background (plan §5.12 \"Textures and media sources\", §8.5 \"Canvas, media, and projection state\", §10.2, §23 [S39])\n\n\"External video textures have a source-dependent lifetime and color conversion, not an indefinitely reusable ordinary texture binding. Refresh them at supported media/render boundaries, retain `VideoFrame` ownership until its uses are done, and close it exactly once. Cache only what remains valid; do not retain an expired external texture in a reusable bundle.\"\n\n## Technical approach\n\n- Texture residency states in `f3d-gpu` (07.1) per source kind; upload paths (`writeTexture` layout rules, `copyExternalImageToTexture` for images/canvas/video frames, `importExternalTexture` for video where supported with the per-frame validity flag that excludes draws from bundles).\n- Format selection from the capability record; compressed formats (BC/ETC2/ASTC via KTX2 transcoding in F3D-19.2) with mips/block-edge rules.\n- Sampler descriptors mapping upstream filter/wrap/anisotropy/compare semantics.\n- Tests transcribed per class; media tests with a local video server.\n\n## Success criteria\n\n- Family suite green; video tests green on both browsers; no bundle retains an external texture.\n\n## Test plan\n\n- Unit: residency/upload-path tests per source kind (`writeTexture` layout rules, `copyExternalImageToTexture`, `importExternalTexture` per-frame validity flag excluding draws from bundles); sampler descriptor mapping tests; format selection tests from capability records (compressed formats, mip/block-edge rules).\n- E2E: texture-kind sweeps in the forced lane on both browsers (2D/cube/3D/array, data/depth/storage/RT, canvas/HTML/video sources, compressed formats via 19.2 fixtures, partial updates, filters/anisotropy/wrap, color space, flipY/premultiply/unpack alignment); media lifecycle tests with the local video server (seeking, pause/resume, `srcObject` replacement, frame closure exactly once, origin restrictions); the video fidelity case via `f3d verify`.\n- Logging: per-texture events `{kind, format, source, upload_path, bytes}`; media lifecycle events `{event, frame_closed}`; error metrics per sweep step; archived under `evidence/14.4/`.\n- Failure artifacts: diff images and the external-texture lifetime trace.\n\n## Considerations\n\n- Multiple renderer residencies + disposal is a mandatory combination test (18b.2).\n","acceptance_criteria":"Every texture-family manifest record has new-WebGPU-path status with tests; compressed formats are used natively where the device supports them and otherwise a justified equivalent representation is used without lossier content; texture partial updates and mip behavior match upstream; external video textures follow the source-dependent lifetime rules with tests for seeking, pause/resume, source replacement, frame closure and origin restrictions; the video fidelity case passes.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:24.529572Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:11.395379Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-assets","f3d-gpu","new-backend","phase-3","tier-solid"],"dependencies":[{"issue_id":"f3d-14-material-light-texture-surface-5lj.4","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.1","type":"blocks","created_at":"2026-09-09T16:49:07.693346Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.4","depends_on_id":"f3d-14-material-light-texture-surface-5lj","type":"parent-child","created_at":"2026-09-09T16:48:24.529572Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.4","depends_on_id":"f3d-14-material-light-texture-surface-5lj.1","type":"blocks","created_at":"2026-09-09T16:49:07.879574Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-14-material-light-texture-surface-5lj.5","title":"Cameras, views and output closure","description":"## Full scope\n\nCameras, views and output: perspective/orthographic/array/stereo/cube cameras, view offsets, custom projections, reversed and logarithmic depth, viewport/scissor/device pixel ratio, multiple canvases and targets, offscreen output; public matrices compared across all views\n\n## Summary\n\nComplete the camera/view surface started in 07.8.\n\n## Background (plan §5.12 \"Cameras, views and output\")\n\n\"Native renderer plus exact native-context route where observed; compare public matrices and all views.\"\n\n## Technical approach\n\n- View iteration in the execution protocol (07.4): `ArrayCamera` renders per sub-camera with its viewport; `StereoCamera` per eye; `CubeCamera` as six passes with the pinned face orientation and `CubeCamera.update` semantics; `setViewOffset` tiling applied to projection as upstream.\n- Depth modes: `reverseDepthBuffer`/`logarithmicDepthBuffer` handled in shader IR (15a.4) and depth-state descriptors; DPR changes reconfigure the canvas (07.2).\n- Multiple canvases/renderer instances: separate output epochs and resource groups; offscreen output targets via `OffscreenCanvas` where the source uses it.\n- Public matrices (`projectionMatrix`, `matrixWorldInverse`, view matrices) compared for every view against upstream.\n\nPrior notes retained:\n- View iteration in the execution protocol (07.4) for array/stereo cameras with per-view viewports; cube camera as six passes with orientation conventions; view offsets.\n- Multi-canvas: separate output epochs and resource groups (07.2).\n\n## Success criteria\n\n- Family suite green; multi-view examples pass equivalence.\n\n## Test plan\n\n- Unit: view-iteration tests for array/stereo cameras (viewport per view); cube-camera pass sequence and orientation tests; `setViewOffset` tiling tests; multi-canvas epoch separation tests.\n- E2E: camera/view sweeps in the forced lane on both browsers (perspective/orthographic/array/stereo/cube, view offsets, custom projections, reversed/log depth, DPR changes, multiple canvases, offscreen targets) with public-matrix comparison for every view; the relevant upstream examples via `f3d verify`.\n- Logging: per-view `metric` events `{camera, view, max_err, projection_equal: bool}`; archived under `evidence/14.5/`.\n- Failure artifacts: per-view diff images and matrix dumps.\n\n## Considerations\n\n- XR views are F3D-21b.3 but reuse this machinery.\n","acceptance_criteria":"Every camera/view manifest record has new-WebGPU-path status with tests; ArrayCamera and StereoCamera multi-view rendering, CubeCamera captures, setViewOffset tiling, DPR changes, multiple canvases with separate epochs and offscreen targets match upstream; public matrices are compared for every view.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:24.622475Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:22.758787Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","new-backend","phase-3","tier-solid"],"dependencies":[{"issue_id":"f3d-14-material-light-texture-surface-5lj.5","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.8","type":"blocks","created_at":"2026-09-09T16:49:08.058706Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.5","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:25.113235Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.5","depends_on_id":"f3d-14-material-light-texture-surface-5lj","type":"parent-child","created_at":"2026-09-09T16:48:24.622475Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-14-material-light-texture-surface-5lj.6","title":"Render-target contract closure (cube/array/3D, MRT, depth/stencil, MSAA, explicit ops)","description":"## Full scope\n\nRender-target contract: cube/array/3D targets, layers and mip levels, multiple render targets, depth/stencil attachments and textures, MSAA and resolves, clipping, explicit clear/load/store behavior, formats and usage flags validated in the plan\n\n## Summary\n\nRender targets as first-class pass-graph resources with the full upstream option surface.\n\n## Background (plan §8.5 \"Support every applicable rendering contract\", §5.6)\n\n\"cube/array/3D textures, render-target layers and mip levels, multiple render targets, depth/stencil, viewport/scissor, multisampling/resolves, clipping, and explicit clear/load/store behavior. Resource formats and usage flags are part of the plan's validation.\"\n\n## Technical approach\n\n- Target descriptors → pass-graph attachments (07.2): 2D/cube/array/3D targets with per-layer and per-mip attachment views, MRT with per-attachment formats, depth/stencil attachments and depth textures with compare samplers, MSAA sample counts with resolve targets, `autoClear`/`clearColor/Depth/Stencil` as explicit load/clear ops, viewport/scissor per target, clipping state.\n- Validation: formats/usages checked against the capability record at plan time (renderable/filterable/blendable per format); an unsupported combination fails with a structured error naming the source object.\n- Readbacks: this bead exposes attachment views and marks source-visible targets so transient reuse (17.2) never touches them; the `readRenderTargetPixelsAsync` staging path itself is delivered and tested by 17.4.\n- Family suite from the manifest: every `WebGLRenderTarget`/`RenderTarget` variant, option and method; upstream MRT/cube/array examples via `f3d verify`.\n\nPrior notes retained:\n- Target descriptors → pass-graph attachments with subresource ranges; MSAA with resolve attachments; depth/stencil formats per capability; `autoClear`/`clearColor/Depth/Stencil` semantics as explicit load ops.\n- Async readback through staging buffers (17.4 integrates copies).\n- Sample counts: WebGPU exposes only 1 or 4 samples; a `WebGLRenderTarget.samples` of 2/8/16 follows the pinned upstream WebGPU backend's mapping, the difference is recorded in the manifest/report as a host limitation shared with the reference WebGPU renderer (never silently), and the quality-invariance guard (14.8) treats that mapping as a declared host limitation, not a quality reduction.\n\n## Success criteria\n\n- Family suite green; MRT/cube/array examples pass equivalence.\n\n## Test plan\n\n- Unit: target-descriptor → attachment mapping tests (cube/array/3D, layers/mips, MRT, depth/stencil formats, sample counts and resolves, explicit load/store/clear from `autoClear`/`clear*`); format/usage validation tests; staging-buffer readback tests.\n- E2E: render-target sweeps in the forced lane on both browsers vs upstream (MRT, cube/array/3D targets, mip-level and layer targets, MSAA resolves, depth textures, clipping); attachment views exposed for 17.4's readback path; a sync-read attempt on a new-backend renderer is proven to have been routed exactly at construction.\n- Logging: per-target events `{target, attachments, format, samples, ops}` and readback checks; archived under `evidence/14.6/`.\n- Failure artifacts: readback dumps and diff images.\n\n## Considerations\n\n- Source-visible render targets prevent transient reuse (17.2).\n","acceptance_criteria":"Every render-target manifest record (WebGLRenderTarget variants, RenderTarget, cube/array/3D targets, MRT, depth textures, sample counts, resolve targets, mip-level/layer targets) has new-WebGPU-path status with tests; attachment operations are explicit in the pass graph; formats/usages are validated; source-visible targets are marked for 17.2 and attachment views are exposed for 17.4's readback path (async readbacks themselves are delivered and tested by 17.4); synchronous reads remain exact-route only; unsupported sample counts follow the pinned upstream WebGPU backend's mapping and are recorded as a shared host limitation.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:24.719931Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:07.146418Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-graph","new-backend","phase-3","tier-solid"],"dependencies":[{"issue_id":"f3d-14-material-light-texture-surface-5lj.6","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.2","type":"blocks","created_at":"2026-09-09T16:49:08.244345Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.6","depends_on_id":"f3d-14-material-light-texture-surface-5lj","type":"parent-child","created_at":"2026-09-09T16:48:24.719931Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.6","depends_on_id":"f3d-14-material-light-texture-surface-5lj.4","type":"blocks","created_at":"2026-09-09T16:49:08.431349Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-14-material-light-texture-surface-5lj.7","title":"Modern renderer/backend public API closure (three/webgpu exports)","description":"## Full scope\n\nModern renderer/backend public APIs: WebGPURenderer/Renderer options and methods, exported Backend classes, BundleGroup, QuadMesh, RenderPipeline/DirectRenderPipeline/PostProcessing, CanvasTarget/BlendMode, readback and storage APIs, backend extension hooks; new WebGPU implementation of standard execution with the exact component for native opaque subclass/handle contracts\n\n## Summary\n\nThe modern renderer surface exported by `three/webgpu`, including things no demo calls.\n\n## Background (plan §2.9, §5.12 \"Modern renderer/backend APIs\", §23 [S53])\n\n\"`Three.WebGPU.js` also exports backend classes, node builders, storage objects, render pipelines, and loaders. Those are part of the actual surface, not hypothetical third-party internals.\" \"New WebGPU implementation of standard execution; exact component for native opaque subclass/handle contracts; no forced downgrade of modern features.\"\n\n## Technical approach\n\n- Enumerate the export list of the pinned `Three.WebGPU.js` from the manifest; for each export decide the mapping: standard execution through the new backend (`WebGPURenderer`/`Renderer` options and methods, `BundleGroup` → 08.4 bundles with upstream semantics, `QuadMesh`, `RenderPipeline`/`DirectRenderPipeline`/`PostProcessing` → 17.1, `CanvasTarget`/`BlendMode`, storage buffers/textures → 16.3, readbacks → 17.4, `Backend` extension hooks for standard use) or exact/native routing (custom `Backend` subclasses, explicit `WebGLBackend`, native device/handle observations → 16.4).\n- Renderer option matrix tests (every constructor option in the pinned `Renderer`/`WebGPURenderer` signature as enumerated by the census, for example `antialias`, `alpha`, `depth`, `stencil`, `samples`, `forceWebGL`, `requiredLimits`, `logarithmicDepthBuffer`, `reverseDepthBuffer`, `outputType`, `colorBufferType`, `multiview`, `trackTimestamp`, `powerPreference`, `device`, `context`, `getFallback`; names taken from the pinned source, never from memory) against upstream behavior; no forced downgrade of modern features.\n- Generated family suite per export in the functional and forced lanes; exact-lane fixtures for the native contracts.\n\nPrior notes retained:\n- From the manifest, enumerate the export list; for each: standard execution mapping (BundleGroup → 08.4 bundles with upstream's semantics; QuadMesh; RenderPipeline/PostProcessing → F3D-17; CanvasTarget/BlendMode; storage buffers/textures → F3D-16.3; readbacks → 17.4) or exact/native routing (custom `Backend` subclass instances, `WebGLBackend` explicit use, native device/handle observations).\n- Renderer option matrix tests.\n\n## Success criteria\n\n- Family suite green; each export's owner recorded.\n\n## Test plan\n\n- Unit: export-list-driven mapping tests (each `three/webgpu` export → standard execution mapping or exact/native route with the named contract); renderer option matrix tests (antialias, alpha, forceWebGL, requiredLimits, etc.).\n- E2E: generated family suite over every `Three.WebGPU.js` public export in the functional and forced lanes on both browsers; custom `Backend` subclass fixtures in the exact lane with real native objects asserted.\n- Logging: per-export `{export, owner, route, test}` feeding the manifest; archived under `evidence/14.7/`.\n- Failure artifacts: the failing export's test log.\n\n## Considerations\n\n- \"For native WebGPU contracts preserve actual device/resource objects through the appropriate component\" (§5.6).\n","acceptance_criteria":"Every Three.WebGPU.js public export has an owner and positive tests: standard execution through the new backend, exact/native components for opaque subclass and native-handle contracts; no forced downgrade of modern features; renderer options (antialias, alpha, forceWebGL, requiredLimits, etc.) behave as upstream.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:24.818100Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:10.408952Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-gpu","f3d-runtime","new-backend","phase-3","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-14-material-light-texture-surface-5lj.7","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.4","type":"blocks","created_at":"2026-09-09T16:49:08.783992Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.7","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.1","type":"blocks","created_at":"2026-09-09T16:49:08.608773Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.7","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:25.323751Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.7","depends_on_id":"f3d-14-material-light-texture-surface-5lj","type":"parent-child","created_at":"2026-09-09T16:48:24.818100Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-14-material-light-texture-surface-5lj.8","title":"Mobile bandwidth and tile behavior without quality reduction","description":"## Full scope\n\nMobile bandwidth and tile behavior: avoid provably unnecessary stores, clears, copies and full-resolution intermediates; optional transient-attachment support as a capability-specific path; no silent reduction of resolution, shadow size, MSAA, anisotropy, samples, geometry detail or temporal history\n\n## Summary\n\nBandwidth wins that are purely structural.\n\n## Background (plan §8.6, §23 [S21])\n\n\"Reduce attachment traffic by avoiding unnecessary stores, clears, copies, and full-resolution intermediates that are provably unused. Optional transient-attachment support is a capability-specific path; do not depend on it universally. Do not silently reduce render resolution, shadow size, MSAA, anisotropy, samples per pixel, geometry detail, or temporal history to obtain a performance win.\"\n\n## Technical approach\n\n- Dead-store analysis in `f3d-graph`: for each attachment, determine from explicit dependency edges whether any later pass or readback reads it; if not, set `storeOp: 'discard'`; never infer from sampling patterns alone (explicit edges only, 07.2).\n- Redundant clears/copies: WebGPU has no don't-care load op (only `load`/`clear`; `discard` exists only for `storeOp`), so when a pass provably covers every texel of an attachment (full-viewport pass, no scissor, depth/stencil test disabled) the preceding `loadOp: 'clear'` may become `loadOp: 'load'`, otherwise the clear stays; avoid full-resolution intermediates when a pass chain can legally run at the target's resolution (no quality change).\n- Transient attachments: when the capability record advertises the transient-attachment feature, allocate memoryless attachments for passes whose contents are never stored; otherwise ordinary attachments.\n- Quality-invariance guard: at plan time, compare every quality-affecting parameter (resolution, shadow map sizes, MSAA, anisotropy, sample counts, geometry detail, temporal history length) against the source's values; any difference fails the plan.\n\nPrior notes retained:\n- Dead-store analysis over the pass graph (explicit dependency edges only, 07.2); load/store op selection; transient attachment flag when the feature exists.\n- Quality-invariance test: compare all quality-affecting settings between reference and candidate at plan time.\n\n## Success criteria\n\n- Telemetry shows reduced store bytes on tile-sensitive tests; quality-invariance test passes.\n\n## Test plan\n\n- Unit: dead-store analysis tests over pass-graph fixtures (store elided only for provably unused attachments; explicit dependency edges only); transient-attachment gating test from the capability record; quality-invariance test comparing every quality-affecting setting between reference and candidate at plan time.\n- E2E: tile-sensitive scenes on the phone and M5 with telemetry showing reduced store bytes and identical output vs upstream; the quality-invariance check runs in every forced-lane test.\n- Logging: attachment traffic `metric` events `{pass, loads, stores, bytes_saved}`; quality-invariance report; archived under `evidence/14.8/`.\n- Failure artifacts: the plan diff showing the elided operation on any output mismatch.\n\n## Considerations\n\n- Thermal behavior is measured in F3D-23.3; this bead enables, not claims.\n","acceptance_criteria":"The pass graph elides store operations for attachments proven unused, avoids redundant clears/copies, and uses transient attachments only when the capability record supports them; a test guards that no quality parameter is changed by any optimization; attachment traffic is reported in telemetry.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:24.919352Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:07.732096Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-graph","new-backend","phase-3","tier-solid"],"dependencies":[{"issue_id":"f3d-14-material-light-texture-surface-5lj.8","depends_on_id":"f3d-14-material-light-texture-surface-5lj","type":"parent-child","created_at":"2026-09-09T16:48:24.919352Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.8","depends_on_id":"f3d-14-material-light-texture-surface-5lj.6","type":"blocks","created_at":"2026-09-09T16:49:08.973384Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-14-material-light-texture-surface-5lj.9","title":"Diagnostic shader targets and material differential infrastructure","description":"## Full scope\n\nDiagnostic shader targets and the material differential infrastructure: intermediate normals, depth, material channels (albedo/roughness/metalness/emissive), world position; color-space-aware comparison, edge/silhouette and region checks, temporal consistency; used by all material/light/pass sweeps\n\n## Summary\n\nMake subtle mismatches findable.\n\n## Background (plan §9.6, §14.6)\n\n\"Diagnostic shaders for intermediate normals, depth, material channels, and world position make subtle mismatches much easier to locate than final-color screenshots alone.\" \"Use color-space-aware comparisons, edge/silhouette checks, region checks, depth/normal diagnostic targets, and temporal consistency in addition to aggregate image metrics.\"\n\n## Technical approach\n\n- Shader IR debug outputs: a per-pass diagnostic mode that replaces the fragment output with intermediate quantities (world-space normal, view depth, albedo/roughness/metalness/emissive channels, world position) without changing the rest of the program; selectable per test.\n- Reference equivalents: upstream `MeshNormalMaterial`, depth materials and small custom debug materials applied identically on the reference side so both sides produce comparable diagnostic images.\n- Comparison library (`tools/harness/compare.mjs`): color-space-aware per-pixel metric, edge/silhouette extraction and comparison, region checks with narrowly identified diagnostic exclusions, depth/normal channel metrics, temporal-consistency metric across frame sequences; tolerances come from protocol v1 (03.6) and cannot be loosened by callers.\n- Output: first mismatching channel and region with coordinates; artifacts stored per the evidence layout (01.8).\n\nPrior notes retained:\n- Shader IR debug outputs selectable per pass; reference-side equivalents via upstream `MeshNormalMaterial`/depth materials/custom debug materials applied identically.\n- Comparison library in the tooling package with the preregistered tolerance model (03.6).\n\n## Success criteria\n\n- Used by 14.1–14.3 sweeps; a synthetic mismatch is localized correctly.\n\n## Test plan\n\n- Unit: comparison-library tests (color-space-aware metric, edge/silhouette, region checks, temporal consistency) on synthetic fixtures; a known-bad fixture localized to the correct channel and region; diagnostic-output selection tests in the shader IR.\n- E2E: diagnostic renders of a reference scene compared against upstream debug materials (normal/depth/world position/channels) on both browsers.\n- Logging: per-comparison `metric` events `{channel, max_err, region}`; archived under `evidence/14.9/`.\n- Failure artifacts: diagnostic images from both sides and the diff.\n\n## Considerations\n\n- Never regenerate goldens from the candidate.\n","acceptance_criteria":"A diagnostic mode renders intermediate quantities from the same shader IR; the comparison library implements the §14.6 checks; sweeps report the first mismatching channel and region; goldens come only from the reference.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:25.017687Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:06.603033Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","f3d-shader","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-14-material-light-texture-surface-5lj.9","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.3","type":"blocks","created_at":"2026-09-09T16:49:09.151915Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-14-material-light-texture-surface-5lj.9","depends_on_id":"f3d-14-material-light-texture-surface-5lj","type":"parent-child","created_at":"2026-09-09T16:48:25.017687Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-15a-early-shader-spike-7k8","title":"F3D-15 (early slice): H1/H2 shader preparation, the finite ESSL-to-WGSL spike and toon node lowering","description":"## What this epic delivers\n\nThe shader half of the first vertical slice. Plan §19 F3D-15 depends on 04; the early acceptance is \"finite spike before 11\". \"F3D-15's initial work is source analysis/normalization and WGSL generation, not dependence on an already complete renderer. Its rendered acceptance integrates with F3D-07.\"\n\n## Why (plan §2.7, §9.1, §9.4, §20 \"Legacy shader translation becomes a compiler project larger than budget\")\n\n\"A plan that says 'run all Three.js GLSL through Naga' is incomplete. Naga's documented GLSL frontend targets Vulkan GLSL versions, not a complete turnkey WebGL GLSL ES 1.00/3.00 frontend.\" The earliest decisive test for the shader-budget risk is \"full finite H2 corpus and H1 material spike before the first product gate\". The ESSL frontend is the largest single budget uncertainty.\n\n## Children\n\n1. ESSL pipeline skeleton with diagnostics.\n2. H2 legacy ShaderMaterial variants and the ShaderLib chunks H2 reaches.\n3. H1 MeshToonNodeMaterial TSL lowering.\n4. Coordinate-system and interface treatment.\n5. Naga admission audit and build-time-only default.\n\n## Success criteria for the epic\n\nAll children closed; every H2 shader variant and H1's toon material produce validated WGSL that renders equivalently; the spike's line count is recorded against the 30,000-line `f3d-shader` budget with a forecast for the full frontend (15b).\n\n## No-claim boundary\n\nThis is a finite corpus. Unseen runtime shaders are F3D-15b's obligation; nothing here implies full ESSL coverage.\n","acceptance_criteria":"A dedicated bounded ESSL preprocessing/normalization/interface/Naga/WGSL pipeline translates the complete finite shader corpus reachable by H2 (its ShaderLib materials and four legacy ShaderMaterial variants) and lowers H1's MeshToonNodeMaterial graph, with source locations, readable diagnostics, explicit coordinate-system treatment and differential render tests; Naga is admitted as a build-time dependency with an audit record.","status":"open","priority":0,"issue_type":"epic","created_at":"2026-09-09T16:46:23.585648Z","created_by":"jemanuel","updated_at":"2026-09-09T16:49:11.084590Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-shader","phase-0","phase-1","shaders","tier-solid","wgsl"],"dependencies":[{"issue_id":"f3d-15a-early-shader-spike-7k8","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.2","type":"blocks","created_at":"2026-09-09T16:49:10.896286Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15a-early-shader-spike-7k8","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.1","type":"blocks","created_at":"2026-09-09T16:49:11.084242Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-15a-early-shader-spike-7k8.1","title":"ESSL compatibility pipeline skeleton (preprocess, normalize, interfaces, Naga, WGSL, diagnostics)","description":"## Full scope\n\nESSL compatibility pipeline skeleton: Three shader source plus includes/defines/material hooks; ESSL preprocessing (macros, ShaderChunk includes, defines) and semantic normalization; explicit stage interfaces, resources and builtins; supported Naga IR/GLSL entrance; validation and WGSL emission; source locations and readable diagnostics\n\n## Summary\n\nThe pipeline of §9.4, built as a real language tool.\n\n## Background (plan §9.4, §9.1, §23 [S20], [S36])\n\n```\nThree shader source + includes/defines + material hooks\n  -> ESSL preprocessing and semantic normalization\n  -> explicit stage interfaces, resources, and builtins\n  -> supported Naga IR/GLSL entrance\n  -> validation and WGSL emission\n  -> browser compilation and differential render tests\n```\n\"The normalization layer must handle actual language constructs, not regex-only syntax substitutions. The first shader spike includes macros, includes, samplers, precision declarations, derivatives, fragment depth, discard, loops, matrix/vector operations, and interface matching.\"\n\n## Technical approach\n\n- Preprocessor: full C-style macro expansion as ESSL specifies (object/function-like macros, `#if/#ifdef/#elif` with expression evaluation, `#include` resolution against the pinned `ShaderChunk` table and material `defines`), `#extension`/`#version`/precision handling recorded as metadata, line/file mapping preserved.\n- Parser/normalizer: an ESSL 1.00/3.00 parser (first-party, bounded to what Three.js and valid application shaders use) producing an AST; normalization passes: resolve implicit `gl_*` builtins to explicit interface variables, attribute/varying/uniform declarations into stage interfaces, combined `sampler2D` uniforms into (texture, sampler) pairs (15a.4), precision qualifiers dropped with a record, legacy syntax (`attribute`/`varying`/`texture2D`) upgraded.\n- Emission: prefer Naga's GLSL frontend on the normalized Vulkan-compatible GLSL when it accepts the program; otherwise emit from the first-party AST to Naga IR or directly to WGSL text (decision recorded per program in the report); Naga validation always runs.\n- Diagnostics with spans mapped through include expansion to `ShaderChunk` names and application lines.\n- Program assembly: reproduce the pinned `WebGLProgram`/`WebGLPrograms` string assembly exactly, so the translator receives the same final GLSL upstream compiles: parameter-derived prefix `#define` lists from `WebGLPrograms.getParameters`, the GLSL3 prefix translations (`#version 300 es`, `#define attribute in`/`varying`, `texture2D`→`texture`, `gl_FragColor`→`pc_fragColor` with `layout(location = 0) out`), the standard uniform/attribute prelude, recursive `resolveIncludes` (also for application-level `#include <common>`), `#pragma unroll_loop_start/end` unrolling, `replaceLightNums`/`replaceClippingPlaneNums`, `RawShaderMaterial`'s minimal prefix and `material.glslVersion` selection; `onBeforeCompile` (15b.2) runs on the pre-prefix strings as upstream does.\n- Builtin function mapping table with semantic notes and differential tests per entry: GLSL `mod(x, y) = x - y * floor(x / y)` is not WGSL `%` (truncating); `atan(y, x)` → `atan2`; `inversesqrt` → `inverseSqrt`; `dFdx`/`dFdy` → `dpdx`/`dpdy`; integer `/` and `%` sign rules; float→int conversion (GLSL undefined out of range vs WGSL saturating); `pow` with a negative base; `min`/`max` with NaN.\n\n## Success criteria\n\n- Test corpus translates and validates; diagnostics tested with deliberately broken chunks; fuzz seed corpus started (15b.5).\n- `f3d-shader/CONTRACT.md` is written before any other crate depends on `f3d-shader` (AGENTS.md \"Documentation and Contracts\"): purpose and position in the dependency direction, public types and semantics, invariants, implementation ownership route, error model, determinism and numeric-fidelity class, cancellation behavior, unsafe boundary if any, feature flags, conformance tests, and no-claim boundaries.\n\n## Test plan\n\n- Unit: preprocessor tests per feature (object/function-like macros, `#if/#ifdef/#elif` expressions, `#include` against the pinned ShaderChunk table with material defines, `#extension`/`#version`/precision metadata, line/file mapping); parser/normalizer tests per construct (builtins → interface variables, attribute/varying/uniform declarations, legacy syntax upgrade); golden WGSL snapshots for the initial corpus; diagnostics tests with deliberately broken chunks mapping back to chunk name and line.\n- E2E: the corpus programs compile in Chrome and Safari (pipeline creation succeeds) and a smoke render matches upstream.\n- Logging: per-program pipeline-stage events `{program, stage, ms, naga_path: glsl-in|first-party, warnings}`; archived under `evidence/15a.1/`.\n- Failure artifacts: the normalized GLSL and emitted WGSL for the failing program with the diagnostic.\n\n## Considerations\n\n- Keep it build-time-only for statically closed pages; the runtime inclusion path is 15b.2.\n","acceptance_criteria":"f3d-shader contains a real (not regex-only) ESSL preprocessor and normalizer producing an explicit stage-interface representation, feeding Naga's supported GLSL entrance (or a first-party IR when Naga's frontend cannot accept the normalized form), validating and emitting WGSL; every diagnostic maps back to the original Three.js chunk/line; a first test corpus (a handful of ShaderLib programs) translates and validates.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:48:25.113610Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:12.467144Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-shader","phase-0","shaders","tier-solid","wgsl"],"dependencies":[{"issue_id":"f3d-15a-early-shader-spike-7k8.1","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.2","type":"blocks","created_at":"2026-09-09T16:49:09.686441Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15a-early-shader-spike-7k8.1","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.1","type":"blocks","created_at":"2026-09-09T16:49:09.859839Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15a-early-shader-spike-7k8.1","depends_on_id":"f3d-15a-early-shader-spike-7k8","type":"parent-child","created_at":"2026-09-09T16:48:25.113610Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-15a-early-shader-spike-7k8.2","title":"H2 legacy ShaderMaterial variants and ShaderLib chunks translated with differential tests","description":"## Full scope\n\nH2 legacy ShaderMaterial variants and the ShaderLib chunks reached by H2's materials translated to WGSL with differential render tests\n\n## Summary\n\nThe finite corpus that the first product gate requires.\n\n## Background (plan §2.7, §15.3, §20 row 5)\n\n\"Its default material and every material-selector branch, including the four legacy ShaderMaterial variants, belong in the first product slice; the UI cannot be restricted to an easy shader after the fact.\"\n\n## Technical approach\n\n- Extract the exact shader sources from the pinned example: the four legacy variants are `ShaderMaterial`s built from the shader objects in the pinned `examples/jsm/shaders/ToonShader.js` that the example imports (verify against the pinned source; do not assume inline definitions); record that module's blob fingerprint and its uniforms/attributes. Derive the chunk closure of the built-in materials H2 uses by running the pinned `WebGLPrograms.getParameters` for each H2 branch (for example `ENVMAP_MODE_REFRACTION` for the liquid branch, `FLAT_SHADED`, `USE_MAP`, `USE_COLOR`, `USE_ENVMAP`) against the pinned `ShaderLib` + `ShaderChunk` tables, never by guessing.\n- Translate; hook the uniforms into the parameter-block layout (05.2); differential render on the marching-cubes mesh at several control states.\n\n## Success criteria\n\n- All programs green; the variant table lists them as precompiled.\n\n## Test plan\n\n- Unit: golden WGSL snapshots for the four legacy ShaderMaterial programs and every ShaderLib/ShaderChunk program in H2's closure; uniform/attribute interface tests against the parameter-block layout.\n- E2E: differential renders `tests/e2e/shaders/h2-corpus.mjs` on the marching-cubes mesh at several control states in the forced lane on both browsers vs upstream with 03.6 tolerances.\n- Logging: per-program translation status and per-render error metrics; archived under `evidence/15a.2/`.\n- Failure artifacts: WGSL, diff images and diagnostic targets.\n\n## Considerations\n\n- Any ESSL construct in this corpus the pipeline cannot handle is a blocker for F3D-11, not a \"legacy-only\" reclassification.\n","acceptance_criteria":"The four legacy ShaderMaterial programs in the pinned webgl_marchingcubes.html (as inventoried in 01.6) and every ShaderLib/ShaderChunk program reached by H2's Standard/Lambert/Phong/texture/color branches translate through the pipeline, validate under Naga, compile in Chrome and Safari, and render equivalently to upstream in differential tests with the H2 geometry.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:48:25.211409Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:12.653006Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-shader","h2","phase-0","phase-1","shaders","tier-solid","wgsl"],"dependencies":[{"issue_id":"f3d-15a-early-shader-spike-7k8.2","depends_on_id":"f3d-15a-early-shader-spike-7k8","type":"parent-child","created_at":"2026-09-09T16:48:25.211409Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15a-early-shader-spike-7k8.2","depends_on_id":"f3d-15a-early-shader-spike-7k8.1","type":"blocks","created_at":"2026-09-09T16:49:10.037991Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15a-early-shader-spike-7k8.2","depends_on_id":"f3d-15a-early-shader-spike-7k8.4","type":"blocks","created_at":"2026-09-09T16:49:10.215213Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-15a-early-shader-spike-7k8.3","title":"H1 MeshToonNodeMaterial TSL lowering","description":"## Full scope\n\nH1 MeshToonNodeMaterial TSL lowering: node semantics for every node in H1's toon graph (gradient step lighting, color/uniform nodes, lighting context nodes) translated to typed shader IR and WGSL with differential tests\n\n## Summary\n\nThe node entrance at H1's scope; the full node inventory is F3D-16.\n\n## Background (plan §9.3, §2.7, §5.12 \"TSL, nodes and compute\")\n\n\"Let ordinary JavaScript construct TSL graphs, then translate recognized nodes into typed shader IR.\"\n\n## Technical approach\n\n- Enumerate the node classes reached by `MeshToonNodeMaterial` in the pinned source (lighting model nodes, `toonLightingModel`-equivalent functions, material reference nodes, uniform/attribute nodes); implement translator entries; compare with upstream's WGSL output as a debugging aid (not as the oracle; the oracle is rendered output).\n- Fallback: for any node not yet translated, use the retained upstream node builder to produce WGSL (disclosed in the report as retained shader construction), still submitted through the new backend.\n\n## Success criteria\n\n- Differential render equivalence on H1; translator coverage report for the graph.\n\n## Test plan\n\n- Unit: translator snapshot tests for every node class in H1's toon graph; a coverage test asserting each node is either translated or explicitly recorded as retained-builder fallback.\n- E2E: differential render of H1's toon scene in the forced lane on both browsers vs upstream's WebGPU renderer.\n- Logging: translator coverage report `{node, status}` and render error metrics; archived under `evidence/15a.3/`.\n- Failure artifacts: IR/WGSL dumps and diff images.\n\n## Considerations\n\n- 07.5 handles the runtime update semantics; this bead is the static lowering.\n","acceptance_criteria":"The node graph constructed by H1's MeshToonNodeMaterial (with its per-object colors) lowers to WGSL through the node translator with every node in the graph recognized (or retained via the upstream WGSL node builder output as a disclosed fallback); the resulting shader renders equivalently to upstream's WebGPU renderer on H1's geometry set.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:48:25.311550Z","created_by":"jemanuel","updated_at":"2026-09-09T17:31:51.185025Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-shader","h1","phase-0","phase-1","shaders","tier-solid","wgsl"],"dependencies":[{"issue_id":"f3d-15a-early-shader-spike-7k8.3","depends_on_id":"f3d-15a-early-shader-spike-7k8","type":"parent-child","created_at":"2026-09-09T16:48:25.311550Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15a-early-shader-spike-7k8.3","depends_on_id":"f3d-15a-early-shader-spike-7k8.1","type":"blocks","created_at":"2026-09-09T16:49:10.382947Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-15a-early-shader-spike-7k8.4","title":"Coordinate-system and interface treatment in translation (depth range, winding, samplers, builtins)","description":"## Full scope\n\nCoordinate-system and interface treatment in translation: clip-depth range, framebuffer conventions, winding, texture orientation, fragment coordinates, interpolation qualifiers; combined-sampler splitting, integer/flat varyings, attribute normalization, explicit buffer layout, source vertex/instance builtins; point-size and line behavior expansion where the source contract requires it\n\n## Summary\n\nThe semantic differences between WebGL GLSL and WebGPU WGSL, handled explicitly.\n\n## Background (plan §9.4 last two paragraphs)\n\n\"Add combined-sampler splitting, integer/flat varyings, attribute normalization, explicit buffer layout, and source vertex/instance builtins.\" \"Coordinate-system differences require explicit treatment: clip-depth range, framebuffer conventions, winding, texture orientation, fragment coordinates, and interpolation. Point-size and line behavior need appropriate geometry/shader expansion when the source contract requires it.\"\n\n## Technical approach\n\n- Normalization passes with unit tests per difference; clip-depth adjustment coordinated with 07.8 so it happens exactly once; `gl_FragCoord` y-flip policy; texture orientation (`flipY`) handled at upload or sampling consistently with upstream.\n- Point sprites: WebGPU has no point size; expand to instanced quads when `gl_PointSize`/`PointsMaterial.size` semantics require it, preserving `gl_PointCoord`.\n- Wide lines: `LineBasicMaterial.linewidth` is effectively 1 on WebGL; `Line2`/`LineMaterial` addon geometry expansion retained.\n- Instance/vertex indices: `gl_InstanceID` is zero-based per draw in WebGL2, but WGSL `@builtin(instance_index)` includes `firstInstance`; map `gl_InstanceID` → `instance_index - base_instance` (base supplied as a uniform or the 08.6 bucket base) and `gl_VertexID` → `vertex_index` (index-buffer value, matching WebGL2); tests cover draws with nonzero `firstInstance`.\n- Fragment outputs: ESSL 1.00 `gl_FragColor`/`gl_FragData[n]` and GLSL3 `layout(location = n) out` map to WGSL `@location(n)` fragment outputs with MRT ordering preserved.\n- Clip depth: apply the private -1..1 → 0..1 adjustment only for renderers whose source convention is WebGL (H2); for `WebGPURenderer`-authored sources (H1) the public projection matrix already uses WebGPU conventions because upstream sets `camera.coordinateSystem = WebGPUCoordinateSystem`, and no adjustment is applied; a test renders both source kinds and asserts exactly one conversion (coordinated with 07.8).\n- Sampler/texture mapping table with per-entry unit tests: `sampler2D`/`sampler2DArray`/`sampler3D`/`samplerCube` → `texture_*<f32>` plus `sampler`; `isampler*`/`usampler*` → `texture_*<i32/u32>` (non-filterable, `textureLoad` only); `sampler2DShadow`/`samplerCubeShadow` → `texture_depth_*` plus `sampler_comparison`/`textureSampleCompare`; `texelFetch` → `textureLoad`, `textureSize` → `textureDimensions`, `textureLod` → `textureSampleLevel`, `textureGrad` → `textureSampleGrad`, `textureProj` → explicit divide; `layout(std140) uniform` blocks (`UniformsGroup`) → WGSL uniform structs via the 05.2 schema (std140 and WGSL differ on `vec3`/array strides and `mat3` column padding).\n\n## Success criteria\n\n- Tests per transformation; corpus renders equivalently.\n\n## Test plan\n\n- Unit: per-transformation tests (clip-depth adjustment applied exactly once in coordination with 07.8; `gl_FragCoord` y policy; texture orientation; combined-sampler splitting; flat/integer varyings; attribute normalization; explicit buffer layout; `gl_VertexID`/`gl_InstanceID`/`gl_FrontFacing`/`gl_PointSize`/`gl_FragDepth` mappings; point-sprite and wide-line expansion).\n- E2E: convention scenes (point sprites with `gl_PointCoord`, front-facing toggles, flat varyings, integer attributes, fragment depth writes) rendered in the forced lane on both browsers vs upstream.\n- Logging: per-transformation application events per program and render error metrics; archived under `evidence/15a.4/`.\n- Failure artifacts: WGSL and diff images.\n- Unit additions: fragment-output location mapping incl. MRT; `gl_InstanceID` under nonzero `firstInstance`; every sampler/texture-function table entry; std140 vs WGSL layout fixtures; exactly-one clip-depth conversion per source convention.\n\n## Considerations\n\n- Implicit-derivative sampling under non-uniform control flow is a 15b.1 category (uniformity).\n","acceptance_criteria":"The normalizer applies explicit, tested transformations for every listed difference; sampler splitting and flat varyings compile under WGSL rules; gl_VertexID/gl_InstanceID (base-instance corrected)/gl_FragCoord/gl_FrontFacing/gl_PointSize/gl_FragDepth and the fragment outputs gl_FragColor/gl_FragData[n]/GLSL3 out locations map to correct WGSL builtins, @location outputs or emulations with tests; the sampler/texture-function and std140 mapping tables are implemented and tested; point sprites and wide lines are expanded where upstream's contract requires it.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:48:25.410568Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:13.011112Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-shader","phase-0","phase-1","shaders","tier-solid","wgsl"],"dependencies":[{"issue_id":"f3d-15a-early-shader-spike-7k8.4","depends_on_id":"f3d-15a-early-shader-spike-7k8","type":"parent-child","created_at":"2026-09-09T16:48:25.410568Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15a-early-shader-spike-7k8.4","depends_on_id":"f3d-15a-early-shader-spike-7k8.1","type":"blocks","created_at":"2026-09-09T16:49:10.554748Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-15a-early-shader-spike-7k8.5","title":"Naga dependency admission audit and build-time-only default","description":"## Full scope\n\nNaga dependency admission audit and build-time-only default: exact revision, used API (GLSL frontend entrance, validator, WGSL backend), wasm32 compile result for the optional runtime path, transitive closure, unsafe boundary, incremental bytes, initialization and allocation cost, demonstrated benefit\n\n## Summary\n\nNaga is the preferred narrow compiler dependency; admit it properly and know its runtime cost.\n\n## Background (plan §9.5 \"Naga remains the preferred narrow compiler dependency\", §13.3, §13.4, §17.3 \"Keep debug instrumentation and runtime shader translation dependency-pruned\")\n\n## Technical approach\n\n- Fill the admission record (13.3 fields) for Naga at the pinned revision: exact revision, API used (`front::glsl`, `valid::Validator`, `back::wgsl`), native compile result, wasm32 compile result with `default-features = false` with `glsl-in` and `wgsl-out` (validation needs no feature flag), transitive closure listing, unsafe/FFI boundary (none expected), initialization requirements, incremental Wasm bytes and instantiation time for the optional runtime path, runtime allocation cost, demonstrated benefit (validation + WGSL emission).\n- Build-time default: `f3d-shader` uses Naga natively during `f3d build`; the runtime path is a separate feature enabled by the closure analysis only when reachable dynamic shaders/nodes require it (15b.2/16.2).\n\n## Success criteria\n\n- Record committed; measurements recorded.\n\n## Test plan\n\n- Unit: admission-record completeness test (every §13.3 field present); `cargo tree` allowlist check for Naga's feature set (`glsl-in`, `wgsl-out`, validation only).\n- E2E: wasm32 build of the optional runtime translation path with size and instantiation time measured in the browser (both browsers).\n- Logging: size/init `metric` events `{artifact_bytes, init_ms, browser}`; archived under `evidence/15a.5/`.\n- Failure artifacts: the dependency tree diff.\n\n## Considerations\n\n- Never a per-frame dependency.\n","acceptance_criteria":"The admission record for Naga exists with all §13.3 fields; the build uses Naga natively at build time by default; the optional wasm32 runtime build of the translation path is measured (bytes, init time) so F3D-15b.2 can include it only where reachable source behavior needs it.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:25.507991Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:13.549575Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-shader","phase-0","shaders","tier-solid","toolchain"],"dependencies":[{"issue_id":"f3d-15a-early-shader-spike-7k8.5","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.2","type":"blocks","created_at":"2026-09-09T16:49:10.724110Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15a-early-shader-spike-7k8.5","depends_on_id":"f3d-15a-early-shader-spike-7k8","type":"parent-child","created_at":"2026-09-09T16:48:25.507991Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-15b-full-essl-runtime-shaders-n51","title":"F3D-15 (closure): Full standard ESSL and runtime shader coverage, dynamic hooks, and correctly retained native semantics","description":"## What this epic delivers\n\nPhase 3's shader closure. Plan §19 F3D-15 closure acceptance: \"unseen runtime shaders/hooks and correctly retained native semantics\".\n\n## Why (plan §5.12 \"Legacy and dynamic shaders\", §9.4, §9.5, §9.6, §5.16)\n\n\"ESSL-to-WGSL and runtime variant handling for portable behavior; genuine GL-dependent contracts run on exact GL, never placeholder shaders.\" \"No blanket 'static shaders only' product mode counts as full compatibility.\" \"A portable program cannot be classified as permanently legacy-only just to avoid completing translation.\"\n\n## Children\n\n1. Complete ESSL 1.00/3.00 frontend coverage with uniformity transformations\n2. Runtime shader compiler path and dynamic hooks (onBeforeCompile, mutable chunks, cache keys)\n3. Nonportable GL program routing to the exact route with real native objects\n4. Shader optimization legality and conservative comparison\n5. Shader preprocessing and translation fuzzing\n6. Addon and test shader corpus translation (every examples/jsm shader, pass shader and test shader beyond ShaderLib)\n\n## Success criteria for the epic\n\nAll children closed; the manifest's legacy/dynamic shader family is fully owned and tested; new-backend tests generate shaders after startup and after prior variants executed.\n\n## No-claim boundary\n\nA source whose results depend on undefined GPU behavior is not an exact cross-backend reference (§9.4); such cases are documented, not \"fixed\".\n","acceptance_criteria":"Every official shader (ShaderLib/ShaderChunk/uniform libraries and all addon shaders), every valid ESSL 1.00/3.00 program a source application can produce at runtime, onBeforeCompile hooks, mutable chunk/lib data, custom cache keys and program diagnostics work through the new backend with uniformity transformations; genuinely GL-dependent contracts route to the exact backend with real native objects; shader translation is fuzzed.","status":"open","priority":2,"issue_type":"epic","created_at":"2026-09-09T16:46:23.666970Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:12.293159Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-shader","phase-3","shaders","tier-solid","wgsl"],"dependencies":[{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T16:49:12.884039Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51","depends_on_id":"f3d-15a-early-shader-spike-7k8","type":"blocks","created_at":"2026-09-09T16:49:12.701321Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-15b-full-essl-runtime-shaders-n51.1","title":"Complete ESSL 1.00/3.00 frontend coverage with uniformity transformations","description":"## Full scope\n\nComplete ESSL 1.00/3.00 frontend coverage: every official ShaderLib/ShaderChunk program and addon shader with all defines/includes; WGSL uniformity transformations (implicit-derivative texture sampling under non-uniform control flow as an explicit test category); point-size/line expansion completion; documented undefined-behavior cases\n\n## Summary\n\nFrom the H2 corpus to everything.\n\n## Background (plan §9.4, §5.16)\n\n\"ESSL-valid code may still require substantial transformation to satisfy WGSL's uniformity and resource rules. Implicit-derivative texture sampling under nonuniform control flow is an explicit test category. Silencing a uniformity diagnostic is not proof that behavior became portable; either establish an equivalent legal transformation or retain the genuinely required original backend. An unimplemented transformation of defined portable behavior remains a new-backend coverage gap, not an automatic intrinsic exception.\"\n\n## Technical approach\n\n- Corpus generation from the manifest: every ShaderLib program × its define combinations reachable from material options (bounded via pairwise where combinatorial), plus `UniformsGroup`/std140 UBO programs and integer-texture, array/3D and shadow-sampler application programs. The addon shader corpus (`examples/jsm/shaders/*`, postprocessing shaders, `Line2`, `Water`, `Sky`, etc.) and the unit-test shaders are 15b.6, so that 14.1 (built-in materials) depends only on the ShaderLib subset.\n- Uniformity: analysis on the AST; transformations such as hoisting derivative-dependent sampling with explicit gradients (`textureSampleGrad`) computed in uniform control flow, or `textureSampleLevel` where the source semantics permit (documented per case); otherwise mark the program's record with the required native evidence.\n- Undefined behavior register: cases where the source depends on UB (e.g., uninitialized varyings) documented with the envelope.\n\n## Success criteria\n\n- Corpus green with per-program status; uniformity category tests pass; register committed.\n\n## Test plan\n\n- Unit: golden WGSL for every ShaderLib program × reachable define combination (bounded pairwise where combinatorial), every addon shader and every unit-test shader; uniformity-transformation tests (implicit-derivative sampling under non-uniform control flow → explicit-gradient or level sampling with the documented equivalence) and a test that a silenced-but-untransformed program is rejected; undefined-behavior register entries validated.\n- E2E: differential rendering of the corpus (all ShaderLib programs; sampled addon shaders; full uniformity category) in the forced lane on both browsers vs upstream.\n- Logging: per-program status `{program, defines, translated|gap|native-evidenced, uniformity_transform}` feeding the manifest; render error metrics; archived under `evidence/15b.1/`.\n- Failure artifacts: WGSL, diagnostics and diff images.\n\n## Considerations\n\n- Budget watch: this is the largest uncertainty; report the forecast continuously (§17.1).\n","acceptance_criteria":"Every ShaderLib/ShaderChunk program with every define combination reachable from material options, plus UniformsGroup (std140 UBO), integer-texture, array/3D and shadow-sampler program kinds, translates, validates and renders equivalently through the new backend; programs requiring uniformity transformations are transformed by an established equivalent legal transformation (or retained on the genuinely required original backend with evidence) rather than silenced; any remaining gap is a new-backend coverage gap in the manifest, never an intrinsic exception. The addon and test shader corpus is 15b.6.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:25.604464Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:11.142686Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-shader","phase-3","shaders","tier-solid","wgsl"],"dependencies":[{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.1","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T16:49:11.441103Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.1","depends_on_id":"f3d-15a-early-shader-spike-7k8.2","type":"blocks","created_at":"2026-09-09T16:49:11.261547Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.1","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51","type":"parent-child","created_at":"2026-09-09T16:48:25.604464Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-15b-full-essl-runtime-shaders-n51.2","title":"Runtime shader compiler path and dynamic hooks (onBeforeCompile, mutable chunks, cache keys)","description":"## Full scope\n\nRuntime shader compiler path and dynamic hooks: ShaderMaterial/RawShaderMaterial with runtime-generated strings, onBeforeCompile, mutable ShaderChunk/ShaderLib/UniformsLib data, custom cache keys, program errors/diagnostics, program-generation-after-startup tests; the runtime translator is included automatically when reachable with its load/compile cost measured\n\n## Summary\n\nDynamic programs are required functionality, not a diagnostic.\n\n## Background (plan §9.5, §10.2)\n\n\"Preserve `onBeforeCompile`, mutable ShaderChunk/ShaderLib/uniform data, custom cache keys, runtime shader strings, program errors/diagnostics, node registrations and custom builders. Execute hooks at the original logical boundaries and retain exact ordering, current uniforms and effects... Never keep rendering an old shader, a placeholder, a simpler material or a constant node while reporting success. New-backend tests generate graphs/shaders after startup and after prior variants have executed.\"\n\n## Technical approach\n\n- Runtime translator: the ESSL pipeline compiled to wasm32 (15a.5 measurement) or a JS build of the first-party normalizer plus Naga-wasm; selected by size/latency; loaded lazily but included whenever the closure analysis finds a reachable dynamic shader path (10.7 policy).\n- Hook execution: `onBeforeCompile(shader, renderer)` receives the same mutable `shader` object shape upstream provides (vertexShader/fragmentShader strings, uniforms, defines); after the hook the result is translated; `customProgramCacheKey` respected.\n- Mutable chunk data: translation reads the live `ShaderChunk`/`ShaderLib` tables at compile time.\n- Error contract: compile errors produce the same console/diagnostic behavior as upstream's WebGL path where the source observes it, plus F3D diagnostics.\n- Program cache key and lifecycle exactly as upstream: the key includes `getProgramCacheKey`'s inputs, `customProgramCacheKey()` and `onBeforeCompile.toString()`; `material.needsUpdate` and `defines` mutation trigger recompilation; programs are refcounted and released on `material.dispose()` (`WebGLPrograms.releaseProgram` semantics); tests for each.\n\n## Success criteria\n\n- Tests for runtime strings, hooks, chunk mutation, cache keys, errors; latency measured and reported.\n\n## Test plan\n\n- Unit: hook-execution tests (`onBeforeCompile` receives the upstream `shader` object shape; `customProgramCacheKey` respected; live ShaderChunk/ShaderLib mutation observed at compile time); error-contract tests (compile error reproduces upstream's diagnostic shape plus the F3D report); runtime translator packaging test (included iff reachable).\n- E2E: `tests/e2e/shaders/runtime.mjs` on both browsers: runtime-generated shader strings after startup and after prior variants executed; chunk mutation mid-run; hook ordering traces vs upstream; translator load/compile latency measured.\n- Logging: runtime compile events `{program, trigger, translate_ms, compile_ms, bytes_loaded}` for the interaction-tail report; archived under `evidence/15b.2/`.\n- Failure artifacts: the runtime shader source, WGSL and diagnostics.\n\n## Considerations\n\n- Its cost belongs to interaction-tail measurements (§9.5).\n","acceptance_criteria":"An application that generates shader strings at runtime or mutates chunks/hooks works on the new backend with the translator included by the closure analysis; hooks execute at the original logical boundaries with current uniforms and effects; program errors reproduce upstream's error/diagnostic contract plus F3D's separate generated-source report; tests generate new programs after startup and after prior variants executed; the translator's bytes and compile latency are reported.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:25.701031Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:13.377023Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-runtime","f3d-shader","phase-3","shaders","tier-solid"],"dependencies":[{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.2","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.7","type":"blocks","created_at":"2026-09-09T16:49:11.797771Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.2","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51","type":"parent-child","created_at":"2026-09-09T16:48:25.701031Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.2","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51.1","type":"blocks","created_at":"2026-09-09T16:49:11.629952Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-15b-full-essl-runtime-shaders-n51.3","title":"Nonportable GL program routing to the exact route with real native objects","description":"## Full scope\n\nNonportable GL program routing: genuinely GL-dependent programs (native shader-object callbacks, GL-specific extensions, program-handle observations) execute on the exact GL route with real native objects for error callbacks; portable programs are never classified legacy-only\n\n## Summary\n\nThe honest boundary between \"hard to translate\" and \"genuinely native\".\n\n## Background (plan §9.5 last two paragraphs, §5.16, §14.10)\n\n\"A nonportable GL program or native shader-object callback uses the exact GL route under Section 6.9 rather than a failed feature. A portable program cannot be classified as permanently legacy-only just to avoid completing translation.\" \"Any intrinsic native-backend exception names the concrete observable contract and its real compatible implementation; it cannot be a catch-all category for unfinished rendering.\"\n\n## Technical approach\n\n- Extend the native-contract detection (12a.6) with shader-level observations: `renderer.debug.onShaderError` callbacks expecting `WebGLProgram`/`WebGLShader`, `gl.getProgramInfoLog`/`getShaderSource` use, `#extension` directives for GL-only extensions with no WGSL equivalent, direct `WebGLProgram` handle observation through the diagnostic APIs.\n- Programs with those observations route their renderer group exactly (04.4); the WebGL error callbacks receive real native objects because the exact component compiles the original GLSL.\n- Manifest: each exact record names the concrete observable contract; the release verifier rule (18a.5) rejects any exact record without one, so \"hard to translate\" can never be recorded as \"native-bound\".\n\nPrior notes retained:\n- Extend 12a.6's native-contract detection to shader-level observations (e.g., `renderer.debug.onShaderError` receiving `WebGLProgram`, `gl.getProgramInfoLog` use, GL-only extensions referenced in `#extension`).\n- Manifest field for the named contract; the release verifier (18a.5) rejects any exception without it.\n\n## Success criteria\n\n- Fixtures route correctly; verifier rule tested.\n\n## Test plan\n\n- Unit: native-contract detection fixtures at shader level (`renderer.debug.onShaderError` receiving `WebGLProgram`, `getProgramInfoLog` use, GL-only `#extension`); verifier-rule test that an exception without a named contract is rejected.\n- E2E: exact-lane tests on both browsers where the error callback receives real native objects; a portable program with a translation gap is asserted to remain a new-backend gap in the manifest (not reclassified).\n- Logging: route decisions with the named contract; archived under `evidence/15b.3/`.\n- Failure artifacts: the decision log.\n\n## Considerations\n\n- Exact-route execution never counts toward new-backend coverage.\n","acceptance_criteria":"The route analysis identifies programs whose semantics require the original GL backend (documented per concrete observable contract) and routes their renderer group exactly; WebGL error callbacks receive real native objects; every such record in the manifest names the observable requirement; portable programs with translation difficulties remain new-backend gaps.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:25.807165Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:25.060221Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-compiler","f3d-shader","phase-3","shaders","tier-solid"],"dependencies":[{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.3","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.4","type":"blocks","created_at":"2026-09-09T16:49:12.153975Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.3","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51","type":"parent-child","created_at":"2026-09-09T16:48:25.807165Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.3","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51.1","type":"blocks","created_at":"2026-09-09T16:49:11.981055Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-15b-full-essl-runtime-shaders-n51.4","title":"Shader optimization legality and conservative comparison","description":"## Full scope\n\nShader optimization legality: default-allowed transformations (constant propagation, dead-code elimination, inactive-feature specialization, equivalent resource access simplification) vs separately justified ones (floating-point reassociation, reduced precision, changed filtering, changed sample counts, algorithm substitution); a conservative generation path retained and every optimization compared against it\n\n## Summary\n\nDo not turn shader optimization into approximation.\n\n## Background (plan §9.6, §5.4)\n\n\"Allowed default transformations include constant propagation, dead code elimination, specialization of inactive material features, and equivalent resource access simplifications. Floating-point reassociation, reduced precision, changed filtering, changed sample counts, and numerical algorithm substitutions require separate justification. Keep a conservative shader-generation path and compare every optimization against it.\"\n\n## Technical approach\n\n- Pass classification table in `f3d-shader` docs: allowed by default (constant propagation, dead-code elimination, specialization of inactive material features, equivalent resource-access simplification); requires separate justification (floating-point reassociation, reduced precision, changed filtering, changed sample counts, algorithm substitution).\n- Implementation: each optimizer pass declares its class; the pipeline refuses to enable a requires-justification pass unless the build is in the separately labeled optional mode, and the report labels the mode.\n- Conservative path: keep the unoptimized emission for every program; differential tests render both with 14.9 diagnostics over the corpus and assert equivalence for allowed passes.\n- Any interaction with 17.3 (fusion) and 20.3 (FP16) follows the same classification.\n\n## Success criteria\n\n- Table complete; tests green; optional mode clearly labeled in reports and never in the primary score.\n\n## Test plan\n\n- Unit: pass classification table test (each optimizer pass is allowed-by-default or requires-justification); per-pass differential tests against the conservative generation path over the corpus with 14.9 diagnostic targets; optional-mode labeling test in the report.\n- E2E: forced-lane renders with each allowed pass on/off comparing to the conservative path on both browsers.\n- Logging: per-pass `metric` events `{pass, program, max_err}`; archived under `evidence/15b.4/`.\n- Failure artifacts: diff images per pass.\n\n## Considerations\n\n- FP16 usage is governed by the precision contract (F3D-20.3).\n","acceptance_criteria":"The shader optimizer's pass list is classified into allowed-by-default and requires-justification; the latter are disabled in the primary comparison and available only in a separately labeled optional mode; each enabled pass has a differential test against the conservative path over the shader corpus.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:25.905997Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:19.040978Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-shader","phase-3","shaders","tier-solid"],"dependencies":[{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.4","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51","type":"parent-child","created_at":"2026-09-09T16:48:25.905997Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.4","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51.1","type":"blocks","created_at":"2026-09-09T16:49:12.337784Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-15b-full-essl-runtime-shaders-n51.5","title":"Shader preprocessing and translation fuzzing","description":"## Full scope\n\nShader preprocessing and translation fuzzing: grammar-aware fuzz targets for the ESSL preprocessor, parser, normalizer and WGSL emitter with validation as the oracle; no panics, bounded time and memory, structured errors for invalid input\n\n## Summary\n\nShader inputs are application-controlled; the translator must be robust.\n\n## Background (plan §14.7 \"Fuzz... shader preprocessing/translation\")\n\n\"Shader input validation and resource/dispatch limits reduce exposure, but do not claim they prove every GPU program terminates within a hard deadline.\"\n\n## Technical approach\n\n- Targets in `crates/f3d-shader/fuzz/`: `preprocess` (macro/include/conditional inputs), `parse` (ESSL 1.00/3.00 source), `normalize` (parsed AST → stage interfaces), `emit` (normalized program → WGSL then Naga validate); each target rejects invalid input with a structured error and never panics or exceeds the time/memory limits.\n- Mutator: a small grammar-aware mutator (token-level swaps/insertions from an ESSL keyword table plus byte-level fallbacks) seeded with the 15b.1 corpus (every ShaderLib/addon/test shader) and the H2 legacy programs.\n- Lane: a fixed-budget run (for example 30 minutes per target) via `cargo-fuzz` on RCH or the build machine; findings are fixed at the root cause and the minimized inputs are added to the regression corpus checked into the crate's tests.\n\n## Success criteria\n\n- Fuzz lane green for the budget; findings fixed at root cause.\n\n## Test plan\n\n- Unit/fuzz: `cargo-fuzz` targets for the preprocessor, parser, normalizer and WGSL emitter with a grammar-aware mutator, time and memory limits, and Naga validation as the oracle; seed corpus from 15b.1.\n- E2E: none (fuzz lane; may run on RCH).\n- Logging: fuzz lane summary `{target, executions, crashes, timeouts, corpus_size}`; archived under `evidence/15b.5/`.\n- Failure artifacts: minimized crashing inputs.\n\n## Considerations\n\n- Keep it proportional; no fuzzing framework beyond the standard tool.\n","acceptance_criteria":"Fuzz targets run for a fixed budget without crashes, hangs or unbounded memory; every produced WGSL either validates under Naga or the pipeline reports a structured error; a checked-in corpus seeds the fuzzer from the real shader inventory.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:25.998267Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:11.663947Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-shader","phase-3","shaders","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.5","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51","type":"parent-child","created_at":"2026-09-09T16:48:25.998267Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.5","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51.1","type":"blocks","created_at":"2026-09-09T16:49:12.520762Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.5","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51.6","type":"blocks","created_at":"2026-09-09T17:44:11.663600Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-15b-full-essl-runtime-shaders-n51.6","title":"Addon and test shader corpus translation (every examples/jsm shader, pass shader and test shader beyond ShaderLib)","description":"## Full scope\n\nAddon and test shader corpus translation: every shader under `examples/jsm/shaders/*`, every postprocessing pass shader, `Line2`/`LineMaterial`, `Water`, `Sky`, reflectors/refractors, GPGPU examples, and every shader embedded in the pinned unit tests and examples, translated through the ESSL pipeline with golden WGSL and differential rendering, so the full official shader inventory (plan §5.12 legacy shaders row) is closed beyond the ShaderLib subset owned by 15b.1.\n\n## Summary\n\n15b.1 closes ShaderLib/ShaderChunk (what built-in materials need, so 14.1 can proceed). This bead closes the rest of the official shader inventory: addon and example/test shaders, which are numerous, use more ESSL surface (custom uniforms, integer attributes, unusual builtins), and feed the postprocessing family (17.1).\n\n## Background (plan §5.12 \"Legacy and dynamic shaders\", §9.4, §9.5, §14.9)\n\n\"Every official shader\" must have a working path through the new backend; a portable program can never be classified legacy-only to avoid completing translation; unseen runtime shaders are 15b.2's obligation and use the same pipeline.\n\n## Technical approach\n\n- Enumerate the corpus from the manifest: every module exporting shader source under `examples/jsm/shaders/`, every `Pass` subclass's embedded shaders, addon materials with custom GLSL (`Line2`, `Water`, `Sky`, `Reflector`/`Refractor`, GPGPU examples, `ShaderMaterial` uses in examples), and shaders embedded in the pinned unit tests.\n- Translate each with the 15a.1/15b.1 pipeline (program assembly, uniformity transformations, sampler/builtin mapping); golden WGSL snapshots; Naga validation; differential rendering against upstream for the examples that use them (sampled where large, all for postprocessing shaders because 17.1 depends on them).\n- Status per program in the manifest: translated / gap / native-evidenced (with the named contract per 15b.3).\n\n## Success criteria\n\n- Corpus green with per-program status; every postprocessing shader translated before 17.1 starts; gaps recorded as new-backend coverage gaps.\n\n## Test plan\n\n- Unit: golden WGSL per program; validation under Naga; per-transformation tests reused from 15a.4/15b.1.\n- E2E: differential renders of the examples using these shaders in the forced-new-backend lane on Chrome and Safari; postprocessing shaders rendered through minimal composer scenes.\n- Logging: per-program `{program, module, status, uniformity_transform, max_err}` feeding the manifest; archived under `evidence/15b.6/`.\n- Failure artifacts: WGSL, diagnostics and diff images.\n\n## Considerations\n\n- Fuzz seeds (15b.5) come from this corpus as well as 15b.1's.\n","acceptance_criteria":"Every addon, pass, example and unit-test shader in the manifest translates, validates and renders equivalently through the new backend or is recorded as a new-backend coverage gap (never an intrinsic exception without a named contract); postprocessing shaders are complete before 17.1 starts.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T17:44:11.268657Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:11.441869Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-shader","phase-3","shaders","tier-solid","wgsl"],"dependencies":[{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.6","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51","type":"parent-child","created_at":"2026-09-09T17:44:11.268657Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-15b-full-essl-runtime-shaders-n51.6","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51.1","type":"blocks","created_at":"2026-09-09T17:44:11.441574Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-16-tsl-nodes-compute-contracts-f0v","title":"F3D-16: Complete TSL/node/backend extension and compute contracts","description":"## What this epic delivers\n\nClosure of the TSL/node/compute family. Plan §19 F3D-16 depends on 14 and 15; acceptance: \"Every standard node/general dynamic path; native custom backend routing; callback frequencies\".\n\n## Why (plan §5.12 \"TSL, nodes and compute\", §9.3, §8.7, §12.3, §15.3 H7/H8)\n\n\"Complete general lowering and pinned construction components, dynamic graphs included; preserve native extension routes; test unseen runtime graphs and source-valid errors.\" H7/H8 are the modern-compute headline cells whose difficulty \"is part of the project's go/no-go test\".\n\n## Children\n\n1. Every standard node export lowering with NodeFrame frequencies and the context stack.\n2. Dynamic graph construction runtime lowering and registration.\n3. Compute contracts.\n4. Native custom backend routing.\n5. H7 end-to-end route.\n6. H8 end-to-end route.\n\n## Success criteria for the epic\n\nAll children closed; the node family in the manifest is fully owned and tested; H7/H8 measured (results published regardless of outcome).\n\n## No-claim boundary\n\n\"Simply relocating JavaScript renderer code cannot be assumed to make [H8] 3× faster\" (§15.3).\n","acceptance_criteria":"Every standard node export, NodeMaterial/loader/serialization/registration path, Fn/flow/control/type/operator, shader builder and WGSL/GLSL function path, every update frequency, and all storage/atomics/indirect/query/feature-gated compute operations have general lowering through the new backend; dynamic graph construction and custom node registration work at runtime; opaque custom backends route exactly; H7 and H8 have measured end-to-end routes.","status":"open","priority":2,"issue_type":"epic","created_at":"2026-09-09T16:46:23.751645Z","created_by":"jemanuel","updated_at":"2026-09-09T16:49:15.652313Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-shader","h7","h8","new-backend","phase-3","tier-frontier","tier-solid"],"dependencies":[{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v","depends_on_id":"f3d-14-material-light-texture-surface-5lj","type":"blocks","created_at":"2026-09-09T16:49:15.462789Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51","type":"blocks","created_at":"2026-09-09T16:49:15.651996Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-16-tsl-nodes-compute-contracts-f0v.1","title":"Every standard node export lowering with NodeFrame frequencies and context stack","description":"## Full scope\n\nEvery standard node export lowering: all three/tsl node classes and functions (math/operators/flow control/Fn/types/accessors/lighting/material/texture/display/utils/parsers), NodeMaterial variants, NodeLoader/serialization, node registration; NodeFrame update frequencies for all nodes; reentrant context stack; retained pinned construction helpers where smaller and disclosed\n\n## Summary\n\nGeneralize 07.5/15a.3 to the complete node inventory.\n\n## Background (plan §5.12 \"TSL, nodes and compute\", §9.3)\n\n\"All existing standard nodes and portable custom node/registration behavior need working general lowering... Retaining pinned, cold TSL construction helpers or build-time shader-generation components is permitted when smaller and more reliable than rewriting them. That reuse is independent of the separately permitted exact backend component. New-backend validation and scored frames still forbid retained renderer submission.\"\n\n## Technical approach\n\n- Translator coverage driven by the manifest's node list; for each node: semantic transcription into shader IR or a documented reliance on the retained node builder's generated code (attributed as retained shader construction with byte cost).\n- NodeLoader/serialization tests (`toJSON`/`fromJSON` of node materials).\n- Registration APIs preserved via retained construction modules: the registration APIs as present in the pinned r186 source per the census (for example `addMethodChaining` and the node-class registration `NodeLoader` uses; names are never taken from memory) plus custom `Node` subclasses.\n- Function nodes: `wgslFn`/WGSL `code` bodies pass through Naga validation; `glslFn`/GLSL `code` bodies are translated as functions through the ESSL pipeline (15b.1) into WGSL functions with the same signature; both are tested with unseen bodies.\n\n## Success criteria\n\n- Family suite green; coverage report of translated vs retained-builder nodes.\n\n## Test plan\n\n- Unit: translator entry tests per node export (or an explicit retained-builder record with byte cost); NodeMaterial subclass and NodeLoader `toJSON`/`fromJSON` round-trip tests; NodeFrame frequency and context-stack tests for every node kind; registration API tests (the pinned registration APIs, method chaining, custom `Node` subclasses).\n- E2E: generated node family suite in the forced lane on both browsers vs upstream's WebGPU renderer over the node examples; translated-vs-retained coverage report asserted complete.\n- Logging: per-node `{node, translated|retained-builder, test}` feeding the manifest; render error metrics; archived under `evidence/16.1/`.\n- Failure artifacts: IR/WGSL dumps and diff images.\n\n## Considerations\n\n- Graph-construction callbacks remain JavaScript unless admitted (§9.3).\n","acceptance_criteria":"Every node export in the manifest's TSL family has a translator entry (or a disclosed retained-builder fallback that still submits through the new backend) with a positive test; NodeMaterial subclasses and NodeLoader round trips work; update frequencies and the context stack hold for every node kind; the node family tests run in the forced lane.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:26.095183Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:13.726096Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-shader","new-backend","phase-3","tier-solid","wgsl"],"dependencies":[{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.1","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.5","type":"blocks","created_at":"2026-09-09T16:49:13.065123Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.1","depends_on_id":"f3d-14-material-light-texture-surface-5lj.1","type":"blocks","created_at":"2026-09-09T16:49:13.439821Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.1","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51.1","type":"blocks","created_at":"2026-09-09T16:49:13.253665Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.1","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v","type":"parent-child","created_at":"2026-09-09T16:48:26.095183Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-16-tsl-nodes-compute-contracts-f0v.2","title":"Dynamic node graph construction runtime lowering and registration","description":"## Full scope\n\nDynamic graph construction runtime lowering: unseen runtime node graphs, custom node registration and builders at runtime, source-valid errors preserved; never substitute a constant or silently retain an old graph; runtime lowering machinery included automatically when reachable with cost reported\n\n## Summary\n\nThe node-side twin of 15b.2.\n\n## Background (plan §9.3 last paragraph, §9.5)\n\n\"Genuinely dynamic graph construction requires the needed runtime lowering machinery. Every reachable material/control variant in a claimed standalone application must have a valid path: included ahead-of-time code or a disclosed runtime compiler. A shader seen only in a training trace is not the whole variant set.\"\n\n## Technical approach\n\n- Runtime lowering: package the node translator (16.1) and the shader emission path for wasm32 (or JS, whichever 15a.5's measurement favors) as a lazily loaded module; the closure analysis (10.7) includes it when the ingestion graph shows runtime node construction, custom `Node` subclasses, the pinned registration APIs/method chaining at runtime, or `NodeLoader` deserialization of unseen graphs.\n- Registration: retained construction modules keep the registration APIs; the translator consults the live registry at lowering time so runtime-registered nodes are recognized; unrecognized custom nodes fall back to the retained builder's generated code (disclosed) and never to a constant or a stale program.\n- Errors: invalid graphs produce upstream's error behavior (thrown errors/console diagnostics) plus F3D's generated-source report.\n- Cost: bytes and translate/compile latency reported in the interaction-tail metrics.\n\n## Success criteria\n\n- Runtime graph tests green; report shows inclusion and cost.\n\n## Test plan\n\n- Unit: runtime-lowering tests for graphs constructed after startup and for custom node classes registered at runtime; invalid-graph error tests reproducing upstream's error behavior; inclusion-rule test (runtime lowering machinery included iff dynamic construction is reachable).\n- E2E: `tests/e2e/nodes/runtime.mjs` on both browsers: new graphs constructed after prior variants executed render correctly through the new backend; bytes/latency of the runtime path measured.\n- Logging: runtime lowering events `{graph, nodes, translate_ms, compile_ms}` and inclusion decision in the report; archived under `evidence/16.2/`.\n- Failure artifacts: the graph dump and diagnostics.\n\n## Considerations\n\n- A missing custom-node adapter is not a full-product feature exemption (§9.3).\n","acceptance_criteria":"Applications that construct new node graphs after startup (including custom node classes registered at runtime) render them through the new backend; invalid graphs produce upstream's error behavior; tests construct graphs after prior variants executed; the runtime lowering path's bytes/latency are reported.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:26.190904Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:13.908137Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-runtime","f3d-shader","new-backend","phase-3","tier-solid"],"dependencies":[{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.2","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51.2","type":"blocks","created_at":"2026-09-09T16:49:13.816333Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.2","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v","type":"parent-child","created_at":"2026-09-09T16:48:26.190904Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.2","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.1","type":"blocks","created_at":"2026-09-09T16:49:13.631926Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-16-tsl-nodes-compute-contracts-f0v.3","title":"Compute contracts: storage/atomics/indirect/queries, dispatch splitting, bounds, subgroups, FP16 gating","description":"## Full scope\n\nCompute contracts: storage buffers/textures, atomics, indirect dispatch, queries and feature-gated operations; multi-dispatch dependencies with correct synchronization (workgroup barriers do not synchronize a grid); bounds checks and workgroup-tail handling for logical array domains; reduction-order and atomic-ordering contracts; subgroup algorithms with a portable path and partial-group handling; FP16 only under the precision contract; optional immediates as a capability-checked strategy\n\n## Summary\n\nGPU compute done to the plan's rules.\n\n## Background (plan §8.7, §12.3, §5.12 \"TSL, nodes and compute\")\n\n\"A compute shader with global dependencies often needs multiple dispatches. Fusing them without a valid synchronization model is incorrect. Workgroup barriers do not synchronize an entire grid. Bounds checks and workgroup-tail handling preserve logical array domains, including overflow/empty cases; WebGPU robustness is not a substitute for generating the right computation. Do not use nondeterministic atomic ordering or a different reduction tree without satisfying the admitted numeric contract.\" \"Subgroup algorithms need a portable path and must handle partial groups correctly. FP16 is enabled only when the application's precision contract permits it.\"\n\n## Technical approach\n\n- Compute passes in the pass graph (07.2) with per-dispatch usage scopes; storage buffers/textures, atomics, indirect dispatch args (08.6 layout), timestamp/occlusion queries where supported, feature-gated operations from the capability record.\n- Dependency splitter: analyze storage accesses across a node/TSL compute graph; when a later stage reads what an earlier stage writes across workgroups, emit separate dispatches (a workgroup barrier never synchronizes the grid); a test fixture with an invalid fusion must diverge from the CPU reference.\n- Bounds: generated `if (index < logical_size)` guards and workgroup-tail handling from the logical domain, not from buffer sizes; overflow/empty-domain cases tested; WebGPU robustness is not relied upon.\n- Numeric contract: reductions use a fixed tree/order; atomics map 1:1; any relaxed ordering or FP16 requires the admitted contract (20.3) and the optional-mode label.\n- Subgroups: capability-gated kernels with a portable fallback and partial-group handling; immediates as an optional strategy.\n\nPrior notes retained:\n- Compute passes in the pass graph with per-dispatch usage scopes (07.2); dependency analysis on storage accesses to split dispatches; generated bounds checks from logical sizes; reduction templates with fixed order; atomics mapped 1:1 to WGSL atomics.\n- Capability gating for subgroups/FP16/immediates (from the record; requested at device creation).\n- TSL workgroup/subgroup primitives and compute builtins map to their WGSL counterparts with identical (intra-workgroup) scope: `workgroupArray`/`var<workgroup>`, `workgroupBarrier`, `storageBarrier`, `textureBarrier`, subgroup operations, and the compute builtins (`instanceIndex`, `globalId`, `localId`, `workgroupId`, `numWorkgroups` → `@builtin(global_invocation_id)` and friends); each has a positive test.\n\n## Success criteria\n\n- Family suite green; invalid-fusion divergence test; tail/empty-domain tests; capability-gating tests on strata with/without features.\n\n## Test plan\n\n- Unit: dependency-splitter tests (a kernel with global dependencies is split into the required dispatches; an invalid fusion fixture diverges from the CPU reference); generated bounds-check tests for logical array domains including overflow/empty cases; reduction-order and atomic tests against the numeric contract; subgroup portable-fallback and partial-group tests; capability-gating tests for FP16/immediates/subgroups.\n- E2E: compute family suite in the forced lane on both browsers (storage buffers/textures, atomics, indirect dispatch, queries, feature-gated operations) with readback comparison vs upstream; the divergence test for invalid fusion; the `webgpu_compute_cloth` fidelity case via `f3d verify` (compute dependencies interacting with simulation state); strata with and without optional features.\n- Logging: per-dispatch events `{kernel, workgroups, splits, features_used}` and readback diffs; archived under `evidence/16.3/`.\n- Failure artifacts: readback dumps and the dispatch plan.\n\n## Considerations\n\n- Moving CPU-authored f64 simulation to f32 compute needs a separately admitted precision contract (§8.7).\n","acceptance_criteria":"The compute node/API family renders/executes through the new backend with tests; a kernel with global dependencies is split into the required dispatches (a test proves an invalid fusion diverges); logical array bounds are enforced in generated code (not by relying on robustness); reductions and atomics follow the admitted numeric contract; subgroup features are used only when present with a portable fallback; FP16 and immediates are gated by capability and contract.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:26.289892Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:14.083622Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-graph","f3d-shader","new-backend","phase-3","tier-solid"],"dependencies":[{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.3","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago.6","type":"blocks","created_at":"2026-09-09T16:49:14.176400Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.3","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v","type":"parent-child","created_at":"2026-09-09T16:48:26.289892Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.3","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.1","type":"blocks","created_at":"2026-09-09T16:49:13.996561Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-16-tsl-nodes-compute-contracts-f0v.4","title":"Native custom backend routing (Backend/Renderer subclasses to the exact component)","description":"## Full scope\n\nNative custom backend routing: legitimate custom Backend/Renderer subclasses and opaque native-handle contracts route to the exact component with actual device/resource objects; name-based shims forbidden\n\n## Summary\n\nPreserve the extension points that make Three.js a platform.\n\n## Background (plan §5.6 second paragraph, §5.12 \"Modern renderer/backend APIs\")\n\n\"Backend-preserving execution also covers legitimate custom `Backend`/`Renderer` subclasses, source-path integrations, runtime shader/backend extensions... For native WebGPU contracts preserve actual device/resource objects through the appropriate component; do not assume arbitrary subclasses can be reproduced by a name-based shim.\"\n\n## Technical approach\n\n- Router rule (04.4/12a.6): any `Backend` or `Renderer` subclass not fingerprinted as the pinned standard classes, any explicit `WebGLBackend` selection, and any observation of native device/resource handles (`renderer.backend.device`, native textures) selects the exact component from construction; the group stays exact for the renderer's lifetime.\n- Exact machinery for WebGPU-native contracts: the pinned upstream `Renderer` + the application's own backend subclass run unmodified with real `GPUDevice`/`GPUTexture` objects; for WebGL-backend subclasses the exact GL component (04.3) hosts them.\n- Manifest: each such contract record names the observable requirement; the build report lists the group and reason.\n\nPrior notes retained:\n- Router rule: unknown subclass of the pinned backend/renderer classes → exact component (pinned `Renderer` + the subclass's own backend), with the new backend unused for that group; report the reason.\n\n## Success criteria\n\n- Fixture tests pass; report entries correct.\n\n## Test plan\n\n- Unit: router-rule tests (unknown `Backend`/`Renderer` subclass → exact component; explicit `WebGLBackend` → exact; report reasons).\n- E2E: custom WebGL backend subclass and custom WebGPU backend subclass fixtures run through the exact machinery on both browsers with real native objects asserted; manifest records updated.\n- Logging: route decisions with reasons; archived under `evidence/16.4/`.\n- Failure artifacts: the decision log and the fixture's console capture.\n\n## Considerations\n\n- These groups never count toward accelerated coverage.\n","acceptance_criteria":"Applications subclassing Backend/Renderer or injecting custom backends run through the pinned exact machinery with real native objects; the route decision is made at construction; tests cover a custom WebGL backend subclass and a custom WebGPU backend subclass; the manifest records each such contract.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:26.389289Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:21.536263Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-compiler","f3d-runtime","phase-3","tier-solid"],"dependencies":[{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.4","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.4","type":"blocks","created_at":"2026-09-09T16:49:14.535276Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.4","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v","type":"parent-child","created_at":"2026-09-09T16:48:26.389289Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.4","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.1","type":"blocks","created_at":"2026-09-09T16:49:14.364643Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-16-tsl-nodes-compute-contracts-f0v.5","title":"H7 end-to-end route (compute skinning instancing individual) and measured throughput","description":"## Full scope\n\nH7 (webgpu_skinning_instancing_individual) end-to-end route: per-instance skeleton/morph storage-buffer data and custom TSL compute skinning through the new backend; automatic transformation, equivalence, and measured completed-frame throughput; candidate gain from pose/update specialization and cross-pass data reuse\n\n## Summary\n\nThe first modern-compute headline cell.\n\n## Background (plan §15.2 H7, §15.3, §16.7 \"Generalization gate\")\n\n\"H7 already computes instanced deformation through storage-buffer data and custom TSL, with per-instance skeleton/morph information. It is a serious compatibility and performance test, not a simple crowd rendered through separate naïve draw calls.\" Generalization gate requires \"a measured end-to-end route for H7/H8\".\n\n## Technical approach\n\n- Build H7 (`webgpu_skinning_instancing_individual`) with `f3d build`; the report must show the storage-buffer skinning path lowered through the node/compute contracts (16.1/16.3), per-instance skeleton/morph data retained in storage buffers, shadows through 14.3, and any refusal with a span.\n- References (03.3): R0 (the upstream WebGPU renderer is already modern; R2 coincides), R1 with the bounded recipe where applicable; calibrate and freeze before candidate runs.\n- `f3d verify` on both browsers including shadows and per-instance variation; then `f3d bench` paired cells on the reserved M5, phone and a discrete-GPU stratum when available; ablations for pose caching and cross-pass reuse.\n- Publish per-stratum results with intervals; a result below 2x is published as such and feeds G4/23.2 unchanged.\n\nPrior notes retained:\n- Pipeline as 11.1; reference configurations (R0/R2 are the same modern renderer; R1 recipe applied where applicable) frozen; `f3d bench` cell; specialization ideas: pose caching by (clip,time) sharing across instances where the source's per-instance data permits, reuse of skinned outputs across shadow and main passes (legal only if the pass graph proves identical inputs).\n\n## Success criteria\n\n- Results document; feeds G4 and F3D-23.2.\n\n## Test plan\n\n- Unit: none new.\n- E2E: build H7; `f3d verify` (including shadows and individual instance variation) on both browsers; `f3d bench` paired runs on the reserved M5, phone and a discrete-GPU stratum when available under protocol v1 with frozen references; ablations for pose caching and cross-pass reuse.\n- Logging: raw bench JSON, analysis reports with intervals, ablation deltas, telemetry breakdown; archived under `evidence/16.5/<stratum>/`.\n- Failure artifacts: invalidated runs with reasons; verify artifacts.\n\n## Considerations\n\n- Do not remove H7 after disappointing results (§15.2).\n","acceptance_criteria":"H7 builds automatically, passes equivalence (including shadows and individual instance variation), and produces paired throughput results with intervals on the M5 and phone strata (and a discrete-GPU stratum when available) through the new backend; ablations attribute any gain; a result below 2x is reported as such.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:26.499549Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:14.254124Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","h7","new-backend","phase-3","tier-frontier"],"dependencies":[{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.5","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr.2","type":"blocks","created_at":"2026-09-09T16:49:14.912074Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.5","depends_on_id":"f3d-14-material-light-texture-surface-5lj.3","type":"blocks","created_at":"2026-09-09T17:44:14.253760Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.5","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v","type":"parent-child","created_at":"2026-09-09T16:48:26.499549Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.5","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.3","type":"blocks","created_at":"2026-09-09T16:49:14.724429Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-16-tsl-nodes-compute-contracts-f0v.6","title":"H8 end-to-end route (MLS-MPM fluid compute) and measured throughput","description":"## Full scope\n\nH8 (webgpu_compute_particles_fluid) end-to-end route: MLS-MPM multi-kernel compute with atomic cell data, indirect dispatch attributes and runtime particle-count changes (default 32,768; storage for 131,072) through the new backend; equivalent compute/data/scheduling plan; device-loss state class recorded as GPU-only simulation\n\n## Summary\n\nThe hardest headline cell, measured honestly.\n\n## Background (plan §15.2 H8, §15.3, §11.5, §20 \"Modern compute demos remain GPU-bound and <2×\")\n\n\"Simply relocating JavaScript renderer code cannot be assumed to make it 3× faster.\" Response if GPU-bound: \"Find an equivalent algorithm/data-path improvement or fail launch gate.\"\n\n## Technical approach\n\n- Pipeline as 11.1 with the compute contracts (16.3); simulation equivalence per §14.5 (chaotic dynamics need a predefined numerical/behavioral contract from 03.6); reference frozen; bench cell; candidate improvements: scheduling of the kernel chain with fewer redundant passes, packed cell data layout, indirect dispatch sizing, all under the numeric contract; no changes to particle count/quality.\n- Device loss: the simulation buffers are classified `ReportLoss` (GPU-only simulation); a loss during simulation runs 22a.3's terminal cleanup and the classification dump records the class; the user-visible reset/error policy itself is implemented and verified in 17.7, which tests on H8.\n\n## Success criteria\n\n- Results document; device-loss test; feeds G4/F3D-23.2.\n\n## Test plan\n\n- Unit: none new beyond 16.3's compute tests.\n- E2E: build H8; `f3d verify` under the temporal/simulation contract (fixed step, seeded, warmup) at default and stress particle counts with runtime count changes on both browsers; `f3d bench` paired runs on the reserved M5 and phone; device-loss injection during simulation runs 22a.3's terminal cleanup and records the simulation buffers as `ReportLoss`-class in the classification dump (the reset/error policy is verified in 17.7).\n- Logging: raw bench JSON, analysis reports, telemetry per kernel `{kernel, dispatches, ms}`, loss-policy events; archived under `evidence/16.6/<stratum>/`.\n- Failure artifacts: readbacks of particle/grid buffers at divergence; invalidated runs with reasons.\n\n## Considerations\n\n- A visually similar image is not a precision contract (§8.7).\n","acceptance_criteria":"H8 builds automatically, passes equivalence under the temporal/simulation contract (fixed-step, seeded, warmup), runs at default and stress particle counts with runtime changes, and produces paired throughput results with intervals on the M5 and phone strata through the new backend; the report identifies any equivalent compute/data-path improvement or documents that the case is GPU-bound; device loss during simulation runs the terminal cleanup and records the simulation state as ReportLoss-class (the reset/error policy itself is verified in 17.7).","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:26.595039Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:14.667677Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","device-loss","h8","new-backend","phase-3","tier-frontier"],"dependencies":[{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.6","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v","type":"parent-child","created_at":"2026-09-09T16:48:26.595039Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.6","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.3","type":"blocks","created_at":"2026-09-09T16:49:15.095676Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-16-tsl-nodes-compute-contracts-f0v.6","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu.3","type":"blocks","created_at":"2026-09-09T16:49:15.274380Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-17-passes-effects-histories-readbacks-0gf","title":"F3D-17: All pass/effect families, histories, resource reuse and copies/readbacks","description":"## What this epic delivers\n\nPhase 3's pass/effect closure and the pass-graph optimizer. Plan §19 F3D-17 depends on 14 and 16; acceptance: \"Complete new-backend applicable coverage plus exact sync/native integration\".\n\n## Why (plan §5.12 \"Postprocessing and special effects\", §8.2 \"Pass/resource planning\", §8.5, §8.6, §15.4)\n\n\"Execute complete passes conservatively before optimizing; same reset/disposal/resize/order; custom/native pass interaction retains exact resource group.\" \"Reuse compatible texture objects across disjoint lifetimes, eliminate genuinely dead passes, and preserve necessary history.\"\n\n## Children\n\n1. EffectComposer/Pass family closure.\n2. Pass-graph optimization (transient reuse, dead passes, history, hazards).\n3. Postprocessing fusion legality.\n4. Copies and readbacks contract.\n5. Temporal effects and the VXGI Sponza case.\n6. H4/H6 end-to-end routes.\n7. GPU-history device-loss recovery classes.\n\n## Success criteria for the epic\n\nAll children closed; the postprocessing family in the manifest is fully owned and tested in the forced lane; `f3d-graph` line count reported against 14,000.\n\n## No-claim boundary\n\nFusion and reuse are optimizations behind legality rules; conservative execution is the reference.\n","acceptance_criteria":"The entire EffectComposer/Pass family and every official pass/shader, output conversion, masks/stencil, ping-pong/history, bloom/AO/SSR/SSGI/DOF/AA/upscaling, custom passes and stereo/anaglyph/ASCII/outline effects execute through the new backend with the same reset/disposal/resize/order semantics; pass-graph optimization reuses resources and eliminates dead passes legally; copies and readbacks follow their own validation; temporal effects and VXGI Sponza pass; H4 and H6 have measured routes; GPU-history device-loss classes are implemented.","status":"open","priority":2,"issue_type":"epic","created_at":"2026-09-09T16:46:23.835544Z","created_by":"jemanuel","updated_at":"2026-09-09T16:49:18.857645Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-graph","h4","h6","new-backend","phase-3","tier-frontier","tier-solid"],"dependencies":[{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf","depends_on_id":"f3d-14-material-light-texture-surface-5lj","type":"blocks","created_at":"2026-09-09T16:49:18.654745Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v","type":"blocks","created_at":"2026-09-09T16:49:18.857381Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-17-passes-effects-histories-readbacks-0gf.1","title":"EffectComposer/Pass family closure executed conservatively first","description":"## Full scope\n\nEffectComposer and Pass family closure: every official pass and shader (RenderPass, ShaderPass, all bloom/AO/SSR/SSGI/DOF/AA/upscaling/outline/mask/texture/film/glitch/etc. passes, OutputPass conversion, MaskPass/ClearMaskPass stencil), the node-based PostProcessing/RenderPipeline path, custom passes, stereo/anaglyph/ASCII effects; executed conservatively first with identical reset/disposal/resize/order behavior; custom or native pass interaction retains the exact resource group\n\n## Summary\n\nAll the passes, conservatively correct first.\n\n## Background (plan §5.12 \"Postprocessing and special effects\", §5.16)\n\n\"Standard dynamic GLSL/TSL programs, public subclass/registration mechanisms, and custom passes that use portable APIs require the generic new execution path, with original execution preserved for cases whose actual semantics need it.\"\n\n## Technical approach\n\n- Retain the `EffectComposer`/`Pass` construction modules; each official pass's shaders translate via 15b.1; its render logic maps to pass-graph passes with explicit attachment ops; ping-pong buffers as versioned resources; `MaskPass` stencil ops explicit.\n- Node-based `PostProcessing` maps through 16.1.\n- Custom pass support: a `Pass` subclass calling `renderer.render`/`setRenderTarget` executes through the general path.\n- Native contracts inside pinned passes: inventory every pinned pass's use of `renderer.getContext()`, `renderer.state` (the pinned `MaskPass`/`ClearMaskPass` drive `state.buffers.{color,depth,stencil}` with GL constants), `renderer.capabilities`, `renderer.extensions` and `renderer.properties`; for the unmodified pinned passes (fingerprinted) provide the specific equivalent transformation on the new backend (MaskPass/ClearMaskPass → explicit stencil operations in the pass graph; capability/extension queries → the capability record) and document each as a §5.6 \"specific correct transformation\"; modified copies of these passes route exact.\n\n## Success criteria\n\n- Family suite green; H6 equivalence; upstream postprocessing examples pass in the forced lane.\n\n## Test plan\n\n- Unit: composer semantics tests (read/write buffer swap, `renderToScreen`, `setSize`, `dispose`, `enabled`/`needsSwap`/`clear` flags) against a Node oracle where possible; per-pass shader translation snapshots; `MaskPass` stencil op tests; node-based `PostProcessing` mapping tests.\n- E2E: generated pass family suite in the forced lane on both browsers vs upstream over every official pass and the postprocessing examples; a custom `Pass` subclass fixture using portable APIs; a native-contract pass fixture keeping its exact resource group; H6 equivalence.\n- Logging: per-pass `{pass, owner, route, max_err}` feeding the manifest; archived under `evidence/17.1/`.\n- Failure artifacts: per-pass diff images and the pass-graph dump.\n\n## Considerations\n\n- Conservative execution first (§5.12); fusion is 17.3.\n","acceptance_criteria":"Every postprocessing manifest record has new-WebGPU-path status with a positive test in the forced lane; composer semantics (read/write buffer swap, renderToScreen, setSize, dispose, pass enabled/needsSwap/clear flags) match upstream; custom application passes using portable APIs execute generically; passes that reach native GL contracts keep an exact resource group.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:26.694197Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:14.853594Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-graph","f3d-shader","new-backend","phase-3","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.1","depends_on_id":"f3d-14-material-light-texture-surface-5lj.6","type":"blocks","created_at":"2026-09-09T16:49:15.845147Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.1","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51.6","type":"blocks","created_at":"2026-09-09T17:44:11.881067Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.1","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.1","type":"blocks","created_at":"2026-09-09T16:49:16.030390Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.1","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf","type":"parent-child","created_at":"2026-09-09T16:48:26.694197Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-17-passes-effects-histories-readbacks-0gf.2","title":"Pass-graph optimization: transient reuse, dead passes, history, hazards","description":"## Full scope\n\nPass-graph optimization: transient texture reuse across disjoint lifetimes, dead-pass elimination, history preservation; reuse only after the old logical contents have no remaining consumers and the queued order is valid; persistent histories, pending readbacks, aliased views and source-visible render targets prevent transient reuse; mapping/staging credits with their own completion requirements\n\n## Summary\n\nResource-object reuse that respects every consumer.\n\n## Background (plan §8.2 \"Pass/resource planning\", §8.5 \"Resource reuse is allowed only...\")\n\n\"This is resource-object reuse, not a claim that the browser exposes arbitrary native heap aliasing.\" \"Resource reuse is allowed only after the old logical contents have no remaining consumers and the queued order is valid. Persistent histories, pending readbacks, aliased views, and source-visible render targets prevent inappropriate transient reuse. Mapping/staging credits have their own completion requirements.\"\n\n## Technical approach\n\n- Lifetime analysis over the scheduled pass list; transient pool (07.1) with compatibility keys; consumers include readback commands and CPU-visible targets (any `WebGLRenderTarget` the application holds is source-visible unless proven unobserved).\n- Dead-pass elimination requires proof that no observable effect (callbacks, node updates, readbacks) depends on the pass (§8.5: \"skip the entire operation only if its effects are also proven absent\").\n\n## Success criteria\n\n- Counterexamples pass; H6 shows reduced transient memory with identical output.\n\n## Test plan\n\n- Unit: lifetime-analysis tests over scheduled pass lists; counterexample tests proving that a source-visible target, a pending readback, an aliased view or a history dependency blocks reuse; dead-pass elimination requires proof of no observable effect (callback/node-update/readback fixtures keep the pass).\n- E2E: H6 and postprocessing examples in the forced lane on both browsers with identical output and reduced transient memory (telemetry); ablation toggle verified.\n- Logging: reuse decisions `{resource, reused_for, reason}` and transient-pool stats; archived under `evidence/17.2/`.\n- Failure artifacts: the plan diff and diff images.\n\n## Considerations\n\n- Copy commands have their own validation (§8.5).\n","acceptance_criteria":"The graph optimizer reuses compatible transient textures across disjoint lifetimes and removes provably dead passes while preserving observable renders, histories and readbacks; counterexample tests prove that a source-visible target, a pending readback, an aliased view or a history dependency blocks reuse; the optimization has an ablation toggle and a legality statement.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:26.793289Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:15.431677Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-graph","new-backend","phase-3","tier-frontier"],"dependencies":[{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.2","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.2","type":"blocks","created_at":"2026-09-09T16:49:16.392018Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.2","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf","type":"parent-child","created_at":"2026-09-09T16:48:26.793289Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.2","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf.1","type":"blocks","created_at":"2026-09-09T16:49:16.217396Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-17-passes-effects-histories-readbacks-0gf.3","title":"Postprocessing fusion legality with preserved quantization boundaries","description":"## Full scope\n\nPostprocessing fusion legality: a legality rule covering sampling, precision, blending, history, derivatives and texture-format boundaries; required intermediate quantization and sampling boundaries preserved; register-pressure/duplicate-sampling profiling before enabling a fusion\n\n## Summary\n\nFusion without changing the picture.\n\n## Background (plan §5.5 last paragraph, §8.6 last paragraph)\n\n\"A fused shader must preserve required intermediate quantization and sampling boundaries. Two full-screen passes are not necessarily equivalent to their algebraic composition evaluated at higher precision.\" \"A fusion that increases shader register pressure or duplicate sampling may lose; profile it.\"\n\n## Technical approach\n\n- Legality rule implemented as a predicate over adjacent full-screen passes: 1:1 texel mapping, no history reads, matching formats/precision, blending state expressible in the fused shader, derivatives not crossing the boundary, no sampling of the intermediate at other texels; when true, emit a fused shader that re-quantizes the intermediate value to the original intermediate format inside the shader (explicit `pack`/`unpack` or rounding to the format's precision) so quantization boundaries are preserved.\n- Profiling: per device, measure fused vs conservative (register pressure/duplicate sampling can lose); enable per device only when it wins and passes 14.9 diagnostics; the decision is part of the portfolio cache (20.2).\n- Ablation toggle and conservative reference retained.\n\nPrior notes retained:\n- Fusion candidates from the graph (adjacent full-screen passes with 1:1 texel mapping, no history, matching formats); explicit re-quantization to the intermediate format inside the fused shader; profiling harness per device.\n\n## Success criteria\n\n- Legality tests; profiles recorded; no fusion in the primary score unless it passes both.\n\n## Test plan\n\n- Unit: fusion legality tests (sampling, precision, blending, history, derivatives, format boundaries); a quantization test showing two full-screen passes composed at higher precision diverge from the reference while the fused path with explicit re-quantization matches.\n- E2E: fused vs conservative renders in the forced lane on both browsers with 14.9 diagnostic targets; per-device profiling of each fusion (register pressure/duplicate sampling) recorded and fusions disabled where they lose.\n- Logging: per-fusion `metric` events `{fusion, device, ms_fused, ms_conservative, enabled: bool, max_err}`; archived under `evidence/17.3/`.\n- Failure artifacts: diff images.\n\n## Considerations\n\n- Keep the conservative path as reference.\n","acceptance_criteria":"Pass fusion is applied only when the rule proves equivalence including intermediate quantization (a test shows two full-screen passes composed at higher precision diverge from the reference and that the fused path preserves the boundary); each fusion is profiled and disabled where it loses; ablation toggle exists.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:26.894103Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:22.587389Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-graph","f3d-shader","new-backend","phase-3","tier-frontier"],"dependencies":[{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.3","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51.4","type":"blocks","created_at":"2026-09-09T16:49:16.764077Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.3","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf","type":"parent-child","created_at":"2026-09-09T16:48:26.894103Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.3","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf.2","type":"blocks","created_at":"2026-09-09T16:49:16.578153Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-17-passes-effects-histories-readbacks-0gf.4","title":"Copies and readbacks contract (encoded copies, mapAsync, sync reads exact-only, real transfers)","description":"## Full scope\n\nCopies and readbacks: buffer/texture copy commands with their own validation and row-pitch rules, copyTextureToTexture/copyFramebufferToTexture equivalents, asynchronous readback via mapAsync with staging lifetimes, readRenderTargetPixelsAsync on the new backend, synchronous readback only on the exact route, explicit real-copy transfer between exact and new-backend groups with measured cost\n\n## Summary\n\nData movement with the right validation model per operation.\n\n## Background (plan §6.8, §6.9 last paragraphs, §5.6, §23 [S25], [S45])\n\n\"Alignment rules belong to the actual operation... encoded buffer-to-texture copies have a separate row-pitch constraint... [do not] assume mapped buffers may simultaneously participate in GPU work.\" \"No zero-copy interchange between `WebGLTexture` and `GPUTexture` is assumed... an explicit supported transfer uses real copies, lawful synchronization and measured costs, not transparent per-draw ping-pong.\"\n\n## Technical approach\n\n- Copy pass kind in the pass graph with its own validation: buffer-to-buffer, buffer-to-texture and texture-to-buffer with the 256-byte `bytesPerRow` rule for encoder copies, texture-to-texture with format/aspect compatibility; `writeTexture`/`writeBuffer` remain queue operations with their separate layout rules (05.2).\n- Upstream API mapping: `copyTextureToTexture`, `copyFramebufferToTexture` equivalents on the new backend as copy passes; `readRenderTargetPixelsAsync` and `getArrayBufferAsync` (storage buffers/attributes) via a staging buffer and `mapAsync` with generation checks; synchronous `readRenderTargetPixels` on a new-backend renderer is impossible by construction because the router (12a.6) selects the exact route when the source uses it.\n- Staging lifecycle states (unmapped/mapping/mapped/in-use) enforced so a mapped buffer never participates in GPU work; credits released on completion (22a.5).\n- Exact↔new transfer: the only supported path copies through CPU bytes with telemetry of bytes and time; no per-draw ping-pong.\n\n## Success criteria\n\n- Tests per operation; the native-context ownership counterexample (05.7) extended with the supported transfer.\n\n## Test plan\n\n- Unit: copy-pass validation tests (bytesPerRow alignment for encoder copies; `writeTexture` layout separate); staging lifecycle-state tests (mapped buffers never participate in GPU work); async readback generation checks; exact↔new transfer accounting tests.\n- E2E: copy/readback family suite on both browsers (`copyTextureToTexture`, `copyFramebufferToTexture` equivalents, `readRenderTargetPixelsAsync`, staging reuse); the native-context ownership counterexample extended with the supported real-copy transfer; a sync-read on a new-backend renderer is shown to have been routed exactly at construction.\n- Logging: per-copy events `{kind, bytes, alignment, staging_state}` and transfer cost `metric` events; archived under `evidence/17.4/`.\n- Failure artifacts: readback dumps.\n\n## Considerations\n\n- Mandatory readback checkpoints for recovery have a real cost (§11.5).\n","acceptance_criteria":"Every copy/readback manifest record has an owner with tests; encoded copies respect bytesPerRow alignment; mapped staging buffers never participate in GPU work while mapped; async readback returns correct data with generation checks; sync readback APIs on a new-backend renderer are routed exactly at construction (never faked); the supported exact-to-new transfer is a real copy with its cost reported.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:26.995135Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:15.028552Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-gpu","f3d-graph","new-backend","phase-3","tier-solid"],"dependencies":[{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.4","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.3","type":"blocks","created_at":"2026-09-09T16:49:17.124622Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.4","depends_on_id":"f3d-14-material-light-texture-surface-5lj.6","type":"blocks","created_at":"2026-09-09T16:49:16.952765Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.4","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf","type":"parent-child","created_at":"2026-09-09T16:48:26.995135Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-17-passes-effects-histories-readbacks-0gf.5","title":"Temporal effects (SSGI/TRAA) and the VXGI Sponza fidelity case","description":"## Full scope\n\nTemporal effects and the VXGI Sponza case: SSGI/TRAA history buffers with correct invalidation, warmup, fixed-step and seeded inputs; temporal consistency comparisons; webgpu_vxgi_sponza (voxel cone-traced GI, MRT, TAA, large assets via the locked external index, first-person control) passes equivalence\n\n## Summary\n\nThe temporal fidelity cases that catch history bugs.\n\n## Background (plan §15.4, §14.5 last paragraph, §10.5)\n\n\"For temporal renderers and simulations, preserve warmup, history, fixed-step policy, and random inputs. Chaotic dynamics require a predefined numerical/behavioral contract.\" Sponza \"explicitly combines VXGI and temporal antialiasing and fetches its asset through an external model index, so both its render graph and asset closure are material parts of the test.\"\n\n## Technical approach\n\n- History resources: SSGI/TRAA accumulation targets and any pass-graph resource with a previous-frame read are `persistent` (never eligible for transient reuse in 17.2) with versioned histories; invalidation events (resize, camera cut, explicit reset) mirrored from the pinned pass implementations.\n- Temporal contract from protocol v1 (03.6): identical warmup length, fixed logical time step, seeded jitter sequences; equivalence compares frames at logical indices with the temporal-consistency metric (14.9) in addition to per-frame tolerances.\n- Sponza: VXGI voxelization passes (3D textures, MRT), cone tracing, TAA; asset closure via the locked external index (10.4); first-person control script; standalone artifact verified by the packaging harness.\n- Fidelity-set reporting: results published separately from the headline gate.\n\nPrior notes retained:\n- History resources as persistent versioned resources excluded from transient reuse; invalidation events mirrored from upstream's logic; equivalence at logical frames with temporal consistency metrics.\n\n## Success criteria\n\n- Three cases pass; Sponza standalone artifact passes the packaging harness with the index locked.\n\n## Test plan\n\n- Unit: history-resource exclusion tests (never transient-reused); invalidation-event mirroring tests (resize/camera cut as upstream does); temporal-consistency metric tests on synthetic sequences.\n- E2E: `f3d verify` on `webgpu_postprocessing_ssgi`, `webgpu_postprocessing_traa` and `webgpu_vxgi_sponza` under the temporal contract (history preserved, warmup identical, invalidation on resize/cut) on both browsers; Sponza standalone artifact through the packaging harness with the index locked.\n- Logging: per-frame temporal metrics `{frame, consistency, history_invalidated}` and the standalone request log; archived under `evidence/17.5/`.\n- Failure artifacts: frame sequences from both sides and diffs.\n\n## Considerations\n\n- Scored separately from the headline gate (§15.4).\n","acceptance_criteria":"webgpu_postprocessing_ssgi, webgpu_postprocessing_traa and webgpu_vxgi_sponza pass the fidelity profile with the temporal contract from the protocol (history preserved, invalidated on resize/camera cut as upstream does, warmup identical); results are published in the fidelity set.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:27.093209Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:21.716117Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-graph","new-backend","phase-3","tier-solid"],"dependencies":[{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.5","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf","type":"parent-child","created_at":"2026-09-09T16:48:27.093209Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.5","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf.1","type":"blocks","created_at":"2026-09-09T16:49:17.320743Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.5","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf.2","type":"blocks","created_at":"2026-09-09T16:49:17.532502Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-17-passes-effects-histories-readbacks-0gf.6","title":"H4 and H6 end-to-end routes and measured throughput","description":"## Full scope\n\nH4 (webgl_shadowmap_performance) and H6 (webgl_postprocessing_advanced) end-to-end routes: automatic transformation, equivalence, and measured completed-frame throughput under the frozen protocol; gains attributed to legal batching/dirty dependencies/shared cross-pass work (H4) and legal graph specialization/traffic reduction (H6)\n\n## Summary\n\nTwo more headline cells.\n\n## Background (plan §15.2 H4/H6, §16.8)\n\nH4: \"Legal batching, dirty dependencies, shared work across passes.\" H6: \"Legal graph specialization and resource/traffic reduction.\"\n\n## Technical approach\n\n- Build H4 (`webgl_shadowmap_performance`) and H6 (`webgl_postprocessing_advanced`) with `f3d build` from unmodified sources; confirm in the reports that the expected islands/patterns were admitted (H4: stable material/lighting variants, legal grouping, shadow-pass dependency versions; H6: pass-graph specialization, transient reuse, no fusion unless 17.3 proved it) and that every refusal has a span.\n- Extend the frozen reference set (03.3) with R0/R1/R2 for H4 and H6 using the same bounded recipe; calibrate and freeze before any candidate run; record the selection.\n- Run `f3d verify` on both browsers for every control (H4: object count/shadow settings as exposed; H6: every effect toggle and parameter) with 03.6 tolerances; only then run `f3d bench` paired cells on the reserved M5 and phone with protocol v1.\n- Ablations: shadow caching, legal batching and dirty-dependency propagation for H4; transient reuse, dead-pass elimination and fusion for H6; record where the bottleneck moves per ablation.\n- Publish per-stratum results documents with cell ratios, intervals and the ownership assertion; a cell under 2x is published as such and feeds G4/23.2 unchanged.\n\n## Success criteria\n\n- Results documents; feeds G4/F3D-23.2.\n\n## Test plan\n\n- Unit: none new.\n- E2E: build H4 and H6; `f3d verify` on both browsers; `f3d bench` paired runs on the reserved M5 and phone under protocol v1 with frozen references; ablation toggles for shadow caching/batching (H4) and pass-graph optimization/fusion (H6).\n- Logging: raw bench JSON, analysis reports with intervals, ablation deltas; archived under `evidence/17.6/<stratum>/`.\n- Failure artifacts: invalidated runs with reasons; verify artifacts.\n\n## Considerations\n\n- Shadow caching must still run source-required callbacks (14.3).\n","acceptance_criteria":"H4 and H6 build automatically, pass equivalence, and produce paired throughput results with intervals on the M5 and phone strata through the new backend; ablations attribute gains; results below 2x are reported as such.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:27.213822Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:15.009269Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","h4","h6","new-backend","phase-3","tier-frontier"],"dependencies":[{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.6","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago.4","type":"blocks","created_at":"2026-09-09T16:49:18.099057Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.6","depends_on_id":"f3d-14-material-light-texture-surface-5lj.3","type":"blocks","created_at":"2026-09-09T16:49:17.915394Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.6","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf","type":"parent-child","created_at":"2026-09-09T16:48:27.213822Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.6","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf.2","type":"blocks","created_at":"2026-09-09T16:49:17.725706Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-17-passes-effects-histories-readbacks-0gf.7","title":"GPU-history device-loss recovery classes with charged checkpoint costs","description":"## Full scope\n\nGPU-history device-loss recovery classes: immutable/CPU-authoritative resources recreated from current authoritative versions; GPU state with a sufficient checkpoint replayed only under the specified numeric/input-history contract; GPU-only simulation, accumulated history or uncaptured output reported as state loss with an explicit reset/restart/error policy; checkpoint readback cost charged\n\n## Summary\n\nDevice loss is not necessarily state recovery, and the plan says exactly how to be honest about it.\n\n## Background (plan §11.5)\n\n| State class | Valid recovery action |\n|---|---|\n| Immutable packed assets or CPU-authoritative data | Recreate allocations and upload the current authoritative version |\n| GPU state with a sufficient checkpoint and admitted replay | Restore/replay only under the specified numeric and input-history contract |\n| GPU-only simulation, accumulated render history, or uncaptured output | Report state loss and follow an explicit reset/restart/error policy |\n\"A snapshot of the initial scene cannot reconstruct the current fluid simulation... A reset is visible application behavior and cannot be described as seamless state preservation.\"\n\n## Technical approach\n\n- Classification: every persistent pass-graph resource gets a `RecoveryClass` field at plan time: `Recreate` (immutable packed assets, CPU-authoritative data), `ReplayUnderContract` (GPU state with an admitted checkpoint and replay contract), `ReportLoss` (GPU-only simulation, accumulated history, uncaptured output); the compiler sets it from ownership analysis (12a.2) and the resource's producer.\n- Recovery driver in `f3d-gpu`: invoked by 22a.3's device replacement; `Recreate` re-uploads from current authoritative versions (06.6 histories); `ReplayUnderContract` restores from the last checkpoint and replays inputs only when the numeric/input-history contract is admitted; `ReportLoss` emits the source-visible loss/reset event per the application's policy (upstream context-loss shape where applicable).\n- Checkpoints: optional scheduled readbacks (17.4) with bandwidth/latency/memory recorded in telemetry and charged in bench results.\n- Tests distinguish the four outcomes explicitly on SSGI/TRAA history and H8's simulation.\n\n## Success criteria\n\n- Tests for each class on SSGI/TRAA history and H8.\n\n## Test plan\n\n- Unit: classification tests (every persistent graph resource assigned one §11.5 class); recovery-driver tests with a fake device (class 1 recreated from authoritative versions; class 2 replayed only under contract; class 3 reports loss).\n- E2E: device-loss injection during SSGI/TRAA history accumulation and H8 simulation on both browsers asserting reconstruction success, admitted reset, unrecoverable loss and deliberate teardown outcomes; checkpoint policy cost measured where enabled.\n- Logging: recovery events `{resource, class, action, outcome, checkpoint_bytes}`; archived under `evidence/17.7/`.\n- Failure artifacts: the resource classification dump and the post-recovery frame.\n\n## Considerations\n\n- Feeds 22b.1.\n","acceptance_criteria":"Every persistent GPU resource in the pass graph is classified into one of the three §11.5 classes; loss during a temporal effect or H8 follows the class policy with tests distinguishing reconstruction success, admitted reset, unrecoverable loss and deliberate teardown; any checkpoint readback cost appears in the results.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:27.322636Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:19.386682Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["device-loss","f3d-gpu","f3d-graph","phase-3","phase-4","tier-solid"],"dependencies":[{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.7","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf","type":"parent-child","created_at":"2026-09-09T16:48:27.322636Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.7","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf.5","type":"blocks","created_at":"2026-09-09T16:49:18.285700Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-17-passes-effects-histories-readbacks-0gf.7","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu.3","type":"blocks","created_at":"2026-09-09T16:49:18.461617Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-18a-early-upstream-tests-feature-gates-zj1","title":"F3D-18 (early slice): Mapped unchanged upstream tests and the feature-gate machinery, started immediately","description":"## What this epic delivers\n\nThe test infrastructure the plan says must \"start mapped upstream tests immediately\" in Phase 1 and \"accrue coverage continuously\". Plan §19 F3D-18 early prerequisite is 04.\n\n## Why (plan §2.4, §14.1–14.4, §14.10, §18 Phase 1)\n\n\"FrankenThreeD therefore needs both the complete upstream test inventory and a separate real-device comparison harness.\" \"Run separate production-route lanes: full functional routing; forced new-backend execution for all applicable rendering cases; and exact native-backend integration.\"\n\n## Children\n\n1. Candidate module-resolution tree for unchanged upstream tests\n2. Core and addon unit suites against the candidate with strict failure rules and owners\n3. E2E screenshot oracle lane (upstream-shaped, thresholds unchanged, never a perf lane)\n4. Three production-route lanes in f3d-conformance (functional, forced new-backend, exact)\n5. Manifest status updater, release-verifier rules and f3d verify-upstream\n6. Semantic utility surfaces for internal-utility tests (WebGLRenderLists etc.)\n7. f3d verify: original-vs-candidate equivalence runner with structured evidence and failure artifacts\n\n## Success criteria for the epic\n\nAll children closed; the lanes run locally via repository-owned commands (DSR wiring), not hosted CI.\n\n## No-claim boundary\n\nGreen upstream suites are necessary and insufficient (§14.1).\n","acceptance_criteria":"A candidate module-resolution tree runs the unchanged upstream core and addon unit suites and the upstream-shaped E2E screenshot lane against the production compatibility exports with strict failure rules and per-result ownership; the three production-route lanes exist in f3d-conformance; the manifest status updater and release-verifier rules are implemented; f3d verify-upstream runs them.","status":"open","priority":1,"issue_type":"epic","created_at":"2026-09-09T16:46:23.916794Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:27.572573Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.4","type":"blocks","created_at":"2026-09-09T16:49:21.393698Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:49:21.210209Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-18a-early-upstream-tests-feature-gates-zj1.1","title":"Candidate module-resolution tree for unchanged upstream tests","description":"## Full scope\n\nCandidate module-resolution tree for upstream tests: redirect every upstream import path (package names and src-level paths such as those in test/unit/three.source.unit.js) to the production compatibility exports through a mapped working copy/resolver; original test bodies, expected values and assertions untouched; the oracle checkout never edited; resolved implementation recorded per import\n\n## Summary\n\nRun the real tests against the real product without touching either.\n\n## Background (plan §14.2, §2.4, §23 [S27])\n\n\"Create a candidate module-resolution tree that redirects the upstream source import paths to the production compatibility exports. Keep the original test bodies, expected values, and assertions intact. Use a mapped working copy/resolver without editing the upstream oracle checkout. Record the resolved implementation for every import. Retained modules and exact renderer components are allowed only as declared production owners in the full functional lane. Hidden/test-only delegation is forbidden.\"\n\n## Technical approach\n\n- Generate the mapped working copy: symlinks/copies of the test tree plus a resolver (Node loader hook / import map for browser tests) mapping `../../src/...` and `three` paths to the candidate build's corresponding routed modules (04.2 routing table); the mapping is derived from the manifest so every src path has a target.\n- Resolution log per import; assertion that no import resolved to the untouched oracle renderer (an accidental oracle import counts toward nothing, §14.2).\n\n## Success criteria\n\n- All unit test modules load under the mapping; log shows owners; an injected oracle import is detected.\n\n## Test plan\n\n- Unit: resolver tests (package name, `../../src/...` relative paths, addon paths → routed candidate modules from the routing table); oracle-import detector test (an injected import of the untouched upstream renderer is flagged).\n- E2E: dry run of the mapped working copy loading every unit test module under Node and the browser runner without executing assertions; the resolution log covers every import.\n- Logging: per-import `{test_module, specifier, resolved, package, revision, instance, submission_path}`; oracle imports at `error`; archived under `evidence/18a.1/`.\n- Failure artifacts: the unresolved/oracle import list.\n\n## Considerations\n\n- Test-only aliases are forbidden; the mapped targets are the production modules.\n","acceptance_criteria":"The upstream test runner executes from a mapped working copy where imports resolve to the candidate's routed modules (retained/facade/exact as declared production owners) without modifying the oracle tree or any test file; a per-import resolution log records package, source revision, instance and (for renderers) submission path.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:27.440648Z","created_by":"jemanuel","updated_at":"2026-09-09T17:31:52.760066Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","js-host","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.1","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.4","type":"blocks","created_at":"2026-09-09T16:49:19.212913Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.1","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:49:19.029011Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.1","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1","type":"parent-child","created_at":"2026-09-09T16:48:27.440648Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.1","depends_on_id":"f3d-gate-1-foundation-tzn","type":"blocks","created_at":"2026-09-09T17:31:52.759775Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-18a-early-upstream-tests-feature-gates-zj1.2","title":"Core and addon unit suites against the candidate with strict failure rules and owners","description":"## Full scope\n\nCore and addon unit suites against the candidate: every active upstream test imported and executed in its required environment; syntax/import/setup errors, assertion-count shortfalls and unhandled rejections fail the inventory; candidate-only skips are failures; upstream skips stay visible as unproven; every result records its implementation owner\n\n## Summary\n\nStrict execution of the whole unit inventory with honest bookkeeping.\n\n## Background (plan §14.3; AGENTS.md \"Upstream suite\")\n\n\"Every active upstream test must be imported and executed in its required environment. Syntax/import/setup errors, assertion-count shortfalls, and unhandled rejections fail the inventory rather than becoming empty successful suites. A candidate-only skip is a failure. An upstream-existing skip remains visible as an unproven upstream case, not a new pass.\"\n\n## Technical approach\n\n- Runner wrapper around upstream's QUnit setup for the mapped working copy (18a.1): executes core and addon unit modules in Node or the browser as upstream's scripts require (browser-required modules on Chrome and Safari); counts assertions per test and compares with 01.4's expectations; classifies each result as pass, fail, upstream-skip (kept visible), candidate-skip (a failure), host-blocked (only with reference evidence), setup/import error (a failure), unhandled rejection (a failure).\n- Owner tagging: from the resolution log (which module served each import) plus runtime route attribution (07.6) for rendering tests.\n- Output: per-test JSON results (01.8 schema) feeding the manifest updater (18a.5); the inventory reconciliation asserts no test disappeared.\n\nPrior notes retained:\n- Runner wrapper around upstream's QUnit setup executing in Node and/or browser as upstream requires; assertion counting compared with 01.4's expectations; owner tagging via the resolution log + runtime attribution (07.6 when rendering is involved).\n- Output: JSON results per test with owner and status categories (pass, fail, upstream-skip, candidate-skip=fail, host-blocked with reference evidence).\n\n## Success criteria\n\n- Lane runs; initial results (mostly retained owners) recorded honestly; failures listed.\n\n## Test plan\n\n- Unit: runner-wrapper tests with synthetic modules (assertion-count shortfall fails; candidate-only skip fails; upstream skip stays visible as unproven; unhandled rejection fails; setup error fails).\n- E2E: full core and addon unit lanes against the candidate in Node and the browser as upstream requires, in the environment upstream's scripts use (Node via `qunit`), with a browser used only for a test whose required environment is a browser as recorded in 01.4; counts reconciled against 01.4's inventory (no silent shrinkage).\n- Logging: per-test results `{test, status: pass|fail|upstream-skip|candidate-skip-fail|host-blocked, owner, route, assertions_expected, assertions_run}`; lane summary by status and owner; archived under `evidence/18a.2/`.\n- Failure artifacts: the failing test's output and the resolution log entry for its imports.\n\n## Considerations\n\n- Do not \"fix\" upstream tests; a failing test is a candidate defect or an honest gap.\n","acceptance_criteria":"The unit lanes run to completion with per-test results tagged by owner (new Rust/WebGPU, general new backend, retained JS, exact component, unresolved); the inventory counts from 01.4 are reconciled against executed tests (no silent shrinkage); the run fails on any of the listed conditions; results feed the manifest status updater.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:27.545378Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:15.399488Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.2","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.4","type":"blocks","created_at":"2026-09-09T17:41:13.106985Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.2","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1","type":"parent-child","created_at":"2026-09-09T16:48:27.545378Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.2","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.1","type":"blocks","created_at":"2026-09-09T16:49:19.389835Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-18a-early-upstream-tests-feature-gates-zj1.3","title":"E2E screenshot oracle lane (upstream-shaped, thresholds unchanged, never a perf lane)","description":"## Full scope\n\nE2E screenshot oracle lane: the upstream-shaped regression lane with its fixed configuration (software Vulkan ICD, launch flags), unchanged expected assets and thresholds (per-pixel rule, 0.1% different-pixel limit), running examples through the candidate; never a performance lane; goldens never regenerated from the candidate\n\n## Summary\n\nKeep the upstream screenshot lane exactly as a regression oracle.\n\n## Background (plan §14.4, §14.6, §2.4)\n\n\"Keep an upstream-shaped regression lane for reproducibility, including its fixed configuration where appropriate. Do not treat it as a performance lane.\" \"The inspected E2E configuration uses its own per-pixel error rule and a 0.1% different-pixel limit; FrankenThreeD must not loosen that threshold to pass.\"\n\n## Technical approach\n\n- Reproduce the pinned `test/e2e/puppeteer.js` flow: same browser launch flags, software Vulkan ICD selection, viewport/DPR, per-pixel rule and 0.1% different-pixel limit, expected-image assets and exclusion list; the only change is the resolver mapping (18a.1) so example pages load the candidate's routed build (for accelerated examples, the transformed artifacts).\n- Route attribution: the harness records which route submitted each example's frames (07.6) so a screenshot produced by a retained renderer is tagged retained; exclusions are listed separately for 18b.3 to supplement.\n- The lane never produces timing numbers; its report header says so.\n\nPrior notes retained:\n- Reuse the pinned `test/e2e/puppeteer.js` flow through the mapping; the candidate's examples are the routed build's example pages (for accelerated ones, the transformed artifacts); route attribution tagged per example.\n- Use upstream's own determinism injection (its `Math.random`/time overrides), viewport, timeouts, retry (`numAttempts`) and exception-list behavior unchanged; the 03.1 harness is not attached in this lane, because double injection would make it no longer upstream-shaped.\n\n## Success criteria\n\n- Lane runs; results honest; exclusions listed for 18b to supplement.\n\n## Test plan\n\n- Unit: configuration-preservation test (ICD selection, launch flags, per-pixel rule and 0.1% limit read from the pinned runner and asserted unchanged).\n- E2E: the upstream E2E flow over candidate-mapped examples with the unchanged expected images; upstream exclusions preserved and listed separately; route attribution per example.\n- Logging: per-example `{example, route, owner, pixel_diff_ratio, result}` and the exclusion list; a header line stating \"regression oracle, not performance\"; archived under `evidence/18a.3/`.\n- Failure artifacts: candidate screenshot and diff against the upstream expected image (goldens never regenerated).\n\n## Considerations\n\n- Software ICD results can never substantiate a hardware speedup claim.\n","acceptance_criteria":"The upstream E2E runner configuration is reproduced against the candidate-mapped examples with unchanged thresholds and expected images; upstream exclusions are preserved and separately listed; results are tagged with owner/route and labeled \"regression oracle, not performance\".","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:27.661963Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:15.208935Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.3","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.6","type":"blocks","created_at":"2026-09-09T16:49:19.743841Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.3","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1","type":"parent-child","created_at":"2026-09-09T16:48:27.661963Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.3","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.1","type":"blocks","created_at":"2026-09-09T16:49:19.566282Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-18a-early-upstream-tests-feature-gates-zj1.4","title":"Three production-route lanes in f3d-conformance (functional, forced new-backend, exact)","description":"## Full scope\n\nThree production-route lanes in f3d-conformance: full functional routing; forced new-backend execution with retained renderer submission disabled; exact native-backend integration with real GL contexts, sync reads, native handles and context loss/restore; separate reporting, no substitution between lanes\n\n## Summary\n\nThe lane structure that prevents both hidden gaps and a fake accelerated wrapper.\n\n## Background (plan §14.1 last paragraph; AGENTS.md \"Three production-route lanes\")\n\n\"Run all three separately. They cannot substitute for each other.\"\n\n## Technical approach\n\n- `f3d-conformance` crate: lane definitions (`functional`, `forced-new-backend`, `exact`) as data plus a runner that invokes the JS harness/automation with the lane's environment flags (the forced-new-backend lane switch of 07.6, set at build time or injected by the harness, for lane 2; exact-lane browser profile for lane 3); each lane writes its own result files (01.8 schema).\n- Lane 2 enforcement: the route attribution assertion (07.6) fails any test whose designated rendering went through retained submission; exact-GL contract cases are excluded from lane 2 by declaration in the manifest (never by silent skipping).\n- Lane 3: `expectNativeGL` and native-object assertions on every test.\n- Coverage matrix: five separate columns (import/symbol, behavior, new-backend rendering, exact integration, capable-host) per family; the aggregator refuses to merge columns and only credits a column from the lane that executed it.\n- Repository-owned commands (DSR `quality` wiring, RCH for non-browser lanes) documented in `CONTRACT.md`.\n\nPrior notes retained:\n- `f3d-conformance` crate (Rust orchestration + JS harness invocation) with lane definitions, result schemas and the per-family coverage matrix (import/symbol, behavior, new-backend rendering, exact integration, capable-host) kept as separate columns.\n\n## Success criteria\n\n- Lanes run on the current slice; matrix has no merged totals.\n- `f3d-conformance/CONTRACT.md` is written before any other crate depends on `f3d-conformance` (AGENTS.md \"Documentation and Contracts\"): purpose and position in the dependency direction, public types and semantics, invariants, implementation ownership route, error model, determinism and numeric-fidelity class, cancellation behavior, unsafe boundary if any, feature flags, conformance tests, and no-claim boundaries.\n\n## Test plan\n\n- Unit: lane-definition tests (lane 2 refuses retained submission; lane 3 asserts native objects; the coverage matrix keeps five separate columns and refuses to merge them).\n- E2E: all three lanes run on the current slice via the repository-owned commands (DSR wiring) with separate result files; a family cannot be marked covered by a lane that did not execute it (test with a synthetic result set).\n- Logging: per-lane summaries `{lane, executed, passed, failed, owners}` and the coverage matrix JSON; archived under `evidence/18a.4/`.\n- Failure artifacts: lane result files.\n\n## Considerations\n\n- DSR is the required runner for repo-level gates (AGENTS.md); wire the commands.\n","acceptance_criteria":"f3d-conformance exposes three lane runners with separate result files; lane 2 refuses retained submission (07.6) and lists portable families it covers; lane 3 asserts native objects; a family cannot be marked covered by a lane that did not execute it.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:27.773139Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:20.478022Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-conformance","new-backend","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.4","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.3","type":"blocks","created_at":"2026-09-09T16:49:20.289771Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.4","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.6","type":"blocks","created_at":"2026-09-09T16:49:20.100358Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.4","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1","type":"parent-child","created_at":"2026-09-09T16:48:27.773139Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.4","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.2","type":"blocks","created_at":"2026-09-09T16:49:19.920305Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-18a-early-upstream-tests-feature-gates-zj1.5","title":"Manifest status updater, release-verifier rules and f3d verify-upstream","description":"## Full scope\n\nManifest status updater, release-verifier rules and f3d verify-upstream: per-record functional status, new-WebGPU-path status and owner updated from lane results; blocking states enforced; separate coverage numbers; verifier fails on unmatched source/export path, feature without implementation/behavioral test, candidate-specific valid-input refusal, placeholder/no-op substitution, unexplained extra skip, incomplete runtime codec/variant closure, or known functional regression\n\n## Summary\n\nThe manifest as a living, evidence-backed status record, and the verifier that keeps it honest.\n\n## Background (plan §5.15, §14.10; AGENTS.md \"THE NO-CUT RULE\")\n\n\"`unclassified`, `unimplemented`, `untested`, `known-regression`, `stub`, `no-op-substitute`, and `candidate-refusal-on-valid-source` are blocking states. `retained` describes ownership, not an automatic pass. `host-blocked` requires the reference to have the same missing prerequisite... Use separate coverage numbers for import/symbol closure, validated behavior families, new-backend rendering coverage, exact-component integration, and capable-host validation. Do not average them into a single flattering '100%' number.\"\n\n## Technical approach\n\n- Updater: reads lane result files (01.8 schema) and joins them to manifest records by test reference; sets `functional_status`, `new_webgpu_path_status` and `owner` with evidence pointers (run id + test name); preserves blocking states until evidence says otherwise; refuses to set `pass` from a retained result for the new-backend column.\n- Verifier rules as small functions over the manifest: unmatched source/export path (census drift), feature without implementation or behavioral test, candidate-specific valid-input refusal, placeholder/no-op substitution (detected via owner `stub`/`no-op-substitute`), unexplained extra skip (candidate skip without an upstream skip), incomplete runtime codec/variant closure (from 10.7's inclusion report), known functional regression (a previously passing record now failing).\n- Output: five separate coverage numbers in text and JSON; `f3d verify-upstream --ref r186 --features all --tests all` runs the lanes then the updater then the verifier; non-zero exit on any violation.\n- Stays one data file plus a few functions; no service or dashboard.\n\nPrior notes retained:\n- Updater: joins lane results (18a.2/18a.3/18a.4, later 18b) to manifest records by test references; sets statuses; records evidence pointers.\n- Verifier rules as a small function list over the manifest; output JSON + text.\n- Remember AGENTS.md: the manifest is one data file inside existing tooling, not a service.\n\n## Success criteria\n\n- Verifier runs on the current manifest and reports the (large) honest gap list; each rule has a unit test with a fixture.\n\n## Test plan\n\n- Unit: updater join tests (lane results → manifest statuses with evidence pointers); one fixture per verifier rule (unmatched path, missing test, valid-input refusal, stub/no-op substitution, unexplained extra skip, incomplete codec/variant closure, known regression) that must fail; a test that no output line contains a merged percentage.\n- E2E: `f3d verify-upstream --ref r186 --features all --tests all` on the current manifest producing the honest gap list and the five separate coverage numbers.\n- Logging: per-record status transitions `{record, from, to, evidence}` and the verifier's rule verdicts; archived under `evidence/18a.5/`.\n- Failure artifacts: the rule-violation list with record ids.\n\n## Considerations\n\n- Existing upstream TODO/skips do not waive the corresponding feature's candidate test obligation.\n","acceptance_criteria":"f3d verify-upstream --ref r186 --features all --tests all runs the lanes, updates the manifest statuses from evidence, prints the five coverage numbers separately, and exits non-zero on any §14.10 invariant violation; no averaging into a single percentage exists anywhere in the output.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:27.883650Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:11.690796Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["compat-manifest","f3d-cli","f3d-conformance","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.5","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.3","type":"blocks","created_at":"2026-09-09T16:49:20.469701Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.5","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.5","type":"blocks","created_at":"2026-09-09T17:41:11.690344Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.5","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.8","type":"blocks","created_at":"2026-09-09T16:49:20.835428Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.5","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1","type":"parent-child","created_at":"2026-09-09T16:48:27.883650Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.5","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.2","type":"blocks","created_at":"2026-09-09T16:49:20.650646Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-18a-early-upstream-tests-feature-gates-zj1.6","title":"Semantic utility surfaces for internal-utility tests (WebGLRenderLists etc.)","description":"## Full scope\n\nSemantic utility surfaces for internal-utility tests: provide the same semantic utility surface for internal classes the upstream tests import directly (for example WebGLRenderLists, WebGLAttributes-style helpers, node builder internals) without forcing the optimized renderer to use them as its hot-path representation\n\n## Summary\n\nCompatible utilities and a private packed schedule can coexist.\n\n## Background (plan §14.2 second paragraph, §23 [S28])\n\n\"For internal utilities such as `WebGLRenderLists`, supply the same semantic utility surface when required by the test contract, without requiring the optimized renderer to use that utility as its hot-path representation. A compatible utility and a private packed render schedule can coexist. The inspected tests assert exact object structure and identity-related behavior, making this distinction necessary.\"\n\n## Technical approach\n\n- Identify from 01.4 every renderer-internal or utility module imported directly by upstream tests (`WebGLRenderLists`, `WebGLAttributes`-style helpers, node builder internals, `WebGLState`, etc.); route those source paths to the retained upstream modules through the resolver (18a.1) sharing the single core (04.2).\n- Assert exact object structure/identity behavior the tests expect (for example `WebGLRenderLists.get()` returning list objects with the pinned shape) by running the tests unchanged; the new backend keeps its packed schedule and never depends on these modules in the hot path (07.7 counters confirm no calls).\n- Mark those manifest records as retained public source-path utilities with the tests as evidence.\n\n## Success criteria\n\n- The relevant upstream tests pass; owner recorded as retained.\n\n## Test plan\n\n- Unit: identity-coherence tests for the retained internal utilities against the shared core (same constructors across facade and utility imports).\n- E2E: the upstream tests importing `WebGLRenderLists`, attribute helpers and node-builder internals pass in the mapped lane with `owner: retained-js`; the new backend's private schedule is asserted untouched (07.7 counters show no use of the utility in the hot path).\n- Logging: per-test owner tags and utility identity checks; archived under `evidence/18a.6/`.\n- Failure artifacts: the failing assertion's object-structure diff.\n\n## Considerations\n\n- Directly importable internal classes get their actual compatibility implementation (§5.11 item 2).\n","acceptance_criteria":"Upstream unit tests that import renderer-internal utilities pass against the candidate's routed modules with exact object structure and identity behavior as asserted, while the new backend keeps its private packed render schedule; the manifest records these utilities as retained public source-path utilities.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:27.992281Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:13.306319Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","phase-1","retained-js","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.6","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.4","type":"blocks","created_at":"2026-09-09T17:41:13.305963Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.6","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1","type":"parent-child","created_at":"2026-09-09T16:48:27.992281Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.6","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.2","type":"blocks","created_at":"2026-09-09T16:49:21.033760Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-18a-early-upstream-tests-feature-gates-zj1.7","title":"f3d verify: original-vs-candidate equivalence runner with structured evidence and failure artifacts","description":"## Full scope\n\n`f3d verify <original> --candidate <artifact>`: the original-vs-candidate equivalence runner that executes the shared deterministic harness on both sides, compares visual/state/control/lifecycle profiles at logical checkpoints under the preregistered tolerances, emits structured JSON-lines evidence with owner/route tags and failure artifacts, and exits non-zero on any divergence.\n\n## Summary\n\nThe plan's proposed command surface includes `f3d verify ./index.html --candidate ./dist/demo.html` (§3.2) and the gates require \"every scored application also passes image/state/interaction checks before timing results count\" (§1.3). No bead owned the runner itself; 11.3, 18b.3 and the packaging harness all need it.\n\n## Background (plan §3.2, §14.5, §14.6, §16.6 \"Equivalence\", §1.3)\n\nCompare event/state traces and visual results at corresponding logical times; specify causal order rather than wall-clock arrival; excluded diagnostic pixels are narrowly identified; goldens come only from the reference; tolerances are preregistered (03.6).\n\n## Technical approach\n\n- `f3d-cli` subcommand wired to `f3d-conformance`: launches original and candidate under the harness (03.1) with the same seed and input script, collects checkpoints, runs the comparison library (14.9's checks: color-space-aware metric, edge/silhouette, region, diagnostic targets when available, temporal consistency), diffs event/state traces with causal-order rules, and applies lifecycle steps (resize, dispose, reload).\n- Profiles: a per-case `verify.toml` (from the corpus manifest) listing checkpoints, tolerance set, excluded diagnostic regions, required controls.\n- Output: `evidence/verify/<case>/<run>/` with `events.jsonl`, `summary.json`, `artifacts/` (reference/candidate/diff images, trace diffs); text and JSON to stdout; exit code documented.\n- Lanes: runs in functional, forced-new-backend (with the route assertion) and exact lanes by flag.\n\n## Success criteria\n\n- `f3d verify` on H1/H2 reproduces 11.3's results; a deliberately perturbed candidate (one wrong pixel region, one reordered event) fails with the correct artifact and message.\n\n## Test plan\n\n- Unit: comparison library tests (metrics, region exclusion, causal-order trace diff) on synthetic fixtures; CLI argument, profile-loading and exit-code tests; a test that tolerances cannot be loosened without the `--unscored` label appearing in output.\n- E2E: `tests/e2e/verify/h1.mjs` and `h2.mjs` on both browsers reproducing 11.3's outcomes; perturbation fixtures (one wrong pixel region; one reordered event; one skipped lifecycle step) that must fail with the right artifact and message.\n- Logging: per-checkpoint events `{checkpoint, metric, observed, expected, tolerance, artifact}`; summary lists the first divergence; archived under `evidence/verify/<case>/`.\n- Failure artifacts: reference/candidate/diff images, trace diff, capability record.\n\n## Considerations\n\n- Never loosen a tolerance to pass (§14.6); tolerances are read from protocol v1 and cannot be overridden on the command line without an explicit `--unscored` label in the output.\n","acceptance_criteria":"f3d verify runs both sides under the shared harness, compares visual/state/control/lifecycle profiles at logical checkpoints under preregistered tolerances, writes JSON-lines evidence and failure artifacts, exits non-zero on divergence, and reproduces the H1/H2 equivalence results; perturbed candidates fail correctly.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T17:11:26.198878Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:06.773040Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-cli","f3d-conformance","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.7","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.1","type":"blocks","created_at":"2026-09-09T17:11:26.348823Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.7","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.6","type":"blocks","created_at":"2026-09-09T17:11:26.938074Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.7","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.8","type":"blocks","created_at":"2026-09-09T17:11:26.551661Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.7","depends_on_id":"f3d-14-material-light-texture-surface-5lj.9","type":"blocks","created_at":"2026-09-09T17:44:06.772749Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.7","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1","type":"parent-child","created_at":"2026-09-09T17:11:26.198878Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.7","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.4","type":"blocks","created_at":"2026-09-09T17:11:26.743735Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-18b-full-feature-gate-closure-mk4","title":"F3D-18 (closure): Full feature gates and unchanged upstream tests, every source/behavior entry covered, positive tests beyond TODO/skips, no ownership laundering","description":"## What this epic delivers\n\nPhase 4's feature-gate closure. Plan §19 F3D-18 closure \"uses 12–17\"; acceptance: \"Every source/behavior entry covered; positive tests beyond TODO/skips; no ownership laundering\".\n\n## Why (plan §14.9, §14.10, §18 Phase 4, §22)\n\n\"From the single feature manifest, require an executable positive case for every existing callable/option/behavior family and the relevant invalid-input, mutation, serialization and lifecycle cases.\" \"The release verifier fails on an unmatched source/export path, feature without an implementation/behavioral test...\"\n\n## Children\n\n1. Positive behavioral tests for every manifest family.\n2. High-risk combination suites and pairwise generation.\n3. Original and transformed official examples with all controls/modes.\n4. WebGPU screenshot and tree-shaking upstream checks.\n5. Genuine GL contract tests and absence-of-WebGPU tests.\n6. Release verifier full run with separate counts.\n\n## Success criteria for the epic\n\nAll children closed; `f3d-conformance` line count reported against 24,000.\n\n## No-claim boundary\n\n\"Function name coverage or one constructor assertion is not full functional coverage\" (§5.15).\n","acceptance_criteria":"Every manifest record has a positive behavioral test plus applicable invalid-input, mutation, serialization and lifecycle cases; mandatory family-wide suites and high-risk combinations exist and pass; original and transformed official examples run with all controls; WebGPU screenshot and tree-shaking upstream checks pass; genuine GL contracts and absence-of-WebGPU cases are tested; the release verifier reports zero blocking states in these families with separate counts.","status":"open","priority":2,"issue_type":"epic","created_at":"2026-09-09T16:46:23.996642Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:23.691400Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","phase-4","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-18b-full-feature-gate-closure-mk4","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s","type":"blocks","created_at":"2026-09-09T16:49:24.810233Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr","type":"blocks","created_at":"2026-09-09T16:49:25.012582Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4","depends_on_id":"f3d-14-material-light-texture-surface-5lj","type":"blocks","created_at":"2026-09-09T16:49:25.215638Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51","type":"blocks","created_at":"2026-09-09T16:49:25.424121Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v","type":"blocks","created_at":"2026-09-09T16:49:25.626076Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf","type":"blocks","created_at":"2026-09-09T16:49:25.833322Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1","type":"blocks","created_at":"2026-09-09T16:49:24.612283Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy","type":"blocks","created_at":"2026-09-09T17:44:23.691070Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-18b-full-feature-gate-closure-mk4.1","title":"Positive behavioral tests for every manifest family","description":"## Full scope\n\nPositive behavioral tests for every manifest family closed by F3D-12b, 13, 14, 15b, 16, 17 and 19 (the host/tool families are supplied by 21b.x and verified in 23.1/24.1): all import paths/aliases; every loader/exporter/codec; every material/map/state family; each TSL/node and shader entry; every pass/effect; all controls and alternate renderers; audio/media; XR; physics wrappers; Inspector/editor/tool workflows; all native-backend public APIs; runtime inputs not present at build time\n\n## Summary\n\nThe mandatory family suites of §14.9, driven from the manifest.\n\n## Background (plan §14.9)\n\n\"Add mandatory family-wide suites for: all import paths/aliases; every loader/exporter and codec; every standard material/map/state family; each TSL/node and shader entry; every pass/effect; all controls/alternate renderers; audio/media; XR; physics wrappers; Inspector/editor/tool workflows; and all native-backend public APIs. Include runtime inputs not present at build time.\"\n\n## Technical approach\n\n- From the manifest, generate a test scaffold per record with the family's template (constructor/positive behavior, invalid-input, mutation, serialization/`toJSON`/clone, lifecycle/dispose) and fill the behavior bodies by hand where the family beads (13.x, 14.x, 16.x, 17.x, 19.x, 21b.x) did not already supply them; the scaffold refuses to count a test that only asserts constructor existence or a function name.\n- Source-error preservation: tests assert upstream's actual errors/warnings and never demand errors upstream does not produce.\n- Runtime inputs: for loaders/shaders/nodes/codecs, tests supply inputs absent at build time (unseen assets, runtime-generated shader strings, runtime node graphs).\n- Lanes: functional for all; forced-new-backend for rendering families; exact for native contracts; results feed the updater (18a.5) with evidence pointers.\n\nPrior notes retained:\n- Test generation scaffolds per family from the manifest with hand-written bodies where behavior needs it; each test declares its owner expectation; family beads (13.x, 14.x, 16.x, 17.x, 19.x, 21b.x) contribute their suites here; this bead ensures completeness and fills gaps.\n\n## Success criteria\n\n- Verifier: zero `untested` in all families.\n\n## Test plan\n\n- Unit: the generated per-family scaffolds plus hand-written behavior bodies for every manifest record (positive case; invalid-input; mutation; serialization; lifecycle where applicable); a completeness test that every record has at least one resolving positive test reference; source-error preservation tests (no invented errors).\n- E2E: family suites executed in the functional lane (and forced/exact lanes where rendering or native contracts apply) on Chrome and Safari; runtime-input cases (assets/shaders/nodes not present at build time).\n- Logging: per-record results `{record, family, owner, route, result, evidence}` feeding the updater; archived under `evidence/18b.1/<family>/`.\n- Failure artifacts: per-test artifacts by family convention.\n\n## Considerations\n\n- Preserve source errors; do not demand errors upstream does not produce.\n","acceptance_criteria":"For every manifest record in the families closed by F3D-12b, 13, 14, 15b, 16, 17 and 19 there is an executable positive test (upstream or added) with the relevant invalid-input, mutation, serialization and lifecycle cases where applicable; source errors are preserved rather than inventing errors upstream does not produce; the updater marks each record tested with evidence pointers; no record in those families remains untested. The host/tool families (controls, alternate renderers, audio/media, XR, physics/misc, workers, editor/Inspector/devtools/TSL tools) are supplied by 21b.x and verified in 23.1/24.1.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:28.097069Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:17.263619Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","phase-4","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.1","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s","type":"blocks","created_at":"2026-09-09T17:44:15.912782Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.1","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr","type":"blocks","created_at":"2026-09-09T17:44:16.137101Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.1","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr.6","type":"blocks","created_at":"2026-09-09T16:49:21.955983Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.1","depends_on_id":"f3d-14-material-light-texture-surface-5lj","type":"blocks","created_at":"2026-09-09T17:44:16.356935Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.1","depends_on_id":"f3d-14-material-light-texture-surface-5lj.1","type":"blocks","created_at":"2026-09-09T16:49:21.773285Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.1","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51","type":"blocks","created_at":"2026-09-09T17:44:16.578049Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.1","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v","type":"blocks","created_at":"2026-09-09T17:44:16.802617Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.1","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf","type":"blocks","created_at":"2026-09-09T17:44:17.037657Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.1","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.5","type":"blocks","created_at":"2026-09-09T16:49:21.579343Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.1","depends_on_id":"f3d-18b-full-feature-gate-closure-mk4","type":"parent-child","created_at":"2026-09-09T16:48:28.097069Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.1","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy","type":"blocks","created_at":"2026-09-09T17:44:17.263290Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.1","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy.1","type":"blocks","created_at":"2026-09-09T16:49:22.134723Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-18b-full-feature-gate-closure-mk4.2","title":"High-risk combination suites and pairwise generation","description":"## Full scope\n\nHigh-risk combination suites and pairwise generation: skinning+morphs+shadows+export; clipping+stencil+transparency; instancing+negative scale+custom shader indices+picking; transmission+color management+render targets; texture update+multiple renderer residencies+disposal; runtime shader/node registration+readback; CSS/SVG+camera changes+input; XR+skinning+layers+session reset; editor undo/redo+shared materials+serialization; plus generated pairwise combinations\n\n## Summary\n\nInteractions are where compatibility breaks.\n\n## Background (plan §14.9 \"Cover known high-risk combinations explicitly\")\n\n## Technical approach\n\n- Named combinations as explicit scenario scripts under `tests/e2e/combos/`, each constructed to make the interaction observable: skinning+morphs+shadows+export (animate, cast shadows, export at a logical frame, re-import and compare); clipping+stencil+transparency (ordered draws with clip planes and stencil masks); instancing+negative scale+custom shader indices+picking (merged draws must keep facing, IDs and raycast results); transmission+color management+render targets; texture update+multiple renderer residencies+disposal; runtime shader/node registration+readback; CSS/SVG+camera changes+input; XR+skinning+layers+session reset (capable lane); editor undo/redo+shared materials+serialization.\n- Pairwise: a generator over each family's option flags (from the manifest) with a fixed seed producing scenario files; run as a lane; failures are triaged into defects, never used to drop a flag.\n- Each scenario compares images and event/state traces with upstream in the functional and forced lanes.\n\n## Success criteria\n\n- All named cases pass; pairwise batch passes or files defects.\n\n## Test plan\n\n- Unit: pairwise generator tests (fixed seed, coverage of every pair of option flags per family).\n- E2E: one scenario script per named combination (`tests/e2e/combos/*.mjs`) with trace/image comparison vs upstream in the functional and forced lanes on both browsers; the XR combination on the capable lane; the generated pairwise batch as a lane.\n- Logging: per-scenario `{combination, lane, result, first_divergence}`; pairwise batch summary with the failing pairs; archived under `evidence/18b.2/`.\n- Failure artifacts: images/traces per failing scenario.\n\n## Considerations\n\n- Ownership: this bead owns the six non-host combinations; the CSS/SVG+camera+input combination is owned by 21b.1 (with 21b.7), XR+skinning+layers+session-reset by 21b.3, and editor undo/redo+shared materials+serialization by 21b.5; 23.1 aggregates all nine.\n","acceptance_criteria":"Each named combination has an explicit test comparing traces/images with upstream; a pairwise generator over feature flags produces additional combinations that run in the functional and forced lanes; pairwise tests supplement, never replace, the named higher-order cases.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:28.197389Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:17.493053Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","phase-4","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.2","depends_on_id":"f3d-18b-full-feature-gate-closure-mk4","type":"parent-child","created_at":"2026-09-09T16:48:28.197389Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.2","depends_on_id":"f3d-18b-full-feature-gate-closure-mk4.1","type":"blocks","created_at":"2026-09-09T16:49:22.342066Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-18b-full-feature-gate-closure-mk4.3","title":"Original and transformed official examples with all controls and modes","description":"## Full scope\n\nOriginal and transformed official examples: run every pinned example both as the original through the routed build and as a transformed artifact, exercising all actual controls, loaders and mode transitions; unchanged upstream skips retained in the inherited lane with supplemental tests; every otherwise unlisted example/manual/tool workflow reconciled into the inventory\n\n## Summary\n\nEvery example, both ways, with every control.\n\n## Background (plan §14.9 last paragraph, §15.5)\n\n\"Run original and transformed official examples with all actual controls, loaders and mode transitions exercised. Retain unchanged upstream skips in the inherited lane, but supply additional tests for their functional surface. Reconcile every otherwise unlisted example/manual/tool workflow into the same inventory.\"\n\n## Technical approach\n\n- Control scripts: generate from 01.7's inventory one interaction script per example covering every GUI control, keyboard/pointer handler, loader path and mode transition (backend switches, quality toggles, file inputs); review by sampling.\n- Batch runner: for every entry in the pinned examples index run (a) the original example through the routed build and (b) the transformed artifact via `f3d verify` with the script; both on Chrome and Safari; attribution per example (owner/route).\n- Upstream E2E exclusions: for each excluded example, add a candidate-side test (checkpoints under the hardware tolerances rather than the upstream screenshot rule) so its functional surface is still covered.\n- Reconcile any example/manual/tool workflow not yet in the inventory into 01.7's crosswalk before the batch is considered complete.\n- Examples whose families are closed by 21b.x (XR, audio, editor, physics, workers) are run by those beads and re-run in 23.1; this bead runs every other example.\n\n## Success criteria\n\n- Batch passes; defects filed per example.\n\n## Test plan\n\n- Unit: control-script generation tests from 01.7's inventory (every recorded control appears in the script).\n- E2E: batch runner over the full examples index, each example run as original-through-routing and as a transformed artifact via `f3d verify` with its control script, on both browsers; supplemental candidate tests for upstream E2E exclusions; crosswalk reconciliation check.\n- Logging: per-example `{example, form: original|transformed, owner, route, result}` and a batch summary; archived under `evidence/18b.3/`.\n- Failure artifacts: verify artifacts per failing example.\n\n## Considerations\n\n- This is large; run it as a lane, not per commit.\n","acceptance_criteria":"The full examples index runs in both forms with control scripts derived from the inventory (01.7); results tagged by owner/route; upstream E2E exclusions are covered by supplemental candidate tests; the crosswalk shows every workflow reconciled.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:28.296863Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:19.333433Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","phase-4","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.3","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.7","type":"blocks","created_at":"2026-09-09T17:41:12.097572Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.3","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s.4","type":"blocks","created_at":"2026-09-09T16:49:22.719314Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.3","depends_on_id":"f3d-14-material-light-texture-surface-5lj","type":"blocks","created_at":"2026-09-09T17:44:18.205294Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.3","depends_on_id":"f3d-15b-full-essl-runtime-shaders-n51","type":"blocks","created_at":"2026-09-09T17:44:18.424126Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.3","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v","type":"blocks","created_at":"2026-09-09T17:44:18.651862Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.3","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf","type":"blocks","created_at":"2026-09-09T17:44:18.876848Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.3","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.3","type":"blocks","created_at":"2026-09-09T16:49:22.526682Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.3","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.7","type":"blocks","created_at":"2026-09-09T17:11:27.337603Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.3","depends_on_id":"f3d-18b-full-feature-gate-closure-mk4","type":"parent-child","created_at":"2026-09-09T16:48:28.296863Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.3","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy","type":"blocks","created_at":"2026-09-09T17:44:19.105315Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-18b-full-feature-gate-closure-mk4.4","title":"WebGPU screenshot and tree-shaking upstream checks against the candidate","description":"## Full scope\n\nWebGPU screenshot and tree-shaking upstream checks against the candidate: the pinned WebGPU screenshot list runs through the routed build; the tree-shaking check verifies routed entry points remain tree-shakeable as upstream requires\n\n## Summary\n\nThe remaining two upstream lanes from §2.4.\n\n## Background (plan §2.4, §14.1)\n\n\"The current package scripts distinguish core unit tests, addon unit tests, end-to-end screenshots, WebGPU screenshots, and tree-shaking checks.\"\n\n## Technical approach\n\n- WebGPU screenshot lane: reuse the pinned runner's WebGPU example list and configuration through the candidate resolver (18a.1) so each example page loads the routed `three/webgpu` build; keep the expected images and thresholds untouched; tag each result with the route that actually submitted (07.6) so a screenshot rendered by a retained renderer is recorded as retained, never as new-backend coverage.\n- Tree-shaking check: run upstream's tree-shaking test inputs against the routed entry points (`three`, `three/webgpu`, `three/tsl`, addon paths); if the routing facade defeats tree-shaking (for example through side-effectful re-exports), fix the facade generation in 04.2 rather than relaxing the check; record bundle sizes per entry point before/after routing.\n- Both checks run as lanes via the repository-owned commands (DSR wiring) and write per-item results into the manifest updater (18a.5).\n\n## Success criteria\n\n- Lanes green or defects filed.\n\n## Test plan\n\n- Unit: mapping tests for the WebGPU screenshot list and the tree-shaking check inputs through the candidate resolver.\n- E2E: both upstream checks executed against the candidate with unchanged expectations on the required browser configuration; results recorded with owners.\n- Logging: per-item `{item, result, owner}`; archived under `evidence/18b.4/`.\n- Failure artifacts: screenshots/diffs and the tree-shaking report.\n\n## Considerations\n\n- Tree-shaking interacts with the routing facade design (04.2).\n","acceptance_criteria":"Both upstream checks execute against the candidate with unchanged expectations; results recorded with owners; any regression is a defect.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:28.395578Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:15.192328Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","phase-4","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.4","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:49:23.092552Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.4","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:25.747187Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.4","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.3","type":"blocks","created_at":"2026-09-09T16:49:22.903220Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.4","depends_on_id":"f3d-18b-full-feature-gate-closure-mk4","type":"parent-child","created_at":"2026-09-09T16:48:28.395578Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-18b-full-feature-gate-closure-mk4.5","title":"Genuine GL contract tests and absence-of-WebGPU tests","description":"## Full scope\n\nGenuine GL contract tests and absence-of-WebGPU tests: context identity, external context reuse, extension/state/native-handle access, sync pixels visible immediately on return, raw-GL plus Three.js interleaving and resetState, GLBufferAttribute behavior, context loss/restoration, canvas capture; WebGPU absent with working upstream GL/DOM/audio cases\n\n## Summary\n\nThe exact lane's full contract list, plus the no-WebGPU environment.\n\n## Background (plan §14.9 last paragraph, §3.3 \"No WebGPU prerequisite for unrelated functionality\")\n\n## Technical approach\n\n- GL contract scripts in the exact lane (each asserts real native objects): context identity across calls and after `resetState`; external context reuse (construct with a caller-supplied `WebGL2RenderingContext`); extension queries and native handles via the diagnostic APIs (`renderer.properties`, program handles); synchronous `readRenderTargetPixels` visible on return; raw-GL interleaving (`gl.*` calls between renders with `resetState`); `GLBufferAttribute` with a user buffer; forced context loss/restoration; `preserveDrawingBuffer` and `toDataURL` capture; constructor option matrix (`alpha`, `antialias`, `premultipliedAlpha`, `powerPreference`, `failIfMajorPerformanceCaveat`).\n- Absence-of-WebGPU profile: a browser profile with `navigator.gpu` deleted; the GL, DOM (CSS/SVG) and audio suites run through their source routes; instrument the module loader to prove no Wasm/GPU initialization.\n- Both suites run on Chrome and Safari; results tagged `owner: exact-backend` or `retained-js` and never counted toward new-backend coverage.\n\n## Success criteria\n\n- All pass on Chrome and Safari.\n\n## Test plan\n\n- Unit: none (contract tests are browser-bound).\n- E2E: exact-lane scripts on Chrome and Safari for every listed GL contract (context identity, external context reuse, extension/state/native-handle access, sync pixels visible on return, raw-GL + Three.js interleaving with `resetState`, `GLBufferAttribute`, context loss/restoration, canvas capture) with real native objects asserted; a browser profile with `navigator.gpu` removed running the GL/DOM/audio cases with no Wasm/GPU initialization.\n- Logging: per-contract `{contract, native_type_observed, result}` tagged `owner: exact-backend|retained-js`; archived under `evidence/18b.5/`.\n- Failure artifacts: GL error logs and screenshots.\n\n## Considerations\n\n- Never counts toward new-backend coverage.\n","acceptance_criteria":"The exact lane covers every listed GL contract with real native objects asserted; a WebGPU-absent environment runs the GL/DOM/audio cases successfully through their source routes with no Wasm/GPU initialization; results tagged owner: exact-backend / retained.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:28.493399Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:19.728122Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-conformance","phase-4","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.5","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.3","type":"blocks","created_at":"2026-09-09T16:49:23.451298Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.5","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:25.968961Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.5","depends_on_id":"f3d-18a-early-upstream-tests-feature-gates-zj1.4","type":"blocks","created_at":"2026-09-09T16:49:23.274121Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.5","depends_on_id":"f3d-18b-full-feature-gate-closure-mk4","type":"parent-child","created_at":"2026-09-09T16:48:28.493399Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.5","depends_on_id":"f3d-21b-all-host-tool-integrations-5mw.2","type":"blocks","created_at":"2026-09-09T17:44:19.501678Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.5","depends_on_id":"f3d-21b-all-host-tool-integrations-5mw.7","type":"blocks","created_at":"2026-09-09T17:44:19.727612Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-18b-full-feature-gate-closure-mk4.6","title":"Release verifier full run with separate coverage counts","description":"## Full scope\n\nRelease verifier full run: zero blocking states across the manifest, separate counts for import/symbol closure, validated behavior families, new-backend rendering coverage, exact-component integration and capable-host validation; unavailable-host obligations listed, never counted\n\n## Summary\n\nThe gate check for Phase 4's feature closure.\n\n## Background (plan §14.10, §18 Phase 4 exit)\n\n\"No unimplemented/unclassified/stubbed valid-source feature, full tests pass, portable-rendering closure passes.\"\n\n## Technical approach\n\n- Run `f3d verify-upstream --ref r186 --features all --tests all` on the final build after all family suites (18b.1–18b.5 plus the per-family beads) have executed; the updater sets statuses from evidence pointers first.\n- Triage every remaining non-passing record into one of: a defect bead (candidate implementation gap, filed with the failing test and owner), or a capable-host obligation (reference shares the missing prerequisite; evidence attached; listed for 23.1). No third category exists.\n- Emit the five counts separately (import/symbol closure; validated behavior families; new-backend rendering coverage; exact-component integration; capable-host validation) in text and JSON; assert programmatically that no line contains a merged percentage.\n- Gate: exit 0 only when all families closed by 12–17 and 19 have zero blocking states; otherwise the run fails and the report lists the exact records.\n- Third evidence class (plan §14.10): a documented upstream limitation, where the pinned feature has no executable positive path even on its specified host prerequisites; the exact conditional implementation/error behavior is preserved and tested, the record is listed under that evidence class explicitly, and it is never counted as positive validation or used to conceal a candidate gap.\n\n## Success criteria\n\n- Verifier exit 0 for the families closed by 12–17 and 19; remaining host obligations feed 23.1.\n\n## Test plan\n\n- Unit: none new (verifier rules tested in 18a.5).\n- E2E: the full verifier run on the final manifest after all family suites; every remaining item triaged into defect beads or listed as a capable-host obligation.\n- Logging: the verifier report with the five separate counts and the obligation list; archived under `evidence/18b.6/`.\n- Failure artifacts: the rule-violation list.\n\n## Considerations\n\n- \"Lack of access to a required headset, browser or other supported host is an outstanding verification obligation, not a passed gate or a feature exclusion\" (§14.10).\n","acceptance_criteria":"f3d verify-upstream reports no unmatched paths, no untested/unimplemented/stub/no-op/refusal records, no unexplained skips, complete codec/variant closure and no known regressions in the closed families; the five numbers are printed separately; capable-host obligations not yet met are listed as open obligations.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:28.592806Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:19.957606Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["compat-manifest","f3d-conformance","phase-4","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.6","depends_on_id":"f3d-18b-full-feature-gate-closure-mk4","type":"parent-child","created_at":"2026-09-09T16:48:28.592806Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.6","depends_on_id":"f3d-18b-full-feature-gate-closure-mk4.1","type":"blocks","created_at":"2026-09-09T16:49:23.657739Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.6","depends_on_id":"f3d-18b-full-feature-gate-closure-mk4.2","type":"blocks","created_at":"2026-09-09T16:49:23.852102Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.6","depends_on_id":"f3d-18b-full-feature-gate-closure-mk4.3","type":"blocks","created_at":"2026-09-09T16:49:24.049528Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.6","depends_on_id":"f3d-18b-full-feature-gate-closure-mk4.4","type":"blocks","created_at":"2026-09-09T16:49:24.236025Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-18b-full-feature-gate-closure-mk4.6","depends_on_id":"f3d-18b-full-feature-gate-closure-mk4.5","type":"blocks","created_at":"2026-09-09T16:49:24.417758Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-19-loaders-exporters-codecs-qmy","title":"F3D-19: All loaders/exporters/registrations/runtime codecs and current-state round trips","description":"## What this epic delivers\n\nThe complete asset I/O surface. Plan §19 F3D-19 depends on 10 and 14; acceptance: \"New inputs, every existing format/options, plugin contracts, import/edit/export/reimport\".\n\n## Why (plan §5.12 loader/exporter rows, §5.13, §10.2)\n\n\"A loader is not complete merely because one bundled model renders.\" \"Export must work after an accelerated update.\" \"Runtime decoding and transcoding are mandatory whenever a caller can provide previously unseen compressed models, textures, archives, or local files.\"\n\n## Children\n\n1. Loader family closure.\n2. Texture/image/compression loaders and runtime codecs/transcoders.\n3. glTF extension and plugin inventory from code registrations.\n4. Exporters and current-state round trips.\n5. Plugin registration/callback identity, independent instances and dedupe rules.\n\n## Success criteria for the epic\n\nAll children closed; the loader/exporter families in the manifest are fully owned and tested; retained codec/engine bytes reported separately with licenses.\n\n## No-claim boundary\n\nRetained loaders are compositional compatibility, not Rust coverage; third-party codec Wasm of C/C++ origin is an inherited dependency, never described as memory-safe Rust (§13.5).\n","acceptance_criteria":"Every loader, texture/compression loader, runtime decoder/transcoder, glTF extension/plugin registration and exporter in the manifest has a production owner with load/loadAsync/parse/async-parse, LoadingManager, headers/credentials/CORS, response type, progress/cancellation/error/caching/relative-dependency and options coverage; unseen runtime inputs decode; import→edit/animate→export→re-import round trips pass in both reference and candidate with cross-loading; plugin registration/unregistration and callback identity/order are preserved.","status":"open","priority":2,"issue_type":"epic","created_at":"2026-09-09T16:46:24.081924Z","created_by":"jemanuel","updated_at":"2026-09-09T16:49:28.248117Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-assets","loaders","phase-2","phase-4","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-19-loaders-exporters-codecs-qmy","depends_on_id":"f3d-10-standalone-networked-packaging-jlu","type":"blocks","created_at":"2026-09-09T16:49:28.047381Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-19-loaders-exporters-codecs-qmy","depends_on_id":"f3d-14-material-light-texture-surface-5lj","type":"blocks","created_at":"2026-09-09T16:49:28.247785Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-19-loaders-exporters-codecs-qmy.1","title":"Loader family closure (every core/addon loader; load/loadAsync/parse; manager; network; errors)","description":"## Full scope\n\nLoader family closure: every core and addon loader (glTF/GLB, OBJ/MTL, FBX, Collada, STL, PLY, 3DM, 3MF, 3DS, USD/USDZ, LDraw, AMF, VRML, VTK, VOX, point clouds and splats, volume/scientific data, fonts, SVG, animation and archive formats present in the tree); load, loadAsync, parse and async parse; LoadingManager handlers and URL modifiers; request headers/credentials/CORS; response types; progress, cancellation where offered, errors, caching and relative dependencies; multi-file references and metadata; arrays, cameras, scenes, animations and independent mutable instances preserved\n\n## Summary\n\nAll the loaders, tested the way the plan demands.\n\n## Background (plan §5.12 \"Model, geometry and scientific loaders\", §5.13)\n\n\"Retain full loaders/runtime dependencies; load/parse/async/local-file/network variants, multi-file references, metadata and error paths.\"\n\n## Technical approach\n\n- Retained loaders through the facade (every core and addon loader from the census: glTF/GLB, OBJ/MTL, FBX, Collada, STL, PLY, 3DM, 3MF, 3DS, USD/USDZ, LDraw, AMF, VRML, VTK, VOX, point clouds/splats, volume/scientific data, fonts, SVG, animation and archive formats), with their runtime dependencies (decoders, worker scripts, external libraries) resolved through the asset resolver (10.1) in embedded and networked modes.\n- Per-loader test template generated from the manifest covering: `load`, `loadAsync`, `parse`, async parse where provided; `LoadingManager` handlers (`onStart/onProgress/onLoad/onError`), `setURLModifier`, `setPath`/`setResourcePath`; request headers/credentials/CORS via a local server with toggles; response types; progress events; cancellation where upstream offers it; error paths (404, malformed data) preserving upstream error shapes; `Cache` behavior; relative dependencies (MTL/texture references, multi-file assets); metadata/cameras/scenes/animations arrays; independent mutable instances per load.\n- Fixtures: small licensed sample files per format (upstream examples' assets where licensed; otherwise synthesized).\n\nPrior notes retained:\n- Retained loader modules routed through the facade; the resolver (10.1) supplies both embedded and networked backends; a generated per-loader test template covering the listed API surface with small fixtures per format (from upstream examples' assets where licensed).\n\n## Success criteria\n\n- Family suite green; manifest updated.\n\n## Test plan\n\n- Unit: generated per-loader test template covering `load`/`loadAsync`/`parse`/async parse, LoadingManager handlers and URL modifiers, headers/credentials/CORS, response types, progress, cancellation where offered, errors, caching, relative dependencies, multi-file references, metadata; small fixtures per format (licensed upstream assets or synthesized).\n- E2E: loader family suite in Node and the browser as each loader requires, on both browsers; local-file (file input) and network (local server with CORS/credential toggles) variants; error paths preserve upstream error shapes.\n- Logging: per-loader per-behavior results `{loader, behavior, variant, result, owner}`; request logs from the local server; archived under `evidence/19.1/`.\n- Failure artifacts: loaded-object semantic dumps and the server request log.\n\n## Considerations\n\n- Cancellation semantics exist only where upstream offers them; do not invent.\n","acceptance_criteria":"Every loader manifest record is owned (retained production loader with its runtime dependencies wired through the resolver) with positive tests for each listed behavior; local-file and network variants tested; error paths preserve upstream error shapes.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:28.693255Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:23.811906Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-assets","loaders","phase-2","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.1","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:49:26.189139Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.1","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.1","type":"blocks","created_at":"2026-09-09T16:49:26.008346Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.1","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:26.182598Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.1","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy","type":"parent-child","created_at":"2026-09-09T16:48:28.693255Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-19-loaders-exporters-codecs-qmy.2","title":"Texture/image/compression loaders with runtime codecs and transcoders included by closure","description":"## Full scope\n\nTexture/image and compression loaders with runtime codecs and transcoders: HDR/RGBE, EXR, UltraHDR, DDS, KTX, KTX2 (Basis Universal transcoder), PVR, TGA, TIFF, LUT loaders; Draco decoder, meshopt decoder and any other shipped decoder/transcoder; included at runtime when reachable; capability-selected embedded variants; mipmaps, block edges, orientation, premultiplication, color conversion and samplers preserved\n\n## Summary\n\nCodecs are part of full compatibility, at runtime, with honest attribution.\n\n## Background (plan §5.12 \"Texture/image and compression loaders\", §10.2, §13.5)\n\n\"Include runtime codecs when future input needs them; browser/build-time preprocessing is an optimization, not the only supported path.\"\n\n## Technical approach\n\n- Codec wiring: pinned Draco decoder (JS/Wasm), Basis Universal transcoder (KTX2), meshopt decoder and any other shipped decoder resolved through the asset resolver (10.1); retained loaders keep their upstream worker pools unchanged (F3D only resolves worker-script URLs and decoder/transcoder assets through the resolver; the F3D worker protocol of 21b.6 applies to F3D-owned jobs only); capability-driven transcode targets (BC/ETC2/ASTC/uncompressed fallback) from the device record (14.4).\n- Loaders retained through the facade: HDR/RGBE, EXR, UltraHDR, DDS, KTX, KTX2, PVR, TGA, TIFF, LUT loaders and any texture/image loader the census lists; each with the 19.1 test template.\n- Variant selection never reduces quality: an embedded capability variant (10.7) is used only when it preserves format/content/filtering behavior; otherwise the runtime transcoder path is used.\n- Invariants tested per format: mipmaps present and correct, block edges, orientation/flipY, premultiplication, color space, sampler settings.\n- Report: codec bytes and licenses listed separately (plan §13.5).\n\nPrior notes retained:\n- Wire the pinned decoder/transcoder assets (Draco JS/Wasm, Basis transcoder, meshopt) through the resolver with worker support where upstream uses workers (`DRACOLoader` worker pool) under 21b.6's worker rules; closure inclusion per 10.7.\n- Texture upload of transcoded formats via 14.4.\n\n## Success criteria\n\n- Family suite green; standalone unseen-asset test passes; report lists codec bytes.\n\n## Test plan\n\n- Unit: codec wiring tests (Draco/Basis/meshopt decoders reachable through the resolver; worker-script and decoder-asset resolution through 10.1 in embedded and networked modes); format-variant selection tests from capability records (never lossier than source); mip/block-edge/orientation/premultiply invariants per format.\n- E2E: texture/compression loader suite on both browsers (HDR/RGBE, EXR, UltraHDR, DDS, KTX, KTX2 with transcoding to every device-supported family, PVR, TGA, TIFF, LUTs); standalone artifact with file input loading an unseen KTX2/Draco/meshopt asset; codec bytes and licenses in the report.\n- Logging: per-asset `{loader, format, codec, transcode_target, ms, bytes}` and inclusion decisions; archived under `evidence/19.2/`.\n- Failure artifacts: decoded-texture dumps and diff images.\n\n## Considerations\n\n- \"A 'please preconvert your model' error is not full compatibility\" (§10.2).\n","acceptance_criteria":"Every texture/compression loader and codec manifest record is owned and tested; an application with runtime input can load an unseen KTX2/Draco/meshopt asset in the standalone artifact because the codec was included by closure; format variants selected by capability never reduce source quality; third-party codec bytes and licenses are reported separately.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:28.792858Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:20.492628Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-assets","loaders","phase-2","phase-4","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.2","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.7","type":"blocks","created_at":"2026-09-09T16:49:26.556722Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.2","depends_on_id":"f3d-14-material-light-texture-surface-5lj.4","type":"blocks","created_at":"2026-09-09T16:49:26.752145Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.2","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy","type":"parent-child","created_at":"2026-09-09T16:48:28.792858Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.2","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy.1","type":"blocks","created_at":"2026-09-09T16:49:26.377219Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-19-loaders-exporters-codecs-qmy.3","title":"glTF extension and plugin inventory from code registrations","description":"## Full scope\n\nglTF extension and plugin inventory from code registrations: every built-in registered extension in the pinned GLTFLoader plus shipped/example-registered plugins (instancing, compression, texture formats/transforms, material extensions, animation pointer, variants, progressive/splat integrations as present); parser/plugin callbacks, metadata and unknown/required-extension behavior preserved\n\n## Summary\n\nInventory code registrations, not doc bullets.\n\n## Background (plan §5.12 \"glTF and material extensions\", §5.13, §23 [S58])\n\n\"Inventory code registrations, not only documentation bullets; preserve parser/plugin callbacks, metadata and unknown/required-extension behavior.\" \"The inspected GLTFLoader registers extensions beyond what a short documentation list can safely establish.\"\n\n## Technical approach\n\n- Static extraction over the pinned `GLTFLoader.js`: the `EXTENSIONS` table, every `this.register(parser => new ...)` call, and the extension classes' `name` fields; plus registrations in examples (`GLTFLoader` plugins registered in example pages, `KHR_materials_variants`, animation pointer, splat/progressive integrations as present); emit the inventory file.\n- Test assets per extension from licensed glTF sample sets or synthesized minimal files; parse through the routed loader and compare the resulting scene semantics with upstream; verify plugin callback ordering (`beforeRoot`/`afterRoot`/`loadMesh`/`loadMaterial`... as pinned).\n- Unknown/required extension behavior (`extensionsRequired` with an unknown extension) matches upstream's error/warning contract.\n- Exporter cross-check for extensions with `GLTFExporter` counterparts (19.4).\n\nPrior notes retained:\n- Static extraction from the pinned `GLTFLoader.js` (`this.register(...)` calls, `EXTENSIONS` table) and from examples registering plugins; test assets per extension (from the glTF sample repositories where licensed, otherwise synthesized).\n\n## Success criteria\n\n- Inventory committed; tests green.\n\n## Test plan\n\n- Unit: registration extractor tests over the pinned `GLTFLoader.js` (`register` calls, `EXTENSIONS` table) and example registrations; per-extension parse tests with sample assets; unknown/required-extension behavior tests matching upstream.\n- E2E: browser loads of every extension asset through the routed build on both browsers with semantic comparison to upstream; exporter cross-check where a counterpart exists.\n- Logging: per-extension `{extension, source: builtin|example, asset, result}`; archived under `evidence/19.3/`.\n- Failure artifacts: parsed-scene semantic diffs.\n\n## Considerations\n\n- Material extensions interact with 14.2's physical closure.\n","acceptance_criteria":"The extension list is derived from the pinned GLTFLoader's registration code and example registrations (not documentation), each with a positive test asset; required-but-unknown extension behavior matches upstream; GLTFExporter plugin counterparts are cross-checked.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:28.900427Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:22.936186Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-assets","loaders","phase-2","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.3","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:26.394435Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.3","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy","type":"parent-child","created_at":"2026-09-09T16:48:28.900427Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.3","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy.1","type":"blocks","created_at":"2026-09-09T16:49:26.929745Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-19-loaders-exporters-codecs-qmy.4","title":"Exporters and current-state round trips after accelerated updates","description":"## Full scope\n\nExporters and current-state round trips: core JSON/object/material/geometry/node serialization and every addon exporter (glTF/GLB, OBJ, PLY, STL, USDZ, Draco, EXR, KTX2 and any additional source entry); export after an accelerated update materializes current CPU-observable transforms, geometry, skin/morph data, animation, textures and metadata at the writer's original boundaries; import→edit/animate→export→re-import in reference and candidate plus cross-loading; byte-exact where specified, semantic compare otherwise; lossy codecs with identical settings and tolerance\n\n## Summary\n\nExport is a read of current state at the writer's boundary.\n\n## Background (plan §5.12 \"Exporters and serialization\", §5.13)\n\n\"Retain production writers, runtime encoding dependencies and CPU materialization; semantic round trips and source-compatible output/options/errors.\" \"Never export the initial captured scene while the user sees a newer scene. Do not promise an exporter captures dynamic GPU-only state that upstream itself does not export.\"\n\n## Technical approach\n\n- Exporters retained through the facade: core `toJSON` family (Object3D/Material/Geometry/Texture/nodes) and every addon exporter (`GLTFExporter` with plugins, `OBJExporter`, `PLYExporter`, `STLExporter`, `USDZExporter`, `DRACOExporter`, `EXRExporter`, `KTX2Exporter`, and any other entry the census lists) with their runtime encoding dependencies included when reachable (10.7).\n- Pre-export materialization: the routed exporter entry points call the publication hook (06.4/12b.2) so Wasm-owned transforms, geometry, skin/morph data, animation state and texture updates are current at the writer's original boundary; exporters never see the initial captured scene.\n- Round-trip harness in `f3d-conformance`: import → edit/animate under acceleration → export → re-import on reference and candidate; cross-load (reference export into candidate and vice versa); compare semantically (node hierarchy, attributes within f32 tolerance where formats round, materials, animations, metadata); byte-exact only where upstream's writer is deterministic for identical input (for example the STL/PLY binary writers); glTF/GLB and USDZ are compared semantically per §5.13, since upstream does not specify byte-exact output for them; lossy codecs with identical settings and tolerance.\n- Option/error coverage per exporter from the manifest; upstream warnings preserved (for example unsupported features per format).\n\n## Success criteria\n\n- Family suite and round-trip suite green.\n\n## Test plan\n\n- Unit: per-exporter option/error tests; pre-export materialization hook tests (exporter reads observe current Wasm-owned state); semantic round-trip comparator tests (byte-exact only where upstream's writer is deterministic, for example STL/PLY binary; semantic for glTF/GLB/USDZ).\n- E2E: round-trip suite `tests/e2e/exporters/roundtrip.mjs` for every format pair upstream supports: import → edit/animate (accelerated) → export → re-import on reference and candidate, plus cross-loading, on both browsers; lossy codecs with identical settings and tolerance; runtime encoders (Draco/KTX2) included when reachable.\n- Logging: per-round-trip `{format, direction, semantic_delta, byte_exact: bool}`; archived under `evidence/19.4/`.\n- Failure artifacts: exported files from both sides and the semantic diff.\n\n## Considerations\n\n- Different lossy encodings are not automatically equivalent.\n","acceptance_criteria":"Every exporter manifest record is owned and tested with options/errors; the round-trip suite passes for every format pair where upstream supports it, after accelerated animation/transform updates; exporters never see stale captured state; runtime encoders (Draco/KTX2 encoding) are included when reachable.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:29.011945Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:20.871891Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-assets","loaders","phase-2","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.4","depends_on_id":"f3d-06-packed-transforms-materialization-roa.4","type":"blocks","created_at":"2026-09-09T16:49:27.289129Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.4","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr.1","type":"blocks","created_at":"2026-09-09T16:49:27.483177Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.4","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy","type":"parent-child","created_at":"2026-09-09T16:48:29.011945Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.4","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy.1","type":"blocks","created_at":"2026-09-09T16:49:27.109702Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-19-loaders-exporters-codecs-qmy.5","title":"Loader/exporter plugin registration, callback identity, independent instances, dedupe rules","description":"## Full scope\n\nLoader/exporter plugin registration and unregistration, callback identity and order, independent mutable instances per load, and asset byte deduplication rules that never share mutable objects\n\n## Summary\n\nThe extension-point contract for asset I/O.\n\n## Background (plan §5.13 last sentence, §10.1)\n\n\"Preserve custom plugin registration/unregistration and callback identity/order for loaders and exporters.\" \"Deduplicating asset bytes must not accidentally share mutable scene objects, materials, animation state, or callback identities.\"\n\n## Technical approach\n\n- Registration APIs: for every loader/exporter exposing plugin hooks (`GLTFLoader.register/unregister`, `GLTFExporter.register/unregister`, `KTX2Loader` transcoder configuration, `DRACOLoader` decoder configuration, `LoadingManager` handlers/modifiers), record the pinned callback signature and invocation order; tests register/unregister custom plugins and assert callbacks fire in upstream order with identical `this`/argument identities.\n- Independent instances: load the same asset twice (and via `parse` from the same bytes) and assert distinct object graphs (`!==`) with independent mutation; verify materials/animation clips/textures are not shared unless upstream shares them.\n- Dedupe: with the resolver's byte-level dedupe (10.7) enabled, repeat the identity tests; also verify that resolver-level caching preserves upstream `Cache` semantics (`Cache.enabled` on/off) rather than adding a second cache layer.\n\n## Success criteria\n\n- Tests green.\n\n## Test plan\n\n- Unit: registration/unregistration API tests with callback identity and invocation order asserted; repeated-load independence tests; resolver dedupe tests (bytes shared, objects independent).\n- E2E: browser tests on both browsers for plugin lifecycles and repeated loads of the same asset.\n- Logging: callback-order traces and identity checks; archived under `evidence/19.5/`.\n- Failure artifacts: the callback trace diff.\n\n## Considerations\n\n- Relates to 10.7's dedupe implementation.\n","acceptance_criteria":"Registering/unregistering custom loader/exporter plugins works with callback identity and invocation order equal to upstream; loading the same asset twice yields independent mutable object graphs; deduplicated bytes are shared only at the byte level.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:29.115771Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:16.417544Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-assets","loaders","phase-2","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.5","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy","type":"parent-child","created_at":"2026-09-09T16:48:29.115771Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.5","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy.1","type":"blocks","created_at":"2026-09-09T16:49:27.664548Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-19-loaders-exporters-codecs-qmy.5","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy.4","type":"blocks","created_at":"2026-09-09T16:49:27.861523Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-20-profile-justified-gpu-improvements-023","title":"F3D-20: Profile-justified new-backend GPU improvements (H7/H8 and the general corpus, with ablations)","description":"## What this epic delivers\n\nPhase 4's \"demanding performance\" work. Plan §19 F3D-20 depends on 13, 16, 17; acceptance: \"H7/H8/general corpus ablations; retained renderers cannot satisfy accelerated closure\".\n\n## Why (plan §8.7, §12.3, §12.4, §16.8, §18 Phase 4)\n\n\"Apply measured GPU optimizations only after the conservative full behavior works.\" \"Run ablations that disable one optimization at a time... Publish where the gain comes from and where the bottleneck moved. Do not multiply isolated kernel speedups together.\"\n\n## Children\n\n1. GPU compute offload decisions with CPU paths retained.\n2. Specialization portfolio, bounded calibration and plan caching.\n3. Optional hardware features (immediates, subgroup-size control, FP16, transient attachments).\n4. H7/H8 profile-driven improvements.\n5. Ablation suite and attribution.\n6. GPU picking as an additional async API and GPU culling with conservative recovery.\n\n## Success criteria for the epic\n\nAll children closed; every optimization has an ablation row; H7/H8 results published with attribution.\n\n## No-claim boundary\n\n\"A 5× transform kernel and a 2× uploader do not imply a 10× application\" (§16.8).\n","acceptance_criteria":"Measured GPU-side improvements (compute offload decisions, the specialization portfolio with bounded calibration, optional hardware features, H7/H8 kernel/data-path improvements, GPU picking/culling as additional strategies) are applied only after conservative full behavior works, each with an ablation measurement attributing gains; retained renderers never satisfy accelerated closure.","status":"open","priority":2,"issue_type":"epic","created_at":"2026-09-09T16:46:24.164322Z","created_by":"jemanuel","updated_at":"2026-09-09T16:49:31.634670Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-gpu","f3d-graph","h7","h8","new-backend","phase-4","tier-frontier"],"dependencies":[{"issue_id":"f3d-20-profile-justified-gpu-improvements-023","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr","type":"blocks","created_at":"2026-09-09T16:49:31.219188Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v","type":"blocks","created_at":"2026-09-09T16:49:31.426255Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf","type":"blocks","created_at":"2026-09-09T16:49:31.634382Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-20-profile-justified-gpu-improvements-023.1","title":"GPU compute offload decisions with CPU paths retained and precision contracts","description":"## Full scope\n\nGPU compute offload decisions: move work to compute only where it removes a demonstrated bottleneck without costly or semantically invalid readback (already-GPU-authored TSL kernels, culling, large animation batches with appropriate state authority, geometry without CPU observations); keep CPU paths for small batches and synchronous public observations; f64-to-f32 moves only under a separately admitted precision contract\n\n## Summary\n\nCompute offload as a justified decision, not a reflex.\n\n## Background (plan §8.7)\n\n\"Move work to compute only when doing so removes a demonstrated bottleneck and does not require costly or semantically invalid readback... Moving CPU-authored `f64` simulation to `f32` compute is not an exact optimization; it requires a separately admitted precision contract and cannot enter the primary score merely because the image initially looks similar.\"\n\n## Technical approach\n\n- Candidate list from profiles (03.4, 07.7 telemetry): already-GPU-authored TSL kernels (keep on GPU), frustum/occlusion culling, large animation batches (pose evaluation for many instances), procedural geometry whose CPU observations are absent (13.7 rule).\n- Per candidate: (1) an ownership analysis proving no synchronous CPU observation depends on the offloaded result before a GPU boundary (12b.2 read paths); (2) a state-authority decision (Wasm-owned region publishing only at boundaries the source allows); (3) a CPU path retained for small batches and for hosts without the needed capabilities; (4) precision: any move of CPU-authored f64 work to f32 compute is only in the separately labeled optional mode with its own contract.\n- Kernels use 16.3's compute contracts; dispatch splitting and bounds handling apply.\n- Ablation entry per offload; profile and paired runs decide whether the offload stays enabled by default.\n\n## Success criteria\n\n- Decisions documented with profiles; tests for fallback equivalence.\n\n## Test plan\n\n- Unit: ownership-analysis tests per offload candidate (a synchronous CPU observation blocks offload); CPU-fallback equivalence tests; precision-contract gating tests (f64→f32 only in the labeled optional mode).\n- E2E: per-candidate paired runs and ablations on the reserved devices; equivalence via `f3d verify` in the forced lane with the fallback on and off.\n- Logging: decision records `{candidate, profile_ref, observations, fallback, mode}` and ablation deltas; archived under `evidence/20.1/`.\n- Failure artifacts: readback diffs between offloaded and CPU paths.\n\n## Considerations\n\n- GPU-only state changes device-loss classification (17.7).\n","acceptance_criteria":"Each offload candidate has a profile showing the bottleneck, an ownership analysis proving no synchronous CPU observation is violated, a CPU fallback retained, and an ablation; any precision change is in a separately labeled optional mode.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:29.212435Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:19.563257Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-compiler","f3d-gpu","new-backend","phase-4","tier-frontier"],"dependencies":[{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.1","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr.1","type":"blocks","created_at":"2026-09-09T16:49:28.648008Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.1","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.3","type":"blocks","created_at":"2026-09-09T16:49:28.452417Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.1","depends_on_id":"f3d-20-profile-justified-gpu-improvements-023","type":"parent-child","created_at":"2026-09-09T16:48:29.212435Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-20-profile-justified-gpu-improvements-023.2","title":"Specialization portfolio, bounded calibration and plan caching (no online ML)","description":"## Full scope\n\nSpecialization portfolio and bounded calibration: choose among CPU vs GPU culling, ordinary draws vs bundles, conservative workgroup sizes, a few legal data-layout choices and simple vs clustered lighting by bounded measurement on representative kernels outside the interactive critical path; cache the selected plan against source/runtime/browser/capability identity; persist source-level recipes and WGSL, never GPU handles; conservative defaults; stale-profile invalidation; no online ML optimizer\n\n## Summary\n\nTuning without a research platform.\n\n## Background (plan §12.3, §12.4)\n\n\"Perform bounded calibration on representative kernels, outside the interactive critical path. Cache the selected plan against source/runtime/browser/capability identity. Persist source-level recipes and WGSL, not GPU handles or assumed portable driver binaries... A tuning decision changes how equivalent work is executed, not how much visual or simulation work is required. Do not introduce an online ML optimizer or numerical-policy subsystem into every frame.\"\n\n## Technical approach\n\n- Portfolio table in `f3d-runtime` with the options and their legality preconditions: CPU vs GPU culling (20.6 rule), ordinary draws vs bundles (08.4 structural key stability), workgroup sizes per compute kernel (16.3 bounds), data-layout choices among the schema's legal alternatives (05.2), simple forward vs clustered lighting (14.3 overflow rule).\n- Calibration: a preparation region task (02.5) runs bounded micro-workloads per option at startup or on demand outside interactive frames; each measurement is a `metric` event; a small explainable policy (best median with a margin, else conservative default) picks per option.\n- Cache: the selection is stored in the build cache (10.6) keyed by capability record, source hash, runtime revision and browser build; any change invalidates; persisted content is recipes and WGSL, never GPU handles.\n- Every option is equivalence-tested (`f3d verify`) so tuning never changes the amount of visual or simulation work.\n\n## Success criteria\n\n- Policy tests; cache invalidation tests; equivalence across all options.\n\n## Test plan\n\n- Unit: policy tests (each option's legality precondition enforced; conservative default chosen when calibration is absent); cache identity/invalidation tests (capability record, source, runtime, browser changes miss); a test that no option changes visual/simulation work.\n- E2E: calibration runs in a preparation region outside interactive frames on each stratum; equivalence via `f3d verify` across every option; chosen options reported.\n- Logging: calibration `metric` events `{option, candidate, ms, chosen}` and cache events; archived under `evidence/20.2/`.\n- Failure artifacts: the calibration table and diff images for any non-equivalent option.\n\n## Considerations\n\n- \"Optional offline analysis may use the sibling scientific libraries, but the chosen policy should remain small and explainable.\"\n","acceptance_criteria":"A small explainable policy selects among the enumerated implementations using calibration runs bounded in time and executed outside interactive frames; the selection is cached by identity and invalidated when any component changes; defaults are conservative; the policy never changes the amount of visual or simulation work.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:29.311397Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:12.897416Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-graph","f3d-runtime","new-backend","phase-4","tier-frontier"],"dependencies":[{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.2","depends_on_id":"f3d-02-asupersync-browser-execution-58j.5","type":"blocks","created_at":"2026-09-09T17:41:12.897018Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.2","depends_on_id":"f3d-08-legal-grouping-versions-bundles-ago.4","type":"blocks","created_at":"2026-09-09T16:49:28.837733Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.2","depends_on_id":"f3d-14-material-light-texture-surface-5lj.3","type":"blocks","created_at":"2026-09-09T16:49:29.032736Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.2","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.3","type":"blocks","created_at":"2026-09-09T16:49:29.230357Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.2","depends_on_id":"f3d-20-profile-justified-gpu-improvements-023","type":"parent-child","created_at":"2026-09-09T16:48:29.311397Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-20-profile-justified-gpu-improvements-023.3","title":"Optional hardware features as capability-checked labeled variants (immediates, subgroups, FP16)","description":"## Full scope\n\nOptional hardware features as capability-checked, separately labeled variants: immediates for small dynamic binding overhead, subgroup-size control for specific compute kernels, FP16 only under the precision contract, transient attachments where supported; portable paths always retained; unknown capabilities recorded as unknown\n\n## Summary\n\nUse what the device offers without assuming it.\n\n## Background (plan §2.6, §12.2, §12.3, §23 [S21], [S22])\n\n\"Current Chrome documentation describes immediates and subgroup-size control. These are useful optional specialization opportunities, not capabilities to assume on every browser or adapter.\"\n\n## Technical approach\n\n- Immediates: where the device enables the feature, bind small per-draw dynamic values (for example object index/base) through immediates instead of dynamic offsets; portable path keeps dynamic-offset slices; equivalence and ablation per stratum.\n- Subgroup-size control: request the feature and choose subgroup-sized workgroups for the specific compute kernels that benefit (reductions in 16.3); the portable path handles arbitrary subgroup widths and partial groups.\n- FP16: enable `shader-f16` variants only for applications whose admitted precision contract allows it (12a.2 numeric semantics); the variant is labeled and excluded from the primary score.\n- Transient attachments: use the capability-specific path from 14.8 where advertised.\n- Every variant reads the enabled features from the created device's capability record (plan §12.2; type from 05.1), never the adapter's advertised maxima; feature names/WGSL directives/binding APIs are versioned together.\n\n## Success criteria\n\n- Tests on strata with and without the features.\n\n## Test plan\n\n- Unit: capability-gating tests per feature (requested only when advertised; used only against enabled features/limits; portable fallback present); report-label tests (optional variants labeled; precision-changing variants excluded from the primary score).\n- E2E: runs on strata with and without each feature on both browsers; equivalence for non-precision variants; labeled bench variants.\n- Logging: feature usage per run `{feature, available, used, variant_label}`; archived under `evidence/20.3/`.\n- Failure artifacts: diff images and the capability record.\n\n## Considerations\n\n- Feature names, WGSL directives and binding APIs are versioned together.\n","acceptance_criteria":"Each optional feature is requested only when advertised, used only against the created device's enabled features/limits, has a portable fallback, and is reported as an optional variant; FP16/relaxed variants never enter the primary score.","status":"open","priority":3,"issue_type":"task","created_at":"2026-09-09T16:48:29.431083Z","created_by":"jemanuel","updated_at":"2026-09-09T17:31:48.757706Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-shader","new-backend","phase-4","tier-frontier"],"dependencies":[{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.3","depends_on_id":"f3d-14-material-light-texture-surface-5lj.8","type":"blocks","created_at":"2026-09-09T16:49:29.627326Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.3","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.3","type":"blocks","created_at":"2026-09-09T16:49:29.431271Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.3","depends_on_id":"f3d-20-profile-justified-gpu-improvements-023","type":"parent-child","created_at":"2026-09-09T16:48:29.431083Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-20-profile-justified-gpu-improvements-023.4","title":"H7/H8 profile-driven improvements measured end to end","description":"## Full scope\n\nH7/H8 profile-driven improvements: equivalent kernel and data-path changes for the modern-compute cells (pose/update specialization and cross-pass reuse for H7; kernel-chain scheduling, packed cell data layout, indirect dispatch sizing for H8) measured end to end under the frozen protocol; results published whether or not they reach 2x\n\n## Summary\n\nThe hardest viability question, answered with measurements rather than postponed.\n\n## Background (plan §16.7 \"Generalization gate\", §20 \"Modern compute demos remain GPU-bound and <2×\")\n\n\"Profile H7/H8 references during Phase 0 and test promising equivalent kernel/data-path changes early, rather than postponing the hardest viability question until the renderer is complete.\"\n\n## Technical approach\n\n- H7: profile the candidate's steady-state frame with 07.7 counters and GPU timestamps where available; candidate improvements: pose evaluation shared by (clip, time) across instances where per-instance data permits, skinned-output reuse across the shadow and main passes when the pass graph proves identical inputs (06.3 versions), storage-buffer layout packing for per-instance skeleton/morph data; each change passes `f3d verify` (including shadows and per-instance variation) before measurement.\n- H8: profile the kernel chain (dispatches, atomics, indirect sizing); candidate improvements: fewer redundant passes in the MLS-MPM chain when dependencies allow (16.3 splitter proves legality), packed cell-data layout, indirect dispatch sizing from runtime particle counts, avoidance of unnecessary clears/copies; the numeric contract (fixed step, seeded, reduction order) is asserted by `f3d verify` under the temporal contract.\n- Per change: legality note, ablation entry (12b.5), paired runs on the reserved M5, phone and the discrete-GPU stratum where provisioned (03.7); publish the achieved ratio with interval even when below 2x and record the bottleneck location.\n- Never alter particle counts, resolution, sample counts or simulation parameters.\n\n## Success criteria\n\n- Results documents; feeds G4/F3D-23.2.\n\n## Test plan\n\n- Unit: legality tests per attempted improvement (numeric contract, observation contract) with CPU/GPU references.\n- E2E: profile → change → ablation → paired run loops for H7 and H8 on the reserved M5, phone and the discrete-GPU stratum where provisioned (03.7); `f3d verify` after each change; results documents per stratum.\n- Logging: per-iteration `{case, change, profile_before, profile_after, ratio, ci}` with telemetry breakdowns and GPU timestamps where available; archived under `evidence/20.4/`.\n- Failure artifacts: readback diffs and invalidated runs with reasons.\n\n## Considerations\n\n- Never reduce particle counts or quality to win.\n","acceptance_criteria":"For H7 and H8, each attempted improvement has a profile motivation, a legality argument (numeric contract, observation contract), an ablation and paired results; the final results document per stratum states the achieved ratio with interval and, if below 2x, says so and records the bottleneck location.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:29.533082Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:22.354027Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","h7","h8","new-backend","phase-4","tier-frontier"],"dependencies":[{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.4","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.4","type":"blocks","created_at":"2026-09-09T16:49:30.208482Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.4","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.5","type":"blocks","created_at":"2026-09-09T16:49:29.827286Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.4","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.6","type":"blocks","created_at":"2026-09-09T16:49:30.024164Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.4","depends_on_id":"f3d-20-profile-justified-gpu-improvements-023","type":"parent-child","created_at":"2026-09-09T16:48:29.533082Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-20-profile-justified-gpu-improvements-023.5","title":"Ablation suite and attribution of gains","description":"## Full scope\n\nAblation suite and attribution of gains: disable one optimization at a time (update compilation, dirty tracking, material-program merging, instancing, bundles, GPU compute selection, pass/resource optimization, optional hardware features) across the corpus; publish where the gain comes from and where the bottleneck moved; no multiplication of isolated kernel speedups\n\n## Summary\n\nKnow why it is faster, when it is. This bead turns the pass registry (12b.5) and the bench protocol into a reproducible attribution report.\n\n## Background (plan §16.8, §14.8, §3.5)\n\n\"Run ablations that disable one optimization at a time: update compilation, dirty tracking, material-program merging, bundles, GPU compute selection, pass/resource optimization, and optional hardware features. Publish where the gain comes from and where the bottleneck moved. Do not multiply isolated kernel speedups together. A 5× transform kernel and a 2× uploader do not imply a 10× application.\" The build report distinguishes structural estimates from measured timings; ablation rows are measured timings.\n\n## Technical approach\n\n- `f3d bench --ablate <pass|all>`: for each registered pass (12b.5) build (or configure at runtime) the artifact with that pass routed to its conservative reference, run the paired protocol on the same reserved device, and compute the delta against the fully optimized artifact with the analysis script (03.8) using the same block bootstrap.\n- Bottleneck attribution: record the telemetry breakdown (07.7: CPU preparation, bridge, uploads, command preparation, GPU time) per ablation so each row states where the bottleneck moved.\n- Interactions are not claimed: one-at-a-time by design; passes that only pay off together are registered as one entry.\n- Output: per-cell attribution tables (markdown/JSON) appended to the results package for 23.2; the analysis refuses to emit a multiplicative combined figure.\n\nPrior notes retained:\n- `f3d bench --ablate <pass|all>`: for each registered pass, build (or configure at runtime where the switch is a runtime flag) the artifact with that pass routed to its conservative reference, run the paired protocol on the same reserved device, and compute the delta against the fully optimized artifact using the analysis script (03.8) with the same block bootstrap.\n- Bottleneck location: for each ablation, record the telemetry breakdown (07.7: CPU preparation, bridge, uploads, command preparation, GPU time) so the report can say \"removing bundles moved the bottleneck from GPU to command preparation\".\n- Ordering caveat: ablations are one-at-a-time by design; interactions are not claimed. If two passes only pay off together, record the pair as a single registry entry rather than inferring it.\n- Output: per-cell markdown/JSON table appended to the results package used by F3D-23.2.\n\n## Success criteria\n\n- Attribution tables for H1–H8 and the fidelity set on at least the M5 stratum; every registered pass has a row per cell; the analysis refuses to emit a \"combined\" multiplicative figure.\n\n## Test plan\n\n- Unit: registry-iteration tests; a synthetic artifact with two known-cost passes verifies ablation deltas within the noise band; a test that the analysis refuses to emit a multiplicative combined figure.\n- E2E: `f3d bench --ablate all` over H1–H8 and the fidelity set on the reserved M5 (and other strata as scheduled with 23.2).\n- Logging: per-ablation `metric` events `{cell, pass_disabled, ratio, ci_low, ci_high, bottleneck_after}` and the attribution tables; raw runs archived under `evidence/20.5/`.\n- Failure artifacts: invalidated runs with reasons.\n\n## Considerations\n\n- Ablation runs consume reserved-device time; schedule them with the scored campaign (23.2) rather than ad hoc.\n- Guards and materialization are part of the measured optimized path and are never \"ablated away\" to inflate a number (§14.8).\n","acceptance_criteria":"f3d bench --ablate runs the registered optimizer passes one-off at a time on every corpus cell and produces an attribution table per cell with paired intervals; each row names the optimization disabled, the throughput delta, and the observed bottleneck location after disabling; the summary never multiplies component speedups.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:29.635805Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:26.838363Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-conformance","phase-4","tier-solid"],"dependencies":[{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.5","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s.5","type":"blocks","created_at":"2026-09-09T16:49:30.614475Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.5","depends_on_id":"f3d-20-profile-justified-gpu-improvements-023","type":"parent-child","created_at":"2026-09-09T16:48:29.635805Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.5","depends_on_id":"f3d-20-profile-justified-gpu-improvements-023.2","type":"blocks","created_at":"2026-09-09T16:49:30.409425Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-20-profile-justified-gpu-improvements-023.6","title":"GPU picking as an additional async API and GPU culling with conservative recovery","description":"## Full scope\n\nGPU picking as an additional asynchronous API (never a substitute for synchronous Raycaster results) and GPU culling with conservative recovery for motion/disocclusion that cannot create missing geometry\n\n## Summary\n\nAdditive strategies, not contract changes.\n\n## Background (plan §5.10, §7.6)\n\n\"GPU picking may be an additional async API or a proven internal strategy, not a Promise substituted for a synchronous `Raycaster` result.\" \"GPU culling is an optional execution strategy, not a different visibility contract. Occlusion based on previous frames requires conservative recovery for motion/disocclusion and cannot silently create missing geometry.\"\n\n## Technical approach\n\n- GPU picking: an ID-buffer pass (object/instance/face IDs in an integer target) rendered on demand with an async readback (17.4) exposed as `__f3d.pickAsync(x, y)` returning the same identity fields the synchronous `Raycaster` would; documented as an additional API, never substituted for `Raycaster`.\n- GPU culling: a compute compaction kernel (08.6 indirect slots) over object bounds against the frustum; occlusion from the previous frame's depth only with a conservative \"unknown → visible\" rule and explicit disocclusion recovery (objects that were occluded are re-tested every frame or when the camera moves beyond a bound); the portfolio (20.2) selects CPU vs GPU culling per device.\n- Shadow/reflection/transmission passes use their own frusta; culling for the main camera never removes contributors to other passes (plan §7.6 rule, implemented by 06.5).\n- Tests compare frames with upstream under motion/disocclusion scripts.\n\n## Success criteria\n\n- Tests; portfolio integration.\n\n## Test plan\n\n- Unit: ID-buffer picking correctness tests vs the synchronous raycaster on synthetic scenes; culling compaction tests with a CPU reference and the \"unknown → visible\" rule; disocclusion recovery tests.\n- E2E: browser tests on both browsers for the async picking API and for GPU culling under motion/disocclusion scripts, asserting no missing geometry vs upstream frames.\n- Logging: picking results and culling stats `{visible, culled, recovered}`; archived under `evidence/20.6/`.\n- Failure artifacts: frames showing missing geometry with the culling decision dump.\n\n## Considerations\n\n- Low priority; only if profiles justify.\n","acceptance_criteria":"An optional async GPU picking API exists under the F3D namespace with documented semantics; the public Raycaster remains synchronous and CPU-authoritative; GPU culling (when selected by the portfolio) is proven never to discard potentially visible geometry, with conservative recovery tests for disocclusion.","status":"open","priority":3,"issue_type":"task","created_at":"2026-09-09T16:48:29.734561Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:20.675341Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-gpu","f3d-scene","new-backend","phase-4","tier-frontier"],"dependencies":[{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.6","depends_on_id":"f3d-06-packed-transforms-materialization-roa.5","type":"blocks","created_at":"2026-09-09T16:49:30.801201Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.6","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.3","type":"blocks","created_at":"2026-09-09T16:49:31.018618Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-20-profile-justified-gpu-improvements-023.6","depends_on_id":"f3d-20-profile-justified-gpu-improvements-023","type":"parent-child","created_at":"2026-09-09T16:48:29.734561Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-21a-early-host-tool-integrations-ff1","title":"F3D-21 (early slice): Host and tool integrations needed by Phase 0/1 (CSS/SVG renderers, controls, Inspector/editor smoke, WebAudio route)","description":"## What this epic delivers\n\nThe early host-integration slice. Plan §19 F3D-21 early prerequisite is 04.\n\n## Why (plan §5.14, §3.3 \"No WebGPU prerequisite\", §18 Phase 0)\n\n\"DOM/CSS/SVG: Preserve actual nodes, vector elements, style/layout behavior, event targets, focus, selection, visibility, parentage and removal... Flattening labels or SVG into the canvas is not equivalent. A page using only these renderers has no reason to require WebGPU.\"\n\n## Children\n\n1. CSS2D/CSS3D/SVG renderer routes without WebGPU.\n2. Controls and interaction retained route with real DOM events.\n3. Inspector/devtools hooks and editor smoke.\n4. WebAudio route smoke.\n\n## Success criteria for the epic\n\nAll children closed; results tagged retained/browser-owned.\n\n## No-claim boundary\n\nThese are retained/browser-owned routes; they are not Rust rewrites and never acceleration evidence.\n","acceptance_criteria":"CSS2D/CSS3D/SVG renderers, the controls used by H1/H2, Inspector/devtools hooks, an editor smoke workflow and a WebAudio smoke path all work through the routed build with real DOM/audio objects and without requiring WebGPU or Wasm initialization where the source does not.","status":"open","priority":1,"issue_type":"epic","created_at":"2026-09-09T16:46:24.249371Z","created_by":"jemanuel","updated_at":"2026-09-09T16:49:32.735602Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["editor","f3d-runtime","phase-0","phase-1","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-21a-early-host-tool-integrations-ff1","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:49:32.735263Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-21a-early-host-tool-integrations-ff1.1","title":"CSS2D/CSS3D/SVG renderer routes without WebGPU","description":"## Full scope\n\nCSS2DRenderer, CSS3DRenderer/objects/sprites, SVGRenderer/SVGObject/Projector routes: real DOM/CSS/vector output through the routed build; mixed CSS/WebGPU views share the source's camera/transform observations; no GPU request for non-GPU scenes\n\n## Summary\n\nAlternate renderers are real features with real DOM.\n\n## Background (plan §5.12 \"Alternate renderers\", §5.14, §23 [S59], [S60])\n\n## Technical approach\n\n- Route `CSS2DRenderer`/`CSS2DObject`, `CSS3DRenderer`/`CSS3DObject`/`CSS3DSprite`, `SVGRenderer`/`SVGObject`/`Projector` through the facade as retained modules with shared core identity (04.2); no wrapping.\n- Camera observation: these renderers read `camera.matrixWorldInverse`/`projectionMatrix` and object `matrixWorld`; when those objects are Wasm-owned in a mixed page, materialization (06.4) must publish at `updateMatrixWorld` time so labels track the accelerated scene exactly.\n- Non-GPU pages: with the ingestion graph proving no path to the runtime/host binding modules, the artifact must not include or instantiate Wasm/WebGPU code; a test removes `navigator.gpu` and instruments the module loader.\n- Structural comparison: serialize the produced DOM/SVG (tags, attributes, computed transforms, event listeners) at logical frames and diff against upstream.\n\n## Success criteria\n\n- Tests green on both browsers.\n\n## Test plan\n\n- Unit: none (retained JS).\n- E2E: `tests/e2e/alt-renderers/smoke.mjs` on both browsers: DOM structure/styles/events of CSS2D/CSS3D/SVG output vs upstream; a CSS/SVG-only page with `navigator.gpu` removed asserting no Wasm instantiation; a mixed CSS+WebGPU page with label positions consistent with the accelerated camera at every frame.\n- Logging: structural diff summaries and initialization events (Wasm/GPU absent); archived under `evidence/21a.1/`.\n- Failure artifacts: serialized DOM/SVG from both sides.\n\n## Considerations\n\n- Documented upstream limitations preserved, not \"fixed\".\n","acceptance_criteria":"The alternate renderers work unmodified through the facade with identical DOM structure/styles/events as upstream; a CSS/SVG-only page initializes no Wasm/GPU; a mixed CSS+WebGPU page keeps label positions consistent with the accelerated camera observations at every frame.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:29.835391Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:17.117270Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-runtime","phase-0","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-21a-early-host-tool-integrations-ff1.1","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:49:31.815414Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21a-early-host-tool-integrations-ff1.1","depends_on_id":"f3d-21a-early-host-tool-integrations-ff1","type":"parent-child","created_at":"2026-09-09T16:48:29.835391Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-21a-early-host-tool-integrations-ff1.2","title":"Controls and interaction retained route with real DOM events (H1-H8 controls)","description":"## Full scope\n\nControls and interaction retained route: OrbitControls and the controls used by H1/H2/H3–H8 with browser-owned input, real identity, focus, pointer capture/lock and event ordering; no rasterized substitute for interactive DOM\n\n## Summary\n\nControls stay JavaScript; the boundary is explicit.\n\n## Background (plan §5.12 \"Controls and interaction\", §4.6 last row)\n\n\"Browser-owned input with real identity, focus, pointer capture/lock and event ordering; no rasterized substitute for interactive DOM.\" \"DOM controls and unusual effectful callbacks → Retain JavaScript and synchronize at explicit boundaries.\"\n\n## Technical approach\n\n- Route the control classes used by the corpus (exactly the control classes the 01.6 closure records list per case) as retained modules; controls stay browser-owned and effectful.\n- Ownership: the camera and any object a control mutates are JavaScript-owned regions (05.5) whose state is snapshotted at the render boundary; tests assert the accelerated path observes control writes at the correct logical frame.\n- Input scripts: real pointer/keyboard/wheel/touch events dispatched through the automation driver on the actual DOM element (not synthetic dispatch where capture matters), including pointer capture/lock and focus transitions; traces of control events and camera state compared with upstream at logical frames.\n\n## Success criteria\n\n- Trace equality under input scripts.\n\n## Test plan\n\n- Unit: none (retained JS); ownership tests for camera-state snapshots at the boundary (05.5).\n- E2E: `tests/e2e/controls/corpus.mjs` on both browsers: the controls used by H1–H8 under scripted real pointer/keyboard/wheel/touch input with event ordering and state traces equal to upstream; pointer capture/lock and focus behavior.\n- Logging: event traces per script with logical-frame indices and control state; archived under `evidence/21a.2/`.\n- Failure artifacts: the trace diff.\n\n## Considerations\n\n- Full control-family closure is 21b.1.\n","acceptance_criteria":"The corpus's controls operate through the routed build with event ordering and state updates equal to upstream under scripted input; pointer capture/lock and focus behave identically; camera state written by controls is observed by the accelerated path at the right boundary.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:29.936595Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:22.531177Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-runtime","phase-0","phase-1","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-21a-early-host-tool-integrations-ff1.2","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:49:32.001593Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21a-early-host-tool-integrations-ff1.2","depends_on_id":"f3d-21a-early-host-tool-integrations-ff1","type":"parent-child","created_at":"2026-09-09T16:48:29.936595Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-21a-early-host-tool-integrations-ff1.3","title":"Inspector/devtools hooks for H1 and an editor smoke workflow","description":"## Full scope\n\nInspector/devtools hooks for H1 and an editor smoke workflow: the upstream Inspector attaches to the routed renderer with truthful public data; devtools integration points preserved; the upstream editor loads a project, edits, undoes, and exports through the routed build\n\n## Summary\n\nExisting tools keep working from day one.\n\n## Background (plan §5.14 \"Editor/Inspector/devtools\", §5.7)\n\n## Technical approach\n\n- Inspector: keep the upstream Inspector module and its renderer hooks working against the routed renderer object; where the Inspector reads renderer stats it gets the public contract's values (logical counts from `renderer.info`); F3D physical telemetry is exposed only under `window.__f3d` (07.7) and never mutates public fields.\n- Devtools: preserve the integration points the upstream devtools extension expects (global hooks/registration) through the routed build; smoke-test that the panel connects.\n- Editor smoke: serve the upstream editor against the routed `three` build (import map), load a project, import a model, edit a material, undo, export, and compare with the pinned original editor running the same automation.\n- Record every hook the tools depend on in the manifest so later beads (21b.5) close the full workflows.\n\n## Success criteria\n\n- Smoke tests green.\n\n## Test plan\n\n- Unit: none.\n- E2E: Inspector opens on H1 through the routed build with panels populated equal to upstream's public fields; devtools hooks respond; editor smoke workflow (load a project, edit a material, undo, export) via browser automation on both browsers; F3D telemetry visible only under `__f3d`.\n- Logging: per-step workflow events and panel snapshots; archived under `evidence/21a.3/`.\n- Failure artifacts: editor state dumps and panel snapshots.\n\n## Considerations\n\n- Full editor workflow closure is 21b.5.\n","acceptance_criteria":"Inspector panels populate on H1 through the routed build (retained renderer at this stage; new backend later via 07.5); devtools hooks respond; the editor smoke workflow passes; F3D telemetry appears only in its own namespace.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:30.041323Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:18.862675Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["editor","f3d-runtime","phase-0","phase-1","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-21a-early-host-tool-integrations-ff1.3","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:49:32.183124Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21a-early-host-tool-integrations-ff1.3","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.4","type":"blocks","created_at":"2026-09-09T16:49:32.368326Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21a-early-host-tool-integrations-ff1.3","depends_on_id":"f3d-21a-early-host-tool-integrations-ff1","type":"parent-child","created_at":"2026-09-09T16:48:30.041323Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-21a-early-host-tool-integrations-ff1.4","title":"WebAudio route smoke with real activation","description":"## Full scope\n\nWebAudio route smoke: Audio/AudioListener/PositionalAudio with real AudioContext nodes, activation preserved from a real user gesture, no new await inserted before activation-sensitive calls\n\n## Summary\n\nNative audio graphs, untouched.\n\n## Background (plan §5.14 \"WebAudio/media\", §5.12 \"Audio and live media\", §23 [S61])\n\n\"Connect Asupersync only to operations it actually owns; do not move a user-activation-sensitive call behind a new await.\"\n\n## Technical approach\n\n- Route `Audio`, `AudioListener`, `PositionalAudio`, `AudioLoader` and `AudioContext` helpers through the facade as retained modules; confirm the listener's transform read goes through materialization (06.4) at `updateMatrixWorld` time.\n- Build a smoke page from the upstream audio example; automation performs a real user gesture (click) before `play()`; assert the resulting `AudioContext` graph (node types and connections, enumerated through a test-only wrapper around `createGain`/`createPanner`/`connect`) equals upstream's for the same script.\n- Negative case: without the gesture, the page must exhibit upstream's autoplay-blocked behavior (suspended context; no exception) and resume after the gesture.\n- Do not insert an `await` before `play()`/`resume()`; the smoke test asserts the call happens synchronously in the gesture handler.\n\n## Success criteria\n\n- Test green on both browsers.\n\n## Test plan\n\n- Unit: none.\n- E2E: audio example through the routed build on both browsers after a real automated gesture: `AudioContext` graph equals upstream's; autoplay blocked without a gesture behaves as upstream; listener follows an accelerated camera at the right boundary.\n- Logging: audio graph dumps and activation events; archived under `evidence/21a.4/`.\n- Failure artifacts: graph dump diffs.\n\n## Considerations\n\n- Full audio closure is 21b.2.\n","acceptance_criteria":"An audio example plays through the routed build after a real user gesture with the same node graph as upstream; autoplay restrictions behave as upstream; the listener follows an accelerated camera at the right boundary.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:30.144459Z","created_by":"jemanuel","updated_at":"2026-09-09T17:31:52.965017Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["audio","f3d-runtime","phase-1","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-21a-early-host-tool-integrations-ff1.4","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.2","type":"blocks","created_at":"2026-09-09T16:49:32.551516Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21a-early-host-tool-integrations-ff1.4","depends_on_id":"f3d-21a-early-host-tool-integrations-ff1","type":"parent-child","created_at":"2026-09-09T16:48:30.144459Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21a-early-host-tool-integrations-ff1.4","depends_on_id":"f3d-gate-1-foundation-tzn","type":"blocks","created_at":"2026-09-09T17:31:52.964631Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-21b-all-host-tool-integrations-5mw","title":"F3D-21 (closure): All host/tool integrations and complete official workflows (controls, CSS/SVG, audio/media, XR, physics, workers, Inspector/editor/player/devtools)","description":"## What this epic delivers\n\nPhase 4's host and tool closure. Plan §19 F3D-21 closure \"uses 17–20\"; acceptance: \"Controls/CSS/SVG/audio/media/XR/physics/worker/Inspector/editor/player/devtools coverage\".\n\n## Why (plan §5.12 rows Controls, Alternate renderers, Audio, WebXR, Misc/physics, Inspector/editor, Deployment/workers; §5.14; §18 Phase 4)\n\n\"Close the entire feature manifest and all inherited/additional tests, including previously untested/TODO features, exact WebGL/WebGPU backend APIs, exporters and plugins, media/audio, real XR integrations, physics wrappers, and editor/player/Inspector/devtools workflows. Complete a capable-host matrix; device limitations match the reference and do not delete features.\"\n\n## Children\n\n1. All controls and interaction closure (every control class, gizmos, HTMLMesh/InteractiveGroup)\n2. Audio and live media closure with real WebAudio/media graphs\n3. WebXR closure (GL-backed and native WebGPU XR, fallback honored, capable headset lane)\n4. Physics wrappers and miscellaneous addons closure (same engines, GPGPU, water/sky, splats)\n5. Editor, Inspector, devtools and TSL tools full workflows\n6. Workers, offscreen, library and environment flows (generation-tagged worker protocol)\n7. Alternate renderers closure (CSS2D/CSS3D/SVG full API and any other shipped output backend)\n\n## Success criteria for the epic\n\nAll children closed; manifest families fully owned and tested; `f3d-runtime` line count reported against 12,000.\n\n## No-claim boundary\n\nExternal engines and tools are reused, never rewritten; they are dependency compatibility obligations (§5.14).\n","acceptance_criteria":"Every controls/interaction, alternate-renderer, audio/media, WebXR, physics/misc-addon, worker/offscreen/library and Inspector/editor/player/devtools/TSL-tool record in the manifest has a production owner with end-to-end tests, including capable-host lanes for XR and permission-dependent behavior; official workflows (author/edit/reload/export/play) work on real projects.","status":"open","priority":2,"issue_type":"epic","created_at":"2026-09-09T16:46:24.332225Z","created_by":"jemanuel","updated_at":"2026-09-09T17:12:27.944999Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["audio","editor","f3d-runtime","phase-4","retained-js","tier-solid","xr"],"dependencies":[{"issue_id":"f3d-21b-all-host-tool-integrations-5mw","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf","type":"blocks","created_at":"2026-09-09T16:49:36.220108Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw","depends_on_id":"f3d-18b-full-feature-gate-closure-mk4","type":"blocks","created_at":"2026-09-09T16:49:36.437410Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy","type":"blocks","created_at":"2026-09-09T16:49:36.659231Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw","depends_on_id":"f3d-20-profile-justified-gpu-improvements-023","type":"blocks","created_at":"2026-09-09T16:49:36.877510Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw","depends_on_id":"f3d-21a-early-host-tool-integrations-ff1","type":"blocks","created_at":"2026-09-09T16:49:36.009586Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-21b-all-host-tool-integrations-5mw.1","title":"All controls and interaction closure (every control class, gizmos, HTMLMesh/InteractiveGroup)","description":"## Full scope\n\nAll controls and interaction: every control class (Orbit/Map/Trackball/Fly/FirstPerson/PointerLock/Drag/Transform/Arcball and every other control in the tree), selection/picking helpers, transform gizmos and snapping, HTMLMesh/InteractiveGroup, helper geometry and camera/scene utilities; event ordering under scripted input\n\n## Summary\n\nClose the interaction family: every control class in `examples/jsm/controls/`, the interactive helpers in `examples/jsm/interactive/`, and the helper/utility objects that respond to input.\n\n## Background (plan §5.12 \"Controls and interaction\", §5.14 \"DOM/CSS/SVG\", §4.6 last row)\n\n\"Browser-owned input with real identity, focus, pointer capture/lock and event ordering; no rasterized substitute for interactive DOM.\" Controls write camera/object state that the accelerated path reads at explicit boundaries; the ownership model (05.5) keeps that state JavaScript-owned with snapshots.\n\n## Technical approach\n\n- From the manifest, list every control/interaction record (all classes under `examples/jsm/controls/`, `examples/jsm/interactive/`, transform/selection helpers, camera/scene utilities that respond to input); retain the modules through the facade (21a.2); no rewrites.\n- Per control, an input script (03.1 format) covering its documented gestures and modes: drag/rotate/pan/zoom/dolly, keyboard modes, touch/pinch, wheel, pointer lock enter/exit, `enableDamping` update loops, `TransformControls` translate/rotate/scale in local/world space with snapping and axis constraints, `DragControls` hover/drag events, `ArcballControls` gizmo, `HTMLMesh`/`InteractiveGroup` DOM events with focus and pointer capture.\n- Gizmo/selection picking uses the synchronous raycast contract (06.5) over Wasm-owned meshes; tests assert identical hit results to upstream.\n- Traces compared with upstream: control state (target/position/quaternion), emitted events (`change`, `start`, `end`, `dragging-changed`, `objectChange`) and their order at logical frames.\n\nPrior notes retained:\n- From the manifest, list every control/interaction record; retain the upstream modules through the facade (04.2); no rewrites.\n- Input scripts (03.1 format) per control covering its documented gestures: drag/rotate/pan/zoom/dolly, keyboard modes, touch/pinch, wheel, pointer lock enter/exit, `enableDamping` update loops, `TransformControls` modes (translate/rotate/scale, local/world space, snapping, axis constraints), `DragControls` hover/drag events, `ArcballControls` gizmo, `HTMLMesh` DOM events through `InteractiveGroup`.\n- Trace comparison with upstream: control state (target/position/quaternion), emitted events (`change`, `start`, `end`, `dragging-changed`, `objectChange`) and their order.\n- Gizmo picking runs through the synchronous raycast (06.5) over meshes whose transforms are Wasm-owned; the pick must observe current state.\n\n## Success criteria\n\n- Family suite green on both browsers with identical event traces; pick tests match upstream `intersectObjects` results including gizmo handles.\n\n## Test plan\n\n- Unit: none (retained JS); gizmo-picking tests use the synchronous raycast suite (06.5).\n- E2E: generated per-control input scripts (drag/rotate/pan/zoom/dolly, keyboard modes, touch/pinch, wheel, pointer lock, damping loops, TransformControls modes/space/snapping/axis constraints, DragControls hover/drag events, ArcballControls gizmo, HTMLMesh/InteractiveGroup events) on both browsers with real pointer events; event and state traces vs upstream; gizmo picks over Wasm-owned meshes.\n- Logging: per-script event traces with logical-frame indices; archived under `evidence/21b.1/`.\n- Failure artifacts: trace diffs.\n\n## Considerations\n\n- This bead owns the CSS/SVG+camera changes+input combination test together with 21b.7 (controls half here, DOM/SVG half there); 23.1 aggregates it with the other combinations.\n- Do not \"optimize\" control math into Rust; it is cold, effectful, and browser-owned by design.\n","acceptance_criteria":"Every controls/interaction manifest record is owned (retained browser-owned JavaScript) and tested with scripted input traces equal to upstream on Chrome and Safari; TransformControls gizmo picking and snapping use the synchronous raycast contract over accelerated meshes; HTMLMesh/InteractiveGroup keep real DOM identity, focus and event targets; pointer capture/lock and keyboard focus behave identically.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:30.248451Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:17.673069Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-runtime","phase-4","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.1","depends_on_id":"f3d-06-packed-transforms-materialization-roa.5","type":"blocks","created_at":"2026-09-09T16:49:33.117033Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.1","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:26.604231Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.1","depends_on_id":"f3d-21a-early-host-tool-integrations-ff1.2","type":"blocks","created_at":"2026-09-09T16:49:32.931270Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.1","depends_on_id":"f3d-21b-all-host-tool-integrations-5mw","type":"parent-child","created_at":"2026-09-09T16:48:30.248451Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-21b-all-host-tool-integrations-5mw.2","title":"Audio and live media closure with real WebAudio/media graphs","description":"## Full scope\n\nAudio and live media closure: Audio/AudioListener/PositionalAudio/AudioAnalyser/AudioContext helpers, filters/nodes/playback/loop/rates, live sources (microphone/media streams) and audio loaders; video/frame/canvas sources; activation, seeking, source replacement, permission and cleanup tests; upstream unsupported-environment behavior preserved\n\n## Summary\n\nComplete the audio/media family started by the 21a.4 smoke path.\n\n## Background (plan §5.12 \"Audio and live media\", §5.14 \"WebAudio/media\", §23 [S61])\n\n\"Actual WebAudio/browser media graphs, not simulated values; activation, seeking, source replacement, permission and cleanup tests.\" \"Preserve native audio nodes, scheduled playback, filters, rate/loop/positional behavior, analyser data, source ownership and activation. Connect Asupersync only to operations it actually owns; do not move a user-activation-sensitive call behind a new await. Keep browser autoplay/origin restrictions and the upstream's actual unsupported-environment behavior.\"\n\n## Technical approach\n\n- Retain the audio modules (21a.4) and close the family from the manifest: `Audio`, `AudioListener`, `PositionalAudio`, `AudioAnalyser`, `AudioContext` helpers, `AudioLoader`; every method/option (`setBuffer`, `setMediaElementSource`, `setMediaStreamSource`, `setNodeSource`, `setFilter(s)`, `setPlaybackRate`, `setLoop`/`setLoopStart/End`, `setDetune`, `play/pause/stop` with offsets and delays, `setVolume`, panner parameters, `setDirectionalCone`, `getFrequencyData`/`getAverageFrequency`).\n- Graph inspection: a test-only wrapper records `create*`/`connect`/`disconnect` calls and node parameters so the real `AudioContext` graph can be diffed against upstream for the same script.\n- Media sources shared with 14.4: video/canvas/media-stream sources with seeking, pause/resume, `srcObject` replacement, cross-origin restrictions.\n- Permissions: microphone granted/denied flows through the automation driver; no-`AudioContext` environment reproduces upstream's behavior; activation-sensitive calls stay synchronous in gesture handlers (no inserted awaits).\n\nPrior notes retained:\n- Retain upstream audio modules; verify the node graph structure by inspecting the real `AudioContext` graph (node types, connections, parameters) against upstream for each API: `setBuffer`, `setMediaElementSource`, `setMediaStreamSource`, `setNodeSource`, `setFilter(s)`, `setPlaybackRate`, `setLoop`, `setDetune`, `play/pause/stop` with offsets, `PositionalAudio` panner parameters and `setDirectionalCone`, `AudioAnalyser.getFrequencyData/getAverageFrequency`.\n- Transform boundary: the listener and positional sources read object matrices via materialization (06.4) at the same update points as upstream (`updateMatrixWorld`).\n- Media sources for textures (14.4) share the lifecycle tests: seeking, pause/resume, `srcObject` replacement, cross-origin restrictions.\n- Permission flows (microphone) tested with granted and denied outcomes through the automation harness; unsupported environments (no `AudioContext`) produce upstream's behavior.\n\n## Success criteria\n\n- Family suite green on Chrome and Safari; graph-structure diffs empty; activation tests pass only from real gestures.\n\n## Test plan\n\n- Unit: none (retained JS).\n- E2E: per-API tests on both browsers inspecting the real `AudioContext` graph (node types, connections, parameters) vs upstream for buffers, media elements, media streams, node sources, filters, playback rate/loop/detune, play/pause/stop with offsets, positional panner parameters and cones, analyser data; media lifecycle (seeking, pause/resume, `srcObject` replacement, cross-origin); permission granted/denied flows; no-`AudioContext` environment behavior.\n- Logging: audio graph dumps per test and activation/permission events; archived under `evidence/21b.2/`.\n- Failure artifacts: graph dump diffs.\n- E2E addition: a `PositionalAudio` attached to a Wasm-owned object: panner position/orientation observed after `updateMatrixWorld` equals upstream at every logical frame.\n\n## Considerations\n\n- Never bypass autoplay policies to make a test pass; a blocked autoplay must match upstream's observable state.\n","acceptance_criteria":"Every audio/media manifest record is owned and tested with real WebAudio/media graphs (no simulated values); activation, seeking, source replacement, permission prompts/denials and cleanup behave as upstream on both browsers; PositionalAudio follows accelerated object transforms at the correct boundary; AudioAnalyser data comes from the real analyser node.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:30.347306Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:23.333430Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["audio","f3d-runtime","phase-4","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.2","depends_on_id":"f3d-14-material-light-texture-surface-5lj.4","type":"blocks","created_at":"2026-09-09T16:49:33.505989Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.2","depends_on_id":"f3d-21a-early-host-tool-integrations-ff1.4","type":"blocks","created_at":"2026-09-09T16:49:33.304656Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.2","depends_on_id":"f3d-21b-all-host-tool-integrations-5mw","type":"parent-child","created_at":"2026-09-09T16:48:30.347306Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-21b-all-host-tool-integrations-5mw.3","title":"WebXR closure (GL-backed and native WebGPU XR, fallback honored, capable headset lane)","description":"## Full scope\n\nWebXR closure: WebXRManager/XRManager sessions, reference spaces and resets, cameras/views/layers/framebuffers, controllers/hands/haptics/buttons/models, hit testing/planes/depth/camera/lighting where upstream supports them; capability-qualified native WebGPU XR and exact GL-backed sessions; WebGLXRFallback honored; real XRFrame lifecycle; capable headset/browser lane with real-device evidence\n\n## Summary\n\nXR through both routes, with honest evidence.\n\n## Background (plan §5.12 \"WebXR/AR/VR\", §5.14 \"WebXR\", §14.9 last paragraph, §23 [S56], [S62])\n\n\"The existing XR fallback helper explicitly constructs and installs a new renderer; honor that contract instead of secretly switching an already-exposed canvas/device. A capable headset/browser lane is required for behavior that a phone cannot exercise.\" \"Emulators/mocks are additional tests, not successful hardware feature evidence.\"\n\n## Technical approach\n\n- Session lifecycle in `f3d-runtime`: `WebXRManager`/`XRManager` public API retained; for the new backend implement the XR frame loop (`XRSession.requestAnimationFrame`), reference spaces and resets, views/cameras per eye, layers (`XRWebGLLayer`/`XRWebGLBinding` projection layers on the exact route; `XRGPUBinding`/projection layers where the browser supports WebGPU XR), framebuffer/target handling per view, controllers/hands/haptics/buttons/models through the retained input modules, hit testing/planes/depth/camera/lighting estimation where upstream supports them.\n- Routing: a renderer whose source requires a GL-backed session (or where the browser lacks WebGPU XR) is exact from construction; `WebGLXRFallback`'s construction-time installation of a new renderer is honored as a source-supported replacement boundary (04.4/22b.3).\n- Capability record extension: XR features/modes supported per host; unknowns recorded as unknown.\n- Capable lane: real headset/browser runs for the family suite and the XR+skinning+layers+session-reset combination; emulator runs labeled as additional tests only.\n- This bead owns the XR+skinning+layers+session-reset combination test (18b.2 owns only the non-host combinations); 23.1 aggregates it.\n\n## Success criteria\n\n- Family green on the capable lane; obligations listed if hardware unavailable.\n\n## Test plan\n\n- Unit: session lifecycle state-machine tests with a fake XR system (spaces, resets, layers, controllers/hands/haptics).\n- E2E: capable headset/browser lane scripts for GL-backed sessions (exact component, including the construction-time fallback installing a new renderer) and native WebGPU XR where supported; XR+skinning+layers+session-reset combination; emulator lane labeled as additional only.\n- Logging: XR frame/session events `{event, space, layers, inputs}` tagged with the lane; archived under `evidence/21b.3/`.\n- Failure artifacts: session traces and frame captures.\n\n## Considerations\n\n- Lack of headset access is an outstanding obligation, never a pass.\n","acceptance_criteria":"Every XR manifest record is owned and tested; GL-backed sessions use the exact component (including the construction-time fallback installing a new renderer); native WebGPU XR is used only where the browser supports it; XR+skinning+layers+session-reset combination passes; evidence comes from a real headset/browser lane, with emulator results labeled as additional only.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:30.448992Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:22.932568Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-gpu","f3d-runtime","new-backend","phase-4","tier-solid","xr"],"dependencies":[{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.3","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.4","type":"blocks","created_at":"2026-09-09T16:49:34.078887Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.3","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr.2","type":"blocks","created_at":"2026-09-09T16:49:34.278757Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.3","depends_on_id":"f3d-14-material-light-texture-surface-5lj.5","type":"blocks","created_at":"2026-09-09T17:44:22.699737Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.3","depends_on_id":"f3d-14-material-light-texture-surface-5lj.6","type":"blocks","created_at":"2026-09-09T16:49:33.708417Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.3","depends_on_id":"f3d-21b-all-host-tool-integrations-5mw","type":"parent-child","created_at":"2026-09-09T16:48:30.448992Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.3","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu.3","type":"blocks","created_at":"2026-09-09T16:49:33.891876Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-21b-all-host-tool-integrations-5mw.4","title":"Physics wrappers and miscellaneous addons closure (same engines, GPGPU, water/sky, splats)","description":"## Full scope\n\nPhysics wrappers and miscellaneous addons: Ammo/Jolt/Rapier wrappers with the same engine version, inputs, solver/time-step semantics and public adapter behavior; GPUComputationRenderer and GPGPU helpers, water/sky/reflectors/refractors, splats, volumes, progressive light maps, sculpting/painters, official path-tracing integrations and every other misc/helper/utility/worker object\n\n## Summary\n\nReuse the engines; optimize only the graphics.\n\n## Background (plan §5.12 \"Miscellaneous addons and physics integrations\", §5.14 \"Physics\", §7.7 last paragraph)\n\n\"An acceleration pass cannot replace Rapier/Ammo/Jolt simulation with a visually plausible approximation.\"\n\n## Technical approach\n\n- Physics wrappers: retain the upstream `AmmoPhysics`, `JoltPhysics`, `RapierPhysics` adapters and their pinned engine Wasm/JS as disclosed dependencies (plan §13.5 notices); tests run the physics examples with scripted inputs and compare per-step body state hashes with upstream (same engine versions, time step, solver iterations); the graphics side may accelerate transforms only after the engine publishes them.\n- GPGPU: `GPUComputationRenderer` and GPGPU helpers map their render-target ping-pong to pass-graph resources (07.2) with versioned histories; portable shaders through 15b; native-contract usage (sync reads) routes exactly.\n- Misc addons: water/sky/reflectors/refractors (render-to-target + material chunks), splats (storage/instanced paths), volumes (3D textures), progressive light maps (accumulation targets), sculpting/painters (interactive attribute updates via 06.6), official path-tracing integrations (retained with their own compute paths), every other misc/helper/utility/worker object from the census: owner recorded, positive tests written.\n- Optimization passes never touch authoritative simulation state (13.7 invariance test reused).\n\n## Success criteria\n\n- Family green; simulation traces identical.\n\n## Test plan\n\n- Unit: none for external engines; GPGPU helper mapping tests (render-target ping-pong through the pass graph).\n- E2E: scripted simulations through the routed build with identical results to upstream (same engine versions); GPGPU/water/sky/reflector/splat/volume/progressive-lightmap/sculpting/path-tracing examples via `f3d verify` in the appropriate lanes on both browsers; a test that graphics optimization leaves wrapper state untouched.\n- Logging: per-record results with owners and engine versions; simulation state hashes per step; archived under `evidence/21b.4/`.\n- Failure artifacts: state dumps and diff images.\n\n## Considerations\n\n- Third-party Wasm of C/C++ origin is never described as memory-safe Rust.\n","acceptance_criteria":"Every misc/physics manifest record is owned and tested; physics wrappers run the pinned external engine versions with identical simulation results for scripted scenes; GPGPU helpers execute through the new backend (portable) or exact route (native contract) as their semantics require; graphics optimization never alters authoritative simulation state.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:30.553822Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:23.103093Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-runtime","new-backend","phase-4","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.4","depends_on_id":"f3d-14-material-light-texture-surface-5lj.6","type":"blocks","created_at":"2026-09-09T17:44:23.102814Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.4","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.3","type":"blocks","created_at":"2026-09-09T16:49:34.478423Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.4","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy.1","type":"blocks","created_at":"2026-09-09T16:49:34.665550Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.4","depends_on_id":"f3d-21b-all-host-tool-integrations-5mw","type":"parent-child","created_at":"2026-09-09T16:48:30.553822Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-21b-all-host-tool-integrations-5mw.5","title":"Editor, Inspector, devtools and TSL tools full workflows","description":"## Full scope\n\nEditor, Inspector, devtools and TSL tools full workflows: import/edit/undo-redo/serialize/script/player/export/publish/XR paths and renderer changes in the upstream editor; Inspector hooks/panels and devtools integration; TSL transpiler/editor tools; editor-authored scripts execute with original host/security requirements; Rust source maps and generated-shader inspection augment existing APIs\n\n## Summary\n\nThe tools are consumers; they must keep working completely.\n\n## Background (plan §5.12 \"Inspector, tooling and editor workflows\", §5.14 \"Editor/Inspector/devtools\", §23 [S57])\n\n## Technical approach\n\n- Workflow scripts (from 01.7): for each editor workflow drive the upstream editor UI through browser automation against (a) the pinned original and (b) the routed build: import glTF/OBJ/FBX models, edit transforms/materials/geometry, undo/redo chains, add/edit/run scripts, animation playback, project persistence (IndexedDB) and reload, player preview, export in every format the editor offers, publish, XR entry on the capable lane, renderer setting changes.\n- Compare editor state (serialized project JSON), viewport screenshots at logical steps, and exported files semantically between (a) and (b).\n- Inspector/devtools: attach to the routed renderer; compare panel contents with upstream's public data; verify the added F3D panels (Rust source maps, generated-shader inspection) appear only under the F3D namespace and never alter public fields.\n- TSL tools: run the transpiler/editor tools on the corpus shaders and compare outputs with upstream's.\n- Editor scripts: confirm script execution uses the editor's original mechanism and security context (no engine-side `eval` policy applied to the application's own feature).\n- This bead owns the editor undo/redo+shared materials+serialization combination test (18b.2 owns only the non-host combinations); 23.1 aggregates it.\n\n## Success criteria\n\n- Workflows green.\n\n## Test plan\n\n- Unit: none.\n- E2E: workflow scripts from 01.7 executed against the upstream editor on the routed build and the pinned original (import, edit, undo/redo, scripts, animation, persistence, player, export/publish, XR entry, renderer changes); Inspector/devtools panels compared to upstream's public data; TSL tools exercised; editor-authored script execution verified.\n- Logging: per-workflow step events and panel snapshots; archived under `evidence/21b.5/`.\n- Failure artifacts: project state dumps and exports.\n\n## Considerations\n\n- The engine's policy against introducing `eval` does not prohibit the editor's scripting feature (§5.14).\n","acceptance_criteria":"Every editor/tool manifest workflow is tested end to end on real projects against the pinned original (author, edit, undo/redo, script, play, export, publish, reload); Inspector/devtools panels show truthful public data plus separate F3D diagnostics; editor scripting works as upstream.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:30.655928Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:18.032939Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["editor","f3d-runtime","phase-4","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.5","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.7","type":"blocks","created_at":"2026-09-09T17:41:12.293338Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.5","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.2","type":"blocks","created_at":"2026-09-09T16:49:35.255776Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.5","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy.4","type":"blocks","created_at":"2026-09-09T16:49:35.053913Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.5","depends_on_id":"f3d-21a-early-host-tool-integrations-ff1.3","type":"blocks","created_at":"2026-09-09T16:49:34.853280Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.5","depends_on_id":"f3d-21b-all-host-tool-integrations-5mw","type":"parent-child","created_at":"2026-09-09T16:48:30.655928Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-21b-all-host-tool-integrations-5mw.6","title":"Workers, offscreen, library and environment flows (generation-tagged worker protocol)","description":"## Full scope\n\nWorkers, offscreen, library and environment flows: existing worker/offscreen/library examples; dedicated-worker ownership of supported CPU/GPU tasks preserving input ordering and observation semantics; device/resources/submission stay in their owner realm (no GPUDevice transfer assumption); OffscreenCanvas selection tested before relying on main-thread fallback; worker messages carry task/region identity, sequence, scene/device generation and ownership; cancelled jobs retain drain obligations; MIME/URL/loading policies, credentials/errors, server-side utility imports; SharedArrayBuffer pools only as the later isolated option with real design and target tests\n\n## Summary\n\nOff-main-thread execution without changing semantics.\n\n## Background (plan §11.6, §5.12 \"Deployment, workers and environment behavior\", §10.4)\n\n\"A synchronous public read cannot become an asynchronous worker round trip without an admitted transformation.\" \"SharedArrayBuffer pools are a later, isolated-deployment option. They require a real scheduler/memory-safety design and target tests, not a `std::thread::spawn` call in an ordinary Wasm build.\"\n\n## Technical approach\n\n- Worker protocol in `f3d-runtime`: messages carry `{task, region, sequence, scene_generation, device_generation, owner}`; the main thread rejects responses whose generation or sequence is stale (05.1 handles; the 05.7 ABA test extended to workers); cancelled jobs keep a drain obligation (the worker acknowledges cancellation and the main side waits for it or times out with a logged outcome, never assuming rollback).\n- Ownership rules: the `GPUDevice`, its resources and submission stay in the realm that created them; a worker-hosted renderer is only selected when the source itself uses `OffscreenCanvas`/workers and the capability probe confirms support; synchronous public reads are never turned into worker round trips.\n- Retained flows: DRACO/Basis worker pools, the upstream offscreen example, `Worker` script resolution through the asset resolver (10.1), MIME/URL/loading policies and credentials/error behavior preserved; server-side utility imports (04.2).\n- Isolated option (SharedArrayBuffer pools): only under 10.3's verified headers, with its own scheduler/memory-safety design document, tests on the target browsers, and additive measurement; never a `std::thread::spawn` in an ordinary Wasm build.\n\nPrior notes retained:\n- Worker job protocol in `f3d-runtime` with generation-tagged messages (05.1 handles); OffscreenCanvas capability probe; retained worker examples (`DRACOLoader` workers, offscreen example) through the resolver; isolated option gated on 10.3's headers.\n\n## Success criteria\n\n- Family green; ABA/delayed-response test (05.7) extended to workers.\n\n## Test plan\n\n- Unit: worker protocol tests (task/region identity, sequence, scene/device generation, ownership tags); delayed-response-after-ID-reuse rejection; OffscreenCanvas capability probe tests; drain-obligation tests for cancelled jobs.\n- E2E: worker/offscreen/library examples through the routed build on both browsers; DRACO worker pools; ordering/observation tests for worker-hosted execution; isolated option (if implemented) under real headers with additive measurement.\n- Logging: worker message traces `{job, region, sequence, generation, event}`; archived under `evidence/21b.6/`.\n- Failure artifacts: message traces around the violation.\n\n## Considerations\n\n- Force-terminating a worker is not proof its committed effects were rolled back (§11.6).\n","acceptance_criteria":"Every deployment/worker/environment manifest record is owned and tested; worker-hosted execution (where selected) passes ordering/observation tests; delayed worker responses after ID reuse are rejected; the isolated shared-memory option, if implemented, has its own scheduler/memory-safety design and target tests and is measured additively.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:30.755474Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:23.281162Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["asupersync-upstream","f3d-runtime","phase-4","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.6","depends_on_id":"f3d-02-asupersync-browser-execution-58j.5","type":"blocks","created_at":"2026-09-09T16:49:35.440239Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.6","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.4","type":"blocks","created_at":"2026-09-09T16:49:35.821324Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.6","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.3","type":"blocks","created_at":"2026-09-09T16:49:35.639393Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.6","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:26.808894Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.6","depends_on_id":"f3d-21b-all-host-tool-integrations-5mw","type":"parent-child","created_at":"2026-09-09T16:48:30.755474Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-21b-all-host-tool-integrations-5mw.7","title":"Alternate renderers closure (CSS2D/CSS3D/SVG full API and any other shipped output backend)","description":"## Full scope\n\nAlternate renderers closure: the complete public API of CSS2DRenderer/CSS2DObject, CSS3DRenderer/CSS3DObject/CSS3DSprite, SVGRenderer/SVGObject/Projector and any other shipped output backend discovered by the census; real DOM/CSS/vector output with documented upstream limitations preserved; mixed views with accelerated cameras; no GPU request for non-GPU scenes; every method/option/event tested.\n\n## Summary\n\n21a.1 established the routes for Phase 0/1. This bead closes the family: every manifest record for the alternate renderers has an owner and positive tests, including options (`setSize`, `render` ordering, `domElement` structure, sorting, `overdraw`, quality settings, `SVGObject` node handling, `Projector` results) and interaction with accelerated scenes.\n\n## Background (plan §5.12 \"Alternate renderers\", §5.14, §23 [S59], [S60])\n\n\"Keep real DOM/CSS/vector output and documented limitations; no mandatory GPU request for non-GPU scenes.\" \"Flattening labels or SVG into the canvas is not equivalent.\"\n\n## Technical approach\n\n- From the manifest, enumerate the family records (all classes/methods/options/events of the CSS2D/CSS3D/SVG renderers and objects, `Projector`, and any other output backend the census lists); route them as retained modules (21a.1) and generate positive tests per record: `setSize`, `render` ordering and sorting, `domElement` structure and `zIndex` handling, `overdraw`/quality options, `SVGObject` node handling, `Projector.projectScene` results, event targets and focus behavior, object add/remove/visibility/parentage.\n- Mixed views: Wasm-owned camera/objects with CSS labels and SVG overlays; materialization at `updateMatrixWorld` keeps DOM transforms in sync at every logical frame.\n- Non-GPU pages: the absence-of-WebGPU profile (18b.5) confirms no Wasm/GPU initialization.\n\nPrior notes retained:\n- From the manifest, enumerate the family records; retained modules through the facade; positive tests per record comparing DOM/SVG structure, computed styles/transforms and event targets with upstream at logical frames; mixed-view tests with a Wasm-owned camera/object set (materialization 06.4).\n- Absence-of-WebGPU tests reuse 18b.5's profile.\n\n## Success criteria\n\n- Family suite green on Chrome and Safari; manifest updated; no Wasm/GPU initialization on non-GPU pages.\n\n## Test plan\n\n- Unit: none (retained JS); the census drives the test list.\n- E2E: `tests/e2e/alt-renderers/*.mjs` per renderer with DOM/SVG snapshot comparison and event-target checks; mixed CSS+WebGPU script.\n- Logging: per-record events with structural diff summaries; `owner: retained-js`; archived under `evidence/21b.7/`.\n- Failure artifacts: serialized DOM/SVG of both sides and the diff.\n\n## Considerations\n\n- Documented upstream limitations (for example SVGRenderer's material support) are preserved, not \"fixed\".\n","acceptance_criteria":"Every alternate-renderer manifest record is owned and tested with DOM/SVG structural equality to upstream, mixed accelerated views keep camera observations consistent, and non-GPU pages never initialize Wasm or WebGPU.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T17:11:27.504832Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:25.596879Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-runtime","phase-4","retained-js","tier-solid"],"dependencies":[{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.7","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:27.662092Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.7","depends_on_id":"f3d-14-material-light-texture-surface-5lj.5","type":"blocks","created_at":"2026-09-09T17:11:27.865195Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.7","depends_on_id":"f3d-21a-early-host-tool-integrations-ff1.1","type":"blocks","created_at":"2026-09-09T17:11:27.667941Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-21b-all-host-tool-integrations-5mw.7","depends_on_id":"f3d-21b-all-host-tool-integrations-5mw","type":"parent-child","created_at":"2026-09-09T17:11:27.504832Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-22a-first-backend-fault-injection-ctu","title":"F3D-22 (early slice): First-backend fault injection (queued effects, error scopes, device loss during compile/upload/submit, cancellation at every boundary, submission-epoch retirement)","description":"## What this epic delivers\n\nThe lifecycle safety that must exist before the first performance claim. Plan §19 F3D-22 depends on 02 and 05 (early); acceptance: \"Queued effects/error scopes/loss, exact native lifetime and stale-result safety\".\n\n## Why (plan §11.4, §11.5, §11.7, §14.7, §19 \"No task defers the safety or semantic precondition of an earlier claimed performance result\")\n\n\"These checks are part of the initial backend, not late release instrumentation. Include red-A/blue-B uniform snapshots, cancelled queued uploads, invalid pipeline creation, bundle/direct state transitions, and delayed errors after device replacement in the first integrated tests.\"\n\n## Children\n\n1. Queued-effect commit boundaries; cancellation is not rollback.\n2. Error-scope serialization, asynchronous validation, failed-compile-not-cached, delayed errors after replacement.\n3. Device loss during compile/upload/submit; fresh device path; state-class table.\n4. Lifecycle cancellation matrix.\n5. Submission-epoch retirement and credit window.\n\n## Success criteria for the epic\n\nAll children closed before F3D-11's scored runs (F3D-11 depends on it).\n\n## No-claim boundary\n\nRecovery of GPU-only state is explicitly not promised here (see 17.7/22b.1).\n","acceptance_criteria":"The first backend distinguishes preparation, issued transfers, submission, completion and publication; cancellation never pretends to roll back issued queue effects; error scopes are serialized; device loss during compilation, upload and submission never publishes stale results into a replacement device; cancellation at every supported await/publication boundary, disposal while pending, Wasm memory growth and delayed asynchronous results after ID reuse (simulated worker/decode tasks until 21b.6 adds real workers, which then extends the matrix) are tested; retirement uses coarse submission epochs with a bounded credit window.","status":"open","priority":0,"issue_type":"epic","created_at":"2026-09-09T16:46:24.417080Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:21.629984Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["device-loss","f3d-gpu","f3d-runtime","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-22a-first-backend-fault-injection-ctu","depends_on_id":"f3d-02-asupersync-browser-execution-58j.3","type":"blocks","created_at":"2026-09-09T16:49:38.592159Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22a-first-backend-fault-injection-ctu","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","type":"blocks","created_at":"2026-09-09T16:49:38.784313Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-22a-first-backend-fault-injection-ctu.1","title":"Queued-effect commit boundaries; cancellation is not rollback","description":"## Full scope\n\nQueued-effect commit boundaries: track preparation, each issued transfer (writeBuffer/writeTexture/external-image copies are already queue effects before submit), command submission, completion and logical publication separately; cancellation prevents stale publication and records terminal outcomes but never claims rollback of issued effects; private/versioned destinations preferred until publication where the source allows\n\n## Summary\n\n\"A policy saying that everything before submission is rollback-safe is incorrect.\"\n\n## Background (plan §11.4; AGENTS.md \"Cancellation is not rollback of issued effects\")\n\n## Technical approach\n\n- Job model in `f3d-runtime`: `GpuJob { state: Preparing | TransferIssued | Submitted | Completed | Published | Cancelled(at_state) | Failed }` with transitions driven by the frame path and the Asupersync region; `writeBuffer`/`writeTexture`/`copyExternalImageToTexture` move the job to `TransferIssued` immediately (they are queue effects), `queue.submit` to `Submitted`, `onSubmittedWorkDone` to `Completed`, and publication to `Published` at the epoch boundary.\n- Cancellation semantics: `Preparing` → nothing issued, staging reclaimed; `TransferIssued` → the effect stands, publication is cancelled and the destination is either a private versioned buffer (preferred, 07.1 staging) so nothing becomes visible, or, where the source contract makes the shared buffer visible, the partial effect is preserved exactly as upstream would; `Submitted` → not retractable; completion still recorded for retirement.\n- Telemetry reports outcomes per job kind so the report can never claim a rollback.\n\n## Success criteria\n\n- Tests: cancel before issue (nothing issued), cancel after issue before submit, cancel after submit; each with the documented outcome.\n\n## Test plan\n\n- Unit: job state-machine tests (preparation → issued transfer → submission → completion → publication) with cancellation at each state asserting the documented outcome; private-destination publication tests.\n- E2E: `tests/e2e/fault/queued-effects.mjs` on both browsers: cancel before issue (nothing issued), after issue before submit (effect issued, publication cancelled, shared-buffer state per contract), after submit (not retractable); no stale publication; no false rollback claim in telemetry.\n- Logging: job events `{job, state, outcome, effects_issued}`; archived under `evidence/22a.1/`.\n- Failure artifacts: buffer readbacks and the job timeline.\n\n## Considerations\n\n- \"After submission the GPU work is not generally retractable.\"\n","acceptance_criteria":"The runtime's GPU job model has explicit states for the five boundaries; a cancelled upload task whose writeBuffer was already issued is recorded as \"effect issued, publication cancelled\" and the shared buffer's visible state is handled per the source contract; tests demonstrate no stale publication and no false rollback claims.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:48:30.857618Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:22.329486Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["device-loss","f3d-gpu","f3d-runtime","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.1","depends_on_id":"f3d-02-asupersync-browser-execution-58j.3","type":"blocks","created_at":"2026-09-09T16:49:37.256346Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.1","depends_on_id":"f3d-02-asupersync-browser-execution-58j.5","type":"blocks","created_at":"2026-09-09T17:41:12.504180Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.1","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","type":"blocks","created_at":"2026-09-09T16:49:37.067950Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.1","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.1","type":"blocks","created_at":"2026-09-09T17:41:22.328979Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.1","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu","type":"parent-child","created_at":"2026-09-09T16:48:30.857618Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-22a-first-backend-fault-injection-ctu.2","title":"Error-scope serialization, async validation, failed-compile-not-cached, delayed errors","description":"## Full scope\n\nError-scope serialization and asynchronous validation: push/issue/pop without intervening awaits or user callbacks, results awaited afterwards; operation-specific result promises and error provenance under concurrent setup; a synchronously returned GPU object is not evidence of validity; failed pipeline compilation never becomes a cache entry; delayed errors after device replacement attributed to the old generation\n\n## Summary\n\nGPU validation is asynchronous and error scopes are shared state.\n\n## Background (plan §11.7, §23 [S38], [S44])\n\n## Technical approach\n\n- Error scopes: extend the 05.6 helper into the job model so every GPU operation batch runs `pushErrorScope('validation'|'out-of-memory'|'internal')` → synchronous issue → `popErrorScope()` calls with no `await` in between; results are awaited afterwards and attributed to the operation id and device generation; a debug assertion detects a turn boundary inside the body.\n- Asynchronous validation: `createRenderPipelineAsync`/`createComputePipelineAsync` results and `uncapturederror` events are joined to the operation registry; a pipeline enters the variant cache only after success; failures produce structured diagnostics with the shader source spans (15a.1 diagnostics).\n- Device replacement: results whose generation is older than the current device are logged against the old generation and ignored; the registry drops them after the terminal cleanup.\n- Concurrency: Asupersync preparation tasks serialize through a per-device scope token; a test spawns two concurrent pipeline preparations and asserts no interleaved scope push/pop.\n\n## Success criteria\n\n- Tests listed pass on both browsers.\n\n## Test plan\n\n- Unit: error-scope helper tests (no `await` inside the body; nesting per operation); promise-registry tests keyed by generation; cache-insertion-after-success tests.\n- E2E: `tests/e2e/fault/validation.mjs` on both browsers: concurrent preparation tasks cannot interleave scopes; invalid pipeline creation produces attributed errors and no cache entry; an error arriving after device replacement is logged against the old generation and ignored by the new device.\n- Logging: validation events `{operation, generation, scope_kinds, error, attributed_to}`; archived under `evidence/22a.2/`.\n- Failure artifacts: the error-scope timeline.\n\n## Considerations\n\n- Out-of-memory handling is not a capacity-discovery algorithm; budgets are conservative (07.1).\n","acceptance_criteria":"Concurrent preparation tasks cannot interleave error scopes (assertion in tests); pipeline/validation errors are attributed to the right operation and generation; an invalid pipeline creation test shows no cache pollution; an error arriving after device replacement is logged against the old generation and ignored by the new device's state.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:48:30.973674Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:22.537904Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["device-loss","f3d-gpu","f3d-runtime","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.2","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","type":"blocks","created_at":"2026-09-09T16:49:37.443182Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.2","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.1","type":"blocks","created_at":"2026-09-09T17:41:22.537549Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.2","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu","type":"parent-child","created_at":"2026-09-09T16:48:30.973674Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-22a-first-backend-fault-injection-ctu.3","title":"Device loss during compile/upload/submit with fresh-device path and state-class table","description":"## Full scope\n\nDevice loss during compilation, upload and submission: every resource/pipeline of the lost generation invalidated; fresh adapter/device acquisition with bounded retries (never re-requesting from a consumed/expired adapter); fresh device-generation handles; explicit canvas/history reconfiguration; stale compile/decode/worker results cannot publish into the replacement; committed source callbacks are not automatically rerun; tests distinguish reconstruction success, admitted reset, unrecoverable loss and deliberate teardown\n\n## Summary\n\nDevice loss handled by generation, not by hope.\n\n## Background (plan §11.5, §6.5, §23 [S51])\n\n\"Use a fresh adapter/device acquisition path, bounded retries, fresh device-generation handles, and explicit canvas/history reconfiguration. Do not repeatedly request replacement devices from an already consumed or expired adapter. A stale compile/decode/worker result cannot publish into the replacement. Do not automatically rerun already committed source callbacks.\"\n\n## Technical approach\n\n- Loss handler in `f3d-runtime`: on `device.lost`, bump the device generation (05.1), mark every resource/pipeline of the old generation dead (07.1), cancel and drain the session region's GPU children (02.3), run the terminal cleanup for outstanding submissions (22a.5).\n- Reacquisition: request a fresh adapter and device through 05.6's negotiation (never re-request from the consumed/expired adapter), bounded retries with backoff, record the new capability record; if the new device lacks a required feature, surface the source-shaped error rather than a degraded device.\n- Recovery: recreate class-1 resources (immutable packed assets, CPU-authoritative data) from current authoritative versions (06.6/06.3); hand class-2/3 to the policies (17.7/22b.1; until 17.7 lands, class-2 and class-3 resources default to report-loss, and H1/H2 contain no such state); reconfigure the canvas and attachment plan (07.2); explicit history reconfiguration.\n- Stale results: compile/decode/worker results tagged with the old generation are rejected on arrival; committed source callbacks are not rerun automatically.\n- Injection points for tests: during pipeline compilation, during an upload, during submission on H1/H2 scenes; outcomes classified as reconstruction success, admitted reset, unrecoverable loss, deliberate teardown.\n\nPrior notes retained:\n- Loss handler in `f3d-runtime`: bump device generation (05.1), tear down the session region's GPU children with drain (02.3), acquire a new adapter+device (05.6), recreate class-1 resources, apply class-2/3 policies (17.7 later extends), reconfigure canvas; expose the source-visible event contract (upstream's `webglcontextlost`/`device.lost` behaviors as applicable to the route).\n- Injection via `device.destroy()` in tests at each point. Because a destroyed device reports `lost.reason === 'destroyed'`, the same signal as deliberate teardown, deliberate teardown is classified by an explicit F3D teardown flag set before `destroy()`, never by `lost.reason`; the injection wrapper leaves the flag unset so the loss is handled as unexpected; genuine `reason: 'unknown'` losses cannot be injected deterministically and are covered by the fake-device unit tests.\n\n## Success criteria\n\n- Tests pass; policies documented.\n\n## Test plan\n\n- Unit: loss-handler tests with a fake device (generation bump; region drain; class-1 recreation from authoritative versions; bounded retries; consumed-adapter rule).\n- E2E: `tests/e2e/fault/device-loss.mjs` on both browsers injecting `device.destroy()` during compilation, upload and submission on H1/H2: recovery or policy outcome per resource class; stale old-generation results rejected; source-visible event contract preserved; the four outcome classes asserted.\n- Logging: loss/recovery events `{point, generation_old, generation_new, resources_recreated, outcome}`; archived under `evidence/22a.3/`.\n- Failure artifacts: the resource arena dump before/after and the first frame after recovery.\n\n## Considerations\n\n- Loss is a terminal cleanup outcome for in-flight frames, never a successful completion (§11.4).\n","acceptance_criteria":"Injected device loss at each of the three points leads to the documented recovery path with immutable/CPU-authoritative resources recreated from current versions, and every other resource class handled per the §11.5 table, with class-2/3 defaulting to report-loss until 17.7 lands; a stale result from the old generation is rejected; H1/H2 recover or report per policy; the four outcome classes are asserted by tests.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:48:31.075982Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:21.445761Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["device-loss","f3d-gpu","f3d-runtime","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.3","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.1","type":"blocks","created_at":"2026-09-09T17:41:22.739381Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.3","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu","type":"parent-child","created_at":"2026-09-09T16:48:31.075982Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.3","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu.1","type":"blocks","created_at":"2026-09-09T16:49:37.643981Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.3","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu.2","type":"blocks","created_at":"2026-09-09T16:49:37.833413Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-22a-first-backend-fault-injection-ctu.4","title":"Lifecycle cancellation matrix (every boundary, dispose pending, memory growth, delayed worker)","description":"## Full scope\n\nLifecycle cancellation matrix: cancel at every supported await/publication boundary; dispose resources while work is pending; grow Wasm memory mid-operation; deliver delayed asynchronous results (simulated worker/decode tasks; real Workers are added by 21b.6, which extends this matrix) after ID reuse; assert no leaks, no stale publication and correct terminal outcomes\n\n## Summary\n\nThe §14.7 lifecycle program, executed systematically.\n\n## Background (plan §14.7 \"Add lifecycle tests\")\n\n## Technical approach\n\n- Enumerate boundaries from the job model (22a.1: preparation, issued transfer, submission, completion, publication) and the region model (02.5: session, scene/view, asset prep, shader prep, worker job) plus every `await` in the runtime's asynchronous paths (device acquisition, pipeline compilation, asset decode, readback mapping).\n- Actions: cancel the owning region; dispose the resource while the job is pending; force `memory.grow` mid-operation (05.4); deliver a delayed asynchronous result (simulated worker/decode task) addressed to a reused ID; 21b.6 re-runs the matrix with real Workers.\n- Generator produces the (boundary × action) matrix; when combinatorial, sample with a fixed seed but always include every boundary at least once per action.\n- Assertions per scenario: documented terminal state reached; arena accounting balanced (no leaked GPU/CPU resources); region drained; no stale publication; the source's error/cleanup contract preserved (04.5).\n\n## Success criteria\n\n- Matrix green.\n\n## Test plan\n\n- Unit: scenario-matrix generator tests (boundary enumeration from the job/region models; bounded sampling when combinatorial).\n- E2E: the generated matrix (cancel at each await/publication boundary × dispose while pending × memory growth mid-operation × delayed asynchronous result after ID reuse) on H1/H2 scenes on both browsers; leak detection via arena accounting and region drain checks; every scenario ends in a documented terminal state.\n- Logging: per-scenario `{boundary, action, terminal_state, leaked_resources}`; archived under `evidence/22a.4/`.\n- Failure artifacts: the leaked-resource list and the scenario timeline.\n\n## Considerations\n\n- Keep scenario count bounded; sample when combinatorial.\n","acceptance_criteria":"A generated matrix of (boundary × action) scenarios runs against the first backend with H1/H2 scenes; every scenario ends in a documented terminal state with cleanup counters balanced; the matrix is part of the functional lane.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:31.182498Z","created_by":"jemanuel","updated_at":"2026-09-09T17:31:53.178820Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["device-loss","f3d-runtime","phase-1","testing","tier-solid"],"dependencies":[{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.4","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.4","type":"blocks","created_at":"2026-09-09T16:49:38.216028Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.4","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu","type":"parent-child","created_at":"2026-09-09T16:48:31.182498Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.4","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu.1","type":"blocks","created_at":"2026-09-09T16:49:38.022643Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.4","depends_on_id":"f3d-gate-1-foundation-tzn","type":"blocks","created_at":"2026-09-09T17:31:53.178502Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-22a-first-backend-fault-injection-ctu.5","title":"Submission-epoch retirement and bounded credit window via onSubmittedWorkDone","description":"## Full scope\n\nSubmission-epoch retirement and bounded credit window: track retirement in coarse submission epochs; use queue.onSubmittedWorkDone as a high-water-mark acknowledgement (not a per-command-buffer fence) to release eligible credits without serially awaiting every frame; loss/failure is terminal cleanup, not completion\n\n## Summary\n\nCompletion tracking that is both correct and fast.\n\n## Background (plan §11.4 last paragraph, §16.3, §23 [S49])\n\n## Technical approach\n\n- Epochs: every `queue.submit` increments a submission epoch; resources retired by that submission are queued under the epoch (07.1 retirement queue); staging-ring segments are stamped with the epoch that last read them.\n- Acknowledgement: after each submission (or every N submissions per policy), call `queue.onSubmittedWorkDone()`; on resolution treat it as a high-water mark: retire all queued resources with epoch <= acked epoch and release the corresponding credits; never treat it as a per-command-buffer fence.\n- Credit window: the two-frame window is enforced only in the bench lane, where the harness drives logical frames (§16.3) and a third logical frame waits for a credit; on-screen, source-requested renders are never dropped or deferred (that would coalesce an observable operation, §6.3), the browser's rAF cadence bounds work in flight, and telemetry reports frames in flight; no frame awaits its own completion.\n- Terminal cleanup: device loss or a rejected `onSubmittedWorkDone` promise runs the terminal path: outstanding epochs are marked failed, credits released, retirement forced, and the frame is not counted as completed (bench invalidation, 03.2).\n\n## Success criteria\n\n- Tests: window enforcement, retirement timing, terminal path; H1 steady state shows two frames in flight.\n\n## Test plan\n\n- Unit: epoch/credit accounting tests (window enforced at two; retirement drains to the acked epoch; terminal cleanup releases credits without counting a completed frame).\n- E2E: `tests/e2e/fault/retirement.mjs` on both browsers: H1 steady state shows two frames in flight with no self-await; a failed/lost submission takes the terminal path; staging-ring reuse only after ack.\n- Logging: per-submission `{epoch, submit_ts, ack_ts, credits, retired}`; archived under `evidence/22a.5/`.\n- Failure artifacts: the credit timeline.\n\n## Considerations\n\n- Do not busy-spin or starve the event loop (§16.3).\n","acceptance_criteria":"Resource retirement and staging-ring reuse are driven by acknowledged submission epochs; the credit window bounds frames in flight to the protocol's value (two) for scored runs; no frame awaits its own completion; a failed/lost submission releases credits via the terminal-cleanup path without counting as a completed frame.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:48:31.284807Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:21.075649Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-gpu","f3d-runtime","phase-1","tier-solid"],"dependencies":[{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.5","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.1","type":"blocks","created_at":"2026-09-09T17:41:22.955338Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.5","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu","type":"parent-child","created_at":"2026-09-09T16:48:31.284807Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22a-first-backend-fault-injection-ctu.5","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu.1","type":"blocks","created_at":"2026-09-09T16:49:38.405034Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-22b-device-context-recovery-closure-lqn","title":"F3D-22 (closure): Final context/device recovery, page lifecycle, exact native lifetimes, memory budgets","description":"## What this epic delivers\n\nLifecycle closure. Plan §19 F3D-22 closure \"uses 17\"; acceptance extends the early slice to \"final context/device recovery\".\n\n## Why (plan §11.5, §12.5, §18 Phase 5, §22 \"The foundation is real\")\n\n\"Verify host/CSP constraints, cold startup, memory pressure, device/context loss, mobile thermal behavior, integration permissions and interaction tails.\"\n\n## Children\n\n1. GPU-history/simulation recovery classes with charged costs.\n2. Page lifecycle behaviors.\n3. Exact native context loss/restore, XR session reset, canvas identity across replacement.\n4. Memory budgets, address-space checks and OOM policy.\n\n## Success criteria for the epic\n\nAll children closed; lifecycle suites in the functional, forced and exact lanes.\n\n## No-claim boundary\n\n\"A terminated page or noncooperative foreign operation has no universal finalization-time guarantee\" (§11.5).\n","acceptance_criteria":"GPU-history and simulation recovery classes are implemented and tested with charged checkpoint costs; page lifecycle transitions (hide/show, suspension, resize, scene replacement, timer-policy restoration, bounded obsolete-frame backlog) behave as upstream; exact native context loss/restore, XR session reset and canvas identity across replacement are correct; CPU/Wasm, staging and GPU memory budgets are enforced with address-space checks and an explicit OOM policy.","status":"open","priority":2,"issue_type":"epic","created_at":"2026-09-09T16:46:24.503137Z","created_by":"jemanuel","updated_at":"2026-09-09T16:49:40.774505Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["device-loss","exact-backend","f3d-gpu","f3d-runtime","phase-4","phase-5","tier-solid"],"dependencies":[{"issue_id":"f3d-22b-device-context-recovery-closure-lqn","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf","type":"blocks","created_at":"2026-09-09T16:49:40.774117Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22b-device-context-recovery-closure-lqn","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu","type":"blocks","created_at":"2026-09-09T16:49:40.543216Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-22b-device-context-recovery-closure-lqn.1","title":"GPU-history and simulation recovery classes end to end with charged costs","description":"## Full scope\n\nGPU-history and simulation recovery classes end to end: checkpoint/replay under the numeric and input-history contract where admitted, visible admitted reset otherwise, unrecoverable-loss reporting; checkpoint readback bandwidth/latency/memory charged to results; no seamless-preservation claims\n\n## Summary\n\nClose the recovery story for state the CPU never had.\n\n## Background (plan §11.5)\n\n\"Mandatory readback checkpoints have a real bandwidth/latency/memory cost and must be included in results; they are not a free recovery guarantee. A reset is visible application behavior and cannot be described as seamless state preservation.\"\n\n## Technical approach\n\n- Policy configuration per resource class (17.7): `Recreate` always; `ReplayUnderContract` only when the application's admitted contract (12a.2 numeric semantics, deterministic inputs from the harness or the source's own seeding) allows replay from the last checkpoint; `ReportLoss` otherwise, with the source-visible outcome (upstream's context-loss events where the source handles them; otherwise the documented reset/restart/error).\n- Checkpoint scheduling: optional periodic readbacks (17.4) of class-2 state with explicit bandwidth/latency/memory cost recorded in telemetry and charged in bench results when enabled.\n- Tests inject loss during SSGI/TRAA accumulation and H8 simulation on both browsers and assert the class policy's outcome; the report never describes a reset as seamless.\n\nPrior notes retained:\n- Policy configuration per resource class; optional checkpoint scheduling through the pass graph with cost telemetry; user-facing event/error shape aligned with upstream's context-loss behavior where the source observes it.\n- Source-visible contract per route: on the exact route it is `webglcontextlost`/`webglcontextrestored`; on the new-backend route it is the pinned `WebGPURenderer` device-loss callback (`onDeviceLost`, as inventoried by the census), which the candidate invokes with upstream's info shape.\n\n## Success criteria\n\n- Tests per class and case; cost lines in results.\n\n## Test plan\n\n- Unit: policy-configuration tests per resource class; checkpoint scheduling tests in the pass graph with cost accounting.\n- E2E: device-loss injection during SSGI/TRAA and H8 on both browsers with each class policy; checkpoint policy enabled runs show the cost in bench results; user-facing event/error shape compared to upstream's context-loss behavior.\n- Logging: recovery events with class, action, outcome and checkpoint bytes; archived under `evidence/22b.1/`.\n- Failure artifacts: post-recovery frames and the policy decision log.\n\n## Considerations\n\n- Deterministic clocks do not make arbitrary GPU replay bit-deterministic.\n","acceptance_criteria":"For the temporal fidelity cases and H8, device loss follows the class policy with tests for each outcome; if a checkpoint policy is enabled its cost appears in the benchmark results; the report never describes a reset as seamless.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:31.393902Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:21.990700Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["device-loss","f3d-gpu","f3d-runtime","phase-4","tier-solid"],"dependencies":[{"issue_id":"f3d-22b-device-context-recovery-closure-lqn.1","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf.7","type":"blocks","created_at":"2026-09-09T16:49:38.987315Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22b-device-context-recovery-closure-lqn.1","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu.3","type":"blocks","created_at":"2026-09-09T16:49:39.180404Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22b-device-context-recovery-closure-lqn.1","depends_on_id":"f3d-22b-device-context-recovery-closure-lqn","type":"parent-child","created_at":"2026-09-09T16:48:31.393902Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-22b-device-context-recovery-closure-lqn.2","title":"Page lifecycle: hide/show, suspension, resize, scene replacement, timer policy, backlog","description":"## Full scope\n\nPage lifecycle: hide/show, page suspension and resume, resize, scene replacement, restoration of the source's timer policy; bounded processing of obsolete-frame backlogs without rewriting a source simulation's time step; teardown ordering with drained regions\n\n## Summary\n\nThe browser lifecycle, respected.\n\n## Background (plan §11.5 last paragraph)\n\n\"Test hide/show, page suspension, resize, scene replacement, and restoration of the source's timer policy. Avoid processing an unbounded obsolete-frame backlog, but do not silently rewrite a source simulation's time step to accomplish it.\"\n\n## Technical approach\n\n- Hooks in `f3d-runtime`: `visibilitychange`/`pagehide`/`pageshow`/`resize` handlers that mirror what the source application and upstream renderer do (rAF naturally pauses in background; the runtime must not spin its own timers), restore the source's timer policy on resume, and reconfigure the canvas/attachment plan on resize (07.2).\n- Scene replacement: replacing the scene/view region drains child tasks (02.3) before the new region publishes; stale results rejected by generation.\n- Backlog: after a long background period, the source's own loop logic determines how many logical updates run (Three.js examples typically compute `delta` from the clock); the runtime must not rewrite the time step; a bounded policy only applies to F3D-internal preparation work, documented in the report.\n- Phone: real backgrounding via the automation driver with the harness recording the logical updates performed vs upstream.\n\n## Success criteria\n\n- Tests green on both browsers and on the phone (backgrounding).\n\n## Test plan\n\n- Unit: lifecycle hook tests with fake events (`visibilitychange`, `pagehide`, resize) and region policies.\n- E2E: scripted lifecycle events on both browsers and on the phone (real backgrounding): rAF pause/resume semantics, resize, scene replacement with drain, timer-policy restoration, bounded backlog after a long background period with the same logical updates as upstream (no time-step rewrite).\n- Logging: lifecycle events `{event, backlog_frames, processed, policy}`; archived under `evidence/22b.2/`.\n- Failure artifacts: trace diffs around the transition.\n\n## Considerations\n\n- Interacts with thermal protocol runs (23.3).\n","acceptance_criteria":"Lifecycle transitions behave observably as upstream (rAF pause/resume semantics, resize handling, scene replacement with region drain); after a long background period the candidate processes the same logical updates upstream would (no silent time-step rewrite) within a bounded backlog policy documented in the report.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:31.496745Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:18.339675Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-runtime","phase-4","tier-solid"],"dependencies":[{"issue_id":"f3d-22b-device-context-recovery-closure-lqn.2","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:27.022467Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22b-device-context-recovery-closure-lqn.2","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu.4","type":"blocks","created_at":"2026-09-09T16:49:39.369935Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22b-device-context-recovery-closure-lqn.2","depends_on_id":"f3d-22b-device-context-recovery-closure-lqn","type":"parent-child","created_at":"2026-09-09T16:48:31.496745Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-22b-device-context-recovery-closure-lqn.3","title":"Exact native context loss/restore, XR session reset, canvas identity across replacement","description":"## Full scope\n\nExact native context loss/restore, XR session reset and canvas identity across replacement: WebGL context loss/restoration through the exact component with upstream event contract; XR session end/reset with reference-space resets; renderer replacement at source-supported boundaries only; stale worker/compile/decode results rejected across replacement\n\n## Summary\n\nThe exact route's lifetimes and the replacement boundary.\n\n## Background (plan §5.6, §6.9, §20 \"Late backend choice breaks canvas identity or native resources\")\n\n## Technical approach\n\n- Exact route: through the exact component (04.3), force `WEBGL_lose_context.loseContext()`/`restoreContext()` and assert upstream's `webglcontextlost`/`webglcontextrestored` handling: event ordering, `preventDefault` behavior, resource re-upload on restore, render resumption; pending readbacks fail per upstream contract.\n- XR: on the capable lane (21b.3), end and re-enter sessions, reset reference spaces, and replace the renderer through `WebGLXRFallback`'s documented path; assert the new renderer instance gets a fresh route decision (04.4) and the old one's resources retire.\n- Replacement boundary: a fixture that destroys and recreates its renderer at a source-supported boundary must succeed with a new decision token; a fixture that tries to re-route the same exposed canvas must fail at build time with a span (12a.6).\n- Stale results: worker/compile/decode results tagged with the old renderer/device generation arriving after replacement are rejected (05.1/22a.3 mechanism) and logged; no source callback is rerun automatically.\n\n## Success criteria\n\n- Tests green.\n\n## Test plan\n\n- Unit: replacement-boundary tests (a new renderer instance is a new decision; switching an exposed canvas fails); generation checks across replacement for worker/compile/decode results.\n- E2E: exact-lane forced context loss/restore with upstream's events and re-upload behavior on both browsers; XR session end/reset on the capable lane; `WebGLXRFallback` replacement pattern; stale results after replacement rejected.\n- Logging: loss/restore and replacement events with generations; archived under `evidence/22b.3/`.\n- Failure artifacts: GL error logs and the decision log.\n\n## Considerations\n\n- Never counts toward new-backend coverage.\n","acceptance_criteria":"Exact-lane tests cover forced context loss/restore with upstream's events and resource re-upload behavior; XR session reset tests pass on the capable lane; a renderer replacement at a source boundary (WebGLXRFallback pattern) creates a new route decision while an attempt to switch an exposed canvas fails; stale results are rejected by generation.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:31.600998Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:16.241997Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["device-loss","exact-backend","f3d-runtime","phase-4","tier-solid","xr"],"dependencies":[{"issue_id":"f3d-22b-device-context-recovery-closure-lqn.3","depends_on_id":"f3d-04-module-routing-exact-boundaries-6mv.3","type":"blocks","created_at":"2026-09-09T16:49:39.756833Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22b-device-context-recovery-closure-lqn.3","depends_on_id":"f3d-21b-all-host-tool-integrations-5mw.3","type":"blocks","created_at":"2026-09-09T16:49:39.965753Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22b-device-context-recovery-closure-lqn.3","depends_on_id":"f3d-22a-first-backend-fault-injection-ctu.3","type":"blocks","created_at":"2026-09-09T16:49:39.558494Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22b-device-context-recovery-closure-lqn.3","depends_on_id":"f3d-22b-device-context-recovery-closure-lqn","type":"parent-child","created_at":"2026-09-09T16:48:31.600998Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-22b-device-context-recovery-closure-lqn.4","title":"Memory budgets: CPU/Wasm, staging and GPU distinct; address-space checks; OOM policy","description":"## Full scope\n\nMemory budgets: CPU/Wasm memory, upload staging and GPU allocations as distinct budgets even on unified memory; individual allocations and total live reservations bounded below tested device budgets; checked address arithmetic and chunking within the wasm32 address space (no memory64 assumption); explicit OOM policy; bounded caches proven under pressure\n\n## Summary\n\nMemory realism.\n\n## Background (plan §12.5, §16.6 \"Startup and memory\")\n\n\"Browser WebGPU does not make arbitrary Wasm memory directly usable as a GPU buffer. Bound individual allocations and total live reservations below the tested application/device budget; a large GPU buffer limit is not available RAM.\"\n\n## Technical approach\n\n- Budgets: extend 07.1's accounting so CPU/Wasm linear memory (pages reserved and in use), upload staging (ring bytes in flight) and GPU allocations (by resource kind) are tracked as three independent counters with configured ceilings per device class from protocol v1 (03.6); the ceilings come from the frozen budgets, not from device limits.\n- Enforcement: allocations above the per-object ceiling are refused with a structured error; total-live above the budget triggers backpressure (staging) or eviction (caches) before any new allocation; the OOM policy for genuinely unavoidable pressure is explicit (fail the operation with the source's error contract; never a silent quality reduction).\n- Address space: all offset/length arithmetic in the ABI and asset paths uses checked `u32`/`usize` math against the actual Wasm memory size; checked address arithmetic is exercised with mocked memory sizes in unit tests (a real near-4GiB reservation would OOM the tab, certainly on the phone); the real pressure test reserves just above the configured budget to trigger backpressure/eviction, never near the wasm32 address-space limit; memory64 is not assumed.\n- Base64/decode transients from the standalone loader (10.2) are counted against the CPU budget during startup and released promptly.\n- Phone/M5 pressure runs of each headline artifact with the three counters sampled per frame; the report shows peaks vs budgets.\n\n## Success criteria\n\n- Tests green; results feed 23.4.\n\n## Test plan\n\n- Unit: budget-enforcement tests per budget (CPU/Wasm, staging, GPU) driving each to its limit and asserting backpressure/eviction; checked address arithmetic tests at the wasm32 boundary; OOM policy tests with a fake allocator.\n- E2E: memory-pressure runs of each headline artifact on the phone and M5 with peak accounting vs the frozen budgets; base64 decoding transients included.\n- Logging: per-run memory `metric` events per budget and pressure events `{budget, action: backpressure|evict|oom-policy}`; archived under `evidence/22b.4/`.\n- Failure artifacts: the allocation timeline at the overage.\n\n## Considerations\n\n- Base64 decoding transients count (10.2).\n","acceptance_criteria":"The three budgets are enforced with tests that drive each to its limit and observe backpressure/eviction rather than OOM; address arithmetic is checked; a memory-pressure run of each headline artifact on the phone stays within the frozen budget or reports the exact overage.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:31.705118Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:21.810372Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-core","f3d-gpu","f3d-runtime","phase-4","phase-5","tier-solid"],"dependencies":[{"issue_id":"f3d-22b-device-context-recovery-closure-lqn.4","depends_on_id":"f3d-07-initial-webgpu-render-slice-2v8.1","type":"blocks","created_at":"2026-09-09T16:49:40.162049Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22b-device-context-recovery-closure-lqn.4","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.2","type":"blocks","created_at":"2026-09-09T16:49:40.354318Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22b-device-context-recovery-closure-lqn.4","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:27.233723Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-22b-device-context-recovery-closure-lqn.4","depends_on_id":"f3d-22b-device-context-recovery-closure-lqn","type":"parent-child","created_at":"2026-09-09T16:48:31.705118Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-23-feature-host-performance-validation-b8v","title":"F3D-23: Complete feature-host and performance hardware validation","description":"## What this epic delivers\n\nPhase 5's validation campaign. Plan §19 F3D-23 depends on 18–22; acceptance: \"Capable-host parity plus unchanged paired performance/thermal/startup/memory gates\".\n\n## Why (plan §16.6, §18 Phase 5, §22)\n\n\"Run held-out workloads and final full-feature/forced-new-backend/exact-native suites on appropriate reserved hardware.\"\n\n## Children\n\n1. Capable-host matrix runs and unavailable-host evidence classes.\n2. Full scored benchmark campaign.\n3. Thermal sustained phone protocol.\n4. Startup/memory validation against frozen budgets.\n5. Build cost and developer-experience measurements.\n6. Held-out final workloads and final suites on reserved hardware.\n\n## Success criteria for the epic\n\nAll children closed with published results.\n\n## No-claim boundary\n\n\"Neither finite corpus establishes a universal 3× guarantee for all Three.js applications\" (§16.6).\n","acceptance_criteria":"All lanes run on all strata and capable hosts with unavailable-host obligations listed; the full scored benchmark campaign across H1–H8, controls and the fidelity set on all four strata meets or fails the unchanged §16.6 gates with published intervals; thermal, startup/memory and build-cost validations complete; held-out workloads and final suites run on reserved hardware.","status":"open","priority":1,"issue_type":"epic","created_at":"2026-09-09T16:46:24.587411Z","created_by":"jemanuel","updated_at":"2026-09-09T16:49:45.337605Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-conformance","phase-5","tier-solid"],"dependencies":[{"issue_id":"f3d-23-feature-host-performance-validation-b8v","depends_on_id":"f3d-18b-full-feature-gate-closure-mk4","type":"blocks","created_at":"2026-09-09T16:49:44.270452Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v","depends_on_id":"f3d-19-loaders-exporters-codecs-qmy","type":"blocks","created_at":"2026-09-09T16:49:44.483667Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v","depends_on_id":"f3d-20-profile-justified-gpu-improvements-023","type":"blocks","created_at":"2026-09-09T16:49:44.701962Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v","depends_on_id":"f3d-21b-all-host-tool-integrations-5mw","type":"blocks","created_at":"2026-09-09T16:49:44.930407Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v","depends_on_id":"f3d-22b-device-context-recovery-closure-lqn","type":"blocks","created_at":"2026-09-09T16:49:45.145636Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v","depends_on_id":"f3d-gate-4-generalization-5hm","type":"blocks","created_at":"2026-09-09T16:49:45.337344Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-23-feature-host-performance-validation-b8v.1","title":"Capable-host matrix runs and unavailable-host evidence classes","description":"## Full scope\n\nCapable-host matrix runs: functional, forced-new-backend and exact lanes on every stratum and browser, XR headset lane, permission-dependent lanes (media, file input, pointer lock, fullscreen); device-specific blocks accepted only when the reference cannot exercise the same operation with the same permissions/capabilities; unavailable hosts recorded as outstanding obligations\n\n## Summary\n\nEvery feature, on every capable host, honestly.\n\n## Background (plan §14.10, §18 Phase 4 \"Complete a capable-host matrix\")\n\n## Technical approach\n\n- Inventory the hosts from 03.7 plus the capable lanes: XR headset/browser, permission-dependent lanes (microphone/media, file input, pointer lock, fullscreen), and any lane a family's prerequisites require (from 01.5's matrix).\n- For each host run the functional lane, the forced-new-backend lane and the exact lane through the repository-owned commands; collect per-family results into the five-column matrix (import/symbol, behavior, new-backend rendering, exact integration, capable-host) per host.\n- `host-blocked` acceptance rule: a block is accepted only with attached evidence that the reference fails the same operation with the same permissions/capabilities on that host; otherwise it is a candidate defect.\n- Unavailable hosts: list the exact families and tests that could not run, the host required, and the obligation status; never count them in any column.\n- Third evidence class (plan §14.10): a documented upstream limitation, where the pinned feature has no executable positive path even on its specified host prerequisites; the exact conditional implementation/error behavior is preserved and tested, the record is listed under that evidence class explicitly, and it is never counted as positive validation or used to conceal a candidate gap.\n\n## Success criteria\n\n- Matrix published.\n\n## Test plan\n\n- Unit: matrix aggregation tests (five separate columns per host; `host-blocked` requires reference evidence; obligations listed, never counted).\n- E2E: functional, forced-new-backend and exact lanes on every stratum/browser from the device inventory plus the XR headset lane and permission-dependent lanes (media, file input, pointer lock, fullscreen).\n- Logging: per-host lane summaries and the matrix JSON with evidence pointers; archived under `evidence/23.1/`.\n- Failure artifacts: lane result files per host.\n\n## Considerations\n\n- Product labels never determine capabilities; probe at runtime.\n","acceptance_criteria":"A per-stratum/per-host results matrix exists with the five coverage numbers per host; every host-blocked record has reference evidence; obligations for unavailable hosts are listed explicitly and never counted as passes.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:31.808050Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:20.135397Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","phase-5","testing","tier-solid","xr"],"dependencies":[{"issue_id":"f3d-23-feature-host-performance-validation-b8v.1","depends_on_id":"f3d-01-upstream-pin-and-census-gl8.5","type":"blocks","created_at":"2026-09-09T17:41:11.893300Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.1","depends_on_id":"f3d-18b-full-feature-gate-closure-mk4.6","type":"blocks","created_at":"2026-09-09T16:49:41.001342Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.1","depends_on_id":"f3d-21b-all-host-tool-integrations-5mw.2","type":"blocks","created_at":"2026-09-09T16:49:41.413528Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.1","depends_on_id":"f3d-21b-all-host-tool-integrations-5mw.3","type":"blocks","created_at":"2026-09-09T16:49:41.205704Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.1","depends_on_id":"f3d-23-feature-host-performance-validation-b8v","type":"parent-child","created_at":"2026-09-09T16:48:31.808050Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-23-feature-host-performance-validation-b8v.2","title":"Full scored benchmark campaign H1-H8 on all four strata under protocol v1","description":"## Full scope\n\nFull scored benchmark campaign: H1–H8 on iPhone 17 Pro Max (Safari; Chrome separately), M5 (Safari and Chrome), NVIDIA and AMD discrete GPUs (Chrome/Windows; Linux lanes separate); frozen protocol v1; per-cell 2x floor with 95% lower bounds, per-stratum 3x geomean; H7/H8 required; tail budgets; controls within 5%; negative and startup controls; fidelity set; all-valid-cases aggregate; every reference configuration published; new-backend ownership asserted for all scored frames\n\n## Summary\n\nThe launch performance contract, executed exactly as frozen.\n\n## Background (plan §16.6, §15.4, §15.6, §16.4)\n\n## Technical approach\n\n- Per stratum (iPhone 17 Pro Max Safari with Chrome separately; M5 Safari and Chrome; NVIDIA and AMD Chrome/Windows with Linux lanes separate) on reserved devices per 03.7's checklist: `f3d bench benchmarks/headline.toml --device <stratum>` (the bench subcommand delivered by 11.4) for H1–H8, the negative and startup controls, and the fidelity set; frozen references (03.3 and the per-case extensions), protocol v1 repeat counts, alternating order, warmup; on-screen companion runs for pacing/input latency.\n- Ownership: attribution counters (07.6) must show zero retained submissions in every scored frame.\n- Analysis (03.8): per-cell median ratios with 95% lower bounds; per-stratum log-space geomean with intervals; tails; controls within 5%; the all-valid-cases aggregate; FAILED_CELL rows for any missing/failed candidate run.\n- Results package: raw runs, capability records, telemetry breakdowns, analysis outputs, every reference configuration; verdict table against every §16.6 row.\n\n## Success criteria\n\n- Results package; verdict table.\n\n## Test plan\n\n- Unit: protocol-loading and cell-set tests (required cells from the corpus manifest; a missing candidate run produces a FAILED_CELL row).\n- E2E: `f3d bench` across all four strata on reserved devices per 03.7 discipline for H1–H8, controls (negative and startup controls) and the fidelity set; on-screen companion runs; ownership assertion on all scored frames; analysis via 03.8 with per-cell and per-stratum verdicts.\n- Logging: raw per-frame JSON per run, capability records, telemetry breakdowns, the analysis reports and the all-valid-cases aggregate; archived under `evidence/23.2/<stratum>/`.\n- Failure artifacts: invalidated runs retained with reasons; failed cells reported, never excluded.\n\n## Considerations\n\n- \"A changed product decision or benchmark corpus requires a newly labeled plan/results version.\"\n","acceptance_criteria":"The complete results package exists: raw paired runs, analysis outputs, per-cell and per-stratum verdicts against every §16.6 row, control and fidelity results, the all-valid-cases aggregate, and the reference configurations; any failed cell or stratum is reported as a failure; nothing is excluded or relabeled.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-09T16:48:31.910895Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:23.890963Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","h1","h2","h3","h4","h5","h6","h7","h8","new-backend","phase-5","tier-frontier"],"dependencies":[{"issue_id":"f3d-23-feature-host-performance-validation-b8v.2","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.6","type":"blocks","created_at":"2026-09-09T16:49:42.413370Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.2","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr.8","type":"blocks","created_at":"2026-09-09T16:49:42.220207Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.2","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf.6","type":"blocks","created_at":"2026-09-09T16:49:42.015992Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.2","depends_on_id":"f3d-20-profile-justified-gpu-improvements-023.5","type":"blocks","created_at":"2026-09-09T16:49:41.809032Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.2","depends_on_id":"f3d-23-feature-host-performance-validation-b8v","type":"parent-child","created_at":"2026-09-09T16:48:31.910895Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.2","depends_on_id":"f3d-gate-4-generalization-5hm","type":"blocks","created_at":"2026-09-09T16:49:41.598830Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-23-feature-host-performance-validation-b8v.3","title":"Thermal sustained phone protocol (20-minute sessions, steady segment scored)","description":"## Full scope\n\nThermal sustained phone protocol: 20-minute sustained sessions with paired cooldown/recovery and recorded power/charging policy; final steady segment reported separately from the initial cool segment; phone floors evaluated on the sustained segment; energy per frame only with a supporting instrument\n\n## Summary\n\nA phone that wins for thirty seconds and then throttles has not won. This bead runs the sustained protocol the gate requires.\n\n## Background (plan §16.5, §12.5, §16.6 \"Thermal\"; AGENTS.md \"Thermal\")\n\n\"Run a sustained mobile session, provisionally 20 minutes, with paired cooldown/recovery conditions and a recorded power/charging policy. Report the final steady segment separately from the initial cool segment. The phone's scored performance floor must hold in the declared sustained segment; a brief cool-start win does not satisfy a sustained-use claim.\" \"Report energy per frame only with an instrument or counter that supports that claim. Do not infer a precise power improvement from CPU timings or a noisy change in battery percentage.\"\n\n## Technical approach\n\n- Use 03.7's thermal script per headline cell on the phone: reference then candidate (order alternated across repeats) for the sustained duration from protocol v1 (provisionally 20 minutes) with periodic completed-frame throughput samples via the offscreen lane (03.2) and on-screen companions; matched cooldown until the device returns to the recorded baseline proxy; identical brightness/DPR/charging policy; Chrome on iOS as a separate lane.\n- Segmentation from the protocol: the steady segment is the final N minutes; both segments reported; the transition profile kept.\n- Analysis (03.8) on steady-segment blocks: cell floors and stratum geomean evaluated on the steady segment; cool-segment numbers published alongside, labeled.\n- OS thermal-state signals recorded where exposed; otherwise unknown; energy per frame only with an instrument (none assumed).\n\nPrior notes retained:\n- Use the thermal script from 03.7: per cell, run reference then candidate (order alternated across repeats) for the protocol's sustained duration with periodic completed-frame throughput samples (03.2 lane in on-screen and offscreen forms), matched cooldown between runs until the device returns to the recorded baseline temperature proxy, identical brightness/DPR/charging policy.\n- Segmentation rule from the protocol (03.6): the steady segment is the final N minutes; report both segments and the transition.\n- Analysis (03.8) on steady-segment blocks; pacing/input-latency companion numbers from the on-screen runs.\n- Record OS thermal-state signals where exposed; otherwise mark unknown.\n\n## Success criteria\n\n- Sustained results table per cell with steady-segment verdicts; cool-segment numbers published alongside, clearly labeled.\n\n## Test plan\n\n- Unit: segmentation tests (steady segment = final N minutes; cool/steady separation cannot manufacture a difference on reference-vs-reference data).\n- E2E: sustained paired sessions per headline cell on the phone (Safari; Chrome lane separately) with alternating order, matched cooldown to the recorded baseline proxy, identical brightness/DPR/charging policy; analysis on steady-segment blocks; on-screen pacing/input-latency companions.\n- Logging: throughput time series `{t, frames, throughput, thermal_proxy, charging}` and segment summaries; archived under `evidence/23.3/`.\n- Failure artifacts: the full time series for any failing cell.\n\n## Considerations\n\n- Reserved device: no other agent work on the phone or its host Mac during runs.\n- Chrome on iOS is a separate lane with its actual engine and capabilities recorded (§12.1).\n","acceptance_criteria":"For every headline cell on the iPhone 17 Pro Max stratum, sustained paired sessions (candidate and frozen reference, alternating order, matched cooldown) produce throughput series segmented into initial-cool and steady segments; the 2x cell floor and 3x stratum geomean are evaluated on the steady segment with intervals; thermal state and charging policy are logged; any energy claim cites the instrument used or is absent.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:32.016422Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:27.702596Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","phase-5","tier-solid"],"dependencies":[{"issue_id":"f3d-23-feature-host-performance-validation-b8v.3","depends_on_id":"f3d-23-feature-host-performance-validation-b8v","type":"parent-child","created_at":"2026-09-09T16:48:32.016422Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.3","depends_on_id":"f3d-23-feature-host-performance-validation-b8v.2","type":"blocks","created_at":"2026-09-09T16:49:42.620136Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-23-feature-host-performance-validation-b8v.4","title":"Startup and memory validation against frozen budgets (cold/warm, hitches, no OOM)","description":"## Full scope\n\nStartup and memory validation against frozen budgets: cold (document acquisition, decoding, Wasm compilation, shader/pipeline compilation, asset preparation, first correct frame, first interactive frame) and warm startup per artifact class and stratum; reachable-variant compilation hitches; peak memory; bounded caches; no OOM\n\n## Summary\n\nStartup and memory are gates, not footnotes.\n\n## Background (plan §16.4, §16.6 \"Startup and memory\", §17.4, §10.3)\n\n## Technical approach\n\n- Artifact classes: the small ordinary closed scene (startup control), every headline artifact (H1–H8 standalone), and a runtime-extensible application (file input + dynamic shaders/nodes) with its included codecs and translators.\n- Cold protocol per stratum: clear browser caches, fresh document load over the local HTTPS server; record the timeline phases from the harness (document acquisition, decode of embedded data, Wasm compile/instantiate, shader/pipeline compilation, asset preparation, first correct frame, first interactive frame) using the measurement clock; warm protocol: reload with caches intact.\n- Hitches: run the interaction script through every reachable variant so lazily compiled variants (08.3/15b.2/16.2) are exercised; record each compile as a hitch with its duration in the interaction-tail metrics.\n- Memory: peak of the three budgets (22b.4) plus browser-reported process memory where available; report per artifact and stratum against the frozen numbers (03.6); embedded Wasm compile time is reported as non-streaming.\n\n## Success criteria\n\n- Results; verdicts.\n\n## Test plan\n\n- Unit: startup-timeline parser tests; budget-comparison tests with fixture numbers.\n- E2E: cold (cleared caches, fresh document) and warm startup runs for every headline artifact, the small closed scene and the runtime-extensible application on every stratum; interaction scripts hitting unvisited variants to capture compile hitches; peak memory via 10.2/07.1 accounting plus browser-reported values.\n- Logging: startup phase `metric` events `{phase, ms}` per run, memory peaks per budget, hitch events `{variant, compile_ms}`; archived under `evidence/23.4/`.\n- Failure artifacts: the timeline of any run exceeding a budget.\n\n## Considerations\n\n- Embedded Wasm gets no streaming compilation; report it.\n","acceptance_criteria":"Every headline artifact, the small closed scene and the runtime-extensible application meet or fail the frozen startup/memory budgets on every stratum with the component breakdown published; interaction-tail hitches from lazy variant compilation are reported.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:32.119800Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:16.592724Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","packaging","phase-5","tier-solid"],"dependencies":[{"issue_id":"f3d-23-feature-host-performance-validation-b8v.4","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.6","type":"blocks","created_at":"2026-09-09T16:49:43.014939Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.4","depends_on_id":"f3d-22b-device-context-recovery-closure-lqn.4","type":"blocks","created_at":"2026-09-09T16:49:42.815839Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.4","depends_on_id":"f3d-23-feature-host-performance-validation-b8v","type":"parent-child","created_at":"2026-09-09T16:48:32.119800Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.4","depends_on_id":"f3d-23-feature-host-performance-validation-b8v.2","type":"blocks","created_at":"2026-09-09T16:49:43.220970Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-23-feature-host-performance-validation-b8v.5","title":"Build cost and developer-experience measurements","description":"## Full scope\n\nBuild cost and developer-experience measurements: clean and incremental conversion time, Rust/Wasm and shader compilation time, cache hit correctness, generated bytes, latency of changing a common material or update function; source maps and reports identify the original span for rejections and expensive synchronization boundaries\n\n## Summary\n\nWhether the compiler is pleasant and predictable to use is part of whether it is useful. This bead measures it on the real corpus.\n\n## Background (plan §16.9, §3.5, §10.6)\n\n\"Measure clean and incremental conversion time, Rust/Wasm and shader compilation time, cache hit correctness, generated bytes, and the latency of changing a common material or update function. These are separate from the frame-throughput score but part of whether the compiler is useful. Rebuilding every generated kernel for a color-only edit is a design regression even if the deployed frame is fast.\" \"Source maps and reports must identify the original application span that caused a rejection or expensive synchronization boundary. Runtime tuning, lazy material compilation, and deoptimization pauses all remain visible in the corresponding cold/interaction measurements.\"\n\n## Technical approach\n\n- Scripted edit set applied to each corpus and held-out application: (a) change a material color literal; (b) change an arithmetic expression in the update loop; (c) replace an asset file; (d) add a GUI control; for each, measure `f3d build` wall time and per-stage timings (ingest, analysis, codegen, Rust/Wasm compile, shader translation, packaging), cache statistics (10.6 hits/misses/units rebuilt) and artifact bytes on the build machine (never the benchmark device).\n- Cache correctness: after each edit, the rebuilt artifact equals a clean build byte-for-byte and untouched units were served from cache; the color-only edit rebuilds only material-variant units.\n- Span verification: sample report entries (barriers, exact-route reasons, synchronization boundaries) resolved through the emitted source maps to the original file/line.\n- Output: `docs/dx-report-v1.md` plus JSON; separate from the throughput score.\n\nPrior notes retained:\n- Scripted edit set applied to each corpus application (H1–H8, fidelity cases, held-out set): (a) change a material color literal, (b) change an arithmetic expression in the update loop, (c) replace an asset file, (d) add a GUI control; measure `f3d build` wall time, per-stage timings (ingest, analysis, codegen, Rust/Wasm compile, shader translation, packaging), cache statistics (10.6), and output bytes.\n- Cache correctness: after each edit, verify the rebuilt artifact equals a clean build byte-for-byte (determinism) and that untouched units were served from cache.\n- Span verification: for a sample of report entries (barriers, exact-route reasons, synchronization boundaries), resolve through the emitted source maps and assert the original file/line.\n- Output: `docs/dx-report-v1.md` plus JSON.\n\n## Success criteria\n\n- Report published; the color-only edit rebuilds only material-variant units (assert by cache logs); span checks pass.\n\n## Test plan\n\n- Unit: measurement-script tests on a fixture project (stage timings parsed; cache statistics read; span resolution through source maps).\n- E2E: the scripted edit set (material color; update-function expression; asset swap; control addition) applied to every corpus and held-out application on the build machine with clean and incremental timings, cache hit/miss logs and artifact bytes; span checks for a sample of report entries.\n- Logging: per-edit `{app, edit, stage, ms, cache_hits, cache_misses, units_rebuilt, bytes}`; archived under `evidence/23.5/`.\n- Failure artifacts: the cache log showing over-rebuilding.\n\n## Considerations\n\n- Separate from the throughput score; never merged into it.\n- If incremental builds are slow because of a design choice, file a defect bead rather than tuning numbers.\n","acceptance_criteria":"A developer-experience report per corpus application records clean build time, incremental build time after scripted edits (material color change, update-function change, asset swap, control addition), Rust/Wasm compile time, shader translation/compile time, cache hit/miss counts with correctness verification, and artifact bytes; a color-only edit demonstrably rebuilds only the affected content-addressed units; every rejection and synchronization boundary in the report resolves through source maps to the original application span.","status":"open","priority":3,"issue_type":"task","created_at":"2026-09-09T16:48:32.227903Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:28.226792Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-cli","f3d-compiler","phase-5","tier-solid"],"dependencies":[{"issue_id":"f3d-23-feature-host-performance-validation-b8v.5","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.6","type":"blocks","created_at":"2026-09-09T16:49:43.417732Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.5","depends_on_id":"f3d-11-first-product-gate-ui0.5","type":"blocks","created_at":"2026-09-09T17:15:27.443879Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.5","depends_on_id":"f3d-12a-early-islands-route-analysis-63y.9","type":"blocks","created_at":"2026-09-09T16:49:43.610501Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.5","depends_on_id":"f3d-23-feature-host-performance-validation-b8v","type":"parent-child","created_at":"2026-09-09T16:48:32.227903Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-23-feature-host-performance-validation-b8v.6","title":"Held-out final workloads, final suites on reserved hardware, upstream drift check","description":"## Full scope\n\nHeld-out final workloads and final full-feature/forced-new-backend/exact-native suites on reserved hardware; latest stable upstream reconciled against the tested r186 target with immutable earlier results retained\n\n## Summary\n\nThe last validation pass before the final audit: fresh workloads, all lanes, all strata, and an honest look at what upstream has shipped since the pin.\n\n## Background (plan §18 Phase 5, §5.17, §14.8, §16.7 \"Final gate\")\n\n\"Run held-out workloads and final full-feature/forced-new-backend/exact-native suites on appropriate reserved hardware. Reconcile latest stable upstream changes against the tested version; retain immutable earlier results.\" \"Check the latest stable release before a new FrankenThreeD release and state its tested Three.js version explicitly... A newer unverified release may be used only with its actual retained component and truthful unverified-version status, never as an already-certified accelerated version.\"\n\n## Technical approach\n\n- Final held-out selection: add workloads not present in any earlier tuning or held-out batch; record selection date and rationale in `benchmarks/heldout-final-v1.toml` before running anything.\n- Lanes: functional, forced-new-backend and exact (18a.4) across the device matrix (03.7) on reserved hardware via the repository-owned commands; archive under `evidence/23.6/`.\n- Held-out measurement: `f3d verify` then `f3d bench` under protocol v1 for the preregistered (12b.4) and final held-out sets; published separately from the headline score.\n- Drift: run the census generator's drift mode (01.3) against the latest stable upstream tag; write `compatibility/drift-r186-to-<latest>.json` for 24.5; the r186 oracle and results are untouched (hash manifest before/after).\n\n## Success criteria\n\n- Lane results green (or defects filed) on every stratum; held-out results published; drift diff recorded; r186 artifacts and results untouched (hash check).\n\n## Test plan\n\n- Unit: hash-manifest test proving r186 evidence immutability before/after; drift-diff determinism test.\n- E2E: all three lanes on reserved hardware across the strata; `f3d verify` and `f3d bench` on the preregistered and final held-out sets; the census drift mode against the latest stable upstream producing the diff.\n- Logging: per-lane per-stratum result files; held-out results with intervals; drift report; archived under `evidence/23.6/`.\n- Failure artifacts: lane failures and verify artifacts.\n\n## Considerations\n\n- \"Do not shift the benchmark oracle midway through an experiment\" (§5.17): the drift check is informational for this release.\n","acceptance_criteria":"The preregistered held-out set (12b.4) plus a final set of additional held-out workloads chosen before this bead starts pass functional equivalence and are measured under protocol v1 with published intervals; all three production-route lanes pass on reserved hardware across the strata with the run logs archived; the census drift check against the latest stable upstream release is executed and its diff recorded as a separate unverified target without modifying the r186 oracle or any r186 result.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:32.357615Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:14.440116Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","f3d-conformance","phase-5","tier-solid"],"dependencies":[{"issue_id":"f3d-23-feature-host-performance-validation-b8v.6","depends_on_id":"f3d-23-feature-host-performance-validation-b8v","type":"parent-child","created_at":"2026-09-09T16:48:32.357615Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.6","depends_on_id":"f3d-23-feature-host-performance-validation-b8v.1","type":"blocks","created_at":"2026-09-09T16:49:43.834863Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-23-feature-host-performance-validation-b8v.6","depends_on_id":"f3d-23-feature-host-performance-validation-b8v.2","type":"blocks","created_at":"2026-09-09T16:49:44.048150Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-24-final-audit-ne8","title":"F3D-24: Final full-functionality, accelerated-coverage, deployment, size and version audit (Definition of done)","description":"## What this epic delivers\n\nKill gate 5 and the definition of done. Plan §19 F3D-24 depends on 23; acceptance: \"All Section 22 gates; no feature gap hidden by a narrowed manifest or exact renderer\".\n\n## Why (plan §22, §16.7 \"Final gate\", §14.10 last paragraph)\n\n\"The product promise is full Three.js functionality plus independently demonstrated acceleration, not 'a fast supported subset' and not 'the same upstream renderer under a new name.'\" \"Failing it means no performance-replacement launch claim, even if the implementation is otherwise impressive.\"\n\n## Children\n\n1. Functional closure audit.\n2. Accelerated-rendering closure audit.\n3. Deployment audit.\n4. Size, ownership and license audit.\n5. Version-drift reconciliation.\n6. Final report and kill gate 5 decision.\n\n## Success criteria for the epic\n\nAll children closed; the decision recorded.\n\n## No-claim boundary\n\nIf any gate fails, the release is not labeled a performance replacement or full compatibility; the report says exactly what holds.\n","acceptance_criteria":"All §22 gates hold and are documented: full functionality is real, the product is real, both compatibility and new-backend coverage are real, performance is real, the foundation is real, the implementation remains focused, completeness remains maintainable; no feature gap is hidden by a narrowed manifest or exact renderer; the final report is published.","status":"open","priority":2,"issue_type":"epic","created_at":"2026-09-09T16:46:24.669321Z","created_by":"jemanuel","updated_at":"2026-09-09T16:49:47.937719Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","kill-gate","phase-5","tier-solid"],"dependencies":[{"issue_id":"f3d-24-final-audit-ne8","depends_on_id":"f3d-23-feature-host-performance-validation-b8v","type":"blocks","created_at":"2026-09-09T16:49:47.937211Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-24-final-audit-ne8.1","title":"Functional closure audit (zero blocking states, obligations listed, no single percentage)","description":"## Full scope\n\nFunctional closure audit: every feature in the pinned source/package/addon/tool universe has a working production implementation under the same source-host prerequisites; no permanent cuts, stubs, placeholder shaders, unimplemented codecs/exporters, omitted runtime variants, missing browser/tool integrations or candidate-specific valid-input refusals; a narrowed application profile cannot change this\n\n## Summary\n\n\"Full functionality is real.\" This audit verifies the first clause of the definition of done against the manifest, the lanes and the plan's family map, and refuses to be summarized as one number.\n\n## Background (plan §22 first paragraph, §5.11, §5.15, §14.10, §18 Phase 4 exit; AGENTS.md \"THE NO-CUT RULE\")\n\n\"Every existing feature in the pinned source/package/addon/tool universe has a working production implementation under the same actual source-host prerequisites. No permanent feature cuts, stubs, placeholder shaders, unimplemented codecs/exporters, omitted runtime variants, missing browser/tool integrations, or candidate-specific valid-input refusals remain. A narrowed application profile cannot change this requirement.\" Blocking states: `unclassified`, `unimplemented`, `untested`, `known-regression`, `stub`, `no-op-substitute`, `candidate-refusal-on-valid-source`.\n\n## Technical approach\n\n- Run `f3d verify-upstream --ref r186 --features all --tests all` on the final build; export the manifest status table.\n- Family-by-family review checklist (one row per §5.12 family): owner route recorded for every record; positive test references resolve to executed tests in the final lane logs (23.6); invalid-input/mutation/serialization/lifecycle cases present where applicable; runtime-input tests present for loaders/shaders/nodes/codecs; host prerequisites match the reference's; no `stub`/`no-op-substitute`/`candidate-refusal-on-valid-source` anywhere.\n- Random-sample review: a script opens a random sample of manifest records, loads the referenced test, and asserts it exercises real behavior (not constructor existence or a name match) with the correct owner tag.\n- Deprecated aliases still shipped in r186 present with their warning behavior; removed pre-r186 APIs absent.\n- Write `docs/audits/functional-closure.md` with counts by family and status, open capable-host obligations (explicitly \"obligations, not exclusions\"), and reviewer sign-off; no aggregate percentage.\n- Third evidence class (plan §14.10): a documented upstream limitation, where the pinned feature has no executable positive path even on its specified host prerequisites; the exact conditional implementation/error behavior is preserved and tested, the record is listed under that evidence class explicitly, and it is never counted as positive validation or used to conceal a candidate gap.\n\n## Success criteria\n\n- Zero blocking states; sampling finds no name-only tests; obligations listed; no aggregate percentage anywhere in the document.\n\n## Test plan\n\n- Unit: none new (verifier rules tested in 18a.5); the sampling script has a fixture test.\n- E2E: the verifier run on the final build plus the random-sample review script opening referenced tests and asserting real behavior (not constructor-existence) and owner tags; deprecated-alias presence and removed-API absence checks.\n- Logging: the audit document with counts by family and status, the sample list with verdicts, and the open-obligation list; archived under `evidence/24.1/`.\n- Failure artifacts: the list of records failing the sample review.\n\n## Considerations\n\n- \"A symbol census is not an implementation\" (AGENTS.md); the audit cites executed tests, never the census alone.\n","acceptance_criteria":"The release verifier (18a.5) reports zero blocking states across every manifest family with evidence pointers, and a family-by-family manual review confirms each row of §5.12 has a real production owner and positive behavioral tests; the audit document lists every capable-host obligation still open, states explicitly that an obligation is not an exclusion, and records the separate import/symbol, behavior-family, and exact-integration counts.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:32.463899Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:20.317130Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["compat-manifest","f3d-conformance","phase-5","tier-solid"],"dependencies":[{"issue_id":"f3d-24-final-audit-ne8.1","depends_on_id":"f3d-23-feature-host-performance-validation-b8v.6","type":"blocks","created_at":"2026-09-09T16:49:45.552466Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-24-final-audit-ne8.1","depends_on_id":"f3d-24-final-audit-ne8","type":"parent-child","created_at":"2026-09-09T16:48:32.463899Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-24-final-audit-ne8.2","title":"Accelerated-rendering closure audit (forced lane covers every portable family)","description":"## Full scope\n\nAccelerated-rendering closure audit: every portable standard rendering family passes through the new WebGPU backend with retained renderer submission disabled; every exact/native exception names its concrete observable contract and has a validated exact implementation; a renamed upstream renderer cannot pass\n\n## Summary\n\n\"Both compatibility and new-backend coverage are real.\" This audit is what prevents a compatibility-only wrapper from shipping under this project's name.\n\n## Background (plan §5.16, §14.10, §16.6 \"New-backend ownership\", §22, §3.3)\n\n\"Every portable standard rendering family also passes through the new WebGPU backend with retained renderer submission disabled. Genuine native-backend contracts have separately validated exact implementations. A compatibility-only wrapper cannot pass.\" \"Any intrinsic native-backend exception names the concrete observable contract and its real compatible implementation; it cannot be a catch-all category for unfinished rendering. Exact-component presence alone is not functional completion until its integration tests pass.\"\n\n## Technical approach\n\n- Collect lane-2 (forced-new-backend) results and route attribution counters from 23.6 per family; any case with a nonzero retained-submission count is a failure; assert every portable material/texture/light/shadow/camera/render-target/node/compute/pass family is represented.\n- Exception review: for every manifest record marked exact/native verify (a) the named observable contract (sync readback, `getContext`, native handles, custom backend subclass, GL-backed XR session, nonportable GL program per 15b.3), (b) its passing exact-lane test, (c) that no portable material/pass/node hides under the label.\n- Scored-frame attribution: confirm from 23.2's results package that every scored frame carried `route: new-backend`.\n- Audit script fixture: a synthetic \"renamed upstream renderer\" build (all submissions retained) must be rejected by the same script.\n- Write `docs/audits/accelerated-closure.md` with family counts, the exception table and the verdict; the new-backend rendering coverage count is stated separately from functional coverage.\n\nPrior notes retained:\n- Collect lane-2 results (18a.4) from 23.6: per family, the list of executed cases and the route attribution counters; any case with a nonzero retained-submission count is a failure.\n- Exception review: for every manifest record marked exact/native, verify (a) the named contract (sync readback, `getContext`, native handles, custom backend subclass, GL-backed XR session, nonportable GL program per 15b.3), (b) the passing exact-lane test, (c) that no portable material/pass/node hides under the label.\n- Scored-frame attribution: confirm from the 23.2 results package that every scored frame carried `route: new-backend`.\n- Write `docs/audits/accelerated-closure.md` with family counts, the exception list, and verdict.\n\n## Success criteria\n\n- Zero portable families outside lane 2; every exception justified; scored attribution confirmed.\n\n## Test plan\n\n- Unit: audit-script tests: a synthetic \"renamed upstream renderer\" fixture (all submissions retained) must be rejected; an exception record without a named contract must be rejected.\n- E2E: lane-2 results and attribution counters from 23.6 reviewed per family; exception-by-exception review with passing exact-lane tests; scored-frame attribution confirmed from 23.2's package.\n- Logging: the audit document with family counts, the exception table `{record, contract, exact_test}` and the scored-frame attribution summary; archived under `evidence/24.2/`.\n- Failure artifacts: the list of families/records failing the audit.\n\n## Considerations\n\n- Retained cold helpers (construction/baking) are allowed and charged; only submission ownership matters here (§9.3).\n","acceptance_criteria":"The forced-new-backend lane results from the final runs cover every portable material/texture/light/shadow/camera/render-target/node/compute/pass family with zero retained submissions (attribution counters from 07.6 attached); the exact-exception list is reviewed record by record and each names an observable native contract with its passing exact-lane test; the audit states the new-backend rendering coverage count separately from functional coverage and confirms the H1–H8 scored frames were attributed to the new backend.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:32.595344Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:27.184476Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["exact-backend","f3d-conformance","new-backend","phase-5","tier-solid"],"dependencies":[{"issue_id":"f3d-24-final-audit-ne8.2","depends_on_id":"f3d-23-feature-host-performance-validation-b8v.6","type":"blocks","created_at":"2026-09-09T16:49:45.775842Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-24-final-audit-ne8.2","depends_on_id":"f3d-24-final-audit-ne8","type":"parent-child","created_at":"2026-09-09T16:48:32.595344Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-24-final-audit-ne8.3","title":"Deployment audit (standalone closure, CSP, networked/library modes, pruning, iframe contexts)","description":"## Full scope\n\nDeployment audit: strict standalone closure with no undeclared subresources or isolation prerequisites, every reachable compatibility component and navigation mode included and verified; CSP allowances published; networked/module/library modes verified; small closed artifacts exclude unneeded compatibility code without removing reachable features; iframe/permissions contexts tested\n\n## Summary\n\n\"The product is real.\" The deliverable is a file that works everywhere the plan promised and says so honestly where it cannot.\n\n## Background (plan §10.3, §10.4, §10.6, §16.6 \"Deployment\" and \"Standalone validity\", §18 Phase 5, §22)\n\n\"Verify normal package/networked/library delivery and strict standalone closure, including reachable native modes and runtime codec/shader paths. Verify host/CSP constraints... Confirm small closed artifacts exclude unneeded compatibility code without removing reachable features.\" \"Test deployment in the intended iframe/permissions context as well as a top-level page; isolation and worker permissions are host conditions, not compiler promises.\"\n\n## Technical approach\n\n- Re-run the packaging verification harness (10.5) on every final artifact (corpus, fidelity set, controls, held-out) and archive the results; any needed-but-denied request is a failure.\n- Pruning check: for the small ordinary closed scene, list the embedded components (10.2 composition table) and compare against the closure analysis (10.4): every reachable component (including exact components reachable through UI branches and runtime codecs/translators reachable through inputs) is present; every absent component has a proof of unreachability.\n- Context tests: standalone artifacts inside an iframe with sandbox/permissions policies, as a top-level HTTPS page, and on localhost; document host-gated features (secure context for WebGPU, isolation headers for the isolated profile, permissions for media/XR) per deployment mode.\n- Networked/module/library outputs: 10.3's verifications and the Node import tests on the final build; the isolated profile measured additively only.\n- Publish `docs/audits/deployment.md` with the CSP directive set per mode and the host matrix; confirm no live-service application is labeled offline-equivalent.\n\nPrior notes retained:\n- Re-run 10.5 on the final artifacts for the whole corpus; archive results.\n- Pruning check: for the small ordinary scene control artifact, list embedded components and confirm each is reachable per the closure analysis (10.4) and that every reachable component (including exact components reachable through UI branches) is present.\n- Context tests: iframe with sandbox/permissions policies, top-level HTTPS, localhost; document which features are host-gated (WebGPU secure context, isolation headers for the isolated profile, permissions for media/XR).\n- Networked/library outputs: run the 10.3 verifications and the Node import tests.\n- Publish `docs/audits/deployment.md` with the CSP set per mode and the host matrix.\n\n## Success criteria\n\n- All artifacts pass; pruning check passes; contexts documented; no false offline claims.\n\n## Test plan\n\n- Unit: pruning-check tests on a fixture artifact (a reachable component missing fails; an unreachable one present is flagged).\n- E2E: packaging harness re-run on every final artifact; iframe (sandbox/permissions policies), top-level HTTPS and localhost contexts; networked/library verifications and Node import tests; CSP directive verification per mode.\n- Logging: per-artifact composition tables, request logs with dispositions, context results; archived under `evidence/24.3/`.\n- Failure artifacts: blocked-request logs and composition diffs.\n\n## Considerations\n\n- \"Isolated/shared-memory results are additional and cannot rescue [the portable] launch score\" (§16.6).\n","acceptance_criteria":"For every corpus, fidelity, control and held-out artifact the packaging verification (10.5) results are reviewed and archived; the small closed scene's artifact composition is compared against its dependency closure to show only provably unreachable components were pruned; standalone artifacts are additionally verified inside an iframe with restricted permissions and as a top-level page; networked/module and library outputs pass their verification; the CSP directive set and host requirements per deployment mode are published; no artifact for a live-service application is labeled offline-equivalent.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:32.703351Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:26.662257Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","packaging","phase-5","tier-solid"],"dependencies":[{"issue_id":"f3d-24-final-audit-ne8.3","depends_on_id":"f3d-10-standalone-networked-packaging-jlu.5","type":"blocks","created_at":"2026-09-09T16:49:46.182561Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-24-final-audit-ne8.3","depends_on_id":"f3d-23-feature-host-performance-validation-b8v.4","type":"blocks","created_at":"2026-09-09T16:49:45.989368Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-24-final-audit-ne8.3","depends_on_id":"f3d-24-final-audit-ne8","type":"parent-child","created_at":"2026-09-09T16:48:32.703351Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-24-final-audit-ne8.4","title":"Size, ownership and license audit (245k ceiling, budgets, allowlist, notices)","description":"## Full scope\n\nSize, ownership and license audit: pinned line-count method applied to new Rust (incl. tests and generated engine source), charged Asupersync work, new JS/TS, authored WGSL, generated per-artifact code, retained upstream modules, third-party dependencies; 245k Rust ceiling, 12k JS/TS and 15k WGSL budgets checked; eleven crates; dated nightly; forbid(unsafe_code) boundary; licenses and provenance for every retained/third-party component; no clean-room claims\n\n## Summary\n\n\"The implementation remains focused.\"\n\n## Background (plan §13.5, §17.1, §17.2, §22)\n\n## Technical approach\n\n- Run the pinned line-count script (01.2) over the final tree and produce the category table: new Rust per crate (including tests), charged Asupersync foundation work (from the upstream diff), new JS/TS (adapters, host bindings, harness), authored WGSL, generated engine source, generated per-artifact code (reported per artifact), retained upstream modules, third-party dependencies, tests/assets.\n- Check the ceilings: 245,000 planned Rust (205k subtotal + 10k charged + 30k reserve), ~12,000 JS/TS, ~15,000 WGSL; state the actual numbers and any overage plainly.\n- Verify structural constraints: exactly eleven first-party workspace member crates (cargo-fuzz packages from 05.3 and 15b.5 are test-only packages excluded from workspace members and from the engine count, disclosed in the report; generated per-application kernel packages are build outputs, 12a.5); `rust-toolchain.toml` still a dated nightly; `#![forbid(unsafe_code)]` on every first-party semantic/numerical/compiler/scene/resource crate with the binding boundary named; `cargo tree` against the allowlist (no Bevy/wgpu/ECS/Tokio/Rayon/FFI/BLAS/Python in the execution core); `default-features = false` where recorded.\n- Licenses/provenance: every retained upstream module, external codec/engine Wasm and third-party crate has its notice in the artifact notices section and the provenance table; C/C++-origin Wasm is labeled as inherited, never as Rust; no \"clean-room\" wording anywhere in docs.\n- Write `docs/audits/size-ownership-license.md` with the tables and the verdict.\n\n## Success criteria\n\n- Audit document.\n\n## Test plan\n\n- Unit: line-count script test on a fixture tree; allowlist and unsafe-boundary checks; notices completeness test (every retained/third-party component has a license entry).\n- E2E: the pinned line-count run over the final tree, `cargo tree` audit, and the notices check on every artifact.\n- Logging: the code-count report by category, the dependency tree, the notices index; archived under `evidence/24.4/`.\n- Failure artifacts: the list of components missing notices or the overage table.\n\n## Considerations\n\n- \"A source-inspected implementation is not described as a legally 'clean-room' rewrite merely because it is written in Rust.\"\n","acceptance_criteria":"The code-count report lists every category with numbers and no category missing; the ceiling and budgets are met or the overage is stated; the dependency allowlist and unsafe boundary are verified; every retained/third-party component has its license and provenance recorded in the artifact notices.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:32.807369Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:23.514392Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","phase-5","tier-solid","toolchain"],"dependencies":[{"issue_id":"f3d-24-final-audit-ne8.4","depends_on_id":"f3d-23-feature-host-performance-validation-b8v.6","type":"blocks","created_at":"2026-09-09T16:49:46.399246Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-24-final-audit-ne8.4","depends_on_id":"f3d-24-final-audit-ne8","type":"parent-child","created_at":"2026-09-09T16:48:32.807369Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-24-final-audit-ne8.5","title":"Version-drift reconciliation against the latest stable upstream","description":"## Full scope\n\nVersion-drift reconciliation: state the tested Three.js version explicitly; immutable diff of exports, options/properties, shader chunks, registrations, backends, addons, examples and tests against the latest stable release; new contracts listed as required parity items for a future target; tested vs unverified targets distinguished; no semver inference\n\n## Summary\n\n\"Completeness remains maintainable.\" The oracle does not move silently, and neither does the claim.\n\n## Background (plan §5.17, §2.2, §22 \"Completeness remains maintainable\"; AGENTS.md \"Upstream Pin Discipline\")\n\n\"Support the actual pinned stable release, then track upstream changes through an immutable diff of exports, public options/properties, shader chunks, registrations, render/backends, addons, examples and tests. Do not infer compatibility from a semver/version string or a stale docs page. Check the latest stable release before a new FrankenThreeD release and state its tested Three.js version explicitly. Preserve prior completed version targets while an updated target closes its new/changed contracts. New functionality remains a required parity item, not an automatic exception.\"\n\n## Technical approach\n\n- Take 23.6's drift diff; categorize entries (added export; changed signature/option/default; removed/deprecated; changed shader chunk; changed registration; changed backend/addon; new example/test) and map each to a §5.12 family.\n- Produce `compatibility/targets/<latest>/parity-items.md` listing every new/changed contract as a required parity item for a future explicit target (a new target decision is the user's; no beads are created here).\n- Release documentation: state r186 (commit `148ef33ecb6d2502ff796d4554abd1549c95d519`) as the tested version; label any newer version as unverified with its retained-component status; no semver inference anywhere.\n- Immutability: hash manifest of `compatibility/three-r186.json`, benchmark results and evidence directories compared with 23.6's snapshot; any change fails the audit.\n\nPrior notes retained:\n- Take the drift diff from 23.6; categorize entries (added export, changed signature/option/default, removed/deprecated, changed shader chunk, changed registration, new example/test); map each to a §5.12 family.\n- Produce the future-target work list (`compatibility/targets/<latest>/parity-items.md`) with counts by family; no beads are created for it in this release (that is a new target decision for the user).\n- Update README/status text to name the tested version and the unverified status of anything newer.\n- Verify immutability: hash manifest of `compatibility/three-r186.json`, benchmark results and evidence directories unchanged since 23.6.\n\n## Success criteria\n\n- Drift report published; parity list produced; version statement correct; hashes unchanged.\n\n## Test plan\n\n- Unit: drift categorization tests on a synthetic diff; determinism test (re-run yields the same diff); hash check of r186 artifacts.\n- E2E: the drift report and parity list generated from 23.6's diff; README/status text validated to name r186 and label newer versions unverified.\n- Logging: the drift report and parity list; hash manifest comparison; archived under `evidence/24.5/`.\n- Failure artifacts: the hash mismatch list if any r186 artifact changed.\n\n## Considerations\n\n- \"A newer unverified release may be used only with its actual retained component and truthful unverified-version status, never as an already-certified accelerated version.\"\n","acceptance_criteria":"The drift report produced in 23.6 is reviewed and published with a categorized list of new/changed/removed contracts between r186 and the latest stable release; each new contract is recorded as a required parity item for a future explicit target (not as an exception); the release documentation names r186 (commit 148ef33ecb6d2502ff796d4554abd1549c95d519) as the tested version and labels any newer version as unverified with its retained-component status; all r186 results remain immutable.","status":"open","priority":3,"issue_type":"task","created_at":"2026-09-09T16:48:32.923150Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:27.360875Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["compat-manifest","phase-5","tier-solid","upstream-pin"],"dependencies":[{"issue_id":"f3d-24-final-audit-ne8.5","depends_on_id":"f3d-24-final-audit-ne8","type":"parent-child","created_at":"2026-09-09T16:48:32.923150Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-24-final-audit-ne8.5","depends_on_id":"f3d-24-final-audit-ne8.1","type":"blocks","created_at":"2026-09-09T16:49:46.618684Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-24-final-audit-ne8.6","title":"Final report and kill gate 5 decision (Definition of done)","description":"## Full scope\n\nFinal report and kill gate 5 decision: pinned version, all inventory counts, new-backend vs exact ownership, completed and unavailable-host evidence, remaining known defects, benchmark results with intervals and raw runs, separately; no plan/static inspection/name census labeled as implemented compatibility; explicit PASS/FAIL against every §22 clause\n\n## Summary\n\nThe last honest document.\n\n## Background (plan §14.10 last paragraph, §16.7 \"Final gate\", §22)\n\n\"The final report states the pinned version, all inventory counts, new-backend versus exact ownership, completed and unavailable-host evidence, remaining known defects, and benchmark results separately. It must not label a plan, static source inspection, or symbol-name census as implemented '100% compatibility.'\"\n\n## Technical approach\n\n- Assemble from the audits (24.1–24.5) and the validation outputs (23.x): for each §22 clause (full functionality real; product real; compatibility and new-backend coverage real; performance real; foundation real; implementation focused; completeness maintainable) a PASS/FAIL with evidence links (run ids, audit documents, results packages).\n- Report contents (§14.10 last paragraph): pinned version; all inventory counts separately; new-backend vs exact ownership; completed and unavailable-host evidence; remaining known defects; benchmark results with intervals and raw-run references; Solid/Frontier/Optional tiers distinguishable.\n- Write `docs/gates/gate-5-final.md` and the published report; update the README status section to state exactly what holds; on any FAIL the release is not labeled a performance replacement or full compatibility.\n\n## Success criteria\n\n- Report published; decision recorded.\n\n## Test plan\n\n- Unit: report assembly test (every §22 clause present with PASS/FAIL and an evidence link; no aggregate percentage; tiers distinguishable).\n- E2E: none; the report is reviewed with the user.\n- Logging: the final report and `docs/gates/gate-5-final.md`; archived under `evidence/24.6/`.\n- Failure artifacts: n/a.\n\n## Considerations\n\n- Solid/frontier/optional tiers remain distinguishable in the report (§18).\n","acceptance_criteria":"docs/gates/gate-5-final.md and the published report exist with every §22 clause marked PASS or FAIL with evidence links; the README status section is updated to reflect exactly what holds; on FAIL the release is not labeled a performance replacement.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-09T16:48:33.024969Z","created_by":"jemanuel","updated_at":"2026-09-09T17:23:27.531418Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["f3d-conformance","kill-gate","phase-5","tier-solid"],"dependencies":[{"issue_id":"f3d-24-final-audit-ne8.6","depends_on_id":"f3d-24-final-audit-ne8","type":"parent-child","created_at":"2026-09-09T16:48:33.024969Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-24-final-audit-ne8.6","depends_on_id":"f3d-24-final-audit-ne8.1","type":"blocks","created_at":"2026-09-09T16:49:46.830795Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-24-final-audit-ne8.6","depends_on_id":"f3d-24-final-audit-ne8.2","type":"blocks","created_at":"2026-09-09T16:49:47.046683Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-24-final-audit-ne8.6","depends_on_id":"f3d-24-final-audit-ne8.3","type":"blocks","created_at":"2026-09-09T16:49:47.257375Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-24-final-audit-ne8.6","depends_on_id":"f3d-24-final-audit-ne8.4","type":"blocks","created_at":"2026-09-09T16:49:47.476478Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-24-final-audit-ne8.6","depends_on_id":"f3d-24-final-audit-ne8.5","type":"blocks","created_at":"2026-09-09T16:49:47.690845Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-gate-1-foundation-tzn","title":"KILL GATE 1 (Foundation): real browser task execution and cleanup through Asupersync plus a legal WebGPU bridge on Safari and Chrome","description":"## Summary\n\nThe first of five kill gates. It exists so the project can stop before any rendering engine is built.\n\n## Background (plan §16.7 \"Foundation gate\", §2.3, §11.2, §21; AGENTS.md \"Early Kill Gates\")\n\n\"Foundation gate: actual browser task execution/cleanup and a legal WebGPU bridge work on Safari and Chrome. No fake ledger-only execution or native-thread dependency.\" \"Passing a `cargo check` for Wasm, or closing an ownership ledger, is not equivalent to proving that browser tasks actually executed and drained.\"\n\n## What the decision must cite\n\n- 02.2: polled futures, timer and host-turn wakeups, nonreentrant pump, bounded microtask bursts (Safari + Chrome logs).\n- 02.3: cancellation delivered, fetch cancelled, drain before teardown, no late publication, unsupported-host behavior.\n- 05.6: first correct frame through the bulk bridge, pixel-identical to direct JS WebGPU, negotiated device profile, serialized error scopes.\n- 05.7: the seven counterexamples passing with mutation checks.\n- Budget line for charged Asupersync work (02.4) vs the 10,000-line allocation.\n\n## Procedure\n\nWrite `docs/gates/gate-1-foundation.md` with the evidence table and verdict. If any item is missing on either browser, the verdict is FAIL and the response per §20 is \"Complete the upstream host; no alternate runtime hidden in F3D\". This gate blocks F3D-06 and F3D-07 (no engine before the foundation is proven).\n\n## Verification and logging\n\n- The decision cites, by run id, the archived evidence directories of 02.2, 02.3, 05.6 and 05.7 on installed Safari (M5 and iPhone) and Chrome (M5); each cited run's `summary.json` must show every probe passing and the `events.jsonl` must contain the lifecycle events named in those beads.\n- A checklist in the decision document maps each foundation requirement to the exact test name and run id; a missing browser or a probe with a `warn`/`error` event is a FAIL.\n- Logging: the decision document itself plus a `metric` summary of the charged Asupersync line count vs the allocation; archived under `evidence/G1/`.\n- Each cited run's capability record must show a non-fallback adapter and the pinned browser/OS build from 03.7; a run on a software/fallback adapter or an unpinned build is not evidence.\n\n## No-claim boundary\n\nPassing says nothing about performance.\n","acceptance_criteria":"A decision document records executable evidence (archived logs from installed Safari and Chrome on physical devices) that Rust futures are polled, woken, cancelled and drained through Asupersync with no native-thread dependency and no ledger-only execution, and that the production bulk bridge renders correct frames and passes the first counterexample suite; verdict PASS or FAIL; on FAIL development stops pending the user's decision.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:24.750298Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:15.892378Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["asupersync-upstream","f3d-runtime","kill-gate","phase-0"],"dependencies":[{"issue_id":"f3d-gate-1-foundation-tzn","depends_on_id":"f3d-02-asupersync-browser-execution-58j.2","type":"blocks","created_at":"2026-09-09T16:49:48.133149Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-gate-1-foundation-tzn","depends_on_id":"f3d-02-asupersync-browser-execution-58j.3","type":"blocks","created_at":"2026-09-09T16:49:48.326321Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-gate-1-foundation-tzn","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","type":"blocks","created_at":"2026-09-09T16:49:48.519070Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-gate-1-foundation-tzn","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.7","type":"blocks","created_at":"2026-09-09T16:49:48.712527Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-gate-2-bridge-apz","title":"KILL GATE 2 (Bridge): the selected Wasm/host bridge shows an advantage opportunity after its own overhead on matched workloads, given the reference critical-path decomposition","description":"## Summary\n\nThe second kill gate: does the architecture's boundary cost leave room for the thesis?\n\n## Background (plan §16.7 \"Bridge gate\", §1.2, §20 row 2)\n\n\"Measure direct JavaScript WebGPU, simple Wasm-to-host calls, bulk command transfer, and generated host submission for matched workloads. The selected path must demonstrate an advantage opportunity after its own overhead; no fixed numeric win is assumed before measurement.\" Response on failure: \"Change granularity/ownership; abandon per-method Wasm wrapping.\"\n\n## What the decision must cite\n\n- 03.5 tables (throughput, bridge CPU time, copies, calls per variant per browser).\n- 03.4 decompositions (p and implied best case per H1/H2/H7/H8).\n- 05.6 production-bridge measurements confirming the prototype numbers hold.\n- The arithmetic: `speedup = 1 / ((1 - p) + p/k + h)` evaluated with measured h and plausible k per case.\n- The overhead term: `h` = (per-frame bridge cost of the selected variant on the H1-shaped workload, measured in 03.5) / (the selected reference's frame time for that case from 03.4), computed per case and stratum; 03.5's \"overhead relative to direct JS\" is an input to that ratio, not `h` itself.\n\n## Procedure\n\nWrite `docs/gates/gate-2-bridge.md`. If no variant leaves room on the phone and M5 strata for H1 and H2, the verdict is FAIL; the user decides whether to change ownership granularity or stop. This gate blocks F3D-07 and F3D-08.\n\n## Verification and logging\n\n- The decision cites 03.5's per-browser/per-workload tables, 03.4's decompositions and 05.6's production-bridge measurements by run id, and reproduces the `speedup = 1 / ((1 - p) + p/k + h)` evaluation per case and stratum with the measured `h` and plausible `k`.\n- A table lists the selected variant per workload class with its overhead and the resulting room for the 2x/3x targets; any case without room is marked and the verdict reflects it.\n- Logging: the decision document plus the computed table as JSON; archived under `evidence/G2/`.\n\n## No-claim boundary\n\nAn \"advantage opportunity\" is a structural estimate, not a result.\n","acceptance_criteria":"A decision document combines the bridge measurements (four variants, per browser, per workload class) with the reference decompositions (removable fraction p per headline case) and states, per case and stratum, whether any bridge variant leaves room for the required 2x/3x after its overhead h; verdict PASS or FAIL with the selected variant per workload class; no fixed numeric win was assumed.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:24.832037Z","created_by":"jemanuel","updated_at":"2026-09-09T17:41:10.792416Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","js-host","kill-gate","phase-0"],"dependencies":[{"issue_id":"f3d-gate-2-bridge-apz","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.4","type":"blocks","created_at":"2026-09-09T16:49:49.096036Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-gate-2-bridge-apz","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.5","type":"blocks","created_at":"2026-09-09T16:49:48.901305Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-gate-2-bridge-apz","depends_on_id":"f3d-05-ids-layouts-epochs-transport-vqa.6","type":"blocks","created_at":"2026-09-09T16:49:49.287042Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-gate-4-generalization-5hm","title":"KILL GATE 4 (Generalization): repeat the result on held-out applications and at least one discrete-GPU stratum, with measured end-to-end routes for H7 and H8","description":"## Summary\n\nThe fourth kill gate: is the first result general, or a two-app trick?\n\n## Background (plan §16.7 \"Generalization gate\", §14.8, §18 Phase 2 exit)\n\n\"Repeat the result on held-out applications and at least one discrete-GPU stratum, with a measured end-to-end route for H7/H8. Profile H7/H8 references during Phase 0 and test promising equivalent kernel/data-path changes early... Microbenchmark improvements alone cannot pass.\"\n\n## What the decision must cite\n\n- 12b.4 held-out corpus results (functional).\n- 13.8, 17.6 (H3–H6) and 16.5/16.6/20.4 (H7/H8) measured routes.\n- A discrete-GPU stratum run (03.7 provisioning) for H1/H2 and the held-out set, using the frozen protocol and analysis (03.6/03.8).\n\n## Procedure\n\nWrite `docs/gates/gate-4-generalization.md`. If H7/H8 remain below 2× after the profile-justified improvements, record it; the plan's response is \"find an equivalent algorithm/data-path improvement or fail launch gate\" and the decision is the user's. This gate blocks F3D-23's scored campaign.\nThis gate itself runs `f3d bench` under protocol v1 for H1/H2 and the held-out set on the provisioned discrete-GPU stratum (03.7) with the frozen references (11.4 and 13.8/17.6 cover only the phone and M5); if no discrete-GPU stratum is provisioned the gate cannot pass and records the obligation.\n\n## Verification and logging\n\n- The decision cites 12b.4's held-out verify results, the measured routes of 13.8/17.6/16.5/16.6/20.4, and at least one discrete-GPU stratum's paired runs by run id; microbenchmark-only evidence is rejected by the checklist.\n- The verdict is computed by the analysis script (03.8) over the cited runs; per-cell and per-stratum rows are reproduced in the document.\n- Logging: the decision document plus the analysis outputs; archived under `evidence/G4/`.\n\n## No-claim boundary\n\nPassing here is still not the launch contract (§16.6).\n","acceptance_criteria":"A decision document records: held-out applications transformed by the same compiler passing equivalence with measured paired results; at least one discrete-GPU stratum (NVIDIA or AMD, Chrome/Windows) with paired results for H1/H2 and the held-out set; H7 and H8 end-to-end routes with measured results (whatever they are); verdict PASS or FAIL against the unchanged 2x floor logic; microbenchmark-only evidence is rejected.","status":"open","priority":0,"issue_type":"task","created_at":"2026-09-09T16:46:24.916160Z","created_by":"jemanuel","updated_at":"2026-09-09T17:44:22.168101Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["benchmark","h7","h8","kill-gate","phase-2","phase-3","phase-4"],"dependencies":[{"issue_id":"f3d-gate-4-generalization-5hm","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.7","type":"blocks","created_at":"2026-09-09T16:49:50.532021Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-gate-4-generalization-5hm","depends_on_id":"f3d-03-reference-profiles-and-bridge-tmt.8","type":"blocks","created_at":"2026-09-09T17:41:13.505271Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-gate-4-generalization-5hm","depends_on_id":"f3d-12b-generalize-islands-ownership-a3s.4","type":"blocks","created_at":"2026-09-09T16:49:49.495220Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-gate-4-generalization-5hm","depends_on_id":"f3d-13-animation-math-geometry-closure-3cr.8","type":"blocks","created_at":"2026-09-09T16:49:49.706084Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-gate-4-generalization-5hm","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.5","type":"blocks","created_at":"2026-09-09T16:49:49.915963Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-gate-4-generalization-5hm","depends_on_id":"f3d-16-tsl-nodes-compute-contracts-f0v.6","type":"blocks","created_at":"2026-09-09T16:49:50.127868Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-gate-4-generalization-5hm","depends_on_id":"f3d-17-passes-effects-histories-readbacks-0gf.6","type":"blocks","created_at":"2026-09-09T16:49:50.745598Z","created_by":"jemanuel","metadata":"{}","thread_id":""},{"issue_id":"f3d-gate-4-generalization-5hm","depends_on_id":"f3d-20-profile-justified-gpu-improvements-023.4","type":"blocks","created_at":"2026-09-09T16:49:50.340720Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
{"id":"f3d-optional-research-parked-316","title":"PARKED: Optional research tier must not start before the core gates pass","description":"## Full scope\n\nPARKED: Optional research tier (geometric algebra kernels, neural rendering, differentiable scene optimization, novel ray tracing, general JavaScript compilation) must not start before the core gates pass\n\n## Summary\n\nThe plan's optional research tier, recorded so it is not forgotten and so nobody starts it early.\n\n## Background (plan §18 \"Solid, frontier, and optional research\", §7.2, §20 \"Scope boundaries\"; AGENTS.md \"Implementation Sequence\")\n\n\"Optional research: generic geometric-algebra optimization, neural rendering, differentiable scene optimization, novel ray tracing, and more general JavaScript compilation. None is needed to declare the primary product complete, and none should consume the compatibility reserve before the core gates pass.\" \"Use geometric-algebra or exterior-algebra kernels only when a specific operation becomes demonstrably simpler, more robust, or faster, and retain the conventional implementation as a differential oracle.\"\n\n## Rules\n\n- Deferred until F3D-24.6 passes.\n- Never consumes the 30,000-line reserve.\n- Must remain distinguishable from Solid and Frontier work in docs, feature gates and status reports.\n\n## No-claim boundary\n\nNothing in this tier is part of the product promise.\n","acceptance_criteria":"This bead is a placeholder and stays deferred; it may be undeferred only after kill gate 5 records PASS, and any research item must then be split into its own beads with a differential oracle against the conventional implementation.","status":"deferred","priority":4,"issue_type":"task","created_at":"2026-09-09T16:46:25.007659Z","created_by":"jemanuel","updated_at":"2026-09-09T16:49:51.001568Z","source_repo":"franken_threed","source_repo_path":"/Users/jemanuel/projects/franken_threed","compaction_level":0,"original_size":0,"labels":["optional-research","phase-5"],"dependencies":[{"issue_id":"f3d-optional-research-parked-316","depends_on_id":"f3d-24-final-audit-ne8.6","type":"blocks","created_at":"2026-09-09T16:49:51.001265Z","created_by":"jemanuel","metadata":"{}","thread_id":""}]}
